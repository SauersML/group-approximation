import GroupApproximation.GGT.HullSCRelatorSeparation2ExactPublished

/-!
# Relative-side relator separation, composed

The active composition is `HullSC.exists_hullRelatorWord₂OfBaseLetterPublished_exact`.
It selects one exact exponent design after fixing the published-gap constants,
uses the relative-side four-way closer for the long-prefix conclusion, and
packages the ordinary, published, and prime-piece bounds consumed by Hull's
Theorem 5.1.

The former generic producer interface supplied same-side exclusions only for
short sides consisting of base letters.  Published pieces measure their short
sides in the full relative alphabet, so that interface does not state the
geometric input needed by its conclusion.  The exact composition imported
above supplies the adjacent clean matches and bounded gaps for arbitrary
relative short sides.
-/
