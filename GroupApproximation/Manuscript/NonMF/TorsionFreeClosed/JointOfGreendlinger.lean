import GroupApproximation.Manuscript.NonMF.TorsionFreeJoint.EndpointOfGreendlinger
import GroupApproximation.Manuscript.NonMF.TorsionFreeClosed.JointIntro
import GroupApproximation.Meta.AxiomGuard

/-!
# `thm:torsion-free` with `cor:regular-nonmf-algebra` for the same `Q`, closing wire

`non_mf_groups_exist.tex`, `thm:torsion-free` (tex 297) and `cor:regular-nonmf-algebra`
(tex 2204):

> The algebra `C*_r(Q)` is separable, unital, simple, and generated by two unitaries.  It has
> a unique tracial state and stable rank one, and it is stably finite but not MF.

about the group `Q` of the theorem, and the introduction's sentence (tex 303):

> Its reduced C⋆-algebra is simple, has a unique tracial state and stable rank one, and is
> not MF.

## Route

Lane nm-torsionfree-07.  Lane nm-torsionfree-06 states the joint predicate
`TorsionFreeJoint.PrintedTorsionFreeTheoremWithRegularAlgebra` and proves it over the
least-area Greendlinger leaf
(`TorsionFreeJoint.printedTorsionFreeTheoremWithRegularAlgebra_of_greendlinger`).  The endpoints
below apply that reduction, and for the introduction's sentence the unconditional weakening
`introReducedAlgebraNotMF_of_joint`.  Their single binder is
`GGT.VanKampen.RelativeGreendlingerQuasiGeodesicLeastAreaStatement.{0, 0, 0}`, whose closer
`GreendlingerLeaf.relativeGreendlingerQuasiGeodesicLeastArea` (in
`GreendlingerLeaf/Assembly.lean`) is not importable yet; once it is, each endpoint closes by
applying it.
-/

namespace GroupApproximation
namespace Manuscript
namespace NonMF
namespace TorsionFreeClosed

/-- **`thm:torsion-free` and `cor:regular-nonmf-algebra`, for the same `Q`** (tex 297 and
2204), from the least-area Greendlinger leaf. -/
theorem manuscriptSentence_torsionFreeWithRegularAlgebra_of_greendlinger
    (hgreendlinger :
      GGT.VanKampen.RelativeGreendlingerQuasiGeodesicLeastAreaStatement.{0, 0, 0}) :
    TorsionFreeJoint.PrintedTorsionFreeTheoremWithRegularAlgebra :=
  TorsionFreeJoint.printedTorsionFreeTheoremWithRegularAlgebra_of_greendlinger hgreendlinger

#audit_axioms
  GroupApproximation.Manuscript.NonMF.TorsionFreeClosed.manuscriptSentence_torsionFreeWithRegularAlgebra_of_greendlinger

/-- **"Its reduced C⋆-algebra is simple, has a unique tracial state and stable rank one, and is
not MF"** (tex 303), obtained as a weakening of the joint endpoint. -/
theorem manuscriptSentence_introReducedAlgebraNotMF_ofJoint_of_greendlinger
    (hgreendlinger :
      GGT.VanKampen.RelativeGreendlingerQuasiGeodesicLeastAreaStatement.{0, 0, 0}) :
    IntroReducedAlgebraNotMF :=
  introReducedAlgebraNotMF_of_joint
    (manuscriptSentence_torsionFreeWithRegularAlgebra_of_greendlinger hgreendlinger)

#audit_axioms
  GroupApproximation.Manuscript.NonMF.TorsionFreeClosed.manuscriptSentence_introReducedAlgebraNotMF_ofJoint_of_greendlinger

end TorsionFreeClosed
end NonMF
end Manuscript
end GroupApproximation
