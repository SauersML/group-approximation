import GroupApproximation.Analysis.MFAlgebraMatrixAmplification
import GroupApproximation.Analysis.ReducedGroupCStarMFObstruction
import GroupApproximation.Analysis.UniversalCStarAmalgam
import GroupApproximation.Manuscript.MFRecognition.HNNPermanenceSetupUniversal

/-!
# The assumed inputs of `thm:hnn-permanence`

Every hypothesis the HNN permanence proof rests on is named here, as a
proposition, and bundled as `HNNInputs`.

This module deliberately closes over *landed* modules only.  The mechanics of
Steps 1 and 2 — the amalgam `P`, Ueda's corner `ePe`, the matrix units — are
being reorganized elsewhere, and nothing in the critical path
(`HNNPermanenceTraceBridge`, `HNNPermanence`) should have to move when they do.
The output of those two steps enters here as one proposition,
`UniversalHNNIsMFStatement`, and `HNNPermanenceShulman` discharges it from the
two citations along the printed route: Ueda's Proposition 2.4 puts `U` inside
the full amalgam, and Shulman's Theorem 16 makes that amalgam MF.
-/

namespace GroupApproximation
namespace Manuscript
namespace MFRecognition
namespace HNNPermanence

open GroupApproximation.Manuscript.OneSidedMFRadical.HNNCoronaConjugatorSentenceAudit
open ReducedGroupCStarTrace

noncomputable section

/-! ## Shulman's amalgamated-free-product criterion -/

/-- Printed: *"the full amalgamated free product `A₁ *_C A₂` of separable
`C*`-algebras is MF as soon as there are injective `*`-homomorphisms `φ_A` and
`φ_B` of `A₁` and `A₂` into one norm matrix corona with
`φ_A ∘ ι_A = φ_B ∘ ι_B`."*  This is **Shulman, Theorem 16**, named as a
proposition so that it is visible in the type of every consumer.

As everywhere in this development the corona embeddings are allowed to be
non-unital, matching the repository's `HasMFEmbedding`. -/
def ShulmanTheorem16Statement : Prop :=
  ∀ {C A₁ A₂ : Type} [CStarAlgebra C] [CStarAlgebra A₁]
    [CStarAlgebra A₂] (iA : C →⋆ₐ[ℂ] A₁) (iB : C →⋆ₐ[ℂ] A₂)
    [Nonempty (CStarAmalgamRepresentation iA iB)],
      TopologicalSpace.SeparableSpace C →
      TopologicalSpace.SeparableSpace A₁ →
      TopologicalSpace.SeparableSpace A₂ →
      ∀ (Y : ℕ → FiniteModel) [∀ n, Nonempty (Y n)]
        (phiA : A₁ →⋆ₙₐ[ℂ] NormMatrixCStarCorona (fun n ↦ Y n))
        (phiB : A₂ →⋆ₙₐ[ℂ] NormMatrixCStarCorona (fun n ↦ Y n)),
        Function.Injective phiA → Function.Injective phiB →
          (∀ c : C, phiA (iA c) = phiB (iB c)) →
            IsMFAlgebra (UniversalCStarAmalgam iA iB)

/-! ## The printed steps of Step 3 that are still open

Each is a *named* proposition, so a consumer's type shows exactly which printed
sentence it rests on.  None of them is a citation: the manuscript proves all
four, and the repository does not yet carry their analytic content. -/

/-- Printed: *"Thus there is a `*`-homomorphism `π : D → C*_r(G)` with
`π(ιρ(g)) = λ_G(g)`."*  The two sentences that feed it are already proved in
the repository, as `regularCharacterGNS_orbit_orthonormal` and
`regularCharacterGNS_left_translation`.  Still open: the compression of the
GNS representation of `D` for `T` to the invariant closed span of the orbit
vectors, and the identification of that compression with `λ_G`. -/
def ReducedFactorisationStatement : Prop :=
  ∀ {G : Type} [Group G] {S T : Subgroup G} {phi : S ≃* T}
    {A : Type} [CStarAlgebra A] {X : ℕ → FiniteModel} [∀ n, Nonempty (X n)]
    (data : CoronaConjugator G S T phi A X),
      ∃ pi : baseAlgebra data →⋆ₐ[ℂ] ReducedGroupCStar G,
        ∀ g : G,
          pi (((baseUnitaryHom data g : unitary (baseAlgebra data)) :
            baseAlgebra data)) = reducedLeftRegular G g

/-- Printed: *"Since `G ≤ R`, the space `ℓ²(R)` is a direct sum of copies of
`ℓ²(G)` indexed by the right cosets of `G`, so `λ_R|_G` is a multiple of `λ_G`
and the assignment `λ_G(g) ↦ λ_R(g)` extends to a `*`-homomorphism
`C*_r(G) → C*_r(R)`."*  Still open. -/
def ReducedSubgroupHomStatement : Prop :=
  ∀ (H : Type) [Group H] (S₀ T₀ : Subgroup H) (psi : S₀ ≃* T₀),
    ∃ q : ReducedGroupCStar H →⋆ₐ[ℂ]
        ReducedGroupCStar (HNNExtension H S₀ T₀ psi),
      ∀ g : H,
        q (reducedLeftRegular H g) =
          reducedLeftRegular (HNNExtension H S₀ T₀ psi) (HNNExtension.of g)

/-- Printed: *"two `*`-homomorphisms that agree on generators agree on
`B₀`"*.  `B₀ = C*(ιρ(S))` is the closure of the star algebra generated by the
`ιρ(s)` and star-algebra homomorphisms of C-star algebras are contractive, so
this is density.  Still open. -/
def EdgeDensityStatement : Prop :=
  ∀ {G : Type} [Group G] {S T : Subgroup G} {phi : S ≃* T}
    {A : Type} [CStarAlgebra A] {X : ℕ → FiniteModel} [∀ n, Nonempty (X n)]
    (data : CoronaConjugator G S T phi A X) {E : Type} [CStarAlgebra E]
    (f g : sourceEdgeAlgebra data →⋆ₐ[ℂ] E),
      (∀ s : S,
          f (((sourceGenerator data s : unitary (sourceEdgeAlgebra data)) :
              sourceEdgeAlgebra data)) =
            g (((sourceGenerator data s :
              unitary (sourceEdgeAlgebra data)) : sourceEdgeAlgebra data))) →
        f = g

/-- The printed group `R` of `eq:hnn` is countable, so `A' = C*(j(R))` is
separable.  Mathlib builds `HNNExtension H S₀ T₀ psi` as a quotient of the free
product of `H` with a copy of `ℤ`.  Not a printed sentence; still open. -/
def HNNCountableStatement : Prop :=
  ∀ (H : Type) [Group H] [Countable H] (S₀ T₀ : Subgroup H)
    (psi : S₀ ≃* T₀), Countable (HNNExtension H S₀ T₀ psi)

/-- A tracial MF realization in any universe gives one in `Type`.  Not a
statement of the manuscript: the repository's `IsRegularlyRealized` pins the
realization algebra to `Type`, while the universal `C*`-HNN algebra of Step 1
lives one universe higher.  Still open. -/
def RegularRealizationDescentStatement : Prop :=
  ∀ {R : Type} [Group R] [Countable R] {B : Type 1} [CStarAlgebra B],
    RegularRealizationData R B → IsRegularlyRealized R

/-- A unital separable MF C-star algebra has a *unital* faithful corona
embedding.  Needed by the printed proof of `cor:central-hnn`, which takes *"`ι`
any injective `*`-homomorphism of `A` into a norm matrix corona"*, while
`HasMFEmbedding` supplies a possibly non-unital one.  Still open. -/
def UnitalCoronaEmbeddingStatement : Prop :=
  ∀ {A : Type} [CStarAlgebra A], IsMFAlgebra A →
    ∃ (X : ℕ → FiniteModel) (hX : ∀ n, Nonempty (X n)),
      letI : ∀ n, Nonempty (X n) := hX
      ∃ iota : A →⋆ₐ[ℂ] NormMatrixCStarCorona (fun n ↦ X n),
        Function.Injective iota

/-- **The conclusion of Steps 1 and 2**: the universal `C*`-HNN algebra `U` is
MF.

This is not a citation.  It is what the printed Step 1 and Step 2 establish
together — `U` embeds in the full amalgamated free product `P = A₁ *_C A₂` by
Ueda's Proposition 2.4, and `P`, hence its corner `ePe ≅ U`, is MF by Shulman's
Theorem 16 — and `HNNPermanenceShulman.universalHNNIsMFStatement_of_citations`
proves exactly this proposition from those two cited results.  It is stated
separately so that Step 3 and the manuscript theorems depend on the *conclusion*
of Steps 1–2 rather than on the particular modules that construct `P`. -/
def UniversalHNNIsMFStatement : Prop :=
  ∀ {G : Type} [Group G] [Countable G] {S T : Subgroup G} {phi : S ≃* T}
    {A : Type} [CStarAlgebra A] {X : ℕ → FiniteModel} [∀ n, Nonempty (X n)]
    (data : CoronaConjugator G S T phi A X),
      IsMFAlgebra (universalHNN data)

/-- **Everything the HNN permanence proof assumes**, bundled as one hypothesis
in the style of `NonMF.TorsionFree.HullInputs`.

A term of this structure is never constructed here, so every downstream
statement carries `(hIn : HNNInputs)` and the assumptions travel with the
conclusions. -/
structure HNNInputs : Type where
  /-- Steps 1 and 2: `U` is MF.  Discharged from Ueda, Proposition 2.4 and
  Shulman, Theorem 16 by `universalHNNIsMFStatement_of_citations`. -/
  universalHNNIsMF : UniversalHNNIsMFStatement
  /-- The printed factorization `π : D → C*_r(G)` of Step 3. -/
  reducedFactorisation : ReducedFactorisationStatement
  /-- The printed extension `C*_r(G) → C*_r(R)` of Step 3. -/
  reducedSubgroupHom : ReducedSubgroupHomStatement
  /-- The printed density sentence of Step 3. -/
  edgeDensity : EdgeDensityStatement
  /-- Countability of the HNN extension. -/
  hnnCountable : HNNCountableStatement
  /-- Universe descent to the repository's `IsRegularlyRealized`. -/
  regularRealizationDescent : RegularRealizationDescentStatement
  /-- The unital corona embedding used by `cor:central-hnn`. -/
  unitalCoronaEmbedding : UnitalCoronaEmbeddingStatement

end

end HNNPermanence
end MFRecognition
end Manuscript
end GroupApproximation
