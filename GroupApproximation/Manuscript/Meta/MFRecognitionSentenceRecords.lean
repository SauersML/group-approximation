import GroupApproximation.Manuscript.Meta.SentenceRecords

/-!
# `mf_recognition_complexity.tex`: the sentences that are records, not claims

GENERATED by `scripts/sentence_records.py` -- do not edit.  The
assignments live in the sentence map; this file is their projection
onto the three statuses that name no declaration, and
`scripts/sentence_records.py --check` fails if the two disagree in
either direction.

`SentenceRecord` carries no `Prop` and no proof.  A row here records
that the manuscript credits someone, poses an open question, or
discloses how the work was produced; it asserts nothing, which is
exactly why the sentence has no theorem.
-/

namespace GroupApproximation
namespace Manuscript
namespace Meta

/-- The `attribution`, `open` and `provenance` sentences of
`mf_recognition_complexity.tex`, as data. -/
def mfRecognitionSentenceRecords : List SentenceRecord := [
  { key := "7fa82cf4cbed"
    status := SentenceStatus.attribution
    tex := "Not every countable group is MF~\\cite{NonMF}: the group $H=\\EL_{12}(L_{\\F_2}(1,2))$ is finitely generated and simple, has property~\\textup{(T)}, and every homomorphism from $H$ to an MF group is trivial, and there is a finitely presented torsion-free group $E$ with the same last property~\\cite[Theorem~C]{NonMF}."
    note := "Credits [NonMF] Theorem B (H) and Theorem C (E)." },
  { key := "7c0a4164b6dc"
    status := SentenceStatus.attribution
    tex := "A classical $\\Pi^0_2$-complete recognition problem for finitely presented groups is torsion-freeness, by Lempp~\\cite{Lempp}."
    note := "Credits Lempp with the classical Pi^0_2-completeness of torsion-freeness recognition, cited as a comparison point." },
  { key := "ab2fb29c5244"
    status := SentenceStatus.attribution
    tex := "Decision problems for finitely presented groups go back to Dehn, and the theorem of Adian and Rabin~\\cite{Adian,Rabin} shows that no Markov property (one that some finitely presented group has and that no group containing some fixed finitely presented group has) can be recognized from a finite presentation."
    note := "Historical background: decision problems for finitely presented groups go back to Dehn; credits the Adian-Rabin theorem (no Markov property is recognizable)." },
  { key := "edd1a38d47cb"
    status := SentenceStatus.attribution
    tex := "Once a finitely presented non-MF group exists, the MF property is Markov and so undecidable; Theorem~\\ref{thm:recognition} determines the exact level."
    note := "Notes that MF is a Markov property, so Adian-Rabin already gives bare undecidability; contrasts with thm:recognition's sharper exact-level result. Comparative background, not a load-bearing derivation in this paper's own proof." },
  { key := "2493b38aeb2d"
    status := SentenceStatus.provenance
    tex := "The certificate checker of Section~\\ref{sec:local} and the constructions of Section~\\ref{sec:compiler} are formalized in Lean~4~\\cite{LeanDev}."
    note := "Discloses the Lean 4 development [LeanDev]; quotes rather than asserts." },
  { key := "ebe3dc3abd3b"
    status := SentenceStatus.attribution
    tex := "This is the operator norm notion of MF group introduced by Carri\\'on--Dadarlat--Eckhardt~\\cite{CDE}, and MF group always means this notion below."
    note := "Credits Carrion-Dadarlat-Eckhardt for the operator-norm MF notion. No separate checkable content; not formalized. Merged 2026-08-27 from 2 sentences of the previous version." },
  { key := "8fcb4c37d063"
    status := SentenceStatus.attribution
    tex := "For $\\theta=\\mathrm{id}$ this is close to Shulman's theorem on central HNN extensions~\\cite{Shulman}; the version below assumes a tracial state vanishing off the identity and allows a nontrivial edge isomorphism."
    note := "Compares theta=id to Shulman's central-HNN theorem. No separate checkable content; not formalized." },
  { key := "28dab2ddc5a2"
    status := SentenceStatus.attribution
    tex := "The first assertion follows by diagonalization from the matrix-corona characterization of MF algebras~\\cite{BK}, and Korchagin uses it in this form~\\cite[Proposition~6]{Korchagin}."
    note := "Credits Blackadar-Kirchberg (see also Brown-Ozawa Ch. 11) for the diagonalisation and Korchagin Prop. 6 for this form. The assertion so credited is the first declaration; the second is the proof it is discharged by. [supersedes p2_map_skeleton.tsv: tensor-sync (Blackadar-Kirchberg's matrix-corona characterization actively used, not background)]" },
  { key := "58b54079885c"
    status := SentenceStatus.provenance
    tex := "So it applies to any group property that is closed under subgroups, given a finitely presented group without the property and an analogue of Lemma~\\ref{lem:tensor-sync} for the twisted HNN extensions; the open problems below state which input is missing for soficity and for hyperlinearity."
    note := "The template claim itself. The repository already carries it in exactly this generality: SwitchPropertyData is 'any group property closed under subgroups with a seed lacking it', and the package theorem is the resulting second-level classification. What the remark says is still missing for soficity and hyperlinearity is the analogue of lem:tensor-sync, i.e. an inhabitant of the analytic input. [supersedes p2_map_skeleton.tsv: Meta-remark on the template's general applicability to any subgroup-closed property; mirrors the pre-rewrite rem:template sentence, same treatment.]" },
  { key := "037e150b0fad"
    status := SentenceStatus.openProblem
    tex := "We do not know whether $\\widehat R_e$ is hyperlinear for $e\\in\\mathrm{INF}$; placing hyperlinearity recognition at the same level would also require a finitely presented non-hyperlinear group, and none is known."
    note := "States it is unknown whether R_e-hat is hyperlinear for e in INF." },
  { key := "95282b9d1936"
    status := SentenceStatus.openProblem
    tex := "We do not know whether an analogous family of finitely presented groups exists for soficity; such a construction would require a permutation-model replacement for Lemma~\\ref{lem:tensor-sync}."
    note := "States it is unknown whether an analogous finitely presented family exists for soficity." },
  { key := "d1084c7db1ac"
    status := SentenceStatus.openProblem
    tex := "Finally, we do not know the complexity of MF recognition under the strong convergence convention of~\\cite{GaoEtAl,Schafhauser}: the norms $\\opnorm{\\lambda(w)}$ in the left regular representation are not in general computable from a finite presentation, so Proposition~\\ref{prop:mf-upper-bound} does not apply."
    note := "States it is unknown what the complexity of MF recognition is under the strong-convergence convention of GaoEtAl/Schafhauser-Zhang." },
  { key := "e00d9e486ac5"
    status := SentenceStatus.openProblem
    tex := "We do not know whether the models of $\\widehat R_e$ can be chosen to converge to $C^*_{\\mathrm r}(\\widehat R_e)$ in operator norm, so that the reduced $C^*$-algebra itself is MF; the argument produces a tracial state on an abstract MF algebra."
    note := "States it is unknown whether the models of R_e-hat can be chosen to converge to C*_r(R_e-hat) in operator norm; no declaration can settle an open problem." },
  { key := "adf031699695"
    status := SentenceStatus.attribution
    tex := "The author also thanks Tatiana Shulman for clarifying the terminology and historical context of the MF question, and Caleb Eckhardt for discussions of MF representations and matrix norms."
    note := "Personal acknowledgments (Shulman, Eckhardt); credits individuals for assistance, no mathematical assertion." },
  { key := "da592533593f"
    status := SentenceStatus.attribution
    tex := "The author thanks Francesco Fournier-Facio for early advice and detailed comments on earlier drafts, and Alon Dogon for organizing the ensuing discussion and for comments on the surrounding literature."
    note := "Personal acknowledgments (Fournier-Facio, Dogon); credits individuals for assistance, no mathematical assertion." }
]

end Meta
end Manuscript
end GroupApproximation
