\DocumentMetadata{lang=en-US,pdfversion=2.0,pdfstandard=UA-2,tagging=on}
\documentclass[11pt]{amsart}

\usepackage[T1]{fontenc}
\IfFileExists{lmodern.sty}{\usepackage{lmodern}}{\PassOptionsToPackage{expansion=false}{microtype}}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{enumitem}
\usepackage[nopatch=footnote]{microtype}
\usepackage{xcolor}
\usepackage{tikz}
\usepackage{float}
\usetikzlibrary{arrows.meta,calc,positioning}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue]{hyperref}
% Allow line breaks with stretch inside linked declaration names.
\Urlmuskip=0mu plus 1mu\relax
\expandafter\def\expandafter\UrlBreaks\expandafter{\UrlBreaks\do\.\do\_%
  \do\/\do\-%
  \do\a\do\b\do\c\do\d\do\e\do\f\do\g\do\h\do\i\do\j\do\k\do\l\do\m\do\n%
  \do\o\do\p\do\q\do\r\do\s\do\t\do\u\do\v\do\w\do\x\do\y\do\z%
  \do\A\do\B\do\C\do\D\do\E\do\F\do\G\do\H\do\I\do\J\do\K\do\L\do\M\do\N%
  \do\O\do\P\do\Q\do\R\do\S\do\T\do\U\do\V\do\W\do\X\do\Y\do\Z%
  \do\0\do\1\do\2\do\3\do\4\do\5\do\6\do\7\do\8\do\9}
\newcommand{\paperauthor}{Anonymous}
\hypersetup{
  pdftitle={An explicit finitely presented non-MF group},
  pdfauthor={\paperauthor},
  pdfsubject={Explicit finitely presented non-MF groups and matrix coronas},
  pdfkeywords={MF groups, matrix coronas, property (T), Clifford lamps, Kazhdan compression}
}
\newcommand{\doi}[1]{\href{https://doi.org/#1}{\nolinkurl{doi:#1}}}
\newcommand{\arxiv}[1]{\href{https://arxiv.org/abs/#1}{\nolinkurl{arXiv:#1}}}
\newcommand{\leanbadge}[3]{%
  \par\smallskip\noindent
  {\normalfont\scriptsize\color{gray!70!black}%
   #3\par\nobreak\noindent
   \hspace*{1em}%
   \nolinkurl{#2}%
  }\par\smallskip}
% Every displayed record certifies the full printed claim.
\newcommand{\leanverified}[2]{\leanbadge{#1}{#2}{\fbox{\sffamily\bfseries EXACT}}}

\setlist[enumerate]{leftmargin=2.35em}
\setlist[itemize]{leftmargin=2.1em}
\allowdisplaybreaks
\raggedbottom
\emergencystretch=1.5em

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{construction}[theorem]{Construction}

\theoremstyle{plain}
\newtheorem{mainthm}{Theorem}
\renewcommand{\themainthm}{\Alph{mainthm}}

\newcommand{\C}{\mathbb C}
\newcommand{\Z}{\mathbb Z}
\newcommand{\R}{\mathbb R}
\newcommand{\N}{\mathbb N}
\newcommand{\SL}{\operatorname{SL}}
\newcommand{\GL}{\operatorname{GL}}
\newcommand{\U}{\mathcal U}
\newcommand{\Ucor}{\mathsf U_{\mathrm{cor}}}
\newcommand{\Fix}{\operatorname{Fix}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\Ad}{\operatorname{Ad}}
\newcommand{\End}{\operatorname{End}}
\newcommand{\Cl}{\operatorname{Cl}}
\newcommand{\ClLamp}{\mathsf{ClLamp}}
\newcommand{\Cmax}{C^*_{\mathrm{max}}}
\newcommand{\Cred}{C^*_{\mathrm{red}}}
\newcommand{\opnorm}[1]{\left\lVert #1\right\rVert}
\newcommand{\hsnorm}[1]{\left\lVert #1\right\rVert_{2}}

\tikzset{
  figbox/.style={draw, line width=.4pt, align=center, inner sep=4.5pt,
    text width=3.05cm, font=\small},
  figout/.style={figbox, line width=.8pt},
  figarr/.style={-{Latex[length=2mm,width=1.4mm]}, line width=.4pt},
  fignote/.style={font=\footnotesize, inner sep=2pt}
}

\begin{document}

\title{An explicit finitely presented non-MF group}
\author{\paperauthor}

\begin{abstract}
A countable discrete group is MF here if it embeds into
the unitary group of a norm matrix corona.  Equivalently, it embeds into the
metric quotient of a product of finite-dimensional unitary groups by the
sequences converging to the identity in operator norm.  We construct an explicit
finitely presented group $E$, together with a designated central involution
$w\in E$, such that $w\neq 1$ but \emph{every} homomorphism from $E$ to
the unitary group of any norm matrix corona sends $w$ to $1$.  Thus $E$ is not
MF, and neither its concrete universe-relative maximal nor
its reduced group $C^*$-algebra is MF.

The eight-generator presentation contains a literal six-generator base and
a stable letter that doubles its three commuting translation generators.
Property~\textup{(T)} of that exact base is certified by a rational
degree-one Hodge decomposition for a six-generator, thirteen-relator group,
a checked quotient onto the rotation subgroup, and an intrinsic fixed-point
argument for the translation subgroup.  The obstruction then uses
coordinatewise averaging and compression, polar correction, a finite-stage
spectral projection, and equality of finite ranks.
In general, for the resulting one-sided compression pattern, every finite
normal subgroup $F$ inside the compression-defect normal closure lies in the
kernel of every corona representation.  Two consequences are that $C^*_{\mathrm{red}}(E)$
is stably finite but not MF, and that MF is not preserved by
quotients.  An explicit cyclic-base comparison shows that exact
finite-dimensional invisibility alone does not force the marked word to be
trivial.  The MF conclusion is a separate uniform statement;
here the property-\textup{(T)} step supplies that additional control.
\end{abstract}

\maketitle

\section{Introduction}\label{sec:intro}

Let $(d_n)_{n\in\N}$ be a sequence of positive integers and let
\begin{equation}\label{eq:corona}
  \mathcal Q\;=\;\prod_{n} M_{d_n}(\C)\Big/\bigoplus_{n} M_{d_n}(\C)
\end{equation}
be the associated \emph{norm matrix corona}: bounded sequences of
matrices modulo sequences converging to $0$ in operator norm.  Following
Blackadar--Kirchberg \cite{BK}, a separable $C^*$-algebra is \emph{MF}
if it embeds into some algebra of the form \eqref{eq:corona}.  We use
\emph{MF group} exclusively in the sense introduced by
Carri\'on--Dadarlat--Eckhardt and used by Shulman \cite{CDE,Shulman}: a
countable discrete group $E$ is MF if, for some strictly increasing sequence
of positive integers $(d_n)$, there is an injective homomorphism
\[
  E\longrightarrow\U(\mathcal Q).
\]
We shall use the equivalent coordinate model
\begin{equation}\label{eq:unitary-corona}
 \Ucor((d_n))
 :=\prod_n\U(M_{d_n}(\C))\Big/
 \bigl\{(u_n):\opnorm{u_n-1}\longrightarrow0\bigr\}.
\end{equation}
The denominator is normal, and polar correction gives a canonical
isomorphism
\begin{equation}\label{eq:unitary-corona-iso}
 \kappa_{(d_n)}\colon \Ucor((d_n))\ \xrightarrow{\ \cong\ }\
 \U(\mathcal Q),
\end{equation}
proved in Lemma~\ref{lem:unitarycorona}.  Thus one may equivalently embed
$E$ into $\Ucor((d_n))$.
\begin{proposition}[equivalent MF formulations]
\label{prop:mf-equivalences}
For every countable group, the genuine $C^*$-corona definition above is
equivalent to the unitary-sequence definition.  The latter is equivalent to
operator-norm local models with separation constant $1$, and allowing
arbitrary positive dimensions is equivalent to requiring the dimensions to
be strictly increasing.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptMFDefinitionEquivalences}
\end{proposition}

For the final equivalence, place the $n$th model in the last block of
dimension $d_0+\cdots+d_n$ and fill the earlier blocks with identities.
This preserves every defect and distance from the identity.
Hereafter ``MF'' means this equivalent actual-corona/local-operator-norm
property.  It should not
be confused with the stronger convention based on reduced-norm convergence
of asymptotic representations \cite{Schafhauser,GKEMP}, or with strongly PMF,
where the approximants are genuine finite-dimensional representations
\cite[Definition~1.2]{MdlS}.  These are genuinely different conventions;
every assertion below uses only this MF property.  The
following theorem gives an explicit finitely presented non-MF group.

\begin{mainthm}[non-MF group]\label{thm:A}
Let $E$ be the group on the eight generators
$v_1,v_2,v_3,x,y,z,t,c$ with the finite list of relators in
Definition~\ref{def:E}, and put
\[
  w\;:=\;\bigl[\,tct^{-1},\;v_1(tct^{-1})v_1^{-1}\bigr].
\]
The defining relations make $w$ central and give $w^2=1$.  Then:
\begin{enumerate}
  \item $w\neq 1$ in $E$, so $w$ is a central involution;
  \item for every sequence $(d_n)$ of positive integers and every group homomorphism
    $\Theta\colon E\to\Ucor((d_n))$ into the corresponding unitary norm
    corona \eqref{eq:unitary-corona}, one has $\Theta(w)=1$.
\end{enumerate}
In particular $E$ is not MF, and neither the universe-relative algebra
$\Cmax(E)$ fixed below nor $\Cred(E)$ is an MF $C^*$-algebra.
\leanverified{Sofic/LiteralNonMFEndpoint}{GroupApproximation.LiteralNonMFEndpoint.manuscriptTheoremA}
\end{mainthm}

The inline label links the theorem directly to its declaration.  Similar
labels below give the declaration name for each numbered claim.

A completely explicit presentation of $E$ --- eight generators, with
every relation displayed --- appears as \eqref{eq:E} in
Definition~\ref{def:E}.

The proof is coordinatewise and has five steps.  Let $F\trianglelefteq H$ be
the finite normal subgroup in the general criterion; for Theorem~\ref{thm:A},
$F=\{1,w\}$.

\begin{enumerate}
  \item\emph{Finite-group averaging.}  From a coordinate model nontrivial on a
    finite normal subgroup $F$, form the coordinate averages
    $A_n=|F|^{-1}\sum_{f\in F}U_{f,n}$.  Hermitianize and spectrally round
    them, then retain the complementary nonzero spectral blocks.
  \item\emph{Compression and polar repair.}  Normality of $F$ makes the
    rounded projections asymptotically central.  Principal compression
    therefore preserves multiplication up to vanishing operator-norm error;
    its Gram defects vanish, and polar correction gives honest unitary
    matrices without changing the asymptotics (Lemma~\ref{lem:corner}).
  \item\emph{Vanishing finite sum.}  On the complementary blocks the
    Reynolds averages vanish asymptotically.  Consequently the repaired
    matrices satisfy
    \[
      \opnorm{\sum_{f\in F}V_{f,n}}\longrightarrow0.
    \]
  \item\emph{Property-\textup{(T)} spectral projection.}  A finite-stage
    spectral projection for the adjoint microstates of $\Gamma$ is almost
    contained in its $t$-translate.  The two projections have equal rank, so the complementary
    block estimate reverses the containment.  This forces every generator
    $[tct^{-1},\iota(\lambda)]$ to be Hilbert--Schmidt trivial
    (Lemma~\ref{lem:compressorcollapse}).
  \item\emph{Contradiction.}  In a tracial matrix ultraproduct the whole
    normal closure, hence $F$, is trivial.  A cofinal diagonal extraction
    makes every $V_{f,n}$ Hilbert--Schmidt close to $V_{1,n}$, contradicting
    the vanishing finite sum from step (3) (Section~\ref{sec:proofA}).
\end{enumerate}

Figure~\ref{fig:mechanism} shows where the two halves of the proof meet.
The upper lane is purely group-theoretic; the lower lane is the
finite-matrix rigidity input.  Their common output is incompatible with the
complementary finite-group corner.

\begin{figure}[H]
\centering
\begin{tikzpicture}[node distance=12mm and 7mm,
    figbox/.append style={text width=3.25cm, minimum height=15mm}]
  \node[figbox] (pattern) {Kazhdan pattern\\
    $t\iota(\Lambda)t^{-1}\subseteq\iota(\Lambda)$,\\
    $[c,\iota(\Lambda)]=1$};
  \node[figbox, right=of pattern] (defect) {transported root\\
    $d=tct^{-1}$; defect\\
    $N_{\rm comp}=\langle\!\langle[d,\iota(\lambda)]\rangle\!\rangle$};
  \node[figbox, right=of defect] (finite) {$F\triangleleft H$ finite\\
    and $F\le N_{\rm comp}$};

  \node[figbox, below=of pattern] (kazhdan) {finite-stage Kazhdan\\
    spectral cuts $P_n$ for\\ the adjoint microstates};
  \node[figbox, right=of kazhdan] (pin) {one-sided leakage and\\
    equal ranks force\\ $\lVert P_n-R_n\rVert\to0$};
  \node[figbox, right=of pin] (collapse) {every $[d,\iota(\lambda)]$\\
    tends to $1$ in\\ normalized HS norm};

  \node[figout, text width=4.2cm, anchor=north east] (contradiction)
    at ($(collapse.south east)+(0,-10mm)$)
    {on the complementary\\ finite-group corner\\
     $\lVert\sum_{f\in F}V_{f,n}\rVert\to0$, while\\
     each $V_{f,n}$ tends to $V_{1,n}$};

  \draw[figarr] (pattern) -- (defect);
  \draw[figarr] (defect) -- (finite);
  \draw[figarr] (pattern) -- node[fignote, right=2pt] {property~(T)}
    (kazhdan);
  \draw[figarr] (kazhdan) -- (pin);
  \draw[figarr] (pin) -- (collapse);
  \draw[figarr] (defect.south) -- ++(0,-4.5mm) -| (collapse.north);
  \draw[figarr] (finite.east) -- ++(4.5mm,0) |- (contradiction.east);
  \draw[figarr] (collapse.south) -- (collapse.south |- contradiction.north);
\end{tikzpicture}
\caption{The finite-normal obstruction.  The upper lane is
group-theoretic and the lower lane is the finite-matrix rigidity input.
Property~(T) supplies a uniform spectral cut, and equality of finite ranks
turns its one-sided leakage estimate into two-sided control.}
\label{fig:mechanism}
\end{figure}

Nontriviality of $w$ is witnessed by a completely explicit
representation of $E$ into a semidirect product of a Clifford group by a
linear group over $\Z[1/2]$ (Section~\ref{sec:witness}); no
combinatorial theory of HNN extensions is needed.

Two companion results clarify the mechanism.  First, in
finite dimensions the obstruction is elementary and needs neither
property (T), nor unitarity, nor centrality of $w$:

For a subgroup $L\le H$, put
\[
 \mathcal C(L)=\{s\in H:sLs^{-1}\subseteq L\},\qquad
 G_{\rm comp}(L)=\langle\mathcal C(L)\rangle,
\]
and define the intrinsic \emph{compression--centralizer defect}
\begin{equation}\label{eq:intrinsicdefect}
 \mathfrak D(H,L)=
 \left\langle\!\left\langle
 [gzg^{-1},\ell]:g\in G_{\rm comp}(L),\ z\in C_H(L),\ \ell\in L
 \right\rangle\!\right\rangle_H .
\end{equation}

\begin{mainthm}[finite-dimensional obstruction]\label{thm:B}
Let $k$ be a field, $V$ a finite-dimensional $k$-vector space, $H$ a
group, $\Gamma\le H$ a subgroup, and $t,c\in H$, $a\in\Gamma$ elements
with $t\Gamma t^{-1}\subseteq\Gamma$ and $c\gamma=\gamma c$ for all
$\gamma\in\Gamma$.  Then every homomorphism $\pi\colon H\to\GL(V)$
satisfies
\[
  \pi\bigl(\bigl[\,tct^{-1},\;a(tct^{-1})a^{-1}\bigr]\bigr)\;=\;1 .
\]
More generally, every such $\pi$ sends the entire subgroup
$\mathfrak D(H,\Gamma)$ to the identity.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptTheoremB}
\end{mainthm}

Applied to $H=E$, Theorem~\ref{thm:B} shows that \emph{every}
finite-dimensional representation of $E$ over \emph{every} field sends
$w$ to the identity: finite-dimensional linear representations do not separate the points
of $E$, no finite
quotient can witness $w\neq 1$, and the infinite Clifford witness of
Section~\ref{sec:witness} cannot be replaced by a finite-dimensional one.
Property (T) enters the corona argument for one reason:
it makes ``the projection onto the $\Gamma$-fixed space'' an element of
the corona, uniformly across all coordinates at once.

Second, the elementary finite-dimensional calculation does not determine
whether its marked word is trivial:

\begin{mainthm}[cyclic-base comparison]\label{thm:C}
Define the literal marked presentation
\[
 E_{\mathrm{BS}}=\left\langle\gamma_0,t,c\ \middle|\
 \begin{aligned}
  &t\gamma_0t^{-1}=\gamma_0^2,\quad c^2=1,\quad[c,\gamma_0]=1,\\
  &w_{\mathrm{BS}}^2=1,\quad
    [w_{\mathrm{BS}},\gamma_0]=[w_{\mathrm{BS}},t]
      =[w_{\mathrm{BS}},c]=1
 \end{aligned}\right\rangle,
\]
where
$w_{\mathrm{BS}}=[tct^{-1},\gamma_0(tct^{-1})\gamma_0^{-1}]$.
Then $E_{\mathrm{BS}}$ is finitely presented and $w_{\mathrm{BS}}\ne1$.
There is an explicit surjective homomorphism from $E_{\mathrm{BS}}$ onto its
realized Clifford quotient that maps $w_{\mathrm{BS}}$ to the nontrivial
central sign.  Nevertheless every finite-dimensional representation of
$E_{\mathrm{BS}}$ over every field maps $w_{\mathrm{BS}}$ to the identity.
\leanverified{Monsters/LiteralCyclicCalibration}{GroupApproximation.LiteralCyclicCalibration.manuscriptCyclicCalibration}
\end{mainthm}

The quotient statement makes the comparison at the level actually used by
the proof: exact finite-dimensional invisibility does not by itself force
triviality of the marked word.  The MF conclusion in
Theorem~\ref{thm:A} is the separate uniform assertion supplied by the
fixed-space argument.

\subsection*{Generalizations and consequences}
Theorem~\ref{thm:A} is an instance of a general finite-normal obstruction
(Definition~\ref{def:pattern}).  A marked Kazhdan pattern consists of a
property~\textup{(T)} group $\Lambda$, a homomorphism
$\iota\colon\Lambda\to H$, and elements $t,c\in H$ such that
$t\iota(\Lambda)t^{-1}\subseteq\iota(\Lambda)$ and
$[c,\iota(\Lambda)]=1$.  Theorem~\ref{thm:criterion} sends every finite
normal subgroup contained in the resulting compression-defect subgroup to
the identity under every corona representation.
For $E$, the relevant subgroup is $\{1,w\}$.  Corollary~\ref{cor:nofaithful}
records the resulting obstruction for $C^*$-subalgebras of norm matrix coronas,
while
Theorem~\ref{thm:C} shows that finite-dimensional collapse by itself does
not decide whether the marked word is trivial.

Section~\ref{sec:consequences} develops structural and $C^*$-algebraic
consequences.  The principal $C^*$-algebraic application is:

\begin{mainthm}[a stably finite non-MF reduced group algebra]\label{thm:D}
$\Cred(E)$ is a unital separable $C^*$-algebra which carries a
faithful tracial state --- and is therefore stably finite --- but is
not an MF algebra.
\leanverified{Sofic/LiteralNonMFEndpoint}{GroupApproximation.LiteralNonMFEndpoint.manuscriptTheoremD}
\end{mainthm}

\subsection*{What this paper does not prove}
The proof of Theorem~\ref{thm:A} does \emph{not} show that $E$ is
nonhyperlinear.  The
finite-rank step in the proof is specific to operator norm: nested unitarily
equivalent projections need not coincide in the properly infinite operator
algebra arising from a tracial ultraproduct.  Section~\ref{sec:limits}
explains this failure.  The argument also leaves $E/\langle w\rangle$
unresolved.

\subsection*{External inputs}
The load-bearing property-\textup{(T)} statement is proved for the literal
twenty-relator base in Section~\ref{sec:group}; it is not imported from an
identification of that presentation with a semantic matrix group.  The proof
uses a finite rational Hodge certificate, checked relator replays, and the
Hilbert-space fixed-point theorem.  The Clifford witness and the
finite-dimensional obstruction are proved directly.

\subsection*{Relation to prior work}
The closest conceptual analogue is the recent work on nonsofic groups.
One-sided compression of a property~\textup{(T)} subgroup and propagation
from its centralizer appear in \cite{OAI,KT}, building on rigidity results of
Kun and Kun--Thom \cite{Kun16,KT19}; related centralizer rigidity for sofic
embeddings is proved in \cite{AlekseevThom}.  Those results concern Hamming or
tracial approximations.  Here coordinatewise averaging, polar correction,
and a finite-stage spectral projection produce a specific element in the
kernel of every operator-norm corona representation.  The Clifford central
extension supplies the element whose survival is proved independently.

The operator-norm argument replaces finite cardinality by the implication
\begin{multline*}
 \text{one-sided leakage of }P_n\text{ into }R_n,
 \qquad \operatorname{rank}P_n=\operatorname{rank}R_n
 \\
 \Longrightarrow\qquad \opnorm{P_n-R_n}\longrightarrow0.
\end{multline*}
The complementary finite-group corner then converts this estimate into a
fixed nontrivial element sent to the identity by every corona representation.

Slofstra's constructions supply close precedents for the Clifford group,
distinguished central involution, shift, and HNN doubling map
\cite[Section~2 and Remark~3.3]{SlofstraProfile}.  Negative-eigenspace
compression appears in Slofstra--Vidick
\cite[proof of Proposition~2.7]{SlofstraVidick}, and
Bachner--Dogon--Lubotzky develop the operator-norm rounding, compression, and
polar-correction procedure in detail
\cite[Lemmas~2.2--2.3, Proposition~2.4, and the proof of
Proposition~1.5]{BDL}.  The present proof
combines that local corner procedure with the one-sided Kazhdan compressor;
equality of the two finite ranks supplies the reverse projection estimate and
hence the universal kernel conclusion.

The analytic passage from operator norm to the Hilbert--Schmidt conjugation
representation belongs to the $\pi\otimes\bar\pi$ framework of Bekka and
Bekka--Valette \cite{BekkaAmenable,BekkaValette}.  Dadarlat uses normalized
finite-rank projections as almost invariant Hilbert--Schmidt vectors in an
operator-norm approximation argument
\cite[Lemma~3.18 and Proposition~3.19]{Dadarlat}.  These are the direct
analytic precedents for the conjugation step used here.

Slofstra's quotients by elements invisible to exact or approximate
finite-dimensional representations precede the MF-radical language used
below \cite[Definitions~2.5--2.7]{SlofstraCorrelations}.  Fritz's marked
$\mathrm{BS}(2,3)$ example provides additional context for the cyclic
comparison \cite{Fritz,SlofstraVidick}.

Central extensions and rigidity of approximate representations also drive
the Frobenius obstruction of De Chiffre--Glebsky--Lubotzky--Thom
\cite{DGLT}.  In the proof of Proposition~1.6,
Bachner--Dogon--Lubotzky treat the finite-central case by a
character-isotypic corner, and reduce the general finite-normal case to it by
passing to the finite-index centralizer \cite{BDL}.  The finite-normal
criterion here combines the corresponding coordinate-corner construction
with the one-sided Kazhdan compressor and does not assume that the quotient is
hyperlinear.  Related finite-extension formulations of soficity appear in
\cite{GR,Gl19}.

Carri\'on--Dadarlat--Eckhardt provide the group-MF framework used here and,
through Abels' group, an amenable MF group that is not residually finite
\cite[Section~2.14 and Corollary~2.18]{CDE}.  The cyclic comparison addresses
the narrower question of what happens to the marked compression pattern when
property~\textup{(T)} is removed.

Finally, failure of the MF property for a group $C^*$-algebra does not by
itself prevent the group from embedding into a corona: the group property
requires only a faithful canonical image in some MF quotient of $\Cmax(E)$.
The result here produces a specific nontrivial element in the kernel of every
such representation.  For broader context see
\cite{DGLT,BDL,OAI,KT,Shulman,Dadarlat,SlofstraCorrelations,SlofstraProfile,SlofstraVidick}.

\subsection*{Organization}
Section~\ref{sec:prelim} fixes notation and states the external inputs.
Section~\ref{sec:group} constructs $E$ and certifies property~\textup{(T)}
for its literal base.  Section~\ref{sec:witness} proves $w\neq 1$.
Section~\ref{sec:findim} proves Theorem~\ref{thm:B}.
Section~\ref{sec:corona} develops the corona lemmas.
Section~\ref{sec:conj} defines marked Kazhdan patterns and their
compression-defect subgroups, states the finite-normal obstruction
criterion (Theorem~\ref{thm:criterion}), and develops the coordinate
Reynolds and Kazhdan-compression estimates.
Section~\ref{sec:proofA} proves the criterion and deduces
Theorem~\ref{thm:A}.  Section~\ref{sec:consequences} derives
Theorem~\ref{thm:D} and further consequences.
Section~\ref{sec:necessity} proves Theorem~\ref{thm:C}.
Section~\ref{sec:limits} discusses limitations.

\section{Notation and external inputs}\label{sec:prelim}

All groups are discrete and countable, and every group and auxiliary index
set is represented on a fixed small carrier.  This is no restriction for the
objects considered here: a countable group is isomorphic to one whose
underlying set is a subset of $\N$, and finite coordinate systems can be
reindexed by natural numbers.  Property
\textup{(T)} below is the textbook complex-unitary property, independent of
the universe containing the Hilbert space.  A $C^*$-homomorphism is unital only
when this is stated.  In particular, an MF embedding may be nonunital, as is
customary for embedding a unital algebra as a $C^*$-subalgebra.  If
$e\colon A\to B$ is an injective, possibly nonunital $*$-homomorphism between
unital $C^*$-algebras, then $u\mapsto e(u)+(1-e(1))$ is an injective
homomorphism $\U(A)\to\U(B)$; this complement correction is used below.  For
a unital $C^*$-algebra $A$
we write $\U(A)$ for its unitary group.  On $M_r(\C)$ we use the
normalized trace $\tr_r$, the operator norm $\opnorm{\cdot}$, and the
normalized Hilbert--Schmidt norm
$\hsnorm{x}=\tr_r(x^*x)^{1/2}$.  We will use three elementary facts
without further comment: $\hsnorm{x}\le\opnorm{x}$;
$|\tr_r(x)|\le\opnorm{x}$; and $\hsnorm{uxv}=\hsnorm{x}$ for unitaries
$u,v$.

Given a sequence $(d_n)_{n\in\N}$ of positive integers, we write
\[
  \mathcal Q\bigl((d_n)_{n\in\N}\bigr)
  \;=\;\prod_{n\in\N}^{\ell^\infty} M_{d_n}(\C)
  \Big/\bigoplus_{n\in\N}^{c_0} M_{d_n}(\C),
\]
the quotient of the bounded $C^*$-product by the ideal of sequences with
$\opnorm{x_n}\to0$.  We call any such algebra a \emph{norm matrix
corona}.  We also write
\[
 \Ucor((d_n)_{n\in\N})
 =\prod_{n\in\N}\U(M_{d_n}(\C))\Big/
 \bigl\{(u_n):\opnorm{u_n-1}\longrightarrow0\bigr\}.
\]
A \emph{corona representation} of a group $E$ is a homomorphism
$E\to\U(\mathcal Q)$.  Lemma~\ref{lem:unitarycorona} canonically identifies
it with a homomorphism into $\Ucor((d_n))$, which we call its coordinate
model.  The group $E$ is \emph{MF} if it admits an injective corona
representation for a strictly increasing dimension sequence.  By the
dimension-amplification equivalence discussed in the introduction, allowing
an arbitrary positive dimension sequence gives the same class of groups.

Fix a universe containing every group and $C^*$-algebra used below.  For a
group $H$ in it, $\Cmax(H)$ denotes the following concrete
universe-relative maximal model.  Take the bounded product of every
same-universe nontrivial unital $C^*$-algebra equipped with a homomorphism
$H\to\U(B)$, map $H$ diagonally into this product, and take the closed
$*$-subalgebra generated by that image.  We write $u_h$ for its canonical
unitaries.
\begin{proposition}[universe-relative maximal model]
\label{prop:maximal-cstar}
The canonical map $h\mapsto u_h$ is injective.  For every same-universe
unital $C^*$-algebra $B$ and every homomorphism $\rho\colon H\to\U(B)$,
there is a unique unital $*$-homomorphism $\Cmax(H)\to B$ that maps $u_h$
to $\rho(h)$ for every $h\in H$.
\leanverified{Analysis/MaximalGroupCStar}{GroupApproximation.manuscriptUniverseRelativeMaximalGroupCStar}
\end{proposition}

Coordinate evaluation gives the extension, while the left-regular
coordinate proves injectivity.  These are precisely the two properties of
the maximal construction used here.

We use the commutator convention $[x,y]=xyx^{-1}y^{-1}$.

\section{The literal group and its Kazhdan certificate}\label{sec:group}

We work throughout with the groups defined by the relators below.  No
identification of these presented groups with a matrix group is used.
First define the \emph{rotation group}
\begin{equation}\label{eq:sl3presentation}
 \mathcal R=\left\langle x,y,z\ \middle|\
 \begin{gathered}
 x^3=y^3=z^2=(xz)^3=(yz)^3=1,\\
 (x^{-1}zxy)^2=(y^{-1}zyx)^2=(xy)^6=1
 \end{gathered}\right\rangle,
\end{equation}
and the \emph{literal base}
\begin{equation}\label{eq:gamma-presentation}
\mathcal B=\left\langle v_1,v_2,v_3,x,y,z\ \middle|\
\begin{gathered}
\text{the eight relations in \eqref{eq:sl3presentation}},\\
[v_1,v_2]=[v_1,v_3]=[v_2,v_3]=1,\\
xv_1x^{-1}=v_3,\ xv_2x^{-1}=v_1,\
xv_3x^{-1}=v_2,\\
yv_1y^{-1}=v_1,\ yv_2y^{-1}=v_2^{-1}v_3,\
yv_3y^{-1}=v_1v_2^{-1},\\
zv_1z^{-1}=v_2v_3^{-1},\ zv_2z^{-1}=v_1v_3^{-1},\
zv_3z^{-1}=v_3^{-1}
\end{gathered}\right\rangle.
\end{equation}
Write $\iota\colon\mathcal B\to E$ for the homomorphism induced by the first
six generators of the presentation below.  This map is not assumed to be
injective.

\begin{definition}\label{def:E}
Use the six literal base generators and the relations displayed in
\eqref{eq:gamma-presentation}.  Define
\begin{equation}\label{eq:E}
  E=\left\langle v_1,v_2,v_3,x,y,z,t,c\ \middle|\
  \begin{gathered}
   \text{all relations in \eqref{eq:gamma-presentation}},\\
   tv_i t^{-1}=v_i^2\ (1\le i\le3),\\
   txt^{-1}=x,\quad tyt^{-1}=y,\quad tzt^{-1}=z,\\
   c^2=1,\qquad w^2=1,\\
   [c,g]=1\quad(g\in\{v_1,v_2,v_3,x,y,z\}),\\
   [w,g]=1\quad(g\in\{v_1,v_2,v_3,x,y,z,t,c\})
  \end{gathered}\right\rangle,
\end{equation}
where $w$ abbreviates the word
$\bigl[\,tct^{-1},\,v_1(tct^{-1})v_1^{-1}\bigr]$.
This is an explicit finite presentation with eight generators.  Its defining
relations make $w$ central and impose $w^2=1$; the relations on the six base
generators also give
\[
 t\iota(\mathcal B)t^{-1}\subseteq\iota(\mathcal B),
 \qquad [c,\iota(\mathcal B)]=1.
\]
Proposition~\ref{prop:witness} below proves that $w\neq1$.
\leanverified{Sofic/LiteralNonMFPresentation}{GroupApproximation.LiteralNonMFPresentation.manuscriptLiteralPresentation}
\end{definition}

Figure~\ref{fig:compression-pattern} isolates the geometry used later in
the analytic argument: $d=tct^{-1}$ centralizes the compressed copy, not
necessarily all of $\iota(\mathcal B)$, and the marked commutator measures
precisely that failure after conjugating $d$ by $\iota(v_1)$.

\begin{figure}[H]
\centering
\begin{tikzpicture}[x=1cm,y=1cm,font=\small]
  \draw[rounded corners=2mm,line width=.55pt]
    (-5.15,-.1) rectangle (.85,2.85);
  \node[anchor=north west] at (-4.9,2.62) {$\iota(\mathcal B)$};
  \draw[rounded corners=2mm,line width=.55pt,fill=black!4]
    (-2.95,.55) rectangle (.35,2.15);
  \node[align=center] at (-1.3,1.35)
    {$t\iota(\mathcal B)t^{-1}$\\[-1pt]$\subseteq\iota(\mathcal B)$\\[3pt]
     {\footnotesize $[d,t\iota(\mathcal B)t^{-1}]=1$}};
  \node[align=center] at (-4.15,1.1)
    {$\iota(v_1)\in\iota(\mathcal B)$\\[3pt]
     {\footnotesize $[c,\iota(\mathcal B)]=1$}};

  \node[draw,circle,inner sep=1.8pt] (c) at (-4.15,-1.0) {$c$};
  \node[draw,circle,inner sep=1.8pt] (d) at (-1.3,-1.0) {$d$};
  \draw[figarr] (c) -- node[fignote,above=2pt]
    {conjugation by $t$} (d);

  \node[draw,rounded corners=2mm,line width=.55pt,align=center,
    text width=4.55cm,minimum height=2.1cm] at (3.55,1.35)
    {marked defect\\[2pt]
     $w=[d,\,\iota(v_1)d\iota(v_1)^{-1}]$\\[3pt]
     {\footnotesize $d^2=1$ gives $w=[d,\iota(v_1)]^2$}};
\end{tikzpicture}
\caption{The marked-compression pattern.  Conjugation by $t$ carries the
image of the literal base into itself.  It sends $c$ to $d=tct^{-1}$, which
centralizes that compressed copy.  Only the one-sided inclusion is used;
no injectivity or strict containment is asserted.}
\label{fig:compression-pattern}
\end{figure}

The presentation itself, rather than an asserted identification with a
semantic matrix group, is the source of every relation used below.  In
particular the argument needs only the one-sided inclusion and centralizer
relations shown in Figure~\ref{fig:compression-pattern}; it never uses
injectivity or properness of the base map.

\subsection{An exact property-\textup{(T)} certificate}

We next prove property~\textup{(T)} for the exact group $\mathcal B$.
\begin{proposition}[exact rational Hodge certificate]
\label{prop:p13-certificate}
Let $P_{13}$ be the group on the six ordered generators
\[
 e_{12},e_{13},e_{21},e_{23},e_{31},e_{32}.
\]
Its first six relators are
\begin{align*}
 [e_{12},e_{23}]e_{13}^{-1},\quad
 [e_{13},e_{32}]e_{12}^{-1},\quad
 [e_{21},e_{13}]e_{23}^{-1},\\
 [e_{23},e_{31}]e_{21}^{-1},\quad
 [e_{31},e_{12}]e_{32}^{-1},\quad
 [e_{32},e_{21}]e_{31}^{-1};
\end{align*}
the next six are the commutators of the displayed commuting-root pairs
\[
 (12,13),\ (12,32),\ (13,23),\ (21,23),\ (21,31),\ (31,32),
\]
and the last is $(e_{12}e_{21}^{-1}e_{12})^4$.

Put $D_i=e_i-1\in\mathbb Q[P_{13}]$, and let $B_1$ be the
$13\times6$ Fox-boundary matrix of the displayed relators.  Fox
telescoping gives $B_1D=0$.  A finite certificate, whose entries are exact
rational data, gives a matrix
$Q\in M_{102\times6}(\mathbb Q[P_{13}])$ supported on 22 explicit words
in the certificate, with denominator $10^8$, and
$R\in M_6(\mathbb Q[P_{13}])$ such that
\begin{equation}\label{eq:p13-hodge}
 B_1^*B_1+DD^*=\frac1{250}I_6+Q^*Q+R.
\end{equation}
The 22-by-22 support products are accompanied by explicit relator replays;
they are collected into 293 finite coordinates.  Further collisions can
only decrease the true group-ring coefficient $\ell^1$-norm.  Both
orientations of the residual estimate are checked:
\begin{equation}\label{eq:p13-residual}
 \max_i\sum_k\lVert R_{ik}\rVert_1,\quad
 \max_k\sum_i\lVert R_{ik}\rVert_1
 \ \le\ \rho:=\frac{11670886519714}{10^{16}}<\frac1{250}.
\end{equation}
Thus the certified gap is
\begin{equation}\label{eq:p13-gap}
 \gamma=\frac1{250}-\rho
       =\frac{28329113480286}{10^{16}}>0.
\end{equation}
Let $\varepsilon_{13}>0$ be the explicit certified tolerance obtained from
these rational data by the formula in the exact declaration.  Then the six
displayed generators, with tolerance $\varepsilon_{13}$, form a Kazhdan pair
for $P_{13}$.  Consequently $P_{13}$ has property~\textup{(T)} in both the
real-orthogonal and textbook complex-unitary formulations, in every
representation-space universe.
\leanverified{Sofic/LiteralP13HodgeCertificate}{GroupApproximation.LiteralP13HodgeCertificate.manuscriptP13HodgeCertificate}
\end{proposition}

\begin{proof}
The relator replay, Fox identity, coefficient decomposition, and rational
row and column bounds are the finite checks recorded in the proposition.
For the analytic passage, let $\pi$ be an orthogonal representation on a
real Hilbert space and put
\[
 A=\sum_i(\pi(e_i)-1)^*(\pi(e_i)-1),\qquad
 z_i=(\pi(e_i)-1)\xi.
\]
The identity $B_1D=0$ and \eqref{eq:p13-hodge} give
\[
 \gamma\langle A\xi,\xi\rangle\le\lVert A\xi\rVert^2.
\]
With $M=\max\{\gamma,24\}$ and
\[
 \sigma=\gamma\bigl(1-\sqrt{1-\gamma/M}\bigr),
\]
the usual self-adjoint gap estimate gives
$\sigma\lVert\xi\rVert\le\lVert A\xi\rVert$ on the subspace without
invariant vectors.  Since
$\lVert A\xi\rVert\le2\sum_i\lVert\pi(e_i)\xi-\xi\rVert$, the six
generators form a Kazhdan pair with tolerance $\sigma/12$, which is the
certified value $\varepsilon_{13}$.  Realification and complexification
identify the real and textbook complex formulations.
\end{proof}

Figure~\ref{fig:hodge-certificate} records the scale of the exact
certificate.  Each heat-map cell represents an upper bound for the
coefficient $\ell^1$-mass of one block of $R$; both its row and column sums
are checked.
\begin{figure}[H]
\centering
\begin{tikzpicture}[x=6.4mm,y=6.4mm,font=\scriptsize]
  \node[anchor=west,font=\small\bfseries] at (-1.25,1.45)
    {Residual block bounds};
  \foreach \i/\lab in {0/12,1/13,2/21,3/23,4/31,5/32} {
    \node[anchor=east] at (-.18,-\i-.5) {$\lab$};
    \node at (\i+.5,.30) {$\lab$};
  }
  \foreach \i in {0,...,5} {
    \foreach \j in {0,...,5} {
      \pgfmathparse{\i==\j ? 68 : 10}
      \edef\shade{\pgfmathresult}
      \fill[blue!\shade] (\j,-\i) rectangle +(1,-1);
      \draw[white,line width=.45pt] (\j,-\i) rectangle +(1,-1);
    }
  }
  \draw[line width=.55pt] (0,0) rectangle (6,-6);
  \node[rotate=90,anchor=south] at (-1.0,-3) {row root};
  \node[anchor=south] at (3,.72) {column root};
  \node[align=left,anchor=north west,text width=4.5cm] at (-.05,-6.45)
    {dark diagonal: $1.162\times10^{-3}$ scale\\
     pale off-diagonal: below $9.3\times10^{-7}$};

  \begin{scope}[xshift=6.0cm,yshift=-.9cm,x=.88cm,y=1cm]
    \node[anchor=west,font=\small\bfseries] at (0,1.15)
      {Certified gap};
    \node[anchor=west] at (0,.72) {$c=1/250=0.004$};
    \fill[orange!42] (0,0) rectangle (1.28,.58);
    \fill[teal!58] (1.28,0) rectangle (4.4,.58);
    \draw[line width=.6pt] (0,0) rectangle (4.4,.58);
    \draw[line width=.6pt] (1.28,0) -- (1.28,.58);
    \fill[orange!42] (0,-.48) rectangle +(.24,-.24);
    \node[anchor=west] at (.38,-.60)
      {$\rho=1.167088652\times10^{-3}$};
    \fill[teal!58] (0,-1.08) rectangle +(.24,-.24);
    \node[anchor=west] at (.38,-1.20)
      {$\gamma=2.832911348\times10^{-3}$};
    \node[anchor=west,font=\small] at (0,-1.78)
      {$\rho+\gamma=c$};
    \node[anchor=west,align=left,text width=4.4cm] at (0,-2.40)
      {The same $\rho$ bounds every checked row sum and every checked column sum.};
  \end{scope}
\end{tikzpicture}
\caption{Exact Hodge-certificate scale.  The residual grid distinguishes the
diagonal and off-diagonal block bounds; the separate bar partitions
$c=1/250$ into the certified residual radius $\rho$ and positive gap
$\gamma=c-\rho$.}
\label{fig:hodge-certificate}
\end{figure}

To pass from $P_{13}$ to the literal base, substitute
\begin{align*}
e_{12}&=y^{-1}xzy^{-1}x^{-1},&
e_{13}&=xzy^{-1}x^{-1}y^{-1},&
e_{21}&=y^{-1}xyxz,\\
e_{23}&=x^{-1}y^{-1}xzy^{-1},&
e_{31}&=xy^{-1}xzy^{-1}x,&
e_{32}&=x^{-1}zy^{-1}x^{-1}y^{-1}x^{-1}.
\end{align*}
Every substituted $P_{13}$ relator is a checked product of conjugates of
the eight relators of $\mathcal R$.  The words
\[
 e_{12}e_{21}^{-1}e_{31}^{-1}e_{12}e_{13}e_{31}^{-2}e_{13},
 \qquad e_{12}^{-1}e_{13}^{-1}e_{31}
\]
map respectively to $z$ and $xy$, so the resulting homomorphism
$P_{13}\to\mathcal R$ contains $z$ and $xy$ in its image.  A separate
31-step relator replay in the literal eight-relator presentation proves
$\mathcal R=\langle z,xy\rangle$: it writes $y$ as a word in $z$ and $xy$,
after which $x=(xy)y^{-1}$.  Hence the homomorphism is surjective, and
property~\textup{(T)} descends to $\mathcal R$.

Finally let $(S,\kappa)$ be a Kazhdan pair for $\mathcal R$, with
$\kappa\le1$.  For the image of $S\cup\{v_2,v_3\}$ in $\mathcal B$, use
tolerance $\kappa/64$.  Project an almost invariant unit vector to an
$\mathcal R$-fixed vector $p$ within $1/64$.  The literal conjugation
relations express every translation as a product of two
$\mathcal R$-conjugates; hence every translation moves $p$ by at most
$1/8$.  The bounded-orbit fixed-point theorem bounds the distance from $p$
to the closed translation-fixed subspace by $1/8$.  Let $q$ be the
orthogonal projection of $p$ onto that subspace.  Rotations normalize the
translation subgroup and fix $p$; uniqueness and equivariance of orthogonal
projection therefore make $q$ rotation-fixed as well.  Hence $q$ is fixed by
all of $\mathcal B$.  Moreover
$\lVert q-p\rVert\le1/8$ and $\lVert p-x\rVert<1/64$, so
$\lVert q-x\rVert<1$; since $x$ is a unit vector, $q\ne0$.

\begin{proposition}\label{prop:literal-base-T}
The literal twenty-relator group $\mathcal B$ has property~\textup{(T)}.
\leanverified{Sofic/LiteralBaseP13PropertyTBridge}{GroupApproximation.LiteralBaseP13PropertyTBridge.manuscriptBaseHasKazhdanPropertyT}
\end{proposition}

\section{The witness: \texorpdfstring{$w\neq 1$}{w is nontrivial}}\label{sec:witness}

Clifford signs, shifts, and HNN doubling have appeared together before in
approximate-representation theory, notably in Slofstra's hyperlinear-profile
construction \cite[Section~2]{SlofstraProfile}.  The construction below uses
the Clifford lamp only as an explicit algebraic witness; it makes no
identification of the literal base with the matrix realization used here.

\begin{construction}[Clifford lamp groups]\label{con:clifford}
For a set $X$, form the real Clifford algebra on generators $(c_x)_{x\in X}$
and let $\ClLamp(X)$ be its subgroup of units generated by $-1$ and the
$c_x$.  Write $\zeta=-1$.  In this concrete group,
\[
 \zeta^2=c_x^2=1,\qquad c_xc_y=\zeta c_yc_x\quad(x\ne y).
\]
The sign $\zeta$ is nontrivial.  Every permutation of $X$ acts on this
group by $c_x\mapsto c_{\sigma(x)}$ and fixes $\zeta$.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptCliffordConstruction}
\end{construction}

\begin{lemma}[linear model]\label{lem:linear}
For $M\in\GL_3(\mathbb Q)$ and $u\in\mathbb Q^3$, write
\[
 A(M,u)=\begin{pmatrix}M&u\\0&1\end{pmatrix}.
\]
Assign $v_i\mapsto A(I_3,e_i)$ and
\[
\begin{aligned}
 x&\mapsto A\!\left(\begin{pmatrix}0&1&0\\0&0&1\\1&0&0\end{pmatrix},0\right),
&y&\mapsto A\!\left(\begin{pmatrix}1&0&1\\0&-1&-1\\0&1&0\end{pmatrix},0\right),\\[3pt]
 z&\mapsto A\!\left(\begin{pmatrix}0&1&0\\1&0&0\\-1&-1&-1\end{pmatrix},0\right).
\end{aligned}
\]
Let $\bar\Gamma\le\GL_4(\mathbb Q)$ be the subgroup generated by these six
matrices, and put
$D=\operatorname{diag}(2,2,2,1)$.  Direct matrix computation proves that
the six matrices satisfy all twenty relators of $\mathcal B$ and that
\[
 \bar\alpha(u)=DuD^{-1}
\]
is an injective endomorphism of $\bar\Gamma$ which squares the three
translation generators and fixes $x,y,z$.  Moreover the matrix assigned to
$v_1$ does not lie in $\operatorname{range}(\bar\alpha)$.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptLinearModel}
\end{lemma}

\begin{proof}
The twenty relator checks reduce to sixteen rational identities for each
matrix equation.  Conjugation by $D$ gives the six stable-letter equations
and is injective because it is conjugation by an invertible matrix.  Every
generator of $\bar\Gamma$ has integral entries, and integrality is preserved
under products and inverses; however $D^{-1}\bar v_1D$ has entry $1/2$ in
position $(1,4)$.  Thus it is not in $\bar\Gamma$, which proves the last
assertion.
\end{proof}

Form the mapping telescope of $\bar\alpha$, extend it by its shift $\tau$,
and call the resulting group $V$.  Let
$j\colon\bar\Gamma\hookrightarrow V$ be its level-zero map and put
$X=V/j(\bar\Gamma)$, with root $o=j(\bar\Gamma)$.  The final assertion of
Lemma~\ref{lem:linear} is exactly the statement that
\[
 \tau o\ne j(\bar v_1)\tau o.
\]
Thus the Clifford generators at these two sites anticommute.

\begin{proposition}\label{prop:witness}
$w\neq1$ in $E$.
\leanverified{Sofic/LiteralNonMFLinearWitness}{GroupApproximation.LiteralNonMFLinearWitness.literal_mark_ne_one}
\end{proposition}

\begin{proof}
Form $W:=\ClLamp(X)\rtimes V$ as in Construction~\ref{con:clifford}.
Define a map on the generators of \eqref{eq:E} by
\[
\begin{gathered}
  g\mapsto j(\bar g)\in V\quad
  (g\in\{v_1,v_2,v_3,x,y,z\}),\\
  t\mapsto\tau,\qquad c\mapsto c_o .
\end{gathered}
\]
The exact checks in Lemma~\ref{lem:linear} give the twenty base and six
stable-letter relations.  The level-zero subgroup fixes $o$, so $c_o$
commutes with every base generator, and $c_o^2=1$.
Under this assignment,
\[
  tct^{-1}\mapsto c_{\tau o},\qquad
  v_1(tct^{-1})v_1^{-1}\mapsto c_{j(\bar v_1)\tau o},
\]
and the two sites are distinct by Lemma~\ref{lem:linear}, so
\[
  w\mapsto[c_{\tau o},c_{j(\bar v_1)\tau o}]=-1=\zeta .
\]
Since $\zeta$ is a central involution of $W$, the remaining relations
$w^2=1$ and $[w,g]=1$ for
$g\in\{v_1,v_2,v_3,x,y,z,t,c\}$ are also satisfied.  Hence the
assignment extends to a group homomorphism $E\to W$ sending $w$ to
$\zeta\neq1$, and therefore $w\neq1$ in $E$.
\end{proof}

\paragraph{Scope of the witness.}
The target uses the infinite group $\ClLamp(X)$ when $X$ is
infinite, but each group element involves only finitely many Clifford sites;
the remaining data are rational matrices.  No normal-form theorem for an HNN
extension enters: the mapping telescope supplies the required group and the
range characterization separates the two sites.

\section{The finite-dimensional obstruction}\label{sec:findim}

We prove Theorem~\ref{thm:B}.  Everything happens inside the
finite-dimensional algebra $\End_k(V)$.

\begin{proof}[Proof of Theorem~\ref{thm:B}]
Let $\pi\colon H\to\GL(V)$ be a homomorphism and let
\[
  C\;=\;\{\,x\in\End_k(V)\;:\;\pi(\gamma)\,x=x\,\pi(\gamma)
  \text{ for all }\gamma\in\Gamma\,\}
\]
be the commutant of $\pi(\Gamma)$, a linear subspace of $\End_k(V)$.
Let $u=\pi(t)$ and consider the linear automorphism
$\Phi(x)=uxu^{-1}$ of $\End_k(V)$.

We claim $C\subseteq\Phi(C)$.  Let $x\in C$ and set $y=u^{-1}xu$, so
that $x=\Phi(y)$.  For $\gamma\in\Gamma$ we have
$t\gamma t^{-1}\in\Gamma$, hence $\pi(t\gamma t^{-1})$ commutes with
$x$, and therefore
\[
  \pi(\gamma)\,y
  = u^{-1}\,\pi(t\gamma t^{-1})\,x\,u
  = u^{-1}\,x\,\pi(t\gamma t^{-1})\,u
  = y\,\pi(\gamma).
\]
Thus $y\in C$, proving the claim.  Since $\Phi$ is a linear
isomorphism, $\dim_k\Phi(C)=\dim_kC<\infty$, and the inclusion
$C\subseteq\Phi(C)$ forces
\begin{equation}\label{eq:pinning}
  C\;=\;\Phi(C)\;=\;uCu^{-1}.
\end{equation}

Now $\pi(c)\in C$ because $c$ centralizes $\Gamma$.  By
\eqref{eq:pinning}, $\pi(tct^{-1})=u\,\pi(c)\,u^{-1}\in C$.  Since
$a\in\Gamma$, the element $\pi(tct^{-1})$ commutes with $\pi(a)$, so
$\pi\bigl(a(tct^{-1})a^{-1}\bigr)=\pi(tct^{-1})$, and therefore
\[
  \pi\bigl(\bigl[\,tct^{-1},\;a(tct^{-1})a^{-1}\bigr]\bigr)
  =\bigl[\,\pi(tct^{-1}),\;\pi(tct^{-1})\bigr]=1. \qedhere
\]
\end{proof}

The same calculation proves the final, subgroup-valued assertion of
Theorem~\ref{thm:B}.  The elements of $H$ whose conjugation action
preserves $C$ in both directions form a subgroup.  The argument above
puts every one-sided compressor in that subgroup, and therefore puts all
of $G_{\rm comp}(\Gamma)$ in it.  Thus, for $g\in
G_{\rm comp}(\Gamma)$ and $z\in C_H(\Gamma)$, the operator
$\pi(gzg^{-1})$ still belongs to $C$ and commutes with every
$\pi(\ell)$, $\ell\in\Gamma$.  Hence $\pi$ kills all the generators
in \eqref{eq:intrinsicdefect}, and normality of its kernel kills their
normal closure.

\begin{corollary}\label{cor:notRFD}
The group $E$ of Definition~\ref{def:E} admits no injective homomorphism
into $\GL(V)$ for any finite-dimensional vector space $V$ over any field.
More strongly, every finite-dimensional linear representation maps $w$ to
the identity, and every homomorphism to a finite group maps $w$ to the
identity.
\leanverified{Sofic/LiteralFiniteDimensionalObstruction}{GroupApproximation.LiteralFiniteDimensionalObstruction.manuscriptFiniteDimensionalConsequences}
\end{corollary}

\begin{proof}
Apply Theorem~\ref{thm:B} with $H=E$, the image of $\mathcal B$, and the
elements $t,c,v_1$; the hypotheses hold by the defining relations
\eqref{eq:E}.  Every finite-dimensional representation kills $w$, and
$w\neq1$ by Proposition~\ref{prop:witness}.  A finite target embeds into a
finite symmetric group and hence into a finite-dimensional general linear
group via permutation matrices, so it also maps $w$ to the identity.
\end{proof}

\paragraph{Why the corona argument is different.}
The proof of Theorem~\ref{thm:B} used only that a finite-dimensional
subspace cannot be properly contained in an isomorphic image of itself.
An MF model is only asymptotically multiplicative, so its
fixed spaces are not literal invariant subspaces.  Property~(T) repairs
this at finite stages: a uniform spectral cut of the adjoint averages
captures the almost-fixed vectors.  One-sided compression gives a leakage
estimate between two equal-rank coordinate projections, and the equal-rank
flip supplies the reverse estimate.  This is the route used below.

\section{Norm matrix coronas}\label{sec:corona}

Throughout this section $\mathcal Q=\mathcal Q((d_n)_{n\in\N})$ is a
norm matrix corona,
$q\colon\prod_{n\in\N}^{\ell^\infty}M_{d_n}(\C)\to\mathcal Q$ the
quotient map.

The following lifting facts are standard perturbation theory; see
Loring \cite{Loring} for a systematic treatment.

\begin{lemma}[lifting]\label{lem:lift}
Every unitary $u\in\mathcal Q$ lifts to a sequence of unitaries.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptUnitaryLifting}
\end{lemma}

\begin{proof}
Lift $u$ to an arbitrary bounded sequence $(x_n)$.  Since
$q((x_n))$ is unitary, both
$\opnorm{x_n^*x_n-1}\to0$ and $\opnorm{x_nx_n^*-1}\to0$.  In particular,
$x_n^*x_n$ is invertible, and hence $x_n$ is invertible, for all sufficiently
large $n$.  At those indices put
$u_n=x_n(x_n^*x_n)^{-1/2}$.  Then $u_n$ is unitary, and, once
$\opnorm{x_n^*x_n-1}\le\tfrac12$, continuous functional calculus gives the
quantitative estimate
\[
 \opnorm{u_n-x_n}
 \le 2\opnorm{x_n}\,\opnorm{x_n^*x_n-1}.
\]
The sequence $(x_n)$ is bounded, so the right-hand side tends to $0$.
Setting $u_n=1$ at the finitely many remaining indices therefore gives a
unitary sequence with $\opnorm{u_n-x_n}\to0$.
\end{proof}

\begin{lemma}[unitaries in the quotient]\label{lem:unitarycorona}
For every sequence $(d_n)$, the map
\[
 \kappa_{(d_n)}\colon \Ucor((d_n))\longrightarrow\U(\mathcal Q),
 \qquad [(u_n)]\longmapsto q((u_n)),
\]
is a canonical group isomorphism.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptUnitaryCoronaEquivalence}
\end{lemma}

\begin{proof}
The map is well defined, and its kernel consists exactly of the unitary
sequences satisfying $\opnorm{u_n-1}\to0$, so it is injective.  It is
surjective by Lemma~\ref{lem:lift}: every unitary in $\mathcal Q$ has a
unitary lift $(u_n)$, whose class in \eqref{eq:unitary-corona} maps to it.
\end{proof}

\section{Kazhdan compression and the finite-normal corner}\label{sec:conj}

The corona half of Theorem~\ref{thm:A} will be deduced from a general
criterion, whose data we now fix.

\begin{definition}[marked Kazhdan pattern]\label{def:pattern}
Let $H$ be a countable group.  A \emph{marked Kazhdan pattern} in $H$
is a tuple $(\Lambda,\iota,t,c)$, where $\Lambda$ is a countable
group with property (T), $\iota\colon\Lambda\to H$ is a group
homomorphism --- \emph{not} assumed injective --- and $t,c\in H$,
subject to
\begin{enumerate}
  \item[(M1)] $t\,\iota(\lambda)\,t^{-1}\in\iota(\Lambda)$ for every
    $\lambda\in\Lambda$;
  \item[(M2)] $c\,\iota(\lambda)=\iota(\lambda)\,c$ for every
    $\lambda\in\Lambda$.
\end{enumerate}
Write $d:=tct^{-1}$.  The \emph{compression-defect subgroup} of the
pattern is the normal closure
\[
  N_{\mathrm{comp}}
  \;=\;\bigl\langle\!\bigl\langle\,
  [\,d,\iota(\lambda)\,]\;:\;\lambda\in\Lambda
  \,\bigr\rangle\!\bigr\rangle_H\;\trianglelefteq\;H .
\]
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptMarkedKazhdanPattern}
\end{definition}

\begin{theorem}[finite-normal obstruction criterion]\label{thm:criterion}
Let $H$ be a countable group with a marked Kazhdan pattern
$(\Lambda,\iota,t,c)$, and let $F\trianglelefteq H$ be a finite
normal subgroup with $F\subseteq N_{\mathrm{comp}}$.  Then every
corona representation $\Theta\colon H\to\U(\mathcal Q)$ satisfies
$\Theta(f)=1$ for all $f\in F$.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptFiniteNormalObstructionCriterion}
\end{theorem}

Theorem~\ref{thm:A}(2) is the instance \(H=E\),
\(\Lambda=\mathcal B\), \(\iota\) the canonical homomorphism, and
\(F=\{1,w\}\).  The membership \(w\in N_{\mathrm{comp}}\) is the identity
\(w=[d,\iota(v_1)]^2\) of Lemma~\ref{lem:square}.
The proof uses the following coordinate construction.  This formulation is
important: the Reynolds average is formed before passing to any quotient,
and its complementary spectral corner is repaired coordinate by coordinate.

\begin{lemma}[coordinate finite-normal corner]\label{lem:corner}
Let $F\trianglelefteq H$ be finite, let $\omega$ be an ultrafilter on an
index set $I$, and let
$\rho$ be a homomorphism from $H$ into the operator-norm metric ultraproduct
of the groups $\U(M_{d_i}(\C))$, $i\in I$.  If $\rho|_F$ is nontrivial,
then there are nonempty finite coordinate sets $Y_n$ and unitaries
$V_{g,n}\in\U(M_{Y_n}(\C))$ such that
\[
 \opnorm{V_{g,n}V_{h,n}-V_{gh,n}}\longrightarrow0
 \quad(g,h\in H),
 \qquad
 \opnorm{\sum_{f\in F}V_{f,n}}\longrightarrow0.
\]
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptCoordinateFiniteNormalCorner}
\end{lemma}

\begin{proof}
Choose $f_0\in F$ with $\rho(f_0)\neq1$.  A standard sequential extraction
from the ultraproduct gives representatives $U_{g,n}$ which are
asymptotically multiplicative in operator norm and a constant $\delta>0$
such that
\[
  \opnorm{U_{f_0,n}-U_{1,n}}\ge\delta
\]
for every $n$.  We now construct the corner model from these coordinates.
Put
\[
 A_n=\frac1{|F|}\sum_{f\in F}U_{f,n},
 \qquad H_n=\frac12(A_n+A_n^*).
\]
Left and right translation permute $F$, while conjugation by any $g\in H$
permutes $F$ by normality.  Expanding the finitely many multiplicative
defects therefore shows, in operator norm, that
\[
 A_n^2-A_n\longrightarrow0,\qquad A_n-H_n\longrightarrow0,
 \qquad U_{g,n}A_nU_{g,n}^*-A_n\longrightarrow0.
\]
Let $P_n=\chi_{(1/2,\infty)}(H_n)$ and $Q_n=1-P_n$.  Spectral rounding gives
\[
 \opnorm{P_n-H_n}\le
 2\opnorm{H_n^2-H_n}\longrightarrow0,
\]
so $P_n-A_n\to0$ and
$U_{g,n}P_nU_{g,n}^*-P_n\to0$ for every fixed $g$.  The separation of
$f_0$ forces $Q_n\neq0$ eventually: if $Q_n=0$, then $P_n=1$, and the
relations $U_{f_0,n}A_n-A_n\to0$, $P_n-A_n\to0$, and
$U_{1,n}-1\to0$ would imply
$\opnorm{U_{f_0,n}-U_{1,n}}<\delta$.

Conjugate into an eigenbasis of $H_n$ and let $C_{g,n}$ be the principal
block of $U_{g,n}$ on $Q_n\C^{d_n}$; put
$r_n=\operatorname{rank}Q_n>0$.  The preceding
commutator estimate makes both off-diagonal blocks tend to zero.  Hence
\[
 \opnorm{C_{g,n}C_{h,n}-C_{gh,n}}\longrightarrow0,
 \qquad
 \opnorm{C_{g,n}^*C_{g,n}-1}\longrightarrow0.
\]
For each fixed $g$ and all sufficiently large $n$ the compressed Gram
matrix is invertible; take the polar correction
\[
 V_{g,n}=C_{g,n}(C_{g,n}^*C_{g,n})^{-1/2}.
\]
When the Gram defect is at most $1/2$, functional calculus gives the
dimension-free estimate
\[
 \opnorm{V_{g,n}-C_{g,n}}
 \le2\opnorm{C_{g,n}^*C_{g,n}-1},
\]
For each fixed $g$, use this polar unitary on the cofinite tail and set
$V_{g,n}=1$ at its finitely many exceptional indices.  Thus polar correction
is asymptotically invisible and the $V_{g,n}$ remain asymptotically
multiplicative.

Finally, the normalized sum of the uncorrected corner matrices is precisely
the $Q_n$-block of $A_n$.  Since $P_n-A_n\to0$ and $Q_nP_n=0$,
\[
 \opnorm{\sum_{f\in F}C_{f,n}}\longrightarrow0.
\]
There are only finitely many $f\in F$, so replacing every $C_{f,n}$ by its
polar correction preserves this limit and proves the lemma.
\end{proof}

The second ingredient is the finite-stage Kazhdan-compression calculation.

\begin{lemma}[compression defects collapse]\label{lem:compressorcollapse}
Let $(V_{g,n})$ be any operator-norm asymptotic unitary representation of
$H$.  For a marked Kazhdan pattern $(\Lambda,\iota,t,c)$, with
$d=tct^{-1}$, for every $\lambda\in\Lambda$ and $\varepsilon>0$ there is
$N$ such that, for all $n\ge N$,
\[
 \hsnorm{V_{[d,\iota(\lambda)],n}-V_{1,n}}^2\le\varepsilon.
\]
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptCompressionDefectsCollapse}
\end{lemma}

\begin{proof}
Apply the adjoint microstates $\Ad V_{\iota(\lambda),n}$ to
$L^2(M_{r_n},\tr_{r_n})$.  Choose a finite symmetric Kazhdan set
$S\subseteq\Lambda$, with $1\in S$, generating $\Lambda$, and a Kazhdan
constant $0<\kappa\le1$.  Fix
\[
  1-\frac{\kappa^2}{4|S|}<\theta<1,
\]
let $H_n$ be the Hermitian average of the adjoint $S$-microstates, and put
$P_n=\chi_{(\theta,\infty)}(H_n)$.  Spectral capture gives, for every fixed
$\lambda\in\Lambda$,
\[
 \opnorm{(\Ad V_{\iota(\lambda),n}-1)P_n}\longrightarrow0.
\]
The constants can be fixed uniformly in the matrix size.  Given a requested
squared Hilbert--Schmidt error $\varepsilon>0$, set
\[
 \eta=\frac{(1-\theta)\varepsilon}{192}.
\]
At a sufficiently large common stage, each of the two conjugation defects of
$V_{c,n}$ associated to every $s\in S$ is at most $\eta$ in operator norm.
The Hermitian average therefore has residual norm at most $\eta$, and the
spectral-cut inequality gives the normalized root-capture estimate
\begin{equation}\label{eq:root-capture-budget}
 \frac1{r_n}\bigl\|(1-P_n)\operatorname{vec}(V_{c,n})\bigr\|_2^2
 \le \frac{\varepsilon}{192}.
\end{equation}

For each generator in the Kazhdan set, (M1) supplies a
$\lambda'\in\Lambda$ with
$t\iota(\lambda)t^{-1}=\iota(\lambda')$.  The multiplicative defects then
show that the adjoint-conjugated spectral corner has asymptotically no leakage
below the same cut.  More precisely, if
$R_n=(\Ad V_{t,n})P_n(\Ad V_{t,n})^*$, this gives
\[
 \opnorm{(1-R_n)P_n}\longrightarrow0.
\]
They have equal rank at every coordinate.  The finite-dimensional
projection flip, applied with
$\varepsilon'=\min\{1/2,\varepsilon/2\}$, bounds the reverse leakage by
$\varepsilon'/\sqrt{1-(\varepsilon')^2}\le2\varepsilon'\le\varepsilon$.
Thus
\[
 \opnorm{(1-P_n)R_n}\longrightarrow0,
\]
and hence
\[
 \opnorm{P_n-R_n}\longrightarrow0.
\]
This equal-rank projection argument is the finite-dimensional replacement for
the stable-finiteness step one might be tempted to perform only after taking
a $C^*$-quotient; Figure~\ref{fig:equal-rank-flip} depicts the geometry.

\begin{figure}[H]
\centering
\begin{tikzpicture}[x=1cm,y=1cm,font=\small]
  \draw[rounded corners=4pt,line width=.65pt,fill=black!2]
    (-5.15,-1.75) rectangle (5.15,1.78);

  \node[draw=blue!55!black,rounded corners=3pt,fill=blue!5,
    inner sep=4pt,font=\footnotesize] at (-2.55,1.32)
    {$\opnorm{(1-R_n)P_n}\to0$};
  \node[draw=teal!55!black,rounded corners=3pt,fill=teal!5,
    inner sep=4pt,font=\footnotesize] at (2.55,1.32)
    {$\opnorm{(1-P_n)R_n}\to0$};

  \begin{scope}[shift={(-2.55,-.05)}]
    \fill[blue!12,draw=blue!65!black,line width=.8pt]
      (-1.55,-.47) -- (1.42,.30) -- (1.66,.71) -- (-1.31,-.06) -- cycle;
    \node[fill=white,fill opacity=.86,text opacity=1,inner sep=1.5pt]
      at (0,.12) {$\operatorname{ran}P_n$};
  \end{scope}
  \begin{scope}[shift={(2.55,-.05)}]
    \fill[teal!14,draw=teal!65!black,line width=.8pt]
      (-1.52,-.44) -- (1.45,.33) -- (1.68,.74) -- (-1.29,-.03) -- cycle;
    \node[fill=white,fill opacity=.86,text opacity=1,inner sep=1.5pt]
      at (0,.12) {$\operatorname{ran}R_n$};
  \end{scope}
  \node[draw,rounded corners=3pt,fill=white,inner sep=5pt,
    align=center,font=\footnotesize] at (0,.42)
    {$\operatorname{rank}P_n=\operatorname{rank}R_n$};
  \node[draw,rounded corners=3pt,fill=black!4,inner sep=5pt,
    align=center,font=\small,text width=6.1cm] at (0,-1.20)
    {one-sided leakage $+$ equal rank $\Longrightarrow$
     reverse leakage $\Longrightarrow\opnorm{P_n-R_n}\to0$};
\end{tikzpicture}
\caption{Equal-rank reversal.  One-sided spectral leakage between the two
schematic ranges, together with equality of their finite dimensions, forces
the reverse leakage and hence convergence of the two projections.}
\label{fig:equal-rank-flip}
\end{figure}

Because $c$ centralizes $\iota(\Lambda)$, the vector represented by
$V_{c,n}$ is asymptotically captured by $P_n$.  The equality just obtained
transports that capture to $V_{d,n}$; hence conjugation by
$V_{\iota(\lambda),n}$ moves $V_{d,n}$ by a quantity tending to zero in
normalized Hilbert--Schmidt norm.  Unitary invariance identifies this
quantity with
$\hsnorm{V_{[d,\iota(\lambda)],n}-V_{1,n}}$, up to the vanishing
multiplicative defects.  More explicitly, the finite-stage vector chain gives
\begin{equation}\label{eq:vector-chain-budget}
 \operatorname{dist}_{2,n}([d,\iota(\lambda)],1)^2
 \le 8\delta_n^2+64\ell_n^2+64\rho_n,
\end{equation}
where $\delta_n$ is the fixed-element Kazhdan displacement, $\ell_n$ is the
reverse compressor leakage, and $\rho_n$ is the normalized mass in
\eqref{eq:root-capture-budget}.  Choose their eventual bounds as
\[
 \delta_n\le\sqrt{\varepsilon/24},\qquad
 \ell_n\le\sqrt{\varepsilon/192},\qquad
 \rho_n\le\varepsilon/192.
\]
Then the right side of \eqref{eq:vector-chain-budget} is at most
\[
 8\frac{\varepsilon}{24}
 +64\frac{\varepsilon}{192}
 +64\frac{\varepsilon}{192}=\varepsilon.
\]
This proves the claim with constants independent of $r_n$.  These are the
same $8/64/64$ finite-stage estimates used by the formal proof; no passage to
an abstract limiting projection is hidden here.
\end{proof}

\section{Proof of the criterion and of Theorem \ref{thm:A}}\label{sec:proofA}

\begin{proof}[Proof of Theorem~\ref{thm:criterion}]
Suppose, for contradiction, that some corona representation
$\Theta\colon H\to\U(\mathcal Q)$ is nontrivial on $F$.  Compose with
$\kappa_{(d_n)}^{-1}$ to obtain its coordinate model.  For some $f_0\in F$
the distance from its image to $1$ has positive limit superior.  Pass to a
subsequence on which that distance is uniformly positive and then to any
free ultrafilter on the subsequence.  Lemma~\ref{lem:corner} produces an
operator-norm asymptotic unitary
representation $(V_{g,n})$ such that
\begin{equation}\label{eq:corner-sum}
  \opnorm{\sum_{f\in F}V_{f,n}}\longrightarrow0.
\end{equation}

Regard the same matrices as an asymptotic representation for the normalized
Hilbert--Schmidt metric, and fix a free ultrafilter $\omega$ containing the
cofinite filter.  They define a homomorphism $\rho_2$ into the corresponding
tracial unitary ultraproduct.  By Lemma~\ref{lem:compressorcollapse},
\[
 \rho_2([d,\iota(\lambda)])=1
 \qquad(\lambda\in\Lambda).
\]
The kernel of a homomorphism is normal; hence it contains
$N_{\mathrm{comp}}$, and therefore $F$.  Consequently, for every $f\in F$,
\begin{equation}\label{eq:finite-hs-collapse}
 \lim_{n\to\omega}\hsnorm{V_{f,n}-V_{1,n}}=0.
\end{equation}

To return from the ultrafilter statement to an ordinary sequence, put
$\eta_k=1/(k+1)$.  Since $F$ is finite, for every $k$ the set of indices
$n\ge k$ for which
\[
 \hsnorm{V_{f,n}-V_{1,n}}^2<\eta_k\qquad(f\in F)
\]
belongs to $\omega$ and is therefore nonempty.  Choose one such index
$\phi(k)\ge k$ and reindex by $W_{g,k}=V_{g,\phi(k)}$.  Cofinality of
$\phi$ preserves asymptotic multiplicativity and
\eqref{eq:corner-sum}, while
\[
 \hsnorm{W_{f,k}-W_{1,k}}\longrightarrow0
 \qquad(f\in F).
\]
Because $F$ is finite,
\[
 \hsnorm{\sum_{f\in F}W_{f,k}-|F|W_{1,k}}
 \le\sum_{f\in F}\hsnorm{W_{f,k}-W_{1,k}}\longrightarrow0.
\]
On the other hand, $W_{1,k}$ is unitary, so
$\hsnorm{|F|W_{1,k}}=|F|$, whereas
\[
 \hsnorm{\sum_{f\in F}W_{f,k}}
 \le\opnorm{\sum_{f\in F}W_{f,k}}\longrightarrow0
\]
by \eqref{eq:corner-sum} after reindexing.  The triangle inequality would
force $|F|=0$, a contradiction.  Thus every corona representation kills
$F$.
\end{proof}

\begin{lemma}[the marked word is a defect square]\label{lem:square}
Let $H_1$ be a group and $d,a_1\in H_1$ with $d^2=1$.  Then
\[
  \bigl[\,d,\;a_1\,d\,a_1^{-1}\bigr]\;=\;[\,d,a_1\,]^{\,2} .
\]
\leanverified{Sofic/CompressionDefectSquare}{GroupApproximation.commutator_conjugate_eq_commutator_sq_of_sq_eq_one}
\end{lemma}

\begin{proof}
Put $b=a_1da_1^{-1}$, so $b^2=a_1d^2a_1^{-1}=1$; thus $b^{-1}=b$ and
$d^{-1}=d$.  Then $[d,a_1]=da_1d^{-1}a_1^{-1}=d\,(a_1da_1^{-1})=db$,
and
\[
  [d,a_1]^2=(db)^2=db\,db=d\,b\,d^{-1}b^{-1}=[d,b] . \qedhere
\]
\end{proof}

\begin{proof}[Proof of Theorem~\ref{thm:A}]
Part (1) is Proposition~\ref{prop:witness}.  For part (2), use the canonical
homomorphism $\iota\colon\mathcal B\to E$ and put $a=v_1$.  The tuple
$(\mathcal B,\iota,t,c)$ is a marked Kazhdan pattern in $E$:
Proposition~\ref{prop:literal-base-T} gives property~\textup{(T)};
the six stable-letter relators give
$t\iota(\mathcal B)t^{-1}\subseteq\iota(\mathcal B)$; and the six
commutator relators give $[c,\iota(\mathcal B)]=1$.  No injectivity of
$\iota$ is required.  In $E$ we have
$d^2=(tct^{-1})^2=tc^2t^{-1}=1$, so
Lemma~\ref{lem:square} with $a_1=\iota(v_1)$ gives
\[
  w=\bigl[\,d,\;\iota(v_1)\,d\,\iota(v_1)^{-1}\bigr]
  =[\,d,\iota(v_1)\,]^{\,2}\;\in\;N_{\mathrm{comp}} ,
\]
since $[\,d,\iota(v_1)\,]$ is one of the defining generators of
$N_{\mathrm{comp}}$.  The subgroup $F=\{1,w\}=\langle w\rangle$ is
finite and normal: centrality and $w^2=1$ are defining relations, while
Proposition~\ref{prop:witness} gives $w\neq1$.  Moreover
$F\subseteq N_{\mathrm{comp}}$.  Theorem~\ref{thm:criterion}
therefore kills $w$ after composing any
$\Theta\colon E\to\Ucor((d_n))$ with the canonical isomorphism
$\kappa_{(d_n)}$ of Lemma~\ref{lem:unitarycorona}.  Since
$\kappa_{(d_n)}$ is injective, $\Theta(w)=1$.  This is part (2).

\medskip

For the final assertions of Theorem~\ref{thm:A}: an MF embedding of
$E$ would be an injective corona representation, which is impossible
because every corona representation sends the nontrivial element $w$
to $1$.  If $\Cred(E)$ were an MF algebra, an injective, possibly nonunital
$*$-homomorphism $e\colon\Cred(E)\to\mathcal Q$ would give the injective
unitary homomorphism $u\mapsto e(u)+(1-e(1))$.  Composing it with the
canonical map $E\to\U(\Cred(E))$, and then with
$\kappa_{(d_n)}^{-1}$, would exhibit an injective corona representation of
  $E$: the map $g\mapsto\lambda_g$ is injective.  The canonical map
  $E\to\U(\Cmax(E))$ is injective as well, because coordinate evaluation at
  the reduced left-regular representation sends $u_g$ to $\lambda_g$.
  Thus an MF embedding of this concrete $\Cmax(E)$ would again produce an
  injective corona representation of $E$ after the same
  $\kappa_{(d_n)}^{-1}$ conversion.
\end{proof}

\paragraph{The marked radical element.}
The proof shows more than non-injectivity of each individual
$\Theta$: the fixed element $w$ lies in the kernel of \emph{every}
corona representation of $E$, so every corona representation factors
through $E/\langle w\rangle$ (note $\langle w\rangle=\{1,w\}$ is
central, hence normal).  In the language of approximation radicals, $w$
is a nontrivial element of the ``MF radical'' of $E$.
This terminology is meant in analogy with Slofstra's earlier quotients
$G^{\mathrm{fin}}$ and $G^{\mathrm{fa}}$, obtained by killing elements
invisible to exact and approximate finite-dimensional representations,
respectively \cite[Definitions~2.5--2.7]{SlofstraCorrelations}.
By Theorem~\ref{thm:criterion} the same holds in every countable
group with a marked Kazhdan pattern: every finite normal subgroup of
the compression-defect subgroup lies in the MF radical.
Section~\ref{sec:consequences} develops the radical systematically;
we do not know whether $E/\langle w\rangle$ is MF, i.e.\ whether the
MF radical of $E$ is exactly $\{1,w\}$.

\paragraph{No density hypothesis.}
No lower bound on the corner ranks $r_n$, or on their density in
$d_n$, is used anywhere: Lemma~\ref{lem:corner} equips each nonzero
complementary block with its own normalized trace.  Thus all subsequent
Hilbert--Schmidt estimates are relative to that block.  This is
what makes the operator-norm setting insensitive to small relative ranks,
which block the same argument in Hilbert--Schmidt metrics
(Section~\ref{sec:limits}).

\section{Consequences}\label{sec:consequences}

Throughout this section, ``corona representation'' and ``MF'' are as
in Section~\ref{sec:prelim}.

We record the universal MF radical, the reduced-group-$C^*$ consequence, and
the failure of quotient permanence.  These are direct consequences of the
same literal marked element; no additional envelope or recursion-theoretic
construction is used.

\subsection{The MF radical}

\begin{definition}\label{def:radical}
For a countable group $G_1$, the \emph{MF radical} is
\[
  \operatorname{Rad}_{\mathrm{MF}}(G_1)
  \;=\;\bigcap\,\bigl\{\ker\Theta:\ \Theta\ \text{a corona
  representation of }G_1\bigr\},
\]
a normal subgroup of $G_1$.  This is the operator-norm analogue of
the invisible subgroups of \cite[Definitions~2.5--2.7]{SlofstraCorrelations}
and of the approximation kernels studied in \cite{NST}.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptMFRadical}
\end{definition}

\begin{lemma}[portability]\label{lem:portable}
For every homomorphism $\varphi\colon G_1\to G_2$ of countable
groups,
\[
  \varphi\bigl(\operatorname{Rad}_{\mathrm{MF}}(G_1)\bigr)
  \;\subseteq\;\operatorname{Rad}_{\mathrm{MF}}(G_2):
\]
the MF radical is functorial.  In particular, if
$\varphi\colon E\to G_2$ satisfies $\varphi(w)\neq1$, then $G_2$ is
not MF.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptRadicalPortability}
\end{lemma}

\begin{proof}
If $x\in\operatorname{Rad}_{\mathrm{MF}}(G_1)$ and $\Theta$ is a
corona representation of $G_2$, then $\Theta\circ\varphi$ is a corona
representation of $G_1$, so $\Theta(\varphi(x))=1$.  For the second
statement: $w\in\operatorname{Rad}_{\mathrm{MF}}(E)$ by
Theorem~\ref{thm:A}, so
$1\neq\varphi(w)\in\operatorname{Rad}_{\mathrm{MF}}(G_2)$, and an
injective corona representation of $G_2$ would kill $\varphi(w)$, a
contradiction.
\end{proof}

\begin{proposition}[the largest MF quotient]\label{prop:univquot}
Let $G_1$ be a countable group and
$R=\operatorname{Rad}_{\mathrm{MF}}(G_1)$.
\begin{enumerate}
\item $R$ is the kernel of a single corona representation of $G_1$.
\item $G_1/R$ is MF.
\item Every homomorphism from $G_1$ to an MF group factors through
$G_1/R$.
\end{enumerate}
In particular $G_1$ is MF if and only if $R=\{1\}$.  The statement is
presumably folklore once the radical is defined; we include a proof
for completeness and claim no novelty for it.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptLargestMFQuotient}
\end{proposition}

\begin{proof}
(3) If $\varphi\colon G_1\to M$ with $M$ MF and
$j\colon M\to\Ucor((d_n))$ is an injective corona representation,
then $j\circ\varphi$ is a corona representation of $G_1$, so
$j(\varphi(R))=1$ and, by injectivity of $j$, $\varphi(R)=1$.

(1) and (2).  If $R=G_1$, the trivial representation has kernel $R$
and $G_1/R$ is trivial, hence MF; so assume $R\neq G_1$ and let
$(g_i)_{i\ge1}$ list, possibly with repetitions, elements of
$G_1\setminus R$ meeting every nontrivial coset of $R$.  For each $i$
choose a corona representation and write it, through
$\kappa_{(d_n^{(i)})}$, as
$\Theta_i\colon G_1\to\U(\mathcal Q_i)$,
$\mathcal Q_i=\mathcal Q\bigl((d^{(i)}_n)_{n\in\N}\bigr)$, with
$\Theta_i(g_i)\neq1$; put
$\varepsilon_i=\tfrac12\opnorm{\Theta_i(g_i)-1}>0$, and choose
unitary lifts $\bigl(U^{(i)}_{g,n}\bigr)_{n\in\N}$ of $\Theta_i(g)$
for every $g\in G_1$ (Lemma~\ref{lem:lift}), with $U^{(i)}_{1,n}=1$.
Recall that the quotient norm in a corona is the limit superior of
the coordinate norms: subtracting a null sequence does not change the
limit superior, and truncating finitely many coordinates realizes it.

Enumerate $G_1=\{h_1,h_2,\dots\}$ and fix a stage $k\ge1$.  For each
$i\le k$: the finitely many products
$U^{(i)}_{g,n}U^{(i)}_{h,n}$ and $U^{(i)}_{gh,n}$ with
$g,h\in\{h_1,\dots,h_k\}$ lift the same corona elements, so there is
$n_0$ with all the corresponding defects at most $1/k$ for
$n\ge n_0$; and since
$\limsup_{n\to\infty}\opnorm{U^{(i)}_{g_i,n}-1}=2\varepsilon_i$, there
are infinitely many $n\in\N$ with
$\opnorm{U^{(i)}_{g_i,n}-1}\ge\varepsilon_i$.  Choose
$n_i(k)\in\N$ satisfying both conditions and set
\[
  \Phi_k(g)\;=\;\bigoplus_{i\le k}U^{(i)}_{g,\,n_i(k)}
  \;\in\;\U\bigl(M_{D_k}(\C)\bigr),\qquad
  D_k=\sum_{i\le k}d^{(i)}_{n_i(k)} .
\]
Define $\Theta_\infty(g)$ as the class of $(\Phi_k(g))_{k\in\N}$ in
$\Ucor((D_k)_{k\in\N})$.  For fixed $g,h\in G_1$, both
lie in $\{h_1,\dots,h_k\}$ for all large $k$, and the operator norm
of a block sum is the maximum of the block norms, so
$\opnorm{\Phi_k(g)\Phi_k(h)-\Phi_k(gh)}\le1/k\to0$; together with
$\Phi_k(1)=1$ this makes $\Theta_\infty$ a corona representation of
$G_1$.  Consequently $\Theta_\infty(R)=1$.  Conversely, if
$g\notin R$, pick $i$ with $g\in g_iR$; then
$\Theta_\infty(g)=\Theta_\infty(g_i)$, and for every $k\ge i$,
$\opnorm{\Phi_k(g_i)-1}\ge\opnorm{U^{(i)}_{g_i,n_i(k)}-1}
\ge\varepsilon_i$, so the representative of
$\Theta_\infty(g_i)$ stays at least $\varepsilon_i$ from the identity
and $\Theta_\infty(g)\neq1$.  Hence $\ker\Theta_\infty=R$, proving
(1), and $\Theta_\infty$ descends to an injective corona
representation of $G_1/R$, proving (2).  For the final equivalence:
if $G_1$ is MF, an injective corona representation forces
$R=\{1\}$; if $R=\{1\}$, then (2) says $G_1=G_1/R$ is MF.
\end{proof}

\begin{corollary}[exact radical from a candidate quotient]\label{cor:exactradical}
Let $N\trianglelefteq G_1$ satisfy
$N\le\operatorname{Rad}_{\mathrm{MF}}(G_1)$.  Every corona
representation of $G_1$ factors uniquely through $G_1/N$.  If in
addition $G_1/N$ is MF, then
\[
 \operatorname{Rad}_{\mathrm{MF}}(G_1)=N.
\]
Thus proving that a concrete defect subgroup is invisible and that its
quotient is MF computes both the entire MF radical and the largest MF
quotient.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptExactRadicalFromCandidateQuotient}
\end{corollary}

\begin{proof}
The containment $N\le\ker\Theta$ gives the unique quotient
factorization for every corona representation $\Theta$.  For the
reverse containment in the displayed equality, compose the quotient
map $G_1\to G_1/N$ with an injective corona representation of $G_1/N$:
its kernel is exactly $N$, while the MF radical lies in the kernel of
every corona representation.
\end{proof}

\begin{corollary}\label{cor:nofaithful}
Let $A$ be a unital $C^*$-algebra admitting an injective, possibly
nonunital, $*$-homomorphism into a norm matrix corona.  Then the group $E$
admits no injective homomorphism into $\U(A)$.
\leanverified{Sofic/LiteralNonMFConsequences}{GroupApproximation.LiteralNonMFConsequences.literal_no_faithful_corona_subalgebra_target}
\end{corollary}

\begin{proof}
Let $j\colon A\to Q$ be the possibly nonunital inclusion into the ambient
corona and let $\phi\colon E\to\U(A)$.  Complement correction gives
\[
   \widehat j(u)=j(u)+1-j(1_A)\in\U(Q).
\]
This is an injective homomorphism on \(\U(A)\), so
\(\widehat j\circ\phi\) is a corona representation of \(E\).  The exact
literal kernel theorem sends \(w\) to the identity there; injectivity of
\(\widehat j\) gives \(\phi(w)=1\).  Proposition~\ref{prop:witness} has
\(w\neq1\), so \(\phi\) cannot be injective.
\end{proof}

\subsection{Stable finiteness does not imply MF}

\begin{lemma}\label{lem:faithfultrace}
Let $G_1$ be a countable discrete group.
\begin{enumerate}
\item The canonical tracial state $\tau$ on $\Cred(G_1)$ is
faithful.
\item Every unital $C^*$-algebra $A$ with a faithful tracial state
is stably finite: for every nonempty finite index set $I$, every isometry
in $M_I(A)$ is a unitary.  In particular this holds in $M_k(A)$ for every
$k\ge1$.
\leanverified{Sofic/ManuscriptExactWrappers}{GroupApproximation.ManuscriptExactWrappers.manuscriptFaithfulTraceAndStableFiniteness}
\end{enumerate}
\end{lemma}

\begin{proof}
(1) Realize $\Cred(G_1)\subseteq B(\ell^2G_1)$ as the norm closure
of the span of the left translation unitaries $\lambda_g$, and let
$\rho_g$ be the right translation unitaries,
$(\rho_g\xi)(h)=\xi(hg)$; each $\rho_g$ commutes with each
$\lambda_{g'}$, hence with every element of $\Cred(G_1)$.  The
canonical trace is $\tau(x)=\langle x\delta_e,\delta_e\rangle$.  If
$\tau(x^*x)=0$ then $\lVert x\delta_e\rVert=0$, so $x\delta_e=0$,
and then
$x\delta_g=x\rho_{g^{-1}}\delta_e=\rho_{g^{-1}}x\delta_e=0$ for
every $g\in G_1$.  The vectors $\delta_g$ are total in
$\ell^2G_1$, so $x=0$.

(2) On $M_k(A)$ the functional
$\tau_k\bigl([x_{ij}]\bigr)=\tfrac1k\sum_i\tau(x_{ii})$ is a state
with the trace property
$\tau_k(xy)=\tfrac1k\sum_{i,j}\tau(x_{ij}y_{ji})=\tau_k(yx)$, and it
is faithful: the $i$-th diagonal entry of $x^*x$ is
$\sum_jx_{ji}^*x_{ji}$, so $\tau_k(x^*x)=0$ forces
$\tau(x_{ji}^*x_{ji})=0$, hence $x_{ji}=0$, for all $i,j$.  If
$v\in M_k(A)$ satisfies $v^*v=1$, then $y:=1-vv^*$ is a projection
with
$\tau_k(y)=\tau_k(1)-\tau_k(vv^*)=\tau_k(1)-\tau_k(v^*v)=0$; since
$y=y^*y$, faithfulness gives $y=0$, i.e.\ $vv^*=1$.
\end{proof}

\begin{proof}[Proof of Theorem~\ref{thm:D}]
$\Cred(E)$ is unital and separable since $E$ is countable; by
Lemma~\ref{lem:faithfultrace} it carries a faithful tracial state
and is stably finite.  It is not an MF algebra by
Theorem~\ref{thm:A}.
\end{proof}

\subsection{Failure of quotient permanence}

\begin{corollary}[quotients]\label{cor:quotclosure}
MF groups are not closed under quotients: the free group
\(F_8\) is MF, and the literal group \(E\) is its quotient
but is not MF.
\leanverified{Sofic/LiteralMFQuotientControls}{GroupApproximation.LiteralMFQuotientControls.manuscriptQuotientNonclosure}
\end{corollary}

\begin{proof}
Map the eight free generators onto the eight displayed generators of \(E\).
The resulting homomorphism is surjective because those generators define the
literal presentation.  The group \(F_8\) is residually finite and hence
MF, while Theorem~\ref{thm:A} shows that \(E\) is not.
\end{proof}

\section{The cyclic-base comparison}\label{sec:necessity}

We prove Theorem~\ref{thm:C} for the literal seven-relator presentation.
The stable relation gives
\(t\langle\gamma_0\rangle t^{-1}\subseteq
\langle\gamma_0\rangle\), and \(c\) centralizes this cyclic base.
The same commutant argument as in Theorem~\ref{thm:B} therefore maps
\(w_{\mathrm{BS}}\) to the identity in every finite-dimensional
representation over every field.

For nontriviality, use the affine realization of
\(\mathrm{BS}(1,2)\) and its coset action.  The root site is fixed by the
cyclic base, while the two sites represented by \(t\) and \(\gamma_0t\)
are distinct.  Attach Clifford generators to these sites and map \(c\) to
the root lamp.  All seven relators hold, and
\[
 w_{\mathrm{BS}}\longmapsto
 [c_{t\langle\gamma_0\rangle},
  c_{\gamma_0t\langle\gamma_0\rangle}]
 =-1.
\]
Thus \(w_{\mathrm{BS}}\ne1\).  Restricting the target to the subgroup
generated by the three displayed images gives the asserted surjective
realized quotient.  This comparison isolates the precise limitation of the
finite-dimensional argument: exact finite-dimensional invisibility does not
itself imply that the marked word is trivial.
\section{Limitations: Hilbert--Schmidt metrics and hyperlinearity}\label{sec:limits}

The proof of Theorem~\ref{thm:A} does not begin from a merely tracial
approximation, for a structural reason worth recording.

Suppose one replaces the norm corona $\mathcal Q$ by a tracial
ultraproduct $\mathcal M=\prod^\omega(M_{d_n}(\C),\tr_{d_n})$, with
defects measured in $\hsnorm\cdot$ rather than operator norm, and asks
whether a hyperlinear-style embedding of $E$ must kill $w$.  The first
coordinate step already breaks: Hilbert--Schmidt multiplicative defects do
not control the operator norms of the off-diagonal blocks cut out by the
Reynolds spectral projections.  Consequently principal compression need not
remain multiplicative in operator norm, its Gram defects need not vanish in
operator norm, and the dimension-free polar-repair estimate used in
Lemma~\ref{lem:corner} is unavailable.  Likewise, the finite-stage Kazhdan
argument needs operator-norm spectral leakage estimates before it passes to
normalized Hilbert--Schmidt norm.  A tracial ultraproduct retains only the
latter information, so the equal-rank coordinate flip cannot be invoked.
The present argument therefore gives no universal tracial analogue of the
MF theorem and decides neither hyperlinearity of $E$ nor the
sofic-versus-hyperlinear problem.

The dividing line in the present argument is thus: \emph{operator-norm matrix
models form a stably finite world, in which property (T) compression is
rigid; for tracial matrix models the available ambient algebra of the
conjugation action can be properly infinite, so the same projection
comparison need not collapse to equality.}

This failure is closely related to a current structural question rather than
to a known impossibility theorem.  Alekseev--Thom ask whether, for a Kazhdan
representation into a tracial ultraproduct of matrices, its commutant can be
recovered as an ultraproduct of finite-dimensional subalgebras, possibly as
coordinate centralizers of suitable lifts \cite[Open Problem~6.2]{AlekseevThom}.
Such a coordinate model would provide finite-level structure absent from the
argument above, but their question is open.  Some additional hypothesis is
therefore essential before the operator-norm proof can be transferred to a
tracial setting.

\begin{thebibliography}{GKEMP}

\bibitem[AT]{AlekseevThom}
V.~Alekseev and A.~Thom,
\emph{Centralizers of sofic approximations of Kazhdan groups},
preprint, 2026, \arxiv{2608.05362}.

\bibitem[BDL]{BDL}
B.~Bachner, A.~Dogon, and A.~Lubotzky,
\emph{On $L^1$-approximation of groups},
online-first article, 2026; assigned to J. Algebra \textbf{702}, 235--243
(issue scheduled for September~15, 2026),
\doi{10.1016/j.jalgebra.2026.04.041}.

\bibitem[Be]{BekkaAmenable}
M.~E.~B. Bekka,
\emph{Amenable unitary representations of locally compact groups},
Invent. Math. \textbf{100} (1990), no.~1, 383--401,
\doi{10.1007/BF01231192}.

\bibitem[BV]{BekkaValette}
M.~E.~B. Bekka and A.~Valette,
\emph{Kazhdan's property (T) and amenable representations},
Math. Z. \textbf{212} (1993), no.~2, 293--299,
\doi{10.1007/BF02571659}.

\bibitem[BK]{BK}
Bruce Blackadar and Eberhard Kirchberg,
\emph{Generalized inductive limits of finite-dimensional
$C^*$-algebras},
Math. Ann. \textbf{307} (1997), no.~3, 343--380,
\doi{10.1007/s002080050039}.

\bibitem[CDE]{CDE}
J.~R. Carri\'on, M.~Dadarlat, and C.~Eckhardt,
\emph{On groups with quasidiagonal $C^*$-algebras},
preprint first posted October~15, 2012, \arxiv{1210.4050};
J. Funct. Anal. \textbf{265} (2013), no.~1, 135--152,
\doi{10.1016/j.jfa.2013.04.004}.

\bibitem[Dad]{Dadarlat}
M.~Dadarlat,
\emph{Obstructions to matricial stability of discrete groups and almost
flat $K$-theory},
Adv. Math. \textbf{384} (2021), Paper No.~107722,
\doi{10.1016/j.aim.2021.107722}.

\bibitem[DGLT]{DGLT}
M.~De~Chiffre, L.~Glebsky, A.~Lubotzky, and A.~Thom,
\emph{Stability, cohomology vanishing, and nonapproximable groups},
Forum Math. Sigma \textbf{8} (2020), Paper No. e18,
\doi{10.1017/fms.2020.5}.

\bibitem[Fri]{Fritz}
T.~Fritz,
\emph{On infinite-dimensional state spaces},
J. Math. Phys. \textbf{54} (2013), no.~5, 052107,
\doi{10.1063/1.4807079}.

\bibitem[Gl]{Gl19}
L.~Glebsky,
\emph{Extension of a residually finite group by a residually finite
group is weakly sofic},
preprint, 2019, \arxiv{1910.08631}.

\bibitem[GR]{GR}
L.~Glebsky and L.~M. Rivera,
\emph{Sofic groups and profinite topology on free groups},
J. Algebra \textbf{320} (2008), no.~9, 3512--3518.

\bibitem[KT]{KT}
G.~Kun and A.~Thom,
\emph{Nonsofic wreath products of residually finite groups},
preprint, 2026, \arxiv{2608.06222}.

\bibitem[MdlS]{MdlS}
M.~Magee and M.~de~la~Salle,
\emph{$\mathrm{SL}_4(\mathbb{Z})$ is not purely matricial field},
C. R. Math. Acad. Sci. Paris \textbf{362} (2024), 903--910,
\arxiv{2312.03220}.

\bibitem[GKEMP]{GKEMP}
D.~Gao, S.~Kunnawalkam Elayavalli, A.~Manzoor, and G.~Patchell,
\emph{A new source of purely finite matricial fields},
preprint, 2026, \arxiv{2603.24502}.

\bibitem[Ku16]{Kun16}
G.~Kun,
\emph{On sofic approximations of property (T) groups},
preprint, 2016, \arxiv{1606.04471}.

\bibitem[KT19]{KT19}
G.~Kun and A.~Thom,
\emph{Inapproximability of actions and Kazhdan's property (T)},
preprint, 2019, \arxiv{1901.03963}.

\bibitem[Lo]{Loring}
T.~A. Loring,
\emph{Lifting solutions to perturbing problems in $C^*$-algebras},
Fields Institute Monographs, vol.~8,
American Mathematical Society, Providence, RI, 1997.

\bibitem[NST]{NST}
N.~Nikolov, J.~Schneider, and A.~Thom,
\emph{Some remarks on finitarily approximable groups},
J. \'Ec. polytech. Math. \textbf{5} (2018), 239--258.

\bibitem[OAI]{OAI}
OpenAI,
\emph{Nonsofic groups exist},
in \emph{Ten advances in mathematics and theoretical computer science}
(non-refereed corporate research document),
Chapter~3, 78--95, version updated August~6, 2026,
\url{https://cdn.openai.com/pdf/ten-proofs-oai.pdf}.
The chapter appeared under the title \emph{A counterexample to the
soficity conjecture}, Chapter~3, 77--93, in the original version of the
document, now at
\url{https://cdn.openai.com/pdf/ten-proofs-oai-original.pdf}.

\bibitem[Sc]{Schafhauser}
C.~Schafhauser,
\emph{Finite dimensional approximations of certain amalgamated free
products of groups},
preprint, 2023, \arxiv{2306.02498}.

\bibitem[Sh]{Shulman}
T.~Shulman,
\emph{The MF property for amalgamated free products},
preprint, 2026, \arxiv{2603.13564}.

\bibitem[Slo17]{SlofstraCorrelations}
W.~Slofstra,
\emph{The set of quantum correlations is not closed},
Forum Math. Pi \textbf{7} (2019), Paper No.~e1,
\doi{10.1017/fmp.2018.3}.

\bibitem[Slo18]{SlofstraProfile}
W.~Slofstra,
\emph{A group with at least subexponential hyperlinear profile},
preprint, 2018, \arxiv{1806.05267}.

\bibitem[SV]{SlofstraVidick}
W.~Slofstra and T.~Vidick,
\emph{Entanglement in non-local games and the hyperlinear profile of groups},
Ann. Henri Poincar\'e \textbf{19} (2018), no.~10, 2979--3005,
\doi{10.1007/s00023-018-0718-y}.

\end{thebibliography}

\end{document}
