# non_mf_groups_exist.tex: sentence-level census

Generated by `scripts/sentence_census.py` from `non_mf_groups_exist.tex`.  Do not edit: the assignments live in `metadata/NON_MF_SENTENCE_MAP.tsv` and everything here is their join with the extraction.

The proof ledger grades claims; this census grades **sentences**, which is the granularity at which a prose assertion can hide from a gate that reads only numbered environments.

| status | sentences |
| --- | --- |
| `formalized` | 125 |
| `definition` | 30 |
| `unassigned` | 28 |
| `structural` | 16 |
| `attribution` | 8 |
| **total** | **207** |

Completely formalized or definitional: **155/207** (74.9%).

A sentence under a single-row ledger anchor inherits that forced row.  A sentence under a multi-row anchor must name its row or rows explicitly in `metadata/NON_MF_SENTENCE_MAP.tsv`; no text-similarity guess and no whole-anchor fallback is accepted.

## What no declaration establishes

These are the sentences the development does not settle, verbatim.  `open` sentences say that something is unknown and no declaration could settle them; `partial` sentences have a clause that is proved and a clause that is not; `unassigned` sentences have not been triaged at all.

* **`unassigned`**, line 65 --- Some groups are not MF.
  * printed badge recorded; an overlay assignment is owed because the paper's proof ledger is retired
* **`unassigned`**, line 65 --- For example, $H=\EL_{12}(L_{\F_2}(1,2))$ is a simple property-\textup{(T)} group where every homomorphism from $H$ to an MF group is trivial, so $C^*_{\mathrm r}(H)$ is a separable stably finite $C^*$-algebra that is not MF\@.
  * printed badge recorded; an overlay assignment is owed because the paper's proof ledger is retired
* **`unassigned`**, line 65 --- Generally, a normal property-\textup{(T)} subgroup contained in the commutators of a one-sided compression of a property-\textup{(T)} subgroup is killed by every homomorphism to an MF group.
  * printed badge recorded; an overlay assignment is owed because the paper's proof ledger is retired
* **`unassigned`**, line 103 --- In other words, $G$ is MF if there are finite unitary matrices, one for each element of $G$, that multiply correctly up to an error tending to zero in operator norm while every element other than the identity stays a fixed distance from the identity matrix.
* **`unassigned`**, line 103 --- The strong convergence convention of~\cite{GaoEtAl,Schafhauser} also requires the models to reproduce the operator norms of the left regular representation, so a group that is not MF as defined here is not MF in that convention either.
* **`unassigned`**, line 118 --- The quotient $\mathcal Q_{\mathbf d}$ is the corona algebra of $\bigoplus_nM_{d_n}(\C)$, since $\prod_nM_{d_n}(\C)$ is its multiplier algebra.
* **`unassigned`**, line 118 --- A separable $C^*$-algebra is MF if it embeds in a norm matrix corona~\cite{BK}.
* **`unassigned`**, line 127 --- The groups here fail to be MF because they contain a subgroup that is conjugated into itself.
* **`unassigned`**, line 132 --- An element $c$ of the centralizer $C_G(L)$ commutes with $L$, so $ucu^{-1}$ commutes with $uLu^{-1}$; it need not commute with the rest of $L$.
* **`unassigned`**, line 132 --- The normal subgroup of $G$ generated by the commutators of all such elements $ucu^{-1}$ with elements of $L$ is
* **`unassigned`**, line 156 --- Asymptotic multiplicativity in operator norm makes the conjugation maps $\operatorname{Ad}(V_n(g))\colon x\mapsto V_n(g)xV_n(g)^*$ an asymptotic unitary representation of $G$ on $M_{d_n}(\C)$ with its normalized Hilbert--Schmidt inner product, because $\opnorm{\operatorname{Ad}(A)-\operatorname{Ad}(B)}\le2\opnorm{A-B}$.
* **`unassigned`**, line 156 --- Property~\textup{(T)} applies to unitary representations on Hilbert spaces, so it applies to this one, and its conclusions are estimates in Hilbert--Schmidt norm.
* **`unassigned`**, line 156 --- In an ultraproduct of these Hilbert spaces the maps $\operatorname{Ad}(V_n(g))$ become a unitary representation of $G$.
* **`unassigned`**, line 156 --- Let $P$ be the projection onto the vectors fixed by $L$, which is the Kazhdan projection of $L$, and let $U$ be the unitary of the compressor $u$.
* **`unassigned`**, line 156 --- If $\xi$ is fixed by $L$, then so is $U^*\xi$, because $\ell\cdot U^*\xi=U^*\bigl((u\ell u^{-1})\cdot\xi\bigr)$ for $\ell\in L$ and $u\ell u^{-1}\in L$ fixes $\xi$; therefore $U^*PU\le P$.
* **`unassigned`**, line 156 --- The projections $P$ and $U^*PU$ are conjugate by the unitary $U$, and in a stably finite $C^*$-algebra two equivalent projections, one below the other, are equal. The matrix corona is stably finite, and $U^*PU=P$ follows.
* **`unassigned`**, line 156 --- In terms of sequences, the vectors fixed by $L$ are the classes of $(x_n)$ with $\hsnorm{[V_n(\ell),x_n]}\to0$ for all $\ell\in L$, and $U^*PU=P$ means that conjugation by $V_n(u)$ maps this Hilbert--Schmidt asymptotic commutant of $L$ onto itself.
* **`unassigned`**, line 156 --- For $c\in C_G(L)$ the sequence $(V_n(c))$ lies in the asymptotic commutant, so $(V_n(ucu^{-1}))$ does too, that is, $\hsnorm{V_n([ucu^{-1},\ell])-1}\to0$ for all $\ell\in L$.
* **`unassigned`**, line 156 --- The elements with this property form a normal subgroup, which then contains $\mathfrak D_G(L)$.
* **`unassigned`**, line 182 --- The hypothesis $K\le\mathfrak D_G(L)$ therefore gives $\hsnorm{V_n(k)-1}\to0$ for $k\in K$, and a second use of property~\textup{(T)}, now for $K$, gives triviality in operator norm.
* **`unassigned`**, line 182 --- Suppose that a corona homomorphism $\Theta\colon G\to\U(\mathcal Q_{\mathbf d})$ is nontrivial on $K$.
* **`unassigned`**, line 182 --- Let $p\in\mathcal Q_{\mathbf d}$ be the Kazhdan projection of $K$, the projection onto the $K$-fixed vectors.
* **`unassigned`**, line 182 --- Then $p\ne1$, and $p$ commutes with $\Theta(G)$ because $K$ is normal. The homomorphism $\Theta$ then compresses to the corner $(1-p)\mathcal Q_{\mathbf d}(1-p)$, where $K$ has no fixed vectors, and the compressed homomorphism lifts to an operator norm asymptotic representation $(W_n)$ of $G$ on smaller matrices.
* **`unassigned`**, line 182 --- The Kazhdan inequality for $K$ bounds the average of $\hsnorm{W_n(s)-1}^2$ over a Kazhdan set $S\subseteq K$ below by a positive constant, so some $s\in S$ stays a fixed Hilbert--Schmidt distance from $1$, against the vanishing on $K$ applied to $(W_n)$.
* **`unassigned`**, line 182 --- Every corona homomorphism is therefore trivial on $K$, and by Lemma~\ref{prop:mf-residual-calculus} the same holds for every homomorphism to an MF group.
* **`unassigned`**, line 182 --- A Hilbert--Schmidt bound on the multiplicative defect gives no operator norm bound on the conjugation maps, so the argument does not apply to sofic or hyperlinear approximations.
* **`unassigned`**, line 202 --- The group $H$ realizes the compression inside a simple group, where one nontrivial commutator $[ucu^{-1},\ell]$ normally generates everything.
* **`unassigned`**, line 673 --- The algebra $C^*_{\mathrm r}(H)$ is separable, and stably finite because its canonical trace is faithful; if an embedding $e\colon C^*_{\mathrm r}(H)\to\mathcal Q_{\mathbf d}$ existed and $p=e(1)$, then $h\mapsto e(\lambda_h)+(1-p)$ would embed $H$ in $\U(\mathcal Q_{\mathbf d})$.

## By section

| section | sentences | unassigned |
| --- | --- | --- |
| (front matter) | 6 | 3 |
| Introduction | 48 | 24 |
| Corona homomorphisms | 4 | 0 |
| Kazhdan transport in normalized Hilbert--Schmidt norm | 41 | 0 |
| From Hilbert--Schmidt to operator norm | 27 | 0 |
| The binary Leavitt group | 41 | 1 |
| A torsion-free finitely presented example | 38 | 0 |
| Acknowledgments | 2 | 0 |
