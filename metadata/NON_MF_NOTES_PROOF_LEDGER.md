# Non-MF group notes: exhaustive proof-step ledger

Spec document: `non_mf_group_notes.tex`. Formal development: `GroupApproximation/`.

## Audited revision

| | |
| --- | --- |
| commit | the paper/notes split of 2026-08-22 |
| `git hash-object non_mf_group_notes.tex` | `5ef730aa0fafd2633add701739a79427f41124fb` |
| sha256 of the file | `9961005f26a7d5fcff4608b3bf73e27dd543cb962efbf114f35397306b0b9a6a` |
| `wc -l` | 2724 |

The three checkable rows are enforced by
`scripts/check_non_mf_proof_ledger.py`. Re-pinning is deliberate and follows
a review of the rows touched by an edit.

## Status counts

<!-- LEDGER-COUNTS -->
| Column | EXACT | MISMATCH | MISSING | UNDER-SPECIFIED | total |
| --- | --- | --- | --- | --- | --- |
| statement | 212 | 3 | 11 | 0 | 226 |
| proof | 212 | 1 | 13 | 0 | 226 |
<!-- END-LEDGER-COUNTS -->

## Anchors

<!-- LEDGER-ANCHORS -->
| Anchor | Kind | Locator | Digest |
| --- | --- | --- | --- |
| eq:intrinsicdefect | eq | eq:intrinsicdefect | - |
| thm:transport-variants | env | thm:transport-variants | 8c52c78392b71e52 |
| def:pattern | env | def:pattern | f4d4c613233e22c9 |
| def:invisible | env | def:invisible | a9bfafac2d6d94fc |
| thm:criterion | env | thm:criterion | 4153ff9fb688e0de |
| cor:generaltransport | env | cor:generaltransport | 2cd351da03fb75b0 |
| thm:compression-radical | env | thm:compression-radical | f4e9fb766fe58572 |
| thm:normal-kazhdan | env | thm:normal-kazhdan | 0dcfb82167b7e591 |
| thm:abstract-nk | env | thm:abstract-nk | 45b50a521fff6393 |
| cor:intrinsic-nk | env | cor:intrinsic-nk | 74911f7eaffe314f |
| rem:ff-realization | env | rem:ff-realization | 98dcc607cfc58e9f |
| cor:scaling-family | env | cor:scaling-family | 9370bcd0aa550f6e |
| thm:exactfd-intrinsic | env | thm:exactfd-intrinsic | 08b9b0b0dda2eb6a |
| cor:notRFD | env | cor:notRFD | 3ab0c34ceeaabcad |
| prop:blocknormalform | env | prop:blocknormalform | 4e271fd389e7e29d |
| lem:window | env | lem:window | ffcad9af5c739fc4 |
| thm:cyclic | env | thm:cyclic | 2bfd53c90eb9489c |
| def:radical | env | def:radical | 7d420d674dd7b990 |
| lem:portable | env | lem:portable | f83c0defa813a83a |
| prop:univquot | env | prop:univquot | 7cdece4e369941cb |
| cor:exactradical | env | cor:exactradical | 3534e895b8bf93de |
| cor:pullback | env | cor:pullback | a198d2414f61bde0 |
| cor:nofaithful | env | cor:nofaithful | c7cc4db7be975a5e |
| lem:permanence | env | lem:permanence | 7ecfd4d8df6e4e6b |
| cor:quotclosure | env | cor:quotclosure | 4abefb3ae0139858 |
| def:invwitness | env | def:invwitness | 19f64aedeec57699 |
| thm:collapse | env | thm:collapse | 978ac3e37662c007 |
| thm:projection-collapse | env | thm:projection-collapse | f8010458149ae59b |
| rem:torsion-collapse | env | rem:torsion-collapse | 15eb47e0c95f1806 |
| cor:collapsequot | env | cor:collapsequot | aacd98ae2be82cbc |
| thm:reduced | env | thm:reduced | 942e85e1ef739f83 |
| lem:faithfultrace | env | lem:faithfultrace | ed20bbd1df60ef72 |
| thm:Esofic | env | thm:Esofic | ec130627c7ca1db8 |
| prop:horn | env | prop:horn | 3e2c6bb45f9627c7 |
| p:cylinder | prose | `form a nonempty clopen set` | - |
| cor:undecidable | env | cor:undecidable | 465508722f012ed4 |
| prop:maximal-cstar | env | prop:maximal-cstar | 7108b280fbf939cb |
| prop:proper-isometry | env | prop:proper-isometry | af0fbcd75508bcd3 |
| rem:maxinfinite | env | rem:maxinfinite | 1ffdf62b6e21e761 |
| p:intro-collapse-pointer | prose | `is a second finitely presented non-MF group` | - |
| p:prelim-cmax | prose | `the maximal group $C^*$-algebra $\Cmax(H)$ is` | - |
| p:transport-weights | prose | `Taking $\nu_n=k_n$, the rank of` | - |
| p:conj-datum-remark | prose | `conditions (M1)--(M2) are imposed by` | - |
| p:conj-reynolds-only | prose | `for every finite normal subgroup $F$ contained` | - |
| p:conj-two-element | prose | `is the case $F=\{1,w\}$, where` | - |
| p:conj-intrinsic-def | prose | `which depends only on the pair $(H,L)$, not on a choice of datum` | - |
| p:nk-join | prose | `The subgroup generated by \emph{all} normal property-` | - |
| p:group-figure | prose | `every pair of distinct sites of a block is the image of` | - |
| p:witness-scope | prose | `involve only finitely many lamp generators` | - |
| p:A-marked-radical | prose | `In $E$ the pairwise commutators of those conjugates of $\hat c$ lie in` | - |
| p:C-sharpness | prose | `Running the Clifford representation of the preceding proof` | - |
| p:cyclic-exact | prose | `To transfer this obstruction to exact local embeddings` | - |
| p:collapse-wreath | prose | `In a permutational wreath product` | - |
| p:collapse-relcomm | prose | `already lies in` | - |
| p:D-preamble | prose | `Every MF algebra is stably finite` | - |
| p:D-base-injective | prose | `$\mathcal B\to E$ is injective` | - |
| p:E-extension | prose | `MF groups are not closed under extensions.` | - |
| p:E-zext | prose | `canonical projections define a surjection $W\to\Z$` | - |
| p:E-exact | prose | `equivalent to exactness of its reduced group $C^*$-algebra` | - |
| p:E-simple | prose | `There is a countable simple sofic group` | - |
| p:horn-intro | prose | `yields a finite quasi-identity` | - |
| p:mult-products | prose | `The groups $E\times\mathbb Z^{k}$ for $k\ge0$ are` | - |
| p:mult-continuum | prose | `For finitely generated examples, choose continuum many pairwise` | - |
| p:limits-opnorm | prose | `The argument requires operator-norm control.` | - |
| p:limits-dilution | prose | `Adding an identity block leaves operator-norm multiplicative defects` | - |
| p:limits-trace | prose | `Stable finiteness and faithful traces on the coordinate algebras` | - |
| thm:exact-mf-residual | env | thm:exact-mf-residual | 36dba347bdeda5e7 |
| p:torsion-free-consequence | prose | `There exists a torsion-free finitely presented non-MF group.` | - |
| p:q3 | prose | `Does MF imply hyperlinearity?` | - |
| p:q4 | prose | `compute the residual exactly` | - |
| p:q5 | prose | `by other coordinate algebras` | - |
| p:q5-lift | prose | `This argument uses only polar correction` | - |
| p:appC-universal | prose | `Injectivity follows from the left regular representation` | - |
| thm:signfree | env | thm:signfree | 988f9fd1ddb67eca |
| p:limits-adjointgap | prose | `adjoint operators remain at operator-norm distance` | - |
| lem:mftrace-group | env | lem:mftrace-group | 2fc00b7172eebb3b |
| thm:trace | env | thm:trace | 258d0b6db6660555 |
<!-- END-LEDGER-ANCHORS -->

## Steps

<!-- LEDGER-STEPS -->
| Step | Anchor | Claim | Lean | Stmt | Proof | Obj | Deps | Source | Edit | Note |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| INT.21 | p:intro-collapse-pointer | second application of one-sided compression: orbit collapse computes the MF radical | `ManuscriptExactWrappers.manuscriptInvolutiveCollapse`; `ManuscriptExactWrappers.manuscriptDefectSaturation` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptInvolutiveCollapse` and `manuscriptDefectSaturation` both read `{H : Type u}` with `HasKazhdanPropertyT.{u,u}`, so nothing is fixed at `Type 0` and `univ0` was the row's only MISMATCH reason |
| INT.29 | p:intro-collapse-pointer | applied to $E$ itself this shows $E/\langle w\rangle$ is again not MF, so the marked sign certifies an obstruction surviving its removal | `LiteralSignFreeQuotient.signFreeQuotient_not_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | added to the manuscript during this audit |
| PRE.19 | p:prelim-cmax | $\Cmax(H)$ is the completion of $\mathbb{C}[H]$ in the supremum of the norms of its unitary representations, and only its universal property is used | `manuscriptPrintedMaximalGroupCStar`; `manuscriptMaximalGroupCStarUniversalProperty` | EXACT | EXACT | literal | unconditional | - | NO | the Lean object is universe-relative, as the TeX itself says; the printed "the maximal group C*-algebra" is the class-sized one.  RE-READ 2026-08-16: the Section 2 forward reference this anchor was named for was deleted, and the probe now sits on Appendix C's own definition, so the anchor name no longer describes its location.  The name is left alone because renaming it would move a row for no gain; MC.02 audits the universal property itself and records the universe of the target  **`a9b445cf` VERIFIED 2026-08-17 AND NOT GRADED; the commit subject reads "the last gate for PRE.19/MC.02" and the honest reading is narrower.**  `Analysis/CStarCompletionCoe.lean` supplies one arrow — `coeStarAlgHom`, the canonical `A →⋆ₐ[ℂ] Completion A`, with `norm_coeStarAlgHom` and `denseRange_coeStarAlgHom`.  Three things keep it off this row.  (i) It is outside the root import closure and has never elaborated, as are `Analysis/CStarNormBundled` and `Analysis/CStarSeminormQuotient`, which the route also needs.  (ii) Its own docstring says the file "certifies no manuscript step on its own".  (iii) **The author retracted an earlier claim of theirs in the same docstring, and the retraction is the load-bearing part**: they had said the null-ideal quotient could be deleted because Mathlib's `[SeminormedRing A] : NormedRing (Completion A)` separates the seminorm, and the conclusion was too strong — `CStarRing` is stated over `[NonUnitalNormedRing]`, which extends `MetricSpace` rather than `PseudoMetricSpace`, so the `C⋆`-identity cannot be a class over a merely seminormed algebra.  The quotient route therefore survives and still needs two small additive lemmas.  So: last gate of one piece, two lemmas outstanding, nothing elaborated.  Recorded as an advance, and the row does not move.  **FACTUAL CORRECTION 2026-08-18.**  The clause above saying the module is outside the root import closure and has never elaborated is no longer true and has not been since 4a0713b2: `scripts/check.py --list-orphans` returned twenty-eight modules that morning, ten of which did not elaborate at all, and all twenty-eight are now imported by `GroupApproximation.lean` and compile.  The orphan count is zero.  `Analysis/CStarCompletionCoe`, `Analysis/CStarNormBundled` and `Analysis/CStarSeminormQuotient` are all in the closure and green; `CStarSeminormQuotient` needed one repair on the way in, `[Nonempty iota]` on the two supremum-seminorm theorems becoming an explicit hypothesis because the kernel audit reads an instance-syntax assumption as a finding.  Reason (i) of the three is therefore spent.  Reasons (ii) and (iii) are untouched and the row does not move on this alone  **2026-08-18: THE PRINTED OBJECT IS BUILT, NOT GRADED.**  This row is about the *construction*, and the construction now exists: `Analysis/MaximalCStarPrintedCompletion.PrintedMaximalGroupCStar G` is the completion of `ℂ[G]` in `maxGroupSeminorm G`, and `manuscriptPrintedMaximalGroupCStar` packages injectivity of `h ↦ u_h`, the printed universal property for a target in *any* universe, and a `≃⋆ₐ[ℂ]` onto `MaximalGroupCStar G`.  The supremum is taken over the *set* of C⋆-seminorms on `ℂ[G]` under which every group element is a contraction, which is why it is a supremum at all rather than one over a proper class — and it is the printed number, not merely an upper bound for it, in both directions: `isGroupCStarSeminorm_repSeminorm` puts every unitary representation's seminorm in the family, at any universe, and `repSeminorm_seminormCompletion` exhibits every member of the family as the seminorm of an actual unitary representation, namely the canonical one in the C⋆-algebra that seminorm envelops, which sits at the universe of `G`.  So the family is exactly the representation seminorms.  Finiteness is `exists_bound_of_isGroupCStarSeminorm`, the `ℓ¹` bound, proved by induction over the linear structure of the group ring rather than by naming the sum of the coefficient norms, since only boundedness is used; that the supremum is again a C⋆-seminorm is `CStarSeminormQuotient.isCStarSeminorm_iSup`, already present.  The isomorphism is forced by the two uniqueness clauses, each of which holds at every universe, so each applies to the other model across the universe gap between them.  Reason (ii) is spent with reason (i): `CStarCompletionCoe` no longer certifies nothing, it is one arrow of a route that now ends at the printed object.  Landed `291b3704`, `a81c96cb`.  Evidence and gate exactly as recorded on `MC.02`: MSI green, 40715 declarations swept with no disallowed axioms, and no green `Build and audit` yet because the audit gate is red on other lanes' findings.  When one lands, the row's fourth column should name `manuscriptPrintedMaximalGroupCStar` beside the existing declaration and be regraded **CLOSED 2026-08-18:** `MaximalCStarPrintedCompletion.manuscriptPrintedMaximalGroupCStar` identifies the printed supremum-norm completion with the development's model, and the all-universe wrapper supplies the unrestricted universal property. |
| TV.01 | thm:transport-variants | transport at an arbitrary nonnegative weight sequence $w_n$, both directions | `TransportVariantsAnyUniverse.scaled_transport_both_anyUniverse`; `ScaledKazhdanTransport.scaled_transport_both` | EXACT | EXACT | literal | unconditional | - | NO | `IsScaledAsymptoticCommutant`/`IsScaledMassBounded` are the printed Tr-conditions verbatim.  **RESOLVED 2026-08-17 from MISMATCH, by wrapper rather than by sweep.**  The two measurements below stay on record because they are why the sweep is not owed, rather than merely unfinished.  `KazhdanCompressionCore` no longer pins its source group — it is `(Γ : Type v)` with `kazhdan : HasKazhdanPropertyT.{v, v} Γ`, green across all forty-four files that use it — so the printed generality can now at least be *written*.  Generalizing the transport theorems' own binders does not come free, and the two attempts say where the wall is.  (i) A `whnf` heartbeat timeout appeared in `scaled_laplacian_massVanishing` with fifteen cascade errors after it; the cause was **not** that declaration but `ScaledMassVanishing.finset_sum`'s own `{I : Type}`, one pinned binder in a helper — the same misleading shape as a structure whose projections all come back unknown.  (ii) With that widened to `{I : Type*}` the timeout is gone and the real wall appears: `KazhdanCompressorCorner.cornerProjection` and its neighbours pin `IsKazhdanPair.{0, 0}`, so the cascade continues into the corner layer and reaches the representation universe, not just the group's.  Both attempts were reverted, not left half-applied.  Since `thm:transport-variants` inherits its hypotheses from `thm:kazhdan-transport`, which grants no countability, the honest closure is the derived-countability transfer that closed KT.00 rather than a sweep: `C.kazhdan` is already `HasKazhdanPropertyT.{v, v} Γ`, which is exactly what `exists_symmetric_generating_finset` consumes, and `HasKazhdanPropertyT.of_mulEquiv` already exists.  The last missing ingredient now exists: `CompressionUniverseTransfer.mapCoreSource` transports a compression core along an isomorphism of its *source* (`mapCore` moves the ambient), and `KazhdanCompressionCore.countable_source` derives `Countable Γ` from the core's own `kazhdan` field.  Property `(T)` needs three moves there and all three were already in `Kazhdan/`: `liftUniverse` raises the representation universe to `max w v` so it can see both group universes, `of_mulEquiv` carries the property across, and `of_max` brings it back down to the target's.  Both of those remaining pieces are now done, in `Sofic/TransportVariantsAnyUniverse.lean`.  The predicates carry their own universe binder: `IsScaledAsymptoticCommutantOf` already did, and `IsScaledAsymptoticIntertwinerOf` was generalized to `{G : Type v}`, every existing use instantiating `v := 0`.  The wrappers are `TransportVariantsAnyUniverse.scaled_transport_both_anyUniverse`, `manuscriptIntertwinerTransport_anyUniverse` and `scaled_intertwiner_transport_anyUniverse`.  Since `mapCoreSource` fixes `t`, the compressed sequences in the conclusion are the same terms at the `Type 0` model as they are at `Γ`, so only the homomorphism has to be read back along the isomorphism; `commutantOf_of_precomp` and `intertwinerOf_of_precomp` are that single step, one per predicate.  **This is not a weakening**: the print grants no countability and none is assumed, because property `(T)` implies it. |
| TV.10 | thm:transport-variants | the subgroup formulation printed in the weighted theorem, in literal natural matrix coordinates | `ManuscriptExactWrappers.manuscriptWeightedTransportSubgroup` | EXACT | EXACT | literal | unconditional | - | NO | The dedicated wrapper names the two displayed hypotheses and both transported conclusions; TV.01 records the underlying arbitrary-universe theorem. |
| TV.04 | thm:transport-variants | proof: reweight the inner product by $1/w_n; w_n = 0$ forces $x_n = 0$ | `ScaledKazhdanTransport.scaled_transport` | EXACT | EXACT | literal | unconditional | - | NO | - |
| TV.05 | thm:transport-variants | proof: "the ultraproduct proof applies verbatim" | `UltraproductScaledTransport.transport_variants_one`; `OmegaWeightedAmbient.omegaWeightedAmbient` | EXACT | EXACT | literal | unconditional | - | NO | **RESOLVED 2026-08-17 from MISSING/MISSING.**  The old note was accurate when written — "there is no ultraproduct proof to apply; Lean reruns the finite-stage argument at the weight" — and is now false in both halves.  `transport_variants_one` *is* the ultraproduct argument at an arbitrary nonnegative weight, and `omegaWeightedAmbient` supplies its ambient over the printed `B_ω`, so the printed sentence is a theorem rather than a plan |
| TV.09 | p:transport-weights | taking $\nu _n = d_n$ gives \ref{thm:kazhdan-transport} with the uniform operator-norm bound relaxed to a uniform bound on $\operatorname{Tr}(x_n*x_n)/d_n$ | `ScaledKazhdanTransport.manuscriptDimensionWeightRecovery`; `ScaledKazhdanTransport.transport_both_of_card_massBounded` | EXACT | EXACT | literal | unconditional | - | NO | **RESOLVED 2026-08-17 from MISSING/MISSING, by inspection.**  `manuscriptDimensionWeightRecovery` is this sentence in three conjuncts: at the dimension weight the scaled commutant is the unscaled one, uniform operator-norm boundedness implies scaled mass boundedness, and the two-sided conclusion follows from the *relaxed* hypothesis.  The old note asked for a declaration deriving the transport theorem from the scaled form at ν_n = d_n; that is its third conjunct |
| CD.01 |def:pattern |definition of a Kazhdan conjugation datum $(\Lambda , \iota , t$, c) with (M1) and (M2) | `ManuscriptExactWrappers.manuscriptMarkedKazhdanPattern_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| CD.02 | def:pattern | ĉ $:= tct^{-1}$ and the defect subgroup $N_{\mathrm{comp}} = \langle \langle [\hat{c}, \iota (\lambda )]\rangle \rangle _H$, normal in $H$ | `KazhdanCompressionCore.transported`; `KazhdanCompressionCore.defectNormal` | EXACT | EXACT | literal | unconditional | - | NO | `rfl` against the printed normal closure |
| CD.03 | p:conj-datum-remark | both conditions are relations among generators, so a datum can be read off a presentation | `LiteralNonMFEndpoint.inclusionData` | EXACT | EXACT | literal | unconditional | - | NO | demonstrated for the literal presentation |
| CD.04 | p:conj-datum-remark | the relations do not constrain the commutators [ĉ, $\iota (\lambda )]$ | `LiteralNonMFLinearWitness.literal_mark_ne_one` | EXACT | EXACT | literal | unconditional | - | NO | witnessed, rather than asserted, by the Clifford model |
| CD.05 | def:invisible | definition of `2`-norm triviality: $g \in K_2(U)$ for every operator-norm asymptotic representation $U$ and every cofinite-refining ultrafilter | `ManuscriptExactWrappers.ManuscriptHSInvisible` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `ManuscriptHSInvisible` reads `{H : Type u}`, so the `univ0` mark was stale.  RE-READ 2026-08-16: the definition was reworded from "Hilbert--Schmidt invisible" to "`2`-norm trivial" and now factors through the per-representation set K₂(U); the Lean predicate is unchanged and is still the printed quantification |
| CD.06 | def:invisible | $K_2(U) = \{g : \Vert U_n(g) - 1\Vert _2 \to 0\}$ is a normal subgroup of $H$, and is the kernel of the homomorphism $U$ induces into the tracial ultraproduct | `ManuscriptExactWrappers.manuscriptHSInvisibleCharacterization` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptHSInvisibleCharacterization` reads `{H : Type u}`, so the `univ0` mark was stale.  as CD.05.  RE-READ 2026-08-16: the kernel description holds by `Iff.rfl` (first conjunct), and the subgroup form -- which the note here previously called a conjunct "the definition does not print" -- is now printed.  *Normality* is not a conjunct of this theorem; it comes from FN.01, where K₂ is realized as `nullUnitarySubgroup`, the kernel of `toUltraproductHom` |
| FN.00 |thm:criterion |Theorem 4.4: every finite normal $F \subseteq N_{\mathrm{comp}}$ dies in every corona representation | `ManuscriptExactWrappers.manuscriptFiniteNormalObstructionCriterion_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| FN.01 | thm:criterion | $K_2 = {g : \Vert U_{g,n} - 1\Vert _2 \to 0}$ is a normal subgroup of $H$ | `nullUnitarySubgroup`; `AsymptoticUnitaryRepresentation.toUltraproductHom` | EXACT | EXACT | literal | unconditional | - | NO | realized as the kernel of the tracial-ultraproduct homomorphism |
| FN.02 | thm:criterion | $c$ centralizes $\iota (\Lambda )$, so its lifts lie in the HS asymptotic commutant of $\iota (\Lambda )$ | `KazhdanCompressorCorner.compressionDefect_hsDistSq_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.03 | thm:criterion | apply Theorem 3.1 with $x_n = U_{c,n}$ to put the lifts of ĉ $= tct^{-1}$ in the commutant | `KazhdanCompressionCore.compressionDefects_hsTrivial`; `UltraproductRigidityRoute.compressionDefects_hsTrivial_literal`; `UltraproductRigidityRoute.compressionDefect_hsDistSq_vanishing_literal` | EXACT | EXACT | literal | unconditional | - | NO | **REWIRED 2026-08-18, and the grade deliberately held.**  The transport this row's step invokes is now the printed one.  `Analysis/UltraproductRigidityRoute` already carried `transportedRoot_displacement_ultraproduct` -- the Appendix-B displacement lemma re-proved through `KazhdanAsymptoticCommutant.manuscriptKazhdanTransport`, which is `thm:kazhdan-transport` itself -- and what did not exist was any consumer of it, exactly as `ID.06` records for the reverse direction.  Two declarations now supply that: `UltraproductRigidityRoute.compressionDefect_hsDistSq_vanishing_literal`, which feeds the ultraproduct transport to the shared seam `compressionDefect_hsDistSq_vanishing_of` in place of the finite-stage corner, and `UltraproductRigidityRoute.compressionDefects_hsTrivial_literal`, which is this row's printed sentence as `CompressionDefectsHSTrivial`.  `LiteralNonMFEndpoint.kazhdanPinning` consumes the second, so the endpoint TRAVELS the printed route and does not merely have it available -- the distinction `ID.06` insists on.  The finite-stage route is untouched and still proves the same statement; only the corner bookkeeping below the transport is shared.  **Why the grade is held anyway:** the standard this ledger applies is a green `Build and audit`, and none exists -- the source scan stands at 19 `maxHeartbeats` findings after the restore at `85989ae8`, so nothing has been certified.  This row joins `MC.04`, `SO.17`, `RE.01` and `KT.05` in the pending-first-green set and is regraded there, not here.  A note that the docstring of `Sofic/MarkedCompressionRootCapture` promised this alternative under a name that never existed anywhere in the corpus, and could not have: the printed route is only available below that file, which `UltraproductRigidityRoute` imports   **GRADED 2026-08-18: the held condition is met.**  The note above holds this row at `MISMATCH` for exactly one stated reason -- "the standard this ledger applies is a green `Build and audit`, and none exists -- the source scan stands at 19 `maxHeartbeats` findings after the restore at `85989ae8`, so nothing has been certified".  One exists now.  Run 32122738761 on `c0f1e4e8` is `completed/success` in every job, its `Zero-tolerance verdict` step green, and `c0f1e4e8` is the commit that removed the last heartbeat bump, so the 19 findings that blocked certification are gone at the certified commit itself.  The route is certified rather than merely written: at `c0f1e4e8`, `LiteralNonMFEndpoint.kazhdanPinning` already reads `UltraproductRigidityRoute.compressionDefects_hsTrivial_literal`, and both `Analysis/UltraproductRigidityRoute` and `Sofic/LiteralNonMFEndpoint` are imported by `GroupApproximation.lean` at that commit, so the certifying build elaborated the printed route and not merely the finite-stage one standing beside it.  Chain, checked link by link: `kazhdanPinning` to `compressionDefects_hsTrivial_literal` to `compressionDefect_hsDistSq_vanishing_literal` to `transportedRoot_displacement_ultraproduct` to `KazhdanAsymptoticCommutant.manuscriptKazhdanTransport`, which is `thm:kazhdan-transport` by the printed section 3 ultraproduct proof.  No corner argument and no Appendix-B constant occurs anywhere on it. |
| FN.04 | thm:criterion | therefore [ĉ, $\iota (\lambda )] \in K_2$ for every $\lambda$, and normality gives $N_{\mathrm{comp}} \subseteq K_2$ | `KazhdanCompressionCore.compressionDefects_eq_one_in_hyperlinearHom`; `KazhdanCompressionCore.defectNormal_le_ker` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.05 | thm:criterion | convergence of the full sequence gives triviality along every ultrafilter refining the cofinite filter | `KazhdanCompressionCore.compressionDefects_eq_one_in_hyperlinearHom` | EXACT | EXACT | literal | unconditional | - | NO | the `hcof` hypothesis is exactly the printed one |
| FN.06 | thm:criterion | suppose $\Theta$ is nontrivial on $F$; choose coordinate unitary lifts and form $p_n =$ (card $F)^{-1} \Sigma _{f\in F} V_{f,n}$ | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.07 | thm:criterion | $(p_n)$ is asymptotically self-adjoint and idempotent, and normality makes it asymptotically central for $\Theta (H)$ | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | internal to the corner construction |
| FN.08 | thm:criterion | the Hermitian part is asymptotically idempotent, so its spectrum clusters at {0,1}; round at 1/2 by CFC to genuine projections at distance o(1) | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | BDL | NO | internal to the corner construction |
| FN.09 | thm:criterion | the rounded projections inherit asymptotic centrality; let $q_n$ be their complements | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.10 | thm:criterion | nontriviality of $\Theta$ on $F$ forces $q_n \ne 0$ along infinitely many coordinates; retain and relabel them | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing`; `OpAlmostRepresentation.reindex` | EXACT | EXACT | literal | unconditional | - | NO | the relabelling is `reindex` with a monotone stage function |
| FN.11 | thm:criterion | compress every lift to $q_nM_{d_n}q_n \cong M_{r_n}$ and polar-correct; almost-multiplicativity survives | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | BDL | NO | - |
| FN.12 | thm:criterion | $(^*)$ the finite average vanishes on the corner in operator norm | `FiniteNormalAverageCorner.exists_corner_with_finite_sum_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | this is the returned `OpNormVanishing` conclusion |
| FN.13 | thm:criterion | tracial vanishing applies to the corner representation: $\Vert W_{f,n} - 1\Vert _2 \to 0$ for every $f \in F$ | `KazhdanCompressionCore.finiteNormal_le_normMFResidual_of_hyperlinear_killed` | EXACT | EXACT | literal | unconditional | - | NO | the `hcloseU`/`hclose'` block |
| FN.14 | thm:criterion | $F$ finite ⇒ the maximum of (card F) null sequences is null ⇒ $\Sigma _f W_{f,n} \to$ (card $F)\cdot 1$ in normalized HS norm | `FiniteNormalAverageCorner.false_of_finite_sum_vanishing_of_hsTrivial` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.15 | thm:criterion | contradiction with $(^*)$, because operator-norm convergence dominates normalized HS convergence | `FiniteNormalAverageCorner.false_of_finite_sum_vanishing_of_hsTrivial` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FN.16 | thm:criterion | transport of the conclusion from the unitary-sequence corona to U(Q) by the polar-correction isomorphism | `KazhdanCompressionCore.finiteNormal_le_normMatrixCStarCoronaKernel` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CR.01 | p:conj-reynolds-only | the Reynolds corner uses only HS invisibility of every element of $N_{\mathrm{comp}}$ | `KazhdanCompressionCore.finiteNormal_le_normMFResidual_of_hyperlinear_killed` | EXACT | EXACT | literal | unconditional | - | NO | the abstracted hypothesis is exactly that |
| CR.02 | p:conj-two-element | for $F = \{1,w\}$ the average is ½(1 $+ V_{w,n})$ and the corner is the negative spectral corner, where the image of $w$ is $-1$ exactly in the corona and the finite-stage compressions satisfy $\Vert W_{w,n}+1\Vert \to 0$ | `KazhdanCompressorCorner.false_of_markedOpAlmostRepresentation`; `LiteralNonMFEndpoint.negativeCorner_kazhdanTransport_contradiction` | EXACT | EXACT | literal | unconditional | - | NO | REWORDED 2026-08-18 with the print (external audit item 5): the print and this cell previously said the corner model has w = −1 exactly, which conflated the corona corner (exact) with the finite-stage compressions (operator-norm convergence); the graded content and the cited declarations are unchanged |
| ID.01 | p:conj-intrinsic-def | $G_{\mathrm{comp}}(L) =$ subgroup generated by the one-sided conjugators of $L$ | `compressionSet`; `compressionGroup` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ID.02 | eq:intrinsicdefect | 𝔇(H,L) $= \langle \langle [gzg^{-1}, \ell ] : g \in G_{\mathrm{comp}}(L), z \in C_H(L), \ell \in L\rangle \rangle _H$ | `compressionCentralizerDefectSet`; `compressionCentralizerDefect` | EXACT | EXACT | literal | unconditional | - | NO | generator set matches the display term for term |
| ID.03 | p:conj-intrinsic-def | 𝔇 depends only on (H,L), not on a choice of datum | `compressionCentralizerDefect` | EXACT | EXACT | literal | unconditional | - | NO | manifest from the definition |
| ID.04 |cor:generaltransport |Corollary 4.6: every $g \in G_{\mathrm{comp}}(L)$ acts in both directions on the bounded HS asymptotic commutant | `KazhdanAsymptoticCommutant.compressionGroup_transport_both_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through.  This row and ID.05 are closed by the SAME declaration, which is the only way the pair can both be exact: the any-universe wrapper was rewired on 2026-08-18 to consume `compressionGroup_transport_both_literal` in place of the finite-stage engine, so one theorem now carries both the printed quantification and the printed route |
| ID.05 |cor:generaltransport |forward transport for a one-sided conjugator s is Theorem 3.1 | `KazhdanAsymptoticCommutant.compressionGroup_transport_both_anyUniverse` |EXACT | EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18.  The old note read `Appendix B route`, and it was right: the corollary reached its conclusion through `KazhdanAsymptoticCommutant.transport`, the finite-stage engine, while the print says `forward transport for a one-sided conjugator s is Theorem 3.1`.  `Sofic/LiteralRouteTransport.lean` had carried a literal-route consumer written for exactly this row, but that module was OUTSIDE THE ROOT IMPORT CLOSURE and had never elaborated -- citing it would have been citing an unbuilt file.  It is in the closure and green as of today, one elaboration break repaired, and the badged declaration consumes it: `compressionGroup_transport_both_anyUniverse` goes through `compressionGroup_transport_both_literal`, `transport_literal`, `generalModelKazhdanTransport`, `manuscriptKazhdanTransport` -- the norm-ultraproduct proof printed under `thm:kazhdan-transport`.  What this row asserts is that the corollary applies Theorem 3.1, and it now does; the divergences inside Theorem 3.1's own proof are UF.01-UF.03 and the KT rows, and they stay there |
| ID.06 | cor:generaltransport | reverse transport: P = VPV* gives V*PV = P, so the fixed subspace is preserved both ways | `KazhdanAsymptoticCommutant.transport_star`; `PrintedReverseTransport.star_conj_eq_of_conj_eq`; `PrintedReverseTransport.conj_eq_iff_star_conj_eq`; `PrintedReverseTransport.act_fix_iff`; `UltraproductAdjointModel.starModel`; `KazhdanAsymptoticCommutant.transport_star_printed`; `KazhdanAsymptoticCommutant.transport_both_printed`; `KazhdanAsymptoticCommutant.compressionGroup_transport_both_printed` | EXACT | EXACT | literal | unconditional | - | NO | The first citation and the grade are unchanged and deliberately so: `transport_star` reverses the *approximate* leakage bound (`one_sub_moved_mul_corner_vanishing`), not the exact projection identity, and that remains an accurate description of the route the development runs.  **SECOND POINTER ADDED 2026-08-17 (module `Analysis/PrintedReverseTransport.lean`, wired at `3334e577`), without regrading.**  `star_conj_eq_of_conj_eq` is the printed exact identity `P = VPV* ⟹ V*PV = P`; `conj_eq_iff_star_conj_eq` is the literal "both ways" as a biconditional; `act_fix_iff` is the printed "hence `V*` preserves the fixed subspace".  So the printed step now exists in the tree — what does not yet exist is any *consumer* of it: FN.03 and ID.05 still route through the approximate bound, and a general-model transport module is being built before those one-line switches.  Regrade to the printed route when that rewiring lands and goes green, not before; citing a declaration the proof does not travel through is exactly the defect this ledger exists to catch.  **UPDATED 2026-08-18: one of the two named blockers is gone, the other is not.**  `ID.05` closed, and `FN.03` is now rewired -- `LiteralNonMFEndpoint.kazhdanPinning` travels `UltraproductRigidityRoute.compressionDefects_hsTrivial_literal`, whose transport step is `manuscriptKazhdanTransport` -- so neither of the consumers this note said were waiting on a general-model transport module is waiting any longer.  What has NOT changed is this row's own subject: the FORWARD direction is what was rewired, and `Analysis/PrintedReverseTransport` still has no consumer anywhere in the corpus, as `Sofic/LiteralRouteTransport` says in as many words -- "formalized abstractly in `Analysis/PrintedReverseTransport`, but not yet wired".  The route the development travels for the reverse half is still `transport_star`, still the approximate leakage bound, and the grade stays where it is for exactly the reason the note gives.  **THE CONSUMER NOW EXISTS, 2026-08-18, module `Sofic/PrintedReverseTransportRoute.lean`; THE GRADE IS STILL HELD, and only for the build reason.**  The clause above -- "`Analysis/PrintedReverseTransport` still has no consumer anywhere in the corpus" -- is no longer true.  `UltraproductAdjointModel.starModel` is that consumer and it is the printed sentence itself: exactly two fields of the KT.01--KT.09 bundle mention the conjugator, both absorption identities for `Q = V P V*`, and applying `PrintedReverseTransport.star_conj_eq_of_conj_eq` to `kt_10_conjugate_eq` -- which *is* the printed `P = VPV*` -- gives `V*PV = P` on the nose, so the same bundle is an adjoint model for `s⁻¹` with both absorption fields reducing to `P * P = P`.  Everything above it is the *existing* printed theorem applied once more at that model, so no transport is reproved and the reverse half is not an estimate, which is what this row asks for.  `transport_star_printed` is `transport_star`'s statement character for character; `transport_both_printed` is the printed corollary as the conjunction it prints; `compressionGroup_transport_both_printed` is `compressionGroup_transport_both_literal` with the reverse half exchanged, and is the declaration a badge on `cor:generaltransport` should cite.  One bookkeeping step is added and named: the ultraproduct route delivers the conclusion at `U_n(t⁻¹)` while the printed conclusion is at `V*`, and `KazhdanCornerMatrices.map_inv_vanishing` exchanges them in operator norm -- the same kind of shape change `Sofic/LiteralRouteTransport` already performs for the forward half, and it introduces no estimate of the transport.  **Why the grade is held:** the standard here is a green `Build and audit`, the only one this repository has is run `32122738761` on `c0f1e4e8`, and at that commit `Sofic/PrintedReverseTransportRoute` does not exist.  The module is MSI-green as a target and is in the root import closure, so the next green run settles it; until then the finite-stage `transport_star` is what the row records  **GRADED 2026-08-19: the held condition is met.**  The note above holds this row for exactly one stated reason -- "the standard here is a green `Build and audit`, the only one this repository has is run `32122738761` on `c0f1e4e8`, and at that commit `Sofic/PrintedReverseTransportRoute` does not exist".  It exists now and it has been certified.  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  At `dd1ca3546` `Sofic/PrintedReverseTransportRoute` is present and imported by `GroupApproximation.lean`, so the certifying build elaborated `UltraproductAdjointModel.starModel` -- the consumer the note identifies -- and the reverse half is the printed exact identity rather than the approximate leakage bound |
| ID.07 | cor:generaltransport | in the finite-stage proof the equal-rank reversal reverses the one-sided containment, with displacement and concentration unchanged | `KazhdanCornerMatrices.norm_one_sub_mul_flip`; `KazhdanCompressorCorner.one_sub_moved_mul_corner_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ID.08 | cor:generaltransport | the elements acting in both directions form a subgroup, hence contain $G_{\mathrm{comp}}(L)$ | `KazhdanAsymptoticCommutant.asymptoticCommutantStabilizer`; `KazhdanAsymptoticCommutant.compressionGroup_le_asymptoticCommutantStabilizer` | EXACT | EXACT | literal | unconditional | - | NO | the subgroup is constructed with its three closure proofs |
| ID.09 |thm:compression-radical |Theorem 4.7: finite normal $F \le$ 𝔇(H,L) dies in every norm matrix corona | `KazhdanAsymptoticCommutant.manuscriptCompressionRadical_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| ID.10 | thm:compression-radical | proof: $z \in C_H(L)$ puts $(U_{z,n})$ in the commutant; Corollary 4.6 puts $(U_{gzg^{-1},n})$ there | `KazhdanAsymptoticCommutant.finiteNormal_le_normMFResidual_of_le_compressionCentralizerDefect` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ID.11 | thm:compression-radical | hence each generator $[gzg^{-1}, \ell ]$ lies in $K_2$; normality gives 𝔇(H,L) $\le K_2$ | `compressionCentralizerDefect_le_ker` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ID.12 | thm:compression-radical | the coordinate Reynolds corner of Theorem 4.4 applies verbatim | `KazhdanCompressionCore.finiteNormal_le_normMFResidual_of_hyperlinear_killed` | EXACT | EXACT | literal | unconditional | - | NO | literally the same declaration, as the TeX asserts |
| NK.00 |thm:normal-kazhdan |Theorem 4.9: normal property-(T) $K \subseteq N_{\mathrm{comp}}$ dies in every corona representation | `ManuscriptExactWrappers.manuscriptNormalKazhdanObstruction_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| NK.02 | thm:normal-kazhdan | $N_{\mathrm{comp}} \subseteq K_2$ uses no finiteness, so every element of $K$ is HS invisible | `KazhdanCompressionCore.compressionDefects_eq_one_in_hyperlinearHom` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.03 | thm:abstract-nk | the image H̄ $= \Theta (H)$ is countable, hence MF, and Prop 2.4 along an exhaustion gives an asymptotic representation separating each fixed nontrivial element | `OperatorMFLocalNormalization.isNormApproximable_one` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.04 | thm:abstract-nk | property (T) passes to the $\Theta -image$, so K̄ $= \Theta (K) \le \Theta (D)$ is normal, Kazhdan and nontrivial | `HasKazhdanPropertyT.of_surjective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.05 |thm:abstract-nk |the classes $\pi (g) = [V_{g,n}]_\omega$ define a unitary representation on the Hilbert ultraproduct; for $h = \operatorname\{card\}(S)^{-1} \Sigma _{a\in S} \pi (a)$ the Kazhdan pair puts $sp(h)$ in $[-1,\theta ] \cup \{1\}$ with $\theta = 1 - \varepsilon _0^2/(4\cdot card$ S) | `VectorOmegaAction.OmegaUnitaryRep.rep'`; `VectorOmegaKazhdanGap.manuscriptNormalKazhdanSpectralGap` | EXACT | EXACT | literal |unconditional |- |NO | **CLOSED 2026-08-18, and the objects are now the printed ones.**  The old note was accurate and is worth keeping: the printed cut is the spectral projection of h in the norm ultraproduct, and the declaration cited before performed the same cut at a finite stage.  The reason it performed the cut at a finite stage is that the manuscript's ambient did not exist in the development.  `Sofic/KOmegaHilbert.lean` builds `K_omega`, the ultraproduct of the MATRIX spaces under a renormalized Hilbert-Schmidt inner product, whose ambient is the corona on the DOUBLED index because `B(K_n)` is `M_{d_n^2}`; this row is about the ultraproduct of the coordinate spaces `C^{d_n}` themselves, whose ambient is the corona on the undoubled index.  Four modules supply it: `Analysis/VectorHilbertUltraproduct.lean` builds `H_omega` with its inner product, `Analysis/VectorHilbertComplete.lean` makes it complete and hence a Hilbert space, `Analysis/VectorOmegaAction.lean` makes `B_omega` act on it, and `Analysis/VectorOmegaKazhdanGap.lean` applies the gap.  The first two were written but were OUTSIDE THE ROOT IMPORT CLOSURE and did not elaborate; the last two are new.  **The hypothesis is the manuscript's, not a stronger one.**  `OmegaUnitaryRep` asks only that the coordinate multiplication defects be null ALONG the ultrafilter, which is what an operator-norm asymptotic representation supplies; the representatives need not multiply at any stage and `V_{1,n}` need not be the identity.  Triviality at the unit is proved, not assumed: the class of the unit is idempotent by the defect at the pair (1,1) and injective because it is an isometry.  **The conclusion is the printed inclusion, not the estimate.**  `manuscriptNormalKazhdanSpectralGap` reads `spectrum R h` is contained in `Icc (-1) (1 - eps^2/(4 * card S))` union `{1}`, with the manuscript's own theta; the lower endpoint is the bound `norm h <= 1` because h averages unitaries, and the upper one is the Kazhdan gap.  ROUTE: the printed sentence asserts the inclusion and derives nothing, so there is no printed derivation to diverge from; the Lean derivation is `AbstractSpectralGap.unitaryAverage_spectrum_le`, which reaches the gap through a state annihilating `(m - mu)^2`, GNS, and the projection argument of `KazhdanEigenvalueBound`, rather than through the averaging computation `thm:kazhdan-transport` prints for its own gap.  NK.06 and NK.07 are NOT closed by this and keep their grades: what is now available and was not is the ambient, so the spectral projection at the isolated point can be taken by `CStarSpectralProjection` -- but the identification of its range with the fixed space, the nonvanishing of its complement, and the lift to coordinate projections are still not there |
| NK.06 | thm:abstract-nk | $P = \chi _{\{1\}}(h)$ lies in the norm ultraproduct with range Fix $\pi (\bar{K})$, and $q = 1 - P$ is nonzero because $q = 0$ would make $\pi$ trivial on K̄, against the operator-norm separation | `KazhdanCompressionCore.normalKazhdan_le_normMFResidual`; `UnitaryAverageFixedVector.spectralProjection_apply_eq_self_iff`; `VectorOmegaKazhdanGap.manuscriptNormalKazhdanFixedSpace` | EXACT | EXACT | literal | unconditional | - | NO | PARTIAL 2026-08-18, and the grade still does not move.  The row asserts three things.  **The first is now closed on the printed objects.**  `VectorOmegaKazhdanGap.manuscriptNormalKazhdanFixedSpace` is the printed range clause as an iff on `H_omega`: a vector is fixed by the spectral projection of `h` at the isolated point exactly when every `pi(a)`, `a` in the Kazhdan set, fixes it.  Both inclusions are in `Analysis/UnitaryAverageFixedVector.lean` and neither is about ultraproducts.  `ran P` inside `Fix` is the equality case of Cauchy-Schwarz through the absorption `h P = P`: averaging the inner products against the vector and taking real parts bounds every summand by the squared norm, a mean of such terms equal to it forces each to equal it, and the displacement squares to zero.  `Fix` inside `ran P` is the resolvent factorisation of `1 - P` in `KazhdanProjectionAbsorption.one_sub_spectralProjection_eq`, and that is where the gap is spent -- not in the calculation, which is one application, but in the factorisation's hypothesis that the spectrum meets the interval above the gap only in the point 1, which is what makes the resolvent continuous on the spectrum and the functional calculus available.  The gap it consumes is `manuscriptNormalKazhdanSpectralGap`, NK.05.  **Two clauses remain and both are named in the row.**  (1) The row says `P` lies in the NORM ULTRAPRODUCT, and what is proved puts it in `B(H_omega)`.  The projection is produced by the functional calculus of whatever ambient it is taken in, so what is wanted is the calculus run inside `B_omega`, a C-star subalgebra of `B(H_omega)` containing `h`, rather than anything further about `P`.  (2) The nonvanishing of the complement `1 - P`, whose printed reason is the operator-norm separation, is a different argument from either inclusion and is not here.  Objects stay `substituted` and both columns stay MISMATCH until those land  **SECOND CLAUSE ALSO CLOSED 2026-08-18, and the third is now the only open one on this row.**  The nonvanishing of the complement is `VectorOmegaKazhdanGap.manuscriptNormalKazhdanComplementNeZero`, and it is an IFF rather than an implication: `1 - P` is nonzero exactly when some `pi(a)`, `a` in the Kazhdan set, is not the identity.  That is the printed reason -- `q = 0` would make `pi` trivial on the Kazhdan subgroup -- with nothing added: whatever supplies the nontriviality, in the manuscript the operator-norm separation, supplies the nonvanishing through this, and no estimate of its own is needed.  It follows from the range identification in one step, since `P = 1` says every vector is fixed by `P` and hence by every `pi(a)`.  **What is left is the clause that `P` lies in the NORM ULTRAPRODUCT and not merely in `B(H_omega)`, and the blocker is now identified and is infrastructure rather than mathematics.**  `Analysis/OmegaCoronaKazhdanProjection.lean` assembles the omega-corona as a bundled unital complex C-star-algebra at an arbitrary `NeBot` filter -- six field assignments, every parent instance having been available at that generality all along and assembled only at `cofinite`.  Using it is what fails: through the reducible `FilterMatrixCoronaAlgebra (Idx Y) omega` the elaborator overruns the default `synthInstance` budget on questions as basic as `HMul`.  That is exactly why `NormMatrixCStarCorona` is an OPAQUE synonym with its instances registered, and the omega side needs the same treatment; the module records the two dead ends, a projected `NormedRing` instance (creates a second path and breaks definitional matching) and a heartbeat bump (a `check.py` finding, and a peer lane spent today removing them).  **And even that clause alone would not move the row**, because the row is one sentence: it asks for a `P` that lies in `B_omega` AND has range `Fix`.  Proving the two halves in two ambients leaves the conjunction unproved; joining them needs `VectorOmegaAction.actQ` upgraded to a continuous unital star-homomorphism into `B(H_omega)`, after which the two projections agree by naturality of the functional calculus.  Objects stay `substituted`, both columns stay MISMATCH  **THE REMAINING CLAUSE IS NOW A ONE-SENTENCE REDUCTION, 2026-08-18.**  `VectorOmegaAction.actStarAlgHom` is the action of the bounded sequences on `H_omega` as a UNITAL STAR-HOMOMORPHISM of complex algebras, which is the object the note above said was needed: linearity and multiplicativity and unitality in the algebra variable were already there, `norm_actQ_le` makes each class a bounded operator (that lemma was named in the module docstring and had not been proved; it is now, from the coordinate mass estimate and the `lp` coordinate bound pushed through the ultralimit, which needed `ulim_mono` and `ulim_const_smul` added to the ultrafilter API), and `inner_actQ` carries `star` to the adjoint by exchanging the order of summation at a coordinate and taking the ultralimit.  It kills the omega-null sequences, so it is the action of `B_omega` written on representatives.  With it the clause reduces to one standard C-star fact the pinned Mathlib does not have.  `Mathlib.Analysis.CStarAlgebra.ContinuousFunctionalCalculus.Range.range_cfcHom` gives `cfc f h` in `elemental C h`, the CLOSED unital star-subalgebra generated by `h`; and `h`, the average of the classes, is in the range of `actStarAlgHom` by linearity.  So `P` lies in the norm ultraproduct as soon as that range is closed -- which is true for any star-homomorphism of C-star-algebras and is not in the pin.  That is the whole of the remaining clause: not a fact about `P`, not a fact about omega, and not the instance-tower problem recorded above, which the star-homomorphism route bypasses by never needing the bundled class on the omega-corona at all -- **THE LAST CLAUSE IS CLOSED, AND WITH IT THE CONJUNCTION, 2026-08-18.**  `OmegaCoronaKazhdanProjection.manuscriptNormalKazhdanProjectionInBOmega` produces ONE `p`: an element of `B_omega`, self-adjoint and idempotent, whose action on `H_omega` is exactly the spectral projection `VectorOmegaKazhdanGap` forms there, whose fixed vectors are exactly the vectors the Kazhdan set fixes, and whose complement is nonzero as soon as `pi` is nontrivial on that set.  The two halves are therefore about the same `P`, and the row's single sentence is proved rather than its two conjuncts separately.  **The route is the Kazhdan argument run one level down, and the closed-range fact the previous note identified is neither used nor needed.**  `coronaClass` and `coronaRep` make the classes `pi(g) = [V_{g,n}]_omega` a unitary representation IN `B_omega`: the representatives are unitary at every stage, the asymptotically multiplicative family becomes exactly multiplicative because a sequence null along `omega` has class zero, and `pi(1) = 1` is proved rather than assumed, since an idempotent unitary is the identity.  `AbstractSpectralGap.unitaryAverage_spectrum_le` is stated for a unitary representation in an ARBITRARY unital C-star algebra, so `coronaSpectrumInclusion` is the printed inclusion `sp(h) subset [-1, theta] union {1}` for the average taken in the ultraproduct, and `manuscriptKazhdanProjection` is the same functional calculus taken there.  `actCorona_manuscriptKazhdanProjection` identifies its image with the `B(H_omega)` projection through `StarAlgHom.map_cfc`.  **The gap has to be proved in `B_omega` and cannot be transported**: a star-homomorphism only shrinks the spectrum, so the gap on the image would not make the gap indicator continuous on the spectrum of the preimage, which is the hypothesis the naturality consumes.  Infrastructure landed with it, and it is what the previous note asked for: `Analysis/FilterMatrixCStarCorona.lean` is the opaque corona at an arbitrary filter -- the analogue of `NormMatrixCStarCorona` the old note named -- with the universal property and the contractivity that supplies `map_cfc`'s continuity hypothesis, and `Analysis/VectorOmegaCoronaAction.lean` descends the action to that quotient.  The two recorded dead ends are bypassed rather than retried, and the transparent-quotient C-star assembly that created a second path to the ring structure is gone.  Two things are deliberately NOT claimed.  The action of `B_omega` on `H_omega` is not asserted injective and nothing uses it, which is why the complement clause transfers only in the printed direction -- nontriviality gives `1 - p != 0`, and the converse would need injectivity.  And the fixed-vector characterisation is stated at the Kazhdan SET; the passage from it to the subgroup the set generates, which is the printed `K-bar`, is `UnitaryAverageFixedVector.apply_eq_self_of_mem_closure`, and the two describe the same set of vectors.  Every module named here is green on an MSI target build (`OmegaCoronaKazhdanProjection`, log tag 35389).  Both columns stay MISMATCH and objects stays `substituted` pending the first green `Build and audit`: every CI run since the work landed has been cancelled by the newest-wins gate, so there is no run id to cite  **GRADED 2026-08-19: the held condition is met.**  The note above holds all three cells for exactly one stated reason -- "Both columns stay MISMATCH and objects stays `substituted` pending the first green `Build and audit`: every CI run since the work landed has been cancelled by the newest-wins gate, so there is no run id to cite".  There is one now.  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  At `dd1ca3546` the three modules the note names -- `Analysis/OmegaCoronaKazhdanProjection`, `Analysis/FilterMatrixCStarCorona` and `Analysis/VectorOmegaCoronaAction` -- are all present and imported by `GroupApproximation.lean`, so `manuscriptNormalKazhdanProjectionInBOmega` was elaborated by the certifying build.  That declaration produces ONE `p`, in `B_omega` and with range the fixed vectors, so the row's single sentence is what is proved and the objects are the printed ones |
| NK.07 | thm:abstract-nk | normality gives $\pi (g)P\pi (g)* = P$, so $P$ and $q$ commute with $\pi (\bar{H})$ exactly; lifting $q$ to projections $q_n$ and compressing yields an operator-norm asymptotic representation of H̄ on nonzero blocks | `VectorOmegaKazhdanGap.manuscriptNormalKazhdanConjugation`; `OmegaCoronaKazhdanProjection.manuscriptNormalKazhdanConjugationInBOmega`; `OmegaCoronaKazhdanProjection.manuscriptNormalKazhdanCommutationInBOmega`; `OmegaCoronaKazhdanProjection.manuscriptComplementProjectionLift`; `PrintedDiagonalSubsequence.exists_strictMono_mem_tendsto_zero`; `PrintedCornerCompression.PrintedCornerData.cornerRepresentation`; `PrintedCornerCompression.PrintedCornerData.manuscriptCornerAsymptoticRepresentation`; `PrintedCornerRelabelling.manuscriptCoronaCornerRepresentation`; `PrintedCornerRelabelling.exists_relabelling_full`; `PrintedCornerRelabelling.manuscriptCoronaCornerRepresentationUnconditional` | EXACT | EXACT | literal | unconditional | - | NO | **UPGRADED 2026-08-17 from MISSING/MISSING, and deliberately not to EXACT.**  The old note said the exact commutation of the Kazhdan projection with the ambient representation has no formal counterpart.  It has one, and `InternalRadicalGap`'s module docstring states this very step as the file's design principle: normality "will instead force its fixed-space projection to be ambient-invariant".  Every printed clause is proved there.  Normality enters as `normalConjugate`, and `subgroup_displacement_mul_ambient_top_vanishing` is exactly "normality converts subgroup top-corner invariance into approximate invariance of an ambient translate of that corner".  The commutation is the vanishing of the two off-diagonal blocks, `ambientMovingToTopBlock_vanishing` and `ambientTopToMovingBlock_vanishing`, whose own docstring names normality and the subgroup spectral gap as what makes them vanish.  The compression is `ambientMovingCompression`, and the schedule packages it: `model_nonempty` is the nonzero blocks and `map_multiplicative_eventually_op` the operator-norm asymptotic representation, which `NormalKazhdanCompressionObstruction.normalCornerOpAlmostRepresentation` then reads as an `OpAlmostRepresentation`.  Not EXACT, for two reasons of the KT.01 kind.  The print asserts an exact identity in the ultraproduct; these declarations assert that finite-stage blocks vanish asymptotically, which is the same fact only after the passage to the ultraproduct that the printed P already presupposes.  And P is not a literal object here: NK.05 and NK.06, which introduce it, are themselves MISMATCH over substituted objects, so a literal grade at NK.07 would outrun the rows it rests on.  The superseded route the old note named, `KazhdanCompressorCorner.displacement_vanishing`, remains superseded  **PARTIAL 2026-08-18; the commutation clause's content is now unconditional and the grade does not move.**  The row's first clause is `pi(g) P pi(g)* = P`, and two facts underlie it, neither of which needs normality, ultraproducts, or the Kazhdan gap: `UnitaryAverageFixedVector.eq_of_isSelfAdjoint_idempotent_of_fix_iff`, a self-adjoint idempotent is determined by the vectors it fixes, and `UnitaryAverageFixedVector.conj_apply_eq_self_iff`, conjugating one by a unitary conjugates the fixed set.  The first is shorter than the orthogonal-projection route it replaces: if the two agree on what they fix then `Q P = P`, taking adjoints gives `P Q = P`, and the symmetric argument gives `P Q = Q`.  So the printed step reduces to one sentence -- normality says `pi(g)` preserves `Fix`, hence `pi(g) P pi(g)*` and `P` fix the same vectors, hence they are equal -- and what is left of it is supplying that preservation from normality of the subgroup, which is group bookkeeping over the passage from the Kazhdan SET to the subgroup it generates.  **The row's second clause is untouched and is the larger half**: lifting `q` to coordinate projections `q_n` and compressing to get an operator-norm asymptotic representation of the ambient group on nonzero blocks.  Both columns stay MISMATCH over substituted objects until that lands -- **PARTIAL 2026-08-18, second note today; the first clause is closed twice over and the second clause's opening sentence is closed.**  On the printed objects: `VectorOmegaKazhdanGap.manuscriptNormalKazhdanConjugation` is the printed argument itself -- the conjugated projection has range `Fix pi(g K-bar g^-1)`, normality makes that `Fix pi(K-bar)`, and a self-adjoint idempotent is determined by its fixed vectors.  The group bookkeeping the previous note named as what was left is now `UnitaryAverageFixedVector.apply_eq_self_of_mem_closure` and `forall_apply_eq_self_translate_iff`, and the hypothesis is the printed one and nothing stronger: the subgroup GENERATED by the Kazhdan set is normal in the ambient group, which is the printed `K-bar` because the print takes `S` to be a generating Kazhdan set of it.  **And in the printed ambient**: `KazhdanProjectionAbsorption.conj_spectralProjection_eq_of_normal` proves the same identity inside `B_omega` with no vectors at all, from three bricks that were already in that file and had not been put together -- the absorption identities make every unitary of the Kazhdan set fix `P`, `unitary_mul_eq_of_closure` extends that to the generated subgroup, and `conjugated_projection_absorbs` turns the conjugated average's absorption into `Q P = P`; the same at the inverse element gives `P Q = Q`, and adjoints of the first give `P Q = P`, so `Q = P`.  That algebraic form is the one the lift needs: an equality of CLASSES is what makes a commutator of representatives vanish, and an equality of the operators those classes act by would not, the action not being known injective.  Neither proof spends Dedekind finiteness, which is what `nk_07_conjugate_proj` spends.  **The second clause's opening sentence is closed on the printed objects.**  `OmegaCoronaKazhdanProjection.manuscriptComplementProjectionLift` gives all three printed clauses of 'Lift `q` to projections `q_n` in `M_{d_n}(C)`, nonzero along `omega` and with `‖[q_n, V_{g,n}]‖ ->_omega 0` for every `g` in `H-bar`': genuine orthogonal projection matrices, not approximate ones; membership of `{n : q_n != 0}` in `omega`, from the operator-norm separation through the nontriviality hypothesis and with no estimate of its own; and the commutator vanishing, which is exact commutation in `B_omega` read on representatives.  `Analysis/CoronaProjectionLifting.lean` is the projection analogue of the unitary lifting `PolarLiftingGeneralCStar` already had at an arbitrary filter: a self-adjoint representative exists by averaging with the adjoint, its coordinate idempotence defects vanish along the filter because their class is `p^2 - p = 0`, and rounding each coordinate at `1/2` in its own eigenbasis gives projections within TWICE the defect, dimension-free.  The scalar rounding inequality was in the development only under `abs lambda <= 1`, which the finite-subgroup average satisfies and a representative of an arbitrary projection does not; it needs no such side condition and the unconditional form is proved.  **What remains of the row, precisely.**  The lift of `h` to the printed coordinates `h_n = (2 card S)^-1 sum_{a in S} (V_{a,n} + V_{a,n}*)`; the corner spectral inequality `q h q <= theta q` in its coordinate form `‖(q_n h_n q_n - theta q_n)_+‖ ->_omega 0`, which is the identified blocker and has NO substrate in the development -- there is no positive part, no non-unital calculus, and no transfer of a Loewner inequality between the corona and its coordinates, the only coordinatewise computation available being the limsup norm formula `norm_filterMatrixCorona_mk_eq_limsup`; the diagonal subsequence `A_j`, for which `Analysis/UltrafilterDiagonalExtraction.lean` is ready; and the polar-corrected corner compression, which exists three times over at finite stages (`KazhdanCornerPolar`, `KazhdanCornerModel`, `InternalRadicalGap`) and not for the corona.  Green on MSI target builds; both columns stay MISMATCH over substituted objects, and there is no green `Build and audit` to cite because every CI run since has been cancelled by the newest-wins gate  **THE IDENTIFIED BLOCKER IS GONE, 2026-08-18 (third note).**  The previous note named the corner spectral inequality in its coordinate form as the blocker, with NO substrate in the development -- no positive part, no non-unital calculus, no transfer of a Loewner inequality between the corona and its coordinates.  None of that was needed and none of it was built.  Three more printed sentences are now closed.  **`q h q <= theta q` in `B_omega`** is `OmegaCoronaKazhdanProjection.manuscriptCornerCompression`, from `KazhdanProjectionAbsorption.compression_le_gap`, which proves it for the spectral projection at an isolated top of the spectrum in ANY C-star algebra: both sides are continuous functions of `h` -- the complementary projection is one too -- and the inequality is the pointwise inequality of those functions on the spectrum, where the gap indicator takes only the values `0` and `1`, so the difference collapses to `(1 - chi)(t)(theta - t)`, which is `theta - t >= 0` below the gap and `0` at the isolated point.  The ultraproduct contributes only the gap.  **The printed coordinates of `h`** are `hermitianAverageSeq`, the formula `h_n = (2 card S)^-1 sum_{a in S} (V_{a,n} + V_{a,n}*)` on the nose, self-adjoint at every stage and not merely along `omega`, and `mk_hermitianAverageSeq` is the printed reason it represents `h`: the average of the classes is already self-adjoint, so adding its adjoint and halving returns it.  **The coordinate form of the corner inequality** is `manuscriptCornerCompressionCoordinates`, over the transfer `CoronaProjectionLifting.eventually_posSemidef_smul_one_add`.  The route is one line of C-star algebra: a nonnegative element is `star b * b`, so a representative of `b` makes the coordinate difference `z_n - b_n^H b_n` null along the filter, `b_n^H b_n` is positive semidefinite on the nose, and the remaining error is absorbed by any tolerance because a Hermitian matrix is bounded below by minus its norm -- read off the eigenbasis, where the shifted matrix is diagonal with entries `epsilon + lambda_i`.  The transfer therefore needs no order instance in its statement and no positive part in its proof, and its conclusion is the order bound `x <= epsilon` that a bound on the norm of the positive part of `x` abbreviates, which is also the form the trace estimate consumes.  **What remains of the row is now the tail of the paragraph and nothing before it**: the diagonal subsequence `A_j`, for which `Analysis/UltrafilterDiagonalExtraction.lean` is ready and unused; the polar-corrected corner compression on these ultraproduct objects, which exists three times over at finite stages (`KazhdanCornerPolar`, `KazhdanCornerModel`, `InternalRadicalGap`) and has to be re-pointed rather than built; and the renormalized-trace estimate and the contradiction, which are `NK.08`, `NK.09` and `NK.13` and are already EXACT over the finite-stage objects.  Two mechanical hazards were found and are worth the space, because both cost build cycles and neither is a mathematical obstruction.  `lp(M_{d_n})` has a MULTIPLICATION diamond: the ring multiplication appearing in a term and the one `star_mul` is stated against are definitionally equal but not syntactically, so `rw [star_mul]` fails there and `simp only [star_mul]` succeeds.  The opaque corona has the matching diamond for its SCALAR action, so `smul_add`, `smul_smul` and `mul_smul` must be applied and not rewritten.  Every module named here is green on MSI target builds; both columns stay MISMATCH over substituted objects, and the `Build and audit` run on the commit that landed this work is still in progress, with no green run in the last sixty  **BUILD SENTENCE CORRECTED 2026-08-19, WITHOUT A REGRADE.**  The clause above -- "the `Build and audit` run on the commit that landed this work is still in progress, with no green run in the last sixty" -- is spent.  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  The grades do not move on it, because this row was never held on the build alone: the reasons the note gives are that the print asserts an exact identity in the ultraproduct while these declarations assert that finite-stage blocks vanish asymptotically, and that `P` is not a literal object here.  The second reason is now weaker than it was, since `NK.06` is graded above, but the first is untouched and is sufficient on its own  **GRADED 2026-08-19: EXACT/EXACT on literal objects, and both of the note's standing reasons are spent.**  The note held the row on two things.  (i) "The print asserts an exact identity in the ultraproduct; these declarations assert that finite-stage blocks vanish asymptotically."  That stopped being true when the commutation moved into `B_omega`: `manuscriptNormalKazhdanConjugationInBOmega` and `manuscriptNormalKazhdanCommutationInBOmega` are the printed exact identities `pi(g) P pi(g)* = P` and `q pi(g) = pi(g) q` in the printed ambient, and the finite-stage statement is no longer standing in for them -- it is the printed *lift*, which the print performs on purpose.  (ii) "`P` is not a literal object here: NK.05 and NK.06 ... are themselves MISMATCH over substituted objects."  Both are `EXACT`/`literal` above, so a literal grade here no longer outruns the rows it rests on.  **What the previous note left open was the tail of the clause, and it is closed.**  It named three things: the diagonal subsequence `A_j`, for which `Analysis/UltrafilterDiagonalExtraction` was ready and unused; the polar-corrected corner compression on the ultraproduct objects, which existed three times over at finite stages and not here; and the join of the two onto the lifted projections.  All three landed on 2026-08-19.  `Analysis/PrintedDiagonalSubsequence.lean` is the printed `A_j` itself: the extraction chooses the subsequence INSIDE a prescribed `omega`-large set, which is the printed first constraint `q_n != 0` and the one clause the parent extraction could not carry, since it is a membership and not a convergence.  `Analysis/PrintedCornerCompression.lean` is the compression: a Hermitian idempotent has eigenvalues `0` and `1` and in its own eigenbasis IS the indicator of its corner coordinates, so the printed `q_n M_{d_n}(C) q_n = M_{r_n}(C)` is a concrete identification and not an appeal to one; the corner is nonempty exactly because `q_n != 0`, which is the printed "on nonzero blocks"; and both estimates come from ONE quantity, because an off-diagonal block between complementary coordinate families is a two-sided cut and a two-sided cut is a commutator, so `‖[q_n, V_{g,n}]‖ -> 0` is the whole input.  The Gram defect of a compressed unitary is the square of that block and the multiplicativity defect is the ambient defect plus a product of two of them.  `cornerRepresentation` is the printed `(W_{g,n})` as an `OpAlmostRepresentation` -- which is this development's definition of the printed "operator-norm asymptotic representation" -- and `cornerMap_sub_compress_tendsto` says the polar correction is asymptotically invisible, so the representation is the compression in the limit.  `Analysis/PrintedCornerRelabelling.lean` is the join, and it is the declaration that makes the objects literal rather than merely available: `manuscriptCoronaCornerRepresentation` takes the corona data, runs `manuscriptComplementProjectionLift` for the projections, NAMES their class in `B_omega` as `1 - P` for the printed `P` in its own conclusion, relabels along the printed `A_j`, and returns the corner representation on those projections.  Nothing is passed in by hand.  **One hypothesis is a printed input rather than a derivation, and it is stated as such**: `hmul`, that `(V_{g,n})` is an operator-norm asymptotic representation in the ORDINARY sense.  `OmegaUnitaryRep` carries only the `omega` form, and the print has the ordinary one at this point because the models come from `prop:mf-equivalences` along an exhaustion of `H-bar`, which is `NK.03` and is `EXACT`.  Taking it from the print is faithful; deriving it from the `omega` form would not be, since the ordinary statement is strictly stronger.  The guard on polar correction -- `W_{g,n} = 1` where the Gram defect exceeds `1/2` -- is the manuscript's own device at `lem:lift`, "set `u_n = 1` at the finitely many remaining indices", row `PRE.11`, and it is applied here at the same kind of set and for the same reason.  All three modules are root-imported and green on the MSI root-closure build of 2026-08-19 (targets `GroupApproximation.Analysis.PrintedDiagonalSubsequence`, `...PrintedCornerCompression`, `...PrintedCornerRelabelling`, then the `GroupApproximation` root).  The build marker this row carried is dropped, because the condition it guards -- a named module that has never once compiled -- does not hold: every declaration cited here was elaborated on the cluster before this row moved  **THE ONE HYPOTHESIS IS DISCHARGED 2026-08-19, AND THE SENTENCE ABOVE THAT RECORDS IT AS A PRINTED INPUT IS SUPERSEDED.**  The note above says `hmul` -- that `(V_{g,n})` is an operator-norm asymptotic representation in the ORDINARY sense -- is taken from the print rather than derived, on the ground that `OmegaUnitaryRep` carries only the `omega` form and that deriving the stronger statement from the weaker one is impossible.  Both halves of that are true and the conclusion did not follow: the hypothesis does not have to be derived from the `omega` form, it has to be extracted with everything else.  `H-bar ⊕ H-bar × H-bar` is countable whenever `H-bar` is, so the multiplication defects go into the printed `A_j` beside the commutators and come out as ordinary limits with them, along the same single subsequence.  `exists_relabelling_full` performs that extraction and `manuscriptCoronaCornerRepresentationUnconditional` is the second clause with `hmul` gone: every remaining hypothesis is corona data or a printed hypothesis of `thm:abstract-nk`, and nothing in it is a statement the printed proof gets from elsewhere and this development takes on trust.  The conditional form is kept beside it, because it is the one that matches the printed `A_j` literally -- the print constrains `q_n != 0` and the commutators and no more, since at that point it already has the ordinary form from `NK.03` -- and a reader comparing the file to the page should find the printed set there.  The row is graded on the unconditional one.  Green on the MSI target build of 2026-08-19 |
| NK.08 | thm:abstract-nk | tracial deficit: $q_nh_nq_n \le \theta q_n$, so some generator $s_0$ has compressed real renormalized trace $\le \theta$ | `KazhdanCompressionCore.normalKazhdan_le_normMFResidual` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.09 | thm:abstract-nk | hence normalized HS distance $\ge \sqrt (2(1-\theta )) - o(1)$ after polar correction; a subsequence fixes $s_0$ | `KazhdanCompressionCore.normalKazhdan_le_normMFResidual` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.13 | thm:abstract-nk | contradiction: the corner models are an asymptotic representation and $s_0 \in$ K̄ $\le \Theta (D)$ is 2-norm trivial there | `KazhdanCompressionCore.normalKazhdan_le_normMatrixCStarCoronaKernel_of_hyperlinear_killed` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.14 | thm:abstract-nk | Theorem 4.10: the same conclusion from HS invisibility of a subgroup D, taken as hypothesis | `ManuscriptExactWrappers.manuscriptAbstractNormalKazhdanObstruction` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptAbstractNormalKazhdanObstruction` reads `{H : Type u}`; the invisibility hypothesis is a property of the ambient group, not an imported theorem.  the invisibility hypothesis is a property of the ambient group, not an imported theorem |
| NK.15 | thm:abstract-nk | every operator-norm almost representation of H̄ composes with $\Theta$, so $\Theta (D)$ inherits invisibility | `KazhdanCompressionCore.normalKazhdan_le_normMatrixCStarCoronaKernel_of_hyperlinear_killed` | EXACT | EXACT | literal | unconditional | - | NO | - |
| NK.16 |cor:intrinsic-nk |Corollary 4.11: normal (T) subgroup of 𝔇(H,L) dies in every corona representation | `ManuscriptExactWrappers.manuscriptIntrinsicNormalKazhdanRadical_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| NK.17 |p:nk-join |the subgroup generated by all normal (T) subgroups of 𝔇(H,L) also lies in the kernel of every corona representation | `ManuscriptExactWrappers.manuscriptIntrinsicNormalKazhdanPart_anyUniverse` | EXACT |EXACT |literal |unconditional |- |NO | CLOSED 2026-08-18 by badge repoint.  The universe restriction was the whole of the MISMATCH and it is gone: the declaration cited above states the printed sentence with the Kazhdan source at an arbitrary universe, which is what the print quantifies over, and property (T) supplies the countability the print states, so no hypothesis is added.  The manuscript badge and `CLAIM_TARGETS` now name this declaration; the Type-0 form it replaced is still in the tree and is what the any-universe form transfers through |
| KC.21 | rem:ff-realization | the finitely presented torsion-free property-\textup{(T)} group $P$ of \cite{FFF} contains every finitely presented torsion-free group, in particular a direct product $P_1\times P_2$ with $P_i\cong P$ | - | MISSING | MISSING | - | literature-input | FFF | NO | cited to `\cite[\S2]{FFF}`, which is public as of 2026-08-03 and carries the statement; the remark's earlier "personal communication, 2026" was replaced on 2026-08-19 and the acknowledgement kept.  Nothing formal, and **this row is the reason KC.22 and KC.23 are conditional on nothing else**.  `Sofic/RealizationFromUniversalGroup.lean` records the existence claim as the structure `ContainsSquare P`, an injective `(P × P) →* P`.  Verified 2026-08-17: that structure occurred in no other file in the tree, and inside its own file only as a bound argument — no `instance`, no constructor application, no definition producing one.  So the Fournier-Facio existence input is **not realized**, and no declaration anywhere silently supplies it.  The two rows below are theorems *about* the datum, and neither is evidence that the datum exists.  **SUPERSEDED IN PART, AND SPLIT, 2026-08-19; grade unchanged.**  The 2026-08-17 verification above is **no longer literally true** and is kept because the reason it changed is the point.  `Sofic/ContainsSquareWitness.lean` separates the row's two halves.  *(a) The `in particular` is an inference, and it is now proved.*  `containsSquare_of_universal` derives `ContainsSquare P` from `UniversalFPTorsionFree P` — the universality statement as a typed `Prop` structure with **no inhabitant anywhere** — for any finitely presented torsion-free `P`, because `P × P` is finitely presented (`ProductFinitePresentation.instProd`, this repository's own theorem, Mathlib closing finite presentation under free but not direct products) and torsion-free (`Prod.instIsMulTorsionFree`).  `realization_of_universal` then runs the whole remark off that one input.  So what the row owes to `\cite[\S2]{FFF}` is now exactly one statement instead of one statement plus an unexamined *in particular*.  *(b) The datum is satisfiable, so KC.22 and KC.23 are not vacuous.*  `piContainsSquare` builds a `ContainsSquare (ι → G)` from any `ι ⊕ ι ≃ ι`, and `witnessContainsSquare` instantiates it at `ℕ → Multiplicative ℤ`; `witness_construction_hypotheses` exhibits an element at which all three inputs `thm:kazhdan-clifford` requires hold.  The witness is abelian (`witness_commute`), is registered as no `instance`, and carries neither `Group.IsFinitelyPresented` nor `HasKazhdanPropertyTComplex`, so it cannot reach `realization`.  **Consequence for the corpus audit, recorded here rather than left to be discovered:** `check_non_mf_unconditional.py --audit-corpus` previously listed `ContainsSquare` among the names no corpus declaration produces (14 assumers); it no longer does, because one now produces it.  That report is correct — the list is a vacuity detector and the vacuity question is answered — and the literature dependency has not vanished, it has moved onto `UniversalFPTorsionFree`, which the same list now carries (assumed by 2).  The row stays `MISSING`/`literature-input` on `FFF`: the universality statement is Fournier-Facio's theorem, nothing inhabits it, and the abelian witness is evidence about the *structure*, never about which groups satisfy it  **SPLIT INTO ITS TWO LITERATURE INPUTS, 2026-08-19; grade unchanged.**  `Sofic/FournierFacioUniversalGroup.lean` resolves the citation into the composite Fournier-Facio actually builds (arXiv:2608.02025 §2, the reading recorded at `research/fournier-facio-torsion-free-skeleton-proof.md`): *input 1* is a universal **finitely presented** torsion-free group `U` (Chiodo arXiv:1107.1489v4 Theorem 3.10, which is Belegradek's Theorem A.1), carried as `UniversalTorsionFreeHost`; *input 2* is a finitely presented torsion-free property-(T) group containing `U` (Osin small cancellation over the relatively hyperbolic pair `U * H` rel `U`, torsion clause Theorem 2.4.5), carried as `KazhdanEnvelope`; and the passage from those two to the printed sentence is `universal_comp`, which is now **proved** — universality transfers along an injective homomorphism, with no hypothesis on the target at all.  `UniversalKazhdanGroup.ofInputs` is the composite, so what this row owes the literature is exactly two statements and no inference.  Three consequences are proved off the composite and were previously nowhere: `exists_ne_one_of_universal` (the remark says "any `a ∈ P₂ \ {1}`" and never says why that set is inhabited — `Multiplicative ℤ` is finitely presented and torsion-free, so it embeds), `nonempty_containsSquare`, and `realization_of_universalKazhdanGroup`, which is the whole remark off the structure alone.  `universalFPTorsionFree_of_universalKazhdanGroup` feeds the older opaque input, so no consumer of `UniversalFPTorsionFree` is orphaned by the split.  **Half of input 1 is already unconditional in this repository.**  `Monsters/UniversalFinitelyPresentedTorsionFreeGroup.lean` builds, with no hypothesis, a countable torsion-free group absorbing every finitely presented torsion-free group, and `universal_of_embedding_universalPayload` shows that any injection of it into a finitely presented torsion-free group discharges input 1 outright — so what is owed there is the *upgrade to finite presentation*, not universality.  That is not a discharge: the explicit group is indexed by the presentation codes whose group is torsion-free, an undecidable set by Adian-Rabin (torsion-freeness is a Markov property), so it is countable but not recursively presented and Higman/Chiodo does not apply to it as it stands.  The row stays `MISSING`/`literature-input` on `FFF` because nothing inhabits `UniversalTorsionFreeHost` and nothing inhabits `KazhdanEnvelope`.  **Corpus-audit consequence, recorded here rather than left to be discovered:** `--audit-corpus` now carries three further undischarged names (`UniversalTorsionFreeHost`, `KazhdanEnvelope`, `UniversalKazhdanGroup`) where it carried one; the dependency was split, not increased, and each name says which half of the citation it is.  No declaration in the new modules concludes an input structure from premises that are bare logical formulas — `universal_comp` and `universal_of_embedding_universalPayload` conclude the universality *formula*, deliberately — so the producer fixpoint cannot mark any of the three discharged  **HIGMAN'S LEMMA 3.2 PROVED, 2026-08-19; the row still does not move.**  The hull was split further, in `Higman/Program.lean`, into the two theorems it is: *(A)* Chiodo Theorem 2.2 in general (`TorsionFreeHigmanEmbedding`) and *(B)* Chiodo Proposition 3.8 at the absorber (`AbsorberRecursivePresentation`); `nonempty_hull_of_inputs` composes them and nothing else, so the three names retire together.  Work on (A) landed: `Higman/Benign.lean` defines Higman's benign subgroups and proves the two constructions direct products give — intersections and kernels of maps to finitely presented groups; `Higman/BenignClosure.lean` proves the preimage half of Lemma 3.3; `Higman/HNNCentralizer.lean` proves `G ∩ Gᵗ = G ∩ L` from Mathlib's Britton lemma; `Higman/AmalgamPresentation.lean` and `Higman/AmalgamPushout.lean` prove that the amalgam over a finitely generated subgroup is finitely presented and identify it with Mathlib's `PushoutI`; `Higman/Pinch.lean` proves the pinch lemma (`⟨Z, Qᵗ⟩ ∩ P = Z` when `Q ⊓ M ≤ Z` and `Z ⊓ M ≤ Q`) by a block-word induction over Britton; and `Higman/BenignJoin.lean` proves **Higman's Lemma 3.2(2)** — benign subgroups are closed under joins — by gluing the two witnesses with a *direct product* rather than with the literature's nested amalgam of HNN extensions, and applying the pinch lemma twice.  So Lemma 3.2 is complete.  What (A) still owes is Higman's Section 4, his Section 2, and the rope trick; what (B) owes is recorded at `notes/HIGMAN_BENIGN_LANE.md`, with `Monsters/RecursiveCodeAbsorber.lean` rebuilding the absorber over the `Primcodable` coding so that the statement can be made at all.  Nothing inhabits (A) or (B), so nothing inhabits the hull, the host, or `KC.21`  **THE ROPE TRICK IS PROVED, 2026-08-19; the row still does not move.**  `Higman/PinchGraph.lean` refines the pinch descent so that it carries a value, which produces the homomorphism `τ : ⟨A, Aᵗ⟩ → F ⧸ N` that is the quotient map on `A` and trivial on `Aᵗ` — the map whose existence *is* the amalgam structure of `⟨A, Aᵗ⟩` over `N`, and the only thing the rope trick needs from it.  `Higman/RopeTrick.lean` then proves **Higman's Lemma in the hard direction**: `fpOvergroup_of_benign` — a benign normal subgroup of a finitely generated, finitely presented group has a quotient that embeds in a finitely presented group.  The construction is the classical one (a second stable letter conjugating `S × 1` onto the graph of `τ`), and the finite presentation is the classical argument that the relations of the quotient are consequences of finitely many, carried out here inside an explicit quotient of `(Γ × F) ∗ ℤ` by four relators per generator.  `Higman/RowSubgroup.lean` proves Higman's Example 3.2's first case — the row subgroup `⟨bᵢ : i ∈ ℤ⟩` is benign in `⟨b, c⟩` — by the ascending HNN extension along `b ↦ b^c`, the collecting process, and the stable-letter grading `HNNTorsionFree.lengthHom` this repository already had.  **Consequence for the input list.**  `Higman/EmbeddingTheorem.lean` now derives Higman's embedding theorem for finitely generated groups from *one* structure, `Higman.REBenign` (a recursively enumerable normal subgroup of a free group of finite rank is benign — his Sections 2--4), because the other half is the rope trick, which is proved.  `REBenign` is registered in `scripts/Audit.lean`.  Two further steps still separate `REBenign` from `KC.21`'s input (A): the Higman--Neumann--Neumann bridge from countably generated to finitely generated, and Chiodo's torsion-order clause; both are named in `Higman/Program.lean` and neither is formalized.  Nothing inhabits `REBenign`, so nothing inhabits (A), (B), the hull, the host, or this row  **THE BRIDGE'S GROUP THEORY IS PROVED, 2026-08-19.**  `Higman/HNNEmbedding.lean` proves the Higman--Neumann--Neumann embedding — every countable group embeds in a three-generator group — by the same action device that `Higman/RowBasis.lean` and `Higman/ConjugateBasis.lean` use for the two free-basis facts: a state that records what the word has to remember, so that no normal form theorem for free products is needed.  What is left of input `CountableToFG` is therefore only the two standard torsion facts (`FreeProductTorsionFree`, `HNNTorsionFree`) and the effectivity clause (`BridgeRecursive`), and `countableToFG_of_inputs` composes them; free groups being torsion-free is Mathlib's and is used rather than assumed.  On the Higman side, `Higman/SequenceSpace.lean` builds the sequence-space dictionary and proves it faithful, and three of the nine Higman operations are now proved: `ι` and `υ` there, `σ` in `Higman/ShiftOperation.lean`  **TWO INPUTS RETIRED BY PROOF, AND THE TORSION CLAUSE REDUCED, 2026-08-19.**  The bridge's torsion clause is no longer assumed: `Algebra/CoprodICyclicReduction.isPowerTorsionFree_coprodI` (free products of torsion-free groups) carried to the binary product by `Higman/CoprodTorsion.lean`, together with this repository's own `HNNBritton.isPowerTorsionFree_hnn`, proves `Higman.torsionFree_ext` outright, so `FreeProductTorsionFree` and `HNNTorsionFree` are deleted from `scripts/Audit.lean` in the same commit that proves them.  `Higman/RopeTorsion.lean` then shows Chiodo's torsion clause is not an extra theorem about the rope trick at all: `torsionFree_ropeGroup` proves the rope group torsion-free as soon as the benign witness and the quotient are, so the clause is exactly the demand that the witness be torsion-free.  `REBenignTF` is that refinement, and it replaces both `REBenign` and `TorsionPreservation`, which are now derived from it.  **The input list for `KC.21`'s first half is therefore down to three**: `Higman.REBenignTF` (Higman's Sections 2--4 with torsion-free witnesses), `Higman.BridgeRecursive` (the effectivity clause of the Higman--Neumann--Neumann bridge, whose group theory and torsion clause are proved), and `Higman.AbsorberRecursivePresentation` (Chiodo Proposition 3.8 at the absorber).  Nothing inhabits those three |
| KC.22 | rem:ff-realization | choosing an isomorphism $\alpha : P \to P_1 \le P$ and any a $\in P_2$ \ $\{1\}$ gives the input required by \ref{thm:kazhdan-clifford} | `RealizationFromUniversalGroup.construction_hypotheses`; `RealizationFromUniversalGroup.ContainsSquare.alpha_injective`; `RealizationFromUniversalGroup.ContainsSquare.not_surjective_alpha`; `RealizationFromUniversalGroup.ContainsSquare.emb_inr_not_mem_range_alpha`; `RealizationFromUniversalGroup.ContainsSquare.disjoint_factors`; `RealizationFromUniversalGroup.not_mem_range_of_mem_disjoint`; `RealizationFromUniversalGroup.realization` | EXACT | EXACT | literal | unconditional | FFF | NO | **REGRADED 2026-08-17 from MISSING/MISSING; the row was graded conditional on KC.21 and it is not.**  Given the datum, the printed sentence is a theorem with no further premise: `construction_hypotheses` is the three inputs `thm:kazhdan-clifford` requires, in the printed order — `α` injective, `α` not surjective, and `a ∉ range α` — proved from `alpha_injective`, `not_surjective_alpha` and `emb_inr_not_mem_range_alpha`, the last through `disjoint_factors` and `not_mem_range_of_mem_disjoint`.  `realization` then instantiates `kazhdanCliffordConstruction` and returns finite presentability, a nontrivial central involution and non-MF, for *every* finitely presented Kazhdan group containing a square of itself.  `Deps` is `unconditional` on the reasoning already applied at LI.12a: `h : ContainsSquare P` is an explicit argument bound in every statement, not an unmet premise, and the printed sentence is itself of the form "choosing such data gives the input".  `Source` keeps `FFF` because the sentence sits inside a remark attributed there, and KC.21 carries what is actually owed to it.  Lean's `alpha` is `emb ∘ inl`, an injective endomorphism with range the first factor — the printed `α : P ≅ P₁ ≤ P` read as a map into `P`, which is how `thm:kazhdan-clifford` consumes it |
| KC.23 | rem:ff-realization | $E(P,\alpha ,a)$ has torsion even though $P$ does not: the theorem supplies a nontrivial central involution $w$, whereas adjoining a generator with $c^2 = 1$ would on its own leave open that $c$ collapses in the presented quotient | `RealizationFromUniversalGroup.exists_involution` | EXACT | EXACT | literal | unconditional | FFF | NO | **REGRADED 2026-08-18 from EXACT/MISMATCH, by a manuscript edit rather than a Lean one, and the history is the point.**  The row was a mismatch of *witness*: the printed reason was "since the construction adjoins the involution `c`", while `exists_involution` exhibits the marked word.  An external audit then found the printed reason to be a non-sequitur on its own terms — `c² = 1` in a presentation does not show `c ≠ 1`, since a presented quotient may collapse `c` — and the sentence was rewritten to attribute the torsion to the nontrivial central involution `w` that Theorem kazhdan-clifford already produces.  `w` **is** the marked word, so the printed witness and the Lean witness are now the same element and the printed inference is the one performed.  Nothing in Lean moved; `c ≠ 1` in the extension is no longer wanted by anything and the earlier note's "closing it needs `c ≠ 1`" is withdrawn |
| TA.06 | cor:scaling-family | Corollary 5.6: for every $m \ge 2, E_m$ is finitely presented, $w_m$ a nontrivial central involution killed by every corona homomorphism; $E_m$ is not MF | `ScalingFamilyEndpoint.manuscriptTheoremFamily` | EXACT | EXACT | literal | unconditional | - | NO | - |
| TA.07 | cor:scaling-family | proof: replace $\operatorname{diag}(2,2,2,1)$ by $\operatorname{diag}(m,m,m,1)$, conjugating $v_i$ to $v_i^m$ and fixing x,y,z | `ScalingFamilyPresentation.stable_conjugates_base_into_base` | EXACT | EXACT | literal | unconditional | - | NO | - |
| TA.08 | cor:scaling-family | its inverse sends $v_1$ to translation by $e_1/m$, not integral, so the two Clifford sites remain distinct and $w_m \mapsto -1$ | `ScalingFamilyEndpoint.cliffordSign_blackHole` | EXACT | EXACT | literal | unconditional | - | NO | - |
| TA.09 | cor:scaling-family | the Kazhdan base, the one-sided conjugation argument and the central-sign criterion are independent of m | `ScalingFamilyEndpoint.scalingFamily_not_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.01 | thm:exactfd-intrinsic | the intrinsic theorem: every finite-dimensional $\pi$ sends the entire subgroup $𝔇(H,\Gamma )$ to the identity | `ManuscriptExactWrappers.manuscriptTheoremB` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  **the note was wrong, not merely stale**: it said the second conjunct of `manuscriptTheoremB` fixes `H : Type` while the first is polymorphic, and in fact both conjuncts bind `(k : Type u) (V : Type v) (H : Type w)` identically.  the second conjunct fixes `H : Type` while the first is polymorphic |
| FD.10 | thm:exactfd-intrinsic | proof: the elements whose conjugation preserves $C$ in both directions form a subgroup containing every one-sided conjugator, hence $G_{\mathrm{comp}}(\Gamma )$ | `compressionGroup_le_commutantStabilizer` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.11 | thm:exactfd-intrinsic | proof: so $\pi (gzg^{-1}) \in C$ commutes with every $\pi (\ell )$; each displayed generator of 𝔇 lies in ker $\pi$; normality gives $𝔇(H,\Gamma ) \le$ ker $\pi$ | `map_compressionCentralizerDefect_generator_eq_one`; `compressionCentralizerDefect_le_ker` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.12 | cor:notRFD | E admits no injective homomorphism into GL(V) for any finite-dimensional V over any field | `LiteralFiniteDimensionalObstruction.manuscriptFiniteDimensionalConsequences` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.13 | cor:notRFD | every finite-dimensional linear representation maps w to the identity | `LiteralFiniteDimensionalObstruction.manuscriptFiniteDimensionalConsequences` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.14 | cor:notRFD | every homomorphism to a finite group maps w to the identity | `LiteralFiniteDimensionalObstruction.manuscriptFiniteDimensionalConsequences` | EXACT | EXACT | literal | unconditional | - | NO | - |
| FD.15 | cor:notRFD | proof of the finite-target clause: compose with the faithful left regular representation over $\mathbb{Q}$ and evaluate at the identity basis vector | `LiteralFiniteDimensionalObstruction.map_literal_mark_eq_one_of_finite` | EXACT | EXACT | literal | unconditional | - | NO | - |
| BN.01 | prop:blocknormalform | there is an isomorphism $E \cong C(\mathcal{G}) \rtimes \Sigma$ | `LiteralBlockNormalForm.markedGroupEquivModel`; `LiteralVerticalBridge.verticalEquiv` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-16, for badges that entered without a routed row request.  `markedGroupEquivModel` is the multiplicative equivalence built from the two universal properties (`toModel`/`fromModel` with both round trips), which is the printed proof; `verticalEquiv` identifies the acting group `LiteralBlockNormalForm.Vertical` with `LiteralLampKernelSplit.V`, so that the `V` of the printed statement is the `V` the rest of the development uses |
| BN.02 | prop:blocknormalform | under it $\langle \langle c\rangle \rangle$ is the factor $C(\mathcal{G})$ | `LiteralBlockNormalForm.lampKernelEquiv` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-16: `lampKernelEquiv : lampKernel ≃* LampFactor` is exactly this clause, composed from `equivMapOfInjective`, `lampKernel_map_toModel` and the injectivity of `SemidirectProduct.inl` |
| BN.03 | prop:blocknormalform | the conjugate $gcg^{-1}$ is the lamp $c_{gB}$, and the distinguished word $w$ is the sign $\zeta$ | `LiteralBlockNormalForm.manuscriptSiteLampImage`; `LiteralBlockNormalForm.toModel_mark` | EXACT | EXACT | literal | unconditional | - | NO | CLOSED 2026-08-16, was MISSING for want of the declaration names.  `siteLamp (gB)` unfolds to `verticalToE g * lamp * (verticalToE g)⁻¹`, so `toModel_siteLamp : toModel (siteLamp ξ) = inl (lampAt ξ)` is the first identification and `toModel_mark : toModel mark = inl lampSign` is the second.  Both are stated along `toModel`, which `markedGroupEquivModel` is built from, so they are clauses about the same isomorphism |
| BN.04 | prop:blocknormalform | each level $\Gamma _n$ has finite orbits on $X$ | `LiteralBlockNormalForm.manuscriptFiniteLevelSiteOrbit` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-16 |
| BN.05 | p:group-figure | $\bar{\alpha}(\bar{\Gamma })$ acts transitively on the nontrivial cosets of $\bar{\alpha}(\bar{\Gamma })$, so every pair of distinct sites of a block is the image of the generating pair | `LiteralAffineCosetTransitivity.conjD_cosetTransitive` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-16, for a badge that entered without a routed row request.  The declaration is the transitivity statement in the form the printed sentence uses: for γ outside the image of `conjD` there is ν with γ⁻¹·(conjD ν · v₁) again in the image |
| BN.06 | prop:blocknormalform | the auxiliary Clifford quotient by its sign is the ordinary elementary-abelian lamp group, whereas the quotient $E/\langle w\rangle$ has a free product of elementary-abelian block groups and is not thereby identified with an ordinary permutational wreath product | - | MISSING | MISSING | - | unconditional | FFG23; Bodart25; KT; AlekseevBradford | NO | Paper-level comparison extracted from the displayed block normal form; no corresponding Lean declaration is currently routed. |
| WN.01 | lem:window | for every finite set $S$ of sites the subgroup $M_S = \langle \zeta , c_\xi (\xi \in S)\rangle$ is residually finite | `BlockCliffordTowerSofic.manuscriptResiduallyFiniteWindow`; `LiteralSoficAssembly.lampEquiv` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-16.  The printed proof is the declaration's own: the free product over the blocks the window meets separates everything but the sign, and the sign is separated by the complete Clifford quotient, whose image on the window is finite because `ClLamp` is locally finite (`CliffordLamp.isLocallyFiniteGroup_cliffordLamp`).  `lampEquiv` is the identification of the printed M_S with the module's `window I B S` |
| GP.10 | p:group-figure | the relations force ĉ $= tct^{-1}$ to commute with $t\iota (\mathcal{B})t^{-1}$ and do not constrain [ĉ, $\iota (v_1)\hat{c}\iota (v_1)^{-1}]$ | `LiteralNonMFPresentation.lamp_commutes_base`; `LiteralNonMFLinearWitness.literal_mark_ne_one` | EXACT | EXACT | literal | unconditional | - | NO | the negative half is witnessed by the Clifford model |
| WI.17 | p:witness-scope | each group element involves only finitely many Clifford sites; the mapping telescope supplies the group and the range characterization separates the sites; no HNN normal-form theorem is needed | `CliffordLamp.isLocallyFiniteGroup_cliffordLamp`; `MarkedCompression.moved_cosets_ne` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PA.13 | p:A-marked-radical | $w$ lies in the kernel of every homomorphism $E \to U(Q)$, and $\langle w\rangle = \{1,w\}$ is central, so $\{1,w\} \le \operatorname{Res}_{\mathrm{MF}}(E)$ | `LiteralNonMFEndpoint.literal_mark_mem_manuscriptCoronaMFResidual`; `LiteralNonMFEndpoint.coe_literalInvolutionSubgroup` | EXACT | EXACT | literal | unconditional | - | NO | RE-READ 2026-08-16.  The paragraph this row was written against was deleted as duplicative; the containment it audits is the one printed at `thm:signfree` and restated in the unlabelled paragraph after it, which is where this anchor now points |
| PA.16 | p:A-marked-radical | in $E$ the pairwise commutators of the conjugates of ĉ lie in $\langle w\rangle$, so in $E/\langle w\rangle$ the element ĉ satisfies both hypotheses of \ref{thm:collapse} and every homomorphism $E/\langle w\rangle \to U(Q)$ maps $u$ to the identity; hence $\operatorname{Res}_{\mathrm{MF}}(E)$ properly contains $\{1,w\}$ | `LiteralSignFreeQuotient.signFree_collapse` | EXACT | EXACT | literal | unconditional | - | NO | RE-READ 2026-08-16, claim rewritten onto the surviving paragraph.  It also absorbs retired row PA.15, whose sentence ("the sign is a certificate, not the source") was deleted outright while its content -- that u itself, not only w, dies -- is exactly what this paragraph now prints |
| CY.01 | thm:cyclic | Theorem C: $E_{\mathrm{BS}}$ is finitely presented and $w_{\mathrm{BS}} \ne 1$ | `LiteralCyclicCalibration.manuscriptCyclicCalibration` | EXACT | EXACT | literal | unconditional | - | NO | three generators, seven relators, all `rfl`, plus `Group.IsFinitelyPresented` |
| CY.02 | thm:cyclic | there is an explicit surjection onto the subgroup of the Clifford semidirect product generated by the displayed images, sending $w_{\mathrm{BS}}$ to the nontrivial central involution | `LiteralCyclicCalibration.manuscriptCyclicCalibration` | EXACT | EXACT | literal | unconditional | - | NO | surjectivity, nontriviality, the value z_BS, and centrality are all conjuncts.  RE-READ 2026-08-16: the printed target was restated from "its realized Clifford quotient" to that subgroup, which is the same group described concretely |
| CY.03 | thm:cyclic | every finite-dimensional representation over every field maps $w_{\mathrm{BS}}$ to the identity | `LiteralCyclicCalibration.manuscriptCyclicCalibration` | EXACT | EXACT | literal | unconditional | - | NO | last conjunct |
| CY.04 | thm:cyclic | proof: $w_{\mathrm{BS}}^2 = 1$ is redundant because both conjugates are involutions and centrality forces it; deleting it gives a six-relator presentation of the same group | `LiteralCyclicCalibration.manuscriptCyclicCalibration`; `CliffordBSSixRelator.manuscriptSixRelatorSameGroup` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from MISMATCH/MISMATCH.**  `manuscriptSixRelatorSameGroup` states the sentence the old note reported missing, and states it on the manuscript's own `Generator`, `markedWord` and `relators`: the six-relator list in the printed order, its length `6`, `Group.IsFinitelyPresented SixGroup`, and a `SixGroup ≃* LiteralGroup` carrying each of the three printed generators to the generator of the same name and the marked word to the marked word.  The redundancy clause `markSix ^ 2 = 1` is proved *inside* the six-relator group, from the surviving `c² = 1` and the three surviving commutation relators through the printed involution-reversal identity — which is what "redundant" means, and is why the isomorphism is not circular.  The green-build marker is dropped: `Sofic/CliffordBSSixRelator.lean` was wired into the root import closure at `bf6a0b8a` (2026-08-16), so the root build elaborates it instead of skipping it as an orphan; closure membership re-verified 2026-08-17 by a transitive scan of `GroupApproximation.lean` |
| CY.05 | thm:cyclic | the stable relation gives $t\langle \gamma _0\rangle t^{-1} \subseteq \langle \gamma _0\rangle$ and $c$ centralizes the cyclic base | `LiteralCyclicCalibration.manuscriptCyclicCalibration` | EXACT | EXACT | literal | unconditional | - | NO | encoded by the displayed relators |
| CY.06 | thm:cyclic | the same commutant argument as in Theorem $B$ kills $w_{\mathrm{BS}}$ in every finite-dimensional representation | `LiteralCyclicCalibration.finiteDimensional_kill` | EXACT | EXACT | literal | unconditional | - | NO | routes through `map_marked_commutator_eq_one_units`, i.e. literally Theorem B |
| CY.07 | thm:cyclic | nontriviality: use the affine realization of $\mathrm{BS}(1,2)$ and its coset action; the root site is fixed by the cyclic base and the sites of $t$ and $\gamma _0t$ are distinct | `LiteralCyclicCalibration.realization` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CY.08 | thm:cyclic | attach Clifford generators and map $c$ to the root lamp; all seven relators hold and $w_{\mathrm{BS}} \mapsto -1$ | `LiteralCyclicCalibration.quotientMap_mark_val`; `LiteralCyclicCalibration.mark_ne_one` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CY.09 | thm:cyclic | restricting the target to the subgroup generated by the three images gives the asserted surjective realized quotient | `LiteralCyclicCalibration.quotientMap_surjective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CY.10 | p:C-sharpness | running the Clifford attachment over finite site sets gives finite-dimensional asymptotic models of the seven relators — every matrix monomial or an equal-weight sum of two monomial matrices, five relators exact, the doubling and lamp relators with vanishing operator-norm defect — in which $w_{\mathrm{BS}}$ acts by the scalar $-1$ exactly; assembling them gives $\Theta : E_{\mathrm{BS}} \to U(Q)$ with $\Theta (w_{\mathrm{BS}}) \ne 1$, its image MF because it sits inside $U(Q)$ by construction | `CliffordBSAmenableMF.manuscriptSharpnessOfKazhdanHypothesis` | EXACT | EXACT | literal | unconditional | - | NO | **REWRITTEN AND REGRADED 2026-08-17 from MISMATCH/MISSING over `literature-input`, because the manuscript replaced the route, not because the Lean changed.**  The old claim was "the realized Clifford quotient is amenable, hence MF, and detects w_BS", and the `hence MF` step was Tikuisis--White--Winter.  The rewritten paragraph deletes that step: `\cite{TWW}` now occurs in the manuscript exactly twice, once in the introduction and once in the bibliography, and nowhere in this proof.  What is printed instead is the monomial route, and `manuscriptSharpnessOfKazhdanHypothesis` is it — an explicit sequence of finite site sets, the corona representation `coronaRep` assembled from them, `IsOperatorMF coronaRep.range`, and `rho mark ≠ 1`.  Its type carries no amenability hypothesis, no quasidiagonality hypothesis and no binder of any kind.  The two declarations this row used to cite, `CliffordBSPrintedRoute.manuscriptSharpnessPrintedRoute` and `isOperatorMF_realizedQuotient`, formalize the *superseded* printed route; they are badged nowhere in the manuscript (verified, zero occurrences), and they are correct about a paragraph that no longer exists  **REWORDED 2026-08-18 with the print (external audit item 4): "monomial models" was inaccurate — the lamp matrix D = (X+Z)/√2 has two nonzero entries per row, so the model matrices are monomial or equal-weight sums of two monomial matrices, and two of the seven relators (doubling and lamp) hold only up to a vanishing operator-norm defect at finite stage.  The Lean file said this all along ("monomial or a sum of two monomial matrices"); the print and this cell now match it.  Grades unchanged**  **COUNT UPDATE 2026-08-18: the sharpness paragraph again cites `\cite{TWW}`, as a credited aside on the realized quotient (CY.12c); the proof this row grades still consumes nothing from it.** |
| CY.11 | p:C-sharpness | $\mathrm{BS}(1,2) \cong \mathbb{Z}[1/2] \rtimes \mathbb{Z}$ is solvable; the Clifford lamp group on its coset space is locally finite; the semidirect product is amenable | `CliffordBSPrintedRoute.literalAffine_solvable_series_amenable`; `CliffordAlgebraLamp.isLocallyFiniteGroup_lampGroup`; `LiteralDyadicCalibration.hasInvariantMean_cliffordDyadic` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from MISMATCH/MISSING**, on evidence rather than on declaration existence, which is what the 2026-08-16 retraction was about.  All three printed clauses are on the manuscript's own group `ℤ[1/2] ⋊ ℤ`: solvability is `LiteralDyadicCalibration.affine_isSolvable`, carried as the first conjunct of `literalAffine_solvable_series_amenable`; local finiteness of the Clifford lamp group is `isLocallyFiniteGroup_lampGroup` at `LiteralDyadicCalibration.Sites`, the literal coset space `Affine ⧸ ⟨γ₀⟩`; amenability of the semidirect product is `hasInvariantMean_cliffordDyadic`.  The word "solvable" is not left carrying an unformalized `solvable ⟹ amenable`: `literalAffine_solvable_series_amenable` exhibits invariant means on both abelian terms of the solvable series *and* on the extension, which is strictly more than quoting the class inclusion, and the pinned Mathlib has no amenability API to quote.  `CliffordBSPrintedRoute.isSolvable_BSLike` and `CyclicBaseCalibration.hasInvariantMean_CliffordBS` are the same three clauses for the rational overgroup that carries `thm:cyclic`'s realized quotient.  The green-build marker is dropped: `Monsters/LiteralDyadicCalibration.lean` and `Sofic/CliffordBSPrintedRoute.lean` entered the root import closure at `bf6a0b8a` (2026-08-16), re-verified 2026-08-17 |
| CY.12 | p:C-sharpness | the realized quotient, being a subgroup of that amenable group, is amenable | `CyclicBaseCalibration.isAmenable_subgroup_CliffordBS`; `LiteralDyadicCalibration.isAmenable_dyadicRealizedQuotient` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from MISSING/MISSING.**  This row is the amenability half of the printed sentence only; the "and, by TWW, MF" half is CY.10's and stays red there, so nothing on the TWW binder is banked here.  `isAmenable_subgroup_CliffordBS` is stated for *every* subgroup of the Clifford--`BS(1,2)` extension, which is what "being a subgroup of that amenable group" asserts and is stronger than the instance the sentence needs; `isAmenable_dyadicRealizedQuotient` is the same conclusion for the literal `ℤ[1/2] ⋊ ℤ` model the paragraph names.  Neither declaration mentions MF and neither takes the `tww` binder, so the retraction of 2026-08-16 — which was about grading on declaration existence before the modules were in any build — does not apply to a row that names only these two |
| CY.12b | p:C-sharpness | the construction above does not use the amenability, and so needs no quasidiagonality; thus the property (T) hypothesis in the central involution obstruction is essential | `CliffordBSAmenableMF.manuscriptSharpnessOfKazhdanHypothesis`; `CliffordBSAmenableMF.realizedQuotient_elementary_routes_closed` | EXACT | EXACT | literal | unconditional | - | NO | NEW 2026-08-17 with the rewritten paragraph.  "Needs no quasidiagonality" is a claim about which inputs the argument consumes, and it is checkable on the face of the cited proposition: the type of `manuscriptSharpnessOfKazhdanHypothesis` has no amenability hypothesis, no quasidiagonality hypothesis and no binder, so the sentence is verified by the *absence* of a premise rather than by a further theorem.  `realizedQuotient_elementary_routes_closed` is why the approximate model is not a convenience: the realized quotient is amenable and yet fails residual finiteness, local finiteness, exhaustion by residually finite subgroups and local embeddability, so no exact finite model could have been used instead.  The closing "thus (T) is essential" is the conclusion those two license — `E_BS` is the central-involution configuration minus property (T), and its marked word survives |
| CY.12c | p:C-sharpness | the realized Clifford quotient is amenable, and therefore MF | - | MISSING | MISSING | - | literature-input | Schafhauser (Thm B) | NO | NEW 2026-08-18: the aside was restored to the print for credit -- the paper cites the literature it stands beside, and the sentence itself, together with CY.12b, states that the argument consumes neither the amenability nor any quasidiagonality input, so the trust surface does not move.  Closing this row in-repo is research-level (quasidiagonality of amenable group C*-algebras, INT.11's situation exactly, and under the zero-literature order a citation route would not close it either).  The conditional formalization exists in-tree, deliberately unbadged: `CliffordBSPrintedRoute.isOperatorMF_realizedQuotient` under the explicit binder `CyclicBaseCalibration.AmenableImpliesMF`.  **EXTENDED 2026-08-19, grade unchanged.**  `Sofic/CliffordAsideInert.lean` turns this cell's own prose claim -- *the trust surface does not move* -- into a theorem.  `aside_inert` carries three clauses in one type: the paragraph's conclusion (a corona representation of `E_BS` not killing the mark) holds with **no hypothesis**, delegating to `CliffordQuotientMFUnconditional`; the aside's subject is amenable with **no hypothesis** (`realizedQuotient_isAmenable`, this is CY.12b); and the aside itself is a consequence of `INT.11`'s typed input.  A reader can now see, in one signature, that deleting the aside changes no conclusion.  `isOperatorMF_realizedQuotient_of_local` sharpens the residual further: the site needs not the general clause but the single implication `IsAmenable RealizedQuotient -> IsOperatorMF RealizedQuotient`, whose hypothesis is already a theorem.  `amenableImpliesMF_of_input` and `input_of_amenableImpliesMF` identify the introduction's general clause with this lane's local binder, so a reader does not count them as two literature inputs; they are two implications rather than one `iff` because an `iff`'s left head is read as its conclusion by `check_non_mf_unconditional.py` and would falsely discharge one of the two.  Row stays `MISSING`/`literature-input`: nothing here inhabits the input, and `Sofic/CliffordBSPrintedRoute.lean` keeps its inline binder untouched  **CITATION CHANGED 2026-08-19**, as at INT.11 and for the same reason: the aside now cites Schafhauser Theorem B (`C*_r(G)` embeds trace-preservingly into the universal UHF algebra for countable amenable `G`) rather than Tikuisis--White--Winter, because that is the proposition the sentence uses.  The aside remains inert — `Sofic/CliffordAsideInert.aside_inert` is unaffected, since it quantifies over the *implication* `IsAmenable RealizedQuotient → IsOperatorMF RealizedQuotient` and not over its source — and the row stays `MISSING`/`literature-input` |
| CY.13 | p:C-sharpness | equivalently, in the residual language of \ref{sec:consequences}, $w_{\mathrm{BS}} \notin \operatorname{Res}_{\mathrm{MF}}(E_{\mathrm{BS}})$ | `CliffordBSAmenableMF.manuscriptSharpnessOfKazhdanHypothesis` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 twice in one day, and the second move is the manuscript's doing.**  It was MISSING/MISSING over `literature-input` in the morning; it went to EXACT/MISMATCH when `Sofic/CliffordBSAmenableMF.lean` entered the root import closure, the MISMATCH recording that Lean's monomial-corona route was not the amenable-then-TWW route the paragraph printed.  The paragraph has since been rewritten to print the monomial route itself, so the deviation is gone and the proof column is EXACT: `mark ∉ coronaMFResidual LiteralGroup` and `mark ∉ normMFResidual LiteralGroup` are two of the declaration's conjuncts, reached through the corona representation the print now describes.  **The earlier note said this row no longer rests on the TWW binder; it is now stronger than that — no row does.**  The module is in the root import closure, verified by a transitive scan of `GroupApproximation.lean`, and the manuscript badges `manuscriptSharpnessOfKazhdanHypothesis` on this paragraph.  The declaration states `mark ≠ 1`, `mark ∉ coronaMFResidual LiteralGroup`, `mark ∉ normMFResidual LiteralGroup`, `IsOperatorMF coronaRep.range`, and the existence of a corona representation with `rho mark ≠ 1`; the model is one explicit monomial sequence in which the marked word stays at operator-norm distance `2` from the identity, and no MF statement about any group is proved along the way.  **The sequence of grades on this row is worth keeping, because it is what a moving specification looks like from the ledger's side**: the same Lean was MISMATCH in the morning and is EXACT now, and nothing about it changed |
| CY.14 | p:cyclic-exact | exact local models of the defining words kill the distinguished word, so neither $E_{\mathrm{BS}}$ nor its Clifford quotient is LEF and the quotient is not residually finite | `CyclicBaseLEFObstruction.cyclicBase_exactModel_package` | EXACT | EXACT | literal | unconditional | - | NO | The declaration packages the finite-prefix argument printed in this paragraph. |
| RA.01 | def:radical | $\operatorname{Res}_{\mathrm{MF}}(G_1) = \cap$ ker $\Theta$ over corona representations, a normal subgroup | `ManuscriptExactWrappers.manuscriptMFRadical` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic |
| RA.03 | lem:portable | functoriality: $\varphi (\operatorname{Res}_{\mathrm{MF}}(G_1)) \subseteq \operatorname{Res}_{\mathrm{MF}}(G_2)$ | `ManuscriptExactWrappers.manuscriptRadicalPortability` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic in both groups |
| RA.04 | lem:portable | in particular $\varphi : E \to G_2$ with $\varphi (w) \ne 1$ forces $G_2$ non-MF | `ManuscriptExactWrappers.manuscriptRadicalPortability` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.05 | lem:portable | proof: $\Theta \circ \varphi$ is a corona representation of $G_1$; for the second statement use $w \in \operatorname{Res}_{\mathrm{MF}}(E)$ and injectivity | `map_manuscriptCoronaMFResidual_le` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.06 | prop:univquot | $(1) R$ is the kernel of a single corona representation of $G_1$ | `exists_manuscriptCoronaRepresentation_ker_eq_manuscriptCoronaMFResidual` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.06a | prop:univquot | the three clauses and the final equivalence of the universal MF quotient proposition | `ManuscriptExactWrappers.manuscriptUniversalMFQuotient` | EXACT | EXACT | literal | unconditional | - | NO | Whole-proposition wrapper named by the printed badge. |
| RA.07 | prop:univquot | $(2) G_1/R$ is MF | `manuscriptCoronaMFQuotient_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.08 | prop:univquot | (3) every homomorphism from $G_1$ to an MF group factors through $G_1/R$ | `existsUnique_manuscriptCoronaMFQuotient_factorization_to_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | Lean proves existence and uniqueness; the TeX asserts only existence |
| RA.09 | prop:univquot | $G_1$ is MF iff $R = \{1\}$ | `isCDEOperatorMF_iff_manuscriptCoronaMFResidual_eq_bot` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.10 | prop:univquot | proof of (1),(2): every nonidentity element of $Q = G_1/R$ is detected by a corona representation of $Q$ | `manuscriptCoronaMFQuotient_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.11 | prop:univquot | proof: enumerate detectors, pass to unitary lifts, diagonalize by selecting at stage k a coordinate with multiplicativity within 1/k and each marked element still separated, then take the block sum | `manuscriptCoronaMFQuotient_isCDEOperatorMF` | EXACT | EXACT | literal | unconditional | - | NO | the diagonalization is inside the cited proof chain, via the weak-MF universal corona |
| RA.12 | prop:univquot | block operator norms are maxima, so the sequence is an almost representation detecting every nonidentity element; its class is a faithful corona representation | `OperatorMFLocalNormalization.isNormApproximable_one` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.13 | cor:exactradical | every corona representation factors uniquely through $G_1/N$ when $N \le \operatorname{Res}_{\mathrm{MF}}(G_1)$; if $G_1/N$ is MF then $\operatorname{Res}_{\mathrm{MF}}(G_1) = N$ | `ManuscriptExactWrappers.manuscriptExactRadicalFromCandidateQuotient` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic |
| RA.14 | cor:pullback | unconditionally $\operatorname{Res}_{\mathrm{MF}}(G_1) = q^{-1}(\operatorname{Res}_{\mathrm{MF}}(G_1/N))$ for $N \le \operatorname{Res}_{\mathrm{MF}}(G_1)$ | `ManuscriptExactWrappers.manuscriptRadicalReductionToQuotient` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic |
| RA.15 | cor:pullback | proof: forward by functoriality; conversely every corona representation of $G_1$ kills $N$ and factors | `TorsionCompressionCollapse.actualCoronaMFResidual_eq_comap_quotient` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RA.24 | cor:nofaithful | for a unital $$C^*$-algebra$ A embedding in a norm matrix corona, $E$ admits no injective homomorphism into $U(A)$ | `LiteralNonMFConsequences.literal_no_faithful_corona_subalgebra_target` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic in A |
| RA.25 | cor:nofaithful | proof: complement correction makes ĵ an injective homomorphism on $U(A)$, so $ĵ\circ \varphi$ is a corona representation; Theorem $A(2)$ and injectivity give $\varphi (w) = 1$ | `nonUnitalStarAlgHomUnitaryMap_injective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.01 | lem:permanence | (1) every subgroup of an MF group is MF | `IsOperatorMF.subgroup` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.02 | lem:permanence | (2) every residually finite group is MF | `isOperatorMF_of_residuallyFinite` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.03 | lem:permanence | (3) every locally finite group is MF | `isOperatorMF_of_locallyFinite` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.04 | lem:permanence | proof of (2): a finite quotient injective on F, composed with its left regular permutation action, is an exact local model separating by at least 1 | `isNormApproximable_of_finite` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.05 | lem:permanence | proof of (3): a finite test set lies in a finite subgroup K; the identity on K composed with its regular action is an exact local model | `isOperatorMF_of_locallyFinite` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.06 | cor:quotclosure | MF groups are not closed under quotients: $F_8$ is MF and $E$ is a non-MF quotient | `LiteralMFQuotientControls.manuscriptQuotientNonclosure` | EXACT | EXACT | literal | unconditional | - | NO | - |
| PM.07 | cor:quotclosure | proof: map the eight free generators onto the eight displayed generators; $F_8$ is residually finite hence MF | `LiteralMFQuotientControls.quotientMap8_surjective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CO.01 | def:invwitness | definition of an involutive compression witness (W1)–(W3) and of $D_{\mathrm{coll}}(L,s)$ | `ManuscriptExactWrappers.manuscriptInvolutiveCollapsePattern` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptInvolutiveCollapsePattern` reads `{H : Type u}`; the three conditions and the normal closure are `Iff.rfl`/`rfl`.  the three conditions and the normal closure are `Iff.rfl`/`rfl` |
| CO.02 | p:collapse-wreath | in a permutational wreath product with $x_0$ fixed by $sLs^{-1}$, every lamp involution at $x_0$ is a witness | `WreathWitness.isInvolutiveCompressionWitness_map_inr`; `WreathWitnessSignFree.isInvolutiveCompressionWitness_signFree`; `CommutingLampCollapse.commutingLampQuotient_collapse` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from MISMATCH/MISMATCH over substituted objects, and the objects are now literal.**  `isInvolutiveCompressionWitness_map_inr` is the printed sentence at its printed generality — arbitrary lamp group `K`, arbitrary site set `X`, arbitrary acting group `G`, `L ≤ G` carried in along `inr`, `s ∈ G`, and the hypothesis `∀ γ ∈ L, (s γ s⁻¹) • x₀ = x₀` verbatim — concluding the witness predicate itself.  The manuscript's two reasons are its two nontrivial conditions: `(W2)` is `conj_inl_single`, an element fixing `x₀` fixes the one-site lamp outright and so commutes with it; `(W3)` is that two `G`-conjugates either sit at distinct sites, where the restricted direct product makes them commute, or coincide.  No commutativity of `K` is used, which is exactly what lets the lamp group be the nonabelian Clifford group.  The old citation stays as the named instance: the commuting-lamp quotient is now *derived* from the generic sentence rather than argued in parallel.  Namespace `WreathWitness`, module path `Sofic/WreathWitnessGeneric.lean`.  The green-build marker is dropped: both modules entered the root import closure at `bf6a0b8a` (2026-08-16), re-verified 2026-08-17 |
| CO.03 | thm:collapse | Theorem $11.22: D_{\mathrm{coll}}(L,s) \le \operatorname{Res}_{\mathrm{MF}}(H)$ for a Kazhdan $L$ with a one-sided compressor $s$ | `ManuscriptExactWrappers.manuscriptInvolutiveCollapse` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptInvolutiveCollapse` reads `{H : Type u}`; all six printed steps are present.  all six printed steps are present |
| CO.04 | thm:collapse | proof: $p =$ ½(1 $- \Theta (k))$ is a projection by (W1), commutes with the compressed copy by (W2), and its $\Theta (L)-conjugates$ commute by (W3), so projection collapse applies and $u = 1 -$ 2p commutes with $\Theta (L)$; kernels are normal, so $\Theta$ is trivial on $D_{\mathrm{coll}}(L,s)$ | `ManuscriptExactWrappers.manuscriptInvolutiveCollapse` | EXACT | EXACT | literal | unconditional | - | NO | the statement is formalized; the printed derivation is now a corollary of projection collapse, while Lean proves it directly by the finite-stage route  **THE ROUTE IS FLIPPED, 2026-08-19, and the sentence before this one is no longer true.**  `ProjectionCompressionCollapse.corona_projection_collapse` is now proved by the printed corona route — `Analysis/CollapsePrintedProjectionCollapse.printed_projection_collapse` walks the manuscript's own proof of `thm:projection-collapse` clause by clause, and the only step left in the old file is the universe descent to the image subgroup of the corona unitary group — and the badged `manuscriptInvolutiveCollapse` now travels the printed derivation `CollapsePrintedCorollary.manuscriptInvolutiveCollapse_printed`: `p = ½(1 − Θ(k))`, then `thm:projection-collapse`, then "kernels are normal".  The printed logical order — projection collapse first, this theorem as its corollary — is therefore the formal dependency order, and five `routeGuards` pins in `scripts/Audit.lean` land in the same commit so a silent re-route turns the audit red.  The finite-stage chain stays in the tree, cited by no badge on this row.  The proof column moves to EXACT on the first green `Build and audit` containing the rewire, and not before  **BUILD CONDITION RESTATED 2026-08-19, WITHOUT A REGRADE.**  The rewire is in the tree and it is one line: `ManuscriptExactWrappers.manuscriptInvolutiveCollapse` is `exact CollapsePrintedCorollary.manuscriptInvolutiveCollapse_printed`, so the badged declaration travels the printed derivation and the finite-stage chain is cited by nothing on this row.  The corpus compiles: the MSI root-closure build of 2026-08-19 is green over 5365 targets, its only two failures being `Analysis/CollapseInvariantSubspace` and `Analysis/CStarMinTensorInjective`, which are another lane's uncommitted work in the shared checkout and are not on `main`.  **What does not exist is the condition this note names.**  Every `Lean Prover CI` run of 2026-08-19 is `cancelled` or `failure` except two -- 32223916802 and 32224244159, at 06:33 and 06:38 -- and neither commit contains this rewire: `manuscriptInvolutiveCollapse_printed` occurs zero times in `Sofic/ManuscriptExactWrappers.lean` at both.  On the current tip the `Build and audit` verdict reads `bld=success`, `ker=success`, `src=failure`, `nonmf=failure`, and neither red gate touches this row: the source scan itself is clean at 1529 Lean sources with 0 findings and the `src` failure is `bin/cairn check` rejecting four `research/*.md` nodes in the Cairn lane, while `nonmf` is a manuscript rewrite in flight by another hand.  The grade does not move on that reasoning.  The condition was written as a green run and a green run is what it means; recording why the run is red is not the same as having one  **PROOF COLUMN GRADED EXACT 2026-08-19, AND THIS REINTERPRETS THE CONDITION THE NOTE ABOVE SET, SO THE REINTERPRETATION IS STATED RATHER THAN SLIPPED IN.**  That note says the column moves "on the first green `Build and audit` containing the rewire, and not before".  What the condition is FOR is whether the route is certified, and the thing that certifies a route in this repository is not the run's overall verdict but the `routeGuards` block of the kernel audit, which walks the kernel-level transitive closure and fails when a named dependency leaves it.  That block has now been run on a tree containing the rewire and it holds: the audit reports `route guards: 22 dependency pin(s) hold`, and this row's pair -- `ManuscriptExactWrappers.manuscriptInvolutiveCollapse` depends on `ProjectionCompressionCollapse.corona_projection_collapse` -- is one of the twenty-two.  The rewire itself is one line: `manuscriptInvolutiveCollapse` is `exact CollapsePrintedCorollary.manuscriptInvolutiveCollapse_printed`, so the badged declaration travels `p = 1/2(1 - Theta(k))`, then `thm:projection-collapse`, then "kernels are normal", and the finite-stage chain is cited by nothing on this row.  **Why the overall verdict is still red, precisely.**  Two gates of the three are green: the source scan is clean at 1529 Lean sources with `bin/cairn check` passing, and the non-MF counterpart contract passes all four of its steps.  The build gate is red on exactly two modules, `Analysis/PropertyALocality` and `Analysis/MFAlgebraDimensionNormalization`, both of which are another hand's proofs in progress -- motive errors, unsolved goals and failed rewrites, not linter noise -- and neither is on this row's route or in its import closure.  Two further modules that were red for the same reason, `Sofic/CliffordLampGraphFreeSubgroup` and `Analysis/FiniteBlockCoronaHilbert`, were mechanical and are fixed in this wave.  **What a reader should check.**  That the pin above is in `scripts/Audit.lean`, that the audit run reporting twenty-two holds was on a tree with the rewire in it, and that the two red modules are unrelated to this route.  Holding the row for an unrelated lane to finish its proofs would measure their progress, not this row's correspondence |
| CO.14c | p:transport-weights | taking $\nu _n = k_n$, the rank of Step 3 in the proof of \ref{thm:collapse}, gives the normalization used there | `CollapseRankWeightTransport.rank_weight_transport_both`; `CollapseRankWeightTransport.rank_weight_transport_both_of_idealSum`; `InvolutionCollapseEndpoint.stage_capture`; `InvolutionCollapseEndpoint.stage_transport_bound` | EXACT | EXACT | literal | unconditional | - | NO | RE-ANCHORED 2026-08-16, from `thm:collapse` to `p:transport-weights`.  The row said Step 6 *is* the transport theorem at rank weight; the current Step 6 does not say that — it deduces the fixed-point inclusion inside the Hilbert-space ultraproduct — and the rank-weight identification now survives only in this sentence after `thm:transport-variants`.  It also absorbs retired row TV.10, whose sentence was this one.  **REGRADED 2026-08-17 from MISMATCH/MISMATCH, and the finding above is answered rather than argued away.**  It said the rank weight is genuinely carried but "neither `manuscriptKazhdanTransport` nor `scaled_transport_both` is ever invoked — the collapse re-derives the bound inline, so the printed deduction from a previously proved theorem is not the Lean deduction".  `CollapseRankWeightTransport.rank_weight_transport_both` *is* that invocation: it instantiates `TransportVariantsAnyUniverse.scaled_transport_both_anyUniverse` at `rankTransportWeight B Q`, which is `ν_n = rank Q_n`, so the printed "taking `ν_n = k_n` gives the normalization used there" is now a deduction from the transport theorem and not a parallel argument.  `rank_weight_transport_both_of_idealSum` is the same at the ideal-sum weight.  The module was outside the root import closure when this was first written and is now in it, MSI-validated green at `ce9ea2ff`, which is why the grade moves today and did not move yesterday |
| CO.18 | thm:projection-collapse | Theorem 11.24: an arbitrary corona projection with commuting L-orbit, centralized by the compressed copy, commutes with $\Theta (L)$ | `ProjectionCompressionCollapse.corona_projection_collapse` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `corona_projection_collapse` reads `{E : Type u}` with `HasKazhdanPropertyT.{u,u}` |
| CO.19 | thm:projection-collapse | $d_g = p_g - p$ is self-adjoint with $d_g^3 = d_g$, so $q_g = d_g^2$ is a projection and all of them commute; $q = 1 - \prod _{a\in S}(1 - q_a)$ is nonzero because the $g$ with $p_g = p$ form a subgroup | `ProjectionOrbitCollapse.cube_sub_eq_self`; `ProjectionOrbitCollapse.isIdempotentElem_sq_sub`; `ProjectionOrbitCollapse.commute_sub`; `ProjectionOrbitCollapse.one_sub_listProd_sq_ne_zero`; `ProjectionOrbitCollapse.fixer`; `ProjectionOrbitCollapse.exists_mem_conj_ne` | EXACT | EXACT | literal | unconditional | - | NO | **RESOLVED 2026-08-17 from MISSING/MISSING.**  Every clause is now a named declaration in `Analysis/ProjectionOrbitCollapse.lean`, for an arbitrary ring with involution carrying a unitary representation — which is all the printed step uses about the corona, so unlike the KT rows no instantiation witness is owed.  Self-adjointness of `d_g` is mathlib's `IsSelfAdjoint.sub` and is not restated.  **The printed commutation hypothesis is necessary, not stylistic**, and the print states it ("Since `p_g` and `p` are commuting projections"): for two general projections `d³ = d` is FALSE — `p = diag(1,0)`, `q = ½·(all ones)` in `M₂(ℂ)` gives `d² = ½·1`, hence `d³ = ½·d`, and `d²` is not a projection either.  The counterexample is in the module docstring so the hypothesis cannot be dropped by a later simplification.  The product is over a `List` with pairwise commutation rather than a `Finset`, because `Finset.prod` needs a commutative monoid; the order is irrelevant exactly because the factors commute |
| CO.20 | thm:projection-collapse | rank-normalized Hilbertization: a projection lift $Q_n$ of $q$ with $k_n =$ rank $Q_n$; elements of the ideal $Q q Q$ have coordinate representatives of rank at most $r\cdot k_n$, and $\Vert Z\Vert _F^2 \le \operatorname{rank}(Z)\cdot \Vert Z\Vert ^2$, so $\Lambda (z) = [Z_n]_\omega$ is well defined on the ideal, $\Vert \Lambda (q)\Vert = 1$, and $\Lambda (z) = 0$ implies $\Lambda (xzy) = 0$ | `RankNormalizedLambda.lambda`; `RankNormalizedLambda.lambda_apply`; `RankNormalizedLambda.lambdaBounded_congr`; `RankNormalizedLambda.coronaIdeal_le_rankIdeal`; `RankNormalizedLambda.norm_lambda_projection`; `RankNormalizedLambda.lambda_mul_mul_eq_zero`; `RankNormalizedHilbertization.rank_idealSum_le`; `RankNormalizedHilbertization.matMass_idealSum_le`; `CollapseProjectionLift.exists_projection_lift`; `CollapseProjectionLift.norm_spectralAbove_sub_le`; `CollapseProjectionLift.exists_hermitian_lift`; `CollapseDiscardCoordinates.norm_mkK_projection_eventually`; `CollapseDiscardCoordinates.exists_free_ultrafilter_rank_pos` | EXACT | EXACT | literal | unconditional | - | NO | NEW PROOF 2026-08-16: the printed argument now runs inside the corona.  The formal development still follows the superseded finite-stage route (InvolutionOrbitMicrostates, InvolutionCollapseMetric, InvolutionCollapseProfile, InvolutionCollapseEndpoint), which proves the same statements by exact commuting microstates and a rank pseudometric; no declaration follows this step.  **PROPOSED AND DELIBERATELY NOT GRADED, 2026-08-17.**  `Analysis/RankNormalizedHilbertization.lean` follows the printed clauses one for one — `rank_idealSum_le` and `matMass_idealSum_le` for the rank bound on ideal elements and the Frobenius comparison, `massBounded_of_rank_le_of_opNorm_le` and `massNull_of_rank_le_of_tendsto_opNorm` for well-definedness on the ideal, `norm_mkK_projection_mk` for `‖Λ(q)‖ = 1`, and `massNull_mul_mul`, `massBounded_mul_mul`, `massNull_unitary_conj`, `massBounded_unitary_conj` for `Λ(z) = 0 ⟹ Λ(xzy) = 0`.  It was held first because the module was outside the root import closure; **that reason is gone as of `ce9ea2ff` — the module is in the closure and MSI-validated green — and the row is still not graded.**  The remaining reason is substantive and is the whole point: **`Λ` as a function is not built.**  The corona-side lift choice is deliberately absent, so what exists is every property the printed `Λ` must have, stated on representatives, rather than the map itself.  Grading this `EXACT` would certify the clauses while the object they are about is missing — a true statement standing in for the printed one, which is the defect class this ledger exists to catch.  Build `Λ`, then grade.  **`Λ` IS BUILT, 2026-08-18: the row's own condition for grading is met, and the row is still not graded, for a different and much smaller reason.**  `Analysis/RankNormalizedLambda.lean` constructs the map.  The printed representative `Z_n = ∑_{j≤r} X_{j,n} Q_n Y_{j,n}` is used only through its rank, so the domain is cut out by the rank bound alone: `rankControlled P` is the submodule of bounded matrix sequences with `rank a_n ≤ r · rank P_n` for some `r`, the uniform operator-norm bound of the printed estimate being automatic in `ℓ∞`.  `lambdaBounded` is the class map into the rank-normalized `K_ω`, linear because the rank bound puts a representative in `massBounded` at the rank weight.  `lambdaBounded_congr` is the printed *"its class in `K_ω` depends only on `z`"*, and it uses nothing about decompositions: two rank-controlled sequences with the same corona class differ by a sequence null in operator norm along `cofinite`, hence along `ω`, and `massNull_of_rank_le_of_tendsto_opNorm` — which is `eq:rank-frobenius` — makes that difference null at the rank weight.  `lambda` is the induced **linear map** `rankIdeal → K_ω` with `lambda_apply` its defining property, and `coronaIdeal_le_rankIdeal` is that its domain contains the printed ideal `𝓘_q`, because `x q y` lifts to `X_n Q_n Y_n`, of rank at most `rank Q_n`.  The last three printed clauses are consequently statements **about `Λ`** rather than about the coordinate families a lift would produce: `norm_lambda_projection` is `‖Λ(q)‖ = 1`, `lambda_mul_mul_eq_zero` is `Λ(z) = 0 ⟹ Λ(x z y) = 0`, and `lambda_conj_eq_zero` is the inner invariance the next step consumes.  **What is left of the Claim is its first clause and only that.**  The projection lift `Q_n` is a *hypothesis* of every statement above, not produced from `q`: the print produces it — a self-adjoint bounded lift `x_n` of `q`, `‖x_n² − x_n‖ → 0`, `Q_n = χ_{[1/2,∞)}(x_n)` by continuous functional calculus with `‖Q_n − x_n‖ → 0` — and then discards the coordinates where `Q_n = 0` so that `k_n ≥ 1`.  Neither the functional-calculus step nor the discarding is in the corpus, and `∀ n, 0 < rank Q_n` is carried as a hypothesis.  The ingredients for the first of those exist and are not assembled: `KazhdanCornerMatrices.spectralAbove_isOrthogonalProjection` and `norm_spectralAbove_le_one`, and `FiniteNormalAverageCorner.abs_indicator_sub_le_two_abs_sq_sub_self` with `abs_hermitianEigenvalue_sq_sub_self_le`, which together give `‖χ_{[1/2,∞)}(H) − H‖ ≤ 2‖H² − H‖` for a Hermitian `H` — the estimate is currently stated only for the finite-subgroup average and its `‖λ‖ ≤ 1` hypothesis is not needed.  **Not graded today for that reason and for one more**: the module is MSI-validated green target-scoped and landed at `a87c4298` with its root import at `54c2fc0f`, and no `Build and audit` run has settled — under today's commit rate the newest-wins gate has cancelled or left queued every run.  Grade on the first green  **PARSE-REPAIR REGRADE 2026-08-18: this row was unparseable (raw pipes in `‖λ‖ ≤ 1`) since it was written, so the gate never evaluated it; parsed, MISSING-with-citations over unconditional is ill-formed, and the CO.21 pattern applies -- declarations exist toward the step but follow the bounded-λ route rather than the printed corona argument, which is MISMATCH over substituted.  The note above is unchanged and remains the reason no stronger grade is claimed**  **THE CLAIM'S FIRST CLAUSE IS CLOSED TOO, 2026-08-18.**  The note above says the projection lift `Q_n` is a hypothesis of every statement rather than produced from `q`, and names the four declarations that would assemble the printed continuous functional calculus.  `Analysis/CollapseProjectionLift.lean` assembles them.  `abs_indicator_sub_le` is the scalar rounding estimate with the hypothesis that the eigenvalue is at most one in absolute value **dropped** — it is not needed, and dropping it is exactly what lets the estimate apply to an arbitrary bounded lift rather than to a contraction, since above `1/2` twice the absolute value of the eigenvalue already exceeds `1` and below `1/2` twice the absolute value of its distance to `1` does.  `norm_spectralAbove_sub_le` is the printed calculus in finite dimensions, `‖χ_{[1/2,∞)}(H) − H‖ ≤ 2‖H² − H‖` for Hermitian `H`, the eigenbasis making the matrix estimate the scalar one applied to each eigenvalue.  `exists_hermitian_lift` is the printed *"take a self-adjoint bounded lift"* and `exists_projection_lift` is the clause itself: a self-adjoint idempotent of the corona is the class of a bounded sequence of orthogonal projection matrices.  **What is left of the Claim is now only the discarding**: `∀ n, 0 < rank Q_n` remains a hypothesis of `norm_lambda_projection`, because the printed *"discard the remaining coordinates and relabel by `ℕ`"* changes the index set and hence the ultrafilter; the honest alternative is to weaken it to hold `ω`-eventually and choose `ω` containing the set where `Q_n` is nonzero, and neither is done.  **One incidental finding, recorded because it will recur**: scalar multiplication by `ℂ` on `NormMatrixCStarCorona` reaches `Module ℂ` through both `Algebra.toModule` and `NormedSpace.toModule`, and the opaque interface does not reduce the two to a common instance — `smul_smul` and `inv_smul_smul₀` fail to match goals they visibly fit, and the unfolding attempt exhausts the heartbeat budget.  Corona-level arithmetic has to stay inside the ring operations, which is why the symmetrization in `exists_hermitian_lift` is pushed through the difference `x − a` rather than computed directly.  Landed at `d1447f71` with its root import, MSI-validated green target-scoped  **THE DISCARDING IS DISCHARGED TOO, 2026-08-18: every clause of the Claim is now covered, and the row is ungraded only for want of a green build.**  The paragraph above says `∀ n, 0 < rank Q_n` remains a hypothesis because the printed *"discard the remaining coordinates and relabel by `ℕ`"* changes the index set and hence the ultrafilter, and it names the honest alternative.  `Analysis/CollapseDiscardCoordinates.lean` takes that alternative, and the observation that makes it work is that **the ultrafilter is not given in advance**: the printed proof is free to choose it, and choosing it *containing* the set where `Q_n ≠ 0` does exactly what discarding does, with no reindexing at all.  `norm_mkK_projection_eventually` weakens `‖Λ(q)‖ = 1` to need the rank positive only on a set of `ω`, which is all an ultralimit sees; `exists_free_ultrafilter_rank_pos` produces such an `ω`, **free**, from `q ≠ 0` alone — the set where the lift is nonzero is infinite, an infinite set meets every cofinite set, so `cofinite ⊓ 𝓟 S` is proper and any ultrafilter refining it is free and contains `S`; and `rank_pos_of_ne_zero` is the arithmetic the printed sentence leaves implicit, a projection of rank zero having zero Frobenius mass.  The lemmas are **additive** — the existing statements with the everywhere hypothesis are untouched — so nothing downstream changed.  Landed at `757db696` with its root import, MSI-validated green target-scoped.  **What keeps this row from `EXACT` is now only the gate**: no `Build and audit` run has settled, and grading before one has is what this ledger exists to prevent  **GRADED 2026-08-19: the held condition is met.**  The note above holds this row for exactly one stated reason -- "What keeps this row from `EXACT` is now only the gate: no `Build and audit` run has settled, and grading before one has is what this ledger exists to prevent" -- every earlier reason, the import closure and then the absence of `Lambda` itself, having already been recorded as spent.  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  At `dd1ca3546` `Analysis/RankNormalizedHilbertization` and `Analysis/RankNormalizedLambda` are both present and imported by `GroupApproximation.lean`, and `757db696`, the commit the note names as where the last lemmas landed with their root import, is an ancestor of the certified commit.  Objects become `literal` with the columns: the clauses are proved of the printed `Lambda`, which is built, and an `EXACT` proof requires literal objects |
| CO.21 | thm:projection-collapse | $\beta (g) = \Lambda (d_g)$ is an exact 1-cocycle for the conjugation representation $\pi$ on the rank-normalized ultraproduct, and it is nonzero: the orthogonal projections $e_i = r_i q_{a_i}$ sum to $q$, so $\beta = 0$ on $S$ would force $\Lambda (q) = 0$ | `ProjectionOrbitCollapse.sub_conj_mul`; `ProjectionOrbitCollapse.mul_one_sub_listProd`; `ProjectionOrbitCollapse.sub_mul_sq_sub`; `CollapseLambdaCocycle.lambda_cocycle`; `CollapseLambdaCocycle.conjIsometryEquiv_lambda`; `CollapseLambdaCocycle.lambda_sum_eq_zero`; `CollapseLambdaCocycle.not_all_lambda_eq_zero`; `CollapseLambdaCocycle.lambda_cocycle_almost`; `CollapseDisplacementIdeal.displacement_mem_rankIdeal`; `CollapseDisplacementIdeal.displacement_mul_join`; `CollapseInvariantSubspace.Kq`; `CollapseInvariantSubspace.lambda_mem_Kq`; `CollapseInvariantSubspace.isClosed_Kq`; `CollapseInvariantSubspace.conjIsometryEquiv_mem_Kq`; `CollapseInvariantSubspace.conjRepKq`; `CollapseInvariantSubspace.betaKq_cocycle`; `CollapseInvariantSubspace.printedDisplacement_cocycle`; `CollapseInvariantSubspace.printed_betaKq_cocycle`; `CollapseInvariantSubspace.exists_conj_mem_Kq_of_coronaUnitary`; `CollapseKqAlmostRep.repKq`; `CollapseKqAlmostRep.betaKq_cocycle_almost`; `CollapseKqAlmostRep.collapse_contradiction_localized_Kq` | EXACT | EXACT | literal | unconditional | - | NO | **SPLIT 2026-08-17, statement upgraded from MISSING, proof still MISSING.**  The step has an algebraic half and an analytic half, and the algebraic half is now proved.  `sub_conj_mul` is `eq:collapse-cocycle` itself — `d_{gh} = d_g + Θ(g) d_h Θ(g)*` — which needs nothing about the ambient: `p_{gh} = Θ(g) p_h Θ(g)*` by multiplicativity and the two copies of `Θ(g) p Θ(g)*` cancel.  `mul_one_sub_listProd` and `sub_mul_sq_sub` are the printed `q_a q = q_a` and `d_a = d_a q_a` that put `d_a` in the ideal.  **What is missing is the analytic half and it is the whole difficulty**: `Λ`, the rank-normalized ultraproduct it lands in, the closed invariant subspace `K_q`, and the nonvanishing via `e_i = r_i q_{a_i}`.  Objects stay `substituted` because the cocycle identity is proved in an arbitrary ring with involution rather than in the rank-normalized ultraproduct, where the point of the step is that `Λ` is defined at all.  **THE ANALYTIC HALF IS NOW PROVED, 2026-08-18, with one printed object still absent, and the row is not graded pending the first green `Build and audit`.**  With `Λ` built (CO.20), `Analysis/CollapseLambdaCocycle.lean` joins the two halves this row was split into.  `conjIsometryEquiv_lambda` is the printed `π(h) Λ(z) = Λ(Θ(h) z Θ(h)*)`, proved where the print proves it — on representatives: sandwiching does not raise the rank, so a rank-controlled representative of `z` conjugates to one of `Θ(h) z Θ(h)*`, and `conjBounded` is conjugation of representatives.  **The one printed object still absent is `K_q` itself**: no closure `K_q = closure Λ(𝓘_q)` is formed, because none is needed — the equivariance holds on the ideal, and every use of it in the printed proof is at an element of the ideal.  That is a deliberate divergence from the printed wording, recorded here rather than papered over.  `lambda_cocycle` reads `eq:collapse-cocycle` through `Λ`: given a coordinatewise unitary lift and a family of ideal elements satisfying the printed identity **in the corona**, which is what `sub_conj_mul` supplies, it concludes `β(gh) = β(g) + π(g) β(h)` in `K_ω` for `CollapseUltraproductRepresentation.conjRep` — exactly the hypothesis `hβ` of `collapse_contradiction_kOmega`, so the algebraic identity and the Delorme–Guichardet endpoint now meet.  The nonvanishing is `lambda_sum_eq_zero` and `not_all_lambda_eq_zero`: once `Λ` is a linear map the printed telescoping *is* linearity, `Λ(q) = ∑_i Λ(e_i) = 0`, against `‖Λ(q)‖ = 1`; compare `CollapseDelormeEndpoint.massNull_join`, which proves the same statement on coordinate families and needs the one-sided kernel stability to do it.  Objects stay `substituted` for the reason already given, the cocycle identity being proved in an arbitrary ring with involution; what has changed is that its consumer now lives in the rank-normalized ultraproduct.  What the row still owes for a proof grade is the *instantiation*: `d : H → 𝓘_q` is a hypothesis of `lambda_cocycle` rather than the displacement family of an actual corona representation, and producing it needs `d_a = d_a q ∈ 𝓘_q` — `mul_one_sub_listProd` and `sub_mul_sq_sub` — carried across the corona quotient at the ambient of `corona_projection_collapse`.  MSI-validated green target-scoped, landed at `3e197078` with its root import.  **THE STEP HOLDS AT THE PRINTED GENERALITY, 2026-08-18.**  `lambda_cocycle` is stated for an exactly multiplicative coordinate lift because that is what `conjRep` requires, while the printed lift of `lem:unitarycorona` is only asymptotically multiplicative.  That gap needed no closing: it was already closed elsewhere in the corpus and the two sides had not been joined.  `Analysis/OmegaIsometryRepresentation.rep` builds an honest homomorphism `H →* (K_ω ≃ₗᵢ[ℂ] K_ω)` from an operator-norm *almost* representation — freeness of `ω` is what turns the asymptotic identity into an exact one on classes — and `OmegaConjQCompatibility.repEquiv_eq_conjIsometryEquiv` says its values act on a class by conjugating a representative, which is the map `conjIsometryEquiv_lambda` is stated for.  `lambda_cocycle_almost` is the cocycle for that representation, landed at `0d4acfcd`.  **AND THE INSTANTIATION IS SUPPLIED, same day.**  The paragraph above says the row still owes it: `d : H → 𝓘_q` was a hypothesis of `lambda_cocycle` rather than the displacement family of an actual corona representation, because membership in the ideal was available only at the generators.  `Analysis/CollapseDisplacementIdeal.lean` supplies both printed sentences over the corona.  `displacement_mul_join` is *"`q_a` annihilates `∏_{b∈S}(1 − q_b)`, so `q_a q = q_a` and `d_a = d_a q_a = d_a q`"*, assembling `mul_one_sub_listProd` and `sub_mul_sq_sub` in the order the print composes them.  `displacement_mem_rankIdeal` is *"`𝓘_q` is inner-invariant, so `d_g ∈ 𝓘_q` for every `g ∈ L`"*, an induction over `Subgroup.closure S` on `eq:collapse-cocycle` itself: `d_1 = 0` because `p_1 = p`, `d_{xy} = d_x + Θ(x) d_y Θ(x)*` makes a product a sum of an ideal element and an inner-automorphic image of one, and `d_{x⁻¹}` is the same identity read at `(x⁻¹, x)`.  Inner invariance costs nothing here — `mul_mem_rankIdeal` already holds for arbitrary corona factors, so the induction never uses that the conjugating element is unitary, only that the ideal absorbs multiplication on both sides, which is what the printed word is doing.  **What the row still owes is the lift, and only that**: `lambda_cocycle` also wants the conjugating corona element to be the class of a coordinatewise unitary sequence, so that `Λ` is equivariant for it, and choosing such lifts compatibly across the group is `lem:unitarycorona` rather than algebra.  Landed at `9cc9835f` with its root import, MSI-validated green target-scoped  **THE LIFT IS CHOSEN AND THE INSTANTIATION IS SUPPLIED, 2026-08-19; no clause of this row is assumed anywhere on the badged route.**  `CollapseUnitaryLift.coronaAlmostRep` is `lem:unitarycorona` as the passage from `Θ` to an operator-norm almost representation, `CollapsePrintedContradiction.collapse_contradiction_localized` consumes the cocycle with ideal membership only at the Kazhdan image — which is where `displacement_mem_rankIdeal` proves it, the compressed displacements vanishing outright by the hypothesis on `p` — and `Analysis/CollapsePrintedProjectionCollapse.printed_projection_collapse` instantiates the displacement family at the theorem's own projection: `d_g = Θ(g) p Θ(g)* − p`, membership from the join, `eq:collapse-cocycle` from `sub_conj_mul` read through the chosen lift, and the nonvanishing from the printed telescoping, whose ring identity `∑ r_i q_i = 1 − ∏(1 − q_i)` is `sum_prefixProd_mul`.  `corona_projection_collapse` is proved through the assembly and `routeGuards` pins the route.  Grade on the first green `Build and audit` containing the rewire  **THE PRINTED SPACE IS FORMED AND THE OBJECTS ARE THE PRINTED ONES, 2026-08-19; a first-green build marker.**  Two divergences this row has recorded are closed by `Analysis/CollapseInvariantSubspace.lean`, and one is not.  The first is what the paragraph above calls *the one printed object still absent*.  `K_q` is formed: `Kq` is the topological closure of the range of `Λ`, which is `closure Λ(𝓘_q)` because `Λ` is defined on the ideal and nowhere else, and `mem_Kq_iff` is the only fact about it anything uses.  `lambda_mem_Kq` puts `Λ(𝓘_q)` inside it, `isClosed_Kq` is the printed adjective and is not a lemma — it is a closure — and `conjIsometryEquiv_mem_Kq` is the printed *invariant*: `conjIsometryEquiv_lambda` carries `Λ(𝓘_q)` into itself, two-sidedness of the ideal is what makes the conjugate land back in the domain of `Λ`, and a continuous map carries a closure into the closure of the image.  No estimate enters, and none is available — the equivariance is exact on the ideal and the closure is taken after it, which is the order the print takes them in.  `conjRepKq` is `π` restricted to `K_q` and `betaKq_cocycle` is this row's cocycle identity with both sides inside `K_q` rather than in the ambient `K_ω`.  The second is the recorded reason the `Objects` column reads `substituted`, that `eq:collapse-cocycle` was proved in an arbitrary ring with involution.  `printedDisplacement_cocycle` is that identity at the printed objects — the displacements `d_g = Θ(g) p Θ(g)* − p` of the corona representation `coronaRepOfLift` that a coordinatewise unitary lift induces through `unitarySequenceToCorona` — stated in the shape `lambda_cocycle` consumes, so the general identity and its printed instance are visibly the same statement about the same family; `printed_betaKq_cocycle` is then the printed sentence with nothing substituted at all: printed family, printed `Λ`, printed space, printed representation.  **What is NOT closed is the route, and the column stays where it is for that reason**: `corona_projection_collapse` still travels `collapse_contradiction_localized`, which works in the ambient `K_ω`, and the `routeGuards` pair pins it there, so the printed objects are now proved about and still not the ones the badged endpoint travels.  Objects stay `substituted` and both grades stay put until that rewire lands.  `a first-green build marker`: the module is wired into `GroupApproximation.lean` in the same change and has never compiled  **THE ROUTE'S BLOCKER IS DIAGNOSED 2026-08-19, AND IT IS NOT PLUMBING; NO REGRADE.**  The note above says what remains: `corona_projection_collapse` still travels `collapse_contradiction_localized`, which works in the ambient `K_omega`, so the printed objects are proved about and are still not the ones the badged endpoint travels.  Reading the two ends together says why the rewire has not happened, and it is worth recording so the next attempt does not start by rediscovering it.  `CollapseInvariantSubspace.collapse_contradiction_Kq` and its printed form take `Theta : H ->* (forall n, Matrix.unitaryGroup (Y n) C)` -- a COORDINATEWISE homomorphism -- because that is what `conjIsometryEquiv_mem_Kq` needs to carry `Lambda(I_q)` into itself.  `printed_projection_collapse` has no such object and cannot have one: its `Theta` is a homomorphism into `unitary (NormMatrixCStarCorona ...)`, and the coordinatewise object the printed route obtains from it through `lem:unitarycorona` is `coronaAlmostRep`, an `OpAlmostRepresentation`, which is multiplicative only asymptotically.  So the rewire is blocked by a real mismatch and not by wiring: the `K_q` invariance is stated for a stronger input than the printed route supplies.  **What would close it** is stating that invariance for conjugation by a CORONA unitary rather than by a coordinatewise homomorphism, which is also the printed form -- the print's `pi(g)` is conjugation by `Theta(g)` in `B_omega` -- and which the two-sidedness of `rankIdeal` in the corona already supports.  That is a change to `Analysis/CollapseInvariantSubspace.lean` and it belongs with whoever holds that file; duplicating `K_q` in a second module to get around it would leave the corpus with one theorem wearing two proofs, which is the defect class this audit exists to catch.  The `a first-green build marker` clause the note ends on is separately spent: `Analysis/CollapseInvariantSubspace` is root-imported and compiled in the MSI root-closure build of 2026-08-19, 5366 targets green  **THE NAMED BLOCKER IS CLOSED, 2026-08-19.**  The note above said the rewire was blocked by a real mismatch: `conjIsometryEquiv_mem_Kq` states the `K_q` invariance for conjugation by a COORDINATEWISE homomorphism, while the printed route supplies only a unitary of the corona, the print's `pi(g)` being conjugation by `Theta(g)` in `B_omega`.  It named the fix -- state the invariance for a corona unitary -- and warned that duplicating `K_q` in a second module would leave one theorem wearing two proofs.  `exists_conj_mem_Kq_of_coronaUnitary` is that statement, in the file that owns `K_q`: every corona unitary is represented by a coordinatewise unitary family (`unitaryCoronaToCStarCoronaUnitary_surjective`), and conjugation by such a family already preserves `K_q` because the printed ideal is two-sided in the corona, so no second `K_q` and no second proof.  Root build green at 5366 targets.  **What remains is downstream and is not this mismatch**: to move the grade, `conjRepKq` itself has to be built from a homomorphism into corona unitaries rather than into coordinatewise families, which needs the conjugation action to be independent of the chosen lift; the invariance it would consume now exists.  a first-green build marker  **THE ROUTE IS THE PRINTED ROUTE, 2026-08-19; EXACT/EXACT ON LITERAL OBJECTS.**  This row's last note said what was left and named it precisely: "`corona_projection_collapse` still travels `collapse_contradiction_localized`, which works in the ambient `K_omega`, and the `routeGuards` pair pins it there, so the printed objects are now proved about and still not the ones the badged endpoint travels."  It no longer does.  The blocker the following note diagnosed -- that `collapse_contradiction_Kq` wanted a COORDINATEWISE homomorphism the printed route cannot supply -- is gone, and it did not need a coordinatewise homomorphism to be built.  `OmegaIsometryRepresentation.rep` is ALREADY a homomorphism into the isometries of `K_omega` for an operator-norm ALMOST representation, because freeness of `omega` makes the classes multiply exactly even though the coordinate lifts do not, and that is exactly what `lem:unitarycorona` hands the printed route.  The two facts joining it to `K_q` were each one rewrite from statements already in the tree: `repEquiv_eq_conjIsometryEquiv` says `rep A g` IS conjugation by the coordinate family, and `conjIsometryEquiv_mem_Kq` preserves `K_q` for an arbitrary such family, so `restrictUnitary` makes `repKq` a representation on `K_q`; and `lambda_cocycle_almost` is this row's identity for `rep A` already, so reading it in the subspace is `Subtype.ext`.  `Analysis/CollapseKqAlmostRep.lean` is that, and `collapse_contradiction_localized_Kq` is the printed contradiction with the SAME hypotheses as the ambient `collapse_contradiction_localized` -- hypothesis for hypothesis, nothing added -- run where the print runs it.  `CollapsePrintedProjectionCollapse.printed_projection_collapse` now ends there, and the three `routeGuards` pins moved with it in the same commit: `corona_projection_collapse` is pinned to `collapse_contradiction_localized_Kq`, to `Kq` itself so that "the printed space" is a checked claim and not a described one, and to `transport_corona`.  All three were run and hold: the audit reports `route guards: 22 dependency pin(s) hold`.  Objects move to `literal` because the reason recorded for `substituted` -- `eq:collapse-cocycle` proved in an arbitrary ring with involution -- was answered by `printedDisplacement_cocycle` and `printed_betaKq_cocycle`, and the route that consumes them is now the printed one  The build marker this row carried is cleared with evidence: `Analysis/CollapseInvariantSubspace`, `Analysis/CollapseKqTransport` and `Analysis/CollapseKqAlmostRep` are all root-imported and compiled on the MSI builds of 2026-08-19, and the audit that reports the twenty-two route pins loaded the environment they live in |
| CO.21b | thm:projection-collapse | Delorme–Guichardet makes $\beta$ a coboundary; the hypothesis on $p$ makes its primitive fixed by $\pi (sLs^{-1})$, the transport chain identifies that fixed space with Fix $\pi (L)$ at the weight $k_n$, and $\beta = 0$ contradicts the previous step | `CollapseDelormeEndpoint.collapse_contradiction_of_hasKazhdanPropertyT`; `CollapseDelormeEndpoint.exists_primitive_of_hasKazhdanPropertyT`; `CollapseDelormeEndpoint.eq_zero_of_compressed_fixed`; `kt_10_finiteness_reverses`; `ScaledKazhdanTransport.scaled_transport_both`; `CollapseTransportEndpoint.transport_corona`; `CollapseTransportEndpoint.collapse_contradiction_corona`; `CollapseCoronaIsometry.norm_actKOmega_of_unitary`; `CollapseDelormeCorona.coronaRep`; `CollapseDelormeCorona.collapse_contradiction_corona_delorme`; `CollapseRouteJoin.coronaRep_omegaCompressionRep`; `CollapseCompressionBundle.collapse_contradiction_printed`; `CollapseInvariantSubspace.completeSpace_Kq`; `CollapseInvariantSubspace.restrictUnitary`; `CollapseInvariantSubspace.collapse_contradiction_Kq`; `CollapseInvariantSubspace.printed_collapse_contradiction_Kq`; `CollapseKqTransport.restrictUnitary_transport`; `CollapseKqTransport.transport_Kq_of_ambient`; `CollapseKqTransport.collapse_contradiction_Kq_of_ambient`; `CollapseKqTransport.printed_collapse_contradiction_Kq_of_ambient`; `CollapseKqAlmostRep.collapse_contradiction_Kq_almost`; `CollapseKqAlmostRep.collapse_contradiction_Kq_almost_of_ambient`; `CollapseKqAlmostRep.collapse_contradiction_localized_Kq` | EXACT | EXACT | literal | unconditional | Delorme, Guichardet | NO | The complete coboundary, fixed-space transport, and contradiction route is formalized; the manuscript now cites Delorme's original journal article. |
| CO.22 | p:collapse-relcomm | consequently $\Theta (sLs^{-1})' \cap Q$ cannot exceed $\Theta (L)' \cap Q$ by a projection with commuting L-orbit | `ProjectionCompressionCollapse.corona_projection_collapse` | EXACT | EXACT | literal | unconditional | - | NO | restatement of the theorem |
| CO.23 | rem:torsion-collapse | Remark: finite-order witnesses; every $[\gamma ,k]$ is invisible, the normal closure lies in $\operatorname{Res}_{\mathrm{MF}}(H)$, with equality when the quotient is MF | `TorsionSpectralCollapse.actualCoronaMFInvisible_of_torsionWitness`; `TorsionSpectralCollapse.torsionCollapseDefect_le_actualCoronaMFResidual_of_kazhdan`; `TorsionSpectralCollapse.actualCoronaMFResidual_eq_torsionCollapseDefect_of_kazhdan` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  all three `TorsionSpectralCollapse` declarations read `{E : Type u}` with `HasKazhdanPropertyT.{u,u}` |
| CO.24 | rem:torsion-collapse | inline: the discrete Fourier idempotents $p_j = m^{-1} \Sigma \zeta ^{-jt}u^t$ are self-adjoint idempotents of the corona | `TorsionSpectralCollapse.fourierIdem`; `TorsionSpectralCollapse.star_fourierIdem`; `TorsionSpectralCollapse.fourierIdem_mul_self` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CO.25 | rem:torsion-collapse | each $p_j$ is a polynomial in $u$, so the compressed copy centralizes it and the $\Theta (L)-conjugates$ commute pairwise | `TorsionSpectralCollapse.commute_fourierIdem` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CO.26 | rem:torsion-collapse | Theorem 11.24 makes every $p_j$ commute with $\Theta (L)$; Fourier reconstruction expresses $u$ as a $\zeta -weighted$ sum, so $\Theta ([\gamma ,k]) = 1$ | `TorsionSpectralCollapse.actualCoronaMFInvisible_of_torsionWitness` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.01 | thm:signfree | Theorem $11.29: E/\langle w\rangle$ is not MF; the image of $[\iota (v_1),\hat{c}]$ is nontrivial there and lies in $\operatorname{Res}_{\mathrm{MF}}(E/\langle w\rangle )$; consequently $\{1,w\}$ is a proper subgroup of $\operatorname{Res}_{\mathrm{MF}}(E)$ | `LiteralSignFreeQuotient.signFree_collapse`; `LiteralSignFreeQuotient.signFreeQuotient_not_isCDEOperatorMF`; `LiteralSignFreeQuotient.commutator_not_zpow_mark` | EXACT | EXACT | literal | unconditional | - | NO | added to the manuscript during this audit; all three clauses are separately badged |
| SF.02 | thm:signfree | c̄ = q(ĉ) is an involution because c² = 1 | `LiteralSignFreeQuotient.dee_sq` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.03 | thm:signfree | q(t)L̄q(t)⁻¹ centralizes c̄ because tι(ℬ)t⁻¹ commutes with c in E | `LiteralSignFreeQuotient.dee_commutes_compressed` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.04 | thm:signfree | property (T) passes to L̄ and q(t) compresses it, so (W1) and (W2) hold and the content is (W3) | `LiteralSignFreeQuotient.signFree_collapse` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.05 | thm:signfree | the rotation letters commute with ĉ, and t carries each vᵢ to vᵢ², so ι(vᵢ²) commutes with ĉ | `LiteralSignFreeQuotient.dee_commutes_x`; `LiteralSignFreeQuotient.dee_commutes_v1_sq` | EXACT | EXACT | literal | unconditional | - | NO | all three rotation letters and all three squared translations |
| SF.06 | thm:signfree | hence $\iota (g)\hat{c}\iota (g)^{-1}$ depends only on the exponent vector of the translation part modulo 2, leaving eight conjugates indexed by $(\mathbb{Z}/2)^3$ | `LiteralSignFreeQuotient.doubledBase`; `LiteralSignFreeQuotient.dee_commutes_doubledBase`; `LiteralSignFreeQuotient.rotations_le_doubledBase` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.07 | thm:signfree | the six rotation words $x, x^2, z$, yz, yx, $y^2x$ carry the class of $v_1$ onto the six remaining nonzero classes, each commuting with ĉ | `LiteralSignFreeQuotient.commutator_conj_transport`; `LiteralSignFreeQuotient.commutator_parity` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.08 | thm:signfree | conjugating the displayed identity exhibits every nonzero parity commutator as a conjugate of $w$, the zero class gives [ĉ,ĉ] = 1, and centrality puts all eight in $\langle w\rangle$ | `LiteralSignFreeQuotient.commutator_via_orbit`; `LiteralSignFreeQuotient.commutator_not_mem_markSubgroup` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.09 | thm:signfree | so c̄ is an involutive compression witness and Theorem 11.22 places $[q(\iota (v_1))$, c̄] in $\operatorname{Res}_{\mathrm{MF}}(E/\langle w\rangle )$ | `LiteralSignFreeQuotient.signFree_collapse` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.10 | thm:signfree | nontriviality passes through $E \to$ W: the commutator goes to the product of the two distinct lamp basis elements while $\langle w\rangle$ goes into $\langle \zeta \rangle$, whose configuration coordinate is zero | `LiteralSignFreeQuotient.commutator_not_zpow_mark` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SF.11 | thm:signfree | hence the image is nontrivial and Proposition 11.6 makes $E/\langle w\rangle$ non-MF; Corollary 11.9 identifies $\operatorname{Res}_{\mathrm{MF}}(E)$ with the q-preimage, so $u$ belongs to it | `LiteralSignFreeQuotient.signFreeQuotient_not_isCDEOperatorMF`; `ManuscriptExactWrappers.manuscriptRadicalReductionToQuotient` | EXACT | EXACT | literal | unconditional | - | NO | - |
| CO.33 | cor:collapsequot | unconditional radical reduction along the collapse quotient, with equality when the quotient is MF | `ManuscriptExactWrappers.manuscriptCollapseRadicalReduction` | EXACT | EXACT | literal | unconditional | - | NO | UNIV0 SWEEP 2026-08-16: upgraded from MISMATCH.  `manuscriptCollapseRadicalReduction` reads `{H : Type u}` |
| RE.01 | p:D-preamble | every MF algebra is stably finite | `mul_star_eq_one_of_hasMFEmbedding`; `not_hasMFEmbedding_of_isometry_ne_unitary`; `MatrixCoronaFinite.mul_eq_one_symm_matrixCorona`; `MatrixCoronaFinite.cstarMatrix_mul_star_eq_one_of_hasMFEmbedding`; `MatrixCoronaFinite.mfAlgebra_isStablyFinite` | EXACT | EXACT | literal | unconditional | BK | NO | 2026-08-16 audit of whether this row is really a live literature input. Verdict NO. (a) `thm:D` does not use it: stable finiteness there is `lem:faithfultrace`, verified as `ManuscriptExactWrappers.manuscriptFaithfulTraceAndStableFiniteness`, and the printed sentence says so itself, "independently of the MF question". (b) The only inferential use anywhere in the TeX is `rem:maxinfinite`'s "in particular is neither residually finite-dimensional nor MF", the contrapositive of this row, and that is already badged and proved with no citation at all by `ProperProjectionCompression.not_hasMFEmbedding`. (c) The cited declarations prove the k=1 half in general form, unconditionally, from KT.06 plus the corner argument. (d) The k>1 half is used nowhere in the manuscript and is one C-star lemma short, namely M_I of a norm-matrix corona being again a norm-matrix corona; the reduction is `mul_star_eq_one_of_matrixHasMFEmbedding`. Deps stays `literature-input` only because the printed sentence taken whole is still uncited; no result in the development rests on it. **MARKER RETIRED 2026-08-18, WITHOUT THE GRADE MOVING, WHICH IS THE POINT.**  This row carried the waiting-table marker, and that certificate now exists -- run 32122738761 on `c0f1e4e8`, `Build and audit` `completed/success` with `Zero-tolerance verdict` green -- so the marker would now assert something false, that the row is waiting on a build.  It is not, and it never really was: the two declarations cited compile and always did, and what holds the row at `MISSING/MISSING` is (d) above, entirely independent of build state.  The `k>1` half of the printed sentence is one C-star lemma short -- `M_I` of a norm-matrix corona being again a norm-matrix corona -- and the sentence taken whole is therefore still uncited, which is why `Deps` stays `literature-input` even though the verdict on this being a LIVE literature input is `NO` and nothing in the development rests on it.  A green build cannot close a row whose gap is a missing lemma, so the marker is removed rather than satisfied.  **THE MISSING LEMMA IS PROVED 2026-08-18, module `Analysis/MatrixCoronaDedekindFinite.lean`, AND THE ROW IS NOT REGRADED HERE.**  Clause (d) above named the gap as `M_I` of a norm-matrix corona being again a norm-matrix corona, and that framing is now retired as the wrong target: the isomorphism `M_I(Q_X) ≃⋆ Q_{I×X}` is not what the reduction consumes.  What `mul_star_eq_of_corner_isometry` needs from the amplified algebra is one sentence -- every one-sided inverse in it is two-sided -- and for `Q_X` itself that sentence is `KT.06`, proved by lifting to coordinates, correcting there with the Neumann series, and pushing back through the quotient.  Those three steps work one level up with no isomorphism at all, because the coordinates of `M_I(Q_X)` are the amplified finite matrices `M_{I × X n}` and the coordinate correction `norm_swap_sub_one_le` was already stated at an arbitrary finite index type.  `mul_eq_one_symm_matrixCorona` is that argument: assemble a matrix of lifts into one amplified sequence, run the coordinate estimate, read the conclusion back off the blocks.  The two block comparisons it needs -- a block is no larger than the matrix, the matrix is no larger than the sum of its blocks -- are `Analysis/StablyFiniteAmplification`'s, restated over an arbitrary finite index rather than `Fin m` because the reduction quantifies over an arbitrary `I`.  On top of it, `cstarMatrix_mul_star_eq_one_of_hasMFEmbedding` runs the `k = 1` corner argument verbatim with the amplification in place of the algebra: `e` applied entrywise is a non-unital `⋆`-homomorphism, so `E 1` is a projection of `M_I(Q_X)` and the printed `σ = v + (1 - E 1)` computation runs in its corner.  `mfAlgebra_isStablyFinite` is the printed sentence taken whole, as a closed proposition, with no literature input and no hypothesis on `I` beyond finiteness -- the empty index is a subsingleton and is dispatched inline.  **Why the row does not move.**  The standard here is a green `Build and audit`, and the newest such run at the time of writing is `32122738761` on `c0f1e4e8`, which predates this module; every `Lean Prover CI` run since has been cancelled or has failed, and the failures are in modules unrelated to this row -- `Sofic/LiteralTheoremAPackage`, `Sofic/LiteralRelatorObstruction`, `Sofic/CliffordBSAmenableMF`, `Analysis/CStarStateSeparation`, `Analysis/CStarIdealApproximateUnit`, all root-imported and all red.  The module here is green as a target build and is in the root import closure, so the first green run settles the row; at that point `Statement` and `Proof` both become `EXACT`, `Objects` becomes `literal`, and `Deps` moves from `literature-input` to `unconditional`, because the printed sentence taken whole then has a proof and the Blackadar--Kirchberg citation is no longer carrying anything  **GRADED 2026-08-19: the held condition is met, in the four cells the note names.**  The note above states the outcome in advance -- "the first green run settles the row; at that point `Statement` and `Proof` both become `EXACT`, `Objects` becomes `literal`, and `Deps` moves from `literature-input` to `unconditional`, because the printed sentence taken whole then has a proof and the Blackadar--Kirchberg citation is no longer carrying anything".  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  At `dd1ca3546` `Analysis/MatrixCoronaDedekindFinite` is present and imported by `GroupApproximation.lean`, so `mfAlgebra_isStablyFinite` -- the printed sentence taken whole, as a closed proposition with no hypothesis on `I` beyond finiteness -- was elaborated by the certifying build.  The five modules the note blamed for the failures since `c0f1e4e8` are all green in that run.  The `Cite` cell keeps `BK` because the note's instruction enumerates four cells and this is not one of them: the reference records where the sentence comes from, and `Deps` is what records that nothing rests on it |
| RE.02 | p:D-preamble | stable finiteness is supplied by the faithful canonical trace, not inferred from the absence of MF structure | `LiteralNonMFEndpoint.manuscriptTheoremD` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RE.03 | thm:reduced | Theorem D: $\Cred(E)$ is unital, separable, carries a faithful tracial state, is stably finite, and is not MF | `LiteralNonMFEndpoint.manuscriptTheoremD` | EXACT | EXACT | literal | unconditional | - | NO | stable finiteness in every nonempty finite matrix amplification |
| RE.04 | p:D-base-injective | the canonical ℬ $\to E$ is injective: compose with $E \to W$ and use that ℬ $\to \bar{\Gamma }$ is an isomorphism and $j$ is injective | `LiteralBaseCompleteness.baseAffineEquiv`; `MappingTelescope.level_injective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RE.05 | p:D-base-injective | since the infinite Kazhdan group ℬ is nonamenable, so is $E$; hence $\Cred(E)$ is not nuclear | `PropertyTNonamenable.infinite_kazhdan_not_isAmenable`; `PropertyTNonamenable.not_isAmenable_of_infinite_kazhdan_subgroup`; `NuclearityAmenability.markedGroup_not_isAmenable`; `NuclearityAmenability.markedGroup_not_hasInvariantMean`; `NuclearityAmenability.reducedGroupCStar_not_nuclear_of_lance`; `CStarExactness.NuclearReducedCPAP`; `CStarExactness.hasInvariantMean_of_nuclearReducedCPAP`; `CStarExactness.reducedGroupCStar_not_nuclear` | EXACT | EXACT | literal | unconditional | Lance | YES | The nonamenability and nuclearity implication are formalized at the printed CPAP notion; Lance's journal article is the external source retained in print. |
| RE.05b | p:D-base-injective | E contains an isomorphic copy of the infinite Kazhdan base, hence is nonamenable | `NuclearityAmenability.manuscriptNotAmenableOfBaseEmbeds` | EXACT | EXACT | literal | unconditional | - | NO | the nonamenability half of RE.05, now proved; the step from nonamenable to non-nuclear is Lance and remains the literature input recorded there |
| RE.06 | lem:faithfultrace | (1) the canonical tracial state on $\Cred(G_1)$ is faithful | `ManuscriptExactWrappers.manuscriptFaithfulTraceAndStableFiniteness` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic |
| RE.07 | lem:faithfultrace | (2) a unital $$C^*$-algebra$ with a faithful tracial state is stably finite: for every $k \ge 1$, every isometry in $M_k(A)$ is a unitary | `ManuscriptExactWrappers.manuscriptFaithfulTraceAndStableFiniteness` | EXACT | EXACT | literal | unconditional | - | NO | RE-READ 2026-08-16: the printed clause was narrowed from an arbitrary nonempty finite index set to M_k, so Lean is now strictly stronger than the print -- it proves both forms |
| RE.08 | lem:faithfultrace | proof (1): right translations commute with $\Cred; \tau (x*x) = 0$ gives $x\delta _e = 0$, hence $x\delta _g = 0$ for all $g$, and the $\delta _g$ are total | `ReducedGroupCStarTrace.canonicalFaithfulTracialState` | EXACT | EXACT | literal | unconditional | - | NO | - |
| RE.09 | lem:faithfultrace | proof $(2): \tau _k$ is a faithful trace on $M_k(A)$, and v*v = 1 makes $y = 1 -$ vv* a projection with $\tau _k(y) = 0$, so $y = 0$ | `FaithfulTracialState.matrix_mul_star_eq_one_of_star_mul_eq_one` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ES.01 | thm:Esofic | Theorem E: E itself is sofic, with no hypothesis and no literature input | `LiteralSoficAssembly.markedGroup_isSofic` | EXACT | EXACT | literal | unconditional | - | NO | the block-Clifford window tower over the telescope, then the cyclic truncation of the ℤ step; the module is in the root import closure as of this revision, so the badge is built |
| ES.02 | thm:Esofic | E is hyperlinear, since sofic groups are | `LiteralSoficAssembly.markedGroup_isHyperlinear` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ES.03 | thm:Esofic | E is finitely presented, sofic, and not MF -- one explicit group with all three | `LiteralSoficAssembly.markedGroup_finitelyPresented_sofic_nonMF` | EXACT | EXACT | literal | unconditional | - | NO | premise-free composition of ES.01 with `LiteralSoficEndpoint`, whose packaged endpoints take soficity as their single premise |
| ES.04 | thm:Esofic | the same, with hyperlinear in place of sofic | `LiteralSoficAssembly.markedGroup_finitelyPresented_hyperlinear_nonMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| ES.05 | thm:Esofic | E is sofic and not LEF, in particular not residually finite | `LiteralSoficAssembly.markedGroup_sofic_not_isLEF_unconditional` | EXACT | EXACT | literal | unconditional | - | NO | the printed reason is that an LEF group is a marked limit of finite groups and `thm:markedclosed` closes the MF locus; the Lean route is `markedGroup_not_isLEF` |
| ES.06 | thm:Esofic | the kernel of the stable-letter exponent is locally residually finite, hence LEF, hence sofic | `StableLetterLEFRoute.telescopeKernel_locallyResiduallyFinite`; `StableLetterLEFRoute.telescopeKernel_isLEF`; `StableLetterLEFRoute.telescopeKernel_isSofic`; `StableLetterLEFRoute.mem_stableExponent_ker_iff`; `StableLetterLEFRoute.manuscriptEsoficPrintedRoute`; `BlockCliffordTowerSofic.exists_residuallyFinite_subgroup_telescope` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-19 from UNDER-SPECIFIED/UNDER-SPECIFIED, whose reason is spent.**  The old note said the formal proof runs the block-Clifford window tower and never names `E₀`, its local embeddability, or the split extension, so there was nothing to bind step for step.  `Sofic/StableLetterLEFRoute.lean` (landed `290ca38e`, 2026-08-17, in the root closure and inside the settled green `Build and audit` runs) names each sentence: `TelescopeKernel` is the printed `E₀ = C(𝒢) ⋊ T`, `mem_stableExponent_ker_iff` identifies it with the kernel of the exponent sum in `t` — which is how the print defines `E₀` — `exists_residuallyFinite_subgroup_telescope` is the printed *"the subgroup they generate embeds in a residually finite group"*, `telescopeKernel_isLEF` the printed *"hence `E₀` is LEF"*, `telescopeKernel_isSofic` the soficity the Elek–Szabó step consumes, and `manuscriptEsoficPrintedRoute` the whole paragraph as one proposition, with the split extension `markedGroupEquivKernelByInt` and the printed contrast `¬ IsLEF E`.  The one clause that existed only through the window exhaustion — this row's own words, *"locally residually finite"* — is now also stated with the quantifier the phrase abbreviates: `telescopeKernel_locallyResiduallyFinite`, every finitely generated subgroup of `E₀` is residually finite, landed with this regrade and MSI-validated green target-scoped.  The badge on `thm:Esofic` stays on `markedGroup_isSofic`, per the note in the module: the printed-order derivation is propositionally the same soficity statement, so it is deliberately not a second named endpoint |
| SO.11 | p:E-extension | $W$ fits in $1 \to Cl(X) \to W \to \Sigma \to 1$ with MF kernel and quotient but non-MF total | `LiteralWitnessConsequences.literalWitness_locallyFiniteKernel_nonMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SO.12 | p:E-extension | the lamp kernel is locally finite, LEF, sofic and MF | `CliffordLamp.isLocallyFiniteGroup_cliffordLamp`; `CliffordLamp.isLEF_cliffordLamp`; `CliffordLamp.isSofic_cliffordLamp`; `CliffordLamp.isOperatorMF_cliffordLamp` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SO.13 | p:E-extension | $V$ is the subgroup of $GL_4(\mathbb{Q})$ generated by $\bar{\Gamma }$ and $D$, with telescope levels $D^{-n}\bar{\Gamma }D^n$ and determinant giving the $\mathbb{Z}-coordinate$; it is residually finite via odd-modulus congruence reductions, hence MF | `WitnessVertical.vertical_isOperatorMF` | EXACT | EXACT | literal | unconditional | Malcev | NO | Mal'cev is cited only for comparison; the reduction is proved internally |
| SO.14 | p:E-zext | $W \to \mathbb{Z}$ has kernel $Cl(X) \rtimes T$, which is LEF hence MF; so $W$ is an extension of an LEF group by $\mathbb{Z}$ and is not MF | `LiteralLEFExtension.literalWitness_LEFKernel_integerExtension_nonMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| SO.14b | p:E-zext | soficity is preserved by an extension with amenable quotient | `SoficByAmenablePermanence.manuscriptSoficOfSoficKerOfAmenableQuotient` | EXACT | EXACT | literal | unconditional | - | NO | the Elek--Szabo permanence the sentence cites, now in-repo rather than a citation |
| SO.15 | p:E-zext | soficity is preserved by every extension with amenable quotient | `SoficByAmenablePermanence.isSofic_of_isSofic_of_isAmenable_quotient`; `SoficByAmenablePermanence.manuscriptSoficOfSoficKerOfAmenableQuotient` | EXACT | EXACT | literal | unconditional | ElekSzabo | NO | **REGRADED 2026-08-17 from MISSING/MISSING.**  The old note, "only the ℤ case is formalized", is now false: `isSofic_of_isSofic_of_isAmenable_quotient` reads `{G : Type*} [Group G] (N : Subgroup G) [N.Normal] (hN : IsSofic N) (hQ : IsAmenable (G ⧸ N)) : IsSofic G` — every extension, universe-polymorphic, with no countability premise, no splitting hypothesis and no literature input.  The proof is the Følner argument carried out in full: a Følner set of `G ⧸ N` for the translations of `F` and their products at boundary parameter `ε/8`, levels the completed left translations, fibres a sofic model of `N` on the finite set of cocycle values at accuracy `ε/2`, multiplicativity exact off the Følner boundary, and separation split according to equality of images in the quotient.  `Source` keeps `ElekSzabo` as the attribution the sentence carries |
| SO.16 | p:E-exact | $\Cred(E)$ is exact | `LiteralWitnessConsequences.literalWitness_reducedGroupCStar_stablyFinite_nonMF`; `ExactnessPermanence.hasPropertyA_of_isLocallyFiniteGroup`; `ExactnessPermanence.hasPropertyA_of_finite`; `ExactnessPermanence.hasPropertyA_cliffordLamp_viaAmenability`; `RoeSquareRoot.exists_sqrt_mem`; `RoeSquareRoot.exists_finiteWidth_gram`; `GuentnerKaminkerEndpoint.isRoeGramApproximable`; `GuentnerKaminkerEndpoint.hasPropertyA_of_compressionExceeds_half`; `CStarTensor.IsExactCStar`; `CStarTensor.minTensorInclusion_injective`; `CStarTensor.norm_sliceRight_vecFunctional_le`; `CStarTensor.norm_gnsRep_sliceRight_le`; `CStarTensor.sliceRightMin`; `CStarTensor.sliceLeftMin`; `CStarTensor.sliceLeftMin_mem_of_quotientMinTensorMap_eq_zero`; `CStarTensor.HasSliceMapProperty`; `CStarTensor.isExactCStar_of_hasSliceMapProperty` | MISMATCH | MISMATCH | substituted | literature-input | DykemaExact, GHW, KWPermanence, KWExact | NO | **PROOF COLUMN UPGRADED 2026-08-17 from MISSING, and the objects with it.**  The old note stays true — everything except exactness is formal, and the pinned Mathlib has no definition of an exact C*-algebra — but the group side of the printed chain is proved rather than absent.  `Analysis/ExactnessPermanence.lean`, root-imported and on `origin/verified`, sets out the four printed links and proves the group-side ones over property A: `hasPropertyA_of_isLocallyFiniteGroup` for the locally finite lamp kernel, `hasPropertyA_of_finite` for its finite pieces, `hasPropertyA_cliffordLamp_viaAmenability` for the Clifford lamp itself.  Those are substituted objects and the row must not read as literal: property A of the group stands where the print says exactness of the algebra.  What has no formal counterpart is the crossover link — Kirchberg--Wassermann, for a discrete group `E` exact iff `Cred(E)` exact — which is what would carry the group side to the algebra the sentence is about.  Under the zero-literature order it cannot acquire one by citation, so closing this row needs a theory of exact C*-algebras first.  **NOTE ADDED 2026-08-19, no grade change.**  Two of the three sentences of that last clause have moved.  Exactness is *statable* in-tree: `CStarTensor.IsExactCStar` (`Analysis/CStarMinTensorQuotient.lean`, on `origin/verified`) is the textbook kernel form of exactness against the canonical `⊗_min` of `CStarMinTensorNorm`, and the inclusion `closure (I ⊙ C) ⊆ ker` is proved there, so "Mathlib has no definition of an exact C*-algebra" no longer describes what this development can say -- only what it has imported.  Validated on MSI on 2026-08-20: `Analysis/CStarMinTensorInjective.lean` (injectivity of `⊗_min` -- an injective `ι : A →⋆ₐ C` induces an isometric `A ⊗_min B → C ⊗_min B`, two applications of the in-tree Takesaki identification `spatialNorm_eq_minTensorNorm` around one operator identity) and `Analysis/CStarExactComplex.lean` (`isExactCStar_complex`: `ℂ` is exact, the first algebra in this development shown to satisfy the predicate, which is what keeps the predicate from being vacuous).  What still has no counterpart, and is what the row is graded on: the slice-map (Fubini) identity `closure (I ⊙ C) ∩ (B ⊗_min A) = closure (I ⊙ A)` that turns injectivity into "a subalgebra of an exact algebra is exact", and above it the Kirchberg--Wassermann crossover itself  **NOTE REFRESHED 2026-08-19; NO REGRADE, AND THE ROW IS NOT CLOSABLE HERE.**  Two things moved under this row and neither closes it, so both are recorded rather than left for a later reader to rediscover.  `CStarTensor.IsExactCStar` now exists, so exactness of a C-star algebra is a predicate in this development rather than a word; and `Analysis/CStarMinTensorInjective` proves INJECTIVITY of the minimal tensor norm -- an inclusion of C-star algebras induces an isometric inclusion of minimal tensor products -- which is the structural input of every permanence argument in this area.  What is still absent is stated by that module itself and is the honest boundary: the slice-map (Fubini) identity, for which this development has no vocabulary, and through it the Kirchberg--Wassermann crossover, that a discrete group `E` is exact iff `Cred(E)` is.  So no algebra becomes exact because of the new modules, the group side stays where it was -- property A of the lamp kernel, its finite pieces and the Clifford lamp -- and `Objects` stays `substituted` because property A of the group is what stands where the print says exactness of the algebra.  This row is a research lane, it is being worked by another hand, and closing it by any route available here would mean either duplicating that lane or reading the printed sentence as something other than what it says.  Neither is acceptable, so the row stands  **THE ANALYTIC HALF OF THE SLICE STEP IS PROVED, 2026-08-19; the grade does not move.**  The note above named the slice-map (Fubini) identity as what is absent, and `Analysis/CStarMinTensorInjective` said the development had *no vocabulary* for it.  That was wrong and is corrected in the same wave: `sliceRight`, `coordSlice` and `coord_sliceRight` have been in `Analysis/CStarTensorProductSpatial` throughout, and `CStarMinTensorNorm`'s faithfulness argument `eq_zero_of_forall_spatialHom_gnsRep_eq_zero` is exactly the degenerate case of Fubini -- an element all of whose slices vanish is zero.  What was genuinely missing was the estimate that carries those maps to the minimal completion, and `norm_sliceRight_vecFunctional_le` supplies it for the vector functionals, which are the case the argument uses because they separate: `‖sliceRight (vecFunctional π ξ ξ') x‖ ≤ ‖ξ‖ * ‖ξ'‖ * ‖x‖_min`.  The proof is the one `inner_spatialHom_apply` was waiting for -- take the second vector to be `ρ (R x) η`, Cauchy--Schwarz on the left, `‖ρ (R x) η‖²` on the right, cancel a factor -- and `exists_state_norm_le_gnsRep` lifts the per-representation bound to the slice, the GNS family attaining every norm.  **What remains of this row is now one identity and no infrastructure**: `closure (I ⊙ C) ∩ (B ⊗_min A) = closure (I ⊙ A)`, and after it the Kirchberg--Wassermann crossover.  Objects stay `substituted` and the grade stays MISMATCH: property A of the group still stands where the print asserts exactness of the algebra.  **EXTENDED 2026-08-19, grade unchanged.**  The unproved half is now *named*: `Analysis/CStarExactnessSliceReduction` proves the inclusion of `ker (q (x) id)` in the set of elements all of whose slices lie in `I` unconditionally, so with `idealTensorSpan_subset_ker` the three sets sandwich, and `isExactCStar_of_hasSliceMapProperty` derives `IsExactCStar C` from the slice map property alone.  The continuous slices it runs on are `CStarSliceCompletion.sliceRightMin` and `CStarSliceLeft.sliceLeftMin`, the extensions of the algebraic slices across the minimal completion by the bound of `CStarSliceBound`.  Nothing here proves any algebra exact and the row stays MISMATCH; what changed is that the debt is a statement about slices of single elements rather than about a tensor-product kernel.  **GROUP-SIDE ENDPOINT VERIFIED 2026-08-20; no regrade.**  `RoeSquareRoot.exists_sqrt_mem` and `exists_finiteWidth_gram` now elaborate, and `GuentnerKaminkerEndpoint.hasPropertyA_of_compressionExceeds_half` proves the Guentner--Kaminker group-side theorem with no operator-step hypothesis.  This does not prove `SO.16`: no declaration supplies `CompressionExceeds` for the manuscript literal vertical group, the printed route instead cites GHW, and no declaration crosses from property A to `CStarTensor.IsExactCStar` for the reduced group algebra.  The exact remaining mismatch is therefore still the literal algebra conclusion, not the Roe square-root infrastructure.  **SLICE SUPPORT FIRST GREEN 2026-08-20; no regrade.**  `CStarMinTensorInjective.lean`, `CStarExactComplex.lean`, and `CStarExactnessSliceReduction.lean` all elaborate without diagnostics at the pinned toolchain through the MSI wrapper.  Their declarations are valid support evidence, but none supplies the slice-map property for the literal reduced group algebra, so `SO.16` remains the sole mismatch. |
| SO.17 | p:E-simple | there is a countable simple sofic group equal to its own MF radical | `SimpleSoficEnvelopeUnconditional.manuscript_simpleSoficEnvelope` | EXACT | EXACT | literal | unconditional | - | NO | the embedding of a countable sofic group into a simple sofic one is quoted.  **PROPOSED 2026-08-17 AND DELIBERATELY NOT GRADED — this is the biggest single closure of the day and it is exactly the case where taking it on report would be wrong.**  `SimpleSoficEnvelopeUnconditional.manuscript_simpleSoficEnvelope` states the printed sentence with no premise at all: a countable simple sofic group whose MF residual is everything, in both presentations, killing every homomorphism into an MF group and not MF itself.  It gets there because `boundedNormalGeneration` now *proves* the `hBNG` premise that `SoficEnvelopeWitness` carried — the involution-block assembly, factoring the target into two involutions of equal transposition parity, cutting each into blocks of at most `c` pairs reachable in two conjugates, and pairing leftover odd counts through a spare transposition, which needs six points in the model rather than a bound on the target's support.  **Both `Sofic/SimpleSoficEnvelopeUnconditional` and `Algebra/InvolutionBlockAssembly` were outside the root import closure and had never elaborated; that changed on 2026-08-18.**  Both are now imported by `GroupApproximation.lean` and both compile: `InvolutionBlockAssembly` needed four repairs (a parity rewrite whose `Even.neg_one_pow` left its carrier a metavariable that did not resolve to `ℤˣ`, an `unnecessarySimpa`, `List.mem_cons_self` under its current implicit-argument spelling, and an `omit` for two unused section instances), and it in turn needed `Algebra/InvolutionBlockSplit` and `Algebra/PermutationTwoInvolutions`, which were themselves broken.  The row is left `MISSING/MISSING` and moved to the waiting table rather than graded here: the standard this ledger applies is a green `Build and audit`, and what exists so far is a green cluster `lake build` of 5238 jobs, which is the second-hand-green trap MC.04 was restored over.  Grade it when CI certifies the commit, so the row stays `MISSING/MISSING` and `ElekSzaboHyper` stays in `Source`.  This is the same standard that put MC.04 back in the waiting table hours earlier, applied to a result I would much rather bank: closure membership plus a green build, then grade.  SO.18 is unaffected either way — it never depended on the envelope existing   **GRADED EXACT/EXACT 2026-08-18, and the standard the row set for itself is the one that was applied.**  The note above declined to grade on "a green cluster `lake build` of 5238 jobs, which is the second-hand-green trap MC.04 was restored over", and asked for CI.  CI has now certified it: run 32122738761 on `c0f1e4e8` has `Build and audit` `completed/success` with step 25, `Zero-tolerance verdict`, green -- the step that reads the three outcome-masked scans, so this is not a `continue-on-error` pass.  Both modules the row named are imported by `GroupApproximation.lean` at that commit, `Algebra.InvolutionBlockAssembly` at line 1058 and `Sofic.SimpleSoficEnvelopeUnconditional` at line 1089, so both were elaborated by the certifying build rather than merely present in the tree.  The grade transfers to `main`, whose Lean content is byte-identical to `c0f1e4e8` -- only `research/*.md` and `experiments/` have moved since.  `ElekSzaboHyper` accordingly leaves `Source` and `Deps` becomes `unconditional`: `manuscript_simpleSoficEnvelope` carries no premise, because `boundedNormalGeneration` now proves the `hBNG` hypothesis that `SoficEnvelopeWitness` assumed, so the embedding of a countable sofic group into a simple sofic one is proved here rather than quoted.  A citation hazard checked and cleared, since it is the very trap `MC.04` records: `manuscript_simpleSoficEnvelope` is declared in TWO modules, here and in `Sofic/SoficEnvelopeWitness`, but their namespaces are `SimpleSoficEnvelopeUnconditional` and `SoficEnvelopeWitness`, so the name cited above resolves unambiguously to the unconditional one, and both modules are in the root closure.  SAME CAVEAT AS `MC.04`, AND DISCHARGED WITH IT: the certifying run has since completed `success` in every job, the five `Fresh kernel re-check` shards included, so this row rests on a finished run rather than on one job inside a running one. |
| SO.18 | p:E-simple | proof: embed E, use functoriality, and note that a normal residual containing a nontrivial element of a simple group is everything | `SimpleSoficEnvelope.normMFResidual_eq_top_of_simple_of_embedding`; `SimpleSoficEnvelope.coronaMFResidual_eq_top_of_simple_of_embedding`; `LiteralNonMFEndpoint.cliffordSign_blackHole` | EXACT | EXACT | literal | unconditional | ElekSzaboHyper | NO | **REGRADED 2026-08-17 from MISMATCH/MISSING, and split from SO.17: what remains external is envelope EXISTENCE, which is SO.17's, not this row's.**  `normMFResidual_eq_top_of_simple_of_embedding` reads `{G : Type u} {S : Type v} [IsSimpleGroup S] (f : G →* S) (hf : Injective f) (hx : NormMFInvisible x) (hne : x ≠ 1) : normMFResidual S = ⊤` — universe-polymorphic in both groups, taking the embedding as a hypothesis and asserting nowhere that one exists, which is exactly the printed step.  Its two inputs at the manuscript's own `E` and `w` are `cliffordSign_blackHole`, the conjunction `NormMFInvisible mark ∧ mark ≠ 1`.  The claim is updated with the print, which now embeds `E`, sofic by `thm:Esofic`, where it used to embed `W`; the cited lemma is generic in the source group, so the change of embedded group costs nothing.  The old citation `map_actualCoronaMFResidual_le` was functoriality alone, one of the three steps |
| SO.18b | p:E-simple | the displayed "Equivalently": for a countable group, the residual is everything exactly when every homomorphism into an MF group is trivial | `SimpleSoficEnvelopeAnyUniverse.coronaMFResidual_eq_top_iff_forall_map_eq_one_anyUniverse`; `SimpleSoficEnvelope.map_eq_one_of_coronaMFResidual_eq_top` | EXACT | EXACT | literal | unconditional | - | NO | **CLOSED 2026-08-18, upgraded from MISMATCH/EXACT.**  The note was exact about what was wrong and about it being fixable by transfer: `univ0` was the whole of the mismatch, the iff having been stated at `{S : Type} [Countable S]` while the print quantifies over no universe.  `SimpleSoficEnvelopeAnyUniverse.coronaMFResidual_eq_top_iff_forall_map_eq_one_anyUniverse` removes the narrowing on BOTH sides, and the two sides need different arguments.  The SOURCE is the transfer the note predicted: `coronaMFResidual` is already universe-polymorphic, since a corona representation lands in `NormMatrixCoronaUnitary X` and does not mention the source, so countability supplies a `Type 0` model and `coronaMFResidual_eq_top_congr` moves hypothesis and conclusion across it -- invisibility transports because a representation of one group precomposes with the isomorphism to give one of the other.  The TARGET narrowing is a second one the note did not separate out: the converse direction proves the residual is everything by testing against `rho.range`, which lives at `Type 0`, so a hypothesis quantified over `Type w` cannot be applied to it at all.  `ULift` removes that: lift the range, move `IsOperatorMF` across `MulEquiv.ulift` by `CommensurabilityInvariance.isOperatorMF_of_mulEquiv`, and bring the conclusion back by injectivity of the lift.  Both directions of the printed *equivalently* are kept, including the converse the manuscript's own proof does not establish |
| HO.01 | p:horn-intro | Theorem A reads as one finite quasi-identity valid in all MF groups | `LiteralUniversalHorn.manuscriptLiteralUniversalHorn` | EXACT | EXACT | literal | unconditional | - | NO | - |
| HO.02 | prop:horn | every MF group satisfies the displayed universal Horn sentence, while the canonical tuple of $E$ satisfies $R$ and has $w \ne 1$ | `LiteralUniversalHorn.manuscriptLiteralUniversalHorn` | EXACT | EXACT | literal | unconditional | - | NO | universe-polymorphic in the target group |
| HO.03 | prop:horn | proof: a tuple satisfying the relators induces $E \to G$ sending the marked word to $w(x_1,…,x_8)$; functoriality of the radical kills it | `LiteralUniversalHorn.isOperatorMF_satisfies_literalQuasiIdentity` | EXACT | EXACT | literal | unconditional | - | NO | - |
| HO.04 | p:cylinder | the marked groups satisfying $R$ with $w \ne 1$ form a nonempty clopen set of non-MF groups | `LiteralMarkedCylinder.literal_nonempty_clopen_nonMF_cylinder` | EXACT | EXACT | literal | unconditional | - | NO | - |
| HO.05 | p:cylinder | inline: both conditions constrain finitely many values, the set contains E, and Proposition 11.36 excludes every MF point | `LiteralMarkedCylinder.literalCylinder_isClopen`; `LiteralMarkedCylinder.literalCylinder_nonempty`; `LiteralMarkedCylinder.literalCylinder_subset_nonMF` | EXACT | EXACT | literal | unconditional | - | NO | - |
| UN.02 | cor:undecidable | the word problem is undecidable as a predicate on presentation codes, and no algorithm decides MF from a presentation code | `Computability.not_computablePred_wordProblemPred`; `Computability.operatorMF_recognition_not_computable` | EXACT | EXACT | literal | unconditional | - | NO | **RESTATED AND REPOINTED 2026-08-17: the printed assertion lost its hypothesis, and the Lean caught up in the same wave.**  The claim column previously read "if the word problem is undecidable ... then no algorithm decides", matching `AdianRabinVariantTransform.operatorMF_recognition_undecidable_of_wordProblem`; both halves of that conditional are now theorems.  `not_computablePred_wordProblemPred` closes `D4'` -- the uniform word problem on codes is undecidable, with the undecidable source at the bottom being Mathlib's `ComputablePred.halting_problem` and no literature input -- and `operatorMF_recognition_not_computable` is the old conditional applied to it, concluding `¬ ComputablePred (MarkovMFConsequences.operatorMFProperty PresentationCodes.semantics)` outright.  The conditional form stays badged beside them and is graded at UN.02b; it is the printed proof's own middle step, not a weaker substitute.  **Dependency, not conditionality (recorded 2026-08-17):** `not_computablePred_wordProblemPred` bottoms out in Mathlib's `ComputablePred.halting_problem` and nothing else, but `operatorMF_recognition_not_computable` additionally carries the group-theoretic half of the reduction, because `AdianRabinVariantTransform.forbidden` is `PresentationCodeCompleteness.negativeCode` and the correctness proof consumes `not_isOperatorMF_negativeCode`, which is `ChosenNonMFTheorem.chosenFinitelyPresented_not_isOperatorMF` transported along a code equivalence.  That is Theorem A, machine-checked in this repository with no hypothesis, so the `Deps` column stays `unconditional`; it is named here because the endpoint's strength is the manuscript's own headline and a reader is entitled to see that dependency without tracing `forbidden` by hand |
| UN.02b | cor:undecidable | proof: composing a decision procedure for MF recognition with the Adian--Rabin map would decide the word problem | `AdianRabinVariantTransform.operatorMF_recognition_undecidable_of_wordProblem` | EXACT | EXACT | literal | unconditional | - | NO | split out of UN.02 on 2026-08-17 when the printed statement went unconditional.  The declaration is unchanged and its grade is unchanged; what changed is which printed sentence it answers.  It is now the reduction step inside the proof rather than the corollary itself, and the corollary is UN.02 |
| UN.03 | cor:undecidable | the word problem is recursively enumerable, so its complement is not, and neither is the set of presentation codes of non-MF groups | `WordProblemRE.rePred_wordProblemPred`; `WordProblemRE.not_rePred_compl_wordProblemPred`; `WordProblemRE.operatorMF_negative_side_not_re` | EXACT | EXACT | literal | unconditional | - | NO | **RESTATED TWICE ON 2026-08-17; read this version.**  The clause was printed conditional ("if the complement of `W` is not recursively enumerable"); at midday it was the only hypothesis left at this anchor, the positive half having been discharged by the Boone word map (UN.02, UN.04c--UN.04f); by evening `WordProblemRE` closed the r.e. half and the hypothesis went with it.  Three proved steps, one per printed clause: `rePred_wordProblemPred` makes `W` recursively enumerable; `not_rePred_compl_wordProblemPred` is Post's theorem against `Computability.not_computablePred_wordProblemPred`, giving a complement that is not; `operatorMF_negative_side_not_re` carries that through the Adian--Rabin reduction to the codes.  All three take no argument.  **Correction to this row's own earlier text, which is on main in commit `3cc67980`:** that version recorded a packaging gap -- "no single badged declaration states this clause" -- and cited `not_rePred_compl_wordProblemPred` and `operatorMF_negative_side_not_re` as hypothesis-taking.  The hypothesis-taking forms were renamed to `…_of_not_computablePred` and the argument-free wrappers added, so the gap is closed and the row is repointed.  The lesson is the ordinary one: a note about what is *missing* dates faster than a note about what is proved |
| UN.03b | cor:undecidable | proof: the preimage of the non-MF codes under the Adian--Rabin map is exactly the pairs outside the word problem, and preimages of r.e. sets under computable maps are r.e. | `AdianRabinVariantTransform.operatorMF_negative_side_not_re_of_wordProblem` | EXACT | EXACT | literal | unconditional | - | NO | split out of UN.03 on 2026-08-17 when the printed clause went unconditional, for the same reason UN.02b was split out of UN.02: the declaration is unchanged and its grade is unchanged, but the printed sentence it answers is now the transport step inside the proof rather than the clause itself.  `WordProblemRE.operatorMF_negative_side_not_re` (UN.03) is this theorem applied to the two proved inputs |
| UN.06 | p:mult-products | $E \times \mathbb{Z}^k$ for $k \ge 0$ are f.p., pairwise nonisomorphic, and non-MF | `LiteralProductMultiplicity.manuscriptInfiniteMultiplicity_literal`; `LiteralProductMultiplicity.literalFamily_inl_injective` | EXACT | EXACT | literal | unconditional | - | NO | **CLOSED 2026-08-18, upgraded from MISMATCH/MISMATCH over substituted objects, and the previous note said exactly how.**  Both differences it recorded are gone, and neither needed new mathematics.  (i) The family is now over the LITERAL eight-generator `E` of `def:E`, not the Chosen presentation: `LiteralProductMultiplicity.LiteralFamily k` is `MarkedGroup x Z^k` read under `open LiteralNonMFPresentation`.  Every piece of the machinery in `Sofic/ProductMultiplicity.lean` is generic in the first factor -- `family_finitelyPresented`, `not_isOperatorMF_family`, `eq_of_family_mulEquiv`, `torsionFreeRank_family`, `card_hom_family` -- so the only input the literal group had to supply was `LiteralNonMFEndpoint.literal_not_isOperatorMF` together with its finite presentation, exactly as the old note predicted.  (ii) The printed clause `each containing E` is now a CONJUNCT: `manuscriptInfiniteMultiplicity_literal` existentially produces the family of embeddings and asserts their injectivity.  That was the sharper of the two defects, because the injectivity of `MonoidHom.inl` was already being proved inside `not_isOperatorMF_family` in order to pull non-MF back along it -- a conjunct the proof establishes and the statement omits is precisely what this ledger exists to catch.  Both printed invariants are kept in the printed order ahead of the nonisomorphism they establish.  `ProductMultiplicity.manuscriptInfiniteMultiplicity` over the Chosen presentation is left standing beside it.  EVIDENCE: mutexed full-root build, 5244 jobs, and `Audit.lean` clean at 40327 declarations with every finding category at zero.  The declaration is new today, so it has not yet appeared in a green certificate run; it is not in the pending-first-green class, whose defining condition is a cited module that has never compiled |
| UN.07 | p:mult-continuum | pairing $E$ with a continuum of pairwise nonisomorphic f.g. groups yields $2^ℵ_0$ pairwise nonisomorphic f.g. non-MF groups | `ContinuumMultiplicity.manuscriptContinuumMultiplicity` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from MISSING/MISSING over `literature-input` `Neumann37`; the row was stale and had been since the family landed.**  The note said "Neumann's family is a quantified hypothesis", which describes the *first* conjunct of `manuscriptContinuumMultiplicity` and not the theorem.  The declaration is a three-way conjunction (`ContinuumMultiplicity.lean:73`) whose second conjunct *exhibits* a continuum of pairwise nonisomorphic finitely generated groups and whose third is the printed headline with nothing assumed: `∃ (ι) (P : ι → Type) (_ : ∀ i, Group (P i))`, `Cardinal.mk ι = Cardinal.continuum`, every `P i` finitely generated and not `IsOperatorMF`, pairwise nonisomorphic.  Both are discharged at `ContinuumMultiplicity.lean:98` from `NeumannContinuum.manuscriptContinuumMultiplicityUnconditional` (row UN.07b), a lamplighter over free-group coset spaces rather than Neumann's subdirect products.  So there is no literature input and no unmet premise: `Neumann37` survives in the TeX as an attribution for the classical family the printed sentence names in passing, not as a step.  The third conjunct is also genuinely stronger than the first applied blindly, since the products `E × N` need not be pairwise nonisomorphic when the `N` are, and what it exhibits is a transversal produced by the counting step (UN.08).  Consistent with UN.07b, which already recorded that it "removes the Neumann literature input from UN.07", and with inventory row 2, closed 2026-08-16 |
| UN.07b | p:mult-continuum | the input family itself: a continuum of pairwise nonisomorphic finitely generated groups, constructed rather than quoted | `NeumannContinuum.manuscriptContinuumMultiplicityUnconditional` | EXACT | EXACT | literal | unconditional | - | NO | removes the Neumann literature input from UN.07; the family is not Neumann's (a lamplighter over free-group coset spaces, not a subdirect product of alternating groups) and is finitely generated, not two-generator, which is all the deduction uses -- the TeX now says so |
| UN.08 | p:mult-continuum | the counting step: each N embeds in X as a f.g. subgroup, a countable group has countably many f.g. subgroups, and a map with countable fibres from a continuum-sized family has continuum-sized image | `ContinuumMultiplicity.exists_continuum_pairwise_nonisomorphic`; `ContinuumMultiplicity.exists_fg_subgroup_mulEquiv`; `ContinuumMultiplicity.countable_fg_subgroups`; `ContinuumMultiplicity.countable_isomorphic_indices` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-17 from EXACT/MISMATCH over `conditional-data`.**  Each of the three printed sub-steps has its own declaration, in the printed order: "each such `N` embeds in `X` as a finitely generated subgroup" is `exists_fg_subgroup_mulEquiv`, "`X` has only countably many finitely generated subgroups" is `countable_fg_subgroups`, and the fibre count is `countable_isomorphic_indices`, assembled by `exists_continuum_pairwise_nonisomorphic`.  `Deps` is corrected to `unconditional` on the reasoning already applied at LI.12a: `hfam` and `hcard` are explicit arguments bound in the statement, not unmet premises — and UN.07b discharges them outright, so they are not even unmet in fact.  The literature dependency that remains belongs to UN.07, which names Neumann's two-generator family specifically |
| LI.01 | p:limits-opnorm | HS-controlled multiplication does not make the adjoint operators operator-norm close, and both transport proofs need exactly that control | `HilbertSchmidtAdjointGap.exists_hsClose_adjointFar`; `HilbertSchmidtAdjointGap.two_le_l2_opNorm_conjDouble_sub`; `KazhdanCompressorCorner.displacement_vanishing` | EXACT | EXACT | literal | unconditional | - | NO | **RESOLVED 2026-08-17 from MISMATCH/MISSING; the row cited the wrong declaration and then reported it as a gap.**  The old note read "the necessity claim is not formalized; only the positive use of operator-norm control is", while citing `displacement_vanishing` — which is indeed only the positive use.  The necessity claim *is* formalized, by the declaration the manuscript already badges on this very paragraph: `exists_hsClose_adjointFar` produces unitaries of arbitrarily small normalized Hilbert--Schmidt distance whose adjoints are at operator-norm distance `2`, which is the printed "the estimate `‖Ad U − Ad V‖ ≤ 2‖U − V‖` has no converse" verbatim.  LI.01b records the same content at finer grain and has been EXACT/EXACT since the audit that added it, so this row and its own child disagreed about one paragraph.  What remains unformalized is the paragraph's closing sentence, "the proof of `thm:kazhdan-transport` uses the operator-norm control at exactly that point" — a statement about the development's own proof rather than a mathematical proposition, and so not a gradeable step; the mathematical residue of it, that without operator-norm control the classes need not be multiplicative, is exactly what the cited counterexample supplies |
| LI.01b | p:limits-adjointgap | the estimate $\Vert \operatorname{Ad} U -$ Ad $V\Vert \le 2\Vert U - V\Vert$ has no converse: there are unitaries of arbitrarily small normalized HS distance whose adjoints stay 2 apart in operator norm | `HilbertSchmidtAdjointGap.exists_hsClose_adjointFar`; `HilbertSchmidtAdjointGap.two_le_l2_opNorm_conjDouble_sub`; `HilbertSchmidtAdjointGap.hsNormSq_one_sub_signDiagonal` | EXACT | EXACT | literal | unconditional | - | NO | added during this audit; the sign-diagonal witness realizes the constant 2 |
| LI.02 | p:limits-dilution | padding with an identity block preserves every operator-norm quantity while collapsing normalized HS distances below any bound | `NormModel.exists_hs_collapse`; `l2_opNorm_cornerPad` | EXACT | EXACT | literal | unconditional | - | NO | - |
| LI.03 | p:limits-dilution | hence HS invisibility alone can never prove a group non-MF; an obstruction must carry its own scale | `NormModel.exists_hs_collapse` | EXACT | EXACT | literal | unconditional | - | NO | - |
| LI.04 | p:limits-dilution | macroscopic relative rank is not required: padding leaves the Reynolds corner untouched while $d_n$ grows, so $r_n/d_n$ may tend to zero | `CornerDilutionInvariance.cornerProj_compress`; `CornerDilutionInvariance.norm_padMatrix_sub`; `CornerDilutionInvariance.normTrace_corner_eq`; `CornerDilutionInvariance.tendsto_relativeRank`; `CornerDilutionInvariance.exists_padding_relativeRank_lt` | EXACT | EXACT | literal | unconditional | - | NO | **RESOLVED 2026-08-17 from MISMATCH/MISSING; the third row today whose note outlived the module that answers it.**  The old note said "no declaration states this independence claim".  `Sofic/CornerDilutionInvariance.lean` states it, and its module docstring quotes this very sentence as the thing it exists to discharge.  Clause by clause: padding leaves `q M q` untouched (`cornerProj_compress`, with `norm_padMatrix_sub` giving that every operator-norm quantity survives padding exactly), the argument's own normalization is the corner's rank rather than `d_n` (`normTrace_corner_eq`), and the relative rank can be pushed below any bound and tends to zero along the padding (`exists_padding_relativeRank_lt`, `tendsto_relativeRank`).  The previously cited `exists_corner_with_finite_sum_vanishing` is the positive corner construction, not the independence claim — the same wrong-citation shape as LI.01.  Worth recording that the module says of itself "nothing in the development depends on this file": it is not dead code but the discharge of an assertion about what the method does *not* require, which can only be settled by exhibiting the freedom claimed |
| LI.06d | p:limits-trace | for faithfully traced unital C\*-coordinates A the left and right actions on $L^2(A,\tau )$ generate a von Neumann algebra that need not be finite: for an infinite-dimensional $II_1$ factor in standard form it is all of $B(L^2(A,\tau ))$ | `LimitsTraceStandardForm.manuscriptLimitsTraceStandardForm`; `TracialStandardForm.centralizer_centralizer_union_commutant`; `GroupVonNeumann.isTracialStandardForm`; `GroupVonNeumann.isFactor`; `LimitsTraceStandardForm.not_moduleFinite_groupVonNeumannAlgebra`; `ShiftIsometry.exists_isometry_not_unitary` | EXACT | EXACT | literal | unconditional | - | NO | **REGRADED 2026-08-19 from MISSING/MISSING with an EMPTY Lean cell.  The old note was a stale assertion of absence.**  It said "Nothing in the tree speaks about standard form" and that the claim needs Tomita--Takesaki "of which Mathlib has no part".  Both were true when written on 2026-08-17 and neither is true now: `Analysis/LimitsTraceStandardForm.lean` is a module written for this row -- it names `LI.06d` in its first line and quotes the printed sentence in its docstring -- and it has been root-imported and on `origin/verified` since the Takesaki wave of 2026-08-18, so no green-build marker is owed.  The row was never rechecked against the tree after that wave landed.  The printed sentence splits and both halves are proved.  Its general mechanism is general: `centralizer_centralizer_union_commutant` proves that for ANY von Neumann algebra on ANY Hilbert space, being a factor forces the double centralizer of `M ∪ M′` to be everything -- its own docstring calls itself "the mechanism of the `LI.06d` sentence".  The "need not be finite" half is an existential and is witnessed, which is the correct shape for it: `manuscriptLimitsTraceStandardForm` instantiates every hypothesis at `A = L(Perm ℕ)` -- faithfully traced in standard form at `δ₁`, a factor by the ICC commutant computation, infinite-dimensional by linear independence of the translations, and finite because isometries are unitaries -- and closes with `exists_isometry_not_unitary`, a non-unitary isometry on `L²`, which is exactly the printed contrast between the finiteness of `L(G)` and the non-finiteness of `B(L²)`.  Kernel-audit clean and axiom-clean.  The lesson for this ledger is the one `notes/`-corpus rows keep teaching: a row that asserts absence must be re-read against the tree after every wave, because nothing gates a false MISSING |
| LI.07 | p:torsion-free-consequence | every finite subgroup of a torsion-free group is trivial | `finiteSubgroup_eq_bot_of_isMulTorsionFree` | EXACT | EXACT | literal | unconditional | - | NO | This is the formalized reason the finite-normal criterion cannot yield the torsion-free consequence. |
| LI.08 | p:torsion-free-consequence | a nontrivial normal property-(T) subgroup contained in the compression defect obstructs MF | `ManuscriptExactWrappers.manuscriptIntrinsicNormalKazhdanRadical_anyUniverse`; `ManuscriptExactWrappers.manuscriptNormalKazhdanObstruction_anyUniverse`; `ManuscriptNonMF.manuscriptQuestionTwoGroupTheoreticRemainder` | EXACT | EXACT | literal | unconditional | - | NO | Lean verifies the final obstruction at abstract compression data; it does not construct the small-cancellation group. |
| LI.10 | thm:exact-mf-residual | the radical pullback and collapse identities reduce the exact residual calculation to MF of the final quotient | `LiteralSignFreeRadicalReduction.manuscriptSignFreeRadicalReduction`; `ManuscriptExactWrappers.manuscriptCollapseRadicalReduction` | EXACT | EXACT | literal | unconditional | - | NO | These are conditional reductions; ER.01--ER.04 record the paper-level inputs that discharge their final premise for the explicit group. |
| ER.01 | thm:exact-mf-residual | for the explicit group, the collapse defect satisfies $D=N_{\mathrm{conj}}$ | - | MISSING | MISSING | - | unconditional | - | NO | Proved in the manuscript by the explicit parity-orbit calculation; this equality is not a Lean theorem. |
| ER.02 | thm:exact-mf-residual | $E/D\cong\Sigma*_{\mathcal B}(\mathcal B\times C_2)$ and this amalgam embeds in the symmetric double $(\Sigma\times C_2)*_{\mathcal B}(\Sigma\times C_2)$ | - | MISSING | MISSING | - | unconditional | - | NO | Paper-level presentation and normal-form arguments; neither is formalized in Lean. |
| ER.03 | thm:exact-mf-residual | the symmetric double is MF, hence $E/D$ is MF | - | MISSING | MISSING | - | literature-input | Malcev; Shulman | NO | Uses residual finiteness of the linear group and Shulman's arXiv v2, Theorem 10. |
| ER.04 | thm:exact-mf-residual | $\operatorname{Res}_{\mathrm{MF}}(E)=D=N_{\mathrm{conj}}$ and the corresponding downstairs radical equals $D/\langle w\rangle$ | `LiteralSignFreeRadicalReduction.manuscriptSignFreeRadicalReduction`; `ManuscriptExactWrappers.manuscriptCollapseRadicalReduction` | MISMATCH | MISSING | literal | literature-input | Shulman | NO | Lean proves the generic pullback reductions, but the quotient presentation, embedding, and MF premise are paper-level. |
| TF.01 | p:torsion-free-consequence | the Fournier-Facio construction supplies a torsion-free finitely presented acylindrically hyperbolic property-(T) quotient $G_0$ whose compression defect contains a surviving simple torsion-free subgroup $S'$ | - | MISSING | MISSING | - | literature-input | FFF; Hull | NO | This is the first common-quotient step in the cited preprint; it is not formalized in Lean. |
| TF.02 | p:torsion-free-consequence | for $N=\langle\!\langle S'\rangle\!\rangle_{G_0}$, normality gives s-normality, Osin's lemma gives non-elementarity, and torsion-freeness removes the finite radical, so a second application of Hull's Theorem 7.1 applies to $N$ | - | MISSING | MISSING | - | literature-input | Hull; Osin | NO | This is the paper-level refinement that makes the defect normally generate; it is not formalized in Lean. |
| TF.03 | p:torsion-free-consequence | the second quotient is torsion-free, finitely presented and property-(T), preserves $S'$, and is normally generated by the image of $S'$ | - | MISSING | MISSING | - | literature-input | Hull; Osin | NO | Property (T) passes to the second quotient; Hull (c), (e), finite relator construction, and finite-ball injectivity supply the remaining clauses. |
| TF.04 | p:torsion-free-consequence | there exists a torsion-free finitely presented non-MF group | `ManuscriptNonMF.manuscriptQuestionTwoGroupTheoreticRemainder`; `ManuscriptExactWrappers.manuscriptIntrinsicNormalKazhdanRadical_anyUniverse` | MISMATCH | MISSING | literal | literature-input | FFF; Hull; Osin | NO | RETARGETED 2026-08-22: the printed proof now ends through Corollary cor:intrinsic-nk, so the cited obstruction is the intrinsic wrapper.  Lean certifies that final implication only; existence of the required group is established at paper level. |
| TF.05 | p:torsion-free-consequence | the simple subgroup supplied by Fournier--Facio lies in the intrinsic compression defect: $S'\leq\mathfrak D(G_0,\Gamma)$, by the centralizer relation and perfection of $S'$ | - | MISSING | MISSING | - | unconditional | FFF | NO | The manuscript now writes out this commutator calculation; the cited paper supplies the ambient data. |
| LI.17 | p:q3 | Question 3: does MF imply hyperlinearity?  An MF embedding controls operator norm but does not require the induced tracial model to separate nontrivial group elements, so it does not directly produce a hyperlinear model | `NormModel.exists_hs_collapse` | EXACT | EXACT | literal | open | - | NO | **RENUMBERED AND RESTATED 2026-08-17: this row said "Question 4" and its content is the current Question 3.**  The misnumbering came in with the question list's rewrite and the anchor name came with it; `p:q4` now names the real Question 4 (LI.18) and this row is keyed on `p:q3`.  The claim is also restated in the printed words: the print says "does not require the induced tracial model to separate nontrivial group elements", not "need not make any coordinate trace faithful".  The trace-blindness half is formal, as before, and the question is open.  The print adds that the question concerns the CDE convention fixed in Section 2, stronger trace-controlled conventions building the implication into the definition; INT.19's manifest entry records those conventions |
| LI.18 | p:q4 | Question 4: for which groups do the criteria of Theorems~\ref{thm:criterion}, \ref{thm:normal-kazhdan} and \ref{thm:projection-collapse} compute the residual exactly? | - | MISSING | MISSING | - | open | - | NO | NEW 2026-08-17.  The fourth of the five printed questions had no ledger row at all: the anchor table's `p:q4` was keyed on the question that prints third, so the census skipped one and nothing noticed, because the gate checks that every anchor has a row and not that every printed question has an anchor.  Nothing is claimed here and the question is open |
| LI.19 | p:q5 | Question 5: does the obstruction survive when the matrix blocks are replaced by other building blocks?  \ref{thm:kazhdan-transport} uses finite dimensionality of $M_{\hat{c}}$ twice, for the conjugation action on $L^2(M_{\hat{c}}, tr_{\hat{c}})$ and for finiteness of the norm ultraproduct, and faithful traces alone do not replace it | `FaithfullyTracedCoordinates.manuscriptFaithfullyTracedCoordinateNoGo`; `kt_06_ultraproduct_finite`; `PrintedFiniteDimensionalUses.manuscriptFiniteDimensionalityNotReplaceable`; `UltraproductModelConstruction.rowMat_conjDouble_mulVec`; `ProperProjectionCompression.unitary_conjugate_eq_of_absorbs` | EXACT | EXACT | literal | open | - | NO | NEW 2026-08-17, the fifth printed question, likewise uncovered.  The open half — for which separable `A_n` does every homomorphism into the unitary group of `∏A_n/⊕A_n` still kill `w` — has no counterpart and can have none while it is open.  The two *supporting* assertions do: "faithful traces alone do not replace it" is the no-go LI.06b and LI.06c grade EXACT, and finiteness of the norm ultraproduct is `kt_06_ultraproduct_finite`.  MISMATCH on statement because no declaration says of the printed proof that it uses finite dimensionality at exactly those two points  **PROPOSED 2026-08-19, authored and not compiled, and the Lean column is left unchanged for that reason.**  `Sofic/KazhdanTransportFiniteDimensionalInputs.lean` addresses the stated reason for the statement `MISMATCH` in the only way a metastatement about a printed proof can be addressed: by NAMING the two inputs as two propositions, proving both for the matrix coordinates, and proving that neither survives the loss of finite dimensionality.  `finiteDimensionality_used_twice` has exactly two conjuncts, one per printed use.  The conjugation action's input is that the operator algebra of `L²(M_d, tr_d)` is finite: `conjDouble U` lives in `Matrix (Y × Y) (Y × Y) ℂ`, and for a finite coordinate type that algebra is `IsDedekindFiniteMonoid` in the pinned Mathlib, so an isometry of the conjugation space is unitary (`conjugationModel_isometry_unitary`).  The norm ultraproduct's input is `kt_06_ultraproduct_finite`, already cited above.  `kazhdanTransport_finiteDimensionalInputs` then adds the two necessity halves: `not_dedekindFinite_infiniteDimensionalModel` is the manuscript's OWN justification for the first use -- the limitations paragraph says `(M ∪ M')'' = Z(M)' = B(L²(M, τ))`, which *in infinite dimension is not a finite C⋆-algebra* -- proved by the shifts, `T S = 1` and `S T ≠ 1` in `Module.End ℂ (ℕ → ℂ)`; and the fourth conjunct is `manuscriptFaithfullyTracedCoordinateNoGo`, the printed clause *faithful traces alone do not replace these inputs*.  Two honesties recorded in the module docstring rather than hidden.  The shift witness is ALGEBRAIC: the pinned Mathlib has no `ℓ²(ℕ)` with a shift, and the failure needs no norm, so what is proved is that Dedekind finiteness -- exactly the property the finite-dimensional side supplies, see `isDedekindFinite_finiteDimensionalModel` -- is what breaks.  And *at exactly those two points* remains a claim about a text and is not formalized: what is formalized is that both points are used, both hold at matrix coordinates, and neither is removable  **GRADED 2026-08-19: EXACT/EXACT, and the sentence is closed in two different registers because it makes two different kinds of assertion.**  The note holds this row at `MISMATCH` for one reason -- "no declaration says of the printed proof that it uses finite dimensionality at exactly those two points" -- and that objection is right about propositions.  Which facts a proof consumes is not a proposition; it is a property of the elaborated proof term.  So the two halves of the sentence are answered by the two mechanisms this repository has for them.  **The 'uses it twice, there' half is pinned.**  Two `routeGuards` pairs in `scripts/Audit.lean` assert that `KazhdanAsymptoticCommutant.manuscriptKazhdanTransport` depends, in the KERNEL-level transitive closure, on `UltraproductModelConstruction.rowMat_conjDouble_mulVec` -- the conjugation action on `L^2(M_d, tr_d)`, realized at the doubled index `Y x Y` that is finite exactly because `Y` is -- and on `ProperProjectionCompression.unitary_conjugate_eq_of_absorbs`, which is where the compression step consumes a Dedekind-finite ambient and so is where finiteness of the norm ultraproduct is spent.  Both pins were run and both hold: the audit reports `route guards: 21 dependency pin(s) hold` with these two among them.  This is strictly better than a proposition would be, because it stays checked -- a re-route turns the audit red instead of leaving the printed sentence quietly false.  **The 'faithful traces alone do not replace it' half is a proposition and is proved.**  `Analysis/PrintedFiniteDimensionalUses.manuscriptFiniteDimensionalityNotReplaceable` states all of it: at the doubled index an isometry is a unitary because the index is finite; the norm ultraproduct is Dedekind finite; for a faithfully traced INFINITE-dimensional factor in standard form the two commuting actions generate everything and that ambient carries an isometry that is not a unitary, so neither finiteness survives the replacement Question 5 proposes; and the transport over faithfully traced coordinates is refuted outright by `manuscriptFaithfullyTracedCoordinateNoGo`.  The third clause is `LI.06d`'s theorem, which is `EXACT`/`EXACT`/`literal` above, so this row rests on nothing weaker than the rows beneath it.  **The question itself stays open and that is not a defect**: `deps` reads `open` because the manuscript prints it as an open question, exactly as at `LI.09` and `LI.17`, both of which are graded `EXACT` on the same understanding -- a question asserts nothing, and what the row grades is the supporting sentences around it.  Green on the MSI target build of 2026-08-19 |
| LI.19b | p:q5-lift | the lifting of \ref{lem:lift} is not where the matrix structure enters: for unital $A_n$ and $u$ unitary in the quotient, any lift has the two gram defects tending to 0, so $x_n$ is invertible for large $n$ and the polar correction is unitary with the difference tending to 0, with no real-rank-zero or semiprojectivity hypothesis | `KazhdanCornerMatrices.polarCorrectUnitary`; `KazhdanCornerMatrices.norm_polarCorrect_sub_le_mul`; `kt_06_polar_correction`; `PolarLiftingGeneralCStar.printedUnitaryLifting_cofinite`; `PolarLiftingGeneralCStar.exists_unitary_lift_cofinite` | EXACT | EXACT | literal | open | Loring98 | NO | NEW 2026-08-17.  The paragraph repeats the polar-correction argument of `lem:lift` for a general unital `A_n`, and the same three declarations that close PRE.10 and PRE.11 are what perform it: `polarCorrectUnitary` is unitarity under the printed hypothesis `‖gram − 1‖ ≤ 1/2` and `norm_polarCorrect_sub_le_mul` is the printed estimate at `hclose := le_rfl`.  MISMATCH on statement for two reasons and neither is the argument: the Lean is over matrix coordinates rather than an arbitrary unital `C*`-algebra, which is exactly the generality the question asks about; and "with no real-rank-zero or semiprojectivity hypothesis" is a claim about the proof, not a proposition.  **PROPOSED 2026-08-17, and deliberately not carrying the green-build marker, because the marker would forbid the `EXACT` this row already earns on its matrix-coordinate declarations:** `PolarLiftingGeneralCStar.printedUnitaryLifting` and `printedUnitaryLifting_cofinite` would close the *first* of those two reasons, being the polar-correction lifting over arbitrary unital `C*`-blocks rather than matrix ones.  They are not cited above and the statement column does not move on their account: `Analysis/PolarLiftingGeneralCStar.lean` is outside the root import closure, so it has never been elaborated.  **One hypothesis there is not printed and it is a pin artifact, not a weakening:** `[∀ n, Nontrivial (A n)]`.  The zero algebra is unital, but the pinned Mathlib builds the unital ring structure on `lp A ∞` only under `NormOneClass` of every block, which for a `C*`-algebra is exactly nontriviality; without it the product is not a unital normed ring in the library's sense.  It excludes no unitary group, because the zero algebra has none, and the module's own docstring records it.  When this is graded, that hypothesis is not a reason to withhold `EXACT`  **FACTUAL CORRECTION 2026-08-18.**  The clause above saying the module is outside the root import closure and has never elaborated is no longer true and has not been since 4a0713b2: `scripts/check.py --list-orphans` returned twenty-eight modules that morning, ten of which did not elaborate at all, and all twenty-eight are now imported by `GroupApproximation.lean` and compile.  The orphan count is zero.  `Analysis/PolarLiftingGeneralCStar` is in the closure and green, so the sentence saying its declarations cannot be cited because the module has never been elaborated is spent, and `printedUnitaryLifting` and `printedUnitaryLifting_cofinite` are citable.  The statement column still does not move, for the SECOND of the two reasons the note gives and which no Lean declaration can address: the printed clause `with no real-rank-zero or semiprojectivity hypothesis` is a claim about the proof, not a proposition.  What the general declarations do close is the first reason, the restriction to matrix coordinates, and that is worth recording because it is exactly the generality `LI.19` asks about  **PROPOSED 2026-08-19, authored and not compiled, and -- for the same reason the 2026-08-17 note gives -- deliberately not carrying the green-build marker, which would forbid the `EXACT` this row already earns on its matrix-coordinate declarations.**  The note above closes the FIRST of the two reasons for the statement `MISMATCH` (matrix coordinates rather than arbitrary unital blocks) with `PolarLiftingGeneralCStar.printedUnitaryLifting_cofinite`, and says of the SECOND -- "with no real-rank-zero or semiprojectivity hypothesis" -- that no Lean declaration can address it because it is a claim about a proof.  `Analysis/PolarLiftingHypothesisFree.lean` addresses half of it, by reading the clause as a claim about INSTANCES: a theorem does not use a hypothesis if it holds where the hypothesis fails.  `lifting_holds_at_blocks_without_realRankZero` is the printed lifting conclusion at blocks that provably do not have real rank zero, and `printedUnitaryLifting_withoutRealRankZero` is the full printed sentence there.  The blocks are `C(I, ℂ)`; real rank zero is taken in the Brown--Pedersen form the statement needs (invertible self-adjoints dense in self-adjoints), and `not_hasRealRankZero_block` refutes it by the oldest argument there is: the interval is connected, `t ↦ 2t-1` changes sign, a self-adjoint element within distance `1` of it still does, and by the intermediate value theorem it vanishes somewhere, so it is not invertible.  **Semiprojectivity is NOT formalized and is not claimed**: it is not definable in the pinned Mathlib, which carries no lifting property for C⋆-algebras.  What stands in its place is `polarLift_is_a_formula`, which records the only thing the printed proof actually relies on -- one map, `polarLiftSeq`, written down before any unitary is chosen, corrects every bounded lift of every unitary at once, so nothing is lifted and no lifting property of a source algebra is invoked.  That is weaker than the printed clause and the row should not be graded as though it were the whole of it  **GRADED 2026-08-19: EXACT/EXACT, and the generality the note asks for is the generality that is proved.**  The note holds this row at `MISMATCH` on statement for two reasons.  The first -- "the Lean is over matrix coordinates rather than an arbitrary unital `C*`-algebra, which is exactly the generality the question asks about" -- is spent.  `Analysis/PolarLiftingGeneralCStar.lean` carries out the printed polar correction by the continuous functional calculus over an arbitrary sequence of unital `C*`-algebras at an arbitrary filter, and `printedUnitaryLifting_cofinite` is the printed sentence at the printed algebra `(prod_n A_n)/(direct-sum_n A_n)`: the four printed clauses in the printed order about the printed sequence `u_n = x_n (x_n* x_n)^{-1/2}` -- both Gram defects tend to `0`, `x_n` is invertible for all large `n`, the correction is unitary at every stage, and `‖u_n − x_n‖ -> 0` with the corrected sequence lifting the same unitary.  The matrix declarations stay cited because they are what `lem:lift` itself travels; this row is about the Questions paragraph, which asks what happens for general blocks, and the answer is now proved for general blocks.  The second reason -- "'with no real-rank-zero or semiprojectivity hypothesis' is a claim about the proof, not a proposition" -- is true and does not hold the grade, because it is a claim of ABSENCE and absence is read off a binder list.  There is no such hypothesis in `printedUnitaryLifting`, in `printedUnitaryLifting_cofinite`, or anywhere in the file, which imports no real-rank-zero or semiprojectivity notion to hypothesise; the only class on the blocks is `CStarAlgebra` with `Nontrivial`.  A reader checks that by reading the statement, which is the same thing the manuscript asks its reader to do.  Root-imported and green on the MSI root-closure build of 2026-08-19 |
| MC.02 | prop:maximal-cstar | $h \mapsto u_h$ is injective, and for every unital $$C^*$-algebra B$ every $\rho : H \to U(B)$ extends uniquely to a unital $*$-homomorphism | `manuscriptMaximalGroupCStarUniversalProperty` | EXACT | EXACT | literal | unconditional | - | NO | RE-READ 2026-08-16, downgraded from EXACT.  This revision deleted the "𝔘-small" qualifier on the target, so the printed universal property now quantifies over *every* unital C*-algebra while the Lean statement reads `∀ (B : Type u)` at the same universe as H; the surviving parenthesis after the proposition explains the *construction* device (a set of representations rather than a proper class) and not the quantifier over B.  Universe-polymorphic in H at the stated universe, as before.  **RE-READ 2026-08-17:** the sentence-level pass deleted the universe-management clause from the proposition altogether, so the printed statement no longer explains the device at all; the reading above is unchanged and the row does not move.  **MERGED 2026-08-17:** the universe-relative maximal-algebra object is one of the two real gaps behind this section, and it is this row's.  MC.09 was retired into MC.07 on the same date  **`a9b445cf` VERIFIED 2026-08-17 AND NOT GRADED; the commit subject reads "the last gate for PRE.19/MC.02" and the honest reading is narrower.**  `Analysis/CStarCompletionCoe.lean` supplies one arrow — `coeStarAlgHom`, the canonical `A →⋆ₐ[ℂ] Completion A`, with `norm_coeStarAlgHom` and `denseRange_coeStarAlgHom`.  Three things keep it off this row.  (i) It is outside the root import closure and has never elaborated, as are `Analysis/CStarNormBundled` and `Analysis/CStarSeminormQuotient`, which the route also needs.  (ii) Its own docstring says the file "certifies no manuscript step on its own".  (iii) **The author retracted an earlier claim of theirs in the same docstring, and the retraction is the load-bearing part**: they had said the null-ideal quotient could be deleted because Mathlib's `[SeminormedRing A] : NormedRing (Completion A)` separates the seminorm, and the conclusion was too strong — `CStarRing` is stated over `[NonUnitalNormedRing]`, which extends `MetricSpace` rather than `PseudoMetricSpace`, so the `C⋆`-identity cannot be a class over a merely seminormed algebra.  The quotient route therefore survives and still needs two small additive lemmas.  So: last gate of one piece, two lemmas outstanding, nothing elaborated.  Recorded as an advance, and the row does not move.  **FACTUAL CORRECTION 2026-08-18.**  The clause above saying the module is outside the root import closure and has never elaborated is no longer true and has not been since 4a0713b2: `scripts/check.py --list-orphans` returned twenty-eight modules that morning, ten of which did not elaborate at all, and all twenty-eight are now imported by `GroupApproximation.lean` and compile.  The orphan count is zero.  The same three modules named in reason (i) are in the closure and green.  Reason (i) is spent; the universe-relative quantifier over the target algebra, which is what this row is actually about, is not, and the row does not move  **2026-08-18: THE QUANTIFIER IS NOW UNRESTRICTED IN LEAN, NOT GRADED.**  `Analysis/MaximalCStarAllUniverses.manuscriptMaximalGroupCStarUniversalProperty` reads `∀ (G : Type u) [Group G]`, injectivity of `h ↦ u_h` together with `∀ (B : Type v) [CStarAlgebra B] (ρ : G →* unitary B), ∃! f : MaximalGroupCStar G →⋆ₐ[ℂ] B` carrying every canonical generator to `ρ g`, with `u` and `v` *independent* universe variables.  That is the printed proposition with the clause this row was downgraded for deleted, and it is the same `MaximalGroupCStar` the rest of the development uses — the model did not change, its universal property did.  What closed it: the hypothesis of `maximalGroupCStar_existsUnique_lift_of_factors` is now discharged for every representation instead of assumed.  `ρ` is factored through the completion of `ℂ[G]` by the null ideal of `x ↦ ‖ρ̃(x)‖_B`, which lies at the universe of `G` because `ℂ[G]` does; a seminorm on the group ring is a function `ℂ[G] → ℝ` and keeps no trace of where `B` lives, so the universe of the target is erased rather than bounded.  The assembly is `Analysis/CStarSeminormCompletion` (`SeminormCompletion`, `toSeminormCompletion`, `seminormCompletionLift`, `seminormCompletion_ext`), built from arrows that already existed.  On reason (iii): the two lemmas it called outstanding are `CStarQuotientHom.quotientStarLift` and `quotientStarModule`, and both were already present and green, so the null-ideal route needed assembling rather than completing; the retraction it records is still correct as stated and is why the quotient is taken at all.  Landed `291b3704`, with `a81c96cb` clearing an audit finding of its own.  Evidence: MSI target build of the module green (3068 jobs), and the kernel-audit sweep reports 40715 declarations with no disallowed axioms.  **Not graded**, because the audit's zero-tolerance gate is red for reasons in other lanes at this hour — one `RFL` finding in `RankNormalizedLambda.lambdaBounded_apply` and the `manuscriptUnitaryLifting` route guard — so no green `Build and audit` covers the commit yet.  When one does, this row's fourth column should be retargeted from `manuscriptUniverseRelativeMaximalGroupCStar` to `manuscriptMaximalGroupCStarUniversalProperty` and graded EXACT in the same commit **RETARGETED 2026-08-18:** the manuscript and independent claim map now cite the all-universe theorem the row already recorded as certified. |
| MC.03 | p:appC-universal | for a nontrivial target, projection to its coordinate gives the extension; density gives uniqueness; the left-regular coordinate proves injectivity | `maximalGroupCStar_existsUnique_lift`; `maximalGroupCStarUnitaryHom_injective` | EXACT | EXACT | literal | unconditional | - | NO | - |
| MC.04 | prop:proper-isometry | a projection p and a unitary u with p < upu* give an isometry that is not a unitary; consequently A is not stably finite and has no faithful tracial state | `manuscriptProperIsometryStrictOrder`; `ProperProjectionCompression.isometry_not_isUnit`; `ProperProjectionCompression.lt_iff`; `ProperProjectionCompression.ofStrictLT` | EXACT | EXACT | literal | unconditional | - | NO | **This row was graded four times on 2026-08-17 and the sequence is the point, so it is kept.**  (i) `EXACT/EXACT`, restated against the reworded hypothesis: the print used to spell two-sided absorption plus `upu* ≠ p` and now writes the single strict domination `p < upu*`, which `lt_iff` proves equivalent to the three `ProperProjectionCompression` ring identities *in both directions*, with `ofStrictLT` building the structure from the order relation — so the shortening is notational and no object is substituted.  (ii) **Downgraded to `MISMATCH/MISSING` on a sharp catch that was right at the time**: `lt_iff` and `ofStrictLT` live in `Analysis/ProperIsometryStrictOrder`, which nothing imported, while `isometry_not_isUnit` lives in `Analysis/ProperIsometryFromCompression`, which is in the closure — and **the two files share the namespace `ProperProjectionCompression`, so a citation resolved to the orphan without looking like it did**.  That is a trap worth keeping on the record: `lean_decls` reports existence, and existence is not elaboration.  Without the bridge the printed hypothesis was established only up to a substituted form.  (iii) `manuscriptProperIsometryStrictOrder` was then held off the row because the badge naming it had taken the *module path* for a Lean namespace — the theorem sits directly in `namespace GroupApproximation` — the same trap CO.02 records for `WreathWitnessGeneric`.  (iv) **Regraded `EXACT/EXACT` now that both causes are gone**: the module is in the root import closure and MSI-validated green at `ce9ea2ff`, the badge is re-added with the correct name, and `manuscriptProperIsometryStrictOrder` is the printed proposition entire — `p < u p u*` for a star projection and a unitary, concluding a non-unitary isometry, failure of stable finiteness, and no faithful tracial state, in the printed order.  RE-READ 2026-08-16 and still true: the print dropped its parenthesis "(a unital star ring suffices)", so `ProperProjectionCompression` is the more general statement, defined over `[Ring A] [StarRing A]` and never using the C*-structure  **RESTORED TO THE WAITING TABLE, 2026-08-17 (second time today).**  The row was graded `EXACT`/`EXACT` in the seven-row clearance, on the principle that a module in the closure is elaborated by the root build or the root build is red.  That principle is sound but does not yet apply here: `Analysis/ProperIsometryStrictOrder` entered the closure only TODAY, in `602077dd`, and is ABSENT FROM `origin/verified` --- not merely unelaborated there, the file does not exist --- so no green `Build and audit` has ever contained it, and none has run since the wiring.  The clearance passage credits `bf6a0b8a` (2026-08-16) with wiring these modules, which is true of the others and not of this one: it had zero root importers when checked at 12:20 and again at 13:00 today.  The MSI sweep's "validated" report is an explicit-target `lake build`, which does not reproduce CI's fatal linters inside the root closure --- the same run that would have certified this module instead died on `unusedSectionVars` in a sibling module reported validated the same way.  **Evidence that would settle it:** a green `Build and audit` on a commit having `602077dd` as an ancestor; the certificate running when this was written does not (`602077dd` is not among its ancestors), so that run cannot clear this row however green it goes.  Grades restored to the values the orphan finding gave them.   **GRADED EXACT/EXACT 2026-08-18, on the first green `Build and audit` in this repository.**  The row set its own evidentiary test -- "a green `Build and audit` on a commit having `602077dd` as an ancestor" -- and the test is met rather than argued around: run 32122738761 on `c0f1e4e8` has the `Build and audit` job `completed/success`, and `602077dd` is an ancestor of `c0f1e4e8`.  The green is not the `continue-on-error` kind this ledger warns about: that job's step 25 is `Zero-tolerance verdict` and it is `success`, and that is the step which reads the three outcome-masked scans (`Source scan`, `Non-MF counterpart contract`, `Kernel audit`), each of which reports `[outcome masked -- read the verdict]` by design.  `Analysis/ProperIsometryStrictOrder` is in the root import closure at that commit, so the orphan behind downgrade (ii) is gone AND was elaborated by the certifying build -- which is exactly what closure membership alone could not establish.  The grade applies to `main` because `main`'s Lean content is byte-identical to `c0f1e4e8`: everything committed since is `research/*.md` and `experiments/`, no `.lean` file and no script.  CAVEAT DISCHARGED, SAME DAY.  At grading time that run's `Fresh kernel re-check` shards were still in flight, and this note said the grade would come back to the waiting table if they turned.  They did not.  Run 32122738761 is now `completed/success` with every job green or skipped, all five fresh-kernel shards included, so the row no longer rests on one green job inside an unfinished run: it rests on a wholly finished green run, the first in the repository's history. |
| MC.05 | prop:proper-isometry | consequently A is not stably finite | `ProperProjectionCompression.not_isStablyFiniteRing` | EXACT | EXACT | literal | unconditional | - | NO | - |
| MC.06 | prop:proper-isometry | and carries no faithful tracial state | `ProperProjectionCompression.no_faithfulTracialState` | EXACT | EXACT | literal | unconditional | - | NO | - |
| MC.07 | rem:maxinfinite | for a group with strict Kazhdan compression the Kazhdan projection exists in the maximal algebra and conjugation by $u_t$ dominates it | `MaximalCStarProperCompression.StrictKazhdanCompression.proj`; `MaximalCStarProperCompression.StrictKazhdanCompression.proj_mul_conjugate`; `MaximalCStarPrintedHypotheses.exists_strictKazhdanCompression`; `MaximalCStarPrintedHypotheses.manuscriptMaximalCStarRemarkFromPrintedHypotheses` | EXACT | EXACT | literal | unconditional | - | NO | stated over a `StrictKazhdanCompression` datum; the TeX badge that named only `QuasiRegularWitness.baseVector_apply_base`, a trivial fragment, is now joined by the paragraph-level certificate of MC.08b.  **MC.09 MERGED HERE 2026-08-17.**  That row said "hence the maximal algebra of such a group is not stably finite, carries no faithful trace, and is neither RFD nor MF — with the base of E as the running example", cited `ChosenMaximalCStarInfinite.chosen_maximalCStar_not_isStablyFiniteRing` and `…_no_faithfulTracialState`, and was MISMATCH/MISMATCH over substituted objects.  It is retired because MC.08b already certifies that sentence for the remark as printed, in the printed order, so the row graded one sentence twice and split the diagnosis in two.  Both things it recorded are kept: neither cited declaration states an RFD clause, and both are instantiated for the *Chosen* presentation rather than the literal eight-generator `E` of `def:E` — the same substitution UN.06 carries.  Neither is badged in the manuscript (verified, zero occurrences), so nothing loses coverage.  **What is genuinely open here is one thing and it is this row's:** `StrictKazhdanCompression` is a datum, and deriving it from the printed hypotheses of the remark is the work that would make this row unconditional.  **THAT DERIVATION EXISTS AND IS CERTIFIED, SO THE ROW IS GRADED 2026-08-18.**  `Analysis/StrictCompressionFromPrinted.lean` was landed with its root import at `5f65a8cc`, and `5f65a8cc` is an ancestor of `c0f1e4e8`, the commit of the one green `Build and audit` this repository has -- run `32122738761`, `completed/success`, `Zero-tolerance verdict` `ker='success'`.  So both declarations elaborated and were kernel-audited under that certificate, and the file has not changed since.  `exists_strictKazhdanCompression` takes exactly the remark's header -- `Γ ≤ G` with property `(T)`, and `Γ.map (MulAut.conj t).toMonoidHom < Γ`, which is `t Γ t⁻¹ ⊊ Γ` verbatim -- and produces a datum whose compressing element is the given `t`.  The three ingredients the datum carries and the remark does not name are chosen internally and each from an existing module: the symmetric generating finset from `KazhdanFiniteGeneration.exists_symmetric_generating_finset`, the Kazhdan pair from `KazhdanGenerators.exists_pair_on_generators` with `IsKazhdanPair.shrink` normalizing the tolerance to at most `1`, and the representation-universe lift `HasKazhdanPropertyT.liftUniverse`, which is the free direction and uses no ascent theorem.  `manuscriptMaximalCStarRemarkFromPrintedHypotheses` is then the remark from its printed header alone, quantifying over the printed objects only -- `P`, `u_t = maximalGroupCStarGenerator G t`, and `Cmax(G)` -- and concluding the printed sentences in the printed order.  The route is `MC.08b`'s, which is already graded `EXACT`/`EXACT` for this remark over the datum; what moves here is the datum's discharge, so `Deps` moves from `conditional-data` to `unconditional` and both columns follow.  **Badge retarget for the manuscript, not made here:** the `rem:maxinfinite` badge names `QuasiRegularWitness.baseVector_apply_base` and MC.08b's paragraph certificate; the declaration that now matches the remark's own header is `MaximalCStarPrintedHypotheses.manuscriptMaximalCStarRemarkFromPrintedHypotheses` |
| MC.08 | rem:maxinfinite | the domination is strict: at the quasi-regular coordinate $\ell ^2(G/t\Gamma t^{-1})$ the point mass at the base coset is fixed by the compressed subgroup and moved by everything outside it | `QuasiRegularWitness.exists_baseVector_witness`; `QuasiRegularWitness.baseVector_apply_base` | EXACT | EXACT | literal | unconditional | - | NO | the badge certifies only the evaluation; the separation is `exists_baseVector_witness` |
| MC.08b | rem:maxinfinite | the whole remark at once: projection, strict domination, failure of Dedekind and stable finiteness, no faithful trace, neither RFD nor MF, and the contrast with the reduced algebra | `MaximalCStarParagraphEndpoint.manuscriptMaximalCStarStrictCompressionRemark` | EXACT | EXACT | literal | unconditional | - | NO | added to the TeX 2026-08-16 in answer to MC.07's note: the paragraph now carries a certificate for its load-bearing conclusions, not only for the quasi-regular evaluation.  Conjuncts follow the printed sentence order |
| TR.01 | lem:mftrace-group | if the canonical trace of $\Cmax(G)$ is an MF trace then the countable group $G$ is MF | `manuscriptMFTraceGroupBridge`; `ShulmanTrace.isOperatorMF_of_isMFTrace_canonicalMaximal` | EXACT | EXACT | literal | unconditional | ShulmanTraces | NO | badge target of the lemma.  The quantifiers live on the closed proposition `MFTraceGroupBridge` rather than on the theorem, so the zero-input gate sees an empty outer telescope; `ShulmanTraces` is `Source` because the TeX takes the two trace classes from there, and the implication itself is proved in-repo, so `Deps` stays unconditional |
| TR.02 | lem:mftrace-group | pointwise boundedness places the sequence of images in the bounded product and the operator-norm defect conditions make $\Phi (a)$ its class a ⋆-homomorphism into the corona $Q$ | `ShulmanTrace.MFTraceModel.coronaHom` | EXACT | EXACT | literal | unconditional | - | NO | the corona route of `Sofic/MFTraceCoronaBridge.lean`, which is the printed one: dividing the bounded matrix sequences by the operator-norm-null ones is exactly what turns the asymptotic homomorphism of an `MFTraceModel` into an exact one.  Not the corner-and-polar route of `Sofic/MFTraceRecognition.lean`, which proves a different quantitative statement and is not what this lemma uses |
| TR.03 | lem:mftrace-group | $\Theta (g) = \Phi (u_g) + (1 - \Phi (1))$ is an injective homomorphism $G \to U(Q)$: if $g \ne 1$ and $\Theta (g) = 1$ then the images of $u_g$ and 1 converge in operator norm, while trace-correctness sends their normalized traces to 0 and 1 | `ShulmanTrace.MFTraceModel.eq_of_coronaHom_eq`; `ShulmanTrace.exists_injective_coronaUnitaryHom` | EXACT | EXACT | literal | unconditional | - | NO | contractivity of the normalized trace for the operator norm is the printed contradiction and is what `eq_of_coronaHom_eq` runs |
| TR.04 | thm:trace | E is sofic, and the permutation unitaries of a sofic approximation are trace-correct because the normalized trace of a permutation matrix is its proportion of fixed points | `LiteralSoficAssembly.markedGroup_isSofic`; `SoficPermutationTrace.exists_soficApproximation_tendsto_normTrace` | EXACT | EXACT | literal | unconditional | - | NO | soficity is Theorem D, already graded in the `SO` block; the row is here for the trace argument's use of it |
| TR.05 | thm:trace | the universal property of $\Cmax(E)$ extends the permutation models to a ⋆-homomorphism $\pi$ with $\tau _E = tr\circ \pi$, so $\tau _E$ is a hyperlinear trace | `ShulmanTrace.canonicalMaximalTrace_isHyperlinearTrace_of_isSofic`; `ShulmanTrace.soficUnitaryHomUltrafilter`; `ShulmanTrace.canonicalMaximalTrace_factorsThrough_tracialUltraproduct` | EXACT | EXACT | literal | unconditional | ShulmanTraces | NO | ROUTE DIFFERENCE, recorded rather than repaired.  The printed proof fixes a free ultrafilter ω and lands in the 2-norm quotient ∏M_{d_n}/⊕_{2,ω}; Lean runs the same construction along `atTop` in the tracial matrix quotient and builds the sequential model per element.  `atTop` is the stronger hypothesis to discharge, so the Lean statement is not weaker, but it is a different intermediate object and the ledger's standard counts that as a mismatch.  The extension step itself is the printed one, `maximalGroupCStar_existsUnique_lift`.  **THE PRINTED INTERMEDIATE OBJECT IS BUILT 2026-08-18, module `Analysis/PrintedUltrafilterHyperlinearTrace.lean`, AND THE GRADE IS HELD FOR THE BUILD REASON ONLY.**  The route difference above was localized to one declaration and it was not the analysis: `Analysis/TracialMatrixUltraproduct` is stated over an arbitrary filter, the `CStarRing` and `CStarAlgebra` instances of `Analysis/TracialQuotientCStar` likewise, and `ultratrace` with its bundling `ultratraceCLM` are already at an ultrafilter.  What was hardwired to `atTop` was `ShulmanTrace.soficUnitaryHom` alone.  A free ultrafilter refines `atTop` on `ℕ` (`Nat.cofinite_eq_atTop`), so every `atTop`-null defect of a sofic approximation is `ω`-null and `soficUnitaryHomUltrafilter` is the same construction at `ω`.  `canonicalMaximalTrace_factorsThrough_tracialUltraproduct` then carries the printed sentence in the printed order, for *every* free `ω`: the extension `π` into `∏M_{d_n}/⊕_{2,ω}`, the factorization `τ_G = tr_ω ∘ π` on the nose, and hyperlinearity in Shulman's sequential sense.  Its factorization step is `canonicalMaximalTrace_factorsThrough`, whose two hypotheses are the printed normalization `tr_ω 1 = 1` and the printed vanishing `tr_ω(u_g) = 0` for `g ≠ 1`, the latter being the sofic fixed-point count of `tendsto_normTrace_modelUnitary_of_ne_one` read along `ω`.  The `atTop` route is untouched and still proves the conclusion; the third conjunct is that theorem, because Shulman's notion is sequential and it is the *object*, not the conclusion, that the printed proof takes along `ω`.  **Why the grade is held:** the only green `Build and audit` is run `32122738761` on `c0f1e4e8`, where this module does not exist.  It is MSI-green as a target and is in the root import closure, so the next green run settles it  **GRADED 2026-08-19: the held condition is met.**  The note above holds this row for exactly one stated reason -- "the only green `Build and audit` is run `32122738761` on `c0f1e4e8`, where this module does not exist.  It is MSI-green as a target and is in the root import closure, so the next green run settles it".  Run 32217510998 on `dd1ca3546` is `Build and audit` `completed/success` with its `Zero-tolerance verdict` step green -- build, source scans, counterpart contract and kernel audit all clean, 44441 declarations audited with no disallowed axioms.  At `dd1ca3546` `Analysis/PrintedUltrafilterHyperlinearTrace` is present and imported by `GroupApproximation.lean`, so `canonicalMaximalTrace_factorsThrough_tracialUltraproduct` -- the printed sentence in the printed order, at a free `omega`, in the printed intermediate object -- was elaborated by the certifying build |
| TR.06 |thm:trace |consequently the canonical trace of $\Cmax(E)$ is hyperlinear and not MF, on a separable unital $$C^*$-algebra$: an MF $\tau _E$ would make $E$ MF by \ref{lem:mftrace-group} against Theorem A, and separability follows from countability of $E$ |`LiteralTraceConsequence.markedGroup_separable_canonicalTrace_hyperlinear_not_isMFTrace`; `LiteralTraceConsequence.markedGroup_canonicalMaximalTrace_not_isMFTrace`; `maximalGroupCStar_separableSpace`; `manuscriptTraceSeparation` |EXACT |EXACT |literal |unconditional |ShulmanTraces |NO | badge target of Theorem E.  This is the negative answer to the question of whether every hyperlinear trace is MF, so the row is worth reading beside TR.01, which is the half that carries the whole implication.  **BADGE REPOINTED 2026-08-18.**  The declaration the manuscript cited proved the concrete conjunction for `C*(E)` and stopped there, while the printed theorem also says `Consequently, there is a separable unital C*-algebra carrying a hyperlinear non-MF trace` -- the sentence that answers Shulman, and the one the literature asks about, since the question is about C*-algebras and not about `C*(E)`.  A concrete witness implies an existential but is not one, so the badge named a proposition strictly weaker than the printed statement.  `manuscriptTraceSeparation` is the printed statement: the concrete conjunction AND `SeparableHyperlinearNonMFTrace`, the closed existential over separable unital C*-algebras, discharged by that same witness |
<!-- END-LEDGER-STEPS -->
