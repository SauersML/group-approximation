# non_mf_group_notes.tex: sentence-level census

Generated by `scripts/sentence_census.py` from `non_mf_group_notes.tex`.  Do not edit: the assignments live in `metadata/NON_MF_NOTES_SENTENCE_MAP.tsv` and everything here is their join with the extraction.

The proof ledger grades claims; this census grades **sentences**, which is the granularity at which a prose assertion can hide from a gate that reads only numbered environments.

| status | sentences |
| --- | --- |
| `ledger` | 472 |
| `formalized` | 182 |
| `unassigned` | 119 |
| `structural` | 25 |
| `definition` | 25 |
| `attribution` | 11 |
| `partial` | 8 |
| `open` | 1 |
| **total** | **843** |

Completely formalized or definitional: **679/843** (80.5%).

A sentence under a single-row ledger anchor inherits that forced row.  A sentence under a multi-row anchor must name its row or rows explicitly in `metadata/NON_MF_SENTENCE_MAP.tsv`; no text-similarity guess and no whole-anchor fallback is accepted.

## What no declaration establishes

These are the sentences the development does not settle, verbatim.  `open` sentences say that something is unknown and no declaration could settle them; `partial` sentences have a clause that is proved and a clause that is not; `unassigned` sentences have not been triaged at all.

* **`unassigned`**, line 273 --- Let $\Cl(X)$ be generated by a central involution $\zeta$ and involutions $c_\xi$, $\xi\in X$, with $[c_\xi,c_\eta]=\zeta$ for $\xi\ne\eta$, and put $W=\Cl(X)\rtimes\Sigma$.
  * anchor has 6 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 346 --- Proposition~\ref{prop:univquot} shows that $G$ is MF exactly when this subgroup is trivial. By Corollary~\ref{cor:marked-obstruction}, $1\ne w\in\operatorname{Res}_{\mathrm{MF}}(E)$.
* **`open`**, line 382 --- For $\Cred(\SL_3(\mathbb Z))$ and $\Cred(\SL_4(\mathbb Z))$ the MF property is undecided; only the stronger PMF property is known to fail, for $\SL_4(\mathbb Z)$ \cite{MdlS}.
  * records the current open MF status of the two reduced group algebras and attributes the PMF comparison
* **`unassigned`**, line 431 --- LEF groups are MF \cite{CDE} and $E$ is not MF (Corollary~\ref{cor:marked-obstruction}), so $E$ is not LEF; a residually finite group is LEF, so $E$ is not residually finite either.
  * anchor has 6 ledger rows; an explicit sentence-level assignment is required
* **`partial`**, line 455 --- Every MF trace is hyperlinear; Shulman asks whether the converse holds.
  * the MF-trace-to-hyperlinear implication is formalized; Shulman's converse question is explicitly open
* **`unassigned`**, line 472 --- Thus $G$ embeds in the unitary group of a norm matrix corona and is MF.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 534 --- If $\tau_E$ were an MF trace, Lemma~\ref{lem:mftrace-group} would imply that $E$ is MF, contradicting Corollary~\ref{cor:marked-obstruction}\@.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 567 --- Let $\pi$ be the induced representation on the Hilbert space ultraproduct, let $P$ project onto $\operatorname{Fix}\pi(L)$, and put $V=\pi(t)$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 567 --- The inclusion $tLt^{-1}\leq L$ gives $P\leq VPV^*$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 567 --- Both projections belong to the norm ultraproduct of the matrix algebras and are unitarily equivalent there.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 567 --- That algebra is finite, so $P=VPV^*$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 567 --- So both $V[x_n]$ and $V^*[x_n]$ are fixed by $\pi(L)$, which gives the asserted estimates.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 646 --- Theorem~\ref{thm:transport-variants}, with $\nu_n=d_n$, applied to $x_n=U_{c,n}$, shows that $(U_{\hat c,n})$ for $\hat c=tct^{-1}$ lies in the same asymptotic commutant.
  * anchor has 17 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 668 --- Compress $V_{g,n}$ to $q_n\mathbb C^{d_n}$ and take its polar part.
  * anchor has 17 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 668 --- Asymptotic centrality of $q_n$ makes the polar correction tend to zero in operator norm.
  * anchor has 17 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 668 --- The corrected operators form an operator-norm asymptotic representation $(W_{g,n})$ of $H$ on the corners $q_nM_{d_n}(\C)q_n\cong M_{r_n}(\C)$, $r_n=\operatorname{rank}q_n>0$.
  * anchor has 17 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 714 --- Proposition~\ref{prop:witness} gives $w\ne1$.
* **`unassigned`**, line 714 --- Put $L=\iota(\mathcal B)$ and $\hat c=tct^{-1}$.
* **`unassigned`**, line 714 --- The element $u=[\hat c,\iota(v_1)]$ belongs to $N_{\mathrm{conj}}$, and $w=u^2$.
* **`unassigned`**, line 714 --- The subgroup $\langle w\rangle$ is finite and normal because $w$ is a central involution.
* **`unassigned`**, line 714 --- Theorem~\ref{thm:criterion}, applied with $F=\langle w\rangle$, implies that every homomorphism from $E$ to a norm matrix corona maps $w$ to the identity.
* **`unassigned`**, line 729 --- For $F=\{1,w\}$, the projection is $p_n=\tfrac12(1+V_{w,n})$; in the corona corner the image of $w$ is $-1$ exactly, while the finite-stage compressions satisfy $\opnorm{W_{w,n}+1}\to0$.
* **`unassigned`**, line 773 --- For a one-sided conjugator $s$, Theorem~\ref{thm:transport-variants}, with $\nu_n=d_n$, applied to $L=\iota(\Lambda)$ gives the conclusion for $V_{s,n}x_nV_{s,n}^*$.
  * anchor has 5 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 831 --- Its image $\bar H=\Theta(H)$ is countable and, as a subgroup of $\U(\mathcal Q)$, is MF.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 831 --- Choose an operator-norm asymptotic representation $(V_{g,n})$ in which every fixed nontrivial element eventually stays at operator-norm distance at least $1$ from the identity.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 844 --- Let $H_\omega$ be the Hilbert-space ultraproduct of the coordinate spaces $\C^{d_n}$, and let $B_\omega=\prod_\omega B(\C^{d_n})$ act on $H_\omega$.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 855 --- The Kazhdan inequality implies that the spectrum of $h$ lies in $[-1,1-\varepsilon_0^{2}/(2|S|)]\cup\{1\}$ and so in $[-1,\theta]\cup\{1\}$, so the spectral projection $P=\chi_{\{1\}}(h)$ belongs to $B_\omega$; its range is $\operatorname{Fix}\pi(\bar K)$, where $h=1$.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 889 --- Compress $V_{g,n}$ to $q_n\mathbb C^{d_n}$ and take its polar part.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 889 --- The polar correction tends to zero in operator norm because $q_n$ is asymptotically central. The corrected operators form an operator-norm asymptotic representation $(W_{g,n})$ of $\bar H$ on the corners $q_nM_{d_n}(\C)q_n$.
  * anchor has 10 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 977 --- Every finite-dimensional representation of $E$ over any field maps $w$ to the identity by Theorem~\ref{thm:exactfd-intrinsic}.
* **`unassigned`**, line 999 --- If $s\in P_\Gamma$, then
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1005 --- because $s\Gamma s^{-1}\leq\Gamma$.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1005 --- Both spaces have the same finite dimension, so equality holds.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1005 --- Thus every element of $P_\Gamma$ preserves $C$ in both directions, and so does all of $G^{+}(\Gamma)=\langle P_\Gamma\rangle$.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1027 --- Apply Theorem~\ref{thm:exactfd-intrinsic} with $H=E$ and $\Gamma=\iota(\mathcal B)$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1027 --- The defining relations put $t$ in $P_\Gamma$, $c$ in $C_E(\Gamma)$, and $u=[tct^{-1},\iota(v_1)]$ in $\mathfrak D(E,\Gamma)$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1027 --- So every finite-dimensional representation maps $u$, and so $w=u^2$, to the identity.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1027 --- Moreover, $w\neq1$ by Proposition~\ref{prop:witness}.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1071 --- The relator $w_{\mathrm{BS}}^2=1$ is redundant: $tct^{-1}$ and $\gamma_0(tct^{-1})\gamma_0^{-1}$ are involutions, so centrality forces their commutator to have order two by the calculation following Definition~\ref{def:E}.
  * anchor has 9 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1071 --- By Theorem~\ref{thm:exactfd-intrinsic}, every finite-dimensional representation over every field maps \(w_{\mathrm{BS}}\) to the identity.
  * anchor has 9 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1239 --- For the second statement, Corollary~\ref{cor:marked-obstruction} gives $w\in\operatorname{Res}_{\mathrm{MF}}(E)$.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1267 --- The resulting sequence is an operator-norm asymptotic representation separating every nonidentity element from the identity.
  * anchor has 8 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1267 --- The corresponding unitary classes give a faithful homomorphism $Q\to\U(\mathcal Q)$.
  * anchor has 8 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1335 --- Uniformly in a finite presentation $P$ and a word $v$,
* **`unassigned`**, line 1341 --- The same $\Sigma^0_2$ upper bound holds for the set of radical words after fixing $P$.
* **`unassigned`**, line 1346 --- This is decidable over the real closed field.
* **`unassigned`**, line 1346 --- Marked corona extraction and amplification, in one direction, and the diagonal norm-corona construction, in the other, give
* **`partial`**, line 1361 --- There is one fixed finitely presented non-MF group $U$ for which membership in $\operatorname{Res}_{\mathrm{MF}}(U)$ is undecidable.
  * PROVED: for any operator-MF countable right factor A, membership of a right-factor word in the MF residual of E*A is equivalent to triviality in A, and E*A is finitely presented and non-MF when E is.  NOT PROVED: that some such A has undecidable word problem -- Kharlampovich's group is an external input the corpus deliberately does not assert (see the module docstring)
* **`unassigned`**, line 1366 --- Let $A$ be Kharlampovich's finitely presented solvable group of derived length three with undecidable word problem \cite{Kharlampovich}.
* **`unassigned`**, line 1366 --- It is amenable, so $\Cred(A)$ is quasidiagonal~\cite{TWW} and embeds in a norm matrix corona, and $A$ is MF\@.
* **`partial`**, line 1383 --- Indeed the forward implication follows by applying $\pi_A$, and the reverse implication is trivial. The inclusion of words in the $A$ factor is computable, so a radical-membership algorithm for $U$ would decide the word problem of $A$.
  * PROVED: both implications of the displayed equivalence, forward by the retraction and reverse by `simp`.  NOT PROVED: that the inclusion of words in the A factor is computable, and the resulting reduction of A's word problem to radical membership in U
* **`unassigned`**, line 1443 --- By Corollary~\ref{cor:marked-obstruction}, \((\widehat j\circ\phi)(w)=1\); injectivity of \(\widehat j\) gives \(\phi(w)=1\).
  * anchor has 2 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1465 --- Distinct permutations differ in a column of their permutation matrices and so have operator-norm distance at least $1$, so the group is MF.
  * anchor has 5 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1492 --- The group \(F_8\) is residually finite, so MF by Lemma~\ref{lem:permanence}(2), while \(E\) is not MF by Corollary~\ref{cor:marked-obstruction}.
  * anchor has 2 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1607 --- Choose unitary coordinate lifts $U_n(h)$ of $\Theta(h)$.
  * anchor has 5 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1635 --- Finiteness of the norm ultraproduct gives $P=Q$.
  * anchor has 5 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 1746 --- By Definition~\ref{def:E}, the distinguished word is $w=[\hat c,\iota(v_1)\,\hat c\,\iota(v_1)^{-1}]$, the commutator attached to the class of $v_1$.
  * anchor has 4 ledger rows; an explicit sentence-level assignment is required
* **`partial`**, line 1950 --- For every finite group $K$, every homomorphism from $W_K$ to a norm matrix corona factors uniquely through $\Phi_K$.
  * PROVED for every finite PERFECT K: precomposition with the wreath projection onto the acting group is a bijection on homomorphisms into genuine norm-matrix coronas, so every corona homomorphism factors uniquely through it.  NOT PROVED for a general finite K, where Phi_K has the heightwise-abelianized target K_ab^(Z) x| G_*, which the corpus does not construct
* **`partial`**, line 1950 --- Moreover
  * PROVED for finite perfect K: Rad_MF = Rad_fd = Res_fin = Rad_lin = ker Phi_K.  NOT PROVED: the Rad_Bohr clause -- `four_radicals_eq_and_bohr_bounds` gives only `bohrResidual <= ker Phi` in general plus equality on totally disconnected (profinite) targets; the general compact case is Peter--Weyl, which the pinned Mathlib lacks.  Also not proved for non-perfect finite K
* **`partial`**, line 1962 --- Equivalently, precomposition with $\Phi_K$ is a bijection on homomorphisms into norm matrix coronas, residually finite groups, finite-dimensional linear groups over arbitrary fields, and compact Hausdorff groups.
  * PROVED for finite perfect K: the precomposition bijection for norm-matrix coronas, residually finite groups, finite groups, finite-dimensional unitary groups, GL_d(F) over every field, and profinite groups.  NOT PROVED: the general compact Hausdorff target, which needs Peter--Weyl; and the whole statement for non-perfect finite K
* **`unassigned`**, line 1968 --- Put $\Gamma_n=t^{-n}\Gamma_*t^n$ and $X_n=G_*/\Gamma_n$.
* **`unassigned`**, line 1968 --- The subgroups $\Gamma_n$ increase, and
* **`unassigned`**, line 1974 --- Let $\Phi_1$ abelianize the lamp values and push them from $X$ to $X_1$.
* **`unassigned`**, line 1974 --- For $n\geq1$, let $\psi_n$ push $K_{\mathrm{ab}}^{(X_n)}$ to $K_{\mathrm{ab}}^{(X_{n+1})}$, and write $\Phi_n=\psi_{n-1}\cdots\psi_1\Phi_1$.
* **`unassigned`**, line 1979 --- For $\Phi_1$, apply Theorem~\ref{thm:notes-spectral-motion} with $L=\Gamma_*$ and $s=t$ to $k_{t\Gamma_*}$, where $k\in K$.
* **`unassigned`**, line 1979 --- Every corona image of this lamp has finite spectrum, commutes with the image of $t\Gamma_*t^{-1}$, and has pairwise commuting $\Gamma_*$-conjugates.
* **`unassigned`**, line 1979 --- Every corona homomorphism $\pi$ then satisfies
* **`unassigned`**, line 1988 --- Conjugating these equalities by elements of $G_*$ identifies the lamps in each fibre of $X\to X_1$.
* **`unassigned`**, line 1988 --- Each fibre contains distinct sites, so commutation between different site groups makes the identified copy of $K$ abelian.
* **`unassigned`**, line 1988 --- These equalities generate $\ker\Phi_1$.
* **`unassigned`**, line 1988 --- So $\ker\Phi_1\leq\operatorname{Rad}_{\mathrm{MF}}(W_K)$.
* **`unassigned`**, line 1995 --- For $n\geq1$, apply the same theorem in $K_{\mathrm{ab}}^{(X_n)}\rtimes G_*$ with $L=\Gamma_n$ and $s=t$.
* **`unassigned`**, line 1995 --- A lamp at $t\Gamma_n$ commutes with $t\Gamma_nt^{-1}=\Gamma_{n-1}$, and its $\Gamma_n$-conjugates commute pairwise.
* **`unassigned`**, line 1995 --- Every corona homomorphism identifies the lamps in each fibre of $X_n\to X_{n+1}$.
* **`unassigned`**, line 1995 --- These equalities generate $\ker\psi_n$, so $\ker\psi_n\leq\operatorname{Rad}_{\mathrm{MF}} (K_{\mathrm{ab}}^{(X_n)}\rtimes G_*)$.
* **`unassigned`**, line 1995 --- Corollary~\ref{cor:pullback}, applied inductively, gives
* **`unassigned`**, line 2009 --- The map $\Phi_K$ is the pushforward to $G_*/N$.
* **`unassigned`**, line 2009 --- If a finitely supported lamp configuration belongs to $\ker\Phi_K$, choose $n$ such that any two points of its support with the same image in $G_*/N$ already have the same image in $X_n$.
* **`unassigned`**, line 2009 --- The configuration then belongs to $\ker\Phi_n$.
* **`unassigned`**, line 2009 --- Thus
* **`unassigned`**, line 2017 --- so every corona homomorphism annihilates $\ker\Phi_K$.
* **`unassigned`**, line 2019 --- The homomorphism
* **`unassigned`**, line 2025 --- is injective.
* **`unassigned`**, line 2025 --- The group $G_*$ and the wreath product $K_{\mathrm{ab}}^{(\mathbb Z)}\rtimes\mathbb Z$ are residually finite, so $P_K$ is residually finite.
* **`unassigned`**, line 2025 --- Its finite quotients separate every element outside $\ker\Phi_K$.
* **`unassigned`**, line 2025 --- So the finite, finite-dimensional unitary, and MF residuals equal $\ker\Phi_K$.
* **`unassigned`**, line 2025 --- A finite-dimensional linear image is residually finite by Mal'cev's theorem, and finite-dimensional unitary representations separate points of compact groups by Peter--Weyl.
* **`unassigned`**, line 2025 --- So the linear and Bohr residuals have the same kernel.
* **`unassigned`**, line 2025 --- Surjectivity of $\Phi_K$ gives uniqueness.
* **`partial`**, line 2064 --- For every countable abelian group $A$,
  * PROVED: the collapse direction -- a finite-order lamp value produces corona-invisible collapse commutators at every order, which is the obstruction when A has torsion.  NOT PROVED: the converse, that a torsion-free A makes the wreath product MF; `isOperatorMF_integerLampTelescope` reaches only the integer-lamp wreath over the TELESCOPE, and its own module says the full group with the shift adjoined is not reached by that argument and remains outside the formal development
* **`unassigned`**, line 2073 --- Theorem \ref{thm:notes-spectral-motion} identifies its image with the image of each $\Gamma_*$-translate in the same fibre of $X\to X_1$.
* **`unassigned`**, line 2073 --- For a nonidentity torsion element $a\in A$, choose $\gamma\in\Gamma_*$ such that $\gamma t\Gamma_*\ne t\Gamma_*$; such an element exists because $[\Gamma_*:t\Gamma_*t^{-1}]=8$.
* **`unassigned`**, line 2073 --- Every corona homomorphism annihilates $a_{\gamma t\Gamma_*}a_{t\Gamma_*}^{-1}$, which is nonidentity in $A^{(X)}$.
* **`unassigned`**, line 2083 --- Suppose that $A$ is torsion-free.
* **`unassigned`**, line 2083 --- The embedding $A\to A\otimes_{\mathbb Z}\mathbb Q$ is injective, and the countable $\mathbb Q$-vector space $A\otimes_{\mathbb Z}\mathbb Q$ embeds in a $\mathbb Q$-linear complement of $2\pi\mathbb Q$ in $\mathbb R$.
* **`unassigned`**, line 2083 --- Composing the two embeddings gives an injective additive map $\theta\colon A\to\mathbb R$ whose image meets $2\pi\mathbb Z$ only at zero.
* **`unassigned`**, line 2083 --- Put $\Gamma_1=t\Gamma_*t^{-1}$ and identify $G_*/\Gamma_1$ with $X$ by $g\Gamma_1\mapsto gt\Gamma_*$.
* **`unassigned`**, line 2083 --- Choose representatives $x_j=r_j\Gamma_1$ for the points of $G_*/\Gamma_1$, and let $T$ be a finite generating set of $\Gamma_1$.
* **`unassigned`**, line 2095 --- Residual finiteness of $G_*$ gives finite quotients $q_n\colon G_*\to F_n$ which separate the first $n$ nonidentity elements of $G_*$ and satisfy
* **`unassigned`**, line 2103 --- Set $K_n=q_n(\Gamma_1)$, let $\ell_n$ be word length in $K_n$ with respect to $q_n(T)$, and define
* **`unassigned`**, line 2112 --- On $\ell^2(F_n)$ define
* **`unassigned`**, line 2118 --- For fixed $a\in A$ and $u\in\Gamma_1$,
* **`unassigned`**, line 2123 --- Consequently $D(a)=[D_n(a)]$ commutes with $\Lambda(u)=[\Lambda_n(u)]$ in the norm matrix corona.
* **`unassigned`**, line 2123 --- For $x=r\Gamma_1$, set $U_x(a)=\Lambda(r)D(a)\Lambda(r)^*$.
* **`unassigned`**, line 2123 --- This is independent of the representative, the unitaries $U_x(a)$ commute, and
* **`unassigned`**, line 2130 --- So they define a homomorphism
* **`unassigned`**, line 2135 --- Let $b$ be a nonzero lamp configuration and choose $a_j=b(x_j)\ne0$.
* **`unassigned`**, line 2135 --- For all sufficiently large $n$, evaluation at $\delta_{q_n(r_j)}$ gives the eigenvalue $e^{i\theta(a_j)}$: the separation condition makes the other lamps in the support contribute $1$.
* **`unassigned`**, line 2135 --- So the image of $b$ differs from the identity.
* **`unassigned`**, line 2135 --- If an element has nontrivial $G_*$-component $g$, then $q_n(g)\ne1$ for all sufficiently large $n$.
* **`unassigned`**, line 2135 --- The corresponding matrix is a diagonal unitary times a fixed-point-free permutation, so its distance from the identity is at least $\sqrt2$.
* **`unassigned`**, line 2135 --- The homomorphism is injective, and the group is MF.
* **`unassigned`**, line 2191 --- Neither is $\Cred(W)$: if $\Cred(W)$ embedded in a matrix quotient, the map $u\mapsto e(u)+1-e(1)$ would give an injective homomorphism $W\to\U(\mathcal Q)$, as in the proof of Corollary~\ref{cor:marked-obstruction}.
* **`unassigned`**, line 2330 --- For the complete graph on $X$ this is $\Cl(X)$, and adjoining the anticommutation relations at the remaining pairs gives a surjection $C(\mathcal G)\to\Cl(X)$.
* **`unassigned`**, line 2517 --- If $G$ is MF, the image is trivial, because $w\in\operatorname{Res}_{\mathrm{MF}}(E)$ by Corollary~\ref{cor:marked-obstruction} and the residual is functorial (Lemma~\ref{lem:portable}).
  * anchor has 2 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2517 --- The canonical tuple of $E$ satisfies $R$ by definition, and its distinguished word is nontrivial by Corollary~\ref{cor:marked-obstruction}.
  * anchor has 2 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2535 --- Lemma~\ref{lem:permanence}(1) and Corollary~\ref{cor:marked-obstruction} show that MF is a Markov property of finitely presented groups: the trivial group is MF, while by subgroup heredity the finitely presented group $E$ embeds in no MF group.
* **`partial`**, line 2612 --- For finite presentation codes,
  * PROVED: both first-level exclusions.  `NONMF_fp` is not recursively enumerable, which is `NONMF_fp not in Sigma^0_1`, and is equally `MF_fp not in Pi^0_1`.  NOT PROVED: the two second-level upper bounds `MF_fp in Pi^0_2` and `NONMF_fp in Sigma^0_2`.  `MicrostateNormalForm` says so in its own words: the remaining distance to a Pi^0_2 upper bound is the effectivisation of the microstate existential, that step is not taken, and no claim about the arithmetical hierarchy is made there
* **`unassigned`**, line 2623 --- Fixed-dimensional feasibility is decidable over the real closed field.
* **`unassigned`**, line 2637 --- for a recursive $C_{\mathrm{MF}}$.
* **`unassigned`**, line 2641 --- The FIN/INF switch gives exact second-level completeness for recursive presentations, and the accompanying manuscript \emph{MF Recognition is $\Pi^0_2$-Complete} extends it to finite presentations through HNN extensions whose edge isomorphism is implemented by a corona unitary.
* **`unassigned`**, line 2709 --- the element $c$ centralizes the Kazhdan base there, and conjugation by $t$ produces $tct^{-1}$, whose commutator with the base is the element $u$ with $u^2=w\ne1$ of Corollary~\ref{cor:marked-obstruction}.
* **`unassigned`**, line 2743 --- There is a torsion-free group with property~\textup{(T)} that equals its own MF radical. Theorem~\ref{paper-thm:torsion-free} of the main paper gives such a group that is two-generated, finitely presented, and acylindrically hyperbolic, and shows that every nontrivial quotient of it also equals its own MF radical.
* **`unassigned`**, line 2804 --- The construction of \cite[\S2]{FFF} gives a finitely presented torsion-free property-\textup{(T)} group $P$ containing $P_1\times P_2\times S$, where $P_i\cong P$ and $S$ is finitely presented, simple, and torsion-free.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2804 --- Choose an isomorphism $\alpha\colon P\to P_1$ and $a\in P_2\setminus\{1\}$.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2804 --- Define $E(P,\alpha,a)$ from a finite presentation of $P$ by adjoining $t,c$, imposing
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2813 --- for the chosen generators $p$ of $P$, and making $w=[tct^{-1},a(tct^{-1})a^{-1}]$ central. The Clifford coset representation used in Proposition~\ref{prop:witness} maps $w$ to its central involution.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required
* **`unassigned`**, line 2813 --- Thus $w\ne1$, and $E(P,\alpha,a)$ has torsion.
  * anchor has 3 ledger rows; an explicit sentence-level assignment is required

## By section

| section | sentences | unassigned |
| --- | --- | --- |
| (front matter) | 3 | 0 |
| A literal presentation and affine certificate | 12 | 0 |
| Property~\textup{(T) | 32 | 1 |
| The presentation-level obstruction | 13 | 0 |
| Consequences of the example | 3 | 1 |
| The quotient by the central involution | 6 | 0 |
| A stably finite non-MF group algebra | 12 | 0 |
| A sofic example | 9 | 1 |
| A hyperlinear non-MF trace | 28 | 2 |
| Weighted asymptotic-commutant invariance | 12 | 5 |
| Obstructions from a one-sided conjugation datum | 45 | 10 |
| An intrinsic form | 20 | 1 |
| The normal-Kazhdan form of the obstruction | 48 | 6 |
| The finite-dimensional obstruction | 23 | 9 |
| The cyclic comparison | 48 | 2 |
| The MF residual | 77 | 10 |
| Permanence and its failure | 17 | 2 |
| Commuting orbits and the MF residual | 101 | 3 |
| The exact residual of $E$ | 28 | 0 |
| Spectral-component rigidity | 14 | 0 |
| The heightwise abelian quotient | 45 | 29 |
| The abelian boundary | 24 | 22 |
| The reduced group algebra of \texorpdfstring{$E$ | 14 | 0 |
| The Clifford quotient and permanence | 28 | 1 |
| Soficity of $E$ | 76 | 1 |
| A finite universal Horn obstruction | 10 | 2 |
| Undecidability of MF recognition | 39 | 4 |
| Limitations of the operator-norm method | 21 | 1 |
| A torsion-free non-MF group | 26 | 6 |
| Open questions | 9 | 0 |
