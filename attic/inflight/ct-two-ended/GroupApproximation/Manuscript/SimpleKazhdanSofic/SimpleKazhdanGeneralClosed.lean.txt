import GroupApproximation.Manuscript.SimpleKazhdanSofic.GeneralTheoremClosure
import GroupApproximation.Manuscript.SimpleKazhdanSofic.MatricialMarkedLimitEndpoint
import GroupApproximation.Meta.AxiomGuard

/-!
# `thm:general`, closed

`simple_kazhdan_sofic_group.tex` at 37551fd93, Theorem `thm:general` (tex l.109–127):

> Let a finitely generated group $\Lambda$ act minimally and topologically freely on a Cantor set $Z$,
> and let $R=\LC(Z,\F_2)\rtimes\Lambda$ be generated by a finite set $S\ni1$. Suppose that $R$ is
> matricial [...] Then for every $n\ge3$, $\EL_n(R)$ is an infinite, finitely generated, simple Kazhdan
> group. Marked by the $e_{ij}(s)$ with $s\in S$, it is the limit of the finite simple groups
> $\SL_{nN_k}(\F_2)$ marked by the $e_{ij}(\varphi_k(s))$, whose Cayley graphs form a family of expanders.

Its pieces are all carried:

* simplicity: `printedGeneralSimplicityStatement` (skf-consequences, via `GeneralTheoremClosure`);
* the marked limit: `printedMatricialMarkedLimitStatement` (ms-core-3, `MatricialMarkedLimitEndpoint`);
* `EL_n(M_N(F₂))` simple and the expanders: closed in `MainFromGeneral` and `GeneralTheorem`;
* (T) and infiniteness: sk-lef-action's `printedGroupCrossedProductPropertyTInfinite`.

`printedSimpleKazhdanSoficMain_of_periodic` reduces `thm:main` to the periodic approximation alone
(`PrintedPeriodicMatricialStatement`, tex l.244–271; lane ms-core-3).
-/

namespace GroupApproximation
namespace SimpleKazhdanSofic

/-- **Theorem `thm:general`** (tex l.109–127). -/
theorem printedSimpleKazhdanGeneral : PrintedSimpleKazhdanGeneral :=
  printedSimpleKazhdanGeneral_of_marked_limit printedMatricialMarkedLimitStatement

/-- **`thm:main` from the periodic approximation** (tex l.238–271). -/
theorem printedSimpleKazhdanSoficMain_of_periodic (hper : PrintedPeriodicMatricialStatement) :
    PrintedSimpleKazhdanSoficMain :=
  printedSimpleKazhdanSoficMain_of_general printedSimpleKazhdanGeneral hper

end SimpleKazhdanSofic
end GroupApproximation

/-! ### Axiom audit -/

#audit_closed_axioms GroupApproximation.SimpleKazhdanSofic.printedSimpleKazhdanGeneral
#audit_axioms GroupApproximation.SimpleKazhdanSofic.printedSimpleKazhdanSoficMain_of_periodic
