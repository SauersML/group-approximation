---
rg: 2
id: forbidden-packet-reynolds-holonomy-payment
kind: claim
title: Transporting a forbidden predicate packet across its Reynolds holonomy costs quadratic mass
distinct_from:
  predicate-rank-jump-forces-adjoint-reynolds-gap: that gives only an adjoint rank difference; this converts it to ordinary normalized-HS word energy.
  fixed-scale-contextual-multiplicity-holonomy: that must construct the returning word network; this is the quantitative endpoint once that network transports the Reynolds averages.
---

Use the notation of `predicate-rank-jump-forces-adjoint-reynolds-gap`: the
baseline spin dimension is `D_0=2^N`, `K=|f^(-1)(1)|`, and forbidden Hilbert
mass is `q`. Suppose a finite holonomy word `T` pairs uniform enumerations of
the represented `A_f` and `B_f` Reynolds averages, after repeating elements
to a common list length, with mean squared normalized-HS transport error
`epsilon` as in `(RHG1)`. Then

```text
epsilon >= (3/(8 D_0^2 K)) q^2.                               (FRH1)
```

For the fixed-pivot predicate packet this is `epsilon>=3q^2/32`.

Thus no integer-index theorem or copywise selector is needed at the final
payment step. A positive forbidden packet density creates a quadratic but
dimension-free ordinary matrix-energy floor as soon as the presentation
returns the two finite Reynolds averages through one word holonomy.

**Consistency scope.** The raw finite groups cannot themselves be conjugate:
`termwise-finite-reynolds-holonomy-has-zero-rank-gap` would then make their
rank gap vanish. The intended use is after an infinite stabilization whose
finite-dimensional local residue supplies the approximate channel transport
in `(RHG1)`.
