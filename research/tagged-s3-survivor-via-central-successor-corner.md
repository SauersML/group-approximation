---
rg: 2
id: tagged-s3-survivor-via-central-successor-corner
kind: route
title: Use the tag survivor itself as the next reducing corner
target: tagged-s3-survivor-installs-next-recurrent-carrier
requires:
  - tag-centralized-successor-corner
  - oriented-c18-rank-gates-give-branch-balance
  - branching-plus-first-hit-exits-forces-hs-floor
---

For each finite Fanizza packet role, apply
`tag-centralized-successor-corner` to consecutive free occurrences.  Equation
`(TSC5)` identifies the transported three-atom live union with the next
analytic carrier `C_(n+1)Q`, so it both lies under the survivor prefix and
reduces the next occurrence's shared BCS words.  The complementary atom is
the first-hit exit, and the telescope excludes it from every later carrier.

The local Schur calculation and `(OCB2b)` supply `(BFE1)--(BFE3)` before
transport.  Unitary transport preserves all four atom traces, while `(TSC6)`
adds only polynomial-times-`sqrt(E)` errors.  The first-hit telescope is
`(BFE4)`.  Thus the complete branching ledger survives with the required
fixed-depth polynomial modulus.  Exact marked completeness is the final
paragraph of `tag-centralized-successor-corner`.

