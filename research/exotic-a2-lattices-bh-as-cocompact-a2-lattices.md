---
rg: 2
id: exotic-a2-lattices-bh-as-cocompact-a2-lattices
kind: route
title: Exotic Ã₂ lattices are cocompact lattices of a locally finite thick Ã₂ building, so the general Ã₂ lattice theorem applies
target: exotic-a2-lattices-satisfy-boone-higman
requires:
  - a2-lattices-embed-in-fp-simple-groups
---

A cocompact lattice `Γ` on a locally finite Ã₂ building `X` that is not the building of
`PGL_3(D)` is in particular a discrete cocompact group of automorphisms of a locally finite
thick Ã₂ building (buildings are thick throughout the Ã₂ nodes, and the target itself
records that it is this special case). So `a2-lattices-embed-in-fp-simple-groups` embeds `Γ` in a finitely
presented simple group.

The target already states that it is a special case of that node. This route records the
special case as graph wiring (bh-integrate, 2026-09-18) and adds no mathematics.
