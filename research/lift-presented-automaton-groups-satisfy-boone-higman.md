---
rg: 2
id: lift-presented-automaton-groups-satisfy-boone-higman
kind: claim
title: An automaton group whose relator kernel is a finitely generated lift ideal embeds in a finitely presented simple group
distinct_from:
  rover-nekrashevych-fp-iff-finite-lift-presentation: that characterizes finite presentation of V_d(G) for finite-state G; this turns that finite presentation into a finitely presented simple overgroup of G, via regrouping and a cone-diagonal embedding into the commutator subgroup.
  fp-self-similar-groups-embed-in-fp-simple-groups: that is Zaremsky's theorem for finitely presented self-similar G (any section structure); this covers finite-state G that need not be finitely presented or contracting, provided the lift-ideal condition holds.
  automaton-groups-satisfy-boone-higman: that is the open statement for all automaton groups; this is the case where the relator kernel is a finitely generated lift ideal.
---

**ESTABLISHED** by `lift-presented-automaton-groups-satisfy-boone-higman-proof`
(lane proof, elementary given the cited inputs; no priority claimed). Referee a (proof gaps) PASS: `research/artifacts/gq-referee-a-lift-presented-automaton-groups-satisfy-boone-higman.md`. Citation review (referee b) pending.

## Setting

As in `rover-nekrashevych-fp-iff-finite-lift-presentation`: `X = {0,…,d-1}`, `d >= 2`; `G <= Aut(X^*)` is
generated by a finite set `S = S_0 ∪ S_0^{-1}` closed under sections, where `F = F(S_0)` is free on `S_0`
and inverses are formal; `N = ker(F -> G)` is the relator kernel; lift ideals and `K_∞` are as there.

## Statement

**Theorem.** If `N = ⟨⟨R⟩⟩_lift` for a finite `R ⊆ N` (equivalently, by Theorem A of
`rover-nekrashevych-fp-iff-finite-lift-presentation`, `V_d(G)` is finitely presented), then `G` embeds in
a finitely presented simple group. Explicitly: for every even `m` such that `1/m` is not an eigenvalue of
Zaremsky's matrix `A` (all but finitely many even `m`), with `V = V_{md}(G)` for the regrouped action of `G`
on `T_{md}` and `e` the exponent of the finite group `V^ab`, the map
`δ_e(g) = L_{v_1}(g) ··· L_{v_e}(g)` (cone copies on `e` pairwise disjoint cones) embeds `G` in the simple
group `[V,V]`, which has finite index in `V` and is finitely presented.

## Consequences

1. **Recovers the finite-state cases of the known theorems.** `G` finitely presented (Scott FP1) or
   contracting (Nekrashevych FP2) satisfies the hypothesis, by consequence 1 of Theorem A.
2. **New hosts for non-fp, non-contracting automaton groups.** The lamplighter automaton group `Z/2 ≀ Z`
   (`rover-nekrashevych-group-of-lamplighter-automaton-is-fp`, `N = K_∞`) and the affine lamplighters and
   `G^+ = AGL_1(F_q[t^{±1}])` of `rn-groups-of-affine-lamplighters-f-q-wr-z-are-fp` get explicit f.p. simple
   commutator hosts `[V_{md}(G),V_{md}(G)]`. Boone–Higman for these particular groups was already known by
   other means (lamplighters and char-p linear groups); what is new here is the self-similar host.
3. **Automaton groups freely generated by their states.** If `F -> G` is injective (for example Aleshin's
   3-state automaton group, free of rank 3 by Vorobets–Vorobets 2007, and the bireversible free automaton
   groups of Steinberg–Vorobets–Vorobets; these sources were not read here), then `N = 1 = K_∞`, so by
   Theorem A **`V_d(G)` is finitely presented**, and the Theorem gives an f.p. simple commutator host.
   Boone–Higman for free groups is trivial; the finite presentation of these Nekrashevych groups is the
   point.

## Scope

- Automaton groups are residually finite, so this is a residually finite route only.
- It does not cover every automaton group: Skipper–Witzel–Zaremsky (arXiv:1712.05361, Thm 7.1, Ex. 7.2)
  give finite-state `G` with `V_d(G)` not finitely presented, and by
  `persistent-automata-rn-group-fp-iff-group-fp` a persistent automaton group satisfies the hypothesis iff
  it is finitely presented. Those groups need an overgroup: see
  `automaton-groups-bh-via-lift-presented-overgroups`.
