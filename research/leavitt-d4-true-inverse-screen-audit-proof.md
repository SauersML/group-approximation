---
rg: 2
id: leavitt-d4-true-inverse-screen-audit-proof
kind: route
title: Check inverse products before every Bergman-chart census
target: leavitt-d4-true-inverse-screen-audit
requires: []
---

Direct multiplication in the faithful chart gives `x_adj x != 1` for the
marked unit when `x_adj` is obtained by swapping prefix pairs.  The corrected
pair representation asserts both `x x_inverse=1` and `x_inverse x=1` for
every library entry before enumeration.  Pair multiplication then preserves
these identities inductively, proving that every inversion used by the
corrected scripts is multiplicative inversion.
