---
rg: 2
id: graded-rings-with-degree-one-units-fail-the-k-budget-proof
kind: route
title: Homogeneous units have homogeneous inverses, so the grading is a skew Laurent structure, and K_1 detection passes down to the degree-zero part
target: graded-rings-with-degree-one-units-fail-the-k-budget
requires:
  - laurent-rational-symbols-block-fp-elementary-groups
  - rational-k1-detection-forces-infinite-order-unit-class
---

**Item 1.**
- **Homogeneous inverses.** Let `h in R_d` be a unit and `v = Σ_k v_k` its inverse. Then `h v_k in R_(d+k)`, and
  `Σ_k h v_k = 1 in R_0`. So `h v_(-d) = 1` and `h v_k = 0` for `k != -d`. Multiplying by `h^-1` gives `v_k = 0` for
  `k != -d`. So `h^-1 = v_(-d) in R_(-d)`.
- **`Q ⊆ R_0`.** Integers lie in `R_0`, since `1 in R_0` (`1 = Σ 1_k` and `1_k = 1 · 1_k` force `1_0 = 1`). For `m >= 1`,
  `m` is a homogeneous unit of degree `0`, so `1/m = m^-1 in R_0`, and `Q ⊆ R_0`.
- **`R_k = R_0 u^k`.** For `r in R_k`, `r u^-k in R_0`, since `u^-k in R_(-k)`.
- **`σ`.** `u R_0 u^-1 ⊆ R_0` and `u^-1 R_0 u ⊆ R_0`, so `σ` is an automorphism of `R_0`. `Q` is central in `R`,
  since integers are central and inverses of central units are central, so `σ` fixes `Q`.
- **Skew Laurent ring.** The map `R_0[t, t^-1; σ] -> R`, `b t^k -> b u^k`, is a ring homomorphism, since
  `u b = σ(b) u`. It is onto because `R_k = R_0 u^k`. It is injective because `b u^k in R_k` and `b u^k = 0` forces
  `b = 0`.

**Item 2.**
- `Q^x = {±1} × ⊕_p p^Z`. If `Q^x / K` is finitely generated, then `K ⊗ Q` has infinite dimension, so `K` is not
  finitely generated. This gives the first case.
- In the second case, the map `Q^x -> K_1(R)` factors through `K_1(R_0)`, since `Q ⊆ R_0`. So the image in
  `K_1(R)` is a quotient of the image in `K_1(R_0)`, and the latter is not finitely generated either.
- `laurent-rational-symbols-block-fp-elementary-groups` applies to `B = R_0` and `σ`, and gives both conclusions.

**The lamplighter.**
- `U_n` is the directed union of the unital subalgebras `⊗_(|k| <= m) M_n(Q) ≅ M_(n^(2m+1))(Q)`. `K_1` commutes
  with directed unions, and `det` identifies `K_1(M_r(Q))` with `Q^x`.
- Under the inclusion `a -> a ⊗ 1_(M_n ⊗ M_n)`, `det` is raised to the power `n^2`. The scalar `λ` has class `λ`
  at every stage. So `[λ] = 0` in `K_1(U_n)` exactly when `λ^(n^(2j)) = 1` for some `j`, that is `λ = ±1`.
- For `U_Q = colim M_(n!)(Q)` the same argument, with powers `(n+1)`, gives kernel `{±1}`.
- The grading: `U_n ⋊_α Z = U_n[u^±1; α]`, graded by the power of `u`. This is the case `R_0 = U_n`.

**Baumslag-type relations.** Let a presentation have generators of degrees `deg(u) = 1`, `deg(y) = 0`, and
degree `0` for the base generators, with every relation homogeneous. Then the free algebra grading descends to
the quotient ring, since the relation ideal is generated by homogeneous elements. `u` stays a unit of degree `1`
if `u^-1` is among the generators with degree `-1`. So item 1 applies.
