---
rg: 2
id: transverse-companion-conservation-proof
kind: route
title: Phase-rotate the transverse gauge and add the two positive deficits
target: transverse-companion-defects-pay-one-kill-completeness
requires:
  - transverse-d8-two-swap-corner-unitarity-collapses-to-one-path
---

# Phase-rotate the transverse gauge and add the two positive deficits

Replacing `W` by `iW` exchanges its skew part with `i` times its real part,
giving `(TCD2)`. The unitary identity `S^2+T^*T=I` gives `(TCD5)` after
conjugation by `U`. Functional calculus yields `(TCD7)`. The two scalar
choices in `(TCD8)` prove the complementary-sector selector escape.
