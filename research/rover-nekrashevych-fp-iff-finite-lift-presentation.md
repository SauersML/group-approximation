---
rg: 2
id: rover-nekrashevych-fp-iff-finite-lift-presentation
kind: claim
title: For a finite-state self-similar group G, V_d(G) is finitely presented if and only if the relator kernel of G is a finitely generated lift ideal
distinct_from:
  rover-nekrashevych-finite-presentation-criteria: that imports two sufficient conditions (G finitely presented, Scott; G contracting, Nekrashevych) and records that no characterization is known; this is a necessary and sufficient condition that contains both.
  contracting-srn-rational-stabilizers-are-fp: that concerns stabilizers of rational sets inside contracting groups; this concerns V_d(G) for arbitrary finite-state G, contracting or not.
  rover-nekrashevych-word-problem-reduces-to-self-similar-group: that bounds the word problem of V_d(G); this decides finite presentability of V_d(G).
---

**ESTABLISHED (proof in `rover-nekrashevych-fp-iff-finite-lift-presentation-proof`; Referee bh-ref-q12 2026-09-19: PASS, both directions checked line by line; see the Referee section).**

## Setting

- `X = {0, ..., d-1}` with `d >= 2`. Sections follow `g(xw) = g(x) g|_x(w)` and `(gh)|_x = g|_{h(x)} h|_x`.
- `G <= Aut(X^*)` is generated by a finite set `S` closed under sections and inverses (a finite-state, or
  automaton, group). `F` is the free group on `S` (the identity, if it lies in `S`, is sent to `1`).
- `ψ: F -> F ≀ Sym(X)` is the recursion homomorphism: on letters it is the wreath recursion of `S`, and on words it
  computes the root permutation and the word-level sections `w|_x`.
- `N = ker(F -> G)` is the **relator kernel**. Every `w in N` has trivial root permutation and `w|_x in N`.
- A subgroup `M <= F` is a **lift ideal** if it is normal and **lift-closed**: whenever `w` has trivial root
  permutation and every `w|_x` lies in `M`, then `w in M`. Intersections of lift ideals are lift ideals, so every
  set `R` generates a smallest lift ideal `⟨⟨R⟩⟩_lift`.
- `K_∞ = ⟨⟨∅⟩⟩_lift` is the set of words that become freely trivial: `w in K_∞` iff for some `n` the word `w` has
  trivial permutation on `X^n` and all its word-level sections at level `n` are freely trivial. `K_∞ ⊆ N`.

## Statement

**Theorem A.** `V_d(G)` is finitely presented if and only if `N = ⟨⟨R⟩⟩_lift` for some finite `R ⊆ N`.

When it holds, `V_d(G)` has the explicit finite presentation of Nekrashevych's proof of his Theorem 5.9, with his
nucleus relations (N) replaced by `L(r) = 1` for `r in R`:

- a finite presentation of the Higman–Thompson group `V_d` on generators `Σ`;
- generators `L(s)`, `s in S`, standing for `s` acting on the cone `0X^ω`;
- **(C)** commutation relations of `L(s)` with a finite generating set of the pointwise fixer of `0X^ω` in `V_d`,
  and of cone copies on disjoint cones `x ≠ y` and `v_1 ≠ v_2 in X^2`;
- **(S)** splitting relations `L(s) = h_s · L_{00}(s|_0) ··· L_{0,d-1}(s|_{d-1})`, with `h_s in V_d` permuting the
  cones `0x` as `s` permutes `X`;
- `L(r) = 1` for `r in R`.

**Consequences.**

1. **Scott (FP1) and Nekrashevych (FP2) are the two ends.** A finite presentation of `G` gives finite `R` with
   `N = ⟨⟨R⟩⟩ ⊆ ⟨⟨R⟩⟩_lift`. For contracting `G`, Nekrashevych's Theorem 5.9 and the necessity half show that `N`
   is a finitely generated lift ideal.
2. **When the recursion alone suffices.** If `N = K_∞`, then `V_d(G)` is finitely presented by `V_d`, (C) and (S)
   alone. Every relator of `G` then dies after finitely many free splittings.
3. **Invariance.** Whether `N` is a finitely generated lift ideal does not depend on the finite self-similar
   generating set `S`, since `V_d(G)` does not.
4. **Decidability of `K_∞`.** Word-level sections of `w` have length at most `|w|`, so the set of reduced words
   reachable from `w` is finite. Then `w in K_∞` iff every reachable word has trivial root permutation and the
   section graph on the nonempty reachable words is acyclic. `w in N` iff every reachable word has trivial root
   permutation. Both are implemented in `experiments/nonc-rn-fp-2026-09-17/autom.py`.

**First application.** `rover-nekrashevych-group-of-lamplighter-automaton-is-fp`: for the lamplighter automaton
`a = σ(a, b)`, `b = (a, b)`, the group `G ≅ Z/2 ≀ Z` is neither finitely presented nor contracting, and
`N = K_∞`. So `V_2(Z/2 ≀ Z)` is finitely presented, which neither FP1 nor FP2 covers.

## Attempts

- 2026-09-18 (w7-101): proved as stated.
  - Sufficiency reruns Nekrashevych's normal-form argument with an arbitrary finite self-similar `S`.
  - Necessity builds the fraction group `V_d(F/M)` for the lift ideal `M = ⟨⟨R⟩⟩_lift`. Its recursion is
    injective, so a table is trivial only if its labels are.
  - Not settled in the sources read: whether some finite-state `G` has `V_d(G)` not finitely presented. By Theorem A
    this needs a relator kernel that is not a finitely generated lift ideal.
  - By the Remark in the proof, finite section-closed `R` give finitely presented groups `V̂/⟨⟨L(R)⟩⟩` that
    approximate `V_d(G)` from above.

## Referee (bh-ref-q12, 2026-09-19): Theorem A PASS in both directions

The proof node `rover-nekrashevych-fp-iff-finite-lift-presentation-proof` was read line by line. The single
import is Nekrashevych's normal-form argument, arXiv:1312.5654, proof of Theorem 5.9, taken with an arbitrary
finite section- and inverse-closed generating set `S`. It was not re-read at source here; the node records reading
it on 09-18.

**Lift ideals (§1): correct.**
- (1a) `K_∞ = ∪K_n` is the least lift ideal.
- (1b) `T` is a subgroup, normal and lift-closed. This uses that elements of `M ⊆ N` act trivially on `X^*`, so
  `(ww')|_u = w|_u w'|_u`.
- (1c) Sections do not lengthen words, because `S` is section-closed.
- (1d) `ψ` descends because `M` is normal and `ψ(M) ⊆ M^X`. Injectivity is lift-closure.

**Sufficiency (§2–3): correct.**
- (2a) Induction on words, using (S), (5.13) and (5.14).
- (2b) `M_𝔑` is the kernel of `w ↦ L(w)𝔑`. It is lift-closed because `h_w = 1` when the root permutation is
  trivial.
- (2c) The last step is sound. `h_v ∈ V_d` equals the action of `W_v^(-1) ∈ G ≤ Aut(X^*)`, and an element of `V_d`
  that preserves word length is finitary. So some level `l` has trivial sections in `G`, i.e. `W_v|_u ∈ N`.
  `⟨Σ⟩ ≅ V_d` inside `V̂`, since it surjects from `V_d` and onto it.
- §3 follows.

**Necessity (§4): correct.**
- (4a) Neumann and (2c) give a finite `R ⊆ N` with `ker π = ⟨⟨L(R)⟩⟩`. The closure `R'` is finite by (1c).
- (4b) This is the standard fraction/cloning-system construction of `V_d` for a wreath recursion that need not be
  faithful. Confluence and well-definedness of the product use exactly that `ψ_Q` is a homomorphism.
- (4c) Checked: the property "some non-diagonal entry, or some diagonal entry with label `≠ 1`" is preserved by
  every single expansion. Non-diagonal entries stay non-diagonal, since `|v| ≠ |u|` or `v ≠ u` at equal length.
  Diagonal entries with label `≠ 1` are handled by injectivity of `ψ_Q`. So only all-`1` diagonal tables
  represent `1`.
- (4d) `Θ` respects `R_V`, (C), (S) and `L(R')`. (S) is the one-step expansion of `(0, s̄, 0)`.
- (4e) follows.
- **Where each hypothesis is used.** Necessity is where `S` finite and section-closed is used, through (1c).
  Sufficiency uses only a finite generating set with a chosen word-level recursion and finitely many relations
  (S).

**Consequence 4 (decidability): correct.** `w ∈ N` iff every reachable reduced word has trivial root permutation,
because `G` acts faithfully. `w ∈ K_∞` iff, in addition, the section graph on nonempty reachable words is acyclic.

**Literature.**
- No necessary-and-sufficient criterion for finite presentability of `V_d(G)` was found. Searches (09-19) covered:
  - Nekrashevych, arXiv:1312.5654, which is sufficient: `G` contracting;
  - Zaremsky, arXiv:2405.09722, sufficient;
  - Skipper–Witzel–Zaremsky, arXiv:1712.05361, which is about higher `F_n`;
  - Skipper–Zaremsky, arXiv:1709.06524;
  - Belk–Hyde–Matucci, arXiv:2407.03149.
- Only abstracts and cited statements were checked, not full texts.
- The necessity half is a lift-ideal rephrasing of the standard fact that `ker π` is normally generated by cone
  copies of relators. The characterization in these terms appears to be new. Credit Nekrashevych for the
  presentation and the normal form. No priority is claimed.

**Verdict: PASS.**
