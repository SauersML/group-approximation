---
rg: 2
id: atlas-m2-refutes-five-global-a4-compilers
kind: claim
title: One multiplicity-two frame refutes five global A4 compilers
invalidates:
  - a4-holonomy-gap-from-moment-simplex
  - nonhyperlinear-from-a4-plus-19243-classicalization
  - a4-holonomy-gap-from-packet-commutator-collapse
  - a4-holonomy-gap-from-normal-generator-compiler
  - a4-holonomy-gap-from-normal-generator-table-compiler
artifacts:
  - experiments/atlas_m2_global_a4_refutation_verify.js
  - experiments/atlas-m2-global-a4-refutation.json
  - research/artifacts/atlas-m2-global-a4-refutation-2026-08-30.md
distinct_from:
  atlas-m2-escape-refutes-six-a4-cycle-bridge-ceilings: that evaluates six scalar cycle and bridge ceilings; this refutes three global geometric or moment-rounding claims and two finite-state compiler interfaces.
  atlas-multiplicity-two-refutes-holonomy-coherence-gap: that evaluates the final two raw S3 coordinates; this propagates the same exact frame backward through five separately stated global prerequisites.
  atlas-q14-augmented-zero-set-has-x30-one-escape: that treats an augmented finite-factor system; this is a finite-dimensional refutation of packet-plus-collision claims and the frame does not satisfy q14.
---

**ESTABLISHED COUNTEREXAMPLE.**  Let

```text
F=401004080301c030 in GL_8(F_2)
```

be the exact multiplicity-two frame.  Let `G` be generated by `F` and the
repeated natural copy of `A8`, pass to `Reg(G)`, and use `lambda(F^-1)` as the
relative chart frame.  Direct-sum amplify this block if growing dimensions
are desired.  The established frame audit gives, exactly,

```text
all thirty directed A4 packet contexts are exact regular,
eta=0,                 q_19243=1.                     (M2G1)
```

The two raw S3 covariance opcodes are the nonidentity order-four elements

```text
o_21=8040281409070201,
o_12=8040201028143e29.
```

Regular-character orthogonality therefore gives

```text
a^2=b^2=2,                 a^2+b^2=4.                 (M2G2)
```

This one constant exact-regular sequence gives five direct contradictions.

1. **Thirty-one-moment rounding.**  Every packet word and collision is the
   identity, so its weighted moment functional is

   ```text
   sum_(j=1)^30 Re tau(w_j)+6 Re tau(q_19243)=36.      (M2G3)
   ```

   If `atlas-a4-alignment-moment-simplex-rounding` held, a classical alignment
   mixture would have the same thirty-one moments asymptotically and hence
   functional tending to `36`.  The established classical-mixture separator
   bounds that functional by `30`.  This is a contradiction.

2. **Collision-conditioned classicalization.**  The established shifted
   centralizer-product compiler applied to the constant exact packet sequence
   gives

   ```text
   max_j dist_2(U,D_j)=0.
   ```

   Together with `q_19243(U)=1`,
   `atlas-centralizer-products-19243-classicalize` would give
   `dist_2(U,B)=0`.  Every `V in B` has collision defect `sqrt(2)`, while `q_19243(U)=1`.
   Collision evaluation contains four conjugated second-chart letters, so
   eight-step telescoping gives

   ```text
   ||q_19243(U)-q_19243(V)||_2<=8||U-V||_2.
   ```

   Hence `dist_2(U,B)>=sqrt(2)/8`, uniformly under amplification, contradicting
   the asserted classicalization.

3. **Packet-commutator collapse.**  The established packet-energy comparison
   gives

   ```text
   4=a^2+b^2 <=(28363/1260) E_packet(U),
   E_packet(U)>=5040/28363>0.                          (M2G4)
   ```

   This contradicts the zero packet-commutator conclusion of
   `atlas-a4-relative-packet-commutator-collapse`.

4. **Exact normal-generator holonomy compiler.**  On `(M2G1)`, its collision
   readout `(A4-NG-COMP-2)` forces `pi(b)=1`.  The established A8
   normal-generator stability theorem then gives `tr(1-Q)=0`, and
   `(A4-NG-COMP-1)` forces `dist_2(U,lambda(A8)')=0`.  That makes both raw S3
   covariance defects zero, contradicting `(M2G2)`.  Hence
   `atlas-a4-full-packet-normal-generator-holonomy-compiler` is false.

5. **Approximate-table normal-generator compiler.**  On `(M2G1)`,
   `(A4-TABLE-1)` gives an exact multiplication table and `(A4-TABLE-2)` gives
   `u(b)=1`.  The collision involution normally generates `A8`, so the
   established table-trivialization theorem gives `u(x)=1` for every
   `x in A8`.  Then `(A4-TABLE-3)` again forces the relative frame into the
   full chart commutant, contradicting `(M2G2)`.  Thus
   `atlas-a4-full-packet-normal-generator-table-compiler` is false.

The frame has nonidentity q14.  None of these contradictions refutes a claim
which includes q14 or another added controller relation.