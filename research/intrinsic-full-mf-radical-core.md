---
rg: 2
id: intrinsic-full-mf-radical-core
kind: claim
title: Every group has a largest subgroup that is intrinsically invisible to finite-matrix limits
artifacts:
  - GroupApproximation/Sofic/FullMFRadicalCore.lean
  - GroupApproximation/Endpoint/ApproximationRadicals.lean
  - research/intrinsic-full-mf-radical-core-proof.md
---

# The intrinsic full-MF-radical core

There are two different ways a subgroup can be invisible to finite-matrix
limits.

For a subgroup `K ≤ G`, the ambient statement

```text
K ≤ Rad_MF(G)
```

says that every matrix-corona representation of `G` kills `K`.  The intrinsic
statement

```text
Rad_MF(K) = K
```

is stronger: every matrix-corona representation of `K` itself is trivial,
whether or not it extends to `G`.

Define

```text
Core_MF(G) = subgroup generated by all K ≤ G with Rad_MF(K)=K.
```

Then:

```text
Rad_MF(Core_MF(G)) = Core_MF(G),                       (1)
Core_MF(G) ≤ Rad_MF(G),                                (2)
f(Core_MF(G)) ≤ Core_MF(H) for every f : G --> H,      (3)
Core_MF(Core_MF(G)) = Core_MF(G),                      (4)
Core_MF(G / Core_MF(G)) = 1.                          (5)
```

Equation (1) uses the closure of full-radical groups under arbitrary
generation: every generating subgroup is already intrinsically invisible, so
their join is intrinsically invisible.  Equation (2) follows by mapping each
such subgroup into `G`.  For (3), the homomorphic image of a full-radical group
is full-radical.  Equation (4) is the idempotence of the construction, written
internally as

```text
fullMFRadicalCore (fullMFRadicalCore G) = top.
```

Equation (5) is the semisimple-quotient theorem.  If a subgroup of the
quotient were intrinsically full-radical, its preimage in `G` would be an
extension of the core by that subgroup and would therefore be intrinsically
full-radical.  Maximality puts the preimage back inside the core, so the
quotient subgroup is trivial.

The core is normal and characteristic.  It is the largest subgroup of `G`
that is intrinsically full-radical, rather than merely a collection of elements
that become invisible in the surrounding group.

Finally,

```text
Core_MF(G)=G  <=>  Rad_MF(G)=G.                       (6)
```

Thus the intrinsic core recognizes full MF collapse exactly, while retaining
strictly more internal information when the ambient radical is only partial.

The construction and equations (1)--(5) are formalized in
`GroupApproximation/Sofic/FullMFRadicalCore.lean` and re-exported by
`GroupApproximation/Endpoint/ApproximationRadicals.lean`.

DERIVATION
intrinsic-full-mf-radical-core via intrinsic-full-mf-radical-core-proof
