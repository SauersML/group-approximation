---
rg: 2
id: simple-ring-with-centralizer-compressing-automorphism
kind: claim
title: Some finitely generated simple ring with an MF elementary group has a centralizer-compressing automorphism
distinct_from:
  simple-ring-centralizer-compression-gives-xmf: That proves this ring statement implies (XMF); this is the existence question itself.
  mf-kazhdan-quotientless-mapping-torus-exact-radical: That is the group-level existence question; this is a ring-level sufficient gate whose two halves can fail independently.
  lef-kazhdan-group-without-finite-quotients-exists: That supplies simple coefficient rings with LEF elementary groups (half (G1)); it supplies no automorphism satisfying (G2).
---

There exist a finitely generated, infinite, simple unital ring `S`,
`sigma in Aut(S)`, a finitely generated unital subring `R <= S` and a unit `z`
such that

```text
(G1) EL_3(S) is MF;
(G2) sigma(R) <= R,  z in [S^x, S^x],  z commutes with R,
     and z fails to commute with some element of sigma^{-1}(R).
```

By `simple-ring-centralizer-compression-gives-xmf`, this implies `(XMF)`.
If moreover `EL_3(S)` is LEF, the resulting kernel `K = EL_3(S)` is a LEF
nontrivial Kazhdan group with `Rad_MF(K semidirectProduct Z) = K`, which
would refute the open mapping-torus form (EKL1) of
`exact-kazhdan-radical-kernel-cannot-be-lef`.

**The two halves fail independently.**
- (G1) without (G2): the crossed product rings `LC(X,F_q) semidirect Z` of
  `lef-kazhdan-group-without-finite-quotients-exists` have LEF elementary
  groups, and no compressing automorphism is known for them.
- (G2) without (G1): (G2) is purely algebraic and makes no reference to
  approximation.  Attempt 1 shows that (G1) together with an implementing
  locally matricial overring already forbids (G2), so the tension lies
  between the halves, not inside either one.

## Attempts

1. **Implemented automorphisms die (2026-09-17, obstruction).**  Suppose
   `sigma` is the restriction of `Ad(s)` for a unit `s` of an overring
   `T >= S`, and `T` embeds exactly on finite subsets into matrix algebras
   over fields.  Then (G2) fails.
   - Take a finite subset containing generators `r_i` of `R`, `s^{+-1}`, `z`,
     the words `s r_i s^{-1} = p_i(r)`, and an element `b = s^{-1} a s` with
     `a in R`, together with the nonzero element `zb - bz`.  Let `phi` be an
     exact model into `M_n(F)`.
   - Let `A` be the algebra generated by the `phi(r_i)`.  Then
     `phi(s) A phi(s)^{-1} <= A`, so equality holds by dimension.  Hence
     `phi(b) in A`, and `phi(z)` commutes with `A`.  So `phi(zb - bz) = 0`,
     which contradicts exactness.
   - *Invariant:* finite dimension of the models.  *Step where it dies:* the
     inclusion `phi(s) A phi(s)^{-1} <= A` is forced to be an equality, so the
     centralizer cannot shrink.
   - This kills inner automorphisms of locally matricial `S`.  It also kills
     every `sigma` whose skew-Laurent ring `S[t^{+-1}; sigma]` embeds into a
     locally matricial ring.  Whether particular crossed-product candidates,
     such as profinite actions of `BS(1,2)`, have this property is not
     checked here.
   - On the group side this matches the triviality that an LEF overgroup
     `GL_3(T)` would make `E` MF.  So any witness needs an automorphism that
     no locally matricial ring implements.
2. **Finite-order squaring units die (2026-09-17, remark).**  In the squaring
   case, if `w` has finite order `m`, then `m` is odd because
   `sigma(w) = w^2` has the same order.  So `w = (w^2)^{(m+1)/2}` commutes
   with `z`.  Hence `w` must have infinite order.
