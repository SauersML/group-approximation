---
rg: 2
id: piecewise-canonical-permutations-are-transposition-products
kind: claim
title: Every piecewise-canonical permutation of Z is a product of class transpositions, by an elementary tree-rewriting argument, so the answer to Kourovka 17.59 needs no groupoid input
distinct_from:
  ct-p-z-is-a-one-vertex-k-graph-full-group: that identifies CT_P(Z) with a k-graph topological full group and derives simplicity and F_infinity from Matui and Li; this proves the generation statement alone, by elementary arithmetic, with no groupoid input.
  ct-z-is-the-rcwa-group-fixing-the-nonnegative-integers: that answers Kourovka 17.59 using the k-graph identification as its only non-elementary input; this replaces that input.
  class-transposition-relations-present-ct-p-z: that asks for a presentation of CT_P(Z) by the transposition relations; this proves only that the transpositions generate the piecewise-canonical group, and says nothing about relations.
artifacts:
  - research/artifacts/gq-bh-bh-kourovka-problems.md
---

**ESTABLISHED** (lane proof, not reviewed). It is elementary: it uses only the Chinese
remainder theorem and the cocycle identity for canonical maps. No priority is claimed.

## Statement

A *box* is a class `r(m) = {r + tm : t ∈ Z}` with `0 ≤ r < m`. For boxes `A = r(m)` and
`B = s(n)`, the *canonical map* `κ(A,B) : A → B` is `r + tm ↦ s + tn`. A permutation
`g` of `Z` is *piecewise canonical* if there are box partitions `{C_i}` and `{D_i}` of
`Z` with `g|C_i = κ(C_i, D_i)`. Write `PC` for these permutations, and `CT(Z)` for the
group generated by the class transpositions `t(A,B)` of disjoint boxes. Then

    PC = CT(Z),

and for a finite set `P` of odd primes the same holds with every modulus `P'`-smooth,
where `P' = P ∪ {2}`. The inclusion `CT(Z) ⊆ PC` is immediate, so the content is
`PC ⊆ CT(Z)`.

## Setup

- **Canonical maps.** They satisfy `κ(B,C) κ(A,B) = κ(A,C)` and `κ(A,B)^(-1) = κ(B,A)`.
  The restriction of `κ(A,B)` to a sub-box of `A` is the canonical map onto its image,
  which is a box.
- **Trees.** The *`k`-split* of `r(m)` is its `k` children `r + jm (km)`, `0 ≤ j < k`.
  A *tree* is built from a root box by repeatedly splitting a leaf by a prime.
  - Its leaves partition the root.
  - Its *type* is the prime at the root together with the multiset of the primes at the
    other internal nodes.
  - A *natural partition* is the leaf set of a tree rooted at `Z`.
  - For a tree `u` rooted at `Z` and a box `N`, `κ(Z,N)(u)` is `u` placed in `N`.
- **Equivalence.** For partitions `P` and `Q`, write `P ≈ Q` if some `h ∈ CT(Z)` maps
  every piece of `P` canonically onto a piece of `Q`. This is an equivalence relation
  by the cocycle identity.
- **Refinement.** `u^x` is `u` with the tree `x` placed in every leaf.

## Lemmas

1. **Natural representatives.** Each `g ∈ PC` maps some natural partition canonically
   onto a natural partition.
   - Refine `{C_i}` to `Π_L`, all residues mod `L = lcm(m_i)`; it is natural. The
     images form a box partition `D'`.
   - Split each piece `Y` of `D'` fully down to `L' = lcm` of the moduli of `D'`, and
     split its preimage by the same integer. The target becomes `Π_{L'}`, the source
     becomes a refinement of `Π_L`, and both are natural.
   - Splitting by `k = ab` is splitting by `a` and then by `b`.
2. **Free bijection.** If `P ≈ Q`, then every `g` mapping `P` canonically onto `Q` lies
   in `CT(Z)`.
   - With `h` witnessing `P ≈ Q`, `σ = g h^(-1)` permutes the pieces of `Q`
     canonically.
   - `π ↦` (the canonical permutation of the pieces by `π`) is a homomorphism from
     `Sym(Q)` to `PC` that sends transpositions to class transpositions.
3. **Transport.** For a box `N`, conjugating by `κ(Z,N)` and extending by the identity
   is an injective homomorphism `φ_N` with `φ_N(t(A,B)) = t(κ(Z,N)A, κ(Z,N)B)`. So if
   `u ≈ v`, a tree with pattern `u` at a node `N` is `≈` to the tree with `v` at `N`,
   where the subtree at each leaf of `u` moves along to its image leaf.
4. **Exchange.** Two trees rooted at `Z` of the same type are `≈`.
   - If two nodes `N` and `N'` are not in ancestor relation, then `t(N,N')` is a class
     transposition. It swaps their subtrees canonically, by the cocycle identity.
   - Such swaps bring every tree to a *chain*: the root has one internal child, at
     position 0, and so on down, with the primes in a fixed order.
   - By induction on the number of internal nodes: move a node `Q` of the first prime to
     a root child. If the root has no leaf child, first swap another root child with a
     leaf under `Q`. Then swap every other internal root child into a leaf of `Q`, and
     recurse inside `Q`.
5. **Commutation.** For primes `p ≠ q`, a node split by `p` whose children are all split
   by `q` gives the same partition as the node split by `q` with all children split by
   `p`. Both are the full `pq`-split, and every grandchild subtree stays on its own box.
   - As a change of type at a non-root node, going from `q` over `q` copies of `p` to
     `p` over `p` copies of `q`: remove one `q` and `q` copies of `p`, add one `p` and
     `p` copies of `q`. For `q = 2` this removes `{2, p, p}` and adds `{p, 2^p}`.
   - At the root: root `q` over `q` copies of `p` becomes root `p` over `p` copies of
     `q`, and conversely.
6. **Cancellation.** If `u^x ≈ v^x`, then `u ≈ v`.
   - Let `h` witness this. Compose it with a canonical permutation `σ` of the leaves of
     `v^x` (Lemma 2) so that `σh` sends the `λ`-th leaf of `x` under the leaf `u_ℓ` to
     the `λ`-th leaf of `x` under `v_(π ℓ)`.
   - By restriction and uniqueness of canonical maps, `σh|u_ℓ = κ(u_ℓ, v_(π ℓ))`.
7. **Base.** For an odd prime `p`, the `p`-split of `Z` is `≈` to a binary tree with `p`
   leaves.

## Proof of Lemma 7

Write `2` and `p` for the node types. Let `a` be the `p`-split, `b` the `2`-split, and
`x = b(a, b)`, a tree of type `(2, {p, 2})`. Let `u` be the `p`-split of `Z`, and `v` a
binary tree with `p` leaves.

1. `u^x` has type `(p, {2p·2, p·p})`, that is, root `p` and non-root multiset
   `{2,…,2 (2p times), p,…,p (p times)}`. Its root children are the roots of the
   `x`'s, all split by `2`. Root commutation (Lemma 5) gives type `(2, {p·2, (p+2)·p})`.
2. By Lemma 4 choose a tree of this type in which some non-root `2`-node has two
   `p`-children, then commute there: type `(2, {(2p−1)·2, (p+1)·p})`.
3. Repeat once: type `(2, {(3p−2)·2, p·p})`.
4. `v^x` has type `(2, {(p−2)·2 + p·{2, p, 2}}) = (2, {(3p−2)·2, p·p})`. Lemma 4 gives
   `u^x ≈ v^x`, and Lemma 6 gives `u ≈ v`.

Only the moduli `2` and `p` occur.

## Proof of the theorem

- **Every natural partition is `≈` to a binary one.** Replace the `p`-split at an odd
  node by a binary tree with `p` leaves (Lemmas 7 and 3), and induct on the number of
  odd nodes.
- **All binary partitions of one size are `≈`.** They have the same type (Lemma 4).
- **Conclusion.** Let `g ∈ PC`. Lemma 1 gives natural `C`, `D` with `g` mapping `C`
  onto `D`, so `C ≈ D`. Lemma 2 then gives `g ∈ CT(Z)`.
- **Smooth moduli.** Every step only splits by primes already present, together with
  `2`. So the proof stays inside `CT_P(Z)` when all moduli are `P'`-smooth.

## Remarks

- **Kourovka 17.59.** Steps 1–3 of `ct-z-is-the-rcwa-group-fixing-the-nonnegative-integers`
  put `RCWA^+(Z)` inside `PC`, so with this node `CT(Z) = RCWA^+(Z)` is elementary. The
  k-graph node is still needed for simplicity and `F_∞` (Kourovka 17.61), not for the
  identification.
- **Relations.** This does not settle `class-transposition-relations-present-ct-p-z`.
  The rewriting uses exchanges, commutations and cancellations, and there it would still
  have to be shown that the loops they create are null in the presented group.
- **Why cancellation is essential.** Swaps never change the root prime, and on a fixed
  partition a `p`-divisible modulus cannot disappear. So the `p`-split and a binary tree
  of size `p` are not related by moves at their own size; the refinement by `x` is what
  makes room.
- **Superseded content.** The first landed version (18ebf78dc) recorded a failed
  piece-count induction. That gap is not needed by this argument.
- **Lean (09-19).** `GroupApproximation.Kourovka1759.kourovka_17_59` proves `CT(Z) = RCWA^+(Z)`
  with Mathlib only and no hypotheses (11 modules in `GroupApproximation/Kourovka1759/`, built
  green under `warningAsError`, no `sorry`). `Palomar/BooneHigmanSolution.lean` uses it for the
  config's `kourovka_17_59`. The formal route follows this node, with three changes.
  - The relation `≈` is on multisets of boxes (`MEq`). A *type* `Ty P k M` says that `P` is `≈`
    to the chain with root arity `k` and lower arities `M`. Lemma 4 becomes: splitting any leaf
    of a chain equals, up to at most three exchanges, inserting that arity below the root.
  - Lemma 5 is one equality of multisets: the `k`-split refined by `q` and the `q`-split refined
    by `k` are both the classes mod `kq`.
  - Lemma 1 is replaced by a two-level refinement. Split class `j` mod `L` and its image box
    `B_j` by `N/m(B_j)`. The target is then the partition mod `N`, and both sides have a type
    and the same number of pieces.

## Lesson for general BH

Showing that an explicitly generated transposition group is the whole topological full
group of a box groupoid is a finite type calculus, not a simplicity argument. Swaps
(transpositions) move subtrees and preserve the type. The factorization square changes the
type by `(1 split by p) ↔ ((p−1) splits by 2)`. Stable refinement cancels. So the
transposition group is everything once the square relations kill the type count modulo
size. That is the elementary shadow of `H_0 ⊗ Z/2 = 0` (a colour with 2 edges gives
`[B] = 2[B]`) and `H_1 = 0` (the square bounds the only 1-cycle).

For host design this means an explicit group such as `CT_P(Z)` can be certified equal to a
full-group host by counting alone, and then inherits the host's simplicity and finite
presentation. Two cautions:
- At a fixed size, swaps never change the root prime or remove a `p`-divisible modulus,
  so the room always comes from refinement, the Hilbert-hotel step.
- Such a check does not give relations. Finite presentation still has to come from the
  groupoid, as in `ct-p-z-is-a-one-vertex-k-graph-full-group`.

## Referee (ref-k1761, 2026-09-19): PASS, for both PC = CT(Z) and the P'-smooth version PC_P = CT_P(Z)

This is an independent adversarial check of every lemma and of the theorem. It completes the chain behind
`ct-p-z-is-finitely-presented-by-position-shifting`, whose only unrefereed input this was.
- **CT ⊆ PC.** A class transposition `t(A,B)` is canonical on `A`, on `B`, and on the boxes of
  the complement. Kohl's formula `n ↦ (m₂n + m₁r₂ − m₂r₁)/m₁` is exactly `κ(A,B)`. PC is a group:
  intersections of boxes are boxes or empty (CRT), so a common refinement of the middle partition
  pulls back and pushes forward along canonical maps. The node calls this "immediate"; that is
  fine, but it is the step that uses CRT for non-coprime moduli.
- **Lemma 1: correct.**
  - `Π_L` is natural.
  - Images of sub-boxes under canonical maps are boxes.
  - Splitting each piece `s(n)` of `D'` by `L'/n`, and its preimage by the same integer, keeps
    both sides natural.
  - A composite split is a tree of prime splits, and it has the same leaf set.
- **Lemma 2: correct.**
  - `σ = gh⁻¹` permutes the pieces of `Q` canonically, by the cocycle identity.
  - The map from `Sym(Q)` is a homomorphism sending transpositions to the class transpositions
    `t(Q_i, Q_j)`.
- **Lemma 3: correct.** `φ_N` is an injective homomorphism. `φ_N(t(A,B))` is the class
  transposition of the transported boxes. Canonical maps carry a placed subtree to the same pattern
  placed in the image box.
- **Lemma 4: correct.** For non-ancestor nodes `N` and `N'`, `t(N,N')` exchanges their subtrees and
  preserves type. The normalization to the chain works:
  - put a node `Q` of the first prime at a root child, which is always possible since the root has
    at least 2 children;
  - swap every other internal root child into a leaf of `Q`, since `Q` always has leaves;
  - move `Q` to position 0;
  - recurse inside `Q`.

  The chain depends only on the type.

  *Expository precision:* the clause "if the root has no leaf child, first swap …" is never needed.
  The steps above always apply.
- **Lemma 5: correct.** Both orders give the full `pq`-split of the node: the grandchildren are
  `a + (j+pl)m ≡ a + (j′+ql′)m (mod pqm)`. The partition is literally equal. The type bookkeeping
  (remove one `q` and `q` copies of `p`, add one `p` and `p` copies of `q`) is right at both
  non-root nodes and the root.
- **Lemma 6: correct.**
  - `|u| = |v|`, because `u^x` and `v^x` have equal piece counts.
  - `σ` exists and is a canonical permutation of the pieces of `v^x`, so it lies in CT by Lemma 2.
  - `σh` agrees with `κ(u_ℓ, v_{πℓ})` on every sub-box `x_λ(u_ℓ)`, by uniqueness of canonical maps
    between two boxes. So it equals `κ(u_ℓ, v_{πℓ})` on `u_ℓ`.
- **Lemma 7: correct.** I recomputed every type.
  - `u^x`: `(p, {2p·2, p·p})`.
  - Root commutation: `(2, {p·2, (p+2)·p})`.
  - The two non-root commutations give `(2, {(2p−1)·2, (p+1)·p})`, then `(2, {(3p−2)·2, p·p})`.
    Each needs a tree of the current type with a non-root 2-node over two `p`-nodes; one exists,
    since the counts are at least 3 and 2.
  - `v^x`: `(2, {(p−2+2p)·2, p·p})`, which matches.
  - The chain alternates equalities of partitions with Lemma 4 steps. Lemma 6 then applies.
- **Theorem: correct.**
  - Replacing each odd node through Lemma 3 lowers the number of odd nodes by one and keeps the
    leaf count.
  - All binary trees with `n ≥ 2` leaves have type `(2, {(n−2)·2})`; the case `n = 1` is the
    identity.
  - `C` and `D` from Lemma 1 have equal size, so `C ≈ D`, and Lemma 2 applies.
- **Smooth version: correct.** Every split, transport box, swap and canonical permutation uses only
  moduli built from primes already present, together with 2. `L` and `L′` are `P′`-smooth when `g`'s
  moduli are. So `PC_P = CT_P(Z)`, the group generated by class transpositions of `P′`-smooth
  moduli, as in `classTranspositionGroupOver P`.

**Consequence:** `ct-p-z-is-finitely-presented-by-position-shifting` now rests only on refereed
nodes. Kourovka 17.61 (CT_P(Z) finitely presented for finite P) is established elementarily,
modulo the two standard groupoid facts of Fact O step O4: Gabriel–Zisman, and vertex groups of
presented connected groupoids.
