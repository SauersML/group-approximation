---
rg: 2
id: atlas-m2-escape-refutes-six-a4-cycle-bridge-ceilings
kind: claim
title: One multiplicity-two escape refutes six A4 cycle and bridge ceilings
artifacts:
  - experiments/atlas_a4_repeated_module_escape_verify.py
  - experiments/atlas_m2_six_ceiling_refutation_verify.js
  - experiments/atlas-m2-six-ceiling-refutation.json
distinct_from:
  atlas-m2-escape-kills-collision-only-scalars: that evaluates the hub trace, unweighted Dirichlet, and root-ellipse frontiers; this evaluates every weighted cycle root and both later parabolic bridge energies.
  atlas-multiplicity-two-packet-collision-hub-escape: that proves the packet and collision hypotheses and moves the hub; this records all additional exact opcodes and their six scalar consequences.
---

Put `rho_2(g)=rho_4(g) tensor I_2` and, in row-byte little-endian
hexadecimal convention, put

```text
F=401004080301c030,
F^-1=8280414010200c04,                 |F|=51.
```

The established claim
`atlas-multiplicity-two-packet-collision-hub-escape` proves that the two
charts defined by `rho_2` and `F^-1 rho_2(-) F` satisfy every directed A4
packet context exactly and make collision `q_19243` the identity.  Let `G`
be the finite subgroup of `GL_8(F_2)` generated by `F` and `rho_2(A8)`, pass
to `Reg(G)`, and use `lambda(F^-1)` as relative frame.  Take `n` direct-sum
copies to obtain a sequence of growing dimensions without changing any
normalized trace.

For each displayed element put

```text
o_g=F^-1 rho_2(g) F rho_2(g)^-1.
```

Exact binary elimination and multiplication give:

| `g` | `rho_2(g)` | `o_g` | `|g|` | `|o_g|` | `rank(o_g-I)` |
|---|---|---|---:|---:|---:|
| `h=h6_2` | `8040080428140201` | `8040281401033221` | 3 | 7 | 6 |
| `k=k_0` | `8040201002010a05` | `8c4024100a05f030` | 3 | 7 | 6 |
| `z` | `2010a05008040201` | `f404c80848540201` | 3 | 15 | 6 |
| `b` | `8040a05008040201` | `8040a05058440201` | 2 | 4 | 3 |
| `c=hk` | `8040020122110a05` | `b04038143cc0cf1e` | 4 | 12 | 6 |
| `a=zb` | `a050201008040201` | `a454281808040201` | 2 | 4 | 3 |
| `t01` | `8040201008040a05` | `8f40251008040a05` | 2 | 4 | 4 |
| `t12` | `8040201028140201` | `8040201028143e29` | 2 | 4 | 4 |
| `t23=b` | `8040a05008040201` | `8040a05058440201` | 2 | 4 | 3 |
| `t30=c^-1ac` | `8241201008040201` | `4182102008040201` | 2 | 4 | 3 |
| `t21` | `8040281408040201` | `8040281409070201` | 2 | 4 | 4 |

Every opcode in the table is nonidentity.  The normalized character of a
nonidentity group element in `Reg(G)` is zero.  Hence, on the amplified
constant sequence,

```text
D_01=D_12=D_23=D_30=1,
delta(h)^2=delta(k)^2=delta(z)^2=delta(b)^2=2,
delta(c)^2=delta(a)^2=2.                              (M2-6-1)
```

These values give six contradictions.

1. The anisotropic four-cycle expression is
   `24(1+1+1)+1=73`, not less than `1/256`.
2. The optimized constraints imply separately
   `c_01>=4`, `c_23>=4`, and `c_30>=16`; its expression is therefore at
   least `4+1+4+16=25`, not less than `1/256`.
3. The joint weighted expression is at least `q`, whereas

   ```text
   lambda
    =3pq/(p+q+sqrt(p^2-pq+q^2))
    <3q,
   ```

   so `lambda/256<q` for every `p,q>0`.
4. The transverse energy is `x_30^2=D_30=1`, not less than `1/2`.
5. The parabolic bridge energy is
   `E_PC3=2(2)+2(2)+2+2=12`, not at most `1/6`.
6. The two-holonomy energy is `E_2HOL=2(2)+2=6`, not at most `1/3`.

Thus the same exact finite packet-and-collision model refutes
`atlas-a4-anisotropic-four-cycle-ceiling`,
`atlas-a4-optimized-weighted-four-cycle-ceiling`,
`atlas-a4-joint-weighted-cycle-ceiling`,
`atlas-a4-t30-energy-ceiling-below-q14-wall`,
`atlas-a4-parabolic-c3-bridge-energy-ceiling`, and
`atlas-a4-two-holonomy-reflection-energy-ceiling`.