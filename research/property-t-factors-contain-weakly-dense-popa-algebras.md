---
rg: 2
id: property-t-factors-contain-weakly-dense-popa-algebras
kind: claim
title: Some II_1 factor with property (T), for instance L(G_X), contains a weakly dense Popa algebra
distinct_from:
  separable-ii1-factors-have-weakly-dense-full-free-group-cstar: that gives weakly dense residually finite-dimensional (non-simple) subalgebras in every separable II_1 factor, settling the QD clause; this is the simple Popa clause.
  kazhdan-group-factor-popa-corners-have-vanishing-trace: that is a necessary scale condition on any example in a property (T) group factor; this is the existence question.
artifacts:
  - research/artifacts/sk-free-5-brown-q3-2026-09-13-part1.md
---

**ESTABLISHED (2026-09-14)** by route `property-t-factor-popa-algebras-via-weakly-dense-combs`, through Theorem P, `separable-ii1-factors-have-weakly-dense-popa-combs` (sk-popa-input2; sk-verify-20 PASS-WITH-FIXES, fixes merged). Every separable II_1 factor contains a weakly dense Popa algebra, property (T) factors such as `L(G_X)` and `L(SL_3(Z))` included. This is the second clause of Brown's question (arXiv:math/0111286v3, §Questions, item 3, read from source): "Can a free group factor or a II$_1$ factor with property T contain a weakly dense, QD C$^*$-subalgebra? How about a Popa algebra?"

**Scope** (sk-verify-20 §3, F2). The answer is to the question as printed. Brown–Dykema's extra property (II), WEP of `M` relative to `A`, is not claimed, and it cannot hold in a separable II_1 factor not embeddable in `R^ω`.

**Definition** (Brown–Dykema, arXiv:math/0207005, introduction): a Popa algebra is a unital simple C*-algebra `A` such that, for every finite `𝔉 ⊆ A` and `ε > 0`, there is a nonzero finite-dimensional subalgebra `B ⊆ A` with unit `e` satisfying `‖[x, e]‖ < ε` and `dist(exe, B) < ε` for `x ∈ 𝔉`.

**Known.**
- Every McDuff factor contains a weakly dense Popa algebra (Brown, Theorem `arbitraryMcDuff`).
- So do the interpolated free group factors `L(F_s)`, `1 < s < ∞` (Brown–Dykema).
- The QD clause holds for every separable II_1 factor (`separable-ii1-factors-have-weakly-dense-full-free-group-cstar`, unreviewed).
- For property (T) the Popa clause was open, as far as the bounded search in the artifact goes, until Theorem P settled it for every separable II_1 factor. Theorem P was not found in a bounded search (sk-verify-20 §5).

**Candidate.** `L(G_X)`, the property (T) factor of the note. `G_X` is generated by two locally finite tower unit groups, so `L(G_X)` is generated by two hyperfinite subalgebras.

## Attempts
- **Residually finite-dimensional copies (sk-free-5, 2026-09-13).** The weakly dense copy of `C*(F_∞)` satisfies Popa's approximation exactly on the corners `q_j`. It dies at simplicity: `C*(F_∞)` has many finite-dimensional quotients, and no simple QD hull of it inside `M` was found.
- **Scale constraint (sk-free-5).** By `kazhdan-group-factor-popa-corners-have-vanishing-trace`, Popa corners serving approximate Kazhdan generators at tolerance `η` have trace `≤ 4ε_S^{-2}(4η+2δ)²`. This is not an obstruction, since Brown–Dykema's corners in the full factor `L(F_s)` also vanish. It is a constraint on any construction.
- **Property (T) bimodule argument (sk-free-5).**
  - The vector `(p ⊗_B p)/τ(p)^{1/2}` in `L²M ⊗_B L²M` lies in a multiple of the coarse bimodule, which has no central vectors for diffuse `M`. It is almost central at relative scale `η` for the finite set.
  - The Kazhdan set of `M` need not lie in `A`. Replacing it by 2-norm approximants costs `‖λ_s − y_s‖_2/τ(p)^{1/2}`, which diverges on the vanishing corners above.
  - It dies at weak density. An obstruction would need a Kazhdan set inside `A`, or a relative-(T) inclusion `A''`-free in its hypotheses.
- **Tower structure of `G_X` (idea, not attempted).** `G_X = ⟨L_y, L_{y′}⟩`, with `L_y = ⋃_n GL_3(A(𝒫_n))` (route of `subshift-elementary-groups-are-character-rigid`).
  - Brown–Dykema's route uses the free product structure of `L(F_s)` and residual finite-dimensionality of full amalgamated free products of matrix algebras.
  - An analogue for `L(G_X)` would need the two hyperfinite algebras in a position whose full C*-hull keeps finite-dimensional representations on vanishing corners while becoming simple.
- **Coarse-bimodule rigidity, combs and a dead mechanism (sk-popa-lgx, 2026-09-13; artifact `research/artifacts/sk-popa-lgx-2026-09-13-part1.md`).**
  - *Scale-free constraints:*
    - `kazhdan-generators-admit-no-norm-popa-corners`: no Popa corner serves exact Kazhdan generators below tolerance `ε/4`, and every Popa algebra `A ⊆ L(Γ)` has `max_s dist(λ_s,A) ≥ ε/6`;
    - `orthogonal-popa-corners-for-kazhdan-approximants-small-trace`: orthogonal Popa corners for approximants carry total trace `O(defect²)`, and the defect concentrates on every corner.
  - *Reduction:* `finite-dimensional-combs-generate-popa-algebras` (full commuting finite-dimensional teeth that separate points give a Popa algebra), with the open `weakly-dense-popa-combs-exist-in-property-t-factors` and route `property-t-factor-popa-algebras-via-weakly-dense-combs`.
  - *Dead:* Brown's basic construction and all its unitary twists always give property Gamma weak closures (artifact §2), so they cannot reach property (T).
  - *Open:* the tooth-tree construction (artifact §3). It dies nowhere yet, and residual finite-dimensionality of the amalgamated stages is missing.
