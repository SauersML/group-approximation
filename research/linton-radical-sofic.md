---
rg: 2
id: linton-radical-sofic
kind: claim
title: The maximal rationally perfect radical of a one-relator group is sofic
distinct_from:
  linton-shell-sofic: that asks for soficity of `R ⋊ F₀`, which is equivalent to the whole of Pestov 4.9; this asks only for soficity of the normal factor `R`, which by the failure of semidirect closure is strictly weaker and does not suffice on its own.
  linton-radical-free-action-sofic: that is a statement about the action of the free complement on `R` and says nothing about whether `R` is sofic; this is the statement about `R` alone. Together they give the shell; separately neither does.
  linton-radical-abelianization-is-truncated-group-ring: that computes `R_ab` exactly and is closed; this asks for an approximation property of `R` itself and is open. The computation constrains the answer without deciding it.
  one-relator-fox-magnus-descent-to-derived-linton-radical: that proves the quotient `G/R'`, and in particular `R/R'`, sofic by an explicit embedding; this asks for soficity of the full nonabelian radical, including the derived core that the Magnus map kills.
  residually-rationally-solvable-one-relator-sofic: that closes the case `R = 1`, where the radical is trivial and there is nothing to ask; this is the question in the remaining case `R ≠ 1`, which that theorem does not touch.
  one-relator-groups-sofic: that is Pestov's question about the whole group; this is about one normal subgroup of it, and by the failure of semidirect closure establishing this would not establish that.
artifacts:
  - research/artifacts/one-relator-soficity-audit-2026-08-17.md
---

OPEN.  For every one-relator group `G = F/<<w>>`, is the maximal rationally
perfect normal subgroup

    R = G_Q^(ω) = <<r>>_G      (Linton, arXiv:2407.09272, Theorem 1.1)

sofic?

This is one of the two halves of `linton-shell-sofic-via-sofic-action`, the
other being [[linton-radical-free-action-sofic]].  It is **not** sufficient on
its own: [[sofic-semidirect-product-closure-fails]] means a sofic `R` and a free
`F₀` still do not give a sofic `R ⋊ F₀`.

## What is known about `R`

- It is normally generated in `G` by a single element `r`, with `|r| <= |w|`.
- `R = 1` exactly when `G` is residually rationally solvable, in which case
  [[residually-rationally-solvable-one-relator-sofic]] closes everything; so the
  interesting case is `R ≠ 1`.
- In that case, in the torsion-free regime that a counterexample must inhabit,
  Linton shows `R` is **infinitely generated**.
- `R_ab ≅ (Z/kZ)[G/R]` by
  [[linton-radical-abelianization-is-truncated-group-ring]].  So `k = 1` makes
  `R` perfect, and for `k > 1` the abelianization is `k`-torsion.
- More strongly,
  [[one-relator-fox-magnus-descent-to-derived-linton-radical]] embeds the whole
  quotient `G/R'` in a finite-lamp wreath product and makes the shell quotient
  `E/R'` residually finite.  Thus the first derived core `R'`, not merely the
  abelianization, is now the exact boundary of first-order Fox information.
- `R` inherits local indicability from `G` whenever `G` is torsion-free, since
  local indicability passes to subgroups.

## Attempts

1. **Reduce to the derived subgroup.**  *The first-order quotient is now
   closed, but the radical is not.*  For `k > 1`, `R/R'` is amenable, so `R'`
   sofic would give `R` sofic by sofic-kernel/amenable-quotient permanence.  In
   the `k = 1` regime `R = R'`, so even this reduction is vacuous.  The
   Fox--Magnus theorem cited above proves substantially more than the old
   abelianization observation, but it deliberately does not prove `R'` sofic.
2. **Directed union of finitely generated pieces.**  *Deferred for want of a
   structure theorem.*  Soficity passes to directed unions, so it would suffice
   that every f.g. subgroup of `R` lie in a sofic subgroup of a class closed
   under such unions.  `R` is a subgroup of a one-relator group, so its f.g.
   subgroups inherit local indicability and the Freiheitssatz constraints — but
   nothing in that package produces a sofic certificate, and the natural
   candidates (free, residually finite) are exactly what fails for a rationally
   perfect radical.
3. **Read `R` off the Magnus decomposition.**  *Completed only to first
   order.*  The mod-`k` relation module now computes the exact kernel `R'` and
   the whole quotient `G/R'`.  What remains is a genuinely nonabelian
   second-order model of `R'`; a proof here must still explain why the
   nonamenable free-edge obstruction of [[magnus-staggered-chain-sofic]] is
   harmless beyond the relation module.
4. **Reality check.**  Recorded because this claim reads more tractable than it
   is.  No technique is known that certifies soficity of an infinitely
   generated, rationally perfect, locally indicable group from the fact that it
   is normally generated by one element in a one-relator group.  The honest
   status is that this is a *component* of the open question, isolated for
   clarity.  First-order Fox calculus is no longer a prospective method for
   it: its exact kernel has now been computed as `R'`.
