---
rg: 2
id: endpoint-external-subgroup-has-native-s3-retract
kind: claim
title: The endpoint Whitehead and its two native external roots retract onto the surviving S3
artifacts:
  - experiments/endpoint_external_s3_retract_audit.py
  - experiments/endpoint_external_subgroup_relator_audit.py
  - research/endpoint-external-native-s3-retract-proof.md
distinct_from:
  endpoint-external-order-four-is-conditional-s3-breaker: that finds incompatible order four once the endpoint gauge is identified with the native Weyl gauge; this proves that every relation internal to the literal subgroup generated by the endpoint and the two roots still permits the endpoint gauge to be identity and the native gauges to form S3.
  native-weyl-return-gauges-have-s3-model: that constructs the three-reflection gauge model from the literal Weyl factorization; this locates a split S3 quotient of the actual elementary subgroup containing the endpoint Whitehead, so it fences all ordinary relations in that subgroup at once.
  eight-external-returns-have-no-whitehead-gauge-lock: that gives a scoped occurrence-interface model for the prefix Whiteheads and eight returns; this is an all-word algebraic retraction for the smaller actual subgroup generated by the endpoint Whitehead and the active opposite-root pair.
---

**ESTABLISHED ALL-WORD FENCE.**  In the literal elementary group put

```text
P=What_(8,2;2),             n=x_78(1),             m=x_87(1),
G=<P,n,m>,                  N=<n,m>.                  (ESR1)
```

Then `N` is the native copy of `GL_2(F_2)=S_3`, and there is a split
retraction

```text
rho:G -> N,                rho(P)=1,  rho(n)=n,  rho(m)=m. (ESR2)
```

Indeed, let

```text
e=s_00t_00,                    r=1-e.                  (ESR3)
```

The scalar matrix `r I` commutes with `P,n,m`.  The endpoint Whitehead is
the identity on this reducing corner,

```text
(rI)P(rI)=rI,                                           (ESR4)
```

while the two constant roots restrict to

```text
n_r=rI+rE_78,                 m_r=rI+rE_87.            (ESR5)
```

Since `r` is the identity of the nonzero corner `rRr`, the two-by-two
blocks in `(ESR5)` are the standard generators

```text
[[1,1],[0,1]],               [[1,0],[1,1]]             (ESR6)
```

of `GL_2(F_2)`.  Thus corner restriction maps `G` onto `S_3`, kills `P`,
and is injective on `N`; composing with its inverse on `N` gives `(ESR2)`.

Consequently **no collection of ordinary word relations inside
`<P,n,m>` can close the endpoint/native occurrence seam**.  Every such
relation has the exact separate-chart reservoir solution

```text
H=1,              E=(2 3),              F=(1 3),      (ESR7)
```

with `E,F` generating the surviving `S_3`; the native Weyl gauge remains
`D=EFE=(1 2)`.  In particular the order-four rows
`(HE)^4=(HF)^4=1` and the order-six row involving `D` cannot remove this
solution.

An MSI-only faithful-normal-form enumeration through freely reduced length
fourteen supplies a useful calibration.  Beyond the Coxeter rows it finds
the first cyclic relator with odd endpoint parity at length thirteen,

```text
P n P n m P m n P n m P m = 1.                       (ESR8)
```

But setting `P=1` reduces `(ESR8)` to

```text
n n m m n n m m=1,                                   (ESR9)
```

so it is exact in `(ESR7)`.  The odd parity does rule out an *external
central-twist* assignment for `H`, but the actual invariant-corner
retraction explains why the identity assignment survives not merely to
length fourteen but through every relation of `G`.

Therefore the next possible breaker must use data outside this subgroup or
must authenticate the missing same-carrier occurrence bridge between `P`
and the constant Weyl word.  Searching for additional short relations in
`<P,n,m>` cannot succeed.  This statement does not decide whether adding
the full endpoint `W/M` actor or all external-return occurrences supplies
such a bridge.  No Property `(T)`, Kazhdan input, trace hypothesis,
stability theorem, or numerical approximation is used.

DERIVATION
endpoint-external-native-s3-retract-proof

