---
rg: 2
id: full-completeness-opposite-gl5-fusion-proof
kind: route
title: Separate coefficient direct-finiteness from a three-copy finite occurrence fusion
target: full-completeness-opposite-menu-has-finite-gl5-fusion
requires:
  - depth-two-cousin-loop-has-finite-ut5-fusion-model
  - private-arm-opposite-cell-has-finite-m3-model
---

For the coefficient no-go, `L_(s_0):E->E` has left inverse `L_(t_0)` by
`(FCO1)`.  Finite dimension makes it bijective, so its left inverse is also
its inverse.  Multiplying `t_0s_1=0` on the left by `s_0` gives `s_1=0`,
contradicting the other diagonal equation.

For the occurrence model, all matrices in `(FCO3)` lie in the finite group
`GL_5(F_2)`.  The depth-two cousin construction supplies a finite role menu
inside `K` with the required diagonal and crossed-zero nested payload paths.
Adjoining `x_51(1)` and the three-factor word `W` keeps `K` finite and makes
the opposite-root rows literal.

Products and commutators of the embeddings in `(FCO5)` are computed
coordinatewise.  Hence every base null word remains a null word in all three
packets.  Multiplying the three outputs in `(FCO6)` gives `(FCO7)` because
`z^2=1`.  Coordinate permutations prove the arm-by-arm transport rows.
Finally `J!=1`, and nonidentity left translation in the finite group `G`
has trace zero, proving `(FCO8)`.

