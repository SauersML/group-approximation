---
rg: 2
id: literal-mark-quotient-via-exact-finite-models
kind: route
title: Exact finite models of the literal marked quotient
target: literal-mark-quotient-mf
requires: []
artifacts:
  - notes/COMMUTING_LAMP_QUOTIENT_STRUCTURE_AND_MF_FORK_2026-08-14.md
---

Separate `E/<w>` by finite quotients or by local embeddings into finite
groups and apply `isWeakMF_of_isLEF`.

Dead: `marked-quotients-not-lef`.  The compression and centralizer relators
of `E` survive in `E/<w>`, while `u` does not die there
(`commuting-lamp-defect-survives`), so the same finite-group collapse applies.
