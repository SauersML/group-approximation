---
rg: 2
id: bestvina-brady-main-theorem
kind: claim
title: "Bestvina--Brady Main Theorem: the kernel of A_L onto Z is FP_{n+1}(R) iff L is homologically n-connected, FP(R) iff L is acyclic, finitely presented iff L is simply connected"
distinct_from:
  bestvina-brady-kernel-finiteness-theorem: that is the homotopical form (type F_n iff (n-1)-connected), derived from this theorem together with Wall's description of F_n and the Hurewicz theorem; this is the theorem as printed.
---

**Imported theorem.** M. Bestvina and N. Brady, *Morse theory and finiteness properties of
groups*, Invent. Math. 129 (1997), 445--470, doi:10.1007/s002220050168, Main Theorem
(pp. 446--447), verbatim:

"Main Theorem. Let L be a finite flag complex. Let G = G_L be the associated right angled
Artin group, and H = H_L the subgroup of G described above. We compute homology with
coefficients in a ring R with 0 ≠ 1.
(1) H ∈ FP_{n+1}(R) if and only if L is homologically n-connected.
(2) H ∈ FP(R) if and only if L is acyclic.
(3) H is finitely presented if and only if L is simply connected."

Here `H_L` is "the kernel H_L of the homomorphism G_L → Z that 'sends all basis elements to 1'"
(p. 446). "Homologically n-connected" means that reduced homology with coefficients in `R`
vanishes in degrees `≤ n`; the paper introduces the phrase on p. 445.

The paper also records (p. 447) that the equivalence of finite generation of `H_L` with
connectivity of `L` was proven earlier by Meier--VanWyk.
