---
rg: 2
id: ct-p1-and-ct-p2-generate-ct-of-the-union
kind: claim
title: For any two sets P_1, P_2 of odd primes, CT_(P_1)(Z) and CT_(P_2)(Z) together generate CT_(P_1 ∪ P_2)(Z); this answers Kourovka Problem 21.75 negatively
distinct_from:
  ct-p-z-groups-are-pairwise-nonisomorphic: that is Kourovka 17.60, that distinct sets P give non-isomorphic groups CT_P(Z); this is Kourovka 21.75, about the subgroup the groups for two incomparable sets generate inside the group for their union, which turns out to be everything.
  ct-p-z-is-a-one-vertex-k-graph-full-group: that identifies CT_P(Z) with a full group of a one-vertex k-graph; this uses only class transpositions and their canonical affine maps.
---

**ESTABLISHED** (route `ct-p1-and-ct-p2-generate-ct-of-the-union-proof`; lane proof, elementary, not independently reviewed). No priority is claimed. The Kourovka Notebook, 21st issue (2026, TeX `gq/src/kourovka/21tkt.tex` in the swarm scratchpad, l.13919–13922), lists 21.75 as unsolved; a bounded arXiv search (`abs:"class transpositions"`, newest 5 hits, including arXiv:2607.17477) found no answer.

## The question

Kourovka 21.75 (S. Kohl), verbatim from the TeX: "Given two distinct sets `𝒫_1` and `𝒫_2` of odd primes none of which is a subset of the other, is it true that `⟨CT_(𝒫_1)(Z), CT_(𝒫_2)(Z)⟩ ⪇ CT_(𝒫_1 ∪ 𝒫_2)(Z)`?" Here (17.57, 17.60) `CT_𝒫(Z)` is generated by the class transpositions `τ_(r_1(m_1), r_2(m_2))` whose two moduli have all prime factors in `𝒫 ∪ {2}`, and `τ_(r_1(m_1), r_2(m_2))` swaps `r_1 + k m_1` with `r_2 + k m_2` for every `k` and fixes everything else.

## Statement

For all sets `P_1, P_2` of odd primes (finite or infinite, comparable or not),

    ⟨ CT_(P_1)(Z), CT_(P_2)(Z) ⟩ = CT_(P_1 ∪ P_2)(Z).

So the answer to 21.75 is **no**: the inclusion is never proper.

## Example

For `P_1 = {3}`, `P_2 = {5}`, the class transposition `τ_(3(12), 6(20))` (moduli `2^2·3` and `2^2·5`, in neither `CT_{3}(Z)` nor `CT_{5}(Z)` as a generator) equals

    g^-1 σ τ_(0(4), 2(4)) σ^-1 g,
    g = τ_(3(4), 0(8)),  σ = τ_(1(4), 0(24)) τ_(3(4), 2(4)) · τ_(0(4), 1(4)) τ_(2(4), 7(20)),

with `g` and `τ_(0(4),2(4))` in `CT_∅(Z)`, the first two factors of `σ` in `CT_{3}(Z)` and the last two in `CT_{5}(Z)`. Explicitly `σ` maps `4k ↦ 1 + 4k ↦ 24k` and `2 + 4k ↦ 7 + 20k ↦ 6 + 20k`, so `σ τ_(0(4),2(4)) σ^-1 = τ_(0(24), 6(20))`, and `g` maps `3(12)` canonically onto `0(24)` and fixes `6(20)`.
