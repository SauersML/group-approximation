---
rg: 2
id: atlas-q14-simple-overgroup-index-is-at-least-496
kind: claim
title: Every augmented finite-simple Atlas escape has A8 index at least four hundred ninety-six
distinct_from:
  atlas-q14-simple-overgroup-index-is-at-least-91: that stops at the natural A10 boundary; this removes the entire intervening simple-order interval.
  atlas-a4-gl5-packet-collision-screen: that excludes same-parity standard GL5 chart pairs; this identifies the opposite graph-parity pair as the only unresolved equality case.
---

Let `S` be finite simple and let two marked `A8` chart subgroups generate `S`
while satisfying the packet, `q_19243`, and q14.  Then

```text
[S:A8] >= 496,       |S| >= 496 |A8| = 9999360.     (OSI496)
```

If equality holds, then `S ~= L5(2)`, both chart subgroups are conjugate to
the standard block-diagonal `GL4(2)`, and their relative marking must have
opposite graph parity.  Equivalently, one chart acts through the natural
four-dimensional `F2` module and the other through its dual.  This one mixed
natural/dual double-coset system is the next exact finite-simple gate.

