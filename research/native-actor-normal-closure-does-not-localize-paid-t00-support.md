---
rg: 2
id: native-actor-normal-closure-does-not-localize-paid-t00-support
kind: claim
title: Native actor normalization does not localize the paid t00 support to the selector source
artifacts:
  - research/native-actor-normal-closure-gl8-fence-proof.md
  - scripts/native_t00_actor_atom_gl8_audit.py
distinct_from:
  native-t00-y1-collision-pays-marked-opnorm: that pays a positive marked part of the collision and computes its two first middle-index contacts; this tests whether normalization by the native actors transports that paid part to their actual constant atom.
  native-t00-collision-first-hecke-attachment-has-gl10-model: that stores the first B2 ejection in a transverse root-position vertex without a source projection; this includes the literal three-pair constant atom, both adjacent Whiteheads and both order-seven heads.
  el20-order-seven-heads-force-zero-common-hecke-reducing-cut: that proves the actual signed source contains no common reducing subcut; this supplies the complementary finite GL fence showing that even actor normalization can leave the paid discrepancy trivial on the common atom.
  paid-t00-pauli-menu-hits-whitehead-constant-atom: that uses the eight literal dual-Pauli translations and guarantees one conjugate with positive constant-atom compression; this proves those translations are essential by showing the smaller J1/J2/order-seven actor normal closure can fix the atom pointwise.
---

**ESTABLISHED NATIVE-ACTOR LOCALIZATION FENCE.**  Retain the paid
discrepancy and its first literal frame contacts

```text
D=x_78(t_00+s_0t_00),
U=[C_7,D]C_8=x_18(a_3),
V=[D,D_8]D_7=x_73(b_0).                              (NAL1)
```

Let

```text
E=P_z product_(k=7,8,9)(1+C_k)/2                    (NAL2)
```

be the actual arbitrary-profile constant atom fixed by the two literal
prefix Whiteheads `J_1,J_2`.  The known identities do not force paid
negative mass from `D`, `U`, or their native actor conjugates into `E`.

There is an exact eight-dimensional monomial model on
`ell^2(F_2^3)`.  The `C_k` are the three coordinate signs, their native
duals are coordinate translations, and `z=-1`.  The words `J_1,J_2` are the
two adjacent coordinate swaps; the two native head products retain exact
order seven.  For the involutory shear

```text
M:(x_7,x_8,x_9) |->
  (x_7+x_8+x_9,x_8,x_9)
```

and its permutation implementer `S_M`, put

```text
rho(D)=C_8S_M.                                       (NAL3)
```

Then

```text
[C_7,rho(D)]C_8=C_9,       [rho(D),D_8]D_7=z.        (NAL4)
```

Thus the two displayed coefficient-sensitive contacts remain exact, the
mark survives, and `rho(D)` has negative dimension four.  Nevertheless
`rho(D)` fixes the line `E=C delta_000` pointwise, so

```text
E P_D=0.                                             (NAL5)
```

Every `J_i` and every order-seven head also fixes `delta_000`.  It follows
that the entire subgroup generated by their conjugates of `rho(D)` fixes
`E` pointwise.  Allowing conjugate- or commutator-derived paid discrepancy
words from this native menu therefore does not repair `(NAL5)`.

This actor-only fence is sharp.  The native dual roots `Y_1,Y_2,Y_3` move
the eight character atoms transitively and are not contained in that actor
normal closure.  Averaging the eight conjugates of `D` by those Pauli flips
does localize one paid compression to `E`, with the quantitative bound in
`paid-t00-pauli-menu-hits-whitehead-constant-atom`.  Thus constant-atom
localization is available, but it comes from the full Heisenberg translation
menu rather than from normalization by `J_1,J_2` or their order-seven heads.

Nor can `E` be transported into the actual signed Hecke source `Q` while
remaining a common actor atom.  The ordinary-word identity `(OZC3)` implies
that every subprojection of `Q` reducing both order-seven heads is zero.
Since `E` reduces both heads, its signed-source component is zero.

Consequently normalizing the scale-neutral `J_1/J_2` atom cannot by itself
reach the signed selector source.  The Pauli menu supplies constant-atom
localization, and `paid-t00-carrier-has-signed-hecke-reynolds-dichotomy`
supplies the separate source-incidence-or-detector payment.  What remains is
to type one of those paid alternatives as the unequal quarter/eighth native
row, or to charge boundary along moving, non-common source cuts.

This is a scoped exact occurrence model, not a representation of the full
binary Leavitt elementary group.  No Property T, Kazhdan input, canonical
trace, literature theorem, local computation, or Lean compilation is used.

DERIVATION
native-actor-normal-closure-gl8-fence-proof
