---
rg: 2
id: stw22-diffuse-stiefel-uniform-five-sphere-fillings
kind: claim
title: Diffuse Stiefel spaces have a factor-uniform five-sphere filling modulus
distinct_from:
  stw22-diffuse-stiefel-uniform-four-sphere-fillings: that theorem supplies the degree-four corner control used here; the present theorem bootstraps it by one dimension.
artifacts:
  - research/artifacts/stw22-dim6-factor-bundle-breakthrough-2026-08-31.md
---

For every separable-predual `II_1` factor `(N,tau)`, projection `q in N`,
and `epsilon>0`, every `S^5->St_N(q)` in the ball of radius

```text
rho_(St,5)(epsilon)=min(epsilon,1)^2/1000000
```

about one point fills over `B^6` in the concentric `epsilon`-ball.  Every
`St_N(q)` is five-connected.  The modulus is independent of `N,q`.

