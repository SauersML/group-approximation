---
rg: 2
id: probability-coinduction-retains-factor-but-not-group-generation
kind: claim
title: Probability coinduction retains the factor payload but loses group generation
distinct_from:
  coinduction-preserves-but-cannot-create-algebraic-envelope: that treats compact abelian Haar actions and the algebraic-envelope target; this treats an arbitrary non-CE finite factor and the factor-character generation target.
  infinite-index-factor-induction-kills-every-coset-payload: that kills direct-sum localized carriers; probability coinduction avoids that trace-zero mechanism by using a unital tensor coordinate, but fails at a different exact interface.
  cdi-character-rigid-image-quotient-is-an-exact-bridge: that succeeds once a group representation generates the non-CE factor with a rigid image character; this shows why standard coinduction does not produce such a representation of the overgroup.
---

Let `H<Lambda` have infinite index, let `Q` be a finite factor generated by
`pi:H->U(Q)`, and coinduce the trace-preserving action `Ad(pi)` to an action
`beta` of `Lambda` on the probability tensor product

```text
A=tensorbar_(Lambda/H)(Q,tau_Q).                       (PCF1)
```

Every tensor coordinate is a unital trace-preserving copy of `Q`; hence if
`Q` is non-Connes-embeddable, so are `A` and the finite crossed product

```text
M=A rtimes_beta Lambda.                                (PCF2)
```

This genuinely avoids the zero-trace carrier obstruction.  Nevertheless the
canonical crossed-product unitaries do not give the required factor
representation compiler:

```text
{u_g:g in Lambda}'' isomorphic_to L(Lambda),
{u_g:g in Lambda}'' intersection A = C1.              (PCF3)
```

Thus the canonical representation of `Lambda` remembers only its regular
group factor, not the coinduced non-CE payload, and does not generate `M`.

The exact surviving target is a payload-generating cocycle: construct a
`beta`-cocycle `c_g in U(A)` such that the genuine representation

```text
g |-> c_g u_g                                          (PCF4)
```

generates a factor containing a tensor coordinate of `Q`.  A coboundary
cocycle cannot work, since it only conjugates the canonical copy of
`L(Lambda)`.  No such non-coboundary generation theorem follows from
standard coinduction or from character rigidity.  If it were proved for a
regular-or-finite-dimensional character-rigid host, the factor/character
compiler would apply.
