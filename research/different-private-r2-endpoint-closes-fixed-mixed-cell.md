---
rg: 2
id: different-private-r2-endpoint-closes-fixed-mixed-cell
kind: route
title: Close the fixed mixed cell by choosing another private R2 endpoint in the second chart
target: bleak-quick-adjacent-seam-retains-one-mixed-r2-cell
requires: []
---

**INVALIDATED PROPOSAL.**  Keep the normalized first-chart input
`s=(i p)` and common pivot `r=(p q)`, but replace the failed canonical
label-flipped output `(j q)` by some other transposition already occurring as
a private R2 endpoint in the second six-letter chart.

`common-pivot-r2-output-transposition-is-unique` proves that a transposition
`t` can satisfy `tst=r` only when `t=(i q)`, the first chart's own private
output.  Every private endpoint in the second chart's missing-letter star
uses `j` instead of `i`, so none closes the mixed boundary.  This rules out
only endpoint substitution inside the two fixed private R2 stars.  It leaves
open a genuinely interacting bounded R1--R5 diagram which retains `(i q)`
across the seam before the private gauges separate.

