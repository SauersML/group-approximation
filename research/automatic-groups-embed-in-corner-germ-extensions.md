---
rg: 2
id: automatic-groups-embed-in-corner-germ-extensions
kind: claim
title: Every automatic group embeds in a normal-form germ extension of a boundary Thompson group that meets the Belk--Hyde--Matucci finiteness hypotheses
distinct_from:
  automatic-groups-satisfy-boone-higman: that is BBMZ survey Problem 5.3(11) itself; this is a sufficient premise for it, stated as a concrete dynamical property of normal-form boundaries.
  z2-free-z-normal-form-boundary-has-corner-singular-orbits: that is the worked instance Z^2 * Z; this is the universal premise over all automatic groups.
  z2-free-z-embeds-in-a-finite-nucleus-rsg: that asks for a finite-nucleus rational similarity group host; this asks for a germ extension over a trivial-nucleus base, where flats are allowed to create singular points.
artifacts:
  - research/artifacts/gq-bh-bh-free-21-automatic-germ-extensions.md
---

**OPEN.**

## Statement

For every automatic group `G` there are:
- a group `G' >= G`;
- a continuous asynchronous automatic structure `L` on `G'`, in the sense of
  Belk--Bleak--Chatterji--Matucci--Perego (preliminary version 2026, §1.1), such that `G'`
  acts faithfully on the Cantor space `∂L`.

These must satisfy the following, where `B` is the boundary Thompson group of `L`: the full
group of local prefix replacements between prefixes with equal follower sets.

- **(E1) Finite germ extension.** The full group `Γ` of homeomorphisms that agree locally with
  `B` off finitely many points, with singular germs in the germ groupoid generated by `G'`, is
  a finite germ extension of `B` (Belk--Hyde--Matucci arXiv:2407.03149, §1).
- **(E2) Finiteness of B.** `Fix_B(M)` is finitely presented for every `M ⊆ sing(Γ)` with
  `|M| <= 2`. The case `M = ∅` is `B` itself.
- **(E3) Finitely many pair orbits.** `B` has finitely many orbits on `sing(Γ)^2`.
- **(E4) Germ groups.** For every `p ∈ sing(Γ)`, either `(B)_p` has finite index in `(Γ)_p`,
  or `(B)_p ⊴ (Γ)_p` with a finitely presented quotient.
- **(E5) Clopen transitive.** `B` is clopen transitive: any two nonempty clopen sets are joined
  by a `B`-local map.

## Why this form

- By Belk--Hyde--Matucci `cor:MainFinitenessCorollary` at `n = 2` (read at source), (E1)–(E4)
  make `Γ` finitely presented.
- `Γ` is full by construction, and clopen transitive because it contains `B`. So
  `permutational-boone-higman-iff-full-cantor-hosts` puts `Γ`, hence `G`, in `B_A`. The route
  `automatic-groups-bh-via-normal-form-germ-extensions` records this.

## Evidence and obstacles

- **Calibration.** For `Z^2 * Z` with its free-product structure,
  `z2-free-z-normal-form-boundary-has-corner-singular-orbits` proves (E1) and (E4):
  - four corner orbits;
  - germ groups `Z^2` over a `Z` of Thompson germs.

  (E2), (E3) and (E5) are open even there.
- **Products break the naive structure.** For `F_2 × F_2` with the product normal form, a
  second-factor generator is singular at every point of the first factor's boundary. That is
  uncountably many points, so (E1) fails.

  So the premise must choose non-product normal forms, or pass to a supergroup `G'`. This is
  allowed, since `V` contains `F_2 × F_2`. Groups with many commuting pieces are the real
  test: closed mapping class groups, graph manifold groups with Seifert pieces, and
  large-type Artin groups.
- **Hyperbolic groups.** For hyperbolic groups the natural base is the contracting rational
  similarity group of Belk--Bleak--Matucci--Zaremsky, not a trivial-nucleus `B`. A version of
  this premise over a contracting-RSG base is the natural generalization.

## Sharpened by recoding (bh-free-21, 2026-09-18, second landing)

- **E2, E3 and E5 can be dropped.** By `prefix-replacement-actions-recode-into-germ-extensions-of-v`,
  the automaton's base `B` can be replaced by Thompson's `V`. The theorem recodes the path space
  with complete binary prefix codes, which turns every state-preserving prefix replacement into
  a `V`-local map. Relative to `V`:
  - E2 is Belk--Hyde--Matucci Appendix A;
  - E3 holds for any finite union of `V`-orbits of rational points;
  - E5 is automatic.
- **What the premise really needs.** Two conditions on a continuous automatic structure of a
  supergroup `G' >= G`:
  - **(E1')** each generator acts by state-preserving prefix replacements off finitely many
    eventually periodic points, and maps those points to eventually periodic points;
  - **(E4')** after recoding, `(V)_p ⊴ (Γ)_p` with a finitely presented quotient at every
    singular point.
- **E5 for the automaton's own base fails in general.** For `Z^2 * Z` the automaton's own
  Thompson group has `K_0 ≅ Z^5` and is not clopen transitive
  (`z2-free-z-normal-form-boundary-has-corner-singular-orbits`). So the recoding is needed, not
  optional.
- **First complete instance.** `z2-free-z-lies-in-an-fp-full-germ-extension-of-v` does
  `Z^2 * Z` in full: type `F_∞`, and in `B_A`.
- **Limits recorded.**
  - The naive diagonal normal form on `Z^3` fails E1'. A first-factor generator is singular at
    each of the infinitely many points `(a_1a_2a_3)^m (a_1a_2)^∞`.
  - Interleaved normal forms on `F_2 × F_2` fail E1' everywhere: prepending a letter to one
    factor shifts the interleaving phase.
  - Neither matters for Boone--Higman, since both groups lie in `V`. They mark where normal-form
    boundaries stop being the right source of actions.
