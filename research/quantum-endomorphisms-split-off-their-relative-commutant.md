---
rg: 2
id: quantum-endomorphisms-split-off-their-relative-commutant
kind: claim
title: An equivariant finite-propagation endomorphism of the quasi-local matrix algebra splits it as its image tensor its relative commutant, with a local finite-dimensional defect and Jones index 1 or infinity
distinct_from:
  trace-preserving-endomorphism-fixes-discrete-type-i-summand: that shows a trace-preserving endomorphism of a finite von Neumann algebra is onto on the discrete type I summand; this is a C*-level tensor splitting A = Phi(A) (x) D for shift-equivariant local endomorphisms of the UHF algebra, with a local defect criterion and the index dichotomy from mixing.
  jones-index-blind-on-unbounded-defect-sector: that concerns pinching and Pimsner-Popa index on Leavitt-cover defect sectors; this shows that the subfactor index of a quantum automaton is 1 or infinity, so no index quantization is available on the quantum Gottschalk side either.
  split-ca-quantization-has-sharp-defect: that measures how far the compression of a classical strict section is from multiplicative; this is about genuine *-endomorphisms, which exist abundantly, and locates their non-surjectivity in a local defect algebra.
---

**ESTABLISHED** (route `quantum-endomorphisms-split-off-their-relative-commutant-proof`, which requires the
established Step 4 node `quantum-automaton-multiplication-map-is-injective-globally`). The 2026-09-17 audit had
returned this claim to OPEN; referee ref-04 (2026-09-17) filled Step 4 and restricted item 4 to infinite `G`. See
Attempts.

**Setting.** Let `G` be a group and `d >= 2`.
- `A = (x)_G M_d` is the quasi-local C*-algebra, with local algebras `A_F` for finite `F` and shift `alpha_g(A_h) = A_(gh)`.
- A *quantum automaton* is a unital *-endomorphism `Phi` of `A` with `alpha_g Phi = Phi alpha_g` for all `g`, and
  `Phi(A_e) <= A_N` for a finite `N` containing `e`. Then `Phi(A_h) <= A_(hN)`, and `Phi` is injective because `A`
  is simple.

**Statement.** Put `D = Phi(A)' n A`.
1. **Splitting.** For finite `L`, `Phi(A_(G \ L))' n A = Phi(A_L) (x) D`. Globally, multiplication gives
   `A = Phi(A) (x) D`. So `D` is simple, unital, `alpha`-invariant, has a unique trace, and satisfies `A (x) D = A`.
2. **Local defect.** `A_e <= Phi(A_(N^-1)) (x) (D n A_(N^-1 N))`. The *defect algebra* `F` is the unital
   *-algebra generated by the slice coefficients of `A_e` in this decomposition. It is a finite-dimensional
   C*-subalgebra of `D n A_(N^-1 N)` and does not depend on the choice of matrix units.
3. **Criterion.** The following are equivalent: `Phi` is onto; `D = C`; `F = C`.
4. **Index dichotomy (infinite `G`).** Let `G` be infinite and `R = pi_tau(A)''`, a II_1 factor (the hyperfinite
   II_1 factor when `G` is countably infinite). `Phi` extends to a normal endomorphism of `R`, with
   `R = Phi(R) (x) Q` and `Q = Phi(R)' n R` a factor, and `Q` is either `C` or infinite dimensional. So
   `[R : Phi(R)]` is `1` or `infinity`, and `Phi_* = id` on `K_0(A) = Z[1/d]`. (For finite `G`, `A` is a matrix
   algebra and `Phi` is onto by dimension.)

**Consequence (obstruction).** Every approach to the quantum conjecture through a numerical index or K-theoretic
invariant of `Phi` dies at the same step, the claim that a strict `Phi` has a finite invariant greater than 1.
- The invariants here are the relative commutant `D` and its von Neumann closure `Q`.
- For a strict `Phi` on an infinite group, `D` is an infinite-dimensional simple monotracial algebra with
  `A (x) D = A`, the Jones index is infinite, and the trace and `K_0` classes do not move.
- This kills the "index quantization" item of playbook lane (a). The surviving invariant is the *local* defect
  `F`, a nonzero finite-dimensional algebra whose translates are packed into `D`. That is what
  `sofic-groups-are-quantum-surjunctive` counts.

## Attempts

1. **Slice by matrix units, then mixing of the Bernoulli shift (2026-09-17, audit: refuted by one of two
   referee lenses).** Route `quantum-endomorphisms-split-off-their-relative-commutant-proof`, kept as the proof
   artifact. It now requires `quantum-automaton-multiplication-map-is-injective-globally`, so the status derives
   OPEN. Lens 2 returned *survives*. The refuting reason from lens 1, verbatim:

   > I checked every step of the route (requires: [], so there are no prerequisites to verify). Most of it holds.
   > - Step 0 slicing identities: correct.
   > - Step 1 splitting Phi(A_(G\L))' n A = Phi(A_L) (x) D: correct.
   > - Step 2 local defect in D n A_(N^-1 N): correct.
   > - Step 3 criterion, onto <=> D=C <=> F=C: correct.
   > - Step 5, tau o Phi = tau, the normal extension, the commutation theorem and the index k^2: correct.
   > - Step 6 mixing contradiction, 1/k vs 1/k^2: correct.
   > - The experiment script (clifford_star_f2.py) belongs to a different claim.
   >
   > It still does not survive the lens, for two reasons.
   >
   > (1) The first place that does not follow is Step 4. It says: "On Phi(A_L) (.) D the multiplication map is injective by Step 0. Phi(A) is the inductive limit of these matrix algebras, so multiplication is injective on Phi(A) (.) D."
   > - Elements of the algebraic tensor product Phi(A) (.) D use a_i that are not local, so injectivity on each Phi(A_L) (.) D does not pass to the union by itself.
   > - The missing argument: the local maps are isometric for the min norm, so they extend isometrically to Phi(A) (x)_min D, and the multiplication map agrees with this extension by continuity. Equivalently, an ideal of an inductive limit is the closure of its intersections with the finite stages.
   > - This is true and standard, but the step is not filled.
   >
   > (2) Quantifier mismatch in Statement item 4.
   > - The Setting says "Let G be a group", with no restriction.
   > - Item 4 says Phi "extends to a normal endomorphism of the hyperfinite II_1 factor R", with R = pi_tau(A)'' (Step 5).
   > - For finite G, pi_tau(A)'' = M_(d^|G|), which is not a II_1 factor.
   > - For uncountable G it is a non-separable injective II_1 factor, not "the" hyperfinite II_1 factor.
   > - Step 5 never proves that pi_tau(A)'' is R; the proof just defines R that way.
   >
   > Fix: restrict item 4 to countably infinite G, and fill in the Step 4 limit argument. The downstream use in sofic-groups-are-quantum-surjunctive relies only on items 2-3, which are sound.

   Where it stops: Step 4 (global injectivity of multiplication) and the quantifier of item 4. Downstream claims
   that needed this node drop from ESTABLISHED until it is restored.

2. **Referee pass (2026-09-17, referee ref-04): both audit defects repaired.**
   - Step 4 is now the established node `quantum-automaton-multiplication-map-is-injective-globally`, route
     `quantum-automaton-multiplication-injective-proof`: the multiplication map exists on
     `Phi(A) (x)_max D = Phi(A) (x)_min D` (UHF, nuclear), is isometric on each `Phi(A_L) (x) D` by matrix slicing,
     and the union of these pieces is dense, so it is isometric. The simplicity of `D` then uses only the easy
     direction: a proper nonzero ideal `J` of `D` gives the proper nonzero ideal `A (x) J` of `A (x) D = A`.
   - Item 4 is restricted to infinite `G`, with `R = pi_tau(A)''`. It is a II_1 factor because `tau` is the unique,
     hence extremal, trace of the infinite-dimensional simple algebra `A`; it is hyperfinite when `G` is countable.
   - Checked line by line: Step 0 slicing, Step 1 splitting over `L`, Step 2 local defect in `D n A_(N^-1 N)`,
     Step 3 criterion (`F = C` gives `A_e <= Phi(A_(N^-1))`), Step 5 (`tau o Phi = tau` by uniqueness of the trace,
     product trace, commutation theorem, index `k^2` versus infinity from the relative commutant), Step 6 (mixing
     along a net `g_j -> infinity` in the compact group `Aut(M_k)` gives `1/k = 1/k^2`). No step fails.
   - Literature: for `G = Z` the conclusion "homomorphism implies automorphism" is Schumacher-Werner
     (quant-ph/0405174), cited in `quantum-surjunctivity-conjecture`. No source was found stating the splitting or
     the index dichotomy for general `G`; the statement does not claim more than it proves.
