---
rg: 2
id: ozawa-leg-algebra-carries-non-amenable-trace
kind: claim
title: The C*-algebra of Ozawa's block representation of SL(3,Z) has a trace with non-injective GNS closure and is not nuclear
distinct_from:
  hyperfinite-factor-not-quasidiagonal: that is Ozawa's theorem that R is not quasidiagonal; this is a property of the right-leg subalgebra his proper isometry uses, and it blocks reading his example as a nuclear witness.
artifacts:
  - research/artifacts/uct-fresh-mf-tensor-route-2026-09-12.md
  - research/artifacts/ozawa-r-not-quasidiagonal-import-2026-09-12.md
---

**ESTABLISHED (derivation `ozawa-leg-algebra-trace-proof`).** Setup, as in Ozawa
arXiv:2609.08892v1:
- `G = SL(3,Z)`;
- `p_k` is the `k`-th prime, and `π_k` is the representation of `G` on
  `ℓ²(P²(F_{p_k})) ⊖ ℂ1`;
- `D` is the C\*-algebra generated by `(π_k(g))_k ∈ ∏_k B(ℓ²(P²(F_{p_k})) ⊖ ℂ1)`,
  equivalently the conjugate representations inside the copy of `∏_k B(H̄_k)` in R.

Conclusions:
- `D` has a tracial state `τ` with `τ(π(g)) = δ_{g,1}`. So `π_τ(D)''` contains
  `L(G)`, which is not injective, and `D` is not nuclear.
- If `D ⊂ C` with `C` nuclear, then `D` is exact.

Consequence for `nuclear-factor-with-infinite-mf-tensor-exists`: Ozawa's proper
isometry, with the right legs as printed, cannot be read inside a nuclear
subalgebra of R unless `D` is exact. Exactness of `D` is not decided here. The
minimal slice algebra of the isometry, which need not contain all of `D`, is not
decided either.
