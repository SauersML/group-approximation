---
rg: 2
id: arithmetic-star-packet-hs-plancherel-anchor
kind: claim
title: Canonical normalized-HS microstates give the S9 packet a two-thirds mark rank and a one-over-192 adjoint gap
distinct_from:
  finite-noncommutative-packet-compression-collapse: That theorem self-normalizes an operator-norm corona discrepancy by its possibly sublinear rank; this uses canonical trace to force linear packet rank and quadratic adjoint Reynolds density in normalized-HS microstates.
  macroscopic-projection-is-one-adjoint-direction: That warning concerns finitely many matrix vectors; this produces an entire Reynolds-difference space of asymptotic adjoint density one over 192.
artifacts:
  - notes/ARITHMETIC_STAR_PACKET_HYPERLINEAR_AUDIT.md
---

In every normalized-HS microstate sequence for the arithmetic star-packet
carrier which converges to the canonical group trace, flexibly exactify the
fixed subgroup `K=S_9`.  The exactified representations have asymptotically
regular Plancherel multiplicities.  Therefore the difference of the two
distinct marked star transpositions has

```text
rank/d -> 2/3,                  normalized HS mass squared -> 2.
```

Let `Q=AGL(3,2)` and `Q_0=GL(3,2)` be the packet-internal images of `P` and
`H`, and let `R_Q<=R_(Q_0)` be their adjoint Reynolds projections.  Then

```text
tr_ad(R_(Q_0)-R_Q)
  -> 1/|Q_0|-1/|Q|
   = 1/168-1/1344
   = 1/192.
```

