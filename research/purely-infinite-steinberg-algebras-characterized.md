---
rg: 2
id: purely-infinite-steinberg-algebras-characterized
kind: claim
title: A Steinberg algebra of a strongly effective ample groupoid is properly purely infinite exactly when its unit-space idempotents are properly infinite
artifacts:
  - research/artifacts/un-prior-art-2026-09-13-part1.md
---

**ESTABLISHED by citation** (`purely-infinite-steinberg-algebras-characterized-citation`). H. Larki, arXiv:1901.07094
(title of the arXiv listing: *Non-simple purely infinite Steinberg algebras with applications to Kumjian–Pask algebras*), read
from the TeX source on MSI on 2026-09-13.

**Thm `thm3.4`** (verbatim up to macro expansion). "Let $G$ be a strongly effective, ample and Hausdorff groupoid and let $K$ be a
field. Suppose that $\mathcal{B}$ is a basis of compact open sets for $G^{(0)}$. Then the following are equivalent:
1. $A_K(G)$ is properly purely infinite.
2. For every $V\in \mathcal{B}$, $1_V$ is properly infinite.
3. For every invariant open subset $U\subseteq G^{(0)}$ and any nonempty relatively compact open subset $V\subseteq
   D:=G^{(0)}\setminus U$, $1_V$ is an infinite idempotent in $A_K(G_D)$.
4. Every nonzero one-sided ideal in any quotient of $A_K(G)$ contains an infinite idempotent.

In particular, if one of the above statements holds (and so all), then $A_K(G)$ is purely infinite as well."

**Cor `cor3.6`.** If for every open invariant U the groupoid G_{G^(0)∖U} is locally contractive, then A_K(G) is properly purely
infinite.

**Scope for the UN program.** This is the algebraic (Steinberg, any field) paradox side. Whenever 1_{G^(0)} is infinite in
A_K(G) and A_K(G) is simple, the non-MF paper's Cor l.1016 applies to EL_n(A_K(G)).
