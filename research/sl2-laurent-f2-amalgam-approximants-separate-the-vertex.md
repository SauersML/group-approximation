---
rg: 2
id: sl2-laurent-f2-amalgam-approximants-separate-the-vertex
kind: claim
title: In the truncated amalgams C1,m *_(B0,n) C1',m approximating SL_2(F_2[t,1/t]) the vertex group is separable
distinct_from:
  function-field-rank-one-vertex-action-is-sofic: that is the soficity statement for the limit pair; this is the residual-finiteness input on the approximating amalgams that the colimit criterion turns into it
  finite-bi-index-kazhdan-stabilizer-is-nonseparable: that proves nonseparability of Kazhdan stabilizers in their hosts; this asks for separability in approximants of a host where the limit stabilizer is nonseparable
---

**OPEN.**  Notation as in `function-field-rank-one-vertex-action-is-sofic`:
`w=[[0,1],[1,0]]`, `u_p = e_12(p)`, `v'_p = e_21(p)`, `S_3 = <w,u_1>`,

```text
V_m   = { u_p : deg p <= m },            C1,m  = <S_3, V_m> = S_3 *_(Z/2) V_m,
V'_n  = { v'_(tq) : deg q <= n-1 },      B0,n  = <V_n, V'_n>,
theta = conjugation by [[0,1],[t,0]],    C1',m = theta(C1,m).
```

For `n >= 1` and `m >= n+1`, `B0,n` lies in `C1,m ∩ C1',m`.  Form the
abstract amalgam

```text
Delta_(n,m) = C1,m *_(B0,n) C1',m .
```

The claim: `C1,m` is closed in the profinite topology of `Delta_(n,m)`.

Both factors are virtually free (amalgams of finite groups) and `B0,n` is a
finitely generated subgroup of each.  Explicitly, for `n=1`, `m=2`,
`Delta` is generated by `w, u_1, u_t, u_(t^2), w'` with `S_3` relations on
`<w,u_1>`, `V_2 = <u_1,u_t,u_(t^2)>` elementary abelian, `w'` an
involution, and

```text
(w' v)^3 = 1,  [v, w' u_1 w'] = 1,  [v, w' u_t w'] = 1,   v = w u_t w,
```

together with the analogous commutation of `u_(t^2)`'s partners at larger
`m`.

## Attempts

- **n = 0.**  `B0,0 = Z/2`, so `Delta_(0,m)` is an amalgam of virtually free
  groups over a finite group, hence virtually free and LERF.  The claim
  holds.  The colimit needs every `n`.
- **n = 1, m = 2: holds.**  `sl2-laurent-f2-degree-one-amalgam-is-virtually-coxeter`
  proves `Delta_(1,2) = W ⋊ S_3`, with `W` a nine-generator Coxeter group and
  `C1,2 = W_T ⋊ S_3` for a standard parabolic `W_T`.  Separability follows by a
  congruence on the integral Tits representation.  The splitting kills
  `u_t` and `u_(1/t)`, a degenerate specialization.
- **n >= 2: the Coxeter splitting dies.**  The rotation relator
  `s u_(t^2) s^(-1) = u_1` forces every homomorphism `a -> (12)`, `b -> (23)`
  to evaluate the identified root groups at `t = 1`.  The resulting kernel
  has a six-term relator from `(yf)^3 = 1`.  What would work instead is a
  faithful integral representation of `Delta_(n,m)` in which `C1,m` is a
  point stabilizer, or a cubulation.
- **Compatible normal subgroups.**  It suffices to find, for each finite
  set of letters outside `B0,n`, normal subgroups `N <| C1,m` and
  `N' <| C1',m` of finite index with `N ∩ B0,n = N' ∩ B0,n` that keep those
  letters outside `B0,n N` and `B0,n N'`.  Then `Delta` maps onto an
  amalgam of finite groups where normal forms survive.  Obstacle: inside
  `Delta`, `s = w'w` satisfies `s u_(t^2 q) s^(-1) = u_q` for small degrees,
  so the common intersection must be saturated under these partial
  conjugacies.  Retractions `C1 -> C1,m` (kill `u_(t^k)`, `k>m`) fix
  `B0,n` but do not by themselves produce the finite quotients.
- **Twisted double.**  `theta` maps `C1,m` to `C1',m` but not `B0,n` to
  itself (`theta(B0,n) = <V'_(n+1), V_(n-1)>`), so the double-retraction
  argument for untwisted doubles does not apply.
- **Edge group structure (lane ex3-hecke-c1m-separable, unreviewed).**  The
  Iwahori `B0 = Gamma_0(t)` acts on the tree at infinity with quotient a line
  `... V'_2 — V'_1 —(1)— <u_1> — V_1 — V_2 ...` (vertex groups; edge groups
  `V'_(k-1)`, trivial, `<u_1>`, `V_k`), so `B0 = V * V'` and
  `B0,n = V_n * V'_n`.  Inside `L1`, `C1,m ∩ C1',m = V_(m-1) * V'_m`, which
  properly contains `B0,n`.  So the natural map `Delta_(n,m) -> L1` collapses
  normal forms, and finite quotients through `L1` cannot separate.
- **Reduction to `m = n+1` (sketch).**  No identification touches `u_(t^k)`
  for `k > n+1`, so
  `Delta_(n,m) = Delta_(n,n+1) *_(V_(n+1)) V_m *_(theta V_(n+1)) theta(V_m)`
  is an amalgam over finite groups.  If `Delta_(n,n+1)` is residually finite
  and `C1,n+1` is separable in it, the finite-edge-group argument (map to a
  virtually free amalgam of finite groups, injective on the finite
  subgroups) should give separability of `C1,m` for every `m`.  Not written out.
- **Natural linear representations are one degree deep.**  Send `w -> w`,
  `u_(t^i) -> u_(g_i)` on `C1,n+1` and `w' -> [[0,a],[a^-1,0]]`, over a
  commutative ring of characteristic 2.  Then `(w' v'_t)^3 = 1` forces
  `g_1 = a^-1`, and the partial rotations force `g_(k+1) = a^-2 g_(k-1)`.
  So `g_i = t^i` for `i <= n`, with `t = a^-1` a unit, and only `g_(n+1)` is
  free.  Invertibility is forced, since the ideal generated by the entries of
  `g - I` is conjugation invariant and `s u_(t^2) s^-1 = u_1`.  Congruence
  separability of `B0,n` in `C1,m` also fails, because reductions of
  `V_n * V'_n` generate `SL_2` of the residue field.  A compatible-filtration
  proof must therefore use non-congruence quotients of `C1,m`.
- **Geometry (sketch).**  `Delta_(n,m)` is the fundamental group of the
  finite truncation of the square complex of groups `L1 \ (T_0 x T_inf)`.
  That truncation is a strip over the segment `b_n ... a_n` of `B0,n`, folded
  at `a_0` onto the ray `[0,m]` of `C1,m` and at `b_1` onto the ray of
  `C1',m`.  It acts properly and cocompactly on a CAT(0) VH square complex
  whose horizontal vertex spaces are convex subtrees of `T_inf`, and `C1,m`
  stabilizes one of them.  Virtual specialness would give separability
  (Haglund--Wise), but irreducible VH lattices can fail residual finiteness
  (Burger--Mozes), so this needs an argument.
- **Low-index data (GAP in sagemath/10.7 on MSI).**  Control case
  `Delta_(1,2)`: `C1,2` has 21869 proper overgroups of index at most 14.
  Every one of them excludes `w'`, and 19635 exclude `f_2 = w' u_t w'`.  The
  runs for `n = 2, 3` did not finish before the lane ended.  The script is
  `lowindex.g` in the lane's experiments directory; it builds the presentation
  with `f_0 = w u_t w`, `f_k = w' u_(t^(k-1)) w'`, and relators
  `w u_(t^(k+1)) w = w' u_(t^(k-1)) w'` for `1 <= k <= n-1`.
- **SAT census: for n >= 2 the vertex looks codense, not separable (lane
  swarm-0917-w5-nh-proj-commutant, computation, not a proof).**  `C1,m` is
  separable only if some finite-index subgroup contains `C1,m` and excludes
  `w'`.  Equivalently, some finite transitive `Delta`-set has a point `x0`
  fixed by `C1,m` and moved by `w'`.  We encode that existence problem as SAT
  (pysat, cadical153).
  - *Encoding.*  The generators are involution permutations `w, u_(t^j)`
    (`0 <= j <= m`) and `w'`, with free `w' u_(t^j) w'`-partners for
    `j >= n+2`.  The relators are those of the presentation above.  The
    search fixes a BFS-standard numbering, so an UNSAT answer at `k` covers
    every transitive action on `k` points.
  - *Encoding check.*  The action of `Delta_(2,3)` on `P^1(F_4)` (`t -> a`
    with `a^2 = a+1`) satisfies the clauses.  The instance with `w' = w`
    forced is UNSAT.
  - *Control, n = 1, m = 2.*  An overgroup excluding `w'` appears at index 4,
    matching the GAP data above.
  - *n = 2, m = 3.*  UNSAT for every `k <= 22` (`k = 22` took 144 s).  So no
    subgroup of index `<= 22` contains `C1,3` and excludes `w'`.
  - *n = 3, m = 4.*  UNSAT for every `k <= 14`.  This is implied by the
    `n = 2` result, since `Delta_(2,3) -> Delta_(3,4)` maps `C1,3` into `C1,4`.
  - *Todd--Coxeter.*  The coset enumeration of `C1,3` in `Delta_(2,3)` does
    not close within 200000 cosets, consistent with infinite index.
  - *Which relators matter (n = 2, m = 3, k <= 12).*  Dropping one relator
    gives these smallest overgroups excluding `w'`:

    | Relator dropped | Smallest index |
    |---|---|
    | `(w u_1)^3` | 4 |
    | `(w' v)^3`, with `v = w u_t w` | 2 |
    | the rotation `w u_(t^2) w = w' u_1 w'` | 4 |
    | `[v, w' u_t w']` | 3 |
    | `[v, w' u_(t^2) w']` | none up to 12 |
    | any single commutator in `V_3` | none up to 12 |

    So the codensity lives in `<w, u_1, u_t, u_(t^2), w'>`.  Only `C1,2`
    needs to fix `x0`: releasing `u_(t^3)` stays UNSAT, while releasing any of
    `w, u_1, u_t, u_(t^2)` gives SAT at `k = 3, 4, 2, 4`.
  - *Core group.*  Take the group
    `<w,u,a,w' | (wu)^3, (w'a)^3, [a, s a s^-1]>`, with `s = w'w`, `a = v`,
    `u = u_1`.  The hypotheses are that `w, u, a` fix `x0`, `u` fixes `w'x0`,
    and `w'` moves `x0`.
    - With all three commutators of `u, w a w, w w' u w' w`: UNSAT to 12.
    - With `{01, 02}` or `{02, 12}`: UNSAT to 12.
    - With only `{01, 12}`: SAT at 8.
    - With at most one commutator: SAT at 4 or 5.
  - *Hand chase.*  Suppose `y = w'x0 != x0`.  Then:
    - `<w', a>` has orbit `{x0, y, z}` on `x0`, with `w' = (x0 y)` and
      `a = (y z)` there;
    - `[a, w' u_t w'] = 1` forces `u_t` to fix `z`;
    - the rotation makes `u_1` fix `y` and `w u_(t^2) w` fix `x0, y, z`, so
      `u_1` fixes `z`;
    - `u_t` swaps `wy` and `wz`, and `w` moves `z`.

    None of these steps uses finiteness, while the claim concerns finite sets
    only.  So a proof must feed finiteness into a chase along `s`-orbits,
    since `s u_(t^2) s^-1 = u_1` on the overlap.  We did not find one.
  - *Obstructions to the obvious proofs.*
    - The Burger--Mozes density argument needs a locally finite tree with
      `C1,m` as a vertex stabilizer, but the Bass--Serre tree of `Delta` has
      infinite valence.
    - `theta^-1(B0,2)` is not codense in `C1,3`, since it has a quotient onto
      `S_3`.
    - Quotients through `L1` collapse normal forms, as noted above.
  - *Verdict.*  This changes belief: for `n >= 2` the prerequisite very likely
    fails.  If `C1,m` is codense in `Delta_(n,m)`, the route
    `function-field-rank-one-vertex-action-via-separable-approximants` dies at
    this, its only open input, with `Delta_(n,m)` as the directed system.  A
    replacement system would have to break the rotation relator, as the
    `n = 1` Coxeter truncation does.
  - *Artifacts.*  Scripts are in
    `experiments/laurent-amalgam-vertex-census-2026-09-17/`:
    - `overgroup_sat.py`, the encoding;
    - `census_std.py`, the main census;
    - `check_encoding.py`, the `P^1(F_4)` sanity check;
    - `drop_std.py` and `drop_relators.py`, the relator drops;
    - `hyp_std.py`, the hypothesis release;
    - `core_sat.py`, the core group;
    - `todd_coxeter.py`, the coset enumeration.
