---
rg: 2
id: native-whitehead-hecke-head-has-order-seven-anchor
kind: claim
title: Each native prefix Whitehead and its center-chain Hecke head have an odd order-seven occurrence anchor
artifacts:
  - research/native-whitehead-hecke-order-seven-proof.md
distinct_from:
  center-chain-mixed-hecke-polars-share-a1-tail: that proves that the two Hecke polar words square to the literal selector roots; this supplies an odd-parity relation between each Hecke head and the corresponding literal prefix Whitehead.
  primitive-native-hecke-relators-have-no-odd-mixed-occurrence: that audits the primitive named Steinberg triangles and explicitly leaves longer derived words open; this is the longer elementary consequence obtained by multiplying those literal words on their three native prefix corners.
  projective-row-data-do-not-authenticate-hecke-polars: that exhibits a balanced spectator involution invisible to squares and separate covariance rows; the order-seven relation detects exactly that spectator once it lies in the multiplicity commutant.
  tracial-word-authentication-is-regular-firewalled: that forbids promoting any fixed finite-word calculation to a vanishing-loss tracial authentication theorem; this is an exact occurrence relation and a commutant-gauge coercivity statement, not the still-missing compression estimate.
---

**ESTABLISHED ODD OCCURRENCE ANCHORS.**  Retain the center-chain arms and
the two literal characteristic-two prefix Whiteheads

```text
A_1=x_47(a_1),       A_2=x_58(a_2),
B_2=x_84(b_2),       B_3=x_95(b_3),

J_1=x_87(x_1)x_78(y_1)x_87(x_1),
J_2=x_98(x_2)x_89(y_2)x_98(x_2),                       (OWH1)
```

where

```text
x_1=b_2a_1=s_(00)t_0,       y_1=b_1a_2=s_0t_(00),
x_2=b_3a_2=s_(000)t_(00),   y_2=b_2a_3=s_(00)t_(000).
```

Put

```text
v_1=B_2A_1,                  v_2=B_3A_2.                (OWH2)
```

Then the following are exact ordinary-word relations in
`EL_20(L_(F_2)(1,2))`:

```text
(J_1v_1)^7=1,               (J_2v_2)^7=1.              (OWH3)
```

For the mixed Hecke polar words

```text
u_1=B_2A_1,                  u_2=B_3A_2A_1
```

this reads

```text
(J_1u_1)^7=1,               (J_2u_2A_1)^7=1.           (OWH4)
```

The occurrence parity downstairs is genuinely odd in each individual Whitehead and
each individual Hecke head.  More precisely, let `pi` be any unitary
representation of the finite packet generated by `J_i,v_i`, and let `D` be
a commuting multiplicity involution.  If one replaces either factor by its
gauged version, then

```text
((pi(J_i)D)pi(v_i))^7=D,
(pi(J_i)(pi(v_i)D))^7=D.                               (OWH5)
```

Consequently the order-seven relator has the exact dimension-free
coercivity identity

```text
||((pi(J_i)D)pi(v_i))^7-I||=||D-I||,                  (OWH6)
```

and likewise when `D` is attached to `v_i`.  The balanced spectator
involution used in `projective-row-data-do-not-authenticate-hecke-polars`
therefore cannot be extended across this occurrence relation with zero
operator-norm defect.  Together with the already native involutivity of the
Whitehead, the coprime exponents `2` and `7` leave no scalar or reservoir
involution gauge.

The relation is the rank-three `F_2` geometry native to the coefficient
occurrence, not an inserted finite tag.  On the three equivalent prefix
corners used by row `i`, the restrictions of `A_i,B_(i+1),J_i` are

```text
A=[[1,1,0],[0,1,0],[0,0,1]],
B=[[1,0,0],[0,1,0],[1,0,1]],
J=[[1,0,0],[0,0,1],[0,1,0]].                          (OWH7)
```

They generate `GL_3(F_2)`, and `M=JBA` satisfies
`M^3+M+I=0`, hence `M^7=I`.

This closes the previously missing **elementary-group algebraic odd-parity
occurrence anchor**, but it does not by itself prove
`center-chain-common-source-authenticates-native-covariances`.  The fixed
center-chain source `Q` is not known to reduce the literal Whiteheads; in
the regular representation its leakage is positive, as computed by
`native-whitehead-corner-collapses-hecke-flags` and the tracial firewall.
The remaining operator-norm step is therefore sharply reduced to proving
that the finite-matrix native occurrence cut on which the reservoir gauge
is defined also inherits `(OWH3)` with vanishing compression leakage.  Once
that inheritance is proved, `(OWH6)` removes the projective spectator gauge
without Property `(T)`, Kazhdan input, a canonical trace profile, or a
literature theorem.

There is also a separate upstairs typing check for the manuscript's current
group `St_20(R)`.  Equality of the displayed elementary matrices does not
formally imply equality of arbitrary chosen Steinberg lifts: the seventh
power may a priori be a central `K_2` word.  Thus `(OWH3)` may be consumed
immediately in the `EL_20(R)` lane.  Consuming it in `St_20(R)` requires
either a direct Steinberg collection of this sevenfold word or an internal
splitting of the restricted finite packet.  No such splitting is being
silently assumed here.

For an unconditional finitely presented proof vehicle, the lift ambiguity
can instead be removed without any `K_2` calculation.  If `r_i` denotes the
two written seventh powers in `St_20(R)`, set

```text
Delta_7=St_20(R)/<<r_1,r_2>>.                           (OWH8)
```

The canonical map to `EL_20(R)` factors through `Delta_7` by `(OWH3)`, so
the marked root remains nontrivial there.  The two order-seven anchors hold
by presentation in `Delta_7`, and only two explicit relators have been
added.  Thus a Property-`(T)`-free MF-collapse proof using `(OWH3)` may work
in `Delta_7` directly and does not need a literature computation of the
Steinberg kernel.  Proving the stronger statement for the unquotiented
`St_20(R)` still requires the lift check just described.

DERIVATION
native-whitehead-hecke-order-seven-proof
