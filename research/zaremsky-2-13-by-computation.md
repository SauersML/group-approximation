---
rg: 2
id: zaremsky-2-13-by-computation
kind: route
title: "Zaremsky Problem 2.13 answered by an explicit formula for every Sigma^m(bF_{n,r})"
target: zaremsky-2-13-bnsr-of-pure-braided-bht-groups
requires:
  - pure-braided-bht-sigma-invariants-miss-only-endpoint-hull
---

Immediate: the required claim describes `Sigma^m(bF_{n,r})` for every
`m >= 1`, `n >= 2`, `r >= 1` and states it through an explicit basis of
the character group. That is what Problem 2.13 asks for. If that formula is
refuted, the corrected formula gets its own answer claim and route into the
root.
