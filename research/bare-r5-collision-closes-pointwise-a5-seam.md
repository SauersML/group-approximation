---
rg: 2
id: bare-r5-collision-closes-pointwise-a5-seam
kind: route
title: Use the lone nonshared R5 collision to synchronize adjacent Carmichael A5 charts
target: thompson-v-r5-a6-seam-pointwise-synchronization
requires:
  - a5-a4-overlap-has-two-seam-charges
---

**INVALIDATED PROPOSAL.**  Exactify the two local Carmichael `A5` packets
on adjacent charts and impose the Bleak--Quick R5 relation on their two
nonshared leaves.  The proposed conclusion was a dimension-free bound on
the two shared-label mismatches using only the two local packet defects and
the R5 collision defect.

`bare-r5-a6-collision-does-not-synchronize-shared-labels` gives an exact
five-dimensional `A6` countermodel: both local defects and the R5 collision
defect vanish, while both shared-label mismatches have squared normalized
Hilbert--Schmidt norm `6/5`.  Hence this bare route is dead.  The strengthened
live target may still use a label-sensitive R2 transport or R3 split anchor
coming from the single ambient Bleak--Quick approximate model.
