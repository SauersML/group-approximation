---
rg: 2
id: one-balanced-actor-qutrit-bridge-is-jacobson-core
kind: claim
title: One infinite balanced actor couples the finite qutrit envelope to the Jacobson core
artifacts:
  - research/one-balanced-actor-qutrit-jacobson-proof.md
distinct_from:
  four-root-qutrit-bridge-is-jacobson-core: that reaches the same coefficient core by adjoining four literal depth roots; this packages the nonpolynomial return into one explicit infinite-order actor and proves a minimal finite-envelope-plus-one-actor generation statement.
  depth-mismatched-qutrit-root-bridge-is-residually-finite: that one-sided return embeds in a polynomial matrix group; the balanced actor contains both Laurent directions and a nonzero Jacobson head.
  qutrit-rank-six-parity-bridge-is-impossible-or-virtually-free: that finite involution bridge is either incoherent or a finite amalgam; this actor has infinite order and produces a group which is not residually finite.
---

Put

```text
J=F_2<S,T | TS=1>,          Q=1-ST,
a=[[S,Q,0],[0,T,0],[0,0,1]] in EL_3(J).                 (BAQ1)
```

Embed this block in `EL_28(J)), and let

```text
K=GL_28(F_2)=EL_28(F_2).
```

Then

```text
<K,a>=EL_28(J).                                          (BAQ2)
```

Under the outer 27-leaf Leavitt module chart, `K` is exactly the finite
envelope generated by the qutrit packet and one matched opposite-root pair.
Thus a finite group containing the entire three-phase qutrit cell, coupled
to the single actor `a`, reaches the full Jacobson elementary core.  The
actor has infinite order.

The symbol quotient provides the exact embedding/quotient test:

```text
J -> F_2[z,z^(-1)],       S |-> z, T |-> z^(-1), Q |-> 0,
a |-> diag(z,z^(-1),1).                                 (BAQ3)
```

Its elementary-group kernel is the infinite simple locally finite finitary
linear group and contains every nonzero head root `x_ij(Q)).  In
particular the head survives in `<K,a>` but dies in every finite quotient.
Consequently this group is not residually finite and cannot be a virtually
free finite amalgam.

This is the first tested bridge which escapes all preceding finite,
virtually-abelian, finite-amalgam, and one-variable polynomial fences.  It is
still not a proof of nonhyperlinearity: the hyperlinearity of this Jacobson
elementary group is not excluded by the finite-quotient obstruction.  The
remaining task is now a concrete one-group problem—couple the qutrit
three-phase marginal to the finitary head kernel in a way that rules out
Hilbert--Schmidt microstates, rather than merely detecting the head in exact
quotients.
