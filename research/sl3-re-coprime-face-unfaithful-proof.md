---
rg: 2
id: sl3-re-coprime-face-unfaithful-proof
kind: route
title: The sector W=1 evaluation puts pi(h) in the subgroup algebra, and near-containment carries pi(Gamma) into B
target: sl3-re-lambda-exact-coprime-face-is-unfaithful
requires:
  - odd-congruence-lambda-exact-sector-collapses
---

The whole argument is in the claim body: the sector collapse's exact
evaluation `(OSP4)` `W = 1`, i.e. `pi(h) = V' in pi(Lambda)''`; the
commuting-square near-containment `pi(Lambda) subset B` upgraded to
`pi(Lambda)'' subseteq B` in the ultraproduct; generation
`Gamma = <Lambda, h>`; and the resulting `pi(Gamma) subset B`, which is
the negation of faithfulness.  The single cited input is
`odd-congruence-lambda-exact-sector-collapses` (coprimality-only form);
no regular-trace or multiplicity hypothesis is used, matching the sector
theorem's scope.
