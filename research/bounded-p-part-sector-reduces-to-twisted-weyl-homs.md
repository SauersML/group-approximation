---
rg: 2
id: bounded-p-part-sector-reduces-to-twisted-weyl-homs
kind: claim
title: At bounded p-part the corrector is an equivariant homomorphism twisted through one fixed finite gauge
distinct_from:
  odd-congruence-lambda-exact-sector-collapses: that closes the coprime-level sector, where the twist is absent and the Weyl evaluation forces the corrector to one; this treats mixed levels with BOUNDED p-part, where the same evaluation runs on the coprime leg and leaves an exactly-solvable-shaped residue: an S3-equivariant homomorphism of the coweight lattice twisted through the fixed finite p-block.
  two-adic-tower-format-of-surviving-lambda-exact-enemy: that is the format at UNBOUNDED p-part (pure p-power towers), where no exact slot exists at all; this shows every bounded-p-part enemy is governed by a finite gauge, so the surviving Lambda-exact sector splits into finitely-checkable bounded gauges and the unbounded tower.
  two-power-sector-conjugator-dichotomy: that is the all-or-nothing embedding rigidity for the unbounded tower; this is the complementary bounded regime, where the question is not existence of a conjugator but classification of twisted equivariant homs compatible with the regular trace.
---

Let `Gamma = SL_3(Z[1/p])`, `Lambda = SL_3(Z)`, and consider
`Lambda`-exact microstates at levels `n_m = p^(b_m) q_m` with
`gcd(q_m, p) = 1` and BOUNDED `p`-part, `b_m <= C`.  Passing to a
subsequence, `sigma_m = sigma_q^(m) otimes sigma_p` with `sigma_p` a
FIXED representation of the finite group `SL_3(Z/p^C)` (the gauge).
Write `B = sigma_p(image of Lambda_-)'`, a fixed finite-dimensional
algebra.  Then, with `V' = sigma(lambda_h)` for `lambda_h = h mod
q_m` and `= 1 mod p^C`:

1. The corrector `W = V'^* V` lies in `R_q otimes-bar B` (property
   (T) lifting; the coprime leg has full image), every `V'`-type slot
   lies in `sigma_q(G_q) otimes 1` and commutes with it, and
   conjugation by `pi(w)` (`w` a Weyl element) acts on `W` only
   through the gauge: `W^((w)) := (1 otimes sigma_p(w)) W (1 otimes
   sigma_p(w))^(-1)`.
2. The Weyl evaluation of the coprime sector now runs verbatim on
   the `q`-leg and leaves the exact system

   ```text
   W^((w_13)) = W^(-1),
   W (W^((w_23)))^(-1) = W^((w_12)),
   [W^((w)), W^((w'))] = 0,                                     (BP1)
   ```

   i.e. the corrector data is an `S_3`-EQUIVARIANT HOMOMORPHISM from
   the coweight lattice `Z^2` into the unitary group of the abelian
   algebra generated by the `W^((w))`, with the `S_3`-action twisted
   through the fixed finite-dimensional `sigma_p|_(Weyl)`.
3. When the gauge twist is trivial (`sigma_p` trivial on the Weyl
   elements, in particular `C = 0`), the vanishing of the
   `S_3`-coinvariants of the coweight lattice forces `W = 1` and the
   sector collapses (recovering the coprime theorem); in general the
   solutions of `(BP1)` are classified by the twisted-equivariant
   homs into `U(A)`, `A` abelian, a FINITE-GAUGE object: for each
   fixed `C` the compatibility of such a hom with the regular-trace
   conditions on the `h`-tower is a concrete, finitely-parametrized
   question.

Consequently the `Lambda`-exact sector of the collapse splits into:
(i) coprime levels — CLOSED; (ii) bounded `p`-part — reduced to
finitely many twisted-hom gauges per bound, each a checkable
classification; (iii) unbounded `p`-part — the tower format and the
conjugator dichotomy.  This is the `SL_3` realization of the
"`mu_12`-twisted carriers" phenomenon Dogon--Vigdorovich record at
`SL_2` (finite central twists as the surviving enemies of the
perfectness argument), here appearing as finite Weyl-gauge twists.

## Attempts

- **What is proved and what remains.**  Points 1-2 are proved by the
  same evaluation algebra as the coprime theorem (proof route); the
  classification demanded in point 3 is set up but not executed —
  the immediate next step is the smallest nontrivial gauge
  (`p = 2, C = 2`, `sigma_p` a representation of `SL_3(Z/4)`), where
  the twisted-hom solutions can be enumerated from the `S_3`-action
  on the character data of `B`, and each surviving hom tested
  against `tr(V^j) -> 0` along the `h`-tower.
- **Falsification honesty.**  A surviving twisted hom compatible
  with regularity at some finite gauge would NOT yet be an enemy (it
  must still satisfy the mixed-word regularity and assemble into a
  full `Gamma`-representation), but it would localize exactly where
  a construction should be attempted; emptiness at every gauge plus
  a closure of the unbounded tower would close the entire
  `Lambda`-exact sector for the pair.
