---
rg: 2
id: harmonic-cuntz-el3-is-fg-simple-over-all-gl-n-q
kind: claim
title: The scalar quotient of EL_3 over the harmonic Cuntz ring is a finitely generated simple group with solvable word problem containing every GL_n(Q)
distinct_from:
  gl-n-q-embeds-in-fg-simple-and-fp-decidable-groups: that gets a finitely generated simple host from an arbitrary two-generated overgroup through Belk–Zaremsky, one n at a time; this is one natural group, EL_3 of a purely infinite simple ring, containing every GL_n(Q), and with an explicit finite-presentation candidate.
  leavitt-unit-groups-over-every-field-are-simple-mod-scalars: that group, L_Q(1,2)^x/Q^x, is not finitely generated; this finitely generated group contains it.
  rational-leavitt-simple-group-embeds-in-fp-simple-group: that asks for a finitely presented simple overgroup of L_Q(1,2)^x/Q^x; this supplies a finitely generated simple overgroup and leaves finite presentation open.
  harmonic-cuntz-simple-group-is-finitely-presented: that is the open finite presentation of this group; this is its existence, simplicity and contents.
artifacts:
  - research/artifacts/gq-gq-cuntz-cstar-dictionary.md
---

**ESTABLISHED** by `harmonic-cuntz-el3-fg-simple-proof` (paper assembly of the
repository's ring-general root-detection theorems; not independently reviewed; no
novelty claimed).

Let `C_h` be the harmonic Cuntz ring of `harmonic-cuntz-ring-is-purely-infinite-simple`,
and let `EL_3(C_h)` be the subgroup of `GL_3(C_h)` generated by the elementary
matrices `e_ij(x)`, `i ≠ j`, `x ∈ C_h`. Let `Z = EL_3(C_h) ∩ {λI : λ ∈ Q^x}` and put

    Λ_h = EL_3(C_h) / Z.

1. **Simple.** For `n >= 3`, every normal subgroup of `EL_n(C_h)` either lies in
   the central scalar matrices or is all of `EL_n(C_h)`. Hence `Λ_h` is an infinite
   simple group.
2. **Finitely generated.** `EL_3(C_h)` is generated by the 42 matrices `e_ij(x)`
   with `i ≠ j` and `x ∈ {1, s_0, s_1, t_0, t_1, m_η, m_ζ}`.
3. **Contains every GL_n(Q).**
   - For `r >= 1`, `SL_(3r)(Q) = E_3(M_r(Q))` embeds in `EL_3(C_h)` through the
     unital embedding `M_r(Q) ⊂ C_h`, and it meets `Z` only in `±I`.
   - Take `3r >= n + 2`. Then `g ↦ diag(g, det(g)^(-1), 1, ..., 1)` embeds `GL_n(Q)`
     in `SL_(3r)(Q)` avoiding `±I`, so `GL_n(Q)` embeds in `Λ_h`.
4. **Contains Λ.** `Λ = L_Q(1,2)^x / Q^x` embeds in `Λ_h`. So does Thompson's `V`,
   and so does `SL_3(Z)`.
5. **Solvable word problem.** `Λ_h` has solvable word problem in the generators
   of part 2.
6. **Z is not finitely generated.** `Z = Q^x · I ≅ Z/2 × Z^(∞)`. So by B. H.
   Neumann's theorem, at most one of `EL_3(C_h)` and `Λ_h` is finitely presented.

## Consequence

`Λ_h` is a single natural finitely generated simple group containing every
`GL_n(Q)`. It is built from a purely infinite simple ring: the algebraic shadow of a
dense subring of the Cuntz algebra `O_2`.
- **Remaining question for the root along this route.** Whether `Λ_h` is finitely
  presented (`harmonic-cuntz-simple-group-is-finitely-presented`). The route
  `gl-n-q-via-harmonic-cuntz-host` records this.
- **Same for the rational Leavitt group.** It records the same for
  `rational-leavitt-simple-group-embeds-in-fp-simple-group`, through
  `rational-leavitt-fp-simple-via-harmonic-host`.
