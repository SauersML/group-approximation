---
rg: 2
id: thin-rational-actions-give-finite-germ-extensions
kind: claim
title: Thin generators plus clopen transitivity give condition (E1): the group generated by a boundary Thompson group and a rational action is a finite germ extension with countable, eventually periodic singular set
distinct_from:
  rational-germ-singular-sets-are-finite-graph-path-spaces: that describes one element's singular set as a finite graph path space; this assembles the three Belk--Hyde--Matucci axioms for the whole group from thinness of finitely many generators together with clopen transitivity.
  automatic-groups-embed-in-corner-germ-extensions: that is the OPEN premise (E1)-(E5) for all automatic groups; this proves (E1) from (E5) and a decidable check, so that premise reduces to (E2), (E3), (E5) and thinness.
  z2-free-z-normal-form-boundary-has-corner-singular-orbits: that verifies (E1) and (E4) for one structure by hand; this derives (E1) in general from thinness, and in particular explains why the hand verification only had to inspect the generators.
artifacts:
  - research/artifacts/gq-bh-bh-free-21-automatic-germ-extensions.md
---

**ESTABLISHED** by `thin-rational-actions-finite-germ-extension-proof` (lane proof,
elementary, not independently reviewed; no priority claimed).

## Statement

Let `L`, `∂L` and `B` be as in `rational-germ-singular-sets-are-finite-graph-path-spaces`, with
`∂L` having no isolated points. Let `G` be a group of rational homeomorphisms of `∂L`,
generated by a finite set `S`, and put `Γ = ⟨B, G⟩`. Assume:

- **(T) Thinness.** Every `s ∈ S` is thin, i.e. `sing_B(s)` is finite.
- **(E5) Clopen transitivity.** Any two nonempty clopen subsets of `∂L` are joined by a
  `B`-local bijection, that is, by a homeomorphism that is locally a prefix replacement.

Then:

1. `Γ` is a finite germ extension of `B` in the sense of Belk--Hyde--Matucci arXiv:2407.03149
   §1. All three of their axioms hold: finitely many singular points per element, `B` is
   exactly the non-singular part of `Γ`, and germs at a single point are realized by elements
   with one singular point.
2. `sing(Γ) = G·(⋃_{s ∈ S} sing_B(s))` is countable, and every point of it is eventually
   periodic.
3. Consequently condition (E1) of `automatic-groups-embed-in-corner-germ-extensions` follows
   from (E5) together with a decidable finite check on the generators, and its remaining
   content is (E2), (E3), (E5) and the germ condition (E4).

## Remarks

- Only (T) uses rationality; axioms (2) and (3) of Belk--Hyde--Matucci hold for any group of
  homeomorphisms with finite singular sets over a full base group with (E5).
- Thinness cannot be weakened to countability of the singular sets: by item 4 of
  `rational-germ-singular-sets-are-finite-graph-path-spaces`, a rational homeomorphism whose
  singular set is infinite either has a Cantor set of singular points or has a singular set
  whose graph has a branching recurrent vertex, and in the second case some power or product
  again has infinitely many singular points.
- The conclusion is about the fixed structure `L`. It says nothing about whether a given group
  admits a thin structure; the product normal form on `F_2 × F_2` is thin for neither factor's
  generators.

## Calibration and where (E5) really comes from

- **(E5) is not available for the automaton's own base.** For `Z^2 * Z` the boundary Thompson
  group of the BBCMP normal form has `K_0 ≅ Z^5` and is not clopen transitive
  (`z2-free-z-normal-form-boundary-has-corner-singular-orbits`, second landing). So the
  hypothesis (E5) of this claim fails for that `B`, and the claim says nothing there directly.
- **After recoding it is automatic.** By `prefix-replacement-actions-recode-into-germ-extensions-of-v`
  the base may be replaced by Thompson's `V`, for which any two nonempty clopen sets are
  `V`-equivalent, so (E5) holds. The intended use of this claim is therefore with `B = V` after
  recoding, where it supplies axiom (3) for free and reduces (E1) to the thinness check.
- **Thinness is the decidable form of (E1').** With `B = V`, hypothesis (T) is exactly the first
  clause of the condition (E1') recorded on `automatic-groups-embed-in-corner-germ-extensions`,
  and by item 3 of `rational-germ-singular-sets-are-finite-graph-path-spaces` the second clause
  of (E1') is redundant: finiteness of the singular sets already forces every singular point to
  be eventually periodic, and `sing_B(g^{-1}) = g(sing_B(g))` forces the images to be singular
  points too.
