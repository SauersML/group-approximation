---
rg: 2
id: same-center-whitehead-native-occurrence-compiler-route
kind: route
title: Promote authenticated same-center Whitehead candidates to the two-shared-gauge selector compiler
target: binary-leavitt-two-shared-gauge-selector-compiler
requires:
  - same-center-whitehead-analytic-candidate-package
  - same-center-whitehead-native-selector-subcorner-attachment
---

The package supplies a uniformly positive active carrier, the common Pauli
label factor, and conjugate reservoir gauges `V_1,V_2`.  The attachment claim
identifies the two actual native selector outputs with the unequal controlled
candidates on one positive model-dependent reducing subcorner.  Their source
and target fractions are therefore exactly the two rows `(SGS1)`, while the
braid supplies the conjugacy required in `(SGS2)`.  This is precisely the
two-shared-gauge selector compiler.

The affine `1/3-o(1)` floor is already contained in the package.  No exact
whole-carrier occurrence identity, Property `(T)`, or Kazhdan transport is
introduced by this route.

