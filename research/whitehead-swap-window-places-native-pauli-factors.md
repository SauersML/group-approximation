---
rg: 2
id: whitehead-swap-window-places-native-pauli-factors
kind: route
title: Add one Whitehead compression and branch swap to place the five native Pauli factors
target: separate-native-quarter-carrier-recovery
requires:
  - five-factor-native-carrier-transport-certificate
---

Dead route.  Strengthen the typed rank-five multiplication window by the
canonical Whitehead inverse pair, the fixed branch swap, and the multiplication
which forms the two-copy amplification.  The hoped conclusion was that this
return identifies the pre-amplification multiplicity reservoir, so the five
native factors lie in the two returned commuting algebras.

`whitehead-swap-copy-completion-retains-rectangular-escape` gives an exact
finite countermodel.  On `E direct sum F`, `F=E direct sum E`, the Whitehead
matrices are mutually inverse unitaries, the swap exchanges the two target
branches, and multiplication produces `Delta(g)=diag(g,g)` exactly.  The
same model contains the full target two-qubit Pauli packet and its fixed
standard-polynomial gap, yet it exchanges label dimension for spectator
multiplicity instead of recovering the original factor.  Thus this entire
bounded Britton/Whitehead/multiplication window still does not imply any of
the five same-reservoir placements.

A successful window must add a non-typed return beyond these cells.  Merely
repeating them at any fixed depth is also refuted by the iterated rectangular
model.  The first such return is the Leavitt range-sum fold
`r_fold=C_0C_1D^(-1)`, but the exact models in
`leavitt-range-sum-fold-does-not-pay-reynolds` and
`closed-compression-fold-loop-retains-s3-gauge` show that adjoining it still
does not recover the source reservoir: the scalar fold leaves an independent
nilpotent payload, and the closed two-branch compression/fold loop leaves an
`S_3` reservoir gauge.  What remains must couple a fold occurrence directly
to the native packet payload or pay a matrix boundary for failing to do so.
