---
rg: 2
id: amenable-bauer-factorial-tracially-complete-without-gamma
kind: claim
title: The Pauli tower completion is an amenable factorial II_1 tracially complete algebra over a Bauer simplex with no projection of constant trace 1/2 in its reduced power
distinct_from:
  pauli-tower-bundle-has-scalar-point-property: that is the scalar point property of the W*-bundle; this reads it in the CCEGSTW category, adding amenability, type II_1, factoriality and the failure of property Gamma in its strongest form.
  toms-ah-counterexample-to-uniform-gamma: that is a simple AH algebra without uniform Gamma whose trace simplex is Poulsen and whose closed face bundles are trivial; this is a non-simple algebra with a Bauer designated trace set and an existence gap at the unit.
---

**Statement (review PASS, part 9 §9.7 of `research/artifacts/ex-review2-analysis-2026-09-13-part9.md`).** Let `A` be the Pauli tower algebra of
`pauli-tower-bundle-has-scalar-point-property`, and `(M, X)` its uniform tracial completion, with
`X = T(A) = Prob(X̂)`.
1. `(M, X)` is a tracially complete C*-algebra that is factorial, of type II_1 and amenable, and `X`
   is a Bauer simplex.
2. For every free ultrafilter `ω`, `M^ω` contains no projection `p` with `τ(p) = 1/2` for all
   `τ ∈ X^ω`.
3. So `(M, X)` fails property Γ already for `S = {1}` and `k = 2`, and `A` fails uniform property Γ.

Proof: route `amenable-bauer-factorial-tracially-complete-without-gamma-proof`.
