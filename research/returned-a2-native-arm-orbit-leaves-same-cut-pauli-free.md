---
rg: 2
id: returned-a2-native-arm-orbit-leaves-same-cut-pauli-free
kind: claim
title: An uncancelled native arm on every returned A2 occurrence still leaves both same-cut Pauli commutators free
invalidates:
  - returned-a2-native-arm-orbit-authenticates-same-v-pauli-rows
artifacts:
  - research/returned-a2-native-arm-orbit-free-proof.md
  - non_mf_groups_exist.tex
  - GroupApproximation/Leavitt/FinitePresentation.lean
distinct_from:
  returned-a2-window-leaves-same-cut-pauli-commutators-free: that leaves every returned occurrence in the Boolean range-idempotent inventory; this attaches a genuine uncancelled s_0/t_0 opposite-arm cell at the source mark and therefore at all three translates.
  native-opposite-arm-return-has-finite-s6-model: that gives one finite native cell but does not put it beside the returned A2 orbit or preserve a common positive Pauli cut with two explicit maximal commutator defects.
  returned-boolean-coefficient-window-has-finite-marked-model: that fences only coefficients generated by 1,q_0,q_1,A; this countermodel has literal non-Boolean s_0 and t_0 occurrences.
---

**ESTABLISHED EXACT NON-BOOLEAN OCCURRENCE FENCE.**  Adjoin to the full
returned `A_2` window a native cell at its source parent mark containing

```text
x_56(s_0), x_16(s_0), x_01(t_0), x_61(t_0),           (NAF1)
```

the fold--compose--return rows, the opposite-root partial Whitehead and its
braid/root swaps, and the three fixed occurrence transports from
`native-opposite-arm-return-has-finite-s6-model`.  Retain the translates of
this whole cell by the literal order-three relative word `V`, so the same
native table is present at every occurrence in the returned orbit.

There is a finite exact model of this enlarged menu with a `V`-invariant
projection `E` of normalized trace `1/8` and an anticommuting Pauli pair
`X,Z` on `E` such that

```text
||[V,X]E||_(2,E)^2=||[V,Z]E||_(2,E)^2=2.              (NAF2)
```

The construction is a finite quotient of an amalgam of the existing finite
returned-Pauli model with the finite native `S_6` model over their common
nontrivial parent-mark involution.  The amalgam is virtually free and hence
residually finite; one finite quotient can be chosen injective on the whole
finite returned-Pauli factor.  Thus all returned trace and commutator
calculations survive verbatim, while the second factor supplies the genuine
uncancelled coefficient arms.

Consequently leaving the Boolean coefficient sublanguage is necessary but
not sufficient.  Neither the presence of `s_0,t_0`, their reuse in one
native opposite-root packet, nor putting conjugate copies of that packet at
all three returned marks identifies the Pauli occurrences.  The remaining
row must share a literal arm or payload generator with `X` or `Z` across the
amalgamation seam, or derive a quantitative boundary payment for failure of
that sharing.

The literal source census supplies no hidden shortcut.  The manuscript
defines the coefficient algebra by the four inverse rows and the one range
sum, and `GroupApproximation/Leavitt/FinitePresentation.lean` records exactly
those five equations together with characteristic two in the absolute ring
presentation.  The elementary-group layer adds the Steinberg relation
schemas.  None of these primitive rows names a Pauli occurrence.  Among the
derived non-Boolean rows currently recorded in Cairn, the native
opposite-arm cell above is the strongest one that carries an uncancelled
`s_i` or `t_i` through a returned mark; the other audited primitive
native/Hecke rows are fenced by
`primitive-native-hecke-relators-have-no-odd-mixed-occurrence`.  Thus this
countermodel reaches the actual present source inventory, rather than only
the earlier Boolean abstraction.

DERIVATION
returned-a2-native-arm-orbit-free-proof
