---
rg: 2
id: seven-head-return-shares-sixteenth-atom-with-u2
kind: claim
title: The seven-head return and the second Whitehead share a positive sixteenth atom
artifacts:
  - research/seven-head-u2-common-atom-proof.md
distinct_from:
  seven-head-star-return-retains-eighth-flag: that computes the exact eighth source/range supports and the formal Coxeter reset; this refines by the remaining Pauli sign and compares the actual head word with an actual outer-corrected Whitehead on one common source and range.
  binary-leavitt-native-two-scale-cross-gram-positive: that asks for the native two selector transports with the prescribed unequal label ranks on a common positive carrier; this proves full cross-Gram mass for one asymmetric seven-head return and one equal-label Whitehead, but does not yet type the return as the native one-eighth selector.
  binary-leavitt-two-label-difference-occurrence-cells: that asks for two independent odd occurrence gauges and would close the full compiler; this exposes one exact relative reservoir gauge but supplies no odd relation forcing it to be trivial.
---

In the four-cell star packet, also write

```text
Z_1=B_1=x_72(b_1).
```

The four commuting `Z_i` are the four Pauli `Z` operators on `E_4`.  Let

```text
R_8=H_3(H_2H_1)^3,
P_epsilon=E_4 (1+epsilon Z_1)(1-Z_2)(1+Z_3)(1+Z_4)/16,
Q_epsilon=E_4 (1+epsilon Z_1)(1+Z_2)(1-Z_3)(1+Z_4)/16, (SCA1)
```

for `epsilon` equal to `+1` or `-1`.  Then

```text
R_8 P_epsilon R_8^(-1)=Q_epsilon.                    (SCA2)
```

Let `U_2=L_2J_2` be the literal outer-corrected Whitehead which swaps star
cells two and three.  It fixes cells one and four, so

```text
U_2 P_epsilon U_2^(-1)=Q_epsilon.                    (SCA3)
```

Both restricted occurrences are therefore unitaries between the **same**
source and range:

```text
S_epsilon=Q_epsilon R_8P_epsilon,
T_epsilon=Q_epsilon U_2P_epsilon,

S_epsilon^*S_epsilon=T_epsilon^*T_epsilon=P_epsilon,
S_epsilon S_epsilon^*=T_epsilon T_epsilon^*=Q_epsilon. (SCA4)
```

In particular their cross-Gram is full:

```text
(S_epsilon^*T_epsilon)(S_epsilon^*T_epsilon)^*=P_epsilon,
tau(P_epsilon)=tau(E_4)/16>0.                         (SCA5)
```

Because all four Pauli `Z` signs have been fixed, the label part of
`P_epsilon H` is one-dimensional.  After the standard four-cell Pauli
decomposition

```text
E_4H=(C^2)^(tensor 4) tensor M,
```

the relative unitary `S_epsilon^*T_epsilon` on `P_epsilon H` is therefore
`I` on the label line tensored with one unitary on the literal common
reservoir `M`.  No mutual-bicommutant or ambient-fullness assertion is
needed.

This is a genuine same-reservoir occurrence result, but not yet the second
selector row.  The seven-head word has not been proved to carry the native
one-eighth controlled-reflection label, and no established odd ordinary
relation forces the exposed relative reservoir unitary to be the identity.
Thus `(SCA5)` removes the common-carrier ambiguity for this comparison but
does not remove its holonomy.

No Property `(T)`, canonical trace, stability theorem, or literature input
is used.

DERIVATION
seven-head-u2-common-atom-proof
