---
rg: 2
id: hyperbolic-simple-triangle-group-without-finite-quotients
kind: claim
title: A hyperbolic triangle of finite simple groups whose fundamental group has no nontrivial finite quotient
distinct_from:
  hyperbolic-group-without-finite-quotients: that is the general Kapovich--Wise target; this restricts to fundamental groups of hyperbolic triangles of finite simple groups, where having no finite quotient is equivalent to failing virtual torsion-freeness
  kazhdan-hyperbolic-group-without-finite-quotients: that demands property (T); this demands simple vertex groups and nontrivial edge groups, and says nothing about (T)
---

Exhibit a triangle of groups with the following data:
- finite simple vertex groups;
- nontrivial edge groups;
- trivial face group;
- Gersten–Stallings angle sum `< π`.

Its fundamental group `G` must have no nontrivial finite quotient. Such a `G` is
infinite and word-hyperbolic. By `simple-vertex-groups-vtf-iff-some-finite-quotient`,
the negative answer in this class is exactly a member that is not virtually
torsion-free, and it would decide `hyperbolic-groups-virtually-torsion-free` and
`non-residually-finite-hyperbolic-group` together.

**OPEN.** No member is known either way in general: neither every member having a
nontrivial finite quotient, nor some member having none.

## Attempts

- **Structured members have quotients (calibration).** If the three vertex groups lie
  in one finite group `S` with the edge groups as their pairwise intersections, the
  inclusions give a vertex-injective quotient. Congruence-type examples behave the same
  way. `cckw-ghb2-kms-group-structure` has `SL_4(F_7)`, but its vertex groups are
  unipotent, not simple. A candidate therefore needs edge identifications with no
  common finite overgroup of small size.
- **Closest data: Caprace–Conder–Kaluba–Witzel, arXiv:2011.09276** (text extraction
  `cckw2.txt` on MSI, read 2026-09-12). Their Theorem 3.1(iv) is the vertex-injective
  torsion-free kernel criterion used here. They enumerate 252 trivalent triangle groups,
  with edge groups of order 3 and vertex groups acting regularly on the edges of small
  cubic graphs. These vertex groups have orders at most about 54, so they are not
  simple, and the Lemma does not apply. Their §4 overview says verbatim: "for some of
  the hyperbolic groups with girth triple (4,8,8), (6,6,6) and (6,6,8) on our list, we
  could not find any (non-abelian) finite simple quotient at all". The searches covered
  simple quotients of order at most `5·10^7` and alternating quotients of degree at most
  30 (sometimes 40). They also warn that such searches miss large quotients:
  `G^{14,14,18}_4` showed no small simple quotient, yet a degree-6 unitary representation
  gives `PSp_6(p)` quotients, and it is virtually torsion-free (their Remark 5.16).
  A simple-vertex analogue of their unresolved cases is exactly a candidate for this
  claim.
- **Explicit candidates (2026-09-12).**
  `research/artifacts/simple-vertex-triangle-quotient-search-2026-09-12.md` gives four
  hyperbolic `(A_7, A_7, A_8)` triangles with `Z/7` edge groups and half-girth type
  `(3,3,4)`, as explicit presentations. None maps nontrivially into `Sp_6(2)`. The zeros
  for `A_8` through `A_13` hold for the whole family by a support argument, so they are
  not evidence. Informative alternating targets start at degree 15.
- **Closest literature.** Lubotzky–Manning–Wilton, arXiv:1702.08200v2, build k-fold
  triangle groups from LPS Ramanujan graphs. Their vertex groups are finite quotients
  of free groups, and the edge groups meeting `G_0` are trivial, so the Lemma does not
  apply. They ask, verbatim, "Question 1.8. Are the examples from Corollary 1.7
  virtually torsion-free? Residually finite? Linear?"
- **Counting heuristic (not a proof).** For a finite simple `S`, the expected number of
  compatible triples of embeddings, averaged over the positions of the edge subgroups
  inside the vertex groups, is `∏_e |C_S(E_e)| / ∏_v |C_S(A_v)|` up to bounded factors.
  In the free-orbit regime this is about `|S|^{1 - χ_orb}` for alternating and
  bounded-rank Lie type `S`. Here `χ_orb = Σ_v 1/|A_v| - Σ_e 1/|E_e| + 1`, so conjugacy
  classes of epimorphisms number about `|S|^{-χ_orb}`. Actions in which edge
  generators fix many points dominate instead, and they inflate the count. So
  candidates should use edge groups generated by elements with uniformly small
  fixed-point ratios, for example regular semisimple elements. The whole count is
  beaten by structure, as the calibration shows.
- **Where it dies.** Showing that no quotient exists means excluding epimorphisms onto
  every finite simple group. This is the same large-quotient barrier as
  `random-complex-group-no-quotients-above-exp-pn`. Low-rank Lie type is the worst
  case: in `PSL(2,q)` the normalizers of cyclic edge groups have order about `q`, and
  three of them already balance `|S|`. So vertex groups should not embed in `PSL(2,q)`.
- **Complete permutation and small Lie-type searches (2026-09-13).** An exhaustive
  enumeration over embedding classes and double cosets finds no nontrivial homomorphism
  from any of the four candidates into `S_n` for `n <= 30`. It also finds none into
  `Sp_6(2)`, `L_5(2)`, `O_8^+(2)`, `O_8^-(2)` or `L_4(4)`. The search is calibrated
  against brute-force counts and known transitive degree-15 actions, and a literal `A_8`
  triple is found in each Lie-type target. So no candidate acts nontrivially on at most
  30 points. This is evidence, not a decision: degrees up to 14 are forced by support,
  and the free-orbit count predicts zeros at this scale either way. See
  `research/artifacts/hyperbolic-triangle-quotient-search-2026-09-13.md`.
