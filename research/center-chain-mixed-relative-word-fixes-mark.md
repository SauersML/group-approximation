---
rg: 2
id: center-chain-mixed-relative-word-fixes-mark
kind: claim
title: The exact mixed center-chain relative word also fixes the marked root
artifacts:
  - research/center-chain-mixed-relative-word-proof.md
distinct_from:
  center-chain-mixed-hecke-overlap-is-common-pauli-source: that proves a maximal common-source polar overlap on one fixed regular character corner; this computes its relative full group word against the marked root and rules out promoting that overlap by the disjoint-root criterion.
  literal-prefix-whitehead-relative-word-fixes-mark: that treats the subgroup generated by the two adjacent native Whitehead words; this treats the separate mixed Hecke words built from the chained A and B arms.
  canonical-cross-gram-does-not-close-arbitrary-mf-active-profile: that gives the trace-profile quantifier mismatch in general; this is the exact word calculation for the strongest current center-chain candidate.
---

Use the literal center-chain mixed words

```text
u_1=B_2A_1,
u_2=B_3A_2A_1,
U=u_1^(-1)u_2                                      (CMF1)
```

of `center-chain-mixed-hecke-overlap-is-common-pauli-source`, and retain the
marked root `z=x_13(q)`.  Every elementary root factor in `(CMF1)` has both
indices in

```text
K={2,4,5,6,7,8,9},
```

which is disjoint from `{1,3}`.  Therefore

```text
[u_1,z]=[u_2,z]=[U,z]=1,
U z U^(-1)=z.                                           (CMF2)
```

The same is true of the tail-removed word

```text
R=B_2UQ=g_1B_3A_2Q
```

before compression by `Q`: each displayed full group-word factor
centralizes `z`.

Consequently the exact common source, common range, and relative polar in
the mixed center-chain calculation do **not** instantiate the
disjoint-root alternative of
`root-valued-relative-transport-forces-cross-gram`.  Their positive
`1/128` overlap is a regular-character Hecke calculation.  It cannot be
promoted to the arbitrary-profile MF claim merely by reinterpreting `U` as
a mark-moving relative word.

This does not refute a new mixed occurrence row containing indices `1` or
`3`.  Such a row would be additional occurrence holonomy and would still
have to authenticate the two unequal native selector meanings and their
mixed Pauli gauge.  None is present in `(CMF1)`.

No Property `(T)`, Kazhdan input, canonical-trace substitution, or
literature theorem is used in the word calculation.

DERIVATION
center-chain-mixed-relative-word-proof
