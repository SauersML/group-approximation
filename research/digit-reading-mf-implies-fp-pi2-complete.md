---
rg: 2
id: digit-reading-mf-implies-fp-pi2-complete
kind: route
title: MF of the digit-reading group, if machine-compatible, closes the finite-presentation compiler
target: c-free-aanderaa-rope-is-mf-with-fine-quotients
requires:
  - machine-free-digit-reading-group-is-mf
  - modular-machine-boone-group-is-mf
---

A construction of approximate representations of `Gamma^K` that survives
the adjunction of the central machine letters over subgroups of `< x, y, t
>` gives clause (a) of `(CF1)`; clauses (b) and (c) then have to be replaced
by the abstract corona compatibility of the insertion edge (finite
quotients of the compiler output kill `C`), which the same approximate
representations must supply.
