---
rg: 2
id: bcs-gap-compression-terminal-proof
kind: route
title: Sandwich the localized BCS energy between active mass and presentation energy
target: bcs-gap-compression-is-the-groupifier
requires:
  - corner-localized-non-ce-bcs-energy-gap
---

Apply the corner-localized gap `(BCF2)`, then the assumed ordinary-word upper
bound `(BCF3)` and seed `(BCF4)`.  Rearrangement gives `(BCF5)`.  If the mark
stayed separated in canonical microstates, the right side would be positive
while presentation defect tends to zero.  The exact tracial model proves the
mark is nontrivial.  Without `(BCF3)`, `(BCF2)` has no presentation-energy
term and supplies no typed multiplicity floor.
