---
rg: 2
id: stw63-uniqueness-via-joint-central-bridge
kind: route
title: Solve approximate Jiang--Su uniqueness by constructing a joint bridge for every pair
target: stw99-problem-lxiii-z-embedding-uniqueness
requires:
  - stw63-every-z-pair-has-joint-central-bridge
  - joint-central-z-bridge-forces-approximate-uniqueness
---

## Why sufficient

Given `B,phi,psi`, the first prerequisite supplies a unital copy of `Z` in
their joint relative commutant in `B_infinity`.  The second prerequisite
applies directly and produces unitaries in `B` implementing approximate
unitary equivalence.
