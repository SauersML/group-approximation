---
rg: 2
id: v-loop-finitely-generated-via-cloning-images
kind: route
title: "V_loop is finitely generated because LB_n is generated by images of the cloning maps"
target: loop-braided-thompson-group-v-loop-is-finitely-generated
requires:
  - loop-braid-cloning-system-is-properly-graded
artifacts:
  - research/artifacts/zp-loop-braid-cloning-properly-graded-2026-09-13.md
---

Witzel–Zaremsky, arXiv:1405.5491v3, Lemma 5.10 (p. 32), verbatim: "Let G∗ be a
family of groups equipped with a properly graded cloning system, with cloning
maps κ^n_k. Suppose that for n sufficiently large, all G_n are finitely
generated and also are generated by the images of the cloning maps with
codomain G_n. Then T(G∗) is finitely generated."

Proper grading is the required claim. For `n ≥ 5`, `LB_n` is generated by the
`β_i, σ_i` (WZ p. 53), and each of these generators is a cloning image:

- for `i ≤ n−3`: `β_i = (β_i)κ^{n−1}_{n−1}` by the case `k > i+1` of (10.1);
- for `i ∈ {n−2, n−1}`: `β_i = (β_{i−1})κ^{n−1}_1` by the case `k < i−1`,
  valid since `i ≥ 3`;
- the same holds for `σ_i` by (10.2).

So `V_loop` is finitely generated. See §4 of the artifact.
