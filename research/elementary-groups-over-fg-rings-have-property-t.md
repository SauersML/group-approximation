---
rg: 2
id: elementary-groups-over-fg-rings-have-property-t
kind: claim
title: EL_n(R) has property (T) for every finitely generated associative ring R and n >= 3 (Ershov--Jaikin-Zapirain)
distinct_from:
  affine-semidirect-sl3-source-has-property-t: that is property (T) for the full integral affine group of rank three; this is the Ershov--Jaikin-Zapirain theorem for EL_n over every finitely generated associative ring.
artifacts:
  - research/artifacts/bh-full-cantor-ca-host-2026-09-12.md
---

**ESTABLISHED (import).** Let `R` be a finitely generated associative ring with
`1`, and `n >= 3`. The subgroup `EL_n(R) <= GL_n(R)` generated by elementary
matrices has Kazhdan's property (T).

The source is Ershov--Jaikin-Zapirain, *Property (T) for noncommutative universal
lattices*, arXiv:0809.4095v2 (Invent. Math. 179 (2010)). Their Theorem
`thm:main`, quoted from the arXiv TeX source `EL_n0909.tex` fetched to MSI on
2026-09-12:

> Let $R$ be a finitely generated (associative) ring with $1$ and $n\geq 3$. Let
> $G=EL_n(R)$, that is, the subgroup of $GL_n(R)$ generated by elementary
> matrices. Then $G$ has Kazhdan's property $(T)$.

**Consequences used in the graph.**
- `EL_3(Z[t])` has (T). By Suslin's theorem (imported, not re-read)
  `SL_3(Z[t]) = EL_3(Z[t])`, so `SL_3(Z[t])` has (T).
- Every finite-index subgroup of `EL_n(R)` has (T), and so (FH), (FW) and (FA).
