---
rg: 2
id: ordinary-green-curl-linear-proof
kind: route
title: Rotate one coordinate between two code characters
target: ordinary-green-triangle-curl-has-a-linear-code-tangent
requires:
  - low-odd-square-removes-pairwise-green-stationarity
  - tangent-commutator-covariance-bypasses-code-riesz-endpoint
---

For `(OGC1)`, `Q_i(t)Z=exp(-2itY)`, so the square is
`exp(-4itY)`.  Substitute into `(LOS1)` to obtain `(OGC2)` and `(OGC3)`.
The scalar coordinate `S_k=I` commutes with every perturbation, and the
unmoved pair `(j,k)` also commutes.  This proves the no-go.
