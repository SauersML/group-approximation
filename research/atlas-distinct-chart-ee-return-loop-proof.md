---
rg: 2
id: atlas-distinct-chart-ee-return-loop-proof
kind: route
title: Weyl-transport the nested return and retain both transported nuisances
target: atlas-distinct-chart-ee-return-retains-loop-gauge
requires:
  - atlas-two-e-strip-return-retains-common-gauge
  - atlas-reverse-dictionary
  - atlas-steinberg-spare-index-independence
  - atlas-mixed-minor-needs-nuisance-quotient-rank
---

The standard Steinberg Weyl words `w_13,w_24` conjugate `x_12(1)` to
`x_34(1)`. The reverse dictionary identifies these unit roots with `p1_23`
and `p2_23`, respectively. The MSI audit verifies the completely expanded
word and the lengths in `(DCR4)`.

In the projected row system the two conjugated expressions are distinct
reduced monomials `N_1,N_2`. Retaining both as nuisances gives `(DCR5)`, so
the nuisance-quotient increment remains two. Assignment `(DCR7)`, with an
arbitrary spectator on the Weyl edge, realizes the kernel exactly.
