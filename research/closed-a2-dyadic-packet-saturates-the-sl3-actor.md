---
rg: 2
id: closed-a2-dyadic-packet-saturates-the-sl3-actor
kind: claim
title: The literal closed A2 root gallery plus one dyadic transport saturates the full SL3 arithmetic actor
distinct_from:
  native-parahoric-triangle-kills-all-corrector-gauges: that proves a dimension-free collapse for a corrector already supported on one common carrier; this identifies the group generated by the literal root and denominator words and shows that there is no smaller closed-gallery actor packet left between that endpoint and the full arithmetic problem.
  six-chamber-a2-holonomy-closes-fixed-clock-sector: that closes a fixed finite oscillator packet after its common carrier has been recovered; this is the integral/dyadic generation statement showing that all literal opposite-root identifications already recover the infinite arithmetic actor.
  one-shared-root-plus-dyadic-depth-has-ut4-countermodels: that leaves the opposite simple roots independent and therefore admits a nilpotent finite-quotient calibration; this proves that closing those roots inside one genuine A2 reservoir destroys that intermediate relaxation and reaches the original SL3 actor.
---

Put

```text
C=SL_3(Z),             A=SL_3(Z[1/2]),
h=diag(2,1,1/2),       x_ij(t)=I+tE_ij.                (CAD1)
```

Let `P` be the subgroup of `A` generated by

```text
x_12(1), x_21(1), x_23(1), x_32(1), h.                (CAD2)
```

Then

```text
P=A.                                                           (CAD3)
```

Thus a packet which literally identifies both simple roots and their
opposites in one `A_2` reservoir and also carries the native dyadic transport
does not define a proper intermediate actor.  It already contains every
arithmetic root word used by the original relative-commutant problem.

## Generation

The four integral root elements in `(CAD2)` generate `SL_3(Z)`: their powers
give the four simple root groups, their Steinberg commutators give the two
remaining root groups, and elementary row reduction over `Z` gives `C`.
In particular `P` contains all signed permutation/Weyl words.

For every `k>=0`,

```text
h^(-k) x_12(1) h^k=x_12(2^(-k)).                       (CAD4)
```

Conjugating `(CAD4)` by signed permutation matrices in `C` gives
`x_ij(plusOrMinus 2^(-k))` for every `i!=j`.  Powers give
`x_ij(m/2^k)` for every `m in Z`.  Since `Z[1/2]` is Euclidean, elementary
row reduction gives

```text
E_3(Z[1/2])=SL_3(Z[1/2]),                               (CAD5)
```

proving `(CAD3)`.

## What exact finite calibration can and cannot do

Odd congruence representations of `A` are exact finite-matrix calibrations
of every literal root, Weyl-gallery, Steinberg, and dyadic-conjugation word
in this packet.  Hence those actor relations alone cannot pay a positive HS
gap.

On the other hand, finite-dimensional co-density gives

```text
rho(C)'=rho(A)'                                         (CAD6)
```

for every exact finite-dimensional representation `rho` of `A`.  Therefore
an exact common-carrier corrector commuting with the four roots in `(CAD2)`
already commutes with the whole actor.  More sharply, if it obeys the native
ordered parahoric triangle, `native-parahoric-triangle-kills-all-corrector-gauges`
gives the dimension-free estimate

```text
||Z-I||_2 <= 9 epsilon_tri+10 epsilon_D                 (CAD7)
```

before the fixed approximate-actor defect terms are added.

Consequently the first unfenced packet is not missing another root or Weyl
relation.  It is missing the prior assertion that the separately decoded
rectangular chamber pieces are restrictions of one common carrier on which
`Z`, its two Weyl translates, and the four parahoric invariances in `(CAD7)`
all live.  Adding further literal actor words cannot manufacture that
support assertion, because `(CAD3)` says the actor has already saturated.

## Regular-HNN firewall

This saturation does not contradict the regular-HNN/coset-Bernoulli leak.
That leak contains an exact copy of the full actor `A`, so it automatically
satisfies every relation in `(CAD2)--(CAD5)`.  Its external `C`-fixed lamp is
not supplied as a common-carrier corrector satisfying the ordered triangle;
extracting such a supported corrector is precisely the open matrix-only
step.  Thus `(CAD7)` is a coercive endpoint after authentication, not a
tracial identity that can be applied to an unauthenticated lamp.

The most canonical attempted authentication also has an exact limitation.
The Jones projection for the intersection of the six depth-`N` parahorics
does place every Weyl chart on one common Fourier atom, but that atom has
trace at most `2^(-2N)`; see
`closed-weyl-iwahori-jones-carrier-has-vanishing-depth-mass`.  The saturated
actor therefore forces one to lift the full growing coset PVM, not select a
single depth-independent Iwahori atom.
