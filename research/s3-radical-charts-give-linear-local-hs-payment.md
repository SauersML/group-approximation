---
rg: 2
id: s3-radical-charts-give-linear-local-hs-payment
kind: claim
title: Three fixed-scale radical charts turn violation mass into linear local HS payment
refuted_by: bass-serre-marginals-do-not-charge-rank-one-failure
distinct_from:
  rank-one-no-signaling-pairing-floor: that gives the scalar contextual failure floor once three endpoint-sharing binary transports exist; this identifies those transports with finite packet types and charges their mismatch to group-word energy.
  finite-group-multiplicity-mismatch-pays-hs-energy: that charges a supplied weighted type-vector discrepancy for one finite group; this forces such a discrepancy from every forbidden radical fiber.
  support-failure-packet-has-explicit-hs-payment: that pays directly for the minimal binary support packet; this works uniformly for every fixed Boolean predicate through its S3-equivariant radical compiler.
---

REFUTED under the stated endpoint-marginal and transport hypotheses.  The
exact finite Bass--Serre model in
`bass-serre-marginals-do-not-charge-rank-one-failure` has positive forbidden
selector mass and perfectly matching endpoint marginals while every transport
energy is zero.  Contextual failure mass is therefore not automatically an
isotypic multiplicity mismatch.

The failed proposed statement was the following.  Fix one predicate `f` and
its three finite crossed-product charts `R_i` from
`three-radical-twist-crossed-products-have-uniform-scale`.  Let `q` be the
normalized Hilbert mass of the marked forbidden selector sectors after fixed
packet exactification.  Suppose three matrix transport words compare the
three `R_i` charts through their shared `K_f` endpoint restrictions, with the
endpoint type marginals identified by the common-subgroup alignments.

There are constants `c_f,C_f>0`, depending only on these finite tables, such
that their total normalized-HS transport energy satisfies

```text
E_transport >= c_f q-C_f eta,                                  (RLP1)
```

where `eta` is the normalized weighted type mass discarded by packet
exactification and overlap alignment.  In an asymptotic microstate sequence,
`eta=o(1)`, so

```text
E_transport >= c_f q-o(1).                                    (RLP2)
```

The invalid argument claimed that on every forbidden simple fiber the three restriction matrices are
the three perfect matchings of the four radical characters.  Their common
endpoint marginals satisfy `(RNS1)`.  Therefore the sum of the three failed
matching masses is at least one on the normalized fiber by `(RNS2)`, and at
least `q` after integration over the forbidden carrier.  Up to the discarded
mass, one finite chart consequently has an extensive weighted irreducible
multiplicity mismatch, after which `(FGMP1)` would charge the mismatch to
ordinary group-generator intertwining energy.  The countermodel instead
populates the failed types with a stationary regular type vector, leaving no
edge mismatch to charge.

Any valid successor needs an independent non-Bass--Serre two-cell or
matrix-only capacity condition making the failed types themselves pay energy.
Constructing the three transport words alone is insufficient.

## Attempts

- The scalar rank-one floor remains valid.  What fails is the implication from
  that scalar mass to transport energy; a future route must add a genuine
  holonomy or same-reservoir hypothesis not present here.
