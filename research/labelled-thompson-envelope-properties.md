---
rg: 2
id: labelled-thompson-envelope-properties
kind: claim
title: The labelled Thompson envelope G into V(G) is injective, preserves type F_n and word-problem solvability, and leaves no proper finite-index subgroups
distinct_from:
  labelled-thompson-groups-are-acyclic: that imports the acyclicity of V(G) (Palmer--Wu Theorem 2.6); this imports the other structural properties of the same envelope, which Palmer--Wu attribute to Thompson and to Wu--Wu--Zhao--Zhou.
artifacts:
  - research/artifacts/hl-kl-minimal-counterexample-2026-09-14.md
---

**ESTABLISHED (literature import, secondary for items 1–5).** For every
discrete group `G`:
- the natural map `iota_0 : G -> V(G)` is injective;
- `V(G)` has solvable word problem if and only if `G` does;
- `V(G)` is of type `F_n` if and only if `G` is, so in particular it is
  finitely presented iff `G` is;
- `V(G)` has no proper finite-index subgroups;
- `V(G)` is strongly torsion generated, and `Z(V(G)) ≅ Z(G)`.

Source: Palmer--Wu, arXiv:2510.16879v1, verbatim.
- "Theorem ([Tho80, WWZZ25]). The functor V and the embedding ι_0 have the
  following properties, for any discrete group G:
  - (1) ι_0: G ↪ V(G) is injective, and is also a quasi-isometric embedding
    when G is finitely generated;
  - (3) V(G) has solvable word problem if and only if G has solvable word
    problem;
  - (4) V(G) is of type F_n if and only if G is of type F_n;
  - (5) V(G) has no proper finite-index subgroups".
- "Proposition B (Propositions 2.8 and 2.9). For any discrete group G, we
  have: the centre of V(G) is isomorphic to the centre of G; the group V(G) is
  strongly torsion generated."

[Tho80] is R. J. Thompson, *Embeddings into finitely generated simple groups
which preserve the word problem*, Word problems II (1980). [WWZZ25] is
Wu--Wu--Zhao--Zhou, J. Lond. Math. Soc. (2) 111 (2025) e70164. Neither
original was read here: items 1–5 are imported through Palmer--Wu's summary
theorem.

DERIVATION
labelled-thompson-envelope-properties-citation
