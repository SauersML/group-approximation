---
rg: 2
id: paid-t00-support-enters-full-native-gl6-hull
kind: claim
title: Paid t00 mass enters a carrier reducing the full native GL6 actor
artifacts:
  - research/paid-full-native-gl6-reynolds-hull-proof.md
distinct_from:
  paid-t00-reynolds-cut-is-whitehead-reducing: that Reynolds-averages over the six-element group generated by J1 and J2 and keeps the cut inside their constant Pauli atom; this averages over the full literal GL6 actor and therefore reduces both native Singer heads and the endpoint actor as well.
  paid-cut-has-moving-hecke-source-or-detector: that constructs a positive moving subcut of the signed Hecke source, or a paying detector; this constructs a full-actor reducing hull but does not put that hull inside the signed source.
  center-chain-common-source-authenticates-native-covariances: that asks for the unequal native source and target rows on the signed common source; this removes all native/endpoint actor leakage on a positive marked carrier while deliberately leaving source incidence open.
---

**ESTABLISHED FULL-ACTOR REYNOLDS HULL.**  Let `rho` be any
finite-dimensional unitary representation of the literal binary-Leavitt
Steinberg packet and put

```text
P=(1-rho(z))/2.
```

Assume `P!=0`.  Choose the paid Pauli-menu member from
`paid-t00-pauli-menu-hits-whitehead-constant-atom`, and write

```text
A=e C_epsilon e,              0<=A<=P,
tau(A)>=tau(P)/128.                                      (PGR1)
```

Let `G_6` be the literal finite actor from
`two-native-singers-and-endpoint-generate-literal-gl6`.  Define

```text
B=|G_6|^(-1) sum_(g in G_6) rho(g)A rho(g)^*,
E=supp(B).                                               (PGR2)
```

Then

```text
0!=E<=P,                    [E,rho(G_6)]=0,
tau(E)>=tau(A)>=tau(P)/128,                              (PGR3)
supp(A)<=E.                                              (PGR4)
```

Consequently `E` reduces both native order-seven heads, all four individual
native head factors, and the endpoint Whitehead.  The paid positive
contraction itself remains entirely inside this full-actor carrier.

The conclusion is an actor hull, not a signed-source return.  In general a
finite Reynolds hull containing a positive projection `R<=Q` need not
contain any nonzero actor-reducing subprojection of `Q`.  Already on
`C^2`, if `S` swaps the two coordinate lines and

```text
R=Q=e_00,
(1/2)(R+SRS)=I/2,
E=I,                                                     (PGR5)
```

then `E` is the full `S`-reducing hull of `R`, while the largest
`S`-reducing subprojection of `Q` is zero.  Thus `(PGR3)--(PGR4)` cannot be
silently intersected with the signed Hecke source.  A continuation needs an
ordinary mixed incidence or full-Gram relation attaching a positive
`Q`-cut to the actor hull.

The construction also exists in the canonical regular finite von Neumann
algebra.  Therefore no dimension-free tracial use of `(PGR2)` can produce
the vanishing-loss native authentication forbidden by
`vanishing-loss-center-chain-authentication-hits-affine-firewall`.  The new
gain is exactly the removal of actor leakage; the surviving seam is
signed-source and unequal-label occurrence typing.

No Property `(T)`, Kazhdan input, canonical trace assumption, stability
theorem, or literature input is used.

DERIVATION
paid-full-native-gl6-reynolds-hull-proof
