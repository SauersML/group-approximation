---
rg: 2
id: sensitivity-right-symmetry-noninjective-proof
kind: route
title: Right invariance of the single-site sensitivity profile makes two single-site configurations collide
target: sensitivity-profile-right-symmetry-forces-noninjectivity
requires: []
artifacts:
  - research/artifacts/gk-vf-nonlinear-verification-2026-09-12.md
---

`tau(b e_k)(g) = f((b e_k(gm))_m)` equals `f(b e_m0)` when `g^-1 k = m0` lies in `M`, and
`f(0)` otherwise. So `tau(b e_k) = f(0) + P_k` with `P_k(g) = p_b(g^-1 k)`.

If `p_b` is right-invariant under `c != 1`, then `P_(kc)(g) = p_b(g^-1 k c) = p_b(g^-1 k) = P_k(g)`.
The configurations `b e_(kc)` and `b e_k` are distinct and have equal images. Section 1.3 of
the artifact.
