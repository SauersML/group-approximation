---
rg: 2
id: middle-index-order-seven-orbit-cut-is-native-scale-neutral
kind: claim
title: The marked order-seven orbit closes on extra Heisenberg pairs but its common atom erases the native scales
invalidates:
  - middle-index-order-seven-fano-orbit-closes-oci4
artifacts:
  - research/middle-index-order-seven-orbit-closure-proof.md
distinct_from:
  middle-index-heisenberg-packet-is-not-center-chain-invariant: that stops after the six first ejected root coordinates leave the original three-pair packet; this closes those coordinates inside a larger marked packet and determines exactly what the resulting common atom does.
  native-whitehead-corner-collapses-hecke-flags: that computes the largest fixed subcorner of the center-chain Hecke flag; this constructs an arbitrary-profile marked constant atom and gives a finite packet model in which that atom is orthogonal to the signed Hecke source.
  order-seven-anchor-stops-at-whitehead-compression-leakage: that leaves a model-dependent low-leakage cut open; this rules out only the proposed fixed Fano-orbit cut and does not refute a genuinely coefficient-sensitive finite-coordinate OCI4 construction.
---

**ESTABLISHED FANO-ORBIT FENCE.**  Put `R=L_(F_2)(1,2)`,
`q=s_1t_1`, and `z=x_13(q)`.  The two ejections already visible in each
order-seven row are not stray roots.  With

```text
C_4=x_14(q), D_4=x_43(q),       C_5=x_15(q), D_5=x_53(q),
C_7=X_1,     D_7=Y_1,           C_8=X_2,     D_8=Y_2,
C_9=X_3,     D_9=Y_3,                                  (MOF1)
```

they form five marked Heisenberg pairs:

```text
[C_k,D_l]=z^(delta_(k,l)),
[C_k,C_l]=[D_k,D_l]=1.                                 (MOF2)
```

The word `T_1=J_1B_2A_1` normalizes the three pairs indexed `{4,7,8}`
and induces a Singer order-seven matrix on their commuting `C` roots.
Similarly `T_2=J_2B_3A_2` acts on `{5,8,9}`.  On the five-bit character
set the group generated by the two contragredient matrices is transitive on
all `31` nonzero characters.  Thus the constant character atom

```text
E_5=P_z product_(k in {4,5,7,8,9})(1+C_k)/2
```

is fixed by both order-seven words and, in fact, by each of their factors
`J_1,A_1,B_2,J_2,A_2,B_3`.  It has

```text
tau(E_5)=tau(P_z)/32                                  (MOF3)
```

in every finite-dimensional marked profile.  This is a genuine common
positive reducing cut for `T_1,T_2`, with no canonical-trace assumption.

Closing under all six center-chain arms adds the two radical ejections

```text
C_2=x_12(q),                    D_6=x_63(q).           (MOF4)
```

Adjoining their actual dual roots `D_2=x_23(q)` and `C_6=x_16(q)` gives a
seven-pair marked Heisenberg packet normalized by every center-chain arm,
the three center-chain simple roots, and both prefix Whiteheads.  Its
constant atom

```text
E_7=P_z product_(k in {2,4,5,6,7,8,9})(1+C_k)/2
```

has arbitrary-profile mass `tau(P_z)/128` and reduces every displayed
actor.

This does **not** supply `(OCI4)`.  Since both `A_1` and `A_2` reduce
`E_7`, the two center-chain heads have full support there:

```text
(E_7A_1E_7)^*(E_7A_1E_7)=E_7,
(E_7A_2A_1E_7)^*(E_7A_2A_1E_7)=E_7.                  (MOF5)
```

Consequently the quarter/eighth distinction has become `1/1`; the cut
cannot be the common Hecke source required by OCI4.

The failure is exact, not an estimate gap.  There is a `128`-dimensional
Schrodinger packet model on `ell^2(F_2^7)` with `z=-I`, the `C_k` acting by
coordinate signs, the `D_k` by coordinate translations, and every actor
acting by its induced linear permutation.  The line `E_7` is the delta
function at zero and every actor fixes it.  Hence the nontrivial
center-chain character projection `e_chi` satisfies

```text
e_chi E_7=0.                                           (MOF6)
```

Thus order-seven/Fano orbit counting alone yields neither the signed Hecke
carrier nor a rank/capacity contradiction.  Any successful OCI4 proof must
still use a representation-dependent mixed occurrence cut rather than this
fixed marked-packet atom.  No Property `(T)`, Kazhdan input, literature
theorem, or infinite-dimensional classification is used.

DERIVATION
middle-index-order-seven-orbit-closure-proof
