---
rg: 2
id: escaping-separate-endpoint-transport-closes-mixed-r2
kind: route
title: Use escaping separate endpoint transports and fill their mixed R2 boundary afterward
target: bleak-quick-adjacent-seam-retains-one-mixed-r2-cell
requires: []
---

**INVALIDATED PROPOSAL.**  Build the two private R2 triangles independently
over an unbounded packet, transport their output arms along a section whose
relative corrections escape modulo the fixed transposition mark, and then
fill the mixed triangle after the two endpoint gauges have been separated.

`mixed-r2-boundary-is-one-finite-mark-centralizer-commutator` rewrites the
mixed boundary, up to the one private R2 cell, as the commutator of the
relative correction `dbar` with that fixed mark.  The lower bound from
`finite-mark-centralizer-commutator-area-is-proper` therefore diverges with
the endpoint correction.  No bounded post-hoc R1--R5 filling exists for this
escaping construction.  This does not touch a fixed local diagram or any
collective construction which couples the occurrences before their gauges
split.

