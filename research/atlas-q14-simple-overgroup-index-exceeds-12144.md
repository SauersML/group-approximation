---
rg: 2
id: atlas-q14-simple-overgroup-index-exceeds-12144
kind: claim
title: Every augmented finite-simple Atlas escape has A8 index strictly above 12144
distinct_from:
  atlas-q14-simple-overgroup-index-exceeds-11880: that reaches only beyond natural A12; this additionally excludes the M24 equality.
  atlas-q14-simple-overgroup-index-exceeds-9792: that stops before the A12 and M24 endpoints.
  atlas-q14-simple-overgroup-index-exceeds-8640: that stops before O8-(2), A12, and M24.
  atlas-q14-simple-overgroup-index-is-at-least-9792: that allows equality O8-(2) and does not address the later endpoints.
---

If a finite simple group `S` is generated by two marked `A8` charts satisfying
the Atlas packet, collision `19243`, and `q14`, then

```text
[S:A8] > 12144.                                      (OSI12145)
```

The only new equality after `A12` is `M24`; its marked-pair exclusion is the
remaining input.

## Attempts

The recorded route reduces this claim to
`atlas-m24-marked-packet-collision-exclusion`.  The first generic maximal-
subgroup computation reached its 300-second cap without output, so the next
attack is deferred to a direct three-carrier fusion construction rather than
an unbounded subgroup-lattice enumeration.
