---
rg: 2
id: external-return-whitehead-commutation-kills-s3
kind: route
title: Kill the S3 gauge with a Whitehead-fixed external return
target: el20-six-moving-coefficient-square-bridges
requires:
  - two-external-return-cell-has-finite-s3-model
---

**INVALIDATED FIXED-RETURN CLOSURE.**  Search the eight enumerated external
root normalizers for a return which both overlaps the active Whitehead
coordinate pair and is fixed by the corresponding Whitehead/Singer head.
Such a root would be the smallest candidate for a native same-carrier
commutation relation `[D,E]=1`, killing the distinct-reflection `S_3`
escape.

`eight-external-returns-have-no-whitehead-gauge-lock` exhausts both rows.
The only fixed external roots are disjoint spectators; every overlapping
root is sent to `L_0`, to an opposite/nonexternal root, or out of the root
menu.  The desired native commutation cell does not occur.

