---
rg: 2
id: quantized-scale-index
kind: claim
title: Quantized scale index synchronizing the two commutant exhaustion scales
---

Separate elementwise coordinate-commutant recovery is closed (shifted-scale-recovery), but
the compressor shifts the exhaustion: `alpha(K_m) ≤ K_{r(m)}` gives only
`C_{r(m)} ≤ D_m = U C_m U*`. Produce a quantized scale index, or a
two-branch relation synchronizing the two scales in one multiplicity corner
(AGENDA 2026-08-14, "locally finite commutants recover at the wrong
scale").
