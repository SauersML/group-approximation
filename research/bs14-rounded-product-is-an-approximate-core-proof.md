---
rg: 2
id: bs14-rounded-product-is-an-approximate-core-proof
kind: route
title: The rounded cubic product gives an approximate BS core but only in an open stability topology
target: bs14-hs-stability-does-not-supply-relative-op-basin
requires:
  - bs14-cubic-rounding-leaves-relative-core-basin
  - far-sector-is-solenoid-measure-rigidity
  - bs14-unitary-representations-are-periodic-cycle-packets
---

Identity `(ROB1)` is a direct calculation and `(ROB2)` is unitary
telescoping.  The first required claim gives `||Chat-C||_op<=eta`; the second
records exactly the available normalized-HS stability theorem.

The two one-dimensional scalar BS packets in `(ROB3)`, amplified by a common
trivial block, are instances of the established packet classification and
prove the topology separation inside the exact locus itself.  Therefore HS
core correction cannot be substituted for operator-norm basin entry.

Finally, reconstructing `R_0=S_0^2C_0S_0^(-1)` preserves the BS relation but
does not preserve either rounded cubic incidence with the common `X`.
Separate correction consequently has no established contraction step.
