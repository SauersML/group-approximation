---
rg: 2
id: full-hecke-support-plus-whitehead-sync-proves-native-cross-gram
kind: route
title: Deduce the native cross-Gram from paid full-Hecke supports and Whitehead gauge synchronization
target: binary-leavitt-native-two-scale-cross-gram-positive
requires:
  - b2-b3-pair-full-hecke-four-native-sectors
  - endpoint-whitehead-star-braid-synchronizes-native-gauges
---

**REFUTED SHORTCUT.**  The route proposed to combine the universal
`1/2,1/2,1/4` source-support geometry with the Whitehead Coxeter braid and
conclude that the two stabilized native ranges have positive cross-Gram.

`full-hecke-whitehead-zero-cross-gram` gives an
exact canonical finite-group regular model with the exact `16:1` target
capacity, all of those source masses, and synchronized braid gauges, but

```text
T_1^*T_2=0.
```

The model is an interface countermodel rather than a representation of all
of `Delta`, so the full native claim remains open.  The missing input is a
mixed target-incidence word; separate self-Grams and gauge-spectrum
synchronization cannot supply it.
