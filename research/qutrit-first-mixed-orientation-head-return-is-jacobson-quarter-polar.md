---
rg: 2
id: qutrit-first-mixed-orientation-head-return-is-jacobson-quarter-polar
kind: claim
title: The first mixed-orientation head return is Jacobson and its polar has quarter source
artifacts:
  - research/qutrit-mixed-orientation-jacobson-quarter-polar-proof.md
distinct_from:
  qutrit-one-raw-letter-head-router-is-residually-finite: that proves every one-letter head algebra is triangular and residually finite; this inserts the opposite raw letter in the same nested commutator and reaches the first head-invisible coefficient group.
  four-root-qutrit-bridge-is-jacobson-core: that identifies an unconditioned four-root occurrence of the Jacobson elementary group; this gives a three-root nested word tied to the named head and computes its exact two-stage polar on every qutrit atom.
  qutrit-third-cell-head-selector-is-h-centralizer-zero: that obtains a one-step D8 half-polar using the complementary branch t1; this uses the original Jacobson letters S=s0,T=t0 and makes the identity TS=1 unavoidable.
---

Put

```text
S=s_0,             T=t_0,             Q=1-ST=s_1t_1,
H=x_(c_2,c_1)(Q),
U=x_(c_1,c_0)(T),
V=x_(c_0,c_3)(S).                                      (QJM1)
```

The shortest mixed-orientation chain is

```text
C_1=[H,U]=x_(c_2,c_0)(QT),
W=[C_1,V]=x_(c_2,c_3)(QTS)=x_(c_2,c_3)(Q).             (QJM2)
```

Thus a raw negative letter and a raw positive letter return the same
finite-dimensional-invisible head coefficient on a noncentral qutrit
root. The last equality uses `TS=1` and cannot occur in either
one-letter triangular algebra.

With `E=(1-H)/2` define

```text
P=E(1+C_1)/2,
G=P(1+W)/2.                                             (QJM3)
```

Then `P,G` are projections and the two exact polar identities are

```text
(EUE)^2=P,
(PVP)^2=G.                                              (QJM4)
```

For every joint `C=<J,Z>` character atom `z`,

```text
tau(zE)=1/18,          tau(zP)=1/36,
tau(zG)=1/72.                                           (QJM5)
```

Consequently, for `R=e_0+e_1`,

```text
tau(ER)=1/9,           tau(PR)=1/18,
tau(GR)=1/36.                                           (QJM6)
```

These are exact overlap energies:
```text
||PR||_2^2=1/18,                 ||GR||_2^2=1/36.
```
They are not polar-support ranks, because `P,G` need not commute with
the qutrit cut `R`. In particular the routed operator
```text
e_2g_1GR
```
has range below `Ee_2` and squared norm at most `1/36`, but its
initial polar support cannot be read off from that number. Thus
`(QJM6)` supplies no Hall lower bound; the finite sign-packet theorem
gives the exact countermodel for the natural recombinations.

Let `K=GL_28(F_2)`. The generated group is exactly

```text
<K,H,U,V>=EL_28(J),
J=F_2<S,T | TS=1>.                                     (QJM7)
```

It is finitely presented and is not residually finite. More sharply, every
finite-dimensional unitary representation kills the normal subgroup
generated by the `Q`-roots, including `H,C_1,W`. Thus there is no finite
regular absorber which retains the canonical head carrier: `(QJM2)` is
the first routed packet to pass that gate.

This is an exact algebraic candidate, not a nonhyperlinearity theorem.
The established Jacobson results do not decide whether `EL_28(J)` is
hyperlinear, and `(QJM6)` supplies no trace contradiction. The next relation must recombine complementary sign branches of `C_1`
and `W` with different routers and control the resulting polar-support
ranks. A common router merely sums the four quarters back to `E`.
