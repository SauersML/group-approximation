---
rg: 2
id: one-transvection-ternary-s0-lift-is-left-invertible
kind: claim
title: The four-term lift of s0 built from the transvection 1 + s00 t01 and a Thompson unit has a left inverse in the anti-central ternary group algebra
distinct_from:
  left-invertible-lift-of-s0-in-leavitt-group-algebra: that asks for any left-invertible lift of s0 over F_2; this is one explicit four-term lift over F_3 with z acting as -1, reduced to one equation in a sign-Hecke corner
  ternary-twisted-algebra-has-swap-separated-one-sided-pair: that seeks a pair separated by the swap idempotent in the twisted algebra; this fixes the involution d1 = 1 + s1t1 and one explicit element, and needs no fullness of a swap corner
artifacts:
  - research/artifacts/ternary-one-transvection-two-piece-lift-2026-09-12.md
---

**OPEN.** Let `G = L_(F_3)(1,2)^x`, `z = -1`, `S_- = eps_- F_3[G]`, `d1 = 1 + s1 t1` and `P_(d1) = 2(1 - [d1])`. Put

```text
u0 = (1 - s00 t01)(s00 t0 + s01 t10 + s1 t11),   h = s0 t1 - s1 t0 + s10 t1 = [[0,1],[-1,s0]],   u1 = u0 h.
```

**Claim.** `Y_h = P_(d1)[h]P_(d1)` has a left inverse in `P_(d1) S_- P_(d1)`. By
`two-piece-left-inverses-are-corner-left-inverses`, the claim is equivalent to
`a = -[u0] - [u0 d1] - [u1] + [u1 d1]` having a left inverse in `S_-`. Since `pi(a) = s0`, `a` would be a
one-sided unit that is not invertible, so `J_1(S_-) != 0`.

**Payoff.** Route `one-transvection-s0-lift-refutes-ternary-surjunctivity` into
`ternary-leavitt-unit-group-nonsurjunctive`. It would also refute Kaplansky direct finiteness.

**Cost.** The group generated by `z, d1, h` and the support of any left inverse has a group algebra that is
not directly finite, so it is a non-sofic subgroup of `G` (artifact Section 6).

## Attempts

- 2026-09-12 `w6-wf-stage1-a`: reduction to one corner equation.
  - **The equation.** By artifact Proposition B, the corner is spanned by the `Y_g`, with
    `Y_(d1 g) = Y_(g d1) = Y_(zg) = -Y_g` and `Y_g Y_k = Y_(g d1 k) - Y_(gk)`. So the claim reads
    `sum lambda_g (Y_(g d1 h) - Y_(gh)) = P_(d1)`.
  - **Visible part.** Under `pi` the equation is solvable. Its content lies in `ker pi`.
- 2026-09-12 `w6-wf-stage1-a`: residually finite charts are dead (artifact Proposition D, Corollary D.2).
  - **Statement.** No left inverse uses only elements of a subgroup `H` containing `z, d1, h` with
    `F_3[H]` directly finite.
  - **Examples.** `GL_2(F_3[s0])` and `GL_m(F_3<s0,s1>)` through the chart, or any locally residually finite
    subgroup.
  - **Both directions needed.** `h` lowers depth and forces equal letter weights, so the support must
    contain depth-raising elements. See the depth-monotone firewall of `w6-mismatch-c3`.
- 2026-09-12 `w6-wf-stage1-a`: the partner `k = [[0,-1],[1,t0]]` dies through the Neumann series (artifact
  Claim D.3).
  - **Near miss.** `pi(Y_k Y_h) = pi(P_(d1))`.
  - **Where it dies.** `w = Y_k Y_h - P_(d1)` is not nilpotent: the representation
    `rho0 ⊗ rho0 ⊗ rho0^*` of `GL_2(F_3[s^(±1)])` gives `tr rho(w) = -1`.
- 2026-09-12 `w6-wf-stage1-a`: where it stands.
  - **The region.** `<h, k, d1, z>` lies in `GL_2(F_3<s0,t0>)`, over the Jacobson algebra. It contains the
    finitary involution `z (hk)^2 = diag(1, 1 + e)`, where `e = 1 - s0 t0`.
  - **Undecided.** That group is locally finite-by-linear and not residually finite. No node on main
    decides its soficity, or direct finiteness of its group algebra.
  - **Next tests.** Artifact Section 6 (T1, T2): invertibility of `P_(d1) + w` in the corner over `<h,k,d1,z>`,
    and partners among the conjugates of `diag(1, 1 + e)`.
