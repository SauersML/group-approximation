---
rg: 2
id: fused-authenticated-cross-return-gauge-proof
kind: route
title: Fuse the two exact branch returns and realize every reservoir equation in S3 squared
target: fused-authenticated-cross-returns-retain-s3-square-gauge
requires:
  - cross-whitehead-sandwiches-branch-roots-into-signed-normalizer
  - stabilized-cross-braid-has-s3-square-gauge-fence
---

Use `(CWR6)--(CWR7)` with `g=r` in branch zero and `g=p` in branch
one.  Since `pr=q_0` and `rp=q_1`, this gives `(FAR2)`.  Root-subgroup
addition and `q_0+q_1=1` give `(FAR3)`; the same argument gives the input
fusion with coefficient `p+r`.

For the finite model, conjugation of `(1 2)` by `(1 3)` is `(2 3)`, so
`cac=b`.  This proves both equations in `(FAR5)`.  Elements in different
factors commute, proving every cross-commutation row, and componentwise
multiplication gives `(FAR6)`.  Since `(b,b)` is nontrivial, the fused
constant output retains a reservoir gauge even though its Leavitt
coefficient is exactly one.
