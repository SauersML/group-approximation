---
rg: 2
id: canonical-bcs-root-detection-via-binary-tetrahedral-bridge
kind: claim
title: The binary-tetrahedral extension detects every adaptive BCS source root in canonical microstates
distinct_from:
  bcs-atom-detects-its-e5-central-root: That asks for the same estimate in arbitrary microstates of the bare wordization; this changes the finite presentation and uses the canonical mass of one fresh central sign.
  binary-tetrahedral-conditional-e5-root-bridge: That is the one-source exact amalgam and compression inequality; this installs the whole fixed finite adaptive menu and records the seed and capacity accounting.
---

**ESTABLISHED CANONICAL EXTENSION.**  Fix the adaptive contradiction depth
and use the characteristic-three wordization.  Attach the bridge `(BTB2)` to
every named source root, with all copies sharing one fresh central involution
`ell`.  Enlarge the structural tuple by all the attached finite packets.
For a canonical normalized-HS microstate, the local estimate below applies
to every reached carrier which reduces the attached packet.  The originally
proposed initialization took `Q_empty` to be the common reducing hull of

```text
q_J^- (1+B)/2 q_ell^-,                                (CBT1)
```

and took every successor to be the common reducing hull for the shared BCS
tuple and all attached packets.  Centrality of `J` and `ell` keeps all these
carriers below `q_J^- q_ell^-`.

For every reached source

```text
E=Q_sigma P_i,                 Z=x_13(a_(sigma,i)),
```

the corresponding binary-tetrahedral packet commutes with the finite
context Reynolds subgroup defining `P_i`, while `Q_sigma` reduces that
packet.  Therefore

```text
tr(E(1-P_(Z,1))) >= (1/2)tr(E)-C sqrt(E_pres),         (CBT2)
```

with one dimension-independent constant for the fixed finite menu.

Canonical trace still gives `(CBT1)` asymptotic trace `1/8`, and every
reached carrier lies below `q_J^-q_ell^-`.  However, the reducing-hull
capacity conclusion in the original formulation is false.
`binary-tetrahedral-packet-hull-saturates-unprotected-seed-sign` proves that
one quaternion element in any attached packet conjugates the independent
Pauli seed sign to a free involution.  Requiring the hull to reduce that
packet therefore makes the initial hull equal to the full
`q_J^-q_ell^-` sector, of trace `1/4`, in the canonical regular trace and
asymptotically in canonical microstates.

Thus `(CBT2)` remains an established local root-detection theorem, while the
factor-two carrier budget uses the repaired amalgam now constructed in
`protected-adaptive-btb-wordization-preserves-canonical-budget`.  Its edge
two-group contains `H_i,J,B`; the added relations
`[q_sigma,B]=[d_(sigma,i),B]=0` make every source root invariant, and the
BTB factor centralizes all three protected signs.

The perfect marked model extends by taking `ell=1` and every attached
binary-tetrahedral factor trivial.  Its carrier `(CBT1)` is zero, while the
old word `J` remains nontrivial.  This is consistent because the canonical
negative `ell` sector is used only under the assumption that the extended
group is hyperlinear.

This claim closes local payload-to-root detection for the **extended
canonical route**.  The separate protected-wordization claim now supplies
the adaptive amalgams required by the capacity audit.  This claim still
does not construct the remaining oriented Morita/first-hit return.

DERIVATION
bcs-atom-root-detection-from-binary-tetrahedral-bridge
