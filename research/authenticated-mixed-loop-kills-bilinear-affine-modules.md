---
rg: 2
id: authenticated-mixed-loop-kills-bilinear-affine-modules
kind: claim
title: The authenticated mixed loop kills the full mixed tensor affine block
artifacts:
  - research/authenticated-mixed-loop-bilinear-module-proof.md
distinct_from:
  authenticated-mixed-loop-kills-quadratic-affine-modules: that kills the symmetric square and hence the tensor square of two defining modules; this kills the different mixed tensor `M tensor M^*`, including its adjoint-type sector without using a semisimple decomposition.
  rank-one-closure-excludes-noncentral-outlier-gauges: that asks for arbitrary unitary or nonlinear gauges and noncongruence actors; this is an exact defining-characteristic bilinear-module calculation.
---

Use the honest finite coefficient actor and odd-characteristic field of
`authenticated-mixed-loop-kills-minuscule-affine-modules`.  The torus and
authenticated mixed-loop Fox rows have zero common kernel on

```text
M tensor M^* ~= End_K(M),             M=K^3.            (BAM1)
```

The conclusion holds in every odd characteristic, including characteristic
three, and for every Frobenius twist of `(BAM1)`.  It does not use a splitting
of `End(M)` into scalars and trace-zero matrices; consequently it remains
valid when that familiar splitting degenerates in characteristic three.

By extension closure, the same conclusion holds on every module with a
finite filtration whose sections are drawn from the previously established
degree-zero, minuscule, quadratic blocks and the mixed tensor `(BAM1)`, with
arbitrary multiplicities and Frobenius twists.

This rules out the adjoint-type affine corrector in addition to the natural,
dual, and symmetric-square correctors.  Arbitrary higher highest-weight
blocks still require the universal two-row group-algebra Bezout identity.

DERIVATION
authenticated-mixed-loop-bilinear-module-proof
