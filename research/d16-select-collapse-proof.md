---
rg: 2
id: d16-select-collapse-proof
kind: route
title: Conjugate the Hadamard commutator and substitute the SELECT covariance
target: d16-common-gauge-select-collapses-charger-to-payload
requires:
  - finite-cocycle-select-forces-one-common-lcu-gauge
  - hadamard-selector-commutator-charges-two-path-leakage
---

With `x=hzh`, conjugating `g=[hch,z]` by `h` gives
`cxc^(-1)x`.  The binary SELECT relation makes this `r`, and the payload
commutes with the finite Hadamard packet, so `g=r`.  A central-sign twist is
therefore the finite clock `Jr`, not a wandering word.

