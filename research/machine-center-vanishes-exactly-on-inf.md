---
rg: 2
id: machine-center-vanishes-exactly-on-inf
kind: claim
title: The pumped search-machine group has a central 2-subgroup that vanishes exactly on INF
distinct_from:
  residual-finiteness-of-finite-presentations-is-pi2-complete: that uses the same family to switch residual finiteness; this isolates a canonical central coefficient subgroup that switches, the input any soficity amplifier would consume.
  pumped-divergent-minsky-word-lies-in-profinite-kernel: that proves one divergent word is profinitely invisible; this adds that all full configuration words are central and all vanish on INF.
---

ESTABLISHED (unreviewed).  Let `G_e = G_0(P(M_e))` (prime `2`) be the pumped
search-machine group of [[rf-fp-completeness-via-pumped-minsky-groups]], with
the KMS stop relations `x_(q_0 w) = 1`
([[kms-group-with-stop-relations-simulates-all-words]]).  Let `Phi_e` be the
subgroup generated by its full configuration words
`x_(q_i A_0) * a^(m) * A_1 * ... * A_(K+2)`.  Then `Phi_e` is central and
elementary abelian, and

```text
e in INF  ==>  Phi_e = 1,
e in FIN  ==>  Phi_e != 1, and Phi_e contains a nontrivial element of the profinite kernel.
```

Why:

- *Central.*  In the KMS model a full configuration word is the basis vector
  indexed by a word containing every `A`-letter, and every letter fixes such
  vectors.  With the stop relations `T` embeds in the model (`T_0 ~= T_1`) and
  is abelian, so the word is central.
- *INF.*  The machine is simple and universally halting, so every full
  configuration reaches stop, and every zero word is `1` in `G_0`.
- *FIN.*  The pumped divergent configuration is full, and its word is
  nontrivial but profinitely invisible.

Consequence: any uniform finite-presentation amplifier sending a finitely
presented amenable group with a nontrivial central 2-subgroup to a nonsofic
group, and with trivial one to a sofic group, proves
[[sofic-recognition-finite-presentations-is-pi2-complete]].

(2026-09-13: stated for `G_0`, following the stop-relation correction of
`ex-verify-logic-quantum`.  For `G(P(M_e))` without the stop relations the
INF clause and the embedding of `T` both fail.)

DERIVATION
[[machine-center-vanishes-exactly-on-inf-proof]]
