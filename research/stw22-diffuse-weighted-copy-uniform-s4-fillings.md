---
rg: 2
id: stw22-diffuse-weighted-copy-uniform-s4-fillings
kind: claim
title: Strictly slack weighted-copy fibres in diffuse II1 factors have a factor-uniform four-sphere filling modulus
distinct_from:
  stw22-diffuse-weighted-copy-uniform-s3-fillings: that theorem is degree three; the present theorem uses the new Stiefel S4 and relative B5 extensions.
artifacts:
  - research/artifacts/stw22-dim5-factor-bundle-breakthrough-2026-08-31.md
---

For every separable-predual II1 factor `(N,tau)`, every `m>=1`, and every
positive contraction `a` with `m tau(supp(a))<1`, the Hilbert-sum
weighted-copy fibre `F_(N,m)(a)` has the factor-uniform four-sphere
modulus

```text
rho_W,4(epsilon)=min(epsilon,1)^3/216000000.           (W4-1)
```

Every such fibre is four-connected, and the family is uniformly
equi-`LC^4` after combining the lower-degree moduli.