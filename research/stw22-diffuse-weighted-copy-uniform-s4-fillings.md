---
rg: 2
id: stw22-diffuse-weighted-copy-uniform-s4-fillings
kind: claim
title: Diffuse weighted-copy fibres have a factor-uniform four-sphere modulus
distinct_from:
  stw22-diffuse-weighted-copy-uniform-s3-fillings: that theorem stops at degree three; the present theorem uses the new five-ball relative extension.
  stw22-matrix-weighted-copy-codimension-two-uniform-s4-fillings: that is the matrix branch; the present theorem permits every positive slack in diffuse factors.
artifacts:
  - research/artifacts/stw22-dim5-factor-bundle-breakthrough-2026-08-31.md
---

For every separable-predual `II_1` factor `(N,tau)`, every `m>=1`, and
every positive contraction `a in N` with

```text
m tau(supp(a))<1,
```

the weighted-copy fibre has the universal four-sphere modulus

```text
rho_(W,4)(epsilon)=min(epsilon,1)^3/216000000.
```

Every such fibre is four-connected.  The assertion is uniform in the
factor, weight, copy number and positive slack.

