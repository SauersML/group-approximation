---
rg: 2
id: matricial-compression-ring-via-schreier-hilbert-hotel
kind: route
title: Matricial compression rings from Hilbert-hotel Schreier data via Bernoulli tensor lamps
target: matricial-compression-ring-exists
requires:
  - schreier-hilbert-hotel-data-give-matricial-compression-rings
  - schreier-hilbert-hotel-compression-data-exist
---

Take (HH) data from `schreier-hilbert-hotel-compression-data-exist`. Then
`schreier-hilbert-hotel-data-give-matricial-compression-rings` shows that the ring
`R = M_2(F_p)^(tensor Gamma/H) x| Gamma`, with the automorphism induced by `alpha`, meets
(R1)-(R5):

- `R_0` is generated by the base-site matrices and `Gamma_0`;
- `z` is the flip tensored over the finite `Gamma_0`-orbit of `t^(-1)(g x_0)`.

This gives `matricial-compression-ring-exists`.

The route is complete except for its second input, which is open. That input is purely
combinatorial, and its dead classes are recorded in its Attempts.
