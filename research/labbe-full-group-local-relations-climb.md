---
rg: 2
id: labbe-full-group-local-relations-climb
kind: claim
title: Finitely many local relations among small 3-cycles of Labbé's derived full group imply all of its local relations
distinct_from:
  labbe-shift-derived-full-group-is-finitely-presented: That is finite presentation itself; this is the climbing of bounded local relations over the infinite generating set of small 3-cycles, which together with zd-subshift-derived-full-group-local-presentation is equivalent to it.
  labbe-relator-shifts-carry-patch-rich-periodic-points: That seeks periodic configurations satisfying short relators, a refutation mechanism; this asks that finitely many local relations imply all local relations, the positive mechanism.
  labbe-wang-shift-crossed-product-is-finitely-presented: That is finite presentation of the crossed-product algebra, decided by commuting idempotent partitions; this concerns relations among 3-cycles in the derived full group.
artifacts:
  - research/artifacts/fp-simple-relation-climbing-2026-09-13.md
---

**OPEN.** Let `G = D([[Z^2 ~ Omega_U]])` for Labbé's shift. Notation `𝒥_m`, `𝒥` and
`(k, ℓ)`-local relations is that of `zd-subshift-derived-full-group-local-presentation`.

**Statement.** For all `k_0, ℓ_0` there are `k, ℓ, m_1` and, for each element of `𝒥`, a fixed
word in `𝒥_(<= m_1)` representing it in `G` (for instance ChJN's nested commutators followed by
refinement products), with the following property. After substituting these words, every
`(k_0, ℓ_0)`-local relation is a consequence of the finitely many `(k, ℓ)`-local relations among
elements of `𝒥_(<= m_1)`.

**Relation to finite presentation.**
- If `G` is finitely presented, the statement holds, since every relation follows from finitely
  many.
- Together with `zd-subshift-derived-full-group-local-presentation`, it gives finite presentation
  (`labbe-fp-via-local-relation-climbing`).

**Model test.** For a minimal `Z`-subshift the analogous statement fails, because periodic points of
the Markov approximations satisfy every local relation of bounded scale (artifact, Remark 2.1).
For an aperiodic `Z^2`-SFT those models do not exist. `labbe-ball-legality-is-pairwise-at-radii-two-to-seven`
is the first layer where the two cases visibly differ at the radii computed.

## Attempts

- **Naive induction on radius.** Dies.
  - Mechanism: relations among `5k` radius-`r` generators give relations among `k` radius-`(r+1)`
    generators, since each finer one is a word in five coarser ones.
  - Failure point: to iterate, each group generated by `5k` finer generators must be the colimit
    of its subgroups on `k` generators. Families whose supports overlap cyclically in different
    local configurations have infinite colimits of finite groups. The one-dimensional
    calibration shows this upgrade must fail at infinitely many scales there (artifact,
    Section 3).
- **Triviality of climbed illegal patches.** The first layer climbs at radii 2–7.
  - A pattern on `B(r+1)` whose five sub-balls are legal but which is illegal yields a trivial
    climbed 3-cycle.
  - At radii 2–7 every such pattern already has an illegal union of two sub-balls
    (`labbe-ball-legality-is-pairwise-at-radii-two-to-seven`).
  - So its triviality follows from relations among at most three radius-`r` generators (artifact,
    Lemma 3.1).
- **Where one dimension dies, untested here.**
  - A relation between two radius-`(r+1)` generators at offset `<= 2` involves ten sub-balls, at
    offsets up to 4.
  - In one dimension the illegality that escapes all bounded relations pairs the first subword of
    one word with the last subword of the other, at the largest offset.
  - The analogous count for `B(r+1) ∪ (B(r+1)+delta)`, `|delta|_1 <= 2`, is test (T1) of the
    artifact. The colimit test for the ten-generator groups is (T2).
- **Neighbouring mechanisms.**
  - The algebra analogue, `sft-crossed-product-fp-iff-quantum-rigid`, converts finite
    presentation of the crossed product into a local-to-global property of commuting idempotent
    partitions.
  - No group analogue of that criterion is recorded.
