---
rg: 2
id: existing-fanizza-mixed-menu-leaves-balanced-mirror-exit
kind: claim
title: The existing Fanizza mixed menu leaves the sharp balanced mirror exit
invalidates:
  - existing-fanizza-mixed-menu-authenticates-selected-oriented-row
distinct_from:
  translation-reset-leaves-a-compressed-gram-spectator: that constructs the scalar-ray Gram deficiency and checks the reset and diagonal-difference incidences; this identifies its conjugate range as the balanced mirror and audits every currently available completeness-preserving mixed test.
  inside-corner-hecke-tests-miss-the-asynchronous-mirror-exit: that proves blindness for arbitrary coefficients supported in the source corner; this also includes the translation reset, Toeplitz diagonal-difference relation, and range-reflection word in one exact model and computes the selected-row floor.
  full-label-chord-normalization-kills-the-hilbert-hotel: that detects the spectator by forcing fixed-label normalization but destroys infinite completeness; this proves that it is the only existing detector outside the completeness-preserving menu.
---

**ESTABLISHED EXISTING-MENU NO-GO.**  The current completeness-preserving
Fanizza chord--reset menu does not authenticate the selected oriented row.
More precisely, there is an exact finite-dimensional model satisfying:

1. the full translation-symmetrized reset and every fixed label/type-vector
   relation;
2. all diagonal-difference Toeplitz/old-spin Pauli incidences;
3. every fixed D8, selector, Hecke, Reynolds, or coefficient test in which
   occurrences of the returned range are sandwiched by source-corner
   coefficients; and
4. the commuting range-reflection relation,

while

```text
||R_F U R_E U^*-1||_2^2=4 tr(H).                       (EMM1)
```

For the native guard this is `1/2`, so the row norm is `1/sqrt(2)` although
every listed presentation defect is zero.  Hence no modulus tending to zero
with those defects can authenticate the row.

This does not refute
`fanizza-matrix-coordinate-source-full-compression`.  A new incidence which
uses genuinely finite matrix-coordinate information could still distinguish
the mirror.  It proves only that none of the already installed mixed
chord--reset relations supplies that incidence.

DERIVATION
existing-fanizza-mixed-menu-mirror-exit-proof
