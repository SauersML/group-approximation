---
rg: 2
id: returned-boolean-coefficient-window-has-finite-marked-model
kind: claim
title: The entire returned Boolean-coefficient window has a finite exact marked model
invalidates:
  - returned-a2-fold-window-places-native-pauli-factors
distinct_from:
  returned-opposite-cross-zero-has-finite-s6-model: that gives an explicit model for the displayed child/parent Whitehead and returned-cross-zero rows; this covers every ordinary word relation made from arbitrary root positions with coefficients in the Boolean ring generated by the two child ranges.
  fixed-group-algebra-polynomials-cannot-supply-atomic-return: that rules out representation-functorial formulas for the four output arrows; this proves that even adjoining arbitrary further returned mixed-commutator identities in the Boolean coefficient inventory leaves an exact finite marked input model, so coordinate-dependent output selection cannot follow from that inventory either.
  aggregate-cross-commutator-sum-has-finite-s6-gauge: that crosses the Boolean boundary using the nonconstant cross coefficients and still retains a finite gauge; this proves only that crossing the boundary is necessary, not that any single non-Boolean occurrence suffices.
  native-opposite-arm-return-has-finite-s6-model: that exposes one uncancelled `s_0`/`t_0` arm through a complete native return and remains finite; this requires a non-Boolean occurrence coupled across both branches, not merely one authenticated branch.
  binary-leavitt-atomic-morita-return: that may use the full finite presentation and representation-dependent finite-coordinate choices; this fences only the sublanguage which never exposes an uncancelled Morita arrow `s_i` or `t_i`.
---

**ESTABLISHED WHOLE-INVENTORY FINITE FENCE.**  In the returned range-sum
fold put

```text
q_i=s_i t_i,             q_i^2=q_i,
q_0q_1=q_1q_0=0,         A=q_0+q_1.                   (RBF1)
```

Let

```text
B=F_2[1,q_0,q_1] subset L_(F_2)(1,2).                (RBF2)
```

Every element of `B` is represented by

```text
epsilon 1+epsilon_0 q_0+epsilon_1 q_1,
epsilon,epsilon_0,epsilon_1 in F_2,                  (RBF3)
```

so `|B|<=8`.  In particular `B` is a finite commutative Boolean ring.  The
whole returned inventory generated by

```text
x_ij(1), x_ij(q_0), x_ij(q_1), x_ij(A)               (RBF4)
```

at arbitrary Steinberg root positions, together with every constant
root-index Weyl word, maps into the finite matrix group `E_20(B)`.  This
includes the child and parent reverse-product Whiteheads, every native
`A_2` orbit and return, and **every** mixed commutator of their relative
branch word with any root/Pauli word constructed inside `(RBF4)`.

All genuine ordinary-word identities among those occurrences therefore
have one simultaneous finite exact model: compose the Steinberg-to-
elementary map with the left regular representation of `E_20(B)`.  The
parent mark survives because

```text
x_14(A) |-> I+A E_14 != I.                            (RBF5)
```

Likewise either nonzero child mark survives.  Its image is a nonidentity
involution, so its `(-1)` spectral projection in the finite left regular
model is nonzero.

Consequently no set of additional mixed Pauli commutator rows drawn only
from `(RBF4)` can imply `binary-leavitt-atomic-morita-return` on the
prescribed marked atom.  Repeat the same exact finite model at every input
tolerance.  The input word defect is zero, whereas any returned four-arrow
output with modulus tending to zero contradicts
`atomic-leavitt-gap-on-nonzero-matrix-corner`.

This answers the returned-window inventory question at its natural
coefficient boundary.  A live coefficient-completeness-to-mark occurrence
must leave the finite Boolean ring `B`.  Concretely it must contain an
uncancelled occurrence of one of

```text
s_0,s_1,t_0,t_1
```

or an equivalent non-Boolean prefix matrix unit, and must type that
occurrence on the prescribed marked carrier.  More conjugacy, Coxeter,
cross-zero, or mixed-commutator rows involving only the range idempotents
cannot close Atomic Morita Return.

This is a finite-fragment fence, not a finite quotient of the full binary
Leavitt Steinberg group and not a refutation of the global atomic-return
claim.  No Property `(T)`, trace hypothesis, stability theorem, literature
result, local computation, or Lean compilation is used.

DERIVATION
returned-boolean-coefficient-finite-model-proof
