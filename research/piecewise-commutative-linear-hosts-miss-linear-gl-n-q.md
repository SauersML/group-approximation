---
rg: 2
id: piecewise-commutative-linear-hosts-miss-linear-gl-n-q
kind: claim
title: A finitely generated group of homeomorphisms that are piecewise in a topologically free group linear over a commutative ring contains no linearly acting divisible group, so no linear copy of GL_n(Q)
distinct_from:
  gl-n-q-embeds-in-fp-simple-group: that asks for an embedding of GL_n(Q) in a finitely presented simple group; this excludes, for the natural linear copies only, every finitely generated host built from commutative-linear pieces.
  rational-linear-groups-satisfy-boone-higman: that embeds each finitely generated subgroup of GL_n(Q); this shows that the globally linear elements of a finitely generated piecewise host form such a finitely generated linear group, never all of GL_n(Q).
  germ-extensions-omit-standard-gl-n-q: that extends part (a) to every group that is locally in a finitely generated finitely-piecewise base off a nowhere dense set, which includes all finite germ extensions; this is the base case, together with the commutative-ring residual finiteness that turns (a) into the exclusion of GL_n(Q) and of every group with a divisible subgroup.
artifacts:
  - research/artifacts/gq-gq-solenoid-adelic-models.md
---

**ESTABLISHED** through `piecewise-commutative-linear-hosts-miss-linear-gl-n-q-proof`.
This is a lane proof; it has not been independently reviewed.

## Setting

- `Y` is a Hausdorff space, `X ⊆ Y` a nonempty open subset, and `L` a group of
  homeomorphisms of `Y`.
- `L` is **topologically free on `X`**: the only element of `L` that fixes a
  nonempty open subset of `X` pointwise is the identity.
- A homeomorphism `f` of `X` is **`L`-piecewise** if there are a finite set
  `F_f ⊆ L` and a closed nowhere dense set `S_f ⊆ X` with the following property.
  Every `x ∈ X \ S_f` has an open neighbourhood `U ⊆ X` with `f|_U = ℓ|_U` for some
  `ℓ ∈ F_f`.
- `PW_X(L)` denotes the set of `L`-piecewise homeomorphisms of `X`. It is a group.

This covers finite clopen partitions (`S_f = ∅`), finitely many breakpoints, and
polyhedral pieces whose boundaries are ignored.

## Statement

**(a) Pieces control the globally linear elements.** Let `Γ ≤ PW_X(L)` be
generated by `γ_1, ..., γ_k`, and put `H = ⟨F_{γ_1} ∪ ... ∪ F_{γ_k}⟩`. Then `H`
is a finitely generated subgroup of `L`. Suppose `γ ∈ Γ` and `ℓ ∈ L` agree on
some nonempty open subset of `X`. Then `ℓ ∈ H`.

**(b) Commutative-linear pieces.** Suppose `L` is isomorphic to a subgroup of
`GL_N(R)` for some commutative ring `R`. Then every finitely generated subgroup
of `L`, and in particular `H`, is residually finite.

Now let `G` be a group with a nontrivial divisible abelian subgroup. Let
`ρ: G -> L` be injective with `ρ(g)(X) = X` for every `g`. Then no finitely
generated subgroup of `PW_X(L)` contains `{ρ(g)|_X : g ∈ G}`.

This applies to `G = (Q,+)`, `Aff(Q)`, `U_3(Q)`, and `GL_n(Q)`, `SL_n(Q)`,
`PGL_n(Q)`, `PSL_n(Q)` for `n >= 2`. Each contains the unipotent `(Q,+)`, which
meets the scalars trivially.

## Instances

In each instance `L` is linear over a commutative ring and topologically free on
`X`. The proof route checks both properties.

1. **Adelic affine.** `X = Y = A_f^n ∪ {∞}`, a Cantor set, with
   `L = Aff_n(A_f) ≤ GL_{n+1}(A_f)`. The linear action of `GL_n(Q)` is one of
   these `ρ`.
2. **Adelic projective.** `X = Y = ∏_p P^{m-1}(Q_p)` with
   `L = GL_m(A_f)/A_f^× ≤ GL_{m^2}(A_f)`, acting by conjugation on `M_m(A_f)`.
   Here `GL_n(Q)` embeds through `g ↦ [diag(g,1)] ∈ PGL_{n+1}(Q)`.
3. **Adelic prefix maps.** `X = Ẑ ⊆ Y = A_f`, with pieces
   `z ↦ b + (N'/N)(z − a)` from `a + NẐ` onto `b + N'Ẑ`, and `L = Aff_1(Q)`.
4. **Real projective.** `X = Y = RP^{n-1}` with `L = PGL_n(R) ≤ GL_{n^2}(R)`.
   - This covers Thompson's `T` in its piecewise-`PSL_2(Z)` form.
   - It covers every group of piecewise-projective homeomorphisms with finitely
     many pieces per element: Monod's groups `H(A)`, Thurston's PIP groups, and
     groups of Lodha–Moore type.
   - So no finitely generated such group contains the natural `PSL_2(Q)` or
     `PGL_n(Q)`.
5. **d-adic prefix maps.** `X = Z_d^n ⊆ Y = Z_d[1/d]^n` with
   `Z_d = lim Z/d^k Z = ∏_{p|d} Z_p` and `L = Aff_n(Z_d[1/d])`. This covers the
   Higman–Thompson groups `V_d`, Scott's groups `Sc(n)` (`d = 2`), and
   Röver–Nekrashevych groups `V_d(G)` of self-similar affine groups `G`.
   - Brin–Thompson `nV` uses `L = Aff_1(Q_2) ≀ S_n ≤ GL_{2n}(Q_2)` on
     `X = Z_2^n`.
   - Here the lemma says only that the globally affine elements of `nV` form a
     residually finite group. It does not exclude Kojima–Sheng's `Q ≤ 2V`, whose
     elements are not globally affine.

## Scope

- **Germ extensions are not an escape for the linear copy.** Elements with infinitely many local pieces near
  singular points (Brin's `A`, `VA` and the other Belk--Hyde--Matucci finite germ extensions of `V`) are handled
  by `germ-extensions-omit-standard-gl-n-q`: off a nowhere dense set they are locally in the finitely generated
  base, so their globally linear elements again lie in the finitely generated piece group. `Q <= T̄ <= VA`
  is consistent with both lemmas because those copies of `Q` are not globally affine.
- **Genuinely piecewise copies.** Copies of `GL_n(Q)` whose elements are not
  restrictions of elements of `L` are not excluded, as `Q ≤ 2V` shows for `(Q,+)`.
- **Other pieces.** Pieces from nonlinear groups, or from groups linear only over
  noncommutative rings, are not covered.

**Consequence for `gl-n-q-embeds-in-fp-simple-group`.** With
`germ-extensions-omit-standard-gl-n-q`, every host built from commutative-linear
pieces (finitely many per generator, or locally in such a base off a nowhere
dense set) must realize the divisible unipotent subgroup by elements that are
not globally in `L`. The natural action of `GL_n(Q)` on
`Q_p`-, `A_f`- or `R`-points cannot be used in any finitely generated host built
from commutative-linear pieces.
