---
rg: 2
id: boone-tower-good-subgroup-transport
kind: claim
title: The halting subgroup is good in the tower, stage by stage
distinct_from:
  boone-halting-index-good-parameter-set: That is the base-group half, proved with no HNN theory: the two sides of a quadruple are images of one parameter set. This is the transport of that statement through the tower, which needs the good-subgroup lemma at every stage.
  hnn-good-subgroup-lemma: That is the single-stable-letter lemma for an arbitrary good subgroup; this applies it to the specific subgroups `T` and `T_M` and iterates it up the tower of a machine.
artifacts:
  - GroupApproximation/Computability/BooneGroupGoodness.lean
  - GroupApproximation/Computability/BooneGroupMachineIndex.lean
  - GroupApproximation/Computability/BooneGroupTower.lean
---

ESTABLISHED via the route below, and machine-checked except for one mechanical
assembly named at the end.  Item **S5b** of the roadmap in
`Computability/BooneGroupBase`.  Inside the tower `G'_M` of a modular machine,

    T = T' cap G     and     T_M = T'_M cap G,

where `T'` and `T'_M` are the subgroups generated by `T` respectively `T_M`
together with all the stable letters.

## What is machine-checked, and what is left

`Computability/BooneGroupGoodness` (2026-08-16) proves goodness at every
quadruple, in both directions: `good_twSub_halting_right` and
`good_twSub_halting_left` compose S5a's two image computations with
`twSub_map_emb` -- `emb` carries `twSub` of a set to `twSub` of its image -- so
Simpson's Definition 2 for `T_M` comes out with no computation left to do, and
`twSub_halting_liftedSubgroup_inf_right` / `_left` then apply the good-subgroup
lemma at that stable letter.  `towerSub_inf_range` supplies the iteration up
the tower under the hypothesis `GoodTower`.

What is not yet a Lean declaration is the last mechanical step: discharging
`GoodTower` for the list `machineIdentifications mm` of an actual machine, i.e.
running the per-quadruple goodness theorems along the fold.  Nothing in it is
new -- both ingredients exist and the induction is over a `List` -- but until
it is written, the claim is established here by the route rather than by a
single Lean theorem.  The estimate of about 120 lines was for the whole of S5b;
what remains of it is perhaps twenty.
