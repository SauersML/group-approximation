---
rg: 2
id: finite-saturated-table-cover-removes-literal-group-presentation
kind: claim
title: A finite saturated table cover removes the literal group-presentation input
distinct_from:
  full-leavitt-idempotent-defect-saturation: that proves the marked Leavitt root normally generates the rank-twenty Steinberg group; this turns any finite list of identities around such a normal generator into a finitely presented cover and descends total MF invisibility through the cover map.
artifacts:
  - research/finite-saturated-table-cover-proof.md
---

**ESTABLISHED FINITE-COVER REDUCTION.**  Let `Delta` be generated by
`g_1,...,g_k`, and let the word `z` be nontrivial and normally generate
`Delta`.  Fix any finite list `B` of words in the generators which are equal
to one in `Delta`.  There is a finitely presented group `Gamma`, an epimorphism

```text
pi:Gamma ->> Delta,                                   (FSC1)
```

and a lift of `z`, again denoted `z`, such that

1. every word in `B` is a defining relator of `Gamma`;
2. `z!=1` in `Gamma`; and
3. `z` normally generates `Gamma`.

Moreover, if every homomorphism from `Gamma` to a norm matrix corona kills
`z`, then every homomorphism from `Delta` to a norm matrix corona is trivial.

For the binary-Leavitt atomic route, `B` may contain the finitely many
prototype discrepancies and every finite coefficient identity actually used
by their compiler.  The saturation identities supplied by
`full-leavitt-idempotent-defect-saturation` make the lifted root normally
generate the cover.  Each prototype is then literally a defining relator and
has area one.  Thus the operator-norm compiler does not require an independent
finite-presentation theorem for the literal Steinberg group: it may be stated
for this explicit finite saturated cover, and its total invisibility descends
through `(FSC1)`.

This reduction does not construct the missing compiler estimate.  It removes
only the finite-presentation interface, leaving the finite prototype gap of
`atomic-three-row-compiler-is-prototype-gap-equivalent` as the exact analytic
input.
