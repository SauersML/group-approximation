---
rg: 2
id: equal-rank-denominator-authentication-proof
kind: route
title: Expand the equal-rank projection distance and charge the section correction
target: equal-rank-denominator-edges-authenticate-decoded-atoms
requires:
  - raw-denominator-incidence-kills-off-diagonal-shell-orbit
  - polar-compression-turns-band-mismatch-into-first-exit
---

Equal traces turn the squared distance between `TET^*` and `F` into twice
the leakage of `TE` outside `F`.  Compare `T` with the fixed section
correction `D`; conjugation costs at most twice their HS distance, while
full-PVM lattice covariance controls `DED^*-Ehat`.  A triangle inequality and one
quadratic split give `(EDA4)`.  Apply it to the forty-two fixed section
identities and sum.  Fixed-area word telescoping controls the section error,
leaving only the already named first-exit and local covariance energies.
