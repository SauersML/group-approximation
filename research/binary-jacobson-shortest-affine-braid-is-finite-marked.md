---
rg: 2
id: binary-jacobson-shortest-affine-braid-is-finite-marked
kind: claim
title: A shortest Jacobson finite-type kernel relation is a finite marked affine braid
distinct_from:
  binary-jacobson-first-mixed-kernel-is-three-syllable: that realizes the minimum three-balanced-syllable length by multiplying the three cyclic balanced shifts to a head-controlled swap; this gives a different minimum relator using the single balanced involution and the constant Weyl word written in all three active-cycle roots, and proves its whole fixed-depth packet is finite.
  binary-jacobson-first-opposite-polar-package-is-finite: that uses the dihedral packet generated by the balanced involution and one constant opposite root; this uses all three directed constant-cycle roots to form an affine `S_3` braid.
  binary-jacobson-raw-cycle-selects-common-pi-intertwiner: that asks for an occurrence-authenticated rectangular return from the native degree-two packet to the degree-four packet; this proves that one shortest literal cross-block equality is only a finite degree-preserving braid.
---

**ESTABLISHED.**  In

```text
J=F_2<S,T | TS=1>,             Q=1-ST,
E=EL_3(J),                     F=EL_3(F_2[Q]),
h=[[Q,S,0],[T,0,0],[0,0,1]],
```

let `Gamma_F -> E` be the complete one-step finite-type envelope map of
`binary-jacobson-one-step-finite-type-envelope-is-marked-mf`.  Write

```text
u=x_12(1),       v=x_23(1),       g=x_31(1),
r=v [g,u] v=x_23(1)x_32(1)x_23(1)=w_23.                 (JB1)
```

Thus `r` is a literal word using the complete directed constant three-cycle.
Then

```text
h^2=r^2=1,                  (h r)^3=1 in E.             (JB2)
```

The word `(h r)^3` is a nontrivial cyclically reduced word in `Gamma_F`.
Its three balanced `h`-syllables have the least possible positive number
among elements of `ker(Gamma_F -> E)`, by the minimum-length theorem in
`binary-jacobson-first-mixed-kernel-is-three-syllable`.  Equivalently, the
literal cross-block braid

```text
h r h = r h r                                               (JB3)
```

is another shortest relation beyond the complete finite overlap.

This occurrence equality is nevertheless not the missing PI selector.  For
every fixed `N`, let `C_N` be the `N`-pair finitary Clifford packet containing
the head `w=x_13(Q)`.  The literal subgroup

```text
H_N=<h,r,C_N> <= E                                         (JB4)
```

is finite and retains `w!=1`.  Hence it has an exact faithful finite-dimensional
unitary model containing the affine braid, the nonzero head cut and the
complete degree-`2^N` Clifford packet.

The fence remains valid after adjoining the **entire** finite packet `F`.
There is a finite marked quotient

```text
(Gamma_F/<normal closure of (hr)^3>) -> GL_4(F_2)        (JB6)
```

which projects `F=G_Q x G_P` to its head factor, embeds that `GL_3(F_2)` on
coordinates `1,2,3`, and sends `h` to the coordinate swap `w_24`.  The
overlap `K` acts only on coordinates `1,3`, so `w_24` centralizes it; and
`r=w_23`, so `w_24 w_23` has order three.  The head transvection `w=x_13(Q)`
maps to the nonidentity transvection `x_13(1)`.

Consequently a common degree-two-to-degree-four PI return cannot be a formal
consequence of `(JB3)` and any fixed finitary packet.  Although the braid
authenticates two literal word paths, both are unitaries in one finite `S_3`
actor and preserve packet degree.  A load-bearing Jacobson occurrence must
combine a shortest relator with another independent raw incidence whose orbit
is not contained in a fixed finite part of the finitary kernel, or must use a
joint compression not forced by the affine braid alone.

The two minimum cells are complementary.  In the quotient `(JB6)`, the
three-cyclic controlled-swap product of
`binary-jacobson-first-mixed-kernel-is-three-syllable` evaluates to
`w_14 w_23`, rather than its required value `w_23`.  Conversely that node's
retraction onto `F` does not satisfy `(hr)^3=1`.  Thus each shortest cell
separately has a full-packet marked finite model, while their joint quotient
is the first finite-holonomy package not decided by either retraction.

DERIVATION
binary-jacobson-shortest-affine-braid-proof
