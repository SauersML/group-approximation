---
rg: 2
id: mark-fixed-relative-word-kernel-authentication
kind: route
title: Cut to the kernel of the mark-fixed relative word to authenticate the native Hecke maps
target: center-chain-common-source-authenticates-native-covariances
requires:
  - center-chain-mixed-relative-word-fixes-mark
  - literal-prefix-whitehead-relative-word-fixes-mark
---

**INVALIDATED MARK-COMMUTATOR KERNEL PROPOSAL.**  Use the newly established
identities

```text
[u_1^(-1)u_2,z]=0
```

for either the mixed center-chain words or the literal prefix-Whitehead
words.  Cut a finite matrix model to the kernel, or a small spectral
subspace, of this commutator and use the resulting invariant marked carrier
to identify the two native occurrence rows with the common-source Hecke
maps.

`mark-fixed-relative-words-do-not-coerce-native-hecke-kernel` invalidates
this route.  The commutator is identically zero in every representation, so
its kernel is the whole carrier and supplies no finite-coordinate selection.
A common balanced spectator gauge cancels from the relative word and
preserves its order, braid, support, and mark-centralizing data, while every
near-full source cut stays a fixed normalized-Hilbert--Schmidt distance from
the ungauged Hecke maps.  The relative identity therefore contains no
coercive information about either individual occurrence.
