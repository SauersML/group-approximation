---
rg: 2
id: simple-kl-via-abelianized-relation-module
kind: route
title: Rule out one-element normal generation by a first-homology computation on the kernel
target: kl-holds-over-two-generator-simple-groups
requires: []
artifacts:
  - research/artifacts/kervaire-laudenbach-audit-2026-08-17.md
---

The first thing to try after the reduction, and the standard tool for
"this normal subgroup is not normally generated by one element": pass to
`N_m^ab`, a `Z[C_m]`-module, and show it is not generated by the class of a
degree-`m` word.  Fox derivatives, Alexander modules and Reidemeister
torsion all live here, the module is computable, and one-relator normal
generation is exactly the kind of statement they were built to obstruct.

Dead: the module is `Z` and the class is a generator, always.  By
`degree-kernel-abelianization-is-infinite-cyclic`,
`N_m = ( *_{i<m} t^i S t^{-i} ) * <t^m>` and perfectness of `S` kills every
copy of `S^ab`, leaving `N_m^ab = Z` with trivial `C_m`-action, on which
`x |-> deg_t(x)/m` sends every candidate word to `±1`.  The necessary
condition is satisfied by all of them, so the test excludes nothing.

**What survives the demolition.**  The failure is specific to `H_1` of the
kernel, and it is informative rather than merely negative: it says the
obstruction, if one exists, is nonabelian or higher-dimensional.  That is
the content of `uniform-normal-rank-certificate-for-degree-kernels`, and the
route through it (`simple-kl-via-normal-rank-certificate`) is the live
successor to this one.

**Where the same computation is still worth doing.**  Over a *presentation*
source `P ->> Q`, the kernel's abelianization `R/[R,R]` as a `ZQ`-module is a
genuine obstruction to one-relator compression of that source, because `Q`
need not make it cyclic.  That is a different module and a different
question; nothing here says it is uninformative.
