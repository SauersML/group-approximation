---
rg: 2
id: decidable-residually-wd-groups-embed-in-lift-presented-hosts
kind: claim
title: Every finitely generated residually-W_d group with solvable word problem embeds in a faithful finitely generated self-similar group whose relator kernel is a finitely generated lift ideal (a self-similar Higman embedding); this suffices for Boone–Higman on that class, and residual chains alone cannot supply it
distinct_from:
  decidable-fg-self-similar-groups-embed-in-fp-self-similar: that asks for finitely presented self-similar hosts; this asks only for finitely lift-presented ones, which is weaker and still enough for a finitely presented Röver–Nekrashevych group.
  every-fp-rf-group-embeds-in-fp-self-similar-group: that takes finitely presented residually finite inputs and finitely presented hosts; this takes all decidable residually-W_d inputs, the largest class self-similar hosts can reach, and lift-presented hosts.
  lift-ideal-criterion-sufficiency-holds-for-infinite-state: that supplies the finite-presentation step for lift-presented hosts; this states the embedding step it feeds.
---

**OPEN** (named by bh-invent-03, 2026-09-19, at the coordinator's request). The observations below are lane-level
and elementary. Item (O2) recalls Nekrashevych's virtual-endomorphism dictionary.

## Statement

Let `G` be finitely generated, residually `W_d` for some `d`, with solvable word problem. Then there exist:
- `d' ≥ 2`;
- a faithful, finitely generated, self-similar `H ≤ Aut(T_(d'))` with `G ≤ H`,

such that the relator kernel of `H` (for a finite generating set) is `⟨⟨R⟩⟩_lift` for some finite `R`.

## What it gives for Boone–Higman

1. `V_(d')(H)` is finitely presented, by `lift-ideal-criterion-sufficiency-holds-for-infinite-state`.
2. Regrouping `(R)` of `rover-nekrashevych-finite-presentation-criteria` makes its abelianization finite.
3. The simple commutator subgroup is then finitely presented, and it contains a copy of `H`, hence of `G`, by
   Zaremsky's wreath embedding. Whether that step needs more than a finitely presented `V` is **not checked**.

So Boone–Higman would follow for every **residually-`W_d`** decidable group. This is exactly the class that
self-similar hosts can ever reach: `every-fp-rf-group-embeds-in-fp-self-similar-group`, (N1).

**Scope correction (after `farley-hughes-complexity-caps-review`).** This is **one sufficient route, not the general
target.** Farley–Hughes certificates need not have self-similar (restriction-closed) structure groups: Röver's,
FH Ex. 4.35, does not. Their groups `Γ_S` are groups of homeomorphisms of Cantor-type spaces, and they need not be
residually finite. So FH engines with non-restriction-closed structure groups may reach inputs beyond the
residually-`W_d` class. The residually-`W_d` bound applies to self-similar hosts only.

## Observations on the natural attempt: a residual chain

- **(O1) Faithfulness is free.** A residual chain `G = N_0 ⊋ N_1 ⊋ …` with `⋂ N_i = 1` and bounded indices gives a
  faithful action on the coset tree, which is spherically homogeneous. Padding makes it `d'`-regular.
- **(O2) Self-similarity is not free.** The action on the subtree below `xN_i` must be given by elements of `G`
  after identifying that subtree with the whole tree. This happens exactly when the chain is generated by a
  virtual endomorphism `φ : G_1 -> G` with trivial core, and then `g|_x = φ(r^(-1) g r)` (Nekrashevych, recalled).
  For a general residually-`W_d` group no such `φ` exists. The **self-similar closure**, generated by all
  sections, is then typically infinitely generated.
- **(O3) For virtual-endomorphism actions, lift generation means that `φ` shrinks relators.**
  - A relator `r` enters `⟨⟨R⟩⟩_lift` through lift closure only once its sections, the `φ`-images of its
    conjugates, are already there.
  - `φ` persistent-like, say fixing a subgroup it acts on as the identity, kills every lift shortcut
    (`persistent-actions-make-lift-ideals-normal-closures`).
  - `φ` contracting, i.e. shrinking all lengths, makes `H` contracting. Its word problem is then polynomial, so it
    can host no hard input (`finite-type-farley-hughes-groups-are-exponentially-capped`, and the contracting case
    of `rover-nekrashevych-engines-cap-orbit-evaluation-exponentially`).
- **(O4) Conclusion on hard inputs.** A host for a hard input needs a `φ` that **contracts relators but expands
  elements**: infinitely many states, with relator sections shrinking.
  - The decoupled product `lamplighter-times-z-infinite-state-rn-group-is-fp` is the only such example recorded. It
    is easy, and its relators and states sit on different coordinates.
  - Whether a hard group can carry a relator-contracting, element-expanding recursion is the crux. Residual chains
    alone give neither the self-similarity nor the relator contraction.

## Lesson for general BH

**The self-similar Higman step is a question about recursions, not about quotients.** Bounded residual quotients
buy faithfulness only.
- **What the embedding must build.** A recursion in which relators shrink while elements do not.
- **The analogy.** This is the self-similar analogue of Higman's benign subgroups. Relators must be *lift-generated*
  by finitely many, as Higman's must be finitely generated after a finite-presentation trick.

## Correction and scope update (bh-gate1-abstract, 2026-09-19, after referee bh-ref-t0)

- **Terminology.** "Residually `W_d`" constrains a **separating family** of finite quotients: the level actions,
  whose composition factors have order at most `d!`. It does **not** bound the composition factors of all finite
  quotients. `F_2 ≤ Aut(T_2)` has every finite group as a quotient. Informal paraphrases of this class as "bounded
  composition factors" should be read that way. Credit: bh-ref-t0, `farley-hughes-chart-coordinates-review`
  (dda7d68b92).
- **Scope of the "Scope correction" above.** Inputs beyond the residually-`W_d` class can still embed in FH groups
  `Γ_S`, which need not be residually finite. But **hardness** cannot come from anywhere else.
  - By `farley-hughes-certificates-have-prefix-chart-coordinates` (50a3370920; nested part refereed in
    dda7d68b92, product addendum a lane proof), every nested or product FH certificate caps seed orbits at
    `2^(O(n))`.
  - Its word problem is plausibly exponential over the word problems of its structure groups `H_τ`. That is item 4(b), a sketch the referee did not check in detail.
  - Those groups are residually `W_d` and carry a finite mutual recursion over a finite chart graph.
  - So a hard input still needs a hard, finitely presented, residually-`W_d` structure group, as this node's route
    assumes. The multi-type recursion is somewhat more permissive than a self-similar action of one group.
