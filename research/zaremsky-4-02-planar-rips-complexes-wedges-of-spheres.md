---
rg: 2
id: zaremsky-4-02-planar-rips-complexes-wedges-of-spheres
kind: claim
title: "Zaremsky Problem 4.2 resolved: is every connected Rips complex of a (finite) planar set homotopy equivalent to a wedge of spheres?"
root: true
---

M. C. B. Zaremsky, *Some open problems* (version of July 12, 2026,
https://zaremsky.github.io/open_problems.pdf), Section 4 (Vietoris–Rips
complexes), Problem 2, verbatim: "Is every (connected) Rips complex of every
(finite?) subset of the plane homotopy equivalent to a wedge of spheres?"

This claim is the question. It is established only through an answer route:

- **No, already for finite sets**: `zaremsky-4-02-by-no-finite` requires
  `some-finite-planar-set-has-a-non-wedge-rips-complex`.
- **Yes, for all subsets**: `zaremsky-4-02-by-yes-all` requires
  `rips-complexes-of-planar-sets-are-wedges-of-spheres`.
- **Yes for finite sets, no in general**: `zaremsky-4-02-by-split` requires
  `rips-complexes-of-finite-planar-sets-are-wedges-of-spheres` and
  `some-infinite-planar-set-has-a-non-wedge-rips-complex`.

Never write a `requires: []` route into this claim.

**Reading.**

- A subset `X ⊆ R^2` carries the Euclidean metric. For `r > 0` the Rips
  complex `R_r(X)` is the simplicial complex with vertex set `X` whose
  simplices are the finite subsets of diameter at most `r`, with the weak
  topology. It is the clique complex of the graph joining points at distance
  at most `r`. Rescaling gives `R_r(X) = R_1(X/r)`.
- For finite `X` the strict convention (diameter `< r`) produces the same
  family of complexes as `r` varies, so the finite reading does not depend on
  the convention. For infinite `X` both conventions are included; the
  non-strict complexes of infinite sets can have homology of uncountable rank
  (Adamaszek–Frick–Vakili, Remark 1.4), which a wedge of uncountably many
  spheres also has.
- "Wedge of spheres" means `∨_{i∈I} S^{n_i}` with every `n_i ≥ 1`, any index
  set `I`, dimensions allowed to differ; the empty wedge is a point.
- "(finite?)" gives two readings, finite `X` and arbitrary `X`. Resolving the
  problem means deciding both; the three routes above cover every outcome.

**What is known** (imported with verified statements):

- `planar-rips-projection-is-a-pi1-isomorphism` (Chambers–de Silva–Erickson–
  Ghrist 2010): for finite planar `X` the Rips complex maps to its shadow by a
  `π_0`- and `π_1`-isomorphism, so `π_1` is free. Adamaszek–Frick–Vakili
  (arXiv:1602.04131) call this "the only known non-trivial piece of
  information about the homotopy types of Rips complexes of subsets of
  Euclidean spaces".
- `planar-rips-pseudomanifolds-are-crosspolytope-boundaries` (AFV Theorem IV):
  induced normal pseudomanifolds of dimension `≥ 2` in planar Rips complexes
  are crosspolytope boundaries. AFV relate it to the question of
  Chambers–de Silva–Erickson–Ghrist [6.(1)], whether the significant
  homology of planar Rips complexes in dimensions `≥ 2` is generated by
  "local" point configurations.
- `euclidean-rips-complexes-model-pl-embeddable-complexes` (AFV Theorems II,
  III): in `R^n` Rips complexes realize every finite complex PL-embeddable in
  `R^n`, so already in `R^4` there are non-wedge Rips complexes; the plane is
  the open case.
- Context, not yet imported: Adamaszek (powers of cycle graphs) and
  Adamaszek–Adams (the circle, arXiv:1503.03669) show that Rips complexes of
  finite subsets of a circle, and of the circle itself, are points, odd
  spheres, or wedges of even spheres of one dimension.

## Attempts

- 2026-09-13 (z4-02-planar-rips): negative-side attacks and a bounded search
  of about 24,000 complexes (no certificate found) are recorded under
  `some-finite-planar-set-has-a-non-wedge-rips-complex` and in
  `research/artifacts/zp-planar-rips-search-2026-09-13.md`.
