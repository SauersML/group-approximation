---
rg: 2
id: far-sector-vertex-edge-proof
kind: route
title: Exactify the modular vertex by its stability, leaving the half-parabolic square root
target: far-sector-vertex-handled-edge-is-square-root
requires: []
artifacts:
  - research/artifacts/hs-dehn-modulus-length-control-2026-08-20.md
---

Virtually free groups are HS-stable, so the restriction of a microstate to
`SL_2(Z)` is near an exact representation; the only ungoverned generator is
the half-parabolic, whose square is the exactified parabolic, so the residual
task is the square-root/edge compatibility of the amalgam presentation.
