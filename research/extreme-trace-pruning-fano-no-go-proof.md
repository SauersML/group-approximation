---
rg: 2
id: extreme-trace-pruning-fano-no-go-proof
kind: route
title: Generate a matrix factor from two full PVMs and use Fano transitivity
target: extreme-traces-and-local-pruning-do-not-select-fano-caps
requires: []
---

The diagonal and Fourier MASAs generate all matrix units by `(ETP2)`, so the
resulting trace has factor GNS closure and is extreme while retaining all
seven atoms.  Equality-linked full domains are visibly an arc-consistency
fixed point.  Finally, translation by the ghost identifies the predicate
with `F_2^3\{0}`; transitivity of `GL(3,2)` rules out every nonempty proper
invariant selector, including a distinguished maximal cap.
