---
rg: 2
id: bk-rr0-uct-case-from-projection-trace-killing
kind: route
title: Kill the radical boundary of a real rank zero counterexample using the traces stable finiteness gives at every projection, then glue with Brown-Dadarlat
target: bk-rr0-uct-stably-finite-algebras-are-qd
requires: [rr0-projection-traces-give-qd-killing, stably-finite-projections-carry-annihilating-traces, brown-dadarlat-qd-extensions-and-k0-hahn-banach, stw07-separating-qd-quotients-force-quasidiagonality]
artifacts:
  - research/artifacts/bk-af-killing-projection-traces-2026-09-13.md
  - research/artifacts/bk-radical-projection-trace-step-2026-09-13.md
---

Let `E` be separable, nuclear, stably finite and of real rank zero, with every
quotient UCT, and suppose `E` is not quasidiagonal.

1. `R = R_qd(E)` is nonzero and `E/R` is quasidiagonal, by
   `stw07-separating-qd-quotients-force-quasidiagonality`.
2. `R` is separable, nuclear and of real rank zero (it is an ideal).  Every
   quotient `R/J` satisfies the UCT by two-out-of-three in
   `0 -> R/J -> E/J -> E/R -> 0`.
3. `H = ∂(K_1(E/R))` is singular in `K_0(R)`, by stable finiteness (step 2 of
   Proposition B in the artifact).  Every projection over `R` carries a
   normalised trace annihilating `H` on its finiteness ideal, by
   `stably-finite-projections-carry-annihilating-traces`.
4. `rr0-projection-traces-give-qd-killing` gives a faithful `φ : R -> D`, `D`
   quasidiagonal, with `φ_*(H) = 0`.
5. Lemma C of `research/artifacts/bk-radical-projection-trace-step-2026-09-13.md`
   (reviewed PASS in `research/artifacts/ex-review2-analysis-2026-09-13-part5.md`)
   applies to `0 -> R -> E -> E/R -> 0` with `ρ = φ` and `G = H`.  Here `E/R` is
   nuclear, quasidiagonal and UCT, `φ` is injective and `φ_*(H) = 0`.  So `E` is
   quasidiagonal.
   - Lemma C is the converse half of Brown–Dadarlat Proposition 4.6.  It embeds
     `D` in `Π M_n + K(H)` and uses Lemma 4.5 so that `H` dies in `K_0` of the
     hereditary subalgebra generated by the image.  Then it pushes the extension
     out with zero boundary and applies Theorem 3.4
     (`brown-dadarlat-qd-extensions-and-k0-hahn-banach`).
   - A naive pushout into `her(φ(R)) ⊆ D` is not enough: `K_0(her φ(R)) -> K_0(D)`
     need not be injective for a general quasidiagonal `D`.  (Review note (2),
     `research/artifacts/ex-review2-analysis-2026-09-13-part19.md`.)

So `E` is quasidiagonal, a contradiction.
