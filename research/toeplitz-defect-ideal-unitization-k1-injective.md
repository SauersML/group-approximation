---
rg: 2
id: toeplitz-defect-ideal-unitization-k1-injective
kind: claim
title: The ideal generated by one Toeplitz defect in T_2 * T_2 has connected unitary group in its unitization
distinct_from:
  o2-multiplier-ideal-with-non-k1-injective-unitization: that asks for some O_2-multiplier ideal with a non-K1-injective unitization; this is one specific ideal of T_2 * T_2, whose multiplier algebra contains T_2 but whose quotient is O_2 * T_2 rather than O_2.
---

Let `A_T = T_2 *_C T_2` be the full unital free product, with inclusions `iota_1, iota_2`, and put
`e = iota_1(1 - s_1s_1^* - s_2s_2^*)`. Let `J` be the closed ideal of `A_T` generated by `e`. Then the unitary
group of `J~ = J + C1` is path-connected.

Facts, all proved in the route `lxi-from-halving-and-toeplitz-defect-ideal`:
- `A_T/J ≅ O_2 * T_2`.
- `K_1(J) = 0` and `K_0(J) ≅ Z`. This uses `K_*(A_T) = (Z, 0)` (Blanchard arXiv:0804.4624v13, Lemma 4.4)
  and `K_*(O_2 * T_2) = (0, 0)` (Germain's six-term sequence).
- So the claim is exactly K1-injectivity of `J~`.
- With the halving homotopy `o2-free-circle-halving-homotopy`, this claim implies STW Problem LXI.

## Attempts

- Stability shortcut: dead. `J` has the unital quotient `O_∞`. Send `iota_1(s_k)` to the first two
  generators of `O_∞`, so that e becomes a full projection, and send `iota_2` anywhere.
- Module picture: `J = K(X)` for the Hilbert `eA_Te`-module `X = A_T e`. The orthonormal family
  `iota_1(s_w) e` spans a copy of `H_{eA_Te}` inside X. It cannot be complemented, since otherwise
  Kasparov stabilization would make J stable.
- Representations: whenever `iota_1(1 - s_1s_1^* - s_2s_2^*)` is represented with finite rank, `J` maps into
  the compact operators. So J carries index-type rank data, and Blanchard's non-cancellation example
  (0804.4624, Remark 4.7(ii)) is built from defect projections of this kind.
- Cancellation route: `o2-ideal-projection-unit-witness-fails-cancellation` would apply if `J` had an
  approximate unit of full projections with order cancellation. No projection approximate unit of J is
  known. The finite Fock projections `iota_1(sum_{|w|<=n} s_w p_3 s_w^*)` do not converge strictly on J.
