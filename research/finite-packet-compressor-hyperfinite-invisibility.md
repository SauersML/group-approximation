---
rg: 2
id: finite-packet-compressor-hyperfinite-invisibility
kind: claim
title: A traceless finite representation packet and an abelian compressor force hyperfinite mark invisibility
distinct_from:
  marked-clifford-compressor-hyperfinite-invisibility: that obtains the finite-dimensional trace zero from signed permutations of Clifford generators; this uses an arbitrary finite subgroup whose negative central-character block is simple and tests the actual inner implementers of the compressor automorphisms.
  hyperfinite-covariant-obstruction-lemma: that begins with proper outerness of a represented action; this supplies it from a representation-independent finite packet and Fourier spreading on an abelian normal subgroup.
artifacts:
  - research/artifacts/finite-packet-hyperfinite-invisibility-audit-2026-08-30.md
  - research/artifacts/stw99-x2-unitary-subgroup-reduction-2026-08-30.md
---

Let `Gamma` be a countable group with a central involution `w`.  Suppose it
contains `B=N semidirect H`, where `N` is abelian and `H` is nonamenable.
Assume there are a finite subgroup `K <= Gamma` containing `w` and a subset
`V` of the normalizer of `K` such that:

1. with `e_-=(1-w)/2`, the finite-dimensional block

   ```text
   e_- C*(K) e_- is isomorphic to M_d
   ```

   for one `d>=1`;
2. for every `v in V`, conjugation by `v` induces an automorphism of this
   block, and an implementing unitary `U_v in M_d` has normalized trace
   zero;
3. every `n in N\{e}` is conjugate in `Gamma` to an element of `V`;
4. every `h in H\{e}` has Haar-null fixed-point set for the dual action on
   `N_hat`.

Then every homomorphism from `Gamma` to the unitary group of a finite
injective von Neumann algebra kills `w`.

The trace condition in (2) is intrinsic: two implementers of the same
automorphism of `M_d` differ by a scalar, so either both normalized traces
vanish or neither does.  No Clifford presentation, property (T), residual
finiteness, invariant-measure rigidity, or character classification is
required.

The signed-Clifford criterion is a special case.  Take `K` to be the finite
extraspecial two-group generated by the central sign and an even Clifford
packet.  Its negative central-character block is the unique spin matrix
block, and the signed-cycle formula computes exactly when the normalizer
implementer in that block is traceless.
