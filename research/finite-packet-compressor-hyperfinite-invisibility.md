---
rg: 2
id: finite-packet-compressor-hyperfinite-invisibility
kind: claim
title: A semisimple finite representation packet and an abelian compressor force hyperfinite mark invisibility
distinct_from:
  marked-clifford-compressor-hyperfinite-invisibility: that obtains the finite-dimensional trace zero from signed permutations of Clifford generators for an involutive mark; this uses arbitrary finite-order central marks and finite subgroups, allows several irreducible summands in each central-character block, and tests only fixed summands.
  hyperfinite-covariant-obstruction-lemma: that begins with proper outerness of a represented action; this supplies it from a representation-independent finite packet and Fourier spreading on an abelian normal subgroup.
artifacts:
  - research/artifacts/finite-packet-hyperfinite-invisibility-audit-2026-08-30.md
  - research/artifacts/stw99-x2-unitary-subgroup-reduction-2026-08-30.md
---

Let `Gamma` be a countable group with a central element `w` of finite order
`q`, and suppose it contains `B=N semidirect H`, where `N` is abelian
and `H` is nonamenable.  For every `q`-th root of unity `lambda!=1`, assume
there are a finite subgroup `K_lambda <= Gamma` containing `w` and a subset
`V_lambda` of its normalizer such that:

1. with

   ```text
   e_lambda=q^(-1) sum_(k=0)^(q-1) lambda^(-k) w^k,
   e_lambda C*(K_lambda) e_lambda
      isomorphic to directSum_(j in J_lambda) M_(d_(lambda,j));
   ```
2. for every `v in V_lambda`, conjugation by `v` induces an automorphism
   `alpha_(lambda,v)` of this block.  On every summand fixed by the induced
   permutation of `J_lambda`, an implementing unitary has normalized trace
   zero;
3. every `n in N\{e}` is conjugate in `Gamma` to an element of `V_lambda`;
4. every `h in H\{e}` has Haar-null fixed-point set for the dual action on
   `N_hat`.

Then every homomorphism from `Gamma` to the unitary group of a finite
injective von Neumann algebra kills `w`.

The trace condition in (2) is intrinsic: two implementers of the same
automorphism of a fixed matrix summand differ by a scalar.  Summands moved
by `alpha_v` require no trace test—the corresponding part of every
implementing unitary is off-diagonal and hence already traceless.  No
Clifford presentation, property (T), residual finiteness, invariant-measure
rigidity, or character classification is required.

The signed-Clifford criterion is a special case.  Take `K` to be the finite
extraspecial two-group generated by the central sign and an even Clifford
packet.  Its negative central-character block is the spin matrix block, and
the signed-cycle formula computes exactly when the normalizer implementer in
that block is traceless.

The spectral-sector formulation also applies to odd-prime and general
finite-order central marks; the packet may depend on the nontrivial central
character.
