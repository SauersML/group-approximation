---
rg: 2
id: half-space-tensor-models
kind: route
title: Half-space tensor models (shift on a one-sided tensor product)
target: hyperlinear-wreath-model
requires: []
---

Represent `Γ` inside `⊗_{k≥0} A` with the compressor acting as the shift.
Dead for half-space towers: any `γ ∈ ⋂ₙ φⁿ(Γ)` lands in the tail `= ℂ`,
contradicting `τ(ρ(γ)) = 0` (half-space-tail-no-go). Scope, corrected: the half-space
hypothesis is essential — two-sided models with the constants on the
diagonal are NOT excluded by this.
