---
rg: 2
id: symmetric-power-fox-smith-reduction-proof
kind: route
title: Write the universal polynomial rows and apply Smith normal form over Z[1/2]
target: symmetric-power-fox-kernel-is-smith-obstruction
requires:
  - authenticated-mixed-loop-kills-quartic-affine-modules
---

For a homogeneous polynomial `P(x,y,z)` of arbitrary degree, the natural
coefficient substitutions in the two universal rows `(MMP3)` give

```text
XP=
 P(x,y+2x,z)-P(-y/2,y+2x,z)
+P(2x,y/2,z+y)-P(2x,-z/2,z+y)
-P(x,y,z),                                               (SPR1)
```

and

```text
YP=
 P(x,y,z)-P(x-y/2,y,z)
-P(x,y,z+x-y/2)+P(x-y/2,y,z+x-y/2)
-P(x/2,y,2z-y)+P(x/2,y,2z).                             (SPR2)
```

These identities follow by substituting successively

```text
q:x|->x-y/2,             A:y|->y+2x,
s:y|->y-z/2,             B:(x,y,z)|->(2x,y/2,z+y),
u:z|->z+x-y/2,           r:z|->z-y,
h^(-1):(x,y,z)|->(x/2,y,2z)                            (SPR3)
```

in `(MMP3)`.  In particular every matrix entry of `T_n` lies in
`R=Z[1/2]`.

The ring `R` is a principal ideal domain.  Apply Smith normal form to the
`2d_n x d_n` matrix of `T_n`.  It is equivalent over `R` to a diagonal
rectangular matrix with nonzero invariant factors

```text
a_1 | a_2 | ... | a_r,                 r<=d_n.          (SPR4)
```

The ideal of maximal minors is zero when `r<d_n`; when `r=d_n` it is the
principal ideal generated by `a_1...a_(d_n)`.  Thus that ideal is `R` exactly
when every invariant factor is a unit.  This is equivalent to `T_n` having
an `R`-linear left inverse, proving the equivalence of items 2 and 3.

Base change of `(SPR4)` to a field of odd characteristic is injective exactly
when no invariant factor vanishes there.  Therefore injectivity in every odd
characteristic is equivalent to `r=d_n` and every nonunit prime divisor of
the invariant factors being absent.  Since `2` is already a unit in `R`,
this is exactly item 4 and exactly the unit-ideal condition in item 2.  This
proves all four equivalences.

The explicit certificates in the preceding authenticated affine nodes show
the unit-minor condition for defining degrees one through four.  Equations
`(SPR1)--(SPR2)` do not preserve a lexicographic triangular filtration: the
rank-one Weyl substitutions exchange `x`- and `y`-degree and exchange
`y`- and `z`-degree.  Consequently the calculations through degree four do
not by themselves supply an induction on `n`; the unit Smith ideal is the
precise remaining recurrence target.
