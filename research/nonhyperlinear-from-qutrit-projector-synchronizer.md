---
rg: 2
id: nonhyperlinear-from-qutrit-projector-synchronizer
kind: route
title: Groupify the moment-exact positive-ONE3 system by qutrit fixed-space synchronization
target: non-hyperlinear-group
requires:
  - non-ce-bcs-has-positive-one3-weighted-form
  - positive-one3-is-one-qutrit-clock-context
  - qutrit-fixed-space-projector-synchronizer
  - central-third-root-no-ce-sector-forces-nonhyperlinear
artifacts:
  - research/artifacts/qutrit-moment-exact-psync-splice-2026-08-23.md
---

**INACTIVE CONDITIONAL ROUTE.**  Its synchronizer prerequisite is refuted by
`qutrit-psync3-tensor-closure-obstruction`.  The argument below records what
the proposed endpoint would have implied, but it is not a live route to the
root.

Take the finite positive-ONE3 system from the first prerequisite.  Introduce
one order-three clock `T_c` per clause and one central letter `J` with
`J^3=1`.  Use `(QOC2)` for the three local clause positions.  For every two
occurrences of the same Boolean variable, attach one copy of the fixed-space
synchronizer to the phase-shifted clocks in `(QOC3)`.  The resulting group is
finitely presented.

The perfect tracial ONE3 model extends copy by copy by complement-flexible
completeness, giving an exact tracial representation with `J=omega I`.
Conversely, in any CE trace in that sector every group relator has zero
defect, so `(PS3-1)` identifies the truth projections of all repeated
occurrences.  The qutrit-clock identities then decode a perfect CE tracial
model of the positive-ONE3 system, contradicting `(N13-1)`.

Thus the group has a nonempty `omega` sector but no CE trace in that sector.
The central-third-root criterion proves that it is nonhyperlinear.
