---
rg: 2
id: atlas-distinct-chart-ee-return-authenticates-common-mode
kind: route
title: Authenticate the common x12(ee) mode by returning it to both atlas charts
target: paired-same-reservoir-boundary-lemma
requires: []
---

**REFUTED ROUTE.** Follow the two-`E` return to `p1_23`, then use the two
disjoint Weyl swaps `w_13w_24` to land on the independently authenticated
second-chart generator `p2_23`.

The second endpoint is the conjugate nested monomial, not a scalar copy of
the unstripped common mode. Keeping both transported monomials as mandatory
nuisances leaves quotient rank two, and arbitrary seam/Weyl gauges realize
the kernel exactly. See
`atlas-distinct-chart-ee-return-retains-loop-gauge`.
