---
rg: 2
id: h-mix-halves-are-lef-by-column-windows-proof
kind: route
title: Replace the fixed column evaluation by restriction to any finite-dimensional invariant window; offset windows serve x_43(S), and the characteristic polynomial of ab forbids any window for D
target: h-mix-halves-are-lef-by-column-windows
requires:
  - jacobson-rank-three-parabolics-are-lef
  - binary-jacobson-el2-is-lef
  - p22-double-parabolic-is-not-lef
artifacts:
  - experiments/h-mix-column-windows-2026-09-17/check_windows.py
---

## Imports (verbatim)

From `jacobson-rank-three-parabolics-lef-proof`, the route of the ESTABLISHED
`jacobson-rank-three-parabolics-are-lef`, which itself requires
`binary-jacobson-el2-is-lef`:

> `V_+ = F^((N)) (x) F^2` carries the near action `g -> g_+` of `M_2(J)`.
> `W_M = span{e_(k,i) : 0 <= k <= M}`

> Fix a finite set `B_2 ⊂ EL_2(J)` with `1 in B_2 = B_2^(-1)`. Let `R` and `C`
> be the constants of that route for `B_2 ∪ B_2 B_2`. For `M > 4(R + C) + 4`
> the route shows:
> - `Phi_M(g) in GL(W_M)` for `g in B_2 ∪ B_2 B_2`;
> - `Phi_M(gh) = Phi_M(g) Phi_M(h)` for `g, h in B_2`;
> - `Phi_M` is injective on `B_2`.

> `Phi_M(g) w = g_+ w   (in particular g_+ w in W_M).                 (E0)`
> [for every vector `w in V_+` supported in `[0, M/2]`]

> **(E3) Kernel.** `ev_d(x) = 0` if and only if `x in span{S^a T^b : b >= d}`.

> `S^a T^b e_s` is `0` or `e_(s - b + a)`

From `p22-double-parabolic-is-not-lef`:

> **Theorem.** Let `Gamma_pig = <g,e,u,r1,r2 | P1, P2, P3>` (6 relators).
> * (a) In every group `G` and every homomorphism `Gamma_pig -> G` for which
>   the subgroup `A` generated by the `<r1,r2>`-conjugates of `u` is finite,
>   the word `[e, g^{-1}ug]` maps to `1`.

> (H) `g^{-1} u g = x_13(t) x_23(-q)`, and `[e, g^{-1} u g] = x_13(-q) = h^{-1}`.

> (c) Hence every finite quotient of `P_{2,2}(R)`, and every
> finite-dimensional linear representation of it over any field, kills
> `h = x_13(1-st)`.

> **Class.** Head-keeping models `sigma` of `P_{2,2}(J)` in which the orbit
> group `A` has a "co-Hopfian" hull. This includes: ... LEF partial models on
> the ball containing (P1)-(P3) and (H)

That class dies: "Pigeonhole ... makes the endomorphism onto. Then
`sigma(h) = 1`." Its certificate is `u = x_13(1)`, `g = diag(u2, 1)` with
`u2 in EL_2(J)` a product of `x_12`, `x_21` factors, `e = x_12(1)`,
`r1 = x_34(-1)` and `r2 = x_43(-s) x_34(-1)`.

**Identification.** The imported `V_+ = F^((N)) (x) F^2` is the column space
`U` of the target, and the near action is `B_+(v_1, v_2)^t = B (v_1, v_2)^t`.
All of `U` is a genuine left `M_2(J)`-module, so `(XY)_+ = X_+ Y_+`.

## Step 1. Theorem 1

**Constants.** Let `B` be a finite subset of `P_R`, with `1 in B = B^-1`, and
write its elements `(g, X, B')`.
- `B_2` is the set of first components. Take `R`, `C` for it as in the import.
- `R_0 <= R` is generated by the last components.
- `D_S` is the largest `S`-exponent in the entries of `X` over `B ∪ BB`.
- `d` exceeds every `T`-exponent in the entries of `X - X'`, over pairs in `B`.
  It also exceeds the `T`-exponents in the entries of `B' - B''` over pairs of
  last components.
- `Omega ⊇ Win_d (+) Win_d` is finite-dimensional and `R_0`-invariant, by
  hypothesis. Let `s_Omega` bound the supports of its vectors.
- `M > max(4(R + C) + 4, 2(s_Omega + D_S + 1))`.

`R_0` acts by invertible maps and `Omega` is finite-dimensional. So
`B'_+ Omega = Omega` and `B'_+|Omega in GL(Omega)`.

**Support.** For `w in Omega` and `(g, X, B') in B ∪ BB`, `X_+ w` is supported
in `[0, s_Omega + D_S] ⊂ [0, M/2]`, by the monomial rule. So
`X_+|Omega : Omega -> W_M`.

**Model.** `Psi(g, X, B') = [[Phi_M(g), X_+|Omega], [0, B'_+|Omega]]` on
`B ∪ BB`, and `1` elsewhere. It lies in the finite group `GL(W_M (+) Omega)`.

**Multiplicativity on `B x B`.** Block multiplication gives
`(g, X, B')(h, Y, C') = (gh, gY + X C', B'C')`. The upper-right block of
`Psi(x) Psi(y)` is `Phi_M(g) Y_+|Omega + X_+|Omega ∘ C'_+|Omega`.
- The first term is `(gY)_+|Omega`, by (E0) and the support bound.
- The second term is `(XC')_+|Omega`, because `C'_+ Omega ⊂ Omega` and
  `U` is a module.
- The diagonal blocks multiply by the import and because `R_0` acts.

**Injectivity on `B`.** Suppose `Psi(x) = Psi(y)`.
- Then `Phi_M(g) = Phi_M(h)`, so `g = h`.
- Also `(X - Y)_+` vanishes on `Win_d (+) Win_d`. By the entrywise (E3),
  `X = Y`.
- Likewise `(B' - C')_+` vanishes on `Win_d (+) Win_d`, so `B' = C'`.

**The head.** The upper-right block of `x_13(Q)` sends `(e_0, 0)` to
`(Q e_0, 0) = (e_0, 0) != 0`.

## Step 2. Theorem 2

- `a_+(v_1, v_2) = (v_1 + v_2, v_2)` preserves `Win_d (+) Win_d`.
- `b_+(v_1, v_2) = (v_1, S v_1 + v_2)` preserves `Win_d (+) Win_(d+1)`,
  because `S Win_d ⊂ Win_(d+1)`.
- `a^2 = b^2 = 1` over `F_2`.
- `W = P_1` is LEF by Step 1 with `Omega = Win_d (+) Win_d`.

`c = ab` has infinite order by Step 3 item 2: a finite-order `c` would
preserve the span of the `c`-orbit of `Win_1 (+) Win_1`. A group generated by
two involutions whose product has infinite order is `D_inf = <a> * <b>`.

`H_mix = W x| D`, since `W` is normal, `W ∩ D = 1` and `H_mix = W D`. The
amalgam lemma of `lef-normal-amalgams-need-not-be-mf-proof` (Step 3: equal
universal properties) gives `W x| (<a> * <b>) = (W x| <a>) *_W (W x| <b>)`.

**W is not residually finite.** Let `W -> F` be finite, with
`M' = ker ∩ M_2(J)`.
- `M'` is invariant under `Y -> gY` for `g in EL_2(J)`, hence under the
  differences `Y -> r E_ij Y` (`i != j`). It is also invariant under their
  products `r E_ij E_ji = r E_ii`, so it is a left `M_2(J)`-submodule.
- `P = M_2(J)/M'` is finite. `J` acts on `E_11 P` through `x -> x E_11`.
- On a finite module `TS = 1` makes `S` injective, hence bijective, so
  `ST = 1` and `Q` acts as `0`.
- So `[Q E_11] = (Q E_11)[E_11] = 0`, which means the head lies in `M'`.

**H_mix is not LEF.** The five certificate elements lie in `H_mix`, since
`r1 = a` and `r2 = ba`. `Gamma_pig` is finitely presented, so a LEF model of
`H_mix`, exact on a large enough ball, restricts to a homomorphism
`Gamma_pig -> finite group`. That homomorphism kills `h`, by the imported
theorem (a) through (c). But `h != 1` lies in the ball, a contradiction.

## Step 3. Theorem 3

**Item 1.**
- (⇐) A finitely generated `R_0` is finite. Take `Omega` to be the span of
  `R_0 (Win_d (+) Win_d)`.
- (⇒) `U` is `F_2[s]^2` as an `F_2[S]`-module, with `e_j <-> s^j`, and `R_0`
  acts by matrix multiplication. So `B'` is determined by the columns
  `B'(e_0, 0)` and `B'(0, e_0)`, and restriction `R_0 -> GL(Omega)` is
  injective. `R_0` is therefore finite.

**Item 2.** Let `Omega != 0` be finite-dimensional and `c`-invariant, and let
`m in F_2[x]` be the minimal polynomial of `c|Omega`.
- `m(c)`, a matrix over `F_2[s]`, kills a nonzero vector of `F_2(s)^2`, so
  `det m(c) = 0`.
- `det m(c) = prod m(lambda_i)`, where `lambda_1, lambda_2` are the roots of
  `x^2 + s x + 1` (trace `(1 + s) + 1 = s`, determinant `(1+s) - s = 1`).
- So some `lambda_i` is a root of `m`, hence algebraic over `F_2`. Then so is
  `s = lambda_i + lambda_i^-1`. That is false.

**Item 3.** Exactness at `(x_13(1), B)` and `(x_24(1), B)` for `B in {a, b}`.
The products are `(1, E_ii B, B)`, and `Psi(1, E_ii, 1)` has upper-left
`phi(1) = 1`, which is forced by `Psi(1) = 1` and exactness at `(1, 1)`.
This gives

```text
(E_ii)_+|Omega ∘ rho(B) = (E_ii B)_+|Omega,   i = 1, 2.
```

Add the two identities, using `E_11 + E_22 = 1`: `rho(B)` equals `B_+|Omega`
as a map into `U`. So `B_+ Omega ⊂ Omega` for `B = a, b`. Then `Omega` is
`c`-invariant, and `Omega = 0` by item 2. The upper-right block is then zero,
and `Psi(x_13(Q)) = Psi(1) = 1`.

The window form is check 4 of the artifact:
- `a_+` sends `(0, e_(d_2-1))` to `(e_(d_2-1), e_(d_2-1))`;
- `b_+` sends `(e_(d_1-1), 0)` to `(e_(d_1-1), e_(d_1))`.

**Caution.** `JQ` is a left ideal, and `JQ S = 0`. For `B in D`, row two of `B`
reduces mod `S` to `(0, 1)`, since `b ≡ 1` and `a ≡ [[1,1],[0,1]]`. So
`[0 | v] B = [v B_21(0) | v B_22(0)] = [0 | v]`.
- `N_0` is therefore invariant under `D`, and under left `EL_2(J)`.
- `M_2(J)` is abelian, so `N_0` is normal in `H_mix`.
- `Q E_11` has a nonzero first column, so it is not in `N_0`.
