---
rg: 2
id: infinite-character-actor-closes-pauli-metapixel
kind: route
title: Use one infinite nonamenable character orbit as the entire capacity backend
target: bounded-degree-entangled-agreement-pauli-metapixel
requires:
  - same-basis-rounding-interface-for-sparse-weyl-sampler
  - infinite-character-actor-word-energy-interface
  - one-seed-infinite-character-expansion-collapses-mark
---

Round the additive module action on the marked carrier.  The word-energy
interface supplies `(ISC2)`, while the single D8 edge supplies the seed
inequality.  Formula `(ISC3)` collapses every finite-dimensional marked
carrier with one fixed modulus.  The exact infinite model retains the seed
and mark, yielding the required metapixel without a size parameter.
