---
rg: 2
id: phase-cycle-active-floor-proof
kind: route
title: Tensor an asymptotic multiplicity solution behind the fixed packet and compare flag ranks
target: phase-cycle-return-needs-active-typed-hs-floor
requires: []
---

Schur decomposition gives `(PAT1)`.  Substitution of the fixed outer
matrices turns every finite actuator word into one fixed matrix equation on
the multiplicity unitaries, so `(PAT3)` produces full approximate actuator
models.  The packet formulas fix the source and target traces in `(PAT4)`.
Unitary conjugacy preserves source trace, and the projection identity
`||E-F||_2^2=tr(E)+tr(F)-2tr(EF)` implies `(PAT5)`.  Summation yields
`(PAT6)`, proving that failure of `(PAT7)` is an exact countermodel to the
desired return estimate.
