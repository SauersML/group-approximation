---
rg: 2
id: stw93-free-factor-central-sequences-are-tracially-scalar
kind: claim
title: Reduced-free-group norm-central sequences are scalar modulo the trace kernel
artifacts:
  - research/artifacts/stw93-xciii-free-group-jiang-su-si-audit-2026-08-30.md
---

Let `B=C*_r(F₂)` with its canonical trace.  Then

```text
F(B)/(J_τ∩F(B)) ≅ ℂ.
```

Under the canonical embedding `F(B)→F(B⊗𝒵)`, the non-scalar part
`x-τ_ω(x)1` of every `x∈F(B)` lies in the XCIII trace-kernel ideal.
