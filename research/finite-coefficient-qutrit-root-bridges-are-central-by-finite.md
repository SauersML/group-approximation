---
rg: 2
id: finite-coefficient-qutrit-root-bridges-are-central-by-finite
kind: claim
title: Mixed-sign qutrit root bridges escape only through infinite coefficient closure
artifacts:
  - research/finite-coefficient-qutrit-root-bridge-proof.md
distinct_from:
  positive-steinberg-qutrit-bridges-are-central-by-finite: that proves finiteness for arbitrary coefficients when every chosen root is positive; this permits arbitrary positive and negative root positions but assumes the coefficient algebra itself is finite.
  partial-whitehead-cell-has-marked-finite-model: that gives a finite specialization of a local partial-equivalence presentation; this proves that the literal subgroup over any finite coefficient closure is finite and lifts it through the Steinberg central extension.
  full-completeness-opposite-menu-has-finite-gl5-fusion: that gives an abstract finite occurrence model for a bounded opposite-root menu; this is a literal subgroup theorem in the binary Leavitt host.
---

Let `R=L_(F_2)(1,2)`, let `F<=R^x` be the order-`3^5`
qutrit prefix packet, and let `S` be a finite subset of `R`.  Form the
unital coefficient algebra

```text
A=F_2-algebra generated inside R by F union S.           (FCQ1)
```

Assume the exact finiteness hypothesis

```text
dim_(F_2)(A)<infinity.                                   (FCQ2)
```

For arbitrary, not necessarily positive, root positions
`i_alpha!=j_alpha` and coefficients `r_alpha in S`, put

```text
H=<D(F),e_(i_alpha,j_alpha)(r_alpha)> <= EL_n(R),
D(f)=diag(f,1,...,1).                                   (FCQ3)
```

Then `H` is finite.

For `n>=5`, the subgroup of `St_n(R)` generated by the lifted qutrit
packet and the corresponding standard root lifts is central-by-finite, hence
amenable and hyperlinear.  Therefore no finite algebraic atlas whose support
stays in such a mixed-sign package can establish nonhyperlinearity.

In particular the hypothesis holds when all coefficients lie in either

1. the finite `F_2)-algebra spanned by the finite packet `F`; or
2. one fixed full prefix-code matrix chart
   `A_D=span_(F_2){s_d t_e:d,e in D}`.

Thus scalar roots, packet-labelled roots, and arbitrary positive/negative
roots or Weyl words labelled inside one fixed code all remain finite
downstairs and central-by-finite upstairs.  Opposite signs alone are not an
escape.

The boundary is exact: a surviving mixed-root bridge must make its
`F_2)-coefficient algebra infinite-dimensional.  The literal Leavitt
one-sided inverses `s_i,t_i`, with `t_i s_j=delta_(ij)`, are the first
natural coefficients not covered by this theorem.  Finitely generated is
not enough; `L_(F_2)(1,2)` itself is finitely generated but
infinite-dimensional.
