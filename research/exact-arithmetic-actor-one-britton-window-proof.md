---
rg: 2
id: exact-arithmetic-actor-one-britton-window-proof
kind: route
title: Kazhdan-project the stable letter and test the single word [t,h]
target: one-britton-commutator-excludes-exact-arithmetic-actor-sector
requires:
  - exact-projective-codensity-has-a-uniform-trace-square-gap
---

Finite-dimensional arithmetic co-density gives `(OB4)`.  Apply the Kazhdan
projection estimate to `T` in the conjugation representation of `Lambda` on
normalized Hilbert--Schmidt space to obtain `(OB5)`.  Since the projection
commutes with `rho(h)`, the triangle inequality gives `(OB6)`, and the exact
unitary trace identity gives `(OB7)`.  The choice `(OB3)` contradicts the
single canonical mixed Britton moment `(OB2)`.
