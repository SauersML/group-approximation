---
rg: 2
id: robust-clifford-capacity-bound
kind: claim
title: A globally approximate Clifford-group homomorphism retaining its central sign has exponential dimension
artifacts:
  - research/artifacts/self-similar-dimension-pressure-map-2026-08-20.md
  - research/artifacts/three-paper-cairn-ingestion-2026-08-20.md
distinct_from:
  clifford-commutator-rank-is-packet-dimension: that concerns exact Pauli packets and rank; this is a normalized-Frobenius stability estimate for an approximate homomorphism on the entire finite Clifford group.
  same-model-supercritical-clifford-trigger: that must robustly manufacture the approximate homomorphism with a supercritical length chosen from the candidate model; this only supplies the terminal capacity inequality once such a map exists.
---

Let `C(n)` be generated by a central involution `J` and involutions
`x_1,...,x_n` with `[x_i,x_j]=J` for `i!=j`.  If

```text
psi:C(n)->U(d)
```

is an `epsilon`-homomorphism on **all pairs** and

```text
||psi(J)-1||_2 > 42 epsilon,
```

then

```text
d >= 2^(floor(n/2)-1).                                  (RCB1)
```

The generic Gowers--Hatami exactification threshold is `epsilon<1/16`.
Here it follows automatically from the displayed mark separation, since
`||psi(J)-1||_2<=2` gives `epsilon<1/21`.

The all-pairs hypothesis matters.  Starting only from defect at the standard
presentation relators and reducing products to normal form incurs an
`(n+1)^2` loss.  Therefore `(RCB1)` is a ready-made same-model capacity
contradiction only after a compiler has converted local checks into a global
approximate homomorphism with error independent of `n`.
