---
rg: 2
id: full-hecke-zero-cross-gram-model
kind: route
title: Realize the paid supports and synchronized braid with orthogonal target ranges
target: full-hecke-whitehead-zero-cross-gram
requires: []
---

Use the finite semidirect product and character projections `(HCG1)`.
Character orthogonality gives `(HCG2)` and the four equal sectors `(HCG4)`.
Conjugating the `h_2` sign in `p` by `j_1,j_2` gives the two self-Grams
`(HCG6)`.  For the mixed Gram, `Q<=e_2^D`, while `j_2Qj_2<=e_3^D`; `p`
commutes with `e_3^D` and `j_1` fixes it.  Thus the product contains
`e_2^D e_3^D=0`, proving `(HCG7)`.

The three actors are simultaneous star transpositions on both coordinate
families, so their label and reservoir factors separately obey the Coxeter
braid.  This verifies every advertised interface property in the canonical
regular trace of the finite group and establishes the firewall.
