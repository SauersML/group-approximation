---
rg: 2
id: sl3-dyadic-tower-exclusion-from-one-mixed-denominator-loop
kind: route
title: Exclude the dyadic A2 tower with the first two-sided mixed denominator loop
target: sl3-outliers-have-no-dyadic-a2-pvm-tower
requires:
  - free-hnn-outlier-has-dyadic-a2-pvm-tower
---

**INVALIDATED PROPOSAL.**  In addition to the unbalanced torus word and the
closed two-return `A_2` packet, retain the literal arithmetic identity

```text
[h x_21(-1) h^(-1),x_13(1)]=h^(-1)x_23(-1)h.          (MDL1)
```

Unlike a lattice-only Steinberg loop, `(MDL1)` traverses both orientations
of the denominator edge.  The proposed route was to apply it to the same
physical nested atoms in `(DPT2)--(DPT5)` and force a nonzero fold or
matrix-coordinate energy.

`one-mixed-denominator-a2-loop-is-dyadic-tower-neutral` gives exact finite
models at every retained dyadic depth.  They satisfy `(MDL1)`, the
unbalanced torus word, both closed `A_2` returns, exact same-atom covariance,
and all free scalar atom overlaps.  Thus this one mixed loop supplies no
exclusion inequality.  A successful denominator-crossing packet must also
kill its remaining rank-one sheet gauge (or otherwise authenticate the full
common lattice actor).
