---
rg: 2
id: full-signed-hecke-native-actor-is-p161-parabolic
kind: claim
title: The full signed-Hecke and native actor packet is a literal (1,6,1) binary parabolic
artifacts:
  - research/full-signed-native-p161-parabolic-proof.md
distinct_from:
  two-native-singers-and-endpoint-generate-literal-gl6: that constructs the six-dimensional Levi actor on the native and endpoint projective summands; this adjoins the complete signed source on two outer q-summands and identifies the generated finite parabolic.
  full-q-a1-projective-head-is-a-literal-finite-subgroup: that embeds the seven-summand canonical A1 extension as the finite subgroup P_A; this adds the endpoint e3 summand, the first native Singer, and identifies the resulting larger group structurally rather than only by its order.
  full-q-quarter-adjacent-profiles-have-half-cross: that computes the native cross operator on two adjacent-character monomial profiles; this reduces the arbitrary finite-type question to one exact central-support calculation in the complete finite parabolic Hecke algebra.
  literal-a1-head-forces-arbitrary-profile-native-cross-gram: that already converts the smaller literal A1-head identity into a profile-uniform cross-Gram floor; this identifies the endpoint-enlarged parabolic and transports the same identity into its complete actor packet.
  native-j1-extension-gives-gl5-but-only-second-whitehead-gram: that constructs a different GL5 on five native and endpoint summands; this reconciles that block with the canonical A1 Levi and identifies the full six-dimensional enlargement.
---

**ESTABLISHED LITERAL FINITE PARABOLIC.**  Put

```text
q=s_1t_1,
a_1=s_1t_0,        a_2=s_1t_00,        a_3=s_1t_000,
b_2=s_00t_1,       b_3=s_000t_1.                       (PSP1)
```

Use the center-chain roots

```text
C_3=x_65(q),       C_2=x_54(q),       C_1=x_42(q),
H=<C_3,C_2,C_1>,

d=x_82(b_2),       f=x_94(b_3),       c=x_97(b_3a_1),
k=x_92(b_3),       v=x_57(a_1),       w=x_67(a_1),
s=x_68(a_2),

L_0=<H,d,f,c,k,v,w,s>,                                (PSP2)
r=x_58(a_2),                 l=x_69(a_3).             (PSP3)
```

The signed character used by the common source is

```text
lambda_0|_H=chi,
lambda_0(d)=lambda_0(f)=lambda_0(c)=lambda_0(k)
 =lambda_0(v)=lambda_0(w)=lambda_0(s)=+1,             (PSP4)
```

where `chi` is negative on the three simple roots `C_1,C_2,C_3` and
positive on their derived roots.  Thus

```text
Q=e_(L_0,lambda_0).                                   (PSP5)
```

Let `G_6` be the literal native actor from
`two-native-singers-and-endpoint-generate-literal-gl6`.  Then

```text
P_(1,6,1):=<G_6,L_0,r,l>                              (PSP6)
```

is the standard binary parabolic with Levi `GL_6(F_2)` and unipotent
radical of order `2^13`:

```text
P_(1,6,1) ~= U_(1,6,1) semidirect GL_6(F_2),
|P_(1,6,1)|
 =2^13 |GL_6(F_2)|
 =165140150353920.                                    (PSP7)
```

This is a literal subgroup of `St_20(L_(F_2)(1,2))`, not only an
elementary-image quotient.  In a conjugate eight-projective-summand chart it
is the subgroup of `GL_8(F_2)` consisting of matrices

```text
[1  alpha  c]
[0    g    beta],       g in GL_6(F_2),
[0    0     1]          alpha in F_2^(1x6),
                        beta in F_2^(6x1), c in F_2.   (PSP8)
```

Consequently the whole fixed signed source, both native order-seven heads,
both native source signs, and the endpoint Whitehead lie in one fixed finite
subgroup.  All finite-packet correction and moving-boundary questions for
these words may therefore be replaced by exact finite representation theory
of `(PSP8)`.

The remaining literal Whitehead cross question is decided inside this
parabolic.  Put

```text
F=Q(1+r)(1+l)/4,             g=J_1J_2,
A_F=F C[P_(1,6,1)] F,        X=FgF.                   (PSP9)
```

```text
F in A_F X A_F.                                       (PSP10)
```

Indeed the subgroup `P_A` of
`full-q-a1-projective-head-is-a-literal-finite-subgroup` is generated by a
subset of the displayed parabolic generators, and its exact finite Hecke
calculation already proves `(PSP10)` inside `F C[P_A] F`.  Inclusion of that
corner in `A_F` proves `(PSP10)` as written.  Equivalently, `X` has full
central support in the finite twisted Hecke corner `A_F`.  Finiteness
therefore gives a constant `gamma>0` such that every finite-dimensional
representation of this parabolic obeys

```text
tr((FJ_1J_2F)^*(FJ_1J_2F)) >= gamma tr(F).             (PSP11)
```

This proves a uniform cross-Gram
for the literal Whitehead transports `J_1F,J_2F`.  It does **not** identify
them with the unequal controlled selector outputs of fractions `1/4` and
`1/8`, and therefore does not by itself prove
`center-chain-common-source-authenticates-native-covariances` or Atomic
Morita Return.

No Property `(T)`, Kazhdan input, canonical trace, approximation theorem,
or literature input is used.

DERIVATION
full-signed-native-p161-parabolic-proof
