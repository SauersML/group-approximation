---
rg: 2
id: opnorm-native-cross-gram-does-not-promote-to-hyperlinearity
kind: claim
title: An operator-norm native cross-Gram proof would not by itself exclude hyperlinear microstates
artifacts:
  - research/opnorm-native-cross-gram-scope-proof.md
distinct_from:
  opnorm-return-row-cannot-come-from-an-hs-ledger: that compares operator and Hilbert--Schmidt norms for one rank-deficient support row; this records the quantifier mismatch for the native occurrence theorem itself.
  binary-leavitt-native-two-scale-cross-gram-positive: that explicitly asks for the normalized-HS occurrence estimate on canonical microstates; this prevents an operator-norm surrogate from being substituted for that open claim.
---

Any proof which starts with an operator-norm asymptotic representation,
applies operator-norm packet exactification, and uses active-core reblocking
available only in a norm matrix corona has an operator-norm MF-radical
conclusion.  It does **not** establish the normalized-HS claim
`binary-leavitt-native-two-scale-cross-gram-positive`.

A hyperlinear approximation supplies only normalized-Hilbert--Schmidt
relator control and canonical trace moments.  It does not satisfy that
operator-norm hypothesis.  Consequently one may not apply an operator-norm
version of `(NCG1)` to a canonical hyperlinear microstate merely because its
marked involution has a trace-one-half negative carrier.

This is only a scope fence.  It does not invalidate
`nonhyperlinear-from-binary-leavitt-native-cross-gram`: that route correctly
requires the HS claim as stated.  Proving the occurrence theorem with all
errors controlled in normalized HS norm on canonical microstates would
therefore close both the tracial nonhyperlinearity route and, a fortiori,
the operator-norm Property-T-free manuscript front end.

DERIVATION
opnorm-native-cross-gram-scope-proof
