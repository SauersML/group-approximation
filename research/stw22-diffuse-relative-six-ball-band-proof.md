---
rg: 2
id: stw22-diffuse-relative-six-ball-band-proof
kind: route
title: Select dyadic low bands relatively over the six-ball
target: stw22-diffuse-relative-low-spectrum-six-ball-extension
requires:
  - stw22-diffuse-stiefel-uniform-five-sphere-fillings
  - stw22-diffuse-relative-low-spectrum-five-ball-extension
artifacts:
  - research/artifacts/stw22-dim6-factor-bundle-breakthrough-2026-08-31.md
---

Repeat the relative dyadic recursion.  After earlier bands, the moving
complement `g_n` has `tau(g_n)-tau(r_n)>=delta_0>0`.  Use the full interior
Stiefel value and replace it on `S^5` by the prescribed singleton boundary
frame.  The two-neighbourhood argument gives lower semicontinuity at the
boundary.  Normalized-corner scaling and the degree-zero through degree-five
diffuse Stiefel theorems give common local moduli and global
fiveconnectivity.  Michael with `n=5` selects each band over `B^6`.

Orthogonal weighted sums converge uniformly in `2`-norm inside a fixed
operator ball.  Their limits lie in the factor; `L^1` product convergence
preserves source, moving-complement membership and exact boundary.  The
weighted fibre diameter gives the asserted estimate.  Reserve-dependent
band moduli affect only existence, not the outer quantitative bound.

