---
rg: 2
id: atlas-q14-simple-overgroup-index-is-at-least-507
kind: claim
title: Every augmented finite-simple Atlas escape has A8 index at least five hundred seven
distinct_from:
  atlas-q14-simple-overgroup-index-is-at-least-506: that leaves the equality case M23; this excludes that exact maximal-A8 marked-pair problem.
  atlas-a4-m23-packet-double-coset-screen: that is the finite sporadic calculation; this combines it with the established ambient classification.
---

Let `S` be finite simple and let two marked `A8` chart subgroups generate `S`
while satisfying the packet, `q_19243`, and q14.  Then

```text
[S:A8] >= 507,       |S| >= 507 |A8| = 10221120.    (OSI507)
```

Indeed the preceding classification says equality in the old index-506
bound forces `S~=M23` with both charts in its unique maximal `A8` class.  The
exact `M23` double-coset screen shows more strongly that the packet already
forces those two chart subgroups to coincide, contrary to generation of
`M23`.

