---
rg: 2
id: fpbs-free-group-sandwich-cost-is-two
kind: claim
title: Every relation sandwiched between a non-cyclic finitely generated subgroup action and a free action of F_2 has cost at least two
distinct_from:
  fpbs-relative-sandwich-cost-bernoulli-lower-bound: that is RBL for all finitely generated groups, comparing Bernoulli with other free actions; this is the absolute value 2 for every free action of F_2 and every non-cyclic K, which implies the F_2 slice of RBL but not conversely without the reverse inequality.
  fpbs-sandwich-cost-fiber-formula: that proves the value 2 for group-generated and virtually group-generated sandwiches only; this asks it for all measurable sandwiches.
artifacts:
  - research/artifacts/fpbs-sandwich-cost-fiber-formula-2026-09-17.md
---

**OPEN.** Let `a` be a free p.m.p. action of `F_2` on `(X, μ)`, and let
`K ≤ F_2` be finitely generated and non-cyclic. Prove: every measurable
equivalence relation `E` with

```text
E_{a|K} ⊆ E ⊆ E_a
```

has `C(E) ≥ 2`. Equivalently, `p^{F_2}_K(a) = 2`. The upper bound is
`C(E_a) = 2`.

This is a measured form of Hanna Neumann's bound. It is the measured
analogue of "every subgroup of `F_2` containing `K` has rank at least 2".
Strong inertness gives that bound after passing to any finite-index
subgroup `N`.

**Implies the `F_2` slice of RBL.** Take `Δ = F_2` and a finite `F`.
- If `⟨F⟩` is non-cyclic, the claim gives `p_F ≡ 2` on free actions.
- If `⟨F⟩` is infinite cyclic, every sandwich contains the aperiodic
  `E_{a|⟨F⟩}`, so it is aperiodic and costs at least 1. `E_{a|⟨F⟩}`
  costs 1, so `p_F ≡ 1`.
- If `⟨F⟩` is trivial, `p_F ≡ 0`.

So `p_F(s) ≤ p_F(a)` holds for every free `a`, which is
`fpbs-relative-sandwich-cost-bernoulli-lower-bound` for `Δ = F_2`.

**Proved cases.** `fpbs-sandwich-cost-fiber-formula` (C), for rank-2 `K`:
- sandwiches `E_{a|M}` with `K ≤ M ≤ F_2`;
- on product actions `b × F_2/N` with `KN = F_2`, every sandwich whose
  restriction to one fibre is `E_{b|M'}` for some `K∩N ≤ M' ≤ N`.

**Why the group argument does not transfer** (artifact §5):
- A treeable relation of cost `3/2` can contain a relation generated by a
  free action of `F_2`, so containment of `E_{a|K}` alone forces nothing.
- Any proof must use `E ⊆ E_a`.
- Artifact Lemma 5 proves one rigidity fact of this kind: a partial
  isomorphism in `[[E_a]]` that commutes with `a|K` is the identity. This
  rules out the amplification construction of cheap sandwiches inside
  `E_a`.
