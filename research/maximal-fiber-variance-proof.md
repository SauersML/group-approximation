---
rg: 2
id: maximal-fiber-variance-proof
kind: route
title: Direct proof of Exact joint covariance can retain maximal fiber variance
target: maximal-fiber-variance
requires: []
artifacts:
  - notes/FALSE_EXACT_JOINT_COMPRESSOR_HAS_MAXIMAL_FIBER_VARIANCE.md
---

# Direct proof of Exact joint covariance can retain maximal fiber variance

Direct establishment: the complete argument is recorded in the
artifact(s) above (kernel-checked where the artifact is a Lean
module; source-verified prose otherwise). This route asserts the
claim `maximal-fiber-variance` outright — `requires: []` is a proof commitment,
not a placeholder.
