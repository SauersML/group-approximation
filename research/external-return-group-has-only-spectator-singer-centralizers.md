---
rg: 2
id: external-return-group-has-only-spectator-singer-centralizers
kind: claim
title: The full external-return group has only spectator Singer centralizers
artifacts:
  - experiments/external_return_exact_singer_centralizer.py
distinct_from:
  eight-external-returns-have-no-whitehead-gauge-lock: that exhausts the eight individual root returns and their direct images; this computes the entire finite group generated by all eight returns together with the signed Hecke subgroup.
  el20-six-moving-coefficient-square-bridges: that permits nonconstant Leavitt coefficients and mixed return identities weaker than direct Singer commutation; this closes only the full constant-root external-return subgroup.
---

**ESTABLISHED EXACT CONSTANT-ROOT FENCE.**  In the seven-coordinate binary
label representation, let `L_0` be the signed Hecke subgroup of order `8192`
and let `X` be the eight external root transvections of
`signed-hecke-normalizer-has-eight-external-root-returns`.  Put

```text
G_X=<X>=<L_0,X>=UT_(6,9,5,8,4,7,2)(F_2).              (ESC1)
```

The eight roots include all six adjacent arrows in the displayed
coordinate order, so iterated Steinberg commutators generate every forward
transvection and in particular contain `L_0`.  They normalize `L_0`, and
exact coset enumeration independently gives

```text
|G_X/L_0|=256,             |G_X|=2097152.              (ESC2)
```

For the two complete Singer heads

```text
t_1=(7 8)x_84(1)x_47(1),
t_2=(8 9)x_95(1)x_58(1),                               (ESC3)
```

their centralizers inside `G_X` both have order `64`.  More importantly,
they lie in the row-specific spectator subgroups

```text
C_(G_X)(t_1) <= <L_0,x_59(1),x_96(1)>,
C_(G_X)(t_2) <= <L_0,x_27(1),x_74(1)>.                 (ESC4)
```

In fact the centralizers are the two four-coordinate unitriangular groups

```text
C_(G_X)(t_1)=<x_96(1),x_59(1),x_25(1)> ~= UT_4(F_2),
C_(G_X)(t_2)=<x_46(1),x_74(1),x_27(1)> ~= UT_4(F_2).  (ESC5)
```

The two displayed external roots in each row are exactly the disjoint fixed
returns from `eight-external-returns-have-no-whitehead-gauge-lock`.
Consequently no product of the eight constant-root returns, of any word
length and even after arbitrary `L_0` dressing, supplies an active
same-chart commutation relation with the complete Singer head.  This closes
the whole constant-root external-return subgroup, not merely a bounded word
menu.

The centralizer description also has a direct primary-space derivation, so
the conclusion does not rest on closing a full general linear group.  Each
`t_i` is irreducible of order seven on its three active coordinates and is
the identity on the complementary four coordinates.  Its irreducible cubic
is coprime to `X+1`, so every commuting matrix preserves that splitting.
The active centralizer has odd order seven, while every element of `G_X` is
unipotent of two-power order; their intersection on the active block is the
identity.  What remains is exactly the four-coordinate unitriangular group
in `(ESC5)`.

The statement is an exact finite label-matrix calculation.  It does not
authenticate multiplicity gauges, exclude nonconstant Leavitt-coefficient
words, or assert a representation of full EL20.  No Property `(T)`,
Kazhdan input, trace profile, stability theorem, literature result, local
compilation, or local computation is used.

DERIVATION
external-return-exact-singer-centralizer-enumeration
