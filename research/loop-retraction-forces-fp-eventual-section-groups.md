---
rg: 2
id: loop-retraction-forces-fp-eventual-section-groups
kind: claim
title: If every state of an automaton fixes a letter x and its Rover–Nekrashevych group is finitely presented, then the eventual section group at x is finitely presented
distinct_from:
  persistent-automata-rn-group-fp-iff-group-fp: that is the fully persistent case (every state fixes x and is its own section there), where the eventual section group is the whole group; this only asks that every state fix x, with arbitrary sections, and concludes finite presentation of the subgroup generated by the eventual sections at x.
  rover-nekrashevych-fp-iff-finite-lift-presentation: that characterizes finite presentation of V_d(G) by lift ideals; this derives from that characterization a necessary condition that is checkable on one letter.
  bhm-singfix-condition-forces-fp-germ-groups: that says the hypothesis of the Belk–Hyde–Matucci finiteness theorem forces finitely presented germ groups; this is a necessary condition for finite presentation of V_d(G) itself, for the eventual section group at a commonly fixed letter.
---

**ESTABLISHED** by `loop-retraction-forces-fp-eventual-section-groups-proof` (lane proof, elementary; it uses
only the necessity half of Theorem A of `rover-nekrashevych-fp-iff-finite-lift-presentation`, which is not yet
refereed; no priority claimed: the argument is a variant of the retraction in Skipper–Witzel–Zaremsky
arXiv:1712.05361 §5).

## Setting

Notation of `rover-nekrashevych-fp-iff-finite-lift-presentation`: `G = ⟨S⟩ <= Aut(X^*)`, `S` finite and closed
under sections and inverses, `F` the free group on `S` (the identity sent to `1`), `N = ker(F -> G)`.

Suppose some letter `x in X` is **fixed by every state**: `s(x) = x` for all `s in S`. Then `σ(s) = s|_x` is a
self-map of `S`, and word-level sections at `x` give an endomorphism `σ: F -> F`, `σ(w) = w|_x`. Choose `p >= 1`
with `σ^p` idempotent on `S` (a finite self-map has one). Put
- `S_∞ = σ^p(S)`, the **eventual sections at `x`**; `σ` permutes `S_∞`, with inverse-closed image;
- `F_∞` the free group on `S_∞`, and `π = σ^p: F -> F_∞`, a retraction;
- `G_∞ = ⟨S_∞⟩ <= G`, the **eventual section group at `x`**. It equals `{g|_{x^p} : g in G}`.

## Statement

**Theorem.** If `V_d(G)` is finitely presented, then `G_∞` is finitely presented. More precisely, if
`N = ⟨⟨R⟩⟩_lift` for a finite `R`, then `N ∩ F_∞ = ⟨⟨θ^j(π(R)) : 0 <= j < q⟩⟩_{F_∞}`, where `θ = σ|_{F_∞}` and
`q` is the order of the permutation `θ` of `S_∞`.

## Consequences

1. **SWZ's `F_2` case.** If `S` is persistent at `x` (`s|_x = s`), then `S_∞ = S`, `G_∞ = G`, and this is the
   "only if" half of `persistent-automata-rn-group-fp-iff-group-fp`.
2. **Partial persistence is enough to kill finite presentation.** Only the letter has to be common; the
   states outside `S_∞` may be arbitrary. This gives
   `linear-activity-automata-can-have-non-fp-nekrashevych-groups`: a linear-activity automaton, with
   `G_∞` the Grigorchuk group, whose `V_3` is not finitely presented.
3. **For the Boone–Higman route** `lift-presented-automaton-groups-satisfy-boone-higman`: an action with a
   commonly fixed letter whose eventual section group is not finitely presented never satisfies its
   hypothesis. Such a group needs another action.
