---
rg: 2
id: agent-first-hit-named-corner-holonomy-seam
kind: claim
title: Named-corner rows defeat product splitting only after payload-to-root reflection
distinct_from:
  shared-bcs-carrier-localizes-free-compressor-quotient: that asks for the complete localized parent and child quotient actions; this isolates the exact logical order between root reflection, named-corner identification, and optional holonomy.
  agent-free-compressor-hnn-letter-preserves-product-split: that proves the unnamed corner rows retain a product split; this explains why the named rows close that split algebraically but not before their physical carrier is decoded.
  agent-first-hit-cross-holonomy-does-not-reflect-named-corner: that gives the exact split countermodel for projective phase matching; this records the surviving positive interface after the countermodel.
---

**ESTABLISHED.**  The rows

```text
[P,x_i]=0,       P(x_i-b_i)=0,       [P,t]=0,
t x_i t^(-1)=alpha(x_i)                              (NCH1)
```

already defeat product splitting and direct-sum dilution in every honest
coefficient-module representation: they put the named `B/A` packet comparison
on one stable carrier, and its extensive Reynolds rank mismatch forces
`P=0`.

For abstract normalized-HS group microstates, the remaining order of proof is

```text
BCS forbidden mass
  -> positive physical root carrier
  -> reflection of (NCH1) on that carrier
  -> finite B/A multiplicity payment.                 (NCH2)
```

A finite scalar/projective holonomy cell cannot establish either of the first
two arrows.  Its stable letters retain arbitrary external multiplicity
operators, and the trivial-root representation satisfies every wordized row
while an inverse projective multiplicity absorbs the cross phase.  Once the
first two arrows are supplied by a root/corner decoder, the final arrow is
already `agent-free-compressor-minimal-nonretractive-corner` together with
`finite-group-multiplicity-mismatch-pays-hs-energy`; no extra Schur two-cell is
needed.

Thus coupling one holonomy relation to the named rows does not create a new
intermediate theorem.  The exact remaining seam is the local metric
payload-to-root and row-reflection theorem, not cross-intertwiner phase
rigidity.

DERIVATION
agent-first-hit-named-corner-holonomy-seam via agent-first-hit-named-corner-holonomy-seam-proof
