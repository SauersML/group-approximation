---
rg: 2
id: full-raw-packet-commutant-witness
kind: claim
title: One full-raw-packet commutant unitary witnesses the Pauli gap
distinct_from:
  pauli-binary-algebra-gap: That measures distance from the two-dimensional algebra generated by one parent cut; this uses the full sixteen-element raw packet algebra and produces a unitary in its coordinate commutant.
  carrier-not-decoder: This is a quantitative terminal witness after the packet has been exactified; the other says a common carrier alone does not decode coefficient maps.
artifacts:
  - notes/TRUE_FULL_RAW_PACKET_COMMUTANT_WITNESS.md
  - GroupApproximation/Leavitt/PauliCarrierBinaryGap.lean
---

Let `K=<raw,z_11,z_001>` be the sixteen-element raw Pauli packet, let
`A_n=Alg(rho_n(K))` in a trace-preserving finite matrix microstate, and let
`F_n` be the transported trace-`1/4` carrier.  Then

```text
dist_2(F_n,A_n)^2 -> 3/16.
```

Haar averaging over `U(A_n')` therefore supplies a coordinate unitary `V_n`
with

```text
[V_n,A_n]=0,
||[V_n,F_n]||_2^2 >= 3/8-o(1).
```

After doubling, `V_n` may be chosen as a self-adjoint involution without
changing the energy.
