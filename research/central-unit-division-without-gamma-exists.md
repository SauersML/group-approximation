---
rg: 2
id: central-unit-division-without-gamma-exists
kind: claim
title: Some factorial tracially complete C*-algebra divides its unit approximately centrally for every k yet fails property Gamma
distinct_from:
  ccegstw-q529-central-unit-division-gives-gamma: that is the published question as a universal assertion; this is its negation.
  toms-no-gamma-algebra-is-trivial-on-closed-bauer-faces: that shows Toms's algebra has central halving and trivial face bundles; this reads its completion in the CCEGSTW category, adds central k-division for every k, and concludes that Proposition 5.27 fails without the Bauer hypothesis.
  amenable-factorial-ii1-tracially-complete-without-gamma: that separates amenability from property Gamma; this separates central division of the unit from property Gamma, which the Pauli completion cannot do because it has no division at all.
---

**Statement (unreviewed).** There is a factorial tracially complete C*-algebra `(M, X)`, amenable and
of type II_1, with the following properties:
- **Central division.** For every `‖·‖_{2,X}`-separable `S ⊆ M` and every `k`, there are pairwise
  orthogonal projections `p_1, …, p_k ∈ M^ω ∩ S'` with `τ(p_i) = 1/k` for all `τ ∈ X^ω`.
- **No Γ.** `(M, X)` does not have property Γ.

So CCEGSTW Question 5.29 (equivalently, the tracially complete form of CETW Question 3.5) has a
negative answer. The designated trace set `X` is necessarily non-Bauer, by Proposition 5.27.

Proof: route `central-unit-division-without-gamma-exists-via-toms`.
