---
rg: 2
id: closed-mcg-out-embedding-via-aut-embedding
kind: route
title: A closed-surface mapping class group inside some Aut(F_n) sits inside Out(F_(n+1))
target: every-closed-surface-mcg-embeds-in-some-out-free-group
requires:
  - every-closed-surface-mcg-embeds-in-some-aut-free-group
  - aut-free-embeddability-passes-to-finite-index-overgroups
---

For each `g`, `MCG(S_g) ↪ Aut(F_n)` for some `n`, and item 3 of
`aut-free-embeddability-passes-to-finite-index-overgroups` embeds `Aut(F_n)` in
`Out(F_{n+1})`.
