---
rg: 2
id: payload-coupled-hecke-separates-every-marked-ce-extension-type
kind: claim
title: Every marked CE extension type has a payload-coupled reverse fixed-space subgroup pair
refuted_by: canonical-types-extend-every-finite-bass-serre-actor
distinct_from:
  type-specific-hecke-nemeses-have-automatic-common-completeness: that proves such pairs can all coexist and robustly pay; this must construct one pair from each actual CE extension type.
  v4-pauli-reynolds-edges-have-no-strict-orientation: that exhausts native Pauli halves and joint atoms; this must use a subgroup outside the V4 whose fixed-space character is coupled to the decoded losing payload.
  tailored-checks-to-common-marked-group-extensions: that permits a general selected-corner groupification; this asks for the narrower subgroup-Reynolds realization sufficient for the HNN actuator.
---

**REFUTED in the stated finite-subgroup/HNN form.**  The requested interface
was: from a code for a neighborhood in the marked canonical CE
extension-type space, construct an authenticated central projection `z` of a
fixed finite packet, finite subgroups `K,H` of one recursively presented base,
and an injection `phi:K->H` such that:

1. on every type in that neighborhood retaining the losing payload,
   `tr(z e_H)-tr(z e_K)>=eta` for a computable `eta>0`;
2. the finite subgroup tables, the authentication of `z`, and stable-letter
   preservation of its packet are ordinary group relations; and
3. the construction is enumerable from rational extension data.

The pair must be payload-coupled.  Native line/whole-subgroup choices in the
relative Pauli `V_4` cannot work by
`v4-pauli-reynolds-edges-have-no-strict-orientation`: strict line-to-line mass
always has an opposite companion, while line-to-`V_4` points in the harmless
order.  A successful surrogate must make the larger abstract subgroup fix
more vectors **on the selected payload type** while retaining the safe
canonical order `1/|H|<=1/|K|` globally.

The filter is mandatory.  For the raw canonical trace one always has
`tau(e_H)=1/|H|<=1/|K|=tau(e_K)`, so no uncompressed subgroup-Hecke edge can
recognize an approximation-specific canonical type.  A positive filtered gap
must be compensated on the other packet blocks; that is exactly where the
actual extension profile enters.

But authentication and stable-letter preservation of a finite packet are
still finite graph-of-groups covariance data.  By
`canonical-types-extend-every-finite-bass-serre-actor`, every canonical
matrix-ultraproduct type extends them exactly.  Hence no such cell can have
the asserted positive filtered gap on a canonical type.  To revive the idea
one must add a payload-sensitive relation among auxiliary intertwiners (a
charged proper-corner/nontransport two-cell).  That is no longer a subgroup-
Hecke separator and common completeness must be proved anew.
