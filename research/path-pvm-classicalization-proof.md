---
rg: 2
id: path-pvm-classicalization-proof
kind: route
title: Direct proof of Path-shaped PVM classicalization has no outcome loss
target: path-pvm-classicalization
requires: []
artifacts:
  - notes/TRUE_PATH_PVMS_CLASSICALIZE_WITHOUT_OUTCOME_LOSS.md
---

# Direct proof of Path-shaped PVM classicalization has no outcome loss

Direct establishment: the complete argument is recorded in the
artifact(s) above (kernel-checked where the artifact is a Lean
module; source-verified prose otherwise). This route asserts the
claim `path-pvm-classicalization` outright — `requires: []` is a proof commitment,
not a placeholder.
