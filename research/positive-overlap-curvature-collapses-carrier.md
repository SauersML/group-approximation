---
rg: 2
id: positive-overlap-curvature-collapses-carrier
kind: claim
title: Positive source-specific overlap and disjoint tag covariance already collapse the forbidden carrier
distinct_from:
  localized-non-ce-leakage-forces-cross-gram-overlap: that supplies the overlap lower bound; this combines it with disjoint-type covariance.
  disjoint-tag-covariance-controls-cross-gram: that supplies the upper bound; this closes the two-sided ledger.
  shared-bcs-eight-slice-full-overlap-actuator: that asks for near-full overlap; this proves any fixed positive surplus is enough.
---

For exit partial isometries `T_1,T_2` with source mass `s`, capacity `c`,
leakage `Lambda`, and disjoint finite-group tag types with covariance energy
`E_tag`,

```text
s-c <= 2 Lambda/beta_B^corner + 4 E_tag/kappa(H,S_H).
```

Hence any forbidden carrier producing a fixed positive surplus `s-c` is
killed by vanishing relation and tag-covariance defects.  Near-full polar
alignment, recursive return, and a full-overlap actuator are unnecessary;
the remaining work is only to realize the three hypotheses on one selected
carrier.
