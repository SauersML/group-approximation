---
rg: 2
id: canonical-btb-tree-local-stationary-seed-proof
kind: route
title: Align every seed-support-preserving edge gauge and induct through the minimal hulls
target: canonical-btb-tree-local-returns-have-stationary-reached-carrier
requires:
  - protected-adaptive-btb-wordization-preserves-canonical-budget
  - btb-three-line-tree-retains-multiplicity-return-gauge
  - finite-packet-graph-atlases-have-a-regular-stationary-flow
---

Install the common regular packet multiplicity vector, tensor it with a
scalar structural assignment having a positive forbidden atom, and take the
full protected seed `e_0` as the root carrier.  Protected wordization makes
`e_0` reducing for the complete carrier menu.  By hypothesis the fixed outer
intertwiner sends the conditioned source to a target cut still occurring in
the `e_0` outer support.  The edgewise Schur theorem then allows the first
multiplicity gauge to preserve the `e_0` reservoir, so its conditioned
return lies below `e_0`.  Therefore the prescribed minimal reducing hull of
the root joined with that return is again exactly `e_0`.

Repeat outward along the finite tree, using the same outer-support hypothesis
at every reached edge.  Since no relator contains two edge intertwiners, the
new gauge choice does not change any earlier edge row.  Regular stationary
restriction multiplicities make every edge covariance table compatible,
while the BTB--E5 cut stays positive.  Induction gives `Q_sigma=e_0` and full
overlap at every reached node, proving the claim.  A complete irreducible
outer action which moves the selected protected-sign type is outside the
hypothesis before any multiplicity gauge is chosen.
