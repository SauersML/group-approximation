---
rg: 2
id: high-density-via-standard-reflection-corner
kind: route
title: Use a standard-type reflection to solve the common-carrier density inequality
target: canonical-high-density-local-sector-groupifier
requires:
  - standard-type-thin-common-reflection-sector
  - standard-reflection-sector-bcs-groupifier
---

Normalize the canonical trace to the nonzero standard-type corner `p`.
Every local sector is the literal same projection `q`, so its meet is `q`.
The established rank computation gives
`1-tau_p(q)=1/(n-1)<Delta/(1+Delta)`.  The groupifier supplies the rows and
the exact marked model.  Apply the common-meet local-sector estimate.

