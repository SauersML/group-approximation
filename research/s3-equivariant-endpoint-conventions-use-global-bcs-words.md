---
rg: 2
id: s3-equivariant-endpoint-conventions-use-global-bcs-words
kind: claim
title: The synchronized S3 reset orbit realizes the diagonal and crossed endpoint conventions through global BCS words
artifacts:
  - research/artifacts/s3-affine-clifford-edge-kernel-audit-2026-08-23.md
  - experiments/s3-affine-clifford-edge-kernel.json
distinct_from:
  regular-induced-affine-reset-synchronizes-s3-holonomy: that removes independent commutant rotations but leaves the transported binary events semantically unlabeled; this must identify them with the actual success and failure conventions.
  full-v4-latent-coordinate-has-an-operator-five-fourths-floor: that names all three failure projections inside one local packet; this must discharge them through the global context incidence without an exact local trace inequality.
  rank-one-no-signaling-pairing-floor: that gives the scalar contradiction after the endpoint laws are supplied; this constructs those laws from ordinary group words.
---

OPEN.  Couple the one-holonomy orbit of
`regular-induced-affine-reset-synchronizes-s3-holonomy` to the original
shared Fanizza BCS words so that, on every forbidden radical fiber, its three
transported binary events are respectively the `00` diagonal success, `01`
crossed-zero success, and `11` diagonal success conventions used in `(RNS1)`.
The resulting marginal drift and convention failure must be bounded by
`O(sqrt(E))` in finite-dimensional normalized-HS tuples.

This coupling cannot be a consequence solely of the finite induced edge or
of any finite graph of packet restriction tables.  The positive regular
stationary representation of that finite graph has forbidden selector mass
and zero edge defect.  Therefore the convention words must consume genuinely
global contextual data: the shared non-CE BCS tuple, a recurrent one-sided
return, or an equivalent matrix-only holonomy two-cell.  They must not yield
an exact trace inequality valid in the left regular representation.

Exact HALT completeness only requires a countably amplified representation
keeping the Fanizza signal nontrivial.  Thus a properly infinite/nontracial
return is allowed on the exact side, while finite matrices must pay the
endpoint mismatch.  The six-copy synchronization has already removed the
independent multiplicity rotations; this claim is now solely the semantic
success/failure attachment.

## Equal-rank bypass

Direct realization of the rank-one marginal laws is no longer the only live
route.  `eight-slice-bookkeeping-symmetrizes-s3-failures` replicates only the
rank-one crossed failure in two private bookkeeping slices.  The three
resulting projections are all genuine failures, are pairwise orthogonal,
have equal rank, and form one finite `S_3` orbit.  Their combined mass is
three-halves of one disjoint parent-capacity slice.  Thus
`shared-bcs-eight-slice-symmetric-return` can bypass the marginal statement
entirely: it need only realize one global finite-matrix capacity return for
the synchronized equal-rank orbit.  The regular-representation firewall is
unchanged and remains the sole non-finite gate.
