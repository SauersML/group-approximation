---
rg: 2
id: every-countable-group-has-an-fp2-shell-envelope
kind: claim
title: Every countable group lies in a finitely generated perfect group with a shell envelope of type FP_2
requires:
  - bhm-singfix-finiteness-holds-for-type-fp-n
distinct_from:
  perfect-decidable-inputs-have-fp-shell-envelopes: that asks for FINITELY PRESENTED shell envelopes of perfect decidable finitely presented inputs, with a computable enumeration, which is the Boone–Higman form; this asks only for type FP_2, for an input and an enumeration that may both be non-computable.
  every-countable-group-lies-in-a-faithful-ha2-actor: that asks for an FP_2 group with a faithful [HA_2] action, whose twisted Brin–Thompson group would be the envelope; this asks for the shell envelope itself to be FP_2, and a shell envelope is already simple for perfect inputs.
---

**OPEN.**

## Statement

For every countable group `H` there are a finitely generated perfect group `P ≥ H` and a
bijection `ν: P → N` such that the shell envelope `E_ν = ⟨V, hat(P)⟩ ≤ Homeo({0,1}^N)` of
`shell-cantor-embeddings-are-finite-germ-extensions` has type `FP_2` over `Z`.

By `perfect-input-shell-envelopes-are-simple`, such an `E_ν` is a simple group of type `FP_2`
containing `H`. This answers Zaremsky Problem 1.25 (route
`every-countable-group-embeds-in-simple-fp2-via-fp2-shells`).

## What must be shown (reduction, proved)

`E_ν` is a finite germ extension of `V` with the single singular orbit `Ω = V·0^∞`, and `V` acts on `Ω^2` with
finitely many orbits. By `bhm-singfix-finiteness-holds-for-type-fp-n` with `n = 2`, `E_ν` has type
`FP_2` as soon as `SingFix_(E_ν)(M,M')` has type `FP_2` for all `M ⊆ M' ⊆ Ω` with `|M'| ≤ 2`.
- The pieces with `M = ∅` are point stabilizers of `V` at rational points, which are `F∞` (BHM Appendix A).
- What remains is the one-point group `A_1 = SingFix({p},{p})` and the two-point groups. `gq-typeA-design` records the latter as reducing to `A_1` in the finitely presented setting; that reduction needs re-checking over `FP_2`.

**Necessary conditions.**
- If `E_ν` is `FP_2` through this route, then `A_1` is `FP_2`.
- Then so is the germ group `Q_ν = A_1/⟨⟨k⟩⟩`, since the kernel `RStab_V(p)` is normally generated by one element (`bhm-singfix-condition-forces-fp-germ-groups`, item 2), and adding finitely many relators preserves `FP_2`.
- Then so is its index-two subgroup `F_ν = R_ν ×_Z R_ν` (`shell-germ-group-has-index-two-fiber-product`).
- Then so is `R_ν = ⟨λ(P), s⟩`, which is a retract of `F_ν` through the diagonal.

## Why this is more flexible than the Boone–Higman shell gate

- `P` need not be finitely presented, recursively presented or decidable. Any finitely generated perfect overgroup of `H` may be used, for example a two-generated simple one (`countable-group-embeds-in-two-generator-simple-group`).
- `ν` need not be computable. The computability gate `shell-recursive-presentations-compute-enumerations` concerns finite presentations only.
- The Röver–Nekrashevych obstruction `rover-nekrashevych-groups-have-co-re-word-problem` does not apply: shell envelopes carry no finite-state data.
- `FP_2` allows the infinitely many far relations of `R_ν` and `F_ν` to be redundant only homologically, as in Leary's and Bestvina–Brady's groups. This is the mechanism that separates `FP_2` from finite presentation.

## Attempts

- **Diagonal germs cannot help (bh-free-08).** By `bhm-corollary-hosts-over-v-are-cover-centralizer-groups`, the cheap clauses of BHM's Corollary 2.10, in homological form too, only certify germ groups commuting with a power of the `V`-germ. For shell germs this recovers the recorded fact that Corollary 2.10 certifies no shell envelope outside virtually cyclic inputs (`research/artifacts/shell-envelope-local-tests-2026-09-13.md`, Step 2). So `A_1` must be treated directly, through Theorem 2.1 (2).
- **Upward direction at one point (bh-free-08, one real attempt, 2026-09-18).**
  - *Proved:* `shell-one-point-groups-are-ascending-hnn-over-window-lifts`. Suppose the germ group has an ascending window `B` in the direction of the `V`-germ (the `ε = +1` case of `shell-germ-fp-is-an-ascending-hnn-gate`). Then `A_1` is the ascending HNN extension, with stable letter `t ∈ V`, of an explicit lift `B̃ = ⟨V_m, lifts of generators of B⟩`. So `A_1` is `FP_2` as soon as `B̃` is.
  - *Calibration, proved:* `ray-germ-one-point-groups-are-v-point-stabilizers`. When the germ group is virtually `Z^k`, translating finitely many rays of blocks, the one-point group is a stabilizer of `k` rational points in `V`, hence `F_∞`. `V` coefficients remove the Houghton loss of finiteness.
  - *Where it stops (heuristic, not proved):* for shells `B̃` appears to contain all of `RStab_V(p)` (remark in that node), which would make it an extension of the window by a non-finitely-generated group, a V-analogue of a lampshuffler. Its finiteness is expected to need a type-(A)-like action of the window, which would already give an envelope through `SV`. Non-effective choices of `ν` could plausibly make the window act on blocks with finitely many pair orbits. For example, Baire-generic windows might be free products acting highly transitively; this is not checked. Finitely generated block stabilizers, which the regular action of `P` works against, were not found.
  - So `FP_2` of `A_1` still needs a base other than `B̃`, or an argument using `t` directly, for example a `Σ^2`-type criterion for `η∘π`.
