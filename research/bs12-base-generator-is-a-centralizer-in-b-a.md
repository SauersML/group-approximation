---
rg: 2
id: bs12-base-generator-is-a-centralizer-in-b-a
kind: claim
title: The cyclic subgroup generated by the base generator of BS(1,2) is a centralizer in a B_A overgroup, so the coset lamplighters over BS(1,2)/<b> and the identity-edge HNN <a, b, t | b^a = b^2, [b, t] = 1> lie in B_A although they are not quasi-linear
distinct_from:
  lamp-wreaths-over-psl2-tree-vertices-lie-in-b-a: that places lamp wreaths over PSL_2(Z[1/2])/PSL_2(Z) in B_A; this pulls that realization back to the base generator of BS(1,2), which does not embed in PSL_2(Z[1/2]), through BS(1,4) and an index-two induction.
  free-group-coset-wreaths-satisfy-pbh: that treats finitely generated subgroups of free groups through normal virtual retracts; here C = <b> is neither a virtual retract nor a centralizer nor a fixed subgroup in BS(1,2).
  pbh-closed-under-decidable-permutational-wreaths: that is the open general closure; this settles a new test case of it, with infinite, non-normal, non-retract stabilizers in a non-free top group.
  char-zero-linear-groups-satisfy-permutational-boone-higman: that covers linear groups; the groups here are not linear over any finite product of fields.
---

**ESTABLISHED** by `bs12-base-generator-centralizer-proof` (lane proof, not reviewed; no
priority claimed). It rests on two lane-proved nodes that are also unreviewed:
`lamp-wreaths-over-psl2-tree-vertices-lie-in-b-a` and
`pbh-coset-wreaths-iff-identity-edge-hnns`.

## Statement

Let `A = BS(1,2) = <a, b | a^-1 b a = b^2>` and `C = <b>`.

1. **Realization.** There are `L_1 ∈ B_A` containing `A` and `m ∈ L_1` with
   `C_A(m) = C`.
2. **Consequences**, by `pbh-coset-wreaths-iff-identity-edge-hnns`. For all `K, Λ ∈ B_A`:
   - the coset lamplighter `Λ wr_(A/C) A` lies in `B_A`;
   - `A *_C (C × K) = (*_(A/C) K) ⋊ A` lies in `B_A`;
   - in particular `E = <a, b, t | a^-1 b a = b^2, [b, t] = 1>` lies in `B_A`, and it
     embeds in a finitely presented simple group.
3. **Not reachable by linearity.** `E` is not linear over any finite product of fields,
   so `quasi-linear-groups-satisfy-permutational-boone-higman` does not reach it. The
   commutator `[t, a b a^-1]` is nontrivial in `E` but dies in every linear image over
   any field.

## Why `C` is not covered by the listed cases

In `A`, `C_A(b)` is the whole base `Z[1/2]`, and no element has centralizer `C`. The
automorphisms of `A` fixing `b` are inner by base elements, with fixed subgroup the
base. No finite-index normal `N` has `N ∩ C` a retract of `N`, because `b^k` is conjugate
in `N` to a proper power of itself. Linear overgroups cannot work either: in any
characteristic-zero linear overgroup the Zariski closure of `<b>` contains `a b a^-1`,
the square root of `b`.

## Lesson for general BH

Centralizer realization transfers **along finite-index subgroups and along embeddings
into groups that already have a realized subgroup**. The pair `(BS(1,2), <b>)` has no
internal witness. It inherits one from the tree lamps of `PSL_2(Z[1/2])`, through
`BS(1,4) <= PSL_2(Z[1/2])`, whose intersection with the vertex stabilizer `PSL_2(Z)` is
exactly `<b>`, and then through Kaloujnine--Krasner induction from index two. The
general move is to embed `(G, C)` in a pair `(G', C')` with `C' ∩ G = C` and `C'` a
vertex stabilizer of a tree or building on which lamps are already known to work. So
coset-wreath closure over "arithmetic" stabilizers propagates to every subgroup pair that
is cut out by them, which is the shape `pbh-closed-under-decidable-permutational-wreaths`
needs.

## Referee (bh-ref-bs12, 2026-09-18): PASS, conditional on two lane nodes

Every step of `bs12-base-generator-centralizer-proof` checks, given
`lamp-wreaths-over-psl2-tree-vertices-lie-in-b-a` and `pbh-coset-wreaths-iff-identity-edge-hnns`, neither
of which has an independent review yet.
- *Step 0.* A normal subgroup meeting the base trivially commutes with it, so it lies in the base.
- *Step 1.* `w^(-1) u w = u^4`, and `b^q a^(2k) ↦ ±[[2^(-k), 2^k q], [0, 2^k]]`. So `ε^(-1)(PSL_2(Z)) = ⟨b⟩`.
- *Step 3.* Coordinates `ε(γ), ε(a^(-1)γa)`, or `ε(δ), ε(aδa)` for `γ = aδ`. Both coordinates must lie
  in `H`, and the parity of the `a`-exponent then gives `C_A(m) = ⟨b⟩`.
- *Step 5.* The eigenvalue argument, and the Zariski-closure argument in characteristic 0, are correct.

**Scope note, relevant downstream.** Item 2 places the **restricted** coset lamplighter `Λ wr_(A/⟨b⟩) A`
in `B_A`. It says nothing about unrestricted products, or about twisted lamplighters such as `M_1`; see
the Referee section of `higman-chain-group-lies-in-b-a`.
