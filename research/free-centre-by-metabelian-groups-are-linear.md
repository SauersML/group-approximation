---
rg: 2
id: free-centre-by-metabelian-groups-are-linear
kind: claim
title: Every free centre-by-metabelian group F_n/[F_n'',F_n] of finite rank is linear over Q(x_1..x_n), residually nilpotent and residually finite
distinct_from:
  free-centre-by-metabelian-corner-kernel-is-the-torsion: that is the corner representation, whose kernel is the finite torsion subgroup T_n; this adds a nilpotent quotient that is injective on T_n, so the product is faithful.
  free-centre-by-metabelian-groups-linear-iff-residually-finite: that reduced linearity for n >= 4 to residual finiteness; this proves both.
  free-centre-by-metabelian-group-of-rank-two-is-linear: that is rank 2 alone, with a 4x4 representation; this is every rank.
---

**ESTABLISHED** by `free-centre-by-metabelian-linearity-via-gamma-seven-quotient`
(lane proof, not independently reviewed).

**Priority. The theorem is known (1976); this node gives a new proof.** The source is N. D. Gupta and
B. A. F. Wehrfritz, "Some residual properties of certain nilpotent-by-Abelian groups", Arch. Math. 27
(1976) 449–455 (doi:10.1007/BF01224698). bh-verify-cbm read it at source on 2026-09-18 (PDF via MSI).
- **p. 454, Remark 3:** "Every free centre-by-metabelian group of finite rank has a faithful
  representation of finite degree (depending on the rank) over the ring R_0". That is linearity in
  characteristic 0, for every finite rank. p. 450 says the same in words.
- **p. 454, facts (ii) and (iii), credited to C. K. Gupta (1969, 1973):**
  - the torsion `[F'', F]N/[F'', F]` is elementary abelian of rank `C(r,4)`, so `d = 2` here;
  - `F/[F'', F]` is residually a finite 2-group, so it is residually nilpotent and residually finite.
- **New here:** the explicit `(2n+1)`-dimensional corner representation, the exact homological
  computation of its kernel as `T_n`, and the `γ_c` patch.
- **Also read, not relevant:** Gupta–Levin, Comm. Algebra 4 (1976) 249–270, which is about separating laws
  for nilpotent quotients. Kofinas, arXiv:2603.27648 §2.3, records only that Hurley's embedding is not
  faithful for `n >= 4`.

## Statement

Let `C_n = F_n/[F_n'', F_n]` for `n >= 1`. Then:
1. `C_n ↪ ρ_n(C_n) × C_n/γ_7(C_n)`, where `ρ_n` is the `(2n+1)`-dimensional corner
   representation over `Z[x_i^(±1)]` of
   `free-centre-by-metabelian-corner-kernel-is-the-torsion`. So `C_n` embeds in
   `GL_N(Q(x_1, …, x_n))` for some `N`.
2. `T_n ∩ γ_7(C_n) = 1`. So the torsion survives in the class-6 quotient, and `C_n` is
   residually nilpotent.
3. `C_n` is residually finite, and it is in `B_A` (`free-centre-by-metabelian-groups-satisfy-boone-higman`).

For `n <= 3` the second factor in item 1 is unnecessary.

Item 3 has a variety-theoretic reading. Relatively free groups of every finite rank are
residually finite, so the variety of centre-by-metabelian groups is generated by its
finite members, indeed by its finite nilpotent members.
