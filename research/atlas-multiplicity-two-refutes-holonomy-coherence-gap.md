---
rg: 2
id: atlas-multiplicity-two-refutes-holonomy-coherence-gap
kind: claim
title: The multiplicity-two Atlas frame refutes the raw-S3 holonomy ceiling
artifacts:
  - experiments/atlas_a4_repeated_module_escape_verify.py
distinct_from:
  atlas-m2-escape-kills-collision-only-scalars: that evaluates the hub and four-cycle scalar ceilings; this evaluates the two raw S3 covariance coordinates occurring in the holonomy-coherence claim itself.
  atlas-multiplicity-two-packet-collision-hub-escape: that constructs the common-frame packet/collision witness and records only its t23 hub opcode; this computes its t21 and t12 opcodes and checks every normalization hypothesis of the proposed holonomy ceiling.
---

**ESTABLISHED COUNTEREXAMPLE.**  Let `U` be the exact `8 by 8` binary frame
from `atlas-multiplicity-two-packet-collision-hub-escape`.  In the repeated
natural representation of `A8=GL_4(F_2)`, direct multiplication gives

```text
(U^(-1)rho(t21)U)rho(t21) !=1,       order=4,
(U^(-1)rho(t12)U)rho(t12) !=1,       order=4.          (M2H1)
```

Let `G<=GL_8(F_2)` be generated by the first chart and `U`, and pass to
`Reg(G)`.  Use `lambda(U^(-1))` as the relative chart frame.  Each covariance
opcode in `(M2H1)` is then the regular unitary of a nonidentity element, so

```text
a^2=2,                 b^2=2,
a^2+b^2=4.                                             (M2H2)
```

This regularized witness satisfies every hypothesis of
`atlas-a4-holonomy-coherence-gap` as a constant sequence.  Both A8 chart
vertices are exact multiples of `Reg(A8)`.  For each of the thirty context
vertices, the verified mixed order-two/order-three pair generates an exact
copy of `A4`; restriction of `Reg(G)` to it is a multiple of `Reg(A4)`.
Choose the context representation to be these literal regular operators.
The two cyclic edge embeddings then agree literally with their chart
operators, so every graph stable letter may be chosen equal to the identity.
Finally all packet contexts are exact and `q_19243=1` by the same verifier.

Thus the normalized dimensions have the required form

```text
dim Reg(G)=20160 k,
Reg(G)|_(A4)=1680 k Reg(A4),
```

all non-tree stable letters have zero distance from the identity, and the
collision defect is zero, while `(M2H2)` contradicts the asserted strict
ceiling

```text
limsup (a_n^2+b_n^2)<1/32.
```

The failure is finite-dimensional; it is not a non-Connes-embeddable quotient
trace escape.  Therefore every route to nonhyperlinearity which requires the
stated A4 holonomy-coherence gap is dead.  Adding `q14` excludes this specific
frame, but is a different augmented claim and does not repair the statement
being refuted here.
