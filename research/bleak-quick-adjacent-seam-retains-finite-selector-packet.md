---
rg: 2
id: bleak-quick-adjacent-seam-retains-finite-selector-packet
kind: claim
title: An adjacent Bleak--Quick seam retains one jointly named finite S6 selector packet
distinct_from:
  bleak-quick-a6-seam-exposes-shared-label-arm-anchor: that asks for a common endpoint cancellation certificate; this permits contextual arms to remain different and instead asks one fixed S4 selector family to detect their relative label inside a jointly rounded S6 packet.
  thompson-v-r5-coherent-alternating-packet: that asks for coherent multiplication tables of unbounded symmetric groups; this uses only the fixed group S6 separately at each adjacent seam.
  finite-mark-centralizer-commutator-area-is-proper: that obstructs a family of individual centralizer loops escaping to unbounded address distance; every word and selector here is obtained by one common conjugation of a fixed depth-three six-letter diagram.
  shared-s5-selector-face-has-multiplicity-rotation-gauge: that exact model shows a common selector S4 or even common S5 face does not automatically glue two S6 extensions; this requires an additional Bleak--Quick occurrence relation which kills that finite multiplicity rotation.
---

For every pair of adjacent contextual Carmichael A5 charts in a
`delta`-model of the Bleak--Quick presentation, and every shared label `i`,
construct in the original matrix dimension:

* contextual arms `S,S'` and a common pivot `R`, with the two shared
  Carmichael matrices `O(delta)`-close to `S S^R` and `S' (S')^R`;
* one map `Phi:S_6->U(d)` of all-pairs multiplicative defect `O(delta)`;
* labels `x,x'` in the five possible common-pivot arm positions such that
  `S` is `O(delta)`-close to `Phi((x p))` and `S'` is `O(delta)`-close to
  `Phi((x' p))`; and
* the same three selector matrices

  ```text
  Phi((j k)), Phi((k l)), Phi((l q))
  ```

  for which every commutator with both `S` and `S'` is `O(delta)`-close to
  the identity.                                                     (BSP1)

All word comparisons must use a bounded number of conjugates of R1--R5,
independent of the addresses, matrix dimension, and packet size.

This is a finite occurrence-retention claim, not an unbounded finite-group
compiler.  Gowers--Hatami exactification of `Phi`, followed by
`finite-s6-selector-centralizer-pins-common-pivot-arm`, forces both arms to
the `i` position with `O(delta)` error and hence synchronizes the two cycles.

## Attempts

The literal depth-three packet has exactly the required selector group:
`finite-s6-selector-centralizer-pins-common-pivot-arm` takes the complement
`S_4`, and each of its three disjointness relations is a conjugate R5 cell.
`depth-three-r5-selector-pays-a6-label-flip` verifies exact payment against
the known swap gauge.

The unresolved step is occurrence retention.  Conjugating a whole seam to
depth three names one selector packet, but the two contextual chart
conjugators may carry their shared arm into different copies of that packet.
Using separately transported selectors merely reinstates the private gauge.
Unlike the refuted common-frame route, no selector or correction here escapes
a fixed S6 packet; therefore `finite-mark-centralizer-commutator-area-is-proper`
does not rule this out.  A proof must construct one interacting bounded
diagram which keeps the selector words common while attaching both contextual
arms.

Even the entire common S5 face is not enough by itself.
`shared-s5-selector-face-has-multiplicity-rotation-gauge` rotates the two
trivial summands in the restricted six-point permutation representation,
fixing that whole face and all selector commutators while changing the
missing arm.  Hence the required interacting diagram must couple the two
extension multiplicity spaces, not merely enlarge the subgroup on which the
charts agree.

The finite coupling relation is now identified exactly.
`mixed-r2-cell-couples-shared-s5-extension-multiplicities` shows that one R2
triangle using the input arm from the first S6 extension and the transported
arm from the second has defect **equal** to the missing-arm mismatch.  In the
normalized depth-three words it is the ordinary R2 shape

```text
(010 001) (010 000) (010 001)=(000 001).
```

This also detects the explicit S5 multiplicity rotation, whereas every
chart-separable relation is blind to it.  The remaining word-geometric leaf
is `bleak-quick-adjacent-seam-retains-one-mixed-r2-cell`: certify that this
one triangle can mix the two contextual occurrences with bounded R1--R5
cost.  `mixed-r2-occurrence-builds-finite-selector-packet` then recovers the
whole selector packet from either private S6 chart.
