---
rg: 2
id: jacobson-coherence-defect-is-an-orthogonal-translation
kind: claim
title: The missing Jacobson root coherence word is conjugate to one explicit translation-root commutator
distinct_from:
  jacobson-shift-literal-quotient-is-laurent-times-z: that identifies the whole literal quotient and compares the abstract kernel; this isolates one exact word in that kernel and the additional finite root packet its vanishing would complete.
  jacobson-positive-root-gluing-misses-laurent-coherence: that shows a finite model when opposite-root gluing is omitted; this keeps the actual full common subgroup and establishes identities without deciding the word's triviality.
artifacts:
  - research/artifacts/jacobson-missing-laurent-coherence-word-and-finite-cell-countermodel-2026-09-08.md
---

In `Lambda` use `c=x_13`, `k=x_31`, `q=w_23`, the second
packet's Weyl element `h`, and

```text
u=[h c h,k],  v=h u h,  r=x_23,  d=h r h,
e=(q v q)d,
s=c k c,
t_0=s q s h q s h s,
B_0=h s q s.
```

Then the following identities hold:

```text
B_0^(-1) e B_0=[t_0,c],
[t_0,s]=1,
kappa(t_0)=diag(z^(-1),z^2,z^(-1)).
```

In the full group `Gamma=Theta_shift`, put

```text
b=w_23,Q w_23,P,   zeta=x_23,Q x_23,P,
u=[h x_13,P h,x_31,P],   v=h u h,
E=b v b h zeta h,
e_P=w_23,P v w_23,P h x_23,P h,
v_0=[h x_21,Q h,x_12,Q].
```

The authenticated finite boundary table gives
`v_0=I+e_(1,1) epsilon_(2,0)` and the exact equality
`E=e_P v_0^(-1)`. Moreover

```text
E in ker(pi),  [E,L]=1,  [E,t]=1,
E=1 in Gamma iff e=1 in Lambda.
```

The equation `e=1` is also exactly what completes the specified
third `GL_3(F_2)` root packet generated by `<h,u>` and `<q,r>`.
Neither `e=1` nor `e!=1` is proved in the actual quotient.
These identities do not prove a matrix gap or identify the full
abstract Laurent cover.

DERIVATION
jacobson-finite-weyl-and-boundary-splitting-proof
