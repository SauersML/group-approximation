---
rg: 2
id: mf-radical-contains-hilbert-hotel-elementary-subgroup
kind: claim
title: Over a torsion ring the MF radical of the elementary group contains the relative elementary subgroup of the Hilbert-hotel radical
distinct_from:
  finite-additive-order-one-sided-defects-are-mf-invisible: that covers the ideal generated by one defect of the ring itself; this closes that construction transfinitely and reaches the smallest ideal with directly finite quotient.
  full-complementary-idempotent-elementary-full-mf-radical: that assumes the defect ideal is the unit ideal and concludes a full radical; this assumes nothing about fullness and returns the ring-defined lower bound.
  mf-radical-contains-defect-ideal-elementary-subgroup: that is the proved first stage d(R); this is the transfinite closure hh(R), conditional on the relative step.
artifacts:
  - research/artifacts/un-open-1-hilbert-hotel-radical-2026-09-13.md
---

**CONDITIONAL / OPEN** (demoted 2026-09-13 after review by `un-verify-open`). The route
`mf-radical-contains-hilbert-hotel-elementary-proof` proves it from the open
`relative-torsion-defect-mf-radical-step`, so it does not fire.

Let `R` be a countable unital ring whose additive group is torsion and let `n >= 4`. Then, conditionally,

```text
EL_n(R, hh(R)) <= Rad_MF(EL_n(R)),
```

where `hh(R)` is the Hilbert-hotel radical, the smallest ideal with directly finite quotient.

**What is unconditional.** The first stage, `EL_n(R, d(R)) <= Rad_MF(EL_n(R))` with `d(R)` the ideal
generated by all defects, is `mf-radical-contains-defect-ideal-elementary-subgroup`. So this claim holds
unconditionally whenever `hh(R) = d(R)`, which is `hilbert-hotel-radical-is-reached-in-one-step`, and in
particular in every calibrating ring known here.

**Review (un-verify, 2026-09-13): GAP.** The successor step invokes `relative-torsion-defect-mf-radical-step`, which has a gap (review line there). Stage 0 and the limit step are correct. See `research/artifacts/un-review-2026-09-13-part8.md` §2.
