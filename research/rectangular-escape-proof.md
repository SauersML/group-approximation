---
rg: 2
id: rectangular-escape-proof
kind: route
title: Direct proof of Equivariant whole maps admit rectangular escape
target: rectangular-escape
requires: []
artifacts:
  - notes/FALSE_EQUIVARIANT_WHOLE_MAPS_ALLOW_RECTANGULAR_ESCAPE.md
---

# Direct proof of Equivariant whole maps admit rectangular escape

Direct establishment: the complete argument is recorded in the
artifact(s) above (kernel-checked where the artifact is a Lean
module; source-verified prose otherwise). This route asserts the
claim `rectangular-escape` outright — `requires: []` is a proof commitment,
not a placeholder.
