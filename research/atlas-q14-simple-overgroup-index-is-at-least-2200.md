---
rg: 2
id: atlas-q14-simple-overgroup-index-is-at-least-2200
kind: claim
title: Every augmented finite-simple Atlas escape has A8 index at least two thousand two hundred
distinct_from:
  atlas-q14-simple-overgroup-index-is-at-least-991: that stops after A11; this removes the next complete simple-order interval and the spurious L3(9) divisibility candidate.
  atlas-a8-index-991-to-2200-reaches-hs-s8-derived: that classifies the ambient equality family without deciding its packet equations; this records the resulting lower bound and precise frontier.
---

Let `S` be finite simple and let two marked `A8` chart subgroups generate `S`
while satisfying the packet, `q_19243`, and q14.  Then

```text
[S:A8] >= 2200,       |S| >= 44352000.              (OSI2200)
```

If equality holds, then `S~=HS`; both charts are derived subgroups of maximal
`S8` subgroups in the unique ATLAS class.  All marking automorphisms are
absorbed by those `S8` normalizers.  The next finite problem is therefore one
exact `HS` conjugator/double-coset screen for packet, collision 19243, and
q14, rather than separate inner and graph parity screens.

The established `atlas-hs-equality-escape-must-cross-s8-maximals` reduction
also removes the identity normalizer cell: any surviving equality position
must cross two distinct maximal `S8` subgroups.

This claim does not assert that the `HS` equality family survives.
