---
rg: 2
id: diagonal-shell-action-has-off-diagonal-orbital-kernel
kind: route
title: Put the two shell charts on independent coordinates of the diagonal coset action
target: two-equivariant-shell-pvms-have-independent-coordinate-escape
requires:
  - native-sl3-edge-parahoric-has-index-forty-two
  - five-parahoric-overlap-saturations-give-global-shell-pvm
---

The coordinate PVMs `(TSE2)` are systems of imprimitivity for the same
diagonal permutation representation, proving exact covariance and zero
shell Dirichlet energy.  Their intersections are the one-dimensional
coordinate cells `{(i,i)}` inside a space of size `42^2`, which gives
`(TSE4)--(TSE5)` directly.

All defining relations of `C` hold because `(TSE1)` is an exact group
representation.  Tensoring with a deep congruence regular representation
forces the normalized trace of every prescribed nonidentity lattice word to
zero and does not change any projection trace, covariance norm, or Gram
deficit.  This proves the canonical-character strengthening.
