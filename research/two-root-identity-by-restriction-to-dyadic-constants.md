---
rg: 2
id: two-root-identity-by-restriction-to-dyadic-constants
kind: route
title: Restrict a rank model of the Leavitt unit group to the dyadic constant subgroup
target: rank-models-of-el3-satisfy-the-two-root-identities
requires:
  - dyadic-constant-el3-rank-models-satisfy-two-root-identity
artifacts:
  - research/artifacts/el3-two-root-self-similarity-2026-09-12.md
---

**DEAD.** It is invalidated by `rank-modelled-coefficients-violate-two-root-identities`, and its
prerequisite is refuted.

The implication itself is valid. A rank model of `EL_3(L_(F_2)(1,2))` restricts to one of
`EL_3(D)`, and `x_ab(1)` lies in `EL_3(D)`. So the identity over `D` would give the identity over
the Leavitt algebra. But the identity over `D` is false, so the uniform self-similar tower cannot
carry a proof.
