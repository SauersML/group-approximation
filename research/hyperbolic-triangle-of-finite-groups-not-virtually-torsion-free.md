---
rg: 2
id: hyperbolic-triangle-of-finite-groups-not-virtually-torsion-free
kind: claim
title: A hyperbolic triangle of finite groups that embeds in no finite group (the hyperbolic Neumann–Neumann problem)
distinct_from:
  hyperbolic-simple-triangle-group-without-finite-quotients: that asks for simple vertex groups and no finite quotient at all; this allows any vertex groups generated by their edge groups and asks only that no finite quotient be injective on the three vertex groups, and the two are equivalent by simple-vertex-triangles-are-universal-for-triangle-vtf
  non-virtually-torsion-free-hyperbolic-group: that is the unrestricted counterexample to Lück's question; this restricts it to colimits of hyperbolic triangles of finite groups, where the torsion test is one finite quotient injective on three vertex groups
---

Exhibit a triangle of finite groups with:
- trivial face group and nontrivial edge groups;
- every vertex group generated by its two edge groups;
- half-girths `m_v` of the coset graphs with `Σ_v 1/m_v < 1`;
such that no homomorphism from its colimit `G` to a finite group is injective on all
three vertex groups.

By Caprace–Conder–Kaluba–Witzel, arXiv:2011.09276, Theorem 3.1 (i), (ii), (iv), (v) (read
at source 2026-09-13), `G` is then infinite, non-elementary hyperbolic and not virtually
torsion-free. Equivalently, the triangle embeds in no finite group, meaning there is no
finite group containing the vertex groups with the given edge identifications and
pairwise intersections.

**OPEN.** This is the hyperbolic case of the embeddability problem of B. H. Neumann and
H. Neumann (Proc. London Math. Soc. (3) 3 (1953), 243–256). Wise, *The residual
finiteness of negatively curved polygons of finite groups*, Invent. Math. 149 (2002),
p. 607 (read at source), says: "the embeddability problem is equivalent to the problem
of whether π_1P is virtually torsion-free", and "it seems likely that there exist
nonpositively curved polygons of finite groups that are not virtually torsion-free, but
no examples are currently known."

Literature check, bounded: Wise 2002 and Caprace–Conder–Kaluba–Witzel were read, and the
repo graph was searched. Web search was unavailable this session, so no search after
2020 was made.

## Attempts

- **More sides are residually finite (Wise).** Wise's Theorem 9.1 takes an `n`-gon of
  finite groups with every angle `<= π/2`. It is residually finite, and by his Theorem
  9.3 virtually torsion-free, when:
  - `n = 4` with two acute angles;
  - `n = 5` with one acute angle;
  - `n >= 6`.

  The proof splits `π_1 P` along a segment `J` whose endpoints lie in two disjoint edges
  and which has corners of `P` on both sides. The splitting is an amalgam of two
  virtually free groups over a finitely generated almost malnormal subgroup, and
  Theorem 1.3 of that paper applies.

  In a triangle, any such `J` leaves a single vertex `v` on one side. That side is the
  finite group `A_v`. The would-be amalgamated subgroup `E_e * E_e'` does not inject
  into `A_v`: it maps onto `A_v` with a free kernel of finite index. So `G` is the
  quotient `G_0/<<N>>` of the virtually free group `G_0` by that kernel
  (`triangle-colimit-llp-reduces-to-relator-local-splitting`, (TC2)), not an amalgam,
  and the separability argument has no foothold. Triangles are exactly the polygons
  Wise's method cannot reach.

- **Simple vertex groups.** `simple-vertex-triangles-are-universal-for-triangle-vtf`
  shows this claim is equivalent to
  `hyperbolic-simple-triangle-group-without-finite-quotients`. There, having one
  nontrivial finite quotient is the whole test. Explicit `(A_7, A_7, A_8)` candidates and
  the Caprace–Conder–Kaluba–Witzel search data are recorded in that node.

- **Order-2 edge groups never work.** By (U6) of the same node, if all three edge
  groups have order 2 the core is a Coxeter group. It is linear, so the triangle embeds
  in a finite group whatever the curvature.

- **Tool audit (lane ex-hyperbolic-triangle-theory, 2026-09-13).**
  - *Counting over random identifications.* `convergent-quotient-count-decides-gromov-question`:
    for any finite family of gluings, merely proving that the expected number of finite
    simple quotients is finite already yields a quotientless hyperbolic Kazhdan group.
    Under "every hyperbolic group is residually finite" that count is infinite for every
    member. So the heuristic `|S|^(-χ_orb)` cannot be turned into a proof by soft
    means.
  - *Bounded degree is computable.* `linear-quotient-exclusion-lifts-from-char-zero`:
    for each `d`, one characteristic-0 Nullstellensatz certificate with its integer `N`,
    plus Groebner bases over `F_p` for the primes dividing `N`, decides every quotient
    with a projective module of dimension `<= d`, in every characteristic. What remains
    is the unbounded part: large alternating groups and classical groups of unbounded
    dimension.
  - *Property (T) does not obstruct quotients.* Caprace–Conder–Kaluba–Witzel Theorems
    1.3–1.4 (read at source): the KMS groups `G_{HB_2^(2)}(p)` are infinite hyperbolic,
    have (T) for `p >= 7`, and have finite simple quotients of arbitrarily large rank.
    Also, amalgamating into simple vertex groups destroys (T)
    (`simple-vertex-triangles-are-universal-for-triangle-vtf`).
  - *Normal subgroup theorems cannot apply.* A non-elementary hyperbolic group has
    infinite normal subgroups of infinite index. For example, a deep Dehn filling
    `G/<<g^m>>` of a loxodromic element is again infinite hyperbolic
    (`hyperbolic-group-relative-to-maximal-elementary-subgroup` followed by
    `relatively-hyperbolic-dehn-filling`). Margulis- or Bader–Shalom-type theorems need irreducible lattices in
    products, which contain `Z^2` and are not hyperbolic.
  - *Deep fillings are not automatically special.* Lubotzky–Manning–Wilton,
    arXiv:1702.08200v2, Corollary 1.7 (read at source): all sufficiently long
    peripherally finite fillings need not be virtually special, and their counterexamples
    are Kazhdan `k`-fold triangle groups. For simple vertex groups Wise's MSQT covers
    almost nothing. It certifies fillings `N <= Ṗ` for a finite-index `Ṗ < E`, and
    `N <= Ṗ` forces `N <= core_E(Ṗ)`. If `E/N` is simple then `core_E(Ṗ) = N` unless
    `Ṗ = E`, so for a given `Ṗ != E` at most one filling with simple quotient is
    covered.
  - *Representation varieties.* All quotients Caprace–Conder–Kaluba–Witzel found for
    their harder groups came from representations with infinite image (`U(3,1)`,
    `U(6)`, `PGL_3(Z[ω])`) followed by congruence images. Their Question 5.17, a
    Groebner-basis form over quotients of `PSL_2(Z)`, is formally equivalent to Gromov's
    question (their Remark 5.18). By Malcev, a simple-vertex triangle is quotientless iff
    every finite-dimensional representation over every field is trivial.
- **Next targets.**
  1. Run the bounded-degree exclusion on the explicit `(A_7, A_7, A_8)` candidates in
     degrees 4–8, and on the cores of the unresolved Caprace–Conder–Kaluba–Witzel
     trivalent groups, keeping integer certificates.
  2. Build a Kazhdan member with simple vertex groups and connected links. A natural
     source would be arithmetic `C_k * C_k` lattices in `PGL_2` over a local field, with
     `k = N(𝔭) + 1` and cyclic vertex stabilizers from CM tori of definite quaternion
     algebras. Their congruence quotients `PSL_2(F_q)` would have Ramanujan coset graphs
     of unbounded girth, giving (T) through Ershov–Jaikin-Zapirain once `k >= 17`. This is
     not constructed, and existence of such lattices is unchecked.
- **Where it dies.** Showing that no vertex-injective finite quotient exists means
  excluding every finite overgroup of the three vertex groups with the prescribed
  intersections. That is the large-quotient barrier recorded in
  `hyperbolic-simple-triangle-group-without-finite-quotients`.
