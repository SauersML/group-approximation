---
rg: 2
id: stw99-l-rotation-oinfty-scaled-k-theory-citation
kind: route
title: Import Pimsner-Voiculescu for the rotation algebra and O-infinity absorption
target: stw99-l-rotation-oinfty-scaled-k-theory
requires: []
---

**Established by citation.**  Pimsner--Voiculescu computes
`K_0(A_theta) = K_1(A_theta) = Z^2` with `[1]` primitive and `[U], [V]`
a `K_1` basis, the connecting maps vanishing because the rotation
automorphism of `C(T)` is homotopic to the identity.  `A_theta` is
unital, separable, nuclear, simple and satisfies the UCT, so
`A_theta (x) O_infinity` is a unital UCT Kirchberg algebra, and
`K_*(O_infinity) = (Z, 0)` with `[1] |-> 1` makes the external product
`K_*(A_theta) -> K_*(A_theta (x) O_infinity)` an isomorphism preserving
the unit class and the named generators.

NOT VERIFIED AGAINST THE SOURCES IN THIS SESSION.
