---
rg: 2
id: one-variable-laurent-models
kind: route
title: One-variable Laurent fiber models
target: hyperlinear-wreath-model
requires: []
---

Model the compression inside `M = SL_r(𝔽_q[t,t⁻¹])`. Dead: `K₀` is compact
open, Haar is conjugation-invariant, so `Γ₀ = M ∩ K₀` has no strict
compressor — the one-variable model does not contain the phenomenon
(one-variable-no-compressor).
