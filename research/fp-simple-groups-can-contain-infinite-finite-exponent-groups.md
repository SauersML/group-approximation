---
rg: 2
id: fp-simple-groups-can-contain-infinite-finite-exponent-groups
kind: claim
title: Duplicate of some-fp-simple-group-has-an-infinite-bounded-exponent-subgroup (some finitely presented simple group contains an infinite finitely generated group of finite exponent)
distinct_from:
  some-fp-simple-group-has-an-infinite-bounded-exponent-subgroup: the same statement, landed 26 seconds earlier by lane bh-free-06 with the separator framing; that node is canonical and this one is kept only so that existing references resolve.
  free-burnside-groups-satisfy-boone-higman: that asks for embeddings of the free Burnside groups themselves; this asks only for some infinite finitely generated subgroup of finite exponent in some finitely presented simple group, which that claim implies.
artifacts:
  - research/artifacts/gq-bh-bh-lit-survey.md
---

**OPEN — DUPLICATE.** This is the same statement as `some-fp-simple-group-has-an-infinite-bounded-exponent-subgroup`
(bh-free-06, 05463330a, landed 09:10:25), which is canonical. This node (bh-free-12, d23442a62, landed 09:10:51) was
opened independently. It is kept only as a pointer, because other nodes cite its id. Add new work to the canonical
node, not here.

**Statement.** There exist a finitely presented simple group `S`, an infinite finitely generated subgroup `E ≤ S` and
an integer `N >= 1` with `e^N = 1` for all `e ∈ E`.

**What this node contributed.** Everything is merged into the canonical node's exclusion list and Attempts:
- the almost automorphism exclusion `almost-automorphism-torsion-subgroups-are-rf`, which covers `V`, all `V_{d,r}`,
  all Röver–Nekrashevych groups `V_d(G)`, Matui's one-sided SFT groups and Exel–Pardo 1-graph groups;
- the 2-dimensional CAT(0) product exclusion `two-dim-cat0-product-groups-are-torsion-locally-finite`, which covers
  the finitely presented simple Kac–Moody lattices with 2-dimensional buildings.
