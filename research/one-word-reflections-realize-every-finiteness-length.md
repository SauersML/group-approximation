---
rg: 2
id: one-word-reflections-realize-every-finiteness-length
kind: claim
title: Singly generated internal MF radicals realize every finite homological finiteness length
root: true
artifacts:
  - research/one-word-reflections-realize-every-finiteness-length-proof.md
---

For every integer `n>=2`, the binary Leavitt unit group `U` contains a
finitely generated non-MF subgroup `K_n` with a split quotient

```text
pi_n:K_n->>T_n                                          (FL1)
```

such that

```text
Rad_MF(K_n)=ker(pi_n)=normalClosure_(K_n)(d_n),          (FL2)
T_n is of type F_(n-1) but not FP_n.                    (FL3)
```

For every normal subgroup `N` of `K_n`,

```text
cl_MF^(K_n)(N)=pi_n^(-1)(cl_MF^(T_n)(pi_n(N))).          (FL4)
```

The kernel in `(FL2)` is also the finite residual, compact residual,
finite-dimensional unitary residual, and linear residual over every field.
Consequently

```text
profiniteCompletion(K_n)~=profiniteCompletion(T_n),
Bohr(K_n)~=Bohr(T_n).                                   (FL5)
```

The visible groups `T_n` are residually finite, linear in characteristic
zero, non-virtually-free, and have non-context-free word problem.  In
contrast, `K_n` is not residually finite, not maximally almost periodic, and
not linear over any field, because the common residual in `(FL2)` is
nontrivial.  Nevertheless, dimension by dimension and field by field, every
finite-dimensional representation of `K_n` factors uniquely through the
linear group `T_n`.  The groups `K_n` are pairwise nonisomorphic.  Thus every
finite homological and homotopical finiteness length occurs in a completely visible quotient
while the entire invisible kernel remains normally generated by one
involution.
