---
rg: 2
id: atlas-q14-simple-overgroup-index-is-at-least-91
kind: claim
title: Every augmented finite-simple Atlas escape has A8 index at least ninety-one
distinct_from:
  atlas-q14-simple-overgroup-index-is-at-least-33: that ends after excluding the prime-power index thirty-two; this removes the complete remaining simple-order interval through the natural A10 boundary.
  atlas-a4-sp6-packet-collision-screen: that excludes one concrete Sp6(2) carrier; this combines its exhaustive screen with an ambient classification proving it is the only carrier below A10.
---

Let `S` be finite simple and let

```text
A=i_1(A8),  B=i_2(A8),  S=<A,B>
```

solve the twelve packet relations, `q_19243`, and q14.  Then

```text
[S:A]=[S:B] >= 91,       |S| >= 91 |A8| = 1834560.  (OSI91)
```

The only possible ambient group from index 33 through 89 is `Sp_6(2)`, and
all of its `A8` subgroups lie in the orthogonal class whose packet-plus-
collision fiber is empty.  At index 90 the ambient group is `A10`; every
embedded `A8` has natural orbit shape `8+1+1`, and the packet already forces
the two natural supports to coincide before collision excludes the relative
marking.  Thus neither q14 nor a new marked computation is needed in this
interval.

The next classification step, `atlas-q14-simple-overgroup-index-is-at-least-496`,
removes the entire interval through 495 and isolates the mixed natural/dual
labeling of the standard `A8<L5(2)` class at index 496.
