---
rg: 2
id: predicate-square-root-dresses-reynolds-section
kind: claim
title: Make one predicate-square-root word preserve and dress the selected Reynolds source
distinct_from:
  same-range-dressed-reynolds-sections-expose-raw-source-unitary: That proves the raw phase and common-range formulas after such a word exists; this must couple the word to the shared BCS and Schur return incidence.
  fresh-central-square-lift-is-extension-trivial: That allows a freely gauged square root unrelated to the payload return; this requires the two actual return paths to be the same Reynolds section dressed by the square-root word.
  central-sign-reynolds-first-hit-compiler: That assembles successive exit layers; this is one local phase-sensitive return cell.
---

**ESTABLISHED.**  Use two central signs: the Schur/BCS payload sign `K` and
an auxiliary companion sign `J`.  For a forbidden assignment `a`,
`two-sign-reynolds-companion-splits-one-assignment-atom` constructs the
Reynolds source

```text
p=A_a(1+J)/2
```

and a source-full section `up` whose unused range has trace
`tau(A_a)/2`.  The flip transvection `J->KJ` makes the source and exit
balanced while leaving the complete payload assignment fixed.

On the same assignment, `schur-flip-square-exposes-payload-root-word`
constructs the ordinary word

```text
d=P_a Q_a u_P u_Q,
```

from the two predicate-packet flip implementers.  It preserves `p` and the
canonical matrix microstates satisfy

```text
[d,p]=o_HS(1),                  p d^2 p=Kp+o_HS(1).     (PSR1)
```

Take the two return paths on that source to be

```text
v_0=up+o_HS(1),                 v_1=udp+o_HS(1),       (PSR2)
```

for the same index-two Reynolds section `up`.  The error must be controlled
by a dimension-independent constant times the presentation defect.

They have the same final support.  Their relative source corner is the raw
unitary `pdp`, whose square is `Kp`.  On a forbidden packet simple the packet
factors in `d` cancel and `pdp` is exactly the external multiplicity
holonomy which cancels the Schur projective cocycle.  It is therefore tied
to the actual payload return and is not a fresh square-root gauge.

By `same-range-dressed-reynolds-sections-expose-raw-source-unitary`, these
conditions recover the literal raw lift `W=pdp` with `W^2=Kp` while leaving
the assignment-atom exit untouched.  This is the sole remaining input in
the Reynolds proper-section phase compiler.

All packet and balancing cells before the HNN edge are fixed finite groups.
Their normalized-HS exactification, together with the subgroup-Hecke polar
correction, bounds the total loss in `(PSR1)--(PSR2)` by a
dimension-independent constant times the square root of presentation
energy.  The finite base embeds in the HNN extension, so its representations
with `K=-1` give exact infinite models with nontrivial payload sign.

There is a smaller source of the same **corner square**:
`selector-product-is-a-payload-corner-square-root` gives
`d_a=p_aq_a` and `P_aQ_-d_a^2=JP_aQ_-` directly from the one-bit selector.
It does not replace `(PSR2)`.  Without a relation identifying its compression
with the two actual Schur return paths, it tensors independently with the
stationary equal-child model.  Thus the Schur-flip construction above remains
the established return-incidence implementation, while the selector product
is a useful minimal falsification/alternative input.
