---
rg: 2
id: atlas-m24-marked-packet-collision-exclusion
kind: claim
title: The augmented Atlas packet has no generating marked A8 pair in M24
distinct_from:
  atlas-a4-m23-packet-double-coset-screen: that excludes only pairs inside M23; M24 also has M22:2 and affine carriers and cross-carrier pairs.
  atlas-m24-a8-three-carrier-fork: that reduces the carrier sources but does not classify their ambient fusion or screen marked pairs.
---

It remains to prove that no pair of marked `A8` subgroups in `M24` both
generates `M24` and satisfies the twelve Atlas packet edges together with
collision `19243` and `q14`.

The exact fusion theorem `atlas-m24-has-one-self-normalizing-a8-class` removes
the apparent carrier ambiguity: there is one ambient class.  The coset
fibration theorem then absorbs the `506` conjugates sharing the fixed point
of the first chart by the existing M23 packet screen.  The remaining double
cells lie over the natural eight- and fifteen-point orbits and are open.

## Attempts

The direct octad-complement construction resolves fusion and normalizers.
A subsequent exact construction of the full degree-`12144` coset action was
stopped at its strict 180-second cap before returning subdegrees; no partial
output is retained.  The next attack should exploit the proven
`506+4048+7590` fixed-point fibration rather than rebuild the unstructured
coset action.
