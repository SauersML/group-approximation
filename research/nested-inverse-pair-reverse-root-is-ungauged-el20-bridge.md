---
rg: 2
id: nested-inverse-pair-reverse-root-is-ungauged-el20-bridge
kind: route
title: Use the first non-Boolean reverse-root return as an ungauged moving bridge
target: el20-six-moving-coefficient-square-bridges
requires:
  - actual-mixed-complement-returns-reverse-root
---

**INVALIDATED UNGAUGEDNESS STEP.**  Continue the actual complementary
output `x_97(1)` by the positive signed root written as the non-Boolean
inverse-pair commutator

```text
x_76(1)=[x_75(t_0),x_56(s_0)].
```

Their outer commutator is indeed the external signed normalizer
`x_96(1)`, so this proposal obtains exact source and range Gram and is a
genuine new coefficient-sensitive occurrence.

However,
`nested-inverse-pair-returns-signed-source-with-free-sign`
gives an exact two-chart Pauli model in which the positive middle root is
the identity on the source while the returned external root acts there by
`-1`.  The output therefore has an uncontrolled reservoir involution, not
the `tensor 1` typing required by `(ESB2)`.  A further relation tying an
inner nonnormal occurrence to the literal native gauge would be needed.
