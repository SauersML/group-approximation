---
rg: 2
id: deligne-lattice-is-universal-central-extension-for-g-ge-4
kind: claim
title: For g at least four Deligne's lattice is the universal central extension of Sp_2g(Z), so it embeds in the universal central extension of every perfect group receiving Sp_2g(Z) injectively on H_2 (a criterion that no host reached through SL_N(Z) can meet)
distinct_from:
  deligne-universal-cover-lattice-is-non-rf-kazhdan: that records the lattice as the universal-cover preimage and its non-residual finiteness; this adds, from Funar--Pitsch at source, that for g >= 4 it is the universal central extension (and is not for g = 2, 3), and draws the functorial consequence for hosts.
  deligne-lattice-satisfies-boone-higman: that is the open Boone--Higman question for the lattice; this reduces it to Boone--Higman for universal central extensions of suitable finitely presented perfect hosts and shows the criterion cannot use hosts reached through SL_N(Z).
  deligne-extension-dies-on-lagrangian-cantor-covers: that shows the class dies on Cantor Sp_2n(Z)-spaces covering the Lagrangian Grassmannian; this is a homological criterion for any host, with no action.
---

**ESTABLISHED** (item 1 by citation, read at source; items 2--3 lane proof, elementary,
not independently reviewed; no priority claimed). Item 4 is conditional.

## 1. Source facts

Funar--Pitsch, "Finite quotients of symplectic groups vs mapping class groups",
arXiv:2004.04129v2, read from the PDF (pages 2 and 7):
- Theorem 1.2: "The universal central extension `Sp(2g,Z)~` is not residually finite when
  `g ≥ 4` since the image of the center under any homomorphism into a finite group has
  order at most two." They add: "The first part of this result is the statement of
  Deligne's non-residual finiteness theorem from [14], which was stated for `g ≥ 2`."
- Remark 1.3: "the intersection of the finite index subgroups of `Sp(2g,Z)~` is precisely
  the subgroup generated by twice the center generator, when `g ≥ 4`."
- Remark 1.4: "`H_2(Sp(6,Z)) = Z ⊕ Z/2Z` ... while `Sp(4,Z/4Z)` is not perfect. Thus the
  central extension by `Z` considered in [14] is not the universal central extension of
  `Sp(2g,Z)`, when `g ∈ {2,3}`."
- Page 7: "`H_2(SL(g,Z)) = Z/2Z`, when `g ≥ 5` and `H_2(Sp(2g,Z)) = Z`, when `g ≥ 4`"
  (citing their [49], [52]).

So for `g ≥ 4` the lattice `Γ~_g` of `deligne-lattice-satisfies-boone-higman` (the
preimage of `Sp_2g(Z)` in the universal cover of `Sp_2g(R)`, which is Deligne's extension
[14]) is the universal central extension of the perfect group `Sp_2g(Z)`, with center
`H_2(Sp_2g(Z)) = Z`. For `g = 2, 3` it is a proper quotient of the universal one.

## 2. Functorial embedding criterion (g ≥ 4)

Let `S` be a perfect group with universal central extension `S~ → S`, and let
`φ : Sp_2g(Z) → S` be injective. Then `φ` lifts uniquely to `φ~ : Γ~_g → S~`, and

```text
φ~ is injective  ⟺  φ_* : H_2(Sp_2g(Z)) = Z → H_2(S) is injective
                 ⟺  φ_*(generator) has infinite order.
```

So `Γ~_g` satisfies Boone--Higman as soon as some finitely presented simple `S` contains
`Sp_2g(Z)` with `φ_*` injective and its universal central extension `S~` (finitely
presented and perfect, with center `H_2(S)`, finitely generated) satisfies Boone--Higman.

## 3. What kills the criterion

If `φ` factors through any group `Q` with `H_2(Q)` torsion (for instance
`Sp_2g(Z) ⊂ SL_N(Z)` with `N ≥ 5`, where `H_2 = Z/2`), then `φ_*` has finite image and
`φ~` is not injective: it kills a finite-index subgroup of the center. In particular, for every
host reached through the standard embedding into `SL_N(Z)` (for instance the hosts of
`finitely-generated-linear-groups-satisfy-boone-higman` applied to the standard
representation), the lift `φ~` kills a finite-index subgroup of the center. This does
not say `Γ~_g` misses such hosts; it says the criterion of item 2 cannot be used there. A host for `Γ~_g` built
this way must carry an infinite-order class in `H_2` coming from the symplectic (Maslov)
class, as `T` does for its Euler class.

## 4. Conditional Steinberg form

If the symplectic Steinberg group `StSp_2g(Z)` is the universal central extension of
`Sp_2g(Z)` for `g ≥ 4` (a stability theorem of Stein / van der Kallen type; **not read at
source**), then `Γ~_g ≅ StSp_2g(Z)`, and `Γ~_g` embeds in `StSp_2g(R)` for every ring
`R ⊇ Z` for which `Sp_2g(Z) → Ep_2g(R)` is injective and `KSp_2(2g, Z) → KSp_2(2g, R)` is
injective. That puts the test case alongside the Steinberg shell route
(`steinberg-resolvent-shell-envelope-is-finitely-presented`), with symplectic `K_2` in
place of linear `K_2`.

## Scope

Nothing here decides Boone--Higman for `Γ~_g`. It converts the question, for `g ≥ 4`,
into Boone--Higman for universal central extensions of suitable perfect hosts, and
shows the criterion needs hosts whose `H_2` sees the Deligne class with infinite order. Compare
`odometer-lift-central-extensions-of-v-subgroups-into-2v-tau`, where a central
`Z`-extension (`T̄`) of a subgroup of `V` does embed in a finitely presented simple group.
