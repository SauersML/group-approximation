---
rg: 2
id: natural-depth-three-r2-r3-expansion-exposes-shared-endpoint
kind: route
title: Use the canonical depth-three R2 triangle or its first R3 refinement as the shared label anchor
target: bleak-quick-a6-seam-exposes-shared-label-arm-anchor
requires: []
---

**INVALIDATED PROPOSAL.**  Normalize one adjacent seam to the six cylinders
`000,001,010,011,100,101`.  Factor the contextual Carmichael cycles through
their two common-pivot swap arms, use the depth-three conjugacy relation as
the R2 anchor, and if necessary split those arms once using R3.  The proposed
conclusion was that one input, output, or complementary child would be the
same exact Bleak--Quick word in both contexts.

`depth-three-bq-arm-expansion-is-equivariant-under-label-flip` expands every
word.  The contextual cycles are `b^g` and `b^(gf)` for the literal label
swap `f=(010 011)`.  It fixes the pivot but transports both R2 endpoints and
all R3 children.  Hence the canonical one-cell/one-level address expansion
does not meet the hypotheses of the shared-endpoint inequality.  This does
not refute an asymmetric bounded multi-cell diagram involving another R4/R5
collision; it kills the natural direct R2/R3 candidate only.

