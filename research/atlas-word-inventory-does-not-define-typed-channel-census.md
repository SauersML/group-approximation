---
rg: 2
id: atlas-word-inventory-does-not-define-typed-channel-census
kind: claim
title: Literal Atlas word lists do not by themselves define the typed nuisance-quotient channel census
distinct_from:
  atlas-typed-hard-interior-comb-row-census: that asks for the missing finite typed artifact; this proves why the existing word inventory is not already such an artifact.
  atlas-hard-packet-authenticates-s4-extension-triple: that asks the packet to construct a common multiplicity cut; this says the cut cannot be inferred merely from the stored operator names.
  atlas-authenticated-s3-extension-minor-decodes-pairings: that asks the completed literal census to have quotient rank three; this concerns whether the coefficient matrix is defined at all.
  atlas-mixed-minor-needs-nuisance-quotient-rank: that proves the rank criterion after a typed matrix exists; this records the prior data/type gate.
artifacts:
  - research/artifacts/atlas-typed-row-census-acquisition-2026-08-23.md
  - experiments/atlas-six-relator-relative-pieces.json
  - experiments/atlas-raw-compressor-source-words.json
  - experiments/atlas-raw-comb-mixed-relations.json
---

The committed Atlas artifacts determine the literal six-relator words, the
hard boundary and A4 packet, and the raw and comb source words.  They do not
determine a coefficient matrix `[C D]` whose desired columns are

```text
R0^* W L0-W,   R1^* W L1-W,   R0^* W L1.
```

A projected coefficient row is defined only after specifying an actual
relator equation (or a bounded normal-closure consequence), fixed finite
carrier projectors, a finite-matrix-only common multiplicity cut, a product
bracketing with its intermediate channels, and the reduced multiplicity
monomial of every term.  The source-word artifact
names the raw and comb operators but supplies no equation constraining them.
The shallow mixed-collision artifact stores counts but not its collision
normal forms or packet fillings.  Most importantly, the existing notes leave
`W,R0,R1,L0,L1` as outputs of the open common-coordinate radical-quotient
extraction, not as compressed blocks of named words.

Without those data, classifying a term as one of the three desired columns
or as nuisance requires choosing an external multiplicity identification.
Two choices differ by the full centralizer-valued fiber holonomy described
in `FALSE_TWO_RAW_COMPRESSORS_DO_NOT_IDENTIFY_FIBERS.md`.  Such a choice is
the holonomy the decoder is meant to force, so inserting it before the row
census is circular.  Likewise, deleting unmatched monomials would violate
`atlas-mixed-minor-needs-nuisance-quotient-rank`.

The absent cut cannot be replaced by a universal word-polynomial projector.
Such a projector would also exist in the exact Leavitt tracial model, where
the resulting three pairing defects would contradict the exact model itself.
The required authentication must instead be a finite-matrix-only selection
with positive normalized mass and dimension-independent HS loss.  This is
the same category boundary recorded by
`universal-atlas-ideal-cannot-force-hall-deficit` for universal support cuts.

Therefore literal word replay can presently enumerate group words and
finite quotient images, but cannot soundly emit the typed matrices `C,D`.
The exact finite fields needed to cross this boundary are recorded in
`research/artifacts/atlas-typed-row-census-acquisition-2026-08-23.md`.
