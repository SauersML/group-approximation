---
rg: 2
id: mf-radical-contains-defect-ideal-elementary-subgroup
kind: claim
title: Over a ring whose one-sided inverse defects have finite additive order the MF radical contains the relative elementary subgroup of the defect ideal
distinct_from:
  finite-additive-order-one-sided-defects-are-mf-invisible: that covers one defect of one pair; this sums over all pairs and returns the ideal d(R) they generate, the first stage of the Hilbert-hotel radical.
  mf-radical-contains-hilbert-hotel-elementary-subgroup: that is the transfinite closure hh(R) and is open; this is the first stage d(R) and is proved.
artifacts:
  - research/artifacts/un-open-1-hilbert-hotel-radical-2026-09-13.md
---

**ESTABLISHED** (route `mf-radical-contains-defect-ideal-elementary-proof`), unreviewed.

Let `R` be a countable unital ring in which every defect `1 - st` of a one-sided pair `ts = 1` has
finite additive order (for instance any ring of positive characteristic), and `n >= 4`. Let `d(R)` be
the two-sided ideal generated by all such defects. Then

```text
EL_n(R, d(R)) <= Rad_MF(EL_n(R)).
```

In every calibration known here `d(R)` already equals the Hilbert-hotel radical `hh(R)`, the smallest
ideal with directly finite quotient: `d = R` for simple rings that are not directly finite, `d = 0` for
the Pestov 9.1 ring, and `d = JeJ` for the Toeplitz-Jacobson algebra.

**Review (un-verify, 2026-09-13): PASS.** Each defect is a genuine one-sided pair of `R` with finite additive order, so the absolute printed statement applies with exact identities; normality, `[e_ki(a), e_ij(e)] = e_kj(ae)`, `[e_kj(ae), e_jl(b)] = e_kl(aeb)`, signed permutations and additivity give `EL_n(R, d(R))`. See `research/artifacts/un-review-2026-09-13-part8.md` §4.
