---
rg: 2
id: sl3z-congruence-elementary-generators-have-finite-index
kind: claim
title: For every m >= 1 the subgroup of SL_3(Z) generated by the elementary matrices e_ij(m) has finite index
---

For every integer `m >= 1`, the subgroup `E(m) <= SL_3(Z)` generated by the
six elementary matrices `e_ij(m) = I + m E_ij` (`i != j`) has finite index in
`SL_3(Z)`.

This is a literature statement for `SL_n(Z)`, `n >= 3`. It is recalled as
J. Tits, *Systèmes générateurs de groupes de congruence*, C. R. Acad. Sci.
Paris (1976), which rests on the congruence subgroup property of `SL_n(Z)`,
`n >= 3` (Bass–Milnor–Serre; Mennicke).

## Attempts

- 2026-09-13 (z3-04-sl3z-incoherent): the source was NOT re-read in this
  session; web and arXiv tools were rate limited. A `-citation` route should
  land only after a source check that quotes the exact statement.
- Easy part, for the record: `E(m)` contains `e_ij(m^2 x)` for all integers
  `x`, since `[e_ik(m), e_kj(mx)] = e_ij(m^2 x)`. Conjugation by `e_ki(1)`
  maps `e_ij(m)` to `e_ij(m) e_kj(±m)`, which lies in `E(m)`. The pairs
  `(j, i)` are where the normal-closure argument needs Tits' input.
