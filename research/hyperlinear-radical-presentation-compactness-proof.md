---
rg: 2
id: hyperlinear-radical-presentation-compactness-proof
kind: route
title: Negate finite-window collapse and take a metric ultraproduct
target: hyperlinear-radical-presentation-compactness
requires: []
---

If finite-window collapse fails for some `epsilon_0>0`, choose for every `n`
a finite-dimensional tuple `U_n` whose first `n` relators have defect below
`1/n` but with `||w(U_n)-I||_2>=epsilon_0`.  In any nonprincipal metric
ultraproduct the generator classes satisfy every relator, hence define a
homomorphism from `Gamma`, while the image of `w` is nontrivial.  Thus `w` is
not in the hyperlinear radical.

Conversely, representatives of any homomorphism into a tracial matrix
ultraproduct satisfy every fixed finite relator window asymptotically.  The
finite-window implication for every `epsilon` therefore makes the image of
`w` equal to `1`.
