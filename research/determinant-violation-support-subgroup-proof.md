---
rg: 2
id: determinant-violation-support-subgroup-proof
kind: route
title: Compute the determinant inside the support subgroup and apply the sofic theorem there
target: determinant-violation-needs-nonsofic-support-subgroup
requires:
  - determinant-conjecture-holds-for-sofic-groups
---

`N(H)` is a trace-preserving subalgebra of `N(G)` containing `A`, so `mu_(A*A)`
and `det(A)` are unchanged when computed in `H`. If `H` were sofic,
Elek--Szabó would give `det(A) >= 1`.
