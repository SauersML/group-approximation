---
rg: 2
id: complete-overlay-code-riesz-identification-proof
kind: route
title: Invert the complete-graph coboundary on each code-character block
target: complete-overlay-row-newton-is-a-code-riesz-multiplier
requires:
  - quadratic-overlay-has-uniform-linearized-synchronization-gap
---

Use the character-block formula `(QLS3)`.  Averaging its complete-graph
differences over `supp(chi+psi)` gives `(CRM2)`.  Code distance controls the
denominator and the equality-cloud Green operator handles occurrences.  On
assembling the blocks, the support-dependent coefficients are exactly the
stated vector-valued Schur multiplier, separating the automatic `L_2` bound
from the missing cb row/column bound.
