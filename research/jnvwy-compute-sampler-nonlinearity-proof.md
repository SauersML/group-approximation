---
rg: 2
id: jnvwy-compute-sampler-nonlinearity-proof
kind: route
title: Follow the axis-line deletion switch through detyping anchoring and repetition
target: jnvwy-compute-sampler-has-nonlinear-role-marginal
requires: []
---

Use equation (30)'s axis-line map `(CSN2)`.  Toggling `s` from `0` to `q/m`
switches the selected deletion map from coordinate `1` to coordinate `2`.
The three inputs `(CSN3)` therefore give the explicit failure `(CSN4)` of the
binary odd affine identity.

The PCP sampler is a direct factor of the answer-reduced typed sampler.  Fix
one valid graph-sampler view selecting this factor during detyping, fix the
`Game` branch during anchoring, and vary only one coordinate during repeated
direct-product sampling.  An odd xor preserves every fixed binary prefix and
every fixed repeated coordinate.  Thus the same deletion-switch defect occurs
in a role marginal of the final universal sampler from Lemma 12.3.
