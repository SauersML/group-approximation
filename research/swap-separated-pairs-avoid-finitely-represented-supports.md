---
rg: 2
id: swap-separated-pairs-avoid-finitely-represented-supports
kind: claim
title: No ternary corner solution or swap-separated pair is supported in a subgroup with a finite-dimensional sign representation
distinct_from:
  subgroup-supported-one-sided-inverse-rigidity: that excludes supports whose generated subgroup has a directly finite group algebra; this excludes swap-separated pairs supported in any subgroup with a finite-dimensional characteristic-three representation sending z to -1, including nonsofic subgroups whose group algebras may fail direct finiteness.
  s0-lifts-cannot-be-supported-in-the-all-ones-stabilizer: that is the binary visible lift of s0 over F_2; this is the ternary anti-central problem, for pairs with any image.
  ternary-twisted-algebra-has-swap-separated-one-sided-pair: that is the open existence of a separated pair; this is an established filter on where such a pair can be supported.
artifacts:
  - research/artifacts/ternary-swap-separated-pairs-2026-09-12.md
---

**ESTABLISHED.** Let `G = L_(F_3)(1,2)^x`, `z = -1`, `w = s0 t1 + s1 t0`, and let `H <= G` contain
`z` and `w`. Suppose `H` has a finite-dimensional representation `rho` over a field of
characteristic three with `rho(z) = -1`.

1. No `a, c` in `eps_- F_3[H]` satisfy `c a = eps_-` and `c [w] a = 0`.
2. If `rho(w)` has eigenvalue `-1`, no `b, c` in `eps_- F_3[H]` satisfy `c e_- b = eps_-`.

The hypothesis holds in the following cases.
* **Finite orbit.** Some nonzero vector of some `R`-module has a finite `H`-orbit. On any
  `R`-module `z` acts as `-1`.
  * *Examples.* The all-ones vector of the Cantor module is fixed by Thompson's `V` and by `w`.
    The line spanned by `1_[0] - 1_[1]` is where `w` acts as `-1`.
* **Finite quotient.** Some finite quotient of `H` does not kill `z`. For part 2 it must also not
  kill `w`.

So the subgroup generated by a solution's supports, with `z` and `w`, must satisfy three
conditions:
* `z` lies in every finite-index subgroup;
* no nonzero vector of any `R`-module has a finite orbit;
* no characteristic-three representation sends `z` to `-1`.

Proof: `rho(eps_-) = 1`. A one-sided inverse in `M_n(k)` is two-sided, so part 1 forces
`rho(w) = 0`, and part 2 forces `1 + rho(w)` to be invertible. Section 4 of the artifact.
