---
rg: 2
id: central-corner-block-is-not-a-group-word
kind: claim
title: A central-corner controlled block is not a word in its control and payload
distinct_from:
  controlled-linear-predication-normal-form: that is an operator-algebra normal form after the controlled observables have been constructed; this proves that the construction itself is not ordinary group-word evaluation.
  controlled-central-phase-finite-group-barrier: that treats a nonlinear central phase over many selector bits; this already blocks the binary controlled payload operation in the subgroup generated by its control and payload.
---

Let `z,u` be commuting involutions, put `c=(1-z)/2`, and consider the
operator-algebra controlled block

```text
T=(1-c)+cu.                                                       (CCB1)
```

In any joint representation containing both `u`-characters over both
`z`-characters, there is no group word `w(z,u)` whose value is `T`.

Indeed every word in the abelian group generated by `z,u` is `z^a u^b`.
On the `z=+1` sector, `(CCB1)` is the identity for both values of `u`, forcing
`b=0`.  On the `z=-1` sector the word is then the constant `(-1)^a`, whereas
`(CCB1)` takes the two values of `u`.  This is impossible.

Naming `T` as a new group generator does not by itself impose `(CCB1)`.
Ordinary group relations only cut out character data of subgroups; a finite
auxiliary overgroup which tries to retain precisely the controlled graph has
the extra induced sectors described by `finite-group-selector-induction-barrier`.
Equivalently, `(CCB1)` is a group-algebra identity involving addition and the
spectral projection `c`, not a group identity.

Consequently an exact matrix model built from controlled blocks proves
existence of one distinguished representation, but not universal soundness
for an ordinary group presentation.  A successful controlled-corner reset
must separately groupify the block through payload-coupled infinite
relations, a proper-corner incidence, or a quantitative matrix theorem which
charges every unwanted sector.

There is one exact escape which does not contradict this statement. After
completing `z,u` to two Pauli pairs, `T` is the Clifford `CZ` implementer and
its **conjugation action** is an automorphism of a finite extraspecial group.
`controlled-pauli-corner-has-clifford-groupification` adjoins that automorphism
by an ordinary stable letter. In an arbitrary marked representation the
letter is `T tensor V` for an uncontrolled multiplicity involution `V`.
Therefore the escape groupifies covariance, not the additive identity or the
spectrum of `T` itself.

Composing only these covariance escapes does not create contraction.
`clifford-covariance-atlases-have-regular-flow` realizes every finite such
atlas on compatible multiples of regular packet representations.  The next
required operation is a two-cell constraining the multiplicity twists, not
another covariance edge.
