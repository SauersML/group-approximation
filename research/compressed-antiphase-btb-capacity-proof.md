---
rg: 2
id: compressed-antiphase-btb-capacity-proof
kind: route
title: Combine BTB density, pairwise antiphase, and contraction capacity
target: compressed-antiphase-menu-implies-btb-escape
requires:
  - pairwise-antiphase-covariance-controls-cross-gram
  - contraction-branch-capacity-forces-collision
  - canonical-bcs-root-detection-via-binary-tetrahedral-bridge
  - e5-idempotent-root-has-two-pair-extraspecial-cell
---

The BTB half-root bound followed by the characteristic-three two-pair cell
gives `(CAM1)`.  Apply the pairwise-antiphase inequality to `(CAM4)` to get
`(CAM7)`, then apply the contraction capacity inequality inside `Q` to get
`(CAM8)`.  The quadratic estimate is `(CAM9)`.  The fixed choice `(CAM2)`
turns the average leakage in `(CAM9)` into `(CAM10)`, so one of the finitely
many branch words satisfies `(CAM5)`.

