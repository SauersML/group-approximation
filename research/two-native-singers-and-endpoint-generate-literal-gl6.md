---
rg: 2
id: two-native-singers-and-endpoint-generate-literal-gl6
kind: claim
title: The two native Singer heads and one endpoint Whitehead generate a literal GL6 block
artifacts:
  - research/two-native-singer-endpoint-gl6-proof.md
distinct_from:
  native-j1-extension-gives-gl5-but-only-second-whitehead-gram: that obtains the literal GL5 block generated by the second native Singer, the endpoint Whitehead, and J1; this adjoins the first native Singer and proves that the resulting six-summand actor is all of GL6.
  first-endpoint-m1-actor-preserves-missing-r-sign: that couples M1 to a separate first-scale endpoint and obtains a GL4 return fence; this couples M1 to the already established second-scale endpoint GL5 through their two shared projective summands.
  fixed-hecke-typed-interface-has-common-c2-grading: that gives a carrier-local sign quotient before the global endpoint actor is authenticated on one carrier; this identifies the complete literal finite actor containing both native heads and the endpoint parity breaker.
---

**ESTABLISHED LITERAL SIX-SUMMAND ACTOR.**  Put

```text
M_1=J_1B_2A_1,                 M_2=J_2B_3A_2
```

and use the six orthogonal equivalent projective summands

```text
u_0=q@4,       u_1=e_1@7,       u_2=e_2@8,
u_3=q@5,       u_4=e_3@9,       u_5=e_3@2.             (NSG1)
```

The literal subgroup

```text
G_6=<W,M_2,J_1,M_1>
```

is the full binary linear group on their direct sum:

```text
G_6|_(u_0+...+u_5)=GL_6(F_2),
|G_6|=20158709760,                                      (NSG2)
```

and every element of `G_6` is the identity on the complementary projective
summand.  The canonical projective-matrix-unit lift is faithful in the
Steinberg group, so `(NSG2)` is a literal subgroup statement in
`St_20(L_(F_2)(1,2))`, not only an elementary-image calculation.

In particular all of

```text
A_1,B_2,J_1,M_1,A_2,B_3,J_2,M_2,W                    (NSG3)
```

belong to this one finite subgroup.  Since their matrix indices avoid
`{1,3}`, `G_6` centralizes the marked root

```text
z=x_13(q).                                             (NSG4)
```

Thus the two order-seven heads and the endpoint order-five actor admit one
literal finite actor frame.  This removes any need to compare those actor
words through separately chosen moving charts.  It does not assert that the
signed Hecke projection `Q`, or either unequal native source projection,
reduces this actor.

No Property `(T)`, Kazhdan input, trace profile, approximation theorem, or
literature input is used.

DERIVATION
two-native-singer-endpoint-gl6-proof
