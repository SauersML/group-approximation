---
rg: 2
id: spherical-defect-congruence-preserves-separation
kind: claim
title: Tight spherical matchings can be chosen so that their invariant relation-defect congruence preserves regular-character separation
distinct_from:
  hypergraph-near-perfect-schreier-packing: that claim chooses the generator matchings jointly so the relations already hold in Hamming distance; this claim permits independently tight matchings and asks only that the canonical invariant quotient generated by their short defect pairs not collapse distinct tested words.
  geometric-schreier-design: that claim outputs a labelled geometric Schreier graph on the cloud itself; this claim is only the noncollapse theorem for a specific quotient exactifier and its output need not remain a geometric matching on the original cloud.
  spherical-one-overlap-forest-rounding: that theorem closes acyclic one-overlap relation systems by exact composition; this claim is aimed precisely at cyclic systems where two or more previously chosen maps meet again.
artifacts:
  - research/artifacts/hyperlinear-cycle-synchronization-bypasses-2026-08-21.md
---

For every finite regular-character multiplication table `T` and every
`eps>0`, there are `rho>0` and a microstate tolerance `delta>0` with the
following property.

Let `{U_s}` be a unitary model of `T` with normalized-HS multiplication defect
and regular-trace error below `delta`.  On one sufficiently large iid uniform
spherical cloud, choose simultaneous near-perfect radius-`rho` matchings for
all table labels as allowed by
`spherical-tight-single-colour-near-perfect-matching`, and complete them to
permutations `sigma_s` after charging the missing domains/ranges to the
exceptional sets.

For each relation `r=(g,h,k)`, let `G_r` be the roots on which all three
matching shadows hold and on which the pointwise unitary relation defect is
below a threshold tending to zero with `delta`.  Let `E` be the least
equivalence relation invariant under all `sigma_s^{+/-1}` and containing

```text
sigma_g sigma_h(i) E sigma_k(i)    for i in G_r.        (SDC1)
```

The matchings can be chosen so that for every tested pair of distinct words
`u != v`,

```text
|{i : sigma_u(i) E sigma_v(i)}| < eps |X|.             (SDC2)
```

By `spherical-relation-defect-pairs-small-chord`, the generating pairs in
`(SDC1)` are short geometric chords outside the already charged exceptional
set.  The content of this claim is that taking their **full invariant
congruence closure** does not percolate far enough to destroy the
regular-character separation in `(SDC2)`.

No lower bound on the number of `E`-classes is asserted or needed: the
cardinality-preserving weighted blow-up in
`defect-congruence-exactifies-permutation-table` converts `(SDC2)` directly
into Hamming separation.

## Attempts

- **Bound every congruence class by the short-chord radius.**  This is false as
  a formal argument: invariance adds conjugates of the defect pairs, and an
  arbitrarily long chain of individually short moves can travel macroscopic
  distance.  Any proof must control percolation/normal closure, not just one
  edge length.
- **Require linearly many quotient classes.**  Unnecessary.  The weighted
  blow-up in `defect-congruence-exactifies-permutation-table` keeps exactly
  `N` states even when `X/E` is tiny.  The only relevant statistic is the
  weighted word-collision set in `(SDC2)`.
- **Deferred falsifier.**  For fixed small tables, sample the cloud, choose
  independent tight matchings, close the good defect pairs under the chosen
  permutations by union-find/orbit saturation, and track `(SDC2)` as `rho`
  and the matrix defect shrink.  Positive-density collapse of a tested
  nontrivial word would kill this route directly.