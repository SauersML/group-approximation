---
rg: 2
id: nonce-relation-admits-free-host
kind: claim
title: The Manzoor relation or the property-(T) non-CE relation is a class-bijective factor of a subrelation of an essentially free pmp action of some countable group
distinct_from:
  non-ce-relation-admits-algebraic-action-envelope: that asks for an algebra-level embedding into a compact abelian algebraic crossed product; this asks for a relation-level class-bijective placement into any essentially free pmp action, with no algebraic structure on the actor or the space.
  unrestricted-algebraic-envelope-equals-nonhyperlinearity: that notes an unrestricted envelope is circular because any non-CE crossed product qualifies; here the relation is the fixed Manzoor or property-(T) relation, so a host is a measured placement of a known object, not a restatement of the root.
  dense-full-subgroup-without-ce-covariant-model: that asks for a subgroup of the full group of the non-CE relation with no CE covariant model; this asks for an embedding of the relation into a free action from outside.
  jacobson-haar-relation-lifts-into-bilateral-relation: that is the same kind of class-bijective placement problem for the Jacobson Haar relation into a fixed sofic ambient; this places a non-CE relation into an unspecified free ambient.
---

**OPEN.** Placement prerequisite of `nonhyperlinear-via-q1-and-free-host-of-nonce-relation`.

**Statement.** Let `R` be the Manzoor relation `R_mu` of `non-ce-relation-is-not-treeable` item 3, or the
property-(T) relation of `non-ce-property-t-equivalence-relation-exists`. Then `R` has a finite free host, in the
sense of `nonce-relation-hosts-force-root-or-q1-failure`. That is, there are:
* a countable group `Lambda` with an essentially free pmp action on a standard probability space `(Z, zeta)`;
* a Borel subrelation `S ⊆ R_Lambda(Z)` (restricting to a positive-measure `A ⊆ Z` is also allowed);
* a measure-preserving Borel map `r : Z -> X` that maps a.e. `S`-class bijectively onto an `R`-class.

**Sufficient condition** (item 2 of `nonce-relation-hosts-force-root-or-q1-failure`): `R` has a Borel cocycle `c`
into some group `Lambda` acting pmp on `X`, with `c(x, x') · x = x'`.

**Parallel input.** If `R` is generated by a pmp action of some group with finite stabilizers, item 3 of the same
claim gives the conclusion the route needs (a non-CE crossed product of that actor). It does so without producing a
relation-level host, so it is an alternative to this claim rather than a proof of it.

**What is excluded.** By item 5 of `nonce-relation-hosts-force-root-or-q1-failure`, the actor can be neither
amenable-by-(finitely generated virtually free) nor free. The finite-stabilizer and splitting versions are excluded
for those actors too. So a host must use an actor outside every class where Q1 is currently known.

**How it can fail.** A proof that `R` has no free host with any actor would be a new orbit-equivalence rigidity
statement. It would be stronger than non-treeability, and it would have to be invisible to Connes embeddability:
Bernoulli relations of non-hyperlinear groups, if such groups exist, have hosts. No such rigidity mechanism for
class-bijective placements is recorded in the graph. `kazhdan-bernoulli-product-seed-stabilizers-embed-in-host` is the
nearest tool, but it constrains only the actor's subgroups, and only for Bernoulli product seeds of Kazhdan groups.

## Attempts

- **Regeneration from the native action (fails as stated).** The native action of the free group `F` has infinite
  stabilizers (`manzoor-isotropy-has-no-positive-finite-type-corner`), so item 3 of the hosting claim does not apply
  to it. Replacing `F` by another generating group changes nothing unless the new stabilizers are finite.
- **Normalized-section extension (partial, swarm-0917 w18, 2026-09-19).** Let `Gamma ↷ X` generate `R`.
  * Take the space of pairs `(x, sigma)`, where `sigma(x') ∈ {g : g x = x'}` for `x' ∈ [x]` and `sigma(x) = 1`.
    Re-rooting `(x, sigma) -> (x', sigma sigma(x')^(-1))` defines a class-bijective extension `R~` of `R`.
  * `c((x, sigma), (x', sigma')) = sigma(x')` is a cocycle into `Gamma` with `c · x = x'`, and it is class-injective.
  * The product of the uniform measures on the stabilizer cosets is invariant under re-rooting, but it exists only
    when stabilizers are finite. That case is already covered by the finite-isotropy corner.
  * For infinite stabilizers one needs a re-rooting-invariant random normalized section. None is known.
  * Even when one exists, a class-injective cocycle on `R~` gives only an infinite-measure host (by the Mackey
    range). By item 5 of the hosting claim, amenable and free actors are excluded even there.
- **Kazhdan poly-Haagerup exclusion (class-kill for the `R_T` branch, swarm-0917 w19, 2026-09-19).**
  `kazhdan-relations-have-no-poly-haagerup-host` shows that the property-(T) relation `R_T` has no free host, finite
  or infinite, whose actor has a normal series with Haagerup quotients.
  * *Mechanism:* the host cocycle induces `C_0` Schur multipliers on `L(S)`. Compressed to `L(R_T)`, property (T)
    makes them uniformly close to the identity. This gives an `[R]`-fixed vector field, and its argmax is a
    finite-set-valued equivariant section.
  * The section cohomologizes the cocycle into a finite-by-(next term) subgroup. Iterating down the series lands it
    in a finite group, which contradicts the smoothness of the skew product.
  * *Consequence for this claim:* for `R_T`, the actor must lie outside a class much larger than item 5 of the
    hosting claim: Haagerup, `Z^2 ⋊ SL_2(Z)`, free-by-cyclic, `P_n`, and all extensions of these. This holds also
    for `zeta = infinity`, and for every subgroup of the actor that carries `S`.
  * A surviving `R_T` host needs an actor that is not normally poly-Haagerup. Every infinite (T) group is such an
    actor.
  * The `R_mu` branch is untouched, since `R_mu` is not known to have (T).
- **The `R_T` branch is subsumed by the `R_mu` branch (decomposition, swarm-0917 w20, 2026-09-20).**
  `property-t-envelope-hosts-restrict-to-manzoor-hosts` shows that every free host of `R_T`, finite or infinite,
  restricts and projects to a free host of `R_mu` with the same actor.
  * *Mechanism:* CDI's proof gives `R_T ⊇ R_mu × Delta_Y`. Restricting the host relation to the preimage of that
    subrelation and projecting off `Y` keeps class-bijectivity.
  * *Consequence for this claim:* the statement is equivalent to its `R_mu` disjunct, "`R_mu` has a finite free host".
    The w19 poly-Haagerup exclusion removes no case of it. It constrains only the stronger `R_T` problem, and whether
    `R_mu` has a poly-Haagerup host is OPEN.
  * The same node shows that `R_T` is the orbit relation of the property-(T) group `G ∈ WR(F_3, B)`, through the
    section `x rho_b b`. Its stabilizers are a.s. `⊕_c Stab_pi(xi_c)`, which contains `Z^(N)`. So the "Parallel input"
    (a finite-stabilizer generator) fails for the native Kazhdan generator of `R_T`, just as it fails for `F` on `R_mu`.
  * The converse transfer dies at the intrinsically infinite tails `rho_b`
    (`cdi-wreath-tail-cocycle-is-intrinsically-nonsplit`). What is left: a free host of `R_mu` itself, or an
    `R_mu`-level rigidity theorem.
