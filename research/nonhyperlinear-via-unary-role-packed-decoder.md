---
rg: 2
id: nonhyperlinear-via-unary-role-packed-decoder
kind: route
title: Iterate one role-packed forbidden idempotent past the marked-sector capacity
target: non-hyperlinear-group
requires:
  - mipstar-bcs-tracial-nonru-exists
  - finite-pauli-seed-reducing-quarter-carrier
  - role-packed-bcs-gap-is-one-idempotent
  - atlas-steinberg-context-absorption
  - murray-von-neumann-arrow-has-a-steinberg-conjugacy-witness
  - three-line-first-hit-solves-local-morita-edge
  - e5-idempotent-root-has-two-pair-extraspecial-cell
  - hs-unary-role-packed-ring-decoder
---

The exact perfect BCS trace, tensored with the finite role and seed spin
packets, has `J=-1` and `B_f=0`.  Extending every unary ladder arrow by zero
therefore proves that the marked word is nontrivial in the presented group.

In a hypothetical canonical matrix microstate, packet correction gives the
quarter carrier and the aggregate estimate `(RBG4)`.  Apply `(URD3)` and
obtain `(URD4)`.  Choose the fixed finite depth with

```text
(1+c beta/m)^N>2.
```

The accumulated geometric sum of the normalized-HS errors is independent
of matrix dimension.  Letting the presentation defect tend to zero gives

```text
tau(Q_N)>1/2,
```

whereas `Q_N<=Q_-` and canonical separation give
`tau(Q_-)->1/2`.  This contradiction excludes canonical matrix microstates
which separate `J`.  Hence the group is nonhyperlinear.

All algebraic, finite-packet, trace-growth, and exact-completeness inputs in
this route are established.  Its only open input is the unary physical
carrier decoder.

