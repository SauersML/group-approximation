---
rg: 2
id: full-sector-single-escape-collapse-proof
kind: route
title: A marked-sector-preserving actuator has full overlap with the full marked carrier
target: full-marked-sector-single-escape-is-direct-atom-collapse
requires:
  - maximal-forbidden-atom-has-a-named-positive-half
---

Because `[U,J]=1`, conjugation by `U` preserves the range of `Q_-`.  Hence
`Q_-UAU^*Q_-=UAU^*` for every `A<=Q_-`, and cyclicity of normalized trace
gives `(FSC1)`.  Substitution in `(SAE1)` yields `(FSC2)`.  The selected-mass
lower bound `(MFA2)` then gives `(FSC3)`.
