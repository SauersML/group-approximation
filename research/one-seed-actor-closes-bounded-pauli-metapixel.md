---
rg: 2
id: one-seed-actor-closes-bounded-pauli-metapixel
kind: route
title: Replace the mixed Weyl table by one seed and an expanding actor orbit
target: bounded-degree-entangled-agreement-pauli-metapixel
requires:
  - same-basis-rounding-interface-for-sparse-weyl-sampler
  - bounded-template-expanding-character-actor-family
  - one-pauli-seed-character-expansion
---

Round the single X occurrence table to one exact additive action.  Parseval
turns actor covariance defect into PVM transport energy.  The one D8 seed
places a fixed fraction of any surviving `J=-1` carrier in nonzero
characters.  Uniform Schreier expansion and `(OPS4)` then force either
exponential local dimension or collapse of that carrier.  Since the finite
presentation realizes every virtual size, choose `n` after seeing the matrix
dimension.  This supplies the metapixel without a second additive table or
mixed X/Z incidence rectangles.
