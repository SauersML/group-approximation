---
rg: 2
id: common-pivot-r2-or-r3-anchor-closes-a6-seam
kind: route
title: Add a common-pivot R2 transport or R3 split to close the A6 seam
target: thompson-v-r5-a6-seam-pointwise-synchronization
requires:
  - bare-r5-a6-collision-does-not-synchronize-shared-labels
---

**INVALIDATED PROPOSAL.**  Enrich both adjacent Carmichael charts by the
same pivot transposition and use either its R2 conjugacy triangles with the
three label arms or its R3 binary split.  The proposed conclusion was that
the shared pivot breaks the permutation gauge left invisible by R5.

`common-pivot-r2-r3-data-preserve-a6-label-swap` gives exact countermodels.
The six-point model shares the pivot and satisfies every contextual R2
transport identity, while the doubled twelve-point model additionally
satisfies exact split identities.  In both cases the common labels can still
be swapped together with all of their contextual arms.  Thus a pivot-only
anchor is dead; a surviving construction must expose one label-sensitive
endpoint as the *same matrix* in both contexts.

