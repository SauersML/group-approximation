# Swarm 2 adversarial review (2026-09-12)

Reviewer lane `swarm2-reviewer`. Each section gives one claim, the verdict
(PASS, FAIL or GAP), and the specifics. Sections are landed one at a time.

## 0. `weyl-algebra-blocks-a-universal-leavitt-host`: SCOPE ERROR, corrected forward

Priority item, flagged by `bh-leavitt-path-steinberg`.

**Finding.** The claim stated "the first Weyl algebra does not embed into
`L_K(1,2)`" for every field `K`. It was the only invalidator of
`boone-higman-via-universal-leavitt-host`, whose far end is this repository's
`F_2` host. The source proves the statement only in characteristic zero.
- **Source check.** From the arXiv:2512.09241v2 PDF, extracted on MSI with
  ghostscript txtwrite: Theorem 3.5, "Let k be a field of characteristic
  zero", and Theorem 3.7, with the same hypothesis. The proof uses
  Proposition 3.1, on Banach algebras over `R` or `C`, and a
  transcendence-degree reduction. The abstract omits the hypothesis, and the
  2026-08-17 import quoted only the abstract.
- **Counterexample to the abstract's wording in characteristic p.** On
  `F_p[t]/(t^p)`, `D = d/dt` and `T = t·` satisfy `DT - TD = 1`. Over `F_2`,
  `D = [[0,1],[0,0]]` and `T = [[0,0],[1,0]]` give `DT - TD = diag(1,-1) = 1`.
  Since `M_n(L) ≅ L` for `L = L_K(1,2)`, the Heisenberg equation is realized in
  `L_{F_p}(1,2)`. Whether `A_1(F_p)` embeds is not settled.

**Correction landed.**
- The claim is narrowed to characteristic zero.
- The citation route and the dead route are updated, together with item 5 of
  `boone-higman-conjecture`.
- A replacement invalidator that holds in every characteristic is added:
  `fixed-leavitt-hosts-cannot-be-universal-bh-containers`, with proof route
  `fixed-leavitt-hosts-not-universal-proof`. It requires only
  `binary-leavitt-unit-word-problem-decidable` and
  `complexity-bounded-host-classes-are-not-universal`.

**False establishment?** No, either before or after. The route stayed
invalidated throughout and `boone-higman-conjecture` stays OPEN; this is
confirmed by `cairn why` on MSI in the landing batch. The defect was a scope
overstatement: over `F_2` the route was dead for the wrong reason.

## 1. `unit-type-vertex-rounding-forces-compressor-commutant-rigidity` (35fccc207): PASS

Read line by line against `research/artifacts/nh-h1-rounding-2026-09-12.md` §§2–5.
- **Lemma 2.1 (finite images).**
  - `pi(U_ij)` is a group of commuting unitaries of exponent `p`, hence of order at most `p^m`.
  - `I = ∩ A_ij` has finite index. It is an ideal: `e_ij(fa) = [e_ik(f), e_kj(a)]` with `k ∉ {i,j}`, possible since `r >= 3`.
  - `St_r(R_+)/<<x_ij(I)>> ≅ St_r(R_+/I)`, because the Steinberg relations descend and the inverse map exists.
- **§3.**
  - `St_r(A_u × A_nu) = St_r(A_u) × St_r(A_nu)` for `r >= 3`.
  - Lemma 3.1 holds. The exponent identity should read `x_i = ∏_j y_j^((A^(-1))_(ji))` (transpose): with `y_j = ∏_k x_k^(A_kj)` one needs `A B^T = 1`. The artifact writes `(A^(-1))_(ij)`. This is harmless, since only integrality of the exponents is used.
  - Lemma 3.2 lifts `gamma` to `St_r(R_+)`, rewrites its image in `St_r(A_u)` as a word in `x_ij(s̄)`, and lifts that word to `EL_r(S) = Lambda`. It is correct. `Lambda = EL_r(S)` because the substitution `x ↦ x^(Ae)` maps `R_+` onto `S`.
- **Theorem 4.1.**
  - `PxP` commutes with `pi(Gamma)`, using that `P` is central in `pi(Gamma)''` and `pi(lambda) ∈ pi(Gamma)''`.
  - `||x - E x||_2 <= 2||x - PxP||_2 <= 4||1-P||_2`.
- **Corollary 4.2.**
  - `SL_d(Z)` is generated by the `e_ij(1)`, each a nonnegative strict compressor.
  - Every exponent vector is an `SL_d(Z)`-translate of a nonnegative one, so `G = <Gamma, e_ij(1)>`.
  - `Ad sigma(t)` maps `sigma(Gamma)'` to `sigma(Lambda)'`, and (CCR) makes these equal.
- **Inputs.** Item 2 of `vertex-rounding-reduces-ccr-to-commutant-excess` (PASS by `major-reviewer`).
- **Hidden hypotheses.** None found. The conclusion is conditional on unit-type rounding (`theorem-e-vertex-rounds-to-unit-type-representations`, OPEN), as stated.

## 2. `hs-rounding-and-actor-gap-force-ccr` (b11faeb9a): PASS

Read against `research/artifacts/hs-s4-assembly-2026-09-12.md` §§0–6.
- **Aspect ratios.**
  - `zeta_A ∈ Z(pi(Gamma)'')` and `zeta_B ∈ Z(pi(Lambda)'') ⊆ pi(Gamma)''`, so they commute.
  - `zeta_B >= zeta_A` because `m'_j >= m_i` and `e_j <= d_i` whenever `theta_j ⊆ rho_i|Lambda`.
  - (F): `x F_a'(x) <= 1/4`, so the defect is at most `(1/4) log(1+c) <= c/4`.
- **L1 and L2.** One-sided orthogonal supports give the sums. `||(1-q)uq||_2^2 = (1/2)||[u,q]||_2^2` by cyclicity.
- **L3.**
  - The transported types in `v^*v` are among the `rho_i ∘ Ad t^(-1)`, so `v^* F_a(zeta~) v = F_a(zeta^) v^*v` with `zeta^ = m_(iota(j))/e_j`.
  - `tau(R_c) <= ((1+c)/c) tau(1 - v^*v)`.
  - Off `R_c`, `zeta^ >= zeta_B/(1+c)`.
  - Steps 1–4 hold: commuting positive element versus its compression, monotonicity of `F_a`, (F), and `k_a >= h_a`.
  - `vQ - 1 = (v - 1) - vR_c` and `vQXQv^* - X = WXQv^* + XW^*`.
- **S4.**
  - Vertex generators: `sigma h sigma^* - h = D h sigma^* + pi h D^*` for `h ∈ Z(A_n)`.
  - Compressor: from `G_t >= f + Y - c/4` and `G_t >= f_B + Y' - c/4`, trace conservation gives `tau(f_B - f) <= 2beta + 2||W||_2 + c/4`.
  - `N >= 0` with `||N|| <= 7` gives the commutator bound.
  - `f` and `f_B` do not depend on `c`.
- **S5.** `median-poincare-concentration` (on main: no unitarity hypothesis, the constant matches), applied blockwise with `[a_(A,s), q_A f q_A] = q_A[sigma(s), f]q_A`.
- **S6.**
  - On `|F_m(x) - 1/2| <= 1/4` we have `rho ∈ [1/3,3]`, and `F_m((1+c)x) - F_m(x) = c rho/((1+(1+c)rho)(1+rho)) >= 3c/28` at both endpoints (unimodal).
  - Chebyshev and `||(h-1/2)q_A||_2^2 = ||q_A(h-1/2)q_A||_2^2 + ||(1-q_A)[h,q_A]||_2^2`.
  - Clause (c) is used exactly for the block-dependent `h_(m_A)`.
  - Then the excess lemma (PASS by `major-reviewer`).
- **Hidden hypotheses.** None found. The strengthened (H2')(c) is block-dependent, as the node states.

## 3. `hs-block-gap-iff-liftable-masa-of-actor-commutant` (ae240e9f4): PASS

- **Random-sign identities.**
  - `E||[u,f]||_2^2 = 2 Σ_(A≠B) tau(q_A u q_B u^*) = Σ_A ||[u,q_A]||_2^2`.
  - `E||[f,x]||_2^2 = 2||x - E_n x||_2^2`.
  - Bounded-coefficient bound `<= 2 Σ_A ||[u,q_A]||_2^2`.
- **(1) and (2).** Correct. `sigma(S)' = sigma(G)'` since `S` generates `G`.
- **Off-diagonal estimate.** The summands `(1-q_A)u q_A x_A` are orthogonal because `x_A x_(A')^* = 0`.
- **(3).**
  - `Ad sigma` on `L^2(M)` has fixed space `L^2(Q)`, and the Kazhdan inequality gives `κ_S^2||x - E_Q x||_2^2`.
  - Bimodularity puts `E_Q x ∈ Q ∩ C' = C`, so `E_Q x = E_C x`.
  - The contradiction argument handles uniformity over `x`.
- **(4).** Correct.
- **(5).**
  - Under (H1), `Q <= prod_U A_n`, and `(z_n) ∈ prod_U Z(A_n)` commutes with `C`.
  - Random signs over minimal central projections, plus `Σ_i ||[q_A,z_i]||_2^2 = 2Σ_(i≠i') ||z_(i') q_A z_i||_2^2`, give the block-dependent bound with factor 2.
- **Hidden hypotheses.** None found.

## 4. `ccr-under-vertex-rounding-from-few-aspect-scales` (ae240e9f4): PASS, rate constant not fully re-derived

- **Step 1.** `tau(D_a) <= ||F_a(zeta_B) - sigma F_a(zeta_A) sigma^*||_2 <= delta_n`, by trace invariance.
- **Step 2.**
  - `F_a(y) - F_a(x) = a(y-x)/((x+a)(y+a))` is increasing in `y`.
  - At `y = (1+c)x` with `x ∈ [a,2a)` it is `>= c/(3(3+2c)) >= c/15`.
  - So `PJ <= (15/c) P D_a`.
- **Steps 3 and 4.**
  - The window count `N_n <= 2 log_2 n' + 2` uses `zeta ∈ [1/n', n']`.
  - With `c = X^(1/3)`, `4 sqrt 15 + 2 <= 18`.
- **Counterexample profile.** `c = delta/4` gives `c_0 = (delta/8)^2`.
- **Log-trace formula.** `tau(log zeta_A) = log n' - H(p) - 2 Σ p_i log d_i` with `p_i = m_i d_i/n'`.
- **Rate form.** The chain Kazhdan projection → corner intertwiner → polar part is correct as far as it goes:
  - `||x-1||_2 <= eps'/kappa`;
  - `1 - v^*v <= 1 - x^*x`;
  - `||1 - x^*x||_1 <= 2||1-x||_2`;
  - `||v - x||_2 <= (2 beta)^(1/2)`.
  The final optimization to `delta_n <= 10 (eps'_n/kappa)^(1/3)` is truncated in the node and was not re-derived here. It matters only for the "Rate form" consequence. The main statement uses `delta_n -> 0` from item 4.

## 5. `realized-relative-twists-embed-in-outer-twist-group` (d9793a122): PASS

- **Setup.** `N_rel(sigma)` is a group. The definition in `relative-defect-normalization-dichotomy` includes `x sigma(M) x^(-1) = sigma(M)`, which closes it under inverses.
- **Steps 1–2.**
  - `x g x^(-1) = [x,g]g ∈ Gamma_s` and `phi_x(g)g^(-1) = [x,g] ∈ M_s`.
  - The kernel is `C_s`, and the preimage of `Ad(M_s)` is `M_s C_s`.
  - `Ad(M_s)` is normal in `Aut_M` because `phi(M_s) = M_s`.
- **Step 3.**
  - `eps_t(phi)(g) ∈ (s^(-1)M_s s) g = M_s g`, using normality of `M` in `G`.
  - Surjectivity onto `Gamma_s` uses `M_s = sigma(tMt^(-1)) <= sigma(tGamma t^(-1))`. Checked.
- **Step 4.** `[s^(-1)xs, g] = s^(-1)[x, sgs^(-1)]s ∈ M_s`.
- **Step 5.** Injectivity on `Q_s` uses NORM for every homomorphism `sigma : G -> S_U`: `sigma(G)` normalizes `C_s`. This is taken from `rigid-compression-defect-normalization-dichotomy` (sofic row PASS by `reviewer`). With `s C_s s^(-1) = C_s` and `s M_s s^(-1) = M_s` the kernel argument closes.
- **Step 6.** `Ad(x)∘iota = iota∘phi` forces `x M_s x^(-1) = phi(M_s) = M_s` and `[x,g] ∈ M_s`.
- **Steps 7–8.** Taken from `relative-defect-compressor-endomorphism-proof` (PASS) and injectivity of `sigma`.
- **Hidden hypotheses.** None found. Property (T) enters exactly where the node says: step 5, through NORM.

## 6. `conjugation-invariant-sizes-miss-relative-defect` (d9793a122): PASS

- **Item 1.** `R(eps_t(q)) = s^(-1)R(q)s`, since `s^(-1)(M_sC_s)s = M_sC_s` by normality of `M` and NORM. Any conjugation-invariant `F` is therefore unchanged.
- **Item 2.** For an injective trace-zero representation, `tr sigma(z) = 0` for `z != 1`, so the support proportion is `1`.
- **Invalidation.** The `invalidates: rnorm-via-support-proportion-size` key is justified.
- **Red-flag check.** Items 1–2 use no property (T) and also hold in `BS(1,2)`, so they prove no nonsoficity. The node states this. It is an obstruction to a method, correctly scoped.

## 7. `kazhdan-quotient-sofic-iff-injective-block-representation` (5ca23228a): PASS

- **(a).**
  - The block representation follows Steps 1–4 of the artifact's Theorem 6, without the triviality hypothesis.
  - Blocks are almost `N`-invariant, so `beta_(gn) = beta_g`.
  - Order-preserving point maps between blocks of almost equal size define `tau_sigma` up to `o(|X_n|)`.
  - Independence of the edit follows because two uniform-Cheeger edits pair their blocks on all but `o(|X_n|)` points.
- **(b), "if".** A nontrivial element of `S_U` has trace `< 1`. Tensor amplification with a diagonal choice gives a sofic approximation. Elek–Szabó already gives soficity from an embedding into `S_U`, so the amplification is correct but not needed.
- **(b), "only if".** The product `rho_n(g) × t_m(gN)` is a sofic approximation of `G`. Generators of `N` act as `rho_n(s) × id`, so product edits `D × {y}` are uniform-Cheeger. Then `tr tau_sigma(q) <= tr t(q) + o(1)`.
- **Scope.** A reformulation, not a permanence theorem, as stated. The circularity remark is correct.

## 8. `char-p-linear-groups-embed-in-projective-leavitt-power-e-groups` (925b9eabd): not re-reviewed

`bh-reviewer` already reviewed this claim (`review-bh-swarm-2026-09-12.md` §5). Verdict: FAIL, corrected forward in 4ba6fa13b. The Whitehead diagonal map is not multiplicative; the repair uses `X ↦ diag(X, I)` with one more tensor factor. It is skipped here to avoid duplicating that review.

## 9. `r-fibre-w-star-bundle-sigma-finite-dim-base-is-trivial` (0bb74f48d): PASS

Read against `research/artifacts/w-bundle-sigma-finite-dim-triviality-2026-09-12.md`.
- **Lemma 1.**
  - `t ↦ ||f(t) - E_(D_m) f(t)||_2` is continuous and decreasing, so Dini applies.
  - Exact units of `M_2 ⊂ D_m' ∩ R` commute with `E_(D_m) f`, so the commutator defect is at most `2||f - E f||_2`.
  - Contractions lift through the onto quotient `q_Y`, and the good set is open.
- **Lemma 2.**
  - **Flip relations.** Checked with `b` good against `F ∪ {a_ij}`:
    - `w^* ≈ w`;
    - `w^2 ≈ Σ a_ii b_jj ≈ 1`;
    - `w a_kl w ≈ Σ_i a_ii b_kl ≈ b_kl`;
    - hence `||h^2 - h||_2 ≤ Cδ` and `||[h,f]||_2 ≤ Cδ`.
  - **Scalar bound.** `exp(iπst) - 1 - (e^(iπs) - 1)t` vanishes at `t = 0, 1`, with bounded quotient by `t(t-1)`. Minor slip: `||w|| ≤ 4`, so `spec h ⊆ [-3/2, 5/2]`, not `[-2,2]`. The constant is still absolute.
  - **Regimes.**
    - `g = κ = 0` gives `c = a`.
    - At every point of `O ∩ C` the estimate covers all `s = g(λ) ∈ [0,1]`.
    - At `g = 1`, `u ≈ 1 - 2h ≈ w`, so `u a u^* ≈ b`, and a convex combination with `b` stays `Cδ`-good.
    - `κ = 1` gives `c = b`.
  - The constant does not depend on dimension.
- **Induction.**
  - The derived length is a successor, by compactness.
  - Monotonicity holds.
  - `X_inf = ∅` gives `dim X < ∞` by the finite sum theorem.
  - O16 applies to `M_Y`; `C = X ∖ O'` has `ℓ(C) ≤ γ`.
  - Then `p = (Re c_11)_+`, with `|t_-| ≤ |t^2 - t|` on `[-1,1]` and `E(p) ≈ 1/2`.
  - Strict density plus `||[p, a - a_k]||_(2,u) ≤ 2||a - a_k||_(2,u)` gives condition (ii) of O15.
- **Remark 2 and Corollary 3.** Checked: Baire in one direction, σ-compact strata in the other.
- **Trust surfaces.** Ozawa Theorem 15 and Corollary 16 as quoted. Novelty was checked only in a bounded way.

## 10. `fpbs-tree-projected-large-rank-strict-thresholds` (2eac9490b): PASS

- **Step 1.** Symmetric choices `g_(t^(-1)) = g_t^(-1)` embed `T_(2n)` injectively, so `p_c <= 1/(2n-1)` by monotonicity.
- **Step 2.**
  - Fibre blocks have row and column sums `<= c_t`. Schur's test gives the block norms.
  - `|<f,Ag>| <= <F, KG>` with `K = vI + Σ m_t ρ(t) <= vI + m A_T` entrywise.
  - Kesten gives `||A_(T_(2n))|| = 2 sqrt(2n-1)`.
- **Steps 3–4.**
  - `T_p <= Σ p^l A^l` entrywise, so `p_(2->2) >= 1/||A||`.
  - Uniqueness plus Harris–FKG make `T_p` unbounded above `p_u`.
- **Table.** Recomputed: `x > 1 + sqrt 3` first holds at `n = 5`; `x > 2 + sqrt 6` first holds at `n = 11`.
- **Scope.** A classical method, correctly credited. Rank 2 is left open.

## 11. `fpbs-rd-quotient-weighted-bubble-gives-critical-l2` (188188f3d): PASS

- **Operator bound.**
  - `S_p` is left convolution by the symmetric `σ_p` on `Q`.
  - Rapid Decay per sphere gives `||λ(σ_R)|| <= C(1+R)^D b_R^(1/2)`.
  - The triangle inequality and Cauchy–Schwarz against `(1+R)^(-1)` give `C (π/sqrt 6) W_D^(1/2)`, with `Σ_(R>=0) (1+R)^(-2) = π^2/6`.
- **Inputs.**
  - The fibre comparison `||T|| <= ||S||` (PASS by `major-reviewer`).
  - Hutchcroft Corollary 2.6, verified from the PDF by `major-reviewer`, turns critical L2 boundedness into `p_c < p_(2->2)`.
- **Scope.** The note that the premise forces a nonamenable `Q` is correct. If `σ_(p_c)` is infinite somewhere, the premise simply fails.

## 12. `integral-spectra-of-small-capacity-are-finite` (ec18bf405): PASS

- **Steps 1–3.**
  - `m_k = Tr(T^k) ∈ Z`.
  - The Stieltjes transform is single-valued on the sphere minus `σ`, with no monodromy, unlike the logarithm.
  - `σ ⊂ R` has connected complement. Pólya applies to `w f(w) = Σ m_k w^(-k)`.
- **Step 4.**
  - A rational Stieltjes transform of a positive measure gives a finitely atomic measure.
  - The trace is faithful, so `supp μ = σ`.
  - Fatou gives `F = P/Q` with `Q(0) = 1`, so the poles are roots of the monic reversed polynomial, hence algebraic integers. Then `m(T) = 0`.
- **Capacity formula.** `cap(p^(-1)E) = cap(E)^(1/2)` for `p = x^2` gives `sqrt(b^2 - a^2)/2`.
- **Trust surface.** Pólya's theorem as imported (`polya-integer-series-rational-off-small-capacity`), not re-read from the 1928 source.
