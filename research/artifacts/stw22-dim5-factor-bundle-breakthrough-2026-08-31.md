# STW XXII: all-factor dimension-five breakthrough audit

## Diffuse induction

Degree-three corner control lifts projection `S^3` maps by Michael `n=2`
and fills the lift by the degree-three unitary modulus. Thus fixed-trace
Grassmannians become `C^3/equi-LC^3`. Michael `n=3` selects the spectral
carrier and both stabilizer lifts over `S^4`; localized Jekel contraction
gives diffuse Stiefel `LC^4/C^4` with the unchanged quadratic modulus.
Relative bands over `B^5` use Michael `n=4`; row amplification gives the
weighted cubic modulus `epsilon_0^3/216000000`.

## Matrix branch

The independently audited codimension-two theorem constructs a rank-5K
carrier, cancels `c_1` on the three-skeleton, and absorbs the remaining
Bott/`c_2` class in a rank-two complement block. It gives weighted
`LC^4/C^4` with modulus `epsilon_0^3/276480`. Half slack and `m>=2` force
every nonzero matrix value to have unused rank at least two.

## Selection

The common smaller modulus, existing type-free lower-semicontinuity, and
global four-connectivity satisfy Michael `n=4` over every compact Hausdorff
base of dimension at most five. The gap proof uses only half-slack values
with arbitrary `m>=2`, so the usual `m->infinity` replication kills every
bounded positive gap trace.