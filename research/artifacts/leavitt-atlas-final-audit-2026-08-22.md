# Leavitt-atlas final audit delta — 2026-08-22

This artifact records only the graph delta from the final audit; the broader
Atlas ledger and literature boundary were already present elsewhere in
Cairn.

## Closed in this pass

- `actual-hard-prefix-generates-s4-over-raw-s3`: each of the four actual
  class-11/class-30 quotient prefixes generates an `S4` with the raw `S3`.
  Adjoining the distinct Leavitt-star word gives an order-168 packet.  The
  exact replay computes true group inverses from source words, so it does not
  repeat the false `q^star=q^{-1}` substitution.
- `separated-channel-identities-retain-rectangular-escape`:
  finite carrier determinants and coefficient equivariance cannot close the
  multiplicity reservoir while they occur in separate tensor factors.

## Frontier after the correction

The cross-chart finite-carrier recovery gate is closed.  The open
`atlas-hard-packet-s3-channel-decoder` must now project actual quotient
relators after exactifying the hard `S4` cells and obtain a system

```text
C X = E
```

where `C` has a fixed nonzero least singular value and every row contains the
same three multiplicity unknowns

```text
R0^* W L0-W,  R1^* W L1-W,  R0^* W L1.
```

Extra independent multiplicity words may not be discarded.  Equivalently,
one may prove the full-packet bicommutant synchronization estimate.  Either
version is matrix-specific and mixed; neither follows from abstract finite
branching data.

The existing Leavitt trace/contraction endpoints remain sufficient once this
mixed estimate is obtained.  No unconditional non-hyperlinear group is
claimed.
