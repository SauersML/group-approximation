# sk-proof-minimal-concrete proposal, part 3: v2, the concrete section with a tower-free extraction

- **This lane now recommends v2 over part 2.**
  - Part 1's items 1–4 and 9–11 still apply to the ring and LEF subsections.
  - Items 5–8 (towers, corner unit, $H_W$, the level ideal through translates of $e_W$) are replaced by the extraction below.
- **Source of the extraction.** sk-proof-minimal-conceptual's edit E4 (2acfcb8559), the explicit form of `local-annihilation-makes-projective-el-simple` (Stepanov's Lemma 4.3 method). It is rewritten here in the note's concrete notation, for $\EL_3$ over $\F_2$, and each identity is checked below.
- **Length.**
  - Disk l.67–227 is 161 source lines. v2 is 105 lines (−56, −35%); v1 (part 2) is 119.
  - The Simplicity subsection is 47 lines, and it replaces l.102–116 plus l.154–227 (89 lines).
  - Full note: 340 → 284 lines.
  - MSI pdflatex, 3 passes (`/scratch.global/sauer354/sk/sk-proof-minimal-concrete/cand2`): 0 errors, 0 overfull boxes, 0 undefined references, 4 pages.
- **Sentences that go stale if v2 is adopted.**
  - Abstract, l.32–33: "with a commutator supported on a finite tower".
  - Introduction, l.58–62: "a nontrivial normal subgroup meets a finite simple matrix group supported on a clopen tower, and therefore contains an elementary matrix".
  - sk-proof-minimal-conceptual E2/E3 propose replacements. E3 cites `Lemma~\ref{lem:simple}`, which v2 does not have, so its wording would say "the simplicity argument" instead.
- **Labels.** `eq:absorb` disappears. Section 2 uses only `eq:elementary` (checked: no remaining `eq:absorb`).
- **(d) Trade-off.**
  - The theorem and the level of rigor are unchanged. v2 is 14 lines shorter than v1 and 56 shorter than the current text.
  - The reader no longer needs towers, $B_m(C)$, absorption, $\LC(C,\GL_d(\F_2))$, or the simplicity of $\GL_d(\F_2)$.
  - In exchange v2 uses the simplicity of $R$, which the current note already proves in five lines.
- **Status:** unreviewed; queued with sk-referee-1.

## Checks for v2's new steps
1. **Local annihilation.**
   - For $f=\sum_jf_ju^j$ with $|j|\le w$ and $s\in\{e_C,e_Cu\}$: $tfs=\sum_jf_je_{W_C}e_{T^jC}u^js'=0$, because $T^jC\cap W_C=\varnothing$. The case $f=1$ is $j=0$.
   - $W$ exists because $X$ is infinite, so the complement of the finite set $\{T^jx\}$ is a nonempty open set, and it contains a nonempty clopen set. $V$ exists by continuity of each $T^j$.
   - Subsets of $V$ keep the property, so the pieces of a common refinement with the letter partition inherit a $W$.
2. **$t(\gamma-I_3)=0$.**
   - Over $\F_2$, $h^{-1}=h$ and $S^2=0$, so $\gamma-I_3=(ghg^{-1}-h)h=(gSg^{-1}-S)(I_3+S)$.
   - The entries of $tgS$ are $tg_{pi}s=0$, and $tS=ts\,E_{ij}=0$.
   - Multiplying $t\gamma=tI_3$ on the right by $\gamma^{-1}$ gives $t\gamma^{-1}=tI_3$.
3. **Some $a$ has $bat\ne0$.** If $bRt=0$ then $RbRt=0$. But $RbR=R$, since $R$ is simple and $b\ne0$. So $Rt=0$ and $t=0$, which is false.
4. **The formula for $\eta$.**
   - $\gamma e_{pl}(at)\gamma^{-1}=I_3+\gamma e_p\,(at\,e_l^{T}\gamma^{-1})=I_3+\gamma e_p\,at\,e_l^{T}$, because $at\gamma^{-1}=atI_3$.
   - Multiply by $e_{pl}(at)^{-1}=I_3-e_p\,at\,e_l^{T}$ and use $e_l^{T}e_p=0$ ($l\ne p$). The result is $I_3+(\gamma-I_3)e_p\,at\,e_l^{T}=I_3+(\gamma-I_3)\,atE_{pl}$.
   - Its $(q,l)$ entry is $bat\ne0$.
5. **$\eta^{-1}$.** $\eta^{-1}-I_3=-\eta^{-1}(\eta-I_3)$ vanishes outside column $l$, and it is nonzero because $\eta\ne I_3$.
6. **$[\eta,e_{dm}(1)]=e_{dl}(c)$ for $d\notin\{m,l\}$.**
   - $\eta E_{dm}=E_{dm}$, since column $d$ of $\eta-I_3$ is zero ($d\ne l$).
   - $E_{dm}\eta^{-1}=e_d(e_m^{T}+c\,e_l^{T})=E_{dm}+cE_{dl}$.
   - So $\eta e_{dm}(1)\eta^{-1}=I_3+E_{dm}+cE_{dl}$. Multiplying by $I_3-E_{dm}$ gives $I_3+cE_{dl}$, because $E_{dm}E_{dm}=0$ ($d\ne m$) and $E_{dl}E_{dm}=0$ ($l\ne d$).
7. **Every position.** With $\{d,l,j\}=\{1,2,3\}$:
   - $e_{dj}(c)=[e_{dl}(c),e_{lj}(1)]$;
   - $e_{jl}(c)=[e_{jd}(1),e_{dl}(c)]$;
   - $e_{jd}(c)=[e_{jl}(c),e_{ld}(1)]$;
   - $e_{lj}(c)=[e_{ld}(1),e_{dj}(c)]$;
   - $e_{ld}(c)=[e_{lj}(c),e_{jd}(1)]$.
8. **$I_N$ is a two-sided ideal.** $e_{ij}(r+r')=e_{ij}(r)e_{ij}(r')$; $e_{ij}(sr)=[e_{ik}(s),e_{kj}(r)]$ and $e_{ij}(rs)=[e_{ik}(r),e_{kj}(s)]$, and $N$ is normal.
9. **Simplicity of $R$** (the argument of disk l.104–109).
   - $e_Ure_U=\sum_jf_je_Ue_{T^jU}u^j=e_Uf_0=e_U$.
   - The ideal contains $e_{T^nU}=u^ne_Uu^{-n}$.
   - $1-\prod_i(1-e_i)$ lies in the ideal generated by the $e_i$.
10. **The scalar case and $Z(R)=\F_2$:** unchanged from part 1, checks 15–16.

## v2 LaTeX (replaces disk l.67–227)
```latex
\section{Proof of Theorem~\ref{thm:main}}

\subsection*{The ring and property (T)}
Write $e_U$ for the indicator of a clopen set $U\subseteq X$. Our
conventions are $(Tx)_n=x_{n+1}$ and
\[
 R=\LC(X,\F_2)\rtimes_T\Z
   =\left\{\sum_j f_j u^j:\text{finite sums}\right\},
 \qquad ufu^{-1}=f\circ T^{-1},
\]
where $\LC$ denotes locally constant functions. Thus $ue_Uu^{-1}=e_{TU}$,
and $R$ is generated by $u^{\pm1}$ and the letter indicators
$e_a=e_{\{x:x_0=a\}}$, since $u^{-n}e_au^n=e_{\{x:x_n=a\}}$.
Put $G=\EL_3(R)$, $e_{ij}(r)=I_3+rE_{ij}$ for $i\ne j$, and
$[g,h]=ghg^{-1}h^{-1}$. The identities
\begin{equation}\label{eq:elementary}
 e_{ij}(r)e_{ij}(s)=e_{ij}(r+s),\qquad
 [e_{ik}(r),e_{kj}(s)]=e_{ij}(rs)\quad(i,j,k\text{ distinct})
\end{equation}
show that $G$ is generated by the $e_{ij}(s)$ with
$s\in\{u,u^{-1}\}\cup\{e_a:a\in A\}$, because $\sum_ae_a=1$.
Ershov and Jaikin-Zapirain proved that $\EL_n$ of every finitely
generated unital associative ring has property~(T) for
$n\ge3$~\cite[Theorem~1.1]{EJZ}. So $G$ has property~(T). It is infinite
because $e_{12}(\LC(X,\F_2))$ is infinite.

\subsection*{Finite models}
A group is LEF if every finite subset embeds injectively into a finite
group, preserving all products that stay in that subset.

Fix $\ell$. By minimality every word of $X$ of length $2\ell+1$ occurs in
every long enough word of $X$. So a point of $X$ contains two occurrences
of a word $v$ of length $2\ell$ at a distance $L$ that can be taken as
large as we like. Let $w$ be the word of length $L$ that begins at the
first occurrence. Then $wv$ occurs in $X$ and $w$ contains every word of
$X$ of length $2\ell+1$, so the periodic sequence $y=w^\infty$ has exactly
the same words of length $2\ell+1$ as $X$.

Let $P\delta_n=\delta_{n+1}$ on $\F_2^{\Z/L\Z}$. If $f\in\LC(X,\F_2)$
depends only on the coordinates in $[-\rho,\rho]$, where $\rho\le\ell$,
let $D(f)$ be the diagonal matrix whose $n$-th entry is the value of $f$
on the word $y_{n-\rho}\cdots y_{n+\rho}$. Send
\[
  \sum_j f_ju^j\ \longmapsto\ \sum_j D(f_j)P^j.
\]
Since $P^iD(f)P^{-i}=D(f\circ T^{-i})$, this map preserves the sums and
products among finitely many elements of $R$ once $\ell$ exceeds the
windows of all coefficients involved, including the shifted ones
$f\circ T^{-i}$. Every allowed window occurs in $y$, so nonzero
coefficients have nonzero images. If $L$ exceeds twice every exponent,
nonzero elements also have nonzero images.

For a finite set $F\subseteq G$, choose $\ell$ and $L$ large enough for
the entries of $F\cup F^{-1}$, their differences, and the sums and
products used in multiplying these matrices. Applying the map entrywise
gives an injective map $F\to\GL_{3L}(\F_2)$ preserving the products that
stay in $F$. So $G$ is LEF, hence sofic and hyperlinear~\cite{Pestov}.

\subsection*{Simplicity}
The ring $R$ is simple with center $\F_2$~\cite{BCFS,ClarkEdie,Steinberg}.
Indeed, $T$ has no periodic points, as $X$ is minimal and infinite. After
multiplication by a power of $u$, a nonzero ideal contains some
$r=\sum_jf_ju^j$ with $f_0\ne0$. A nonempty clopen $U\subseteq\{f_0=1\}$
disjoint from $T^jU$ for the nonzero exponents $j$ of $r$ gives
$e_Ure_U=e_U$. Finitely many translates of $U$ cover $X$, so the ideal
contains $1=1-\prod_i(1-e_{T^{n_i}U})$. If $c=\sum_jf_ju^j$ is central and
$j\ne0$, the coefficient $f_j(e_{T^jV}-e_V)$ of $u^j$ in $ce_V-e_Vc$
vanishes for every clopen $V$, and a clopen $V$ containing $x$ but not
$T^{-j}x$ gives $f_j(x)=0$. Then $cu=uc$ makes $f_0$ invariant, hence
constant by minimality.

Let $1\ne N\trianglelefteq G$ and $1\ne g\in N$, and let $w$ bound the
absolute values of the exponents in the entries of $g$. Each $x\in X$ has
a clopen neighborhood $V$, and there is a nonempty clopen $W$, with
$T^jV\cap W=\varnothing$ for $|j|\le w$: choose $W$ off the finite set
$\{T^jx:|j|\le w\}$, then $V$. By compactness there are a finite clopen
partition $\mathcal P$ refining the letter partition and nonempty clopen
sets $W_C$ with $T^jC\cap W_C=\varnothing$ for $C\in\mathcal P$ and
$|j|\le w$. Some $h=e_{ij}(s)$ with $s\in\{e_C,e_Cu:C\in\mathcal P\}$ does
not commute with $g$. Otherwise $g$ commutes with every
$e_{ij}(1)=\prod_Ce_{ij}(e_C)$, so $g=cI_3$ with $c\in R$. Then $c$
commutes with every $e_C$ and $e_Cu$, hence with $u^{\pm1}$ and every
$e_a$, so $c\in Z(R)^\times=\{1\}$, a contradiction.

Put $t=e_{W_C}$, $S=sE_{ij}$ and $\gamma=[g,h]\in N\setminus\{1\}$, so
$h^{\pm1}=I_3+S$. Every entry $f$ of $g$, and $f=1$, satisfies $tfs=0$. So
\[
 t(\gamma-I_3)=t\,(gSg^{-1}-S)(I_3+S)=0,
 \qquad\text{hence}\qquad t\gamma^{-1}=tI_3.
\]
Choose $p,q$ with $b=(\gamma-I_3)_{qp}\ne0$, then $a\in R$ with $bat\ne0$,
which exists because $R$ is simple, and $l\ne p$. As $at\gamma^{-1}=atI_3$,
\[
 \eta=[\gamma,e_{pl}(at)]=I_3+(\gamma-I_3)\,at\,E_{pl}
\]
is an element of $N$ with $\eta\ne I_3$, and $\eta-I_3$ vanishes outside
column~$l$. So does $\eta^{-1}-I_3=-\eta^{-1}(\eta-I_3)$, which has an
entry $c\ne0$ in some row $m$. For $d\notin\{m,l\}$ we get
$[\eta,e_{dm}(1)]=e_{dl}(c)\in N$.

Finally, commutators with the $e_{ij}(1)$ move $e_{dl}(c)$ to every
position by~\eqref{eq:elementary}, and
$I_N=\{r\in R:e_{ij}(r)\in N\text{ for all }i\ne j\}$ is a two-sided
ideal. It contains $c\ne0$, so $I_N=R$ because $R$ is simple. Thus $N$
contains every $e_{ij}(r)$, and $N=G$.\hfill$\square$
```
