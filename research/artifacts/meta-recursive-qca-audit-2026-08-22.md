# Meta-recursive QCA integration audit — 2026-08-22

The supplied QCA report was screened against Cairn rather than copied as a
parallel branch.  Its rigorous content maps as follows.

| Report component | Cairn integration |
|---|---|
| finite catch for exhaustive existential QCA challenges | `bespoke-extension-nemesis-library-defeats-diagonal-schedules` and `finite-nemesis-subcover-has-a-uniform-existential-residual-gap` |
| capacity-prefix and local-depth firewall | `exact-code-prefixes-have-matrix-ultraproduct-model`, with the canonical-mass qualification in `finite-static-nemesis-core-needs-canonical-base-embedding` |
| static-core principle | corrected by `finite-static-nemesis-core-needs-canonical-base-embedding` |
| normalized-HS/maximally-entangled verifier identities | `maximally-entangled-vectorization-is-hs-isometry` and `consistency-defect-is-state-dependent-distance` |
| finite non-CE BCS energy floor | `non-ce-bcs-has-robust-approximate-energy-gap` and its proof |
| finite tracial verifier-to-group endpoint | `central-sign-bcs-energy-decoder-implies-nonhyperlinear` |
| affine context obstruction | `affine-auxiliary-control-flow-barrier` and `twisted-group-basis-forces-affine-context-support` |
| stabilizer synchronization after exact rounding | `relative-commutant-coset-field-removes-stabilizer` |
| sharply two-transitive Clifford tape no-go | `sharply-two-transitive-augmentation-lamp-host-audit` |
| final nonlinear predicate payment cell | remains `bcs-projective-kernel-phase-compilation` / `tailored-checks-to-common-marked-group-extensions` |

## Corrections retained during integration

1. Exact extendability is closed for fixed finite gadgets over complete
   matrix-ultraproduct tracial types.  For quantifier-free scalar profiles
   with an externally restricted witness correspondence, continuity is too
   strong; lower semicontinuity/closed nonextension neighborhoods are the safe
   formulation.
2. An infinite exhaustive library has a finite residual subcover, but that
   does not alone give a finitely presented group.  The base must itself be
   finitely presented (or separately and faithfully compiled), the selected
   gadgets need one simultaneous exact completion, and the final extension
   must preserve the base canonical profile.
3. A common exact representation retaining the mark does not imply base
   injectivity or canonical-profile preservation.
4. Finite tracial soundness means a positive infimum over all dimensions,
   equivalently exclusion of a zero-energy matrix-ultraproduct strategy.  The
   absence of an exact strategy in each individual dimension is insufficient.
5. In a central-sign decoder, ambient energy is carrier mass times normalized
   corner energy.  The approximate central involution must be spectrally
   rounded and the decoded letters block/polar corrected before applying the
   BCS gap.
6. Parity relations describe an affine character space, but an arbitrary
   representation can occupy a subset of it.  Uniform support requires a
   canonical twisted-regular or otherwise authenticated sector.
7. Relative-commutant synchronization requires an actual positive-density
   reducing carrier and exact actor/commutant correction.  Flexible dimension
   alone does not return exact upstairs relations to a downstairs corner, and
   sharp two-transitivity controls pairs but not general QCA triple patterns.
8. There is no additional simultaneous-extension obstruction after fixing one
   complete base type.  If every disjoint finite gadget has existential
   residual zero on that same type, countable saturation realizes all gadgets
   together; for a matricial type, an ultrapower remains a matrix ultraproduct
   after flattening.  The invalid quantifier is instead `for every gadget
   there exists some marked base model`: the base types may differ.  This
   correction does not imply canonical base embedding.

## Net conclusion

Self-reference can select or describe a semantic obstruction, but cannot be
its normalized-HS source.  If a meta-recursive QCA excludes every separated
matrix-ultraproduct type, compactness exposes a finite static core.  If every
finite prefix only exceeds a larger finite capacity, diagonalization builds an
ultraproduct model.  The remaining live construction is finite: transduce a
fixed non-CE BCS forbidden-projection energy into ordinary group-word energy
on one common positive-density central-sign carrier.

## Self-aware canonical diagonal addendum — 22 August 2026

The later note *Self-Aware Canonical Diagonal Toward a Non-Hyperlinear Group*
was integrated into the same lane. It does not establish the root
`non-hyperlinear-group`; its proved content sharpens existing nodes and its
controlled-reflection proposal names one local part of the already open
transducer. The table below is the canonical ownership map, so the note is
not reproduced as a parallel family of claims.

| Note sections | Canonical Cairn owner | Integration result |
|---|---|---|
| 1--2, canonical microstates and reverse Kleene | `canonical-profile-kleene-higman` and its proof route | already established; the finite rational mark and r.e. zero-certificate argument are the canonical formulation |
| 3, fixed-mass type compactification and diagonal escape | `bespoke-extension-nemesis-library-defeats-diagonal-schedules`, `finite-nemesis-subcover-has-a-uniform-existential-residual-gap`, and `canonical-profile-collapse-compactness` | already established on complete extension types; scalar canonical word moments alone are not an approximation fingerprint |
| 4, halting-word HS area obstruction | `hs-dehn-modulus-bounded-by-dehn-function`, `halt-now-certificate-area-still-needs-a-crossing-envelope`, and `conjugacy-compressed-dimension-trigger` | already established; runtime-dependent van Kampen area supplies no self-referential crossing modulus |
| 5, marked involution carrier | `central-packet-sign-supplies-marked-carrier` and `central-involution-corner-exactification` | sharpened with the direct estimate `tau(Q)>=(alpha-epsilon)^2/4`; canonical nontrivial involutions have limiting negative density `1/2` |
| 6--7, finite truth-table and transformation-groupoid corner | `finite-one-hot-algebraic-corners-reduce-to-stabilizers` and its proof route | expanded with the rational idempotents `e_q`, exact action of every Boolean lamp `a_f`, trace `|Q|/2^|Q|`, and `z_Q C[A rtimes P]z_Q ~= C^Q rtimes P` |
| 8 and Lemma 8.1, controlled reflection and affine no-go | `affine-auxiliary-control-flow-barrier` and `selector-gated-equality-is-not-affine-pp-definable` | expanded with the literal nonaffine graph `y=cx`; commuting affine auxiliaries cannot implement `Y=X^c` |
| 9, 21 and 22, canonical robust router/CRR | `tailored-checks-to-common-marked-group-extensions`, `hyperoctahedral-router-interface-for-fanizza`, and `fanizza-coherent-verifier-wandering-promotion` | retained as an open local interface `qYq=q_+ + q_-X`, allowed to depend on a positive mass floor |
| 10 and 27, proposed router closure theorem | `fanizza-canonical-profile-groupification`, `common-nemesis-via-tailored-check-groupification`, and `approximation-specific-nemesis-kleene-route` | corrected: local routers imply the compiler only together with common-carrier/coherent-verifier alignment across noncommuting contexts |
| 11, 18 and 19, QCA layers, scale-by-scale transducers and pseudocode | `meta-recursive-qca-reduces-to-finite-static-tracial-transducer` and `common-complete-approximation-specific-nemesis-compiler` | represented semantically; compactness gives non-effective finite subcovers, so the pseudocode's “certify a finite cover” line needs a separately enumerable certificate and cannot be assumed |
| 12, canonical rather than universal profiles | `canonical-profile-kleene-higman`, `fanizza-canonical-profile-groupification`, and `unconditional-bcs-ultraproduct-decoder-forces-ce` | already canonical; the trivial homomorphism is intentionally outside the quantified profile |
| 13, extraspecial/Clifford authentication | `extraspecial-common-gauge-fourier-controller`, `controlled-reflection-cell-has-free-orientation`, and the central-sign carrier nodes | the unique marked spin block is reusable, but its multiplicity orientation remains free |
| 14, authenticated Fredkin proposal | `controlled-reflection-cell-has-free-orientation`, `right-regular-covariance-forces-one-common-router`, and `second-layer-lookup-cancellation-no-go` | kept as architecture, not a theorem: Schur multiplicity is shared only after extra coupling, and a fixed finite unitary cannot localize `I tensor X` to one control channel |
| 15, local readable packets | `context-local-schur-packet-marked-extension`, `selector-correlated-direct-power-router`, and the local part of `tailored-checks-to-common-marked-group-extensions` | hidden global state size is unnecessary; the unresolved datum is cross-context carrier/holonomy coherence |
| 16, infinite evaluation orbit | `finite-one-hot-algebraic-corners-reduce-to-stabilizers` | expanded with the nonclosed evaluation-orbit proof for `direct_sum_X C_2`, separate from the independent-register zero-density proof |
| 17, exact algebraic corner map | `nonce-bcs-unital-algebraic-corner-map-forces-nonhyperlinear` and `algebraic-bcs-corner-has-relator-energy-decoder` | already stronger: finite algebraic support plus van Kampen telescoping supplies the HS payment automatically |
| 20 and 26, failure modes/router audit | the obstruction nodes cited above, plus `finite-product-selected-type-mark-does-not-align-carriers`, `embedded-packet-projection-has-no-relator-ideal-certificate`, and `post-candidate-nemesis-has-a-finality-dilemma` | preserved as audit constraints rather than duplicate claims |
| 23--24, commuting compression and LCS endpoint | `re-oriented-lcs-compiler-gives-nonhyperlinear-group`, `perfect-lcs-gap-implies-nonhyperlinear`, and `tailored-gap-is-lcs-or-has-a-nonaffine-edge` | already integrated; if every full accepted relation is affine, the existing LCS endpoint applies |
| 25, 28 and 29, synthesis and references | this artifact and the cited nodes | the synthesis is retained; bibliography entries are construction inputs, not independent Cairn claims |

### Exact local router interface retained

For a finite context packet `A=F_2^Omega`, write

```text
q=sum_(omega in Omega)e_omega,
P_C=(q-Cq)/2,
q=q_+ + q_-.
```

A `beta`-local canonical controlled-reflection router is a finite ordinary
group extension with a named involution `Y` such that intended exact models
satisfy

```text
qYq=q[(1-P_C)+P_C X]q=q_+ + q_-X,                     (CRR1)
```

and every canonical approximate representation retaining `q`-mass at least
`beta` satisfies the same identity asymptotically after corner rounding in
the normalized corner Hilbert--Schmidt norm. Dependence on `beta` is allowed.
`affine-auxiliary-control-flow-barrier` proves that a successful gadget must
use genuinely noncommutative data.

This is deliberately recorded inside the existing open transducer rather
than as a new duplicate claim. Even `(CRR1)` for every context does not align
the different `q_c` carriers. The valid closure implication is

```text
local canonical routers
+ common positive-density carrier / coherent verifier return
+ ordinary-relator payment
=> fanizza-canonical-profile-groupification
=> canonical-profile-kleene-higman
=> a finitely presented nonhyperlinear group.                    (CRR2)
```

The first and last arrows are accounted for in Cairn. The common-carrier
return in the middle remains open.

### Construction inputs named in the supplied note

The note attributes its surrounding verifier/compression architecture to the
following inputs. They are retained here as provenance, not promoted to
additional mathematical claims by this integration:

- Junqiao Lin, *MIP(co) = coRE*, arXiv:2510.07162;
- Aareyan Manzoor, *There is an equivalence relation whose von Neumann algebra
  is not Connes embeddable*, arXiv:2502.06697;
- Lewis Bowen, Michael Chapman and Thomas Vidick, the TailoredMIP construction,
  arXiv:2501.00173;
- Connor Paddock and William Slofstra, the LCS/solution-group separation
  framework, arXiv:2310.07901; and
- Zhengfeng Ji, Anand Natarajan, Thomas Vidick, John Wright and Henry Yuen,
  *MIP-star = RE*, arXiv:2001.04383.

The note's `D=3` regular-representation computation is recorded as a sanity
check only. The exact character-orthogonality proof in
`finite-one-hot-stabilizer-reduction-proof` is the canonical certificate, so
no duplicate computational claim or experiment node was added.
