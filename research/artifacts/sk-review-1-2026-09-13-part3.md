# sk-review-1, part 3: sk-proof-minimal-concrete's full proof section and sk-simplicity-short-b

- Reviewer: lane sk-referee-1, a Claude Code agent. This is not a human review.
- Base: frozen note md5 7b3dc4ec92b4581553bc0dc2b0edcb99. Main tip at review: 2c1af4e1d2.
- Every step was re-derived by hand.

## 1. sk-proof-minimal-concrete part 2 (candidate for l.67-227, 119 lines): PASS
No GAP, no ERROR. Checked sentence by sentence:
- **The ring and (T).**
  - u^{-t}e_au^t = e_{\{x_t=a\}} ✓.
  - e_ij(1) = ∏_a e_ij(e_a), so s = 1 is redundant ✓.
  - EJZ, and infiniteness ✓.
- **Finite models.**
  - Uniform recurrence gives two occurrences of v at distance L, as large as wanted ✓.
  - w = the length-L word from the first occurrence, so wv is a word of X. Every window of y = w^∞ lies in a copy of wv, and w contains all words of length 2ℓ+1 once L exceeds the recurrence bound ✓.
  - D(f): the n-th entry is the value of f on y_{n-ρ}⋯y_{n+ρ} ✓, which is the convention that makes P^iD(f)P^{-i} = D(f∘T^{-i}).
  - Separation for L > 2·max|j| ✓.
  - Entrywise map on F ∪ F^{-1}: the products used in multiplying these matrices include gg^{-1}, so the images are invertible ✓.
- **Towers.**
  - E_ab(W)E_cd(W') = δ_bc E_ad(W∩W') for 0<|c-b| ≤ 2m ✓.
  - Absorption rB_{m-w}(C)r' ⊆ B_m(C) ✓.
  - {k : k^{±1}-I_3 ∈ M_3(B_m(C))} is a group: (I+X)(I+X') = I + (X+X'+XX'). The map k ↦ I_d + Φ(k-I_3) is a group isomorphism onto LC(C, GL_d(F_2)), with inverse κ ↦ I_3 + Φ^{-1}(κ - I_d) ✓.
  - H_W ⊆ G: for p ≠ q, I_d + 1_W E_{(p,a),(q,b)} corresponds to e_pq(E_ab(W)). For p = q, use [t_xy, t_yz] = t_xz with y = (q,c), q ≠ p ✓.
- **Centre of R.** The u^j coefficient of ce_V - e_Vc is f_j(e_{T^jV} - e_V). At x ∈ V with T^{-j}x ∉ V its value is f_j(x). Invariance of f_0 and minimality ✓.
- **Noncommuting h.**
  - g = cI_3.
  - c commutes with e_C and e_Cu, hence with u = Σ_C e_Cu, with u^{-1}, and with e_a = Σ_{C⊆[x_0=a]} e_C, since 𝒫 refines the letter partition.
  - So c ∈ Z(R)^× = {1} ✓.
- **Commutator.**
  - h^{±1} = I_3 + S with S² = 0.
  - k - I_3 = (gSg^{-1} - S)(I_3+S) and k^{-1} - I_3 = (I_3+S)(gSg^{-1} - S) ✓.
  - E_{0,-1}(C) = e_Cu ✓.
  - Absorption with m = w+1 ✓.
- **κ and conjugation.** Conjugation by k on H_W is pointwise conjugation by κ under the group isomorphism above, so it is conjugation by κ_0 on W. This closes G3 of sk-referee-1's report ✓. d ≥ 9 ✓.
- **Level ideal and covering.** ✓ (review part 1 §2).

**Optional notes on the text.**
- C1. The centre paragraph separates x from T^{-j}x before the text says that T has no periodic points, which comes in the next paragraph. Move that clause up, or add "(T has no periodic points)".
- C2. "Put $G=\EL_3(R)$" still does not say that EL_3(R) is the subgroup generated by the e_ij(r). This is sk-referee-1 P5.
- C3. The set display {Σ_j f_ju^j : finite sums} still does not state that the coefficients are unique. The centre and separation steps use uniqueness. This is sk-referee-1 P6.
- C4. "Every allowed window occurs in $y$": "allowed" is undefined. "Every word of $X$ of length $2\ell+1$ occurs in $y$" says it.
- C5. "hence sofic" goes against the prose doctrine. Use "and so".

## 2. `binary-subshift-normal-subgroups-contain-tower-gl-copy` + `-proof` and its note text (sk-simplicity-short-b): PASS
The mechanism is the same as sk-simplicity-short-a's (part 1 §1), and both landed within minutes of each other. Credit both. The binary node adds covering by translates in place of ring simplicity plus a level ideal, as does sk-proof-minimal-concrete's level-ideal step.

**Route steps.**
1. **Small sets.**
   - Both conditions pass to clopen subsets.
   - Every point has a small neighborhood, because T has no periodic points and the finitely many f∘T^i are locally constant.
   - Disjointifying a finite subcover gives the decomposition ✓.
2. **Noncommuting h.**
   - Additivity extends commutation from small V to all clopen V ✓. V = X gives g = cI_3 ✓.
   - e_Vc - ce_V = Σ_j f_j(e_V - e_{T^jV})u^j. Its value at x is f_j(x)(1_V(x) - 1_V(T^{-j}x)) ✓.
   - c = f_0 is a unit of R lying in LC(X,F_2). The u^0 coefficient of cc^{-1} = 1 gives c·c'_0 = 1, so c = 1 ✓.
3. **Tower copy.**
   - E_abE_cd = δ_bcE_ad ✓.
   - φ is multiplicative and injective (nonzero matrix units), and φ(I_d) is the idempotent e of the tower.
   - (1-e)E_ab = 0, and E_ab(1-e) = e_{T^aV}((1-e)∘T^{b-a})u^{a-b} = 0 because T^bV lies in the tower. So A ↦ I_3 - φ(I_d) + φ(A) is an injective homomorphism ✓.
   - Transvections as in §1 ✓.
4. **The commutator lies in H_V.**
   - fu^ie_Vf'u^j = f e_{T^iV}(f'∘T^{-i})u^{i+j}. At T^ix with x ∈ V its coefficient is f(T^ix)f'(x), constant on V, so the product is 0 or E_{i,-j} ✓.
   - Y = g e_V E_ij g^{-1} ∈ φ(M_d) ✓, and h = h^{-1} = I_3 + E_00E_ij.
   - k = (I+Y)h and k^{-1} = h(I+Y), since g h^{-1} g^{-1} - I = Y in characteristic 2 ✓.
   - Both are I + φ(·). Multiplicativity plus injectivity of φ make I_d + X invertible with image k ✓.
5. **H_V ⊆ N** by simplicity of GL_d(F_2), d = 3(2w+1) ≥ 3. GL_3(F_2) of order 168 covers w = 0 ✓.
6. **Spreading.**
   - [e_ij(r), e_jl(e_V)] = e_il(re_V) and [e_il(re_V), e_lj(s)] = e_ij(re_Vs), each with one factor in N ✓.
   - 1 = 1 - ∏_n(1-e_{T^nV}) is a sum of monomials containing some u^ne_Vu^{-n}, each of the form re_Vs. So t = Σ t r e_V s for every t ∈ R ✓.
   - Additivity gives every e_ij(t) ∈ N, so N = G ✓.

**Note text (proposal §3), sentence by sentence:** matches the route ✓. Deletions §1 (l.78-82) and §2 (l.101-116): nothing else in the note uses the partition remark, ring simplicity, Z(R) or Z(G). Greps: `Z(` occurs only at l.102, 116, 187; `\cite{BCFS` only at l.103. `eq:absorb` is referenced only at l.168 and l.195 ✓.

**Optional notes on the text.**
- B1. Three kinds of E in one paragraph: 3×3 units E_ij, tower units E_ab, and d×d units E_{(p,a),(q,b)}, meeting in "E_{(p,a),(q,b)} ↦ E_abE_pq". Rename the tower units, e.g. to ε_ab.
- B2. "So $c\in\LC(X,\F_2)^\times=\{1\}$" skips why c is a unit of LC. The u^0 coefficient of cc^{-1} = 1 gives it. +0 lines with "(compare constant coefficients in $cc^{-1}=1$)".
- B3. "So $k-I_3$ and $k^{-1}-I_3$ lie in $\varphi(M_d(\F_2))$, and $k\in H_V$" uses the injectivity of φ. "embeds" in the previous paragraph implies it, but "since $\varphi$ is injective" would name it.
- B4. The node body says "ESTABLISHED (unreviewed; review requested from sk-referee-1)". The owner may cite this review.

## 3. Choosing among the three simplicity texts for revision 1 (all PASS)
| text | source lines for l.154-227 | ring paragraph l.102-116 | partition remark l.78-82 | net for the note |
|---|---|---|---|---|
| sk-simplicity-short-a part 1 | 47 | l.110-116 deleted; ring simplicity kept | kept | -34 |
| sk-proof-minimal-concrete part 2 (whole l.67-227) | whole section 119 | ring simplicity deleted; centre kept (4 lines) | moved | -42 |
| sk-simplicity-short-b §3 | 48 | all deleted | deleted | -47 |

- All three are correct.
- short-b is the shortest and needs neither simplicity nor the centre of R.
- minimal-concrete also rewrites the finite-models and ring sections. Its finite-models text is correct, and sk-lef-short Replacement B (review part 2) is 9 lines shorter than the current section.
- short-b's simplicity section and Replacement B's finite-models section are independent, so they combine cleanly.
