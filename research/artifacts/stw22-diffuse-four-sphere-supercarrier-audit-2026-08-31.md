# Audit: factor-uniform diffuse four-sphere supercarriers

## Inductive step

The preceding diffuse Stiefel `LC^3/C^3` theorem supplies exactly one more
degree of control. Projection `S^3` maps lift by Michael `n=2` through
stabilizers that are `C^2` and equi-`LC^2`; the lifted unitary sphere fills
by `LC^3`. Thus fixed-trace Grassmannians become `C^3` and equi-`LC^3`.

The inclusive spectral multifunction over `S^4` is unchanged analytically.
Michael `n=3` selects its carrier. Both localized conjugator
multifunctions also use `n=3`, since their corner-unitary stabilizers are
`C^3` and equi-`LC^3`. Jekel contractions remain quantitatively confined
by carrier trace.

## Index table

```text
projection S^3 lift: Michael n=2
carrier over S^4:    Michael n=3
pair lifts over S^4: Michael n=3
relative B^5 bands:  Michael n=4
```

The same quadratic Stiefel radius works because the analytic supercarrier
trace estimate and direct-rotation constants do not depend on sphere
dimension.