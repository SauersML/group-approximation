# Wave 4: actual dual-number images and their central extensions

Date: 2026-09-20. Lane: ghb7_matching.

**Result.** For R=F_7[t]/(t^2), the scalar CCKW parameters x=y=1,z=1+t generate the full Sp_4(R). This finite group is perfect and has trivial integral Schur multiplier. Consequently no central extension of this actual finite image detects the canonical K7 central generator. This is stronger than excluding only stable or symplectic Steinberg pushouts.

The x=y=1,z=t image is also identified exactly as a semidirect product. Its central detection question remains open here. No conclusion over all finite quotients is claimed.

## 1. Matrices, module, and reproducible certificates

Use indices 0,1,2,3 and

    A=I+E_03+E_12,
    B=I+E_10-E_23,
    C_u=I+u E_31,
    J=[[0,-I_2],[I_2,0]].

All three matrices preserve J over any commutative F_7-algebra. Put S=Sp(J,F_7). The existing [base-image theorem](../ghb7-quotient-image-is-sp4-f7.md) proves that A,B,C_1 generate S, of order 276595200. Let

    V={X in Mat_4(F_7): X^T J+JX=0}.

This has dimension ten, with S-action g.X=gXg^-1. Reduction of Sp(J,R) has kernel 1+tV. Constant matrices split reduction, so

    Sp(J,R) = V semidirect S.

The script [dual_number.py](../../experiments/hrf-wave4-dual-number-2026-09-20/dual_number.py) uses only standard-library integer arithmetic modulo seven. It builds the ten-element basis

    X=[[P,Q],[R,-P^T]],  P arbitrary 2x2, Q and R symmetric,

in coordinate order

    P00,P01,P10,P11,Q00,Q01,Q11,R00,R01,R11.

It derives the three 10x10 adjoint matrices directly from the 4x4 matrices. Every matrix order used below is verified by exact multiplication. Gaussian elimination is performed modulo seven; the decisive ranks are asserted. Output is [dual_number.json](../../experiments/hrf-wave4-dual-number-2026-09-20/dual_number.json).

## 2. The nondegenerate image is the entire dual-number group

Let Q_1=<A,B,C_(1+t)>. Its reduction is S. The exact calculation gives

    (A C_(1+t) B C_(1+t))^24 = I+tN,

where

    N=[[4,3,2,2],
       [2,6,2,5],
       [5,1,3,5],
       [1,6,4,1]].

The adjoint orbit of N under A,B,C_1 spans all ten dimensions of V. The script proves this by repeatedly adjoining the images of a row-reduced spanning set until its dimension stabilizes; the final rank is ten.

The kernel Q_1->S is an additive F_7-subspace of V. Conjugation on that kernel depends only on reduction, so it contains the S-submodule generated by N. Thus it contains V, and Q_1 is the full inverse image of S, namely Sp(J,R). In particular

    |Q_1|=7^10 * 276595200.

The invariant-vector constraint matrix for V has rank ten. The trace pairing (X,Y)->tr(XY) is S-invariant and has rank ten, hence V is self-dual. Therefore V has zero coinvariants as well. Since S is perfect, the semidirect product Q_1 is perfect. For K=ker(GHB7->C_7), the image of K in Q_1 is all of Q_1: its quotient is a quotient of C_7 and also a quotient of a perfect group.

## 3. Exact vanishing of H^1(S,V)

A 1-cocycle f:S->V is determined by its three values f(A),f(B),f(C_1), a vector of thirty unknowns. For a positive word w=s_1...s_l, the cocycle identity gives

    f(w)=sum_(j=1)^l Ad(s_1...s_(j-1)) f(s_j).

Whenever the matrix w has order n, every cocycle satisfies

    (I+Ad(w)+...+Ad(w)^(n-1)) f(w)=0.

The following fifteen verified matrix relations produce the indicated cumulative ranks on these thirty unknowns:

| word w | order n | cumulative rank |
|---|---:|---:|
| ac | 7 | 1 |
| bc | 7 | 2 |
| aac | 7 | 3 |
| bbc | 7 | 4 |
| abac | 7 | 5 |
| abcb | 7 | 6 |
| acbc | 24 | 8 |
| aabcb | 7 | 9 |
| aacbc | 8 | 11 |
| abacc | 7 | 12 |
| acacb | 12 | 14 |
| acbbc | 8 | 16 |
| acbcb | 12 | 18 |
| acbcc | 8 | 19 |
| aaabcb | 7 | 20 |

Thus Z^1(S,V) has dimension at most ten. The coboundary map

    v -> ((Ad(A)-I)v,(Ad(B)-I)v,(Ad(C_1)-I)v)

has rank ten, as verified separately. Coboundaries satisfy every genuine matrix relation, so B^1(S,V) is a ten-dimensional subspace of Z^1(S,V). Equality follows and H^1(S,V)=0.

This argument does not assume the fifteen relations present S. Only their validity is needed to bound the cocycle space above, while the coboundaries bound it below.

## 4. The full integral Schur multiplier vanishes

The exterior-square representation on Lambda^2 V has dimension 45. Stacking the three matrices Lambda^2 Ad(g)-I gives rank 45. Thus (Lambda^2 V)^S=0. The nondegenerate trace pairing induces a nondegenerate invariant pairing on Lambda^2 V, so the coinvariant space (Lambda^2 V)_S also vanishes.

Self-duality of V and duality of the finite-field bar chain/cochain complexes give

    H_1(S,V)^* = H^1(S,V^*) = 0.

Here integral group homology with the exponent-seven module V is computed by the same F_7 bar complex; no rational or characteristic-zero substitution is used.

Now apply the integral homological Lyndon--Hochschild--Serre spectral sequence to

    1 -> V -> Q_1 -> S -> 1.

Its three entries of total degree two are

    E2_(2,0)=H_2(S;Z)=0,
    E2_(1,1)=H_1(S,V)=0,
    E2_(0,2)=H_0(S,H_2(V;Z))=(Lambda^2_F7 V)_S=0.

For an elementary abelian group V, H_2(V;Z)=Lambda^2_Z V=Lambda^2_F7 V. The first vanishing is Steinberg's finite-field central-closure theorem, with the precise primary citation already recorded in [the wave-three visibility proof](hyperbolic-rf-wave3-visibility-2026-09-20.md), Section 3. Every associated graded piece of H_2(Q_1;Z) therefore vanishes, proving

    H_2(Sp_4(F_7[t]/(t^2));Z)=0.

In particular H^2(Q_1;F_7)=0. Because Q_1 is perfect, all its central extensions by a trivial Q_1-module split. This conclusion concerns the entire actual finite group, not merely a named construction of central extensions.

## 5. Exclusion of canonical central detectors

Let B_c0 be the canonical central Z-extension of K, with generator z. The [primitive-evaluation theorem](../wave3-canonical-k7-class-has-primitive-evaluation.md) gives z in [B_c0,B_c0]. For any finite image F of B_c0, the cyclic subgroup D=<image(z)> is central and the five-term homology sequence gives a surjection

    H_2(F/D;Z) -> D.

If F/D is isomorphic to Q_1, Section 4 forces D=1. Equivalently no nonzero reduction c0 mod m, m>1, can inflate from this actual Q_1 along the CCKW map K->Q_1. Thus the entire first-order nondegenerate image fails as a canonical centre detector, at every central order.

This says nothing about central extensions of proper subgroups or different finite quotients of K; vanishing multipliers are not inherited by arbitrary subgroups.

## 6. The degenerate z=t image is a different group

For Q_0=<A,B,C_t>, reduction gives U=<A,B>, the Heisenberg group of order 7^3, while A,B themselves are constant matrices. The congruence kernel is the additive span of the U-orbit of E_31. The same exact script computes this orbit span to have dimension ten. Therefore

    Q_0=V semidirect U,   |Q_0|=7^13.

The U-coinvariants of V have dimension one: the difference span generated by Ad(A)-I and Ad(B)-I has rank nine on V. The invariant functional

    lambda(X)=X_31

generates the dual coinvariants and takes value one on E_31. Let chi_U:U->F_7 send both A and B to one. Then the original character a,b,c->1 descends to Q_0 as

    chi_0((I+tX)h)=lambda(X)+chi_U(h).

Thus Q_0^ab is C_7^3, and the image of K is exactly ker(chi_0), of order 7^12. Its intersection with V is ker(lambda), of dimension nine, and it projects onto U.

The invariants in Sections 3-4 used the full group S. They cannot be transferred to U or ker(chi_0). This document does not compute H_2(ker(chi_0);Z) or the pullback of its mod-seven classes to c0. That is the remaining case of this two-parameter probe.

## 7. Independent verification

Both the root lane and stability-audit lane independently replayed the standard-library script and reviewed the proof, returning PASS. They checked the sufficient cocycle constraints without assuming a presentation, the independent coboundary lower bound, the trace and exterior-square pairings, the actual-image argument, and all three integral LHS terms. The audit also checked the degenerate image and its character. The base Sp4 generation and finite-field multiplier remain explicitly identified prior inputs. This is a replayable finite-arithmetic mathematical certificate, not a Lean formalization.

Final canonical Cairn compilation with `compile_graph(use_cache=False)` completed with zero errors and 46 warnings elsewhere. Earlier concurrent missing-artifact errors have been resolved. No issue names any wave-four visibility node; all five claims and five proof routes in the expanded visibility lane are ESTABLISHED or COMPLETE. Unbounded central survival and flexible stability remain OPEN. The [validation receipt](hyperbolic-rf-wave4-visibility-validation-2026-09-20.json) records the latest scope; this was not a full `cairn check` run.
