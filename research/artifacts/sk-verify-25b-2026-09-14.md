# sk-verify-25b: adversarial review of the Frobenius-bicommutant rigidity theorem (2026-09-14)

Lane sk-verify-25b. Subject: sk-orthogonality-o parts 1–2 (`research/artifacts/sk-orthogonality-o-2026-09-14-part1.md`, `-part2.md`), claims `singer-identities-make-subshift-el3-isos-standard` (Theorem A) and `frobenius-bicommutants-make-subshift-el3-isos-standard` (Lemma X, Theorem C, Lemma S3, Corollaries O, O′, T, Theorem D), main tip 1dc1a7ee1a. I tried to break the result, re-derived every step of the Theorem D chain by hand, and checked the representation theory by computation on MSI.

## 0. Verdicts

- **`frobenius-bicommutants-make-subshift-el3-isos-standard`: PASS-WITH-FIXES** (C1–C4, wording and import hygiene; no gap).
- **`singer-identities-make-subshift-el3-isos-standard`: PASS-WITH-FIXES** (A1–A3; A2 removes the one flagged import).
- **Combined statement: PASS.** Let `X, Y` be infinite minimal subshifts. Every isomorphism `α: G_X → G_Y` equals `inn(k)∘Θ∘γ_X^ε` on `G_X`, for a ring isomorphism `Θ: M_3(R_X) → M_3(R_Y)`, some `k ∈ GL_3(R_Y)` and `ε ∈ {0,1}`, where `γ_X(g) = (g*)^{-1}` is the graph automorphism. So `G_X ≅ G_Y` implies that `X` and `Y` are strongly orbit equivalent.
  - This is exactly "standard" in `subshift-el3-isomorphisms-are-standard-over-f2` (`Θ∘*` is the anti-isomorphism when `ε = 1`).
  - It answers the "at least strongly orbit equivalent" half of the note's question (tex l.525–529). Flip conjugacy, and `L(G_X) ≅ L(G_Y) ⇒ G_X ≅ G_Y`, stay open.
- No counterexample found (§4).

## 1. Imports and their review status

| Import | Used in | Status on main |
|---|---|---|
| `subshift-gl3-index-kernel-is-elementary-group` (O1) | Lemma I, Lemma X, Theorem C, Cor O′, Theorem A step 3 | sk-verify-18 PASS (part6), conditional on reviewed I1 and Theorem D. The claims' "unreviewed" wording is stale. |
| `subshift-elementary-group-rigid-supports-are-mutual-centralizers` (C2) | Lemma X | sk-verify-3 PASS |
| `frobenius-support-idempotents-give-boolean-realization` (Prop F) | Theorem A steps 4–5 | sk-verify-19 PASS. Item 2(⇐) imports the first-return corner isomorphism; Fix A2 avoids item 2. |
| `el3-root-subgroups-are-bicommutant-closed` | Theorem A step 6 | sk-verify-5 PASS |
| `peirce-root-homomorphisms-of-e3-are-standard` | Theorem A step 7 | sk-verify-15 PASS |
| `cantor-crossed-product-matrix-ring-iso-forces-soe` | Theorem A step 7 | sk-verify-3 PASS |
| `cantor-crossed-product-idempotents-similar-to-clopen-ones` (DP) | Theorem A step 3, Cor O′ | sk-verify-16 PASS |
| `subshift-crossed-product-k0-is-coinvariant-group` | Theorem A step 3 | sk-verify-3 PASS at group level; only torsion-freeness and `[χ_U] ↦ [χ_U]` are used |
| (Div) Matui, arXiv:math/0404224, Lemma 2.5 | Theorem A step 3 | **Read at source by me**: e-print `acfg.tex`, l.272–283, `\label{Hopfequiv}`, verbatim as the artifact quotes it; the §2 counter (four definitions precede it) gives 2.5 |
| First-return corner isomorphism | Lemma X (as stated), Prop F item 2(⇐) | Not source-checked, and **not needed** after Fixes C2 and A2 |

## 2. Theorem A (Singer identities ⇒ standard)

Re-derived:
- **Lemma 0.** `F_2[C_7] = F_2 × F_8 × F_8`; `m(α(c_V)) = I + S_V` iff no type `2` or `3*`.
- **Lemma I.** `ind` kills torsion, and `(I+n)² = I` when `n² = 0`.
- **Step 1 (O).** The joint piece has type `(3,3)`; its diagonal has a `3*` part (F5), which (Sing_{V⊔W}) forbids.
- **Step 2.** Composition factors `1, 3*, 8` restrict to constituents `1, 3*, 2`, all forbidden. Ext¹(3,3) = 0 then makes `M ≅ 3 ⊗ P`.
- **Step 3.**
  - `P³ ≅ R_Y³`, and torsion-free `K_0` gives `[P] = [R_Y]`.
  - DP gives `P ≅ ⊕e_{W_i}R_Y`; Lemma 2.5(2) moves `W_2` into `Y∖W_1`, and Lemma 2.5(1) moves the remainder onto `W_3`. So `P ≅ R_Y`, and `k` exists. `G_Y ⊴ GL_3(R_Y)` by O1.
- **Step 4.** `S′_V` commutes with `α′(q_V)α′(q_{X∖V}) = q` for `q ∈ F21`. `span F21 = M_3(F_2)` (computed), so `S′_V = b_VI`.
- **Step 5.** `α′(q_V) = I + b_V(q−I)`, and `α′(L_X) = GL_3(𝔅′)`.
- **Step 6.**
  - In `G_X`, `C(T_ij(D_X))` consists of the `g` with column `i` equal to `e_i` and row `j` equal to `e_j^T`: the diagonal entry `d` lies in `D_X`, is idempotent and left invertible, so `d = 1`. Every such `g` commutes with `A_ij(R_X)`, so `CC(T_ij(D_X)) = A_ij(R_X)`.
  - `CC(T_ij(𝔅′)) ⊆ CC(A_ij(R_Y)) = A_ij(R_Y)`, and `1 ∈ J_ij`, the commutator relations and generation give `J = R_Y`.
- **Step 7.** Peirce standardness, then SOE.

Fixes:
- **A1 (status).** Replace "the unreviewed index kernel theorem" by "the index kernel theorem (sk-verify-18 PASS)".
- **A2 (Step 5, removes the corner import).** Replace "By Proposition F item 2, …" by: "By (O) and (O′), `S_{X∖V} = I − S_V`. `α(Q_V)` commutes with `K_{X∖V}`, so it preserves `S_VM` and `(I−S_V)M = Fix(K_V)`. The kernel of its action on `Fix(K_V)` is normal in `α(Q_V) ≅ GL_3(2)`, which is simple, and contains `K_V ≠ 1`. So `α(q_V) − I = S_V(α(q_V) − I)S_V`, and conjugating by `k` gives `α′(q_V) − I ∈ b_VM_3b_V`."
- **A3 (Step 2).** After "restricts to `F21_X` with a constituent", add: "(restriction to `F21_X` is semisimple since 21 is odd, so this constituent is a summand of `M` under `K_X`)".

## 3. Part 2 (Theorem D chain)

Re-derived:
- **Lemma Z.** The Peirce components of `xn = nx` kill both off-diagonal blocks (fullness). `x_ff` is central in `fSf = fS(1−f)Sf`, and `λ = λ′`.
- **Lemma X.**
  - Elements of `C(F21_V)` commute with the isotypic idempotents `e_VI`, `e_{X∖V}I`, so they are `rI_3` on `e_VM_X`, since the commutant of `span F21 = M_3(F_2)` is scalar.
  - Conversely, C2 puts `x` in `G_V`. The order-2 elements `I + nI_3`, `n ∈ fR_Vf′ ∪ f′R_Vf`, lie in `G_X` by O1 and commute with `F21_V`, so every entry of `x` is central in `R_V`.
- **Theorem C.**
  - `CC_{G_Y}(K_V) = α(Q_V)` has order 168.
  - For each isotypic component `t` with `M_t ≠ 0`, the unit group of the `t`-component of the image of `F_2[F21]` (`GL_3(2)` for `t = 3, 3*`, `F_4^×` for `t = 2`) is torsion, lies in `G_Y` by O1, and commutes with `C_{G_Y}(K_V)`.
  - `C_7` acts trivially on `1 ⊕ 2`, and `K_V` is faithful, so some `t ∈ {3,3*}` occurs. Order 168 forces exactly one `t` and no `2`.
- **Lemma S3.** A splitting gives an equivariant quadratic `q: 3 → Λ²` with polarization `v⊗w + w⊗v`. `Stab(e_1)` has no fixed vector on `Λ²`, so `q = 0`, which is a contradiction.
- **Corollary O.** `N = S_VS_WM ≅ t_V ⊠ t_W ⊗ P_N`, and the diagonal acts as `t_V⊗t_W ⊗ id` inside the semisimple `α(Q_{V⊔W})`-module `M` with constituents `1, t_{V⊔W}`. `3⊗3` is not semisimple, and `3⊗3* ⊇ 8`, so `N = 0`.
- **Corollary O′.**
  - `E = I − S_X ≤ I − S_V`, and `α(L_X)` is trivial on `EM`.
  - Every square-zero `n ∈ EM_3E` gives `I+n ∈ C_{G_Y}(α(L_X)) = α(C_{G_X}(L_X)) = 1`. The units of `LC(X,F_2)` are `{1}`.
  - DP splits a nonzero `E`, and simplicity of `M_3(R_Y)` gives such an `n ≠ 0`.
- **Corollary T.** `S_VM = Fix(K_{X∖V})`, and `α(Q_X)` acts there as `α(Q_V)`. Theorem C at `X` allows only `t_X`, and the `Q`-type of `ψ_V` equals `t_V` because `3|_{F21} ≇ 3*|_{F21}`.
- **Theorem D.** After `γ_X` when `t_X = 3*`, (Sing_V) holds for all `V` (Lemma 0) and (O′) holds, so Theorem A applies.

Fixes:
- **C1 (status).** Both claims and the part 2 import list should say the index kernel theorem is reviewed (sk-verify-18 PASS).
- **C2 (Lemma X, removes the corner import).** Replace the first-return corner isomorphism import by: "`R_V = e_VR_Xe_V` is simple with centre `F_2e_V`: a corner of the simple ring `R_X` is simple, and since `e_V` is full, `Z(e_VR_Xe_V) = e_VZ(R_X) = F_2e_V`." With A2, Theorem D uses no unreviewed import.
- **C3 (Theorem C, clarity).** Add: "`g_t(a)` lies in `F_2I + span_{F_2}K_V`, since the `t`-component of `F_2[F21]` is `M_3(F_2)` (`F_4` for `t = 2`), so it commutes with `C_{G_Y}(K_V)`."
- **C4 (scope).** Replace "This answers the question in `simple_kazhdan_sofic_group.tex` (§4, …)" by: "This answers the second alternative of the question in `simple_kazhdan_sofic_group.tex` (§4): `G_X ≅ G_Y` implies strong orbit equivalence. Flip conjugacy stays open." The route's `requires` entry `diagonal-recognition-makes-subshift-el3-isos-standard` is unused and can be dropped.

## 4. Adversarial hunt (no counterexample)

| Candidate | Outcome |
|---|---|
| `inn(d_k)`, `d_k = diag(u^k,1,1)` | Outer in `Out(G_X)` (`subshift-group-factor-out-contains-z`), but standard: `k = d_k ∈ GL_3(R_Y)`. `t_V = 3`; Sing and (O′) hold. Consistent with the refuted G_Y-conjugacy recognition node, since Theorem A conjugates in `GL_3`, not in `G_Y`. |
| `γ_X` and ring anti-automorphisms (`(fu^n)* = u^{-n}f`) | `t_V = 3*` for all `V`; handled by `ε = 1`. |
| Ring isomorphisms from flip conjugacy, or Morita-type `M_3(R_X) ≅ M_3(R_Y)` (SOE) | Standard by definition. Constants map to a type-3 image after the `k` of Step 3. |
| Central or index twists `g ↦ χ(g)g` | `G_X` is perfect and centreless, so none. |
| Locally mixed orientation (Petechuk's product-ring pattern) | Requires central idempotents. `R_Y` is simple, and Corollary T proves `t_V` constant. |
| Mixed `3 ⊕ 3*` or type-2 images of `F21_V` | Excluded by the 168-count. On MSI a mixed image in `GL_12(F_2)` has 168² units in its double commutant. |
| Natural ⊕ trivial and Steinberg fakes | Not isomorphisms. They fail (O′) and type-2 respectively, which Corollary O′ and Theorem C prove for every isomorphism. |
| Stress of Theorem C for large `V`, or supports not preserved | The count uses only `CC_{G_Y}(K_V) = α(Q_V)` and the isotypic decomposition of the image; no support preservation is assumed. |

## 5. Computation on MSI (one-shot `python3`, pure F_2 linear algebra)

Script `$SK2/lanes/sk-verify-25b/reptest.py` (not landed). Output, verbatim:

```
OK   |GL_3(2)| = 168 : 168
OK   c^3 + c + 1 = 0
OK   |F21| = 21 : 21
OK   generators generate GL_3(2) : 168
OK   span_F2(F21) = M_3(F_2) : 9
OK   dim sl_3 = 8
OK   sl_3 is an irreducible 8-dim GL_3(2)-module (F4: 3(x)3* = 1 + 8)
OK   Ext^1(3,3) = H^1(Q, End 3) = 0 : dimZ1=8 dimB1=8 H1=0
OK   Sym and Lambda are submodules
OK   Lemma S3: Sym has no invariant complement to Lambda (3(x)3 not semisimple) : 0
OK   Stab(e_1) fixes no nonzero vector of Lambda : 24
OK   natural 3: c has type 3 (x^3+x+1) : (0, 3, 0)
OK   dual 3*: c has type 3* (x^3+x^2+1) : (0, 0, 3)
OK   F1: End 3 on <c> = 1 + (2) + 3 + 3*, so 8|F21 = 2 + 3 + 3* : (3, 3, 3)
OK   F1: 8 has no F21-fixed vector
OK   F5: 3(x)3 restricted to F21 = 3 + 3* + 3* : (0, 3, 6)
OK   finite Lemma X: dim C(F21_V) = 4 + 36 = 40 : 40
OK   finite Lemma X: dim CC(F21_V) = 10 : 10
OK   finite Lemma X: |units of CC(F21_V)| = 168 : 168
OK   finite Lemma X: units of C(F21_V) span C(F21_V) : 44
OK   finite Theorem C: mixed 3 + 3* image has |units CC| = 168^2 : 19
```

- `Ext¹` is computed as `dim Z¹ − dim B¹` over all 168 group elements.
- The End(3) kernel dimensions `(3,3,3)` are `ker(c−1) = 1 ⊕ 2`, `3` and `3*`.
- The finite model is `R = M_4(F_2)`, `V = {0,1}`.

## 6. Not reviewed

Theorem B and Corollary B′ (part 1 §4) and part 1 §5 are not on the Theorem D chain; part 2 supersedes them.

## 7. Sharpest next target

- Apply A1–A3 and C1–C4 on the claims, and move `subshift-el3-isomorphisms-are-standard-over-f2` from OPEN to ESTABLISHED through `frobenius-bicommutants-make-subshift-el3-isos-standard`. That is main's call; a second independent verifier is sk-verify-25a.
- For the note: the Questions paragraph could state the SOE implication as a theorem, and ask only for flip conjugacy.
