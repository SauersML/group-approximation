# sk-verify-16 part 2: wave-11 queue lines 57–63 (2026-09-13)

Lane sk-verify-16, an independent verifier serving `sk/queue/sk-verify-13.txt` lines 57–63. Part 1 is `research/artifacts/sk-verify-16-2026-09-13.md`. Every step was re-derived by hand from the artifacts and nodes on main at tip 95d4026173.

## 0. Verdicts
| line | item | verdict |
|---|---|---|
| 57 | sk-fq-port LaTeX + derivation (`research/artifacts/sk-fq-port-proposal-2026-09-13.md`) | **PASS-WITH-FIXES** (one overclaiming word; optional half line) |
| 58 | `kazhdan-generators-admit-no-norm-popa-corners` | **PASS** |
| 59 | `orthogonal-popa-corners-for-kazhdan-approximants-small-trace` | **PASS** |
| 60 | `subshift-el3-diagonal-recognition-fails-up-to-g-conjugacy` | **PASS** |
| 61 | `laurent-right-ideals-of-cantor-crossed-products-columnwise` | **PASS** |
| 62 | `laurent-elements-of-cantor-crossed-products-are-suitable` | **PASS** |
| 63 | `minimal-cantor-crossed-products-stabilize-laurent-pairs` | **PASS** |

No FAIL, no GAP.

## 1. Line 57: every finite field, rev4 architecture
**Hypotheses.**
- F_q-linearity of φ_k is needed and correctly isolated. It gives φ_k(μ) = μI, so words in Z map to scalars, and e_ij(α) and e_ij(τ_α) have the same image.
- EL_n(F) is generated by e_ij(1), e_ij(α) and e_ij(τ_s): e_ij(α^t) = [e_il(α^{t−1}), e_lj(α)], sums give e_ij(F_q), and e_ij(c·monomial) = [e_il(c), e_lj(monomial)].

**Change 1.**
- g = cI with c ∈ LC(Z,F_q) nowhere zero, by the characteristic-free parts.
- e_ij(e_Vu_s)·cI = e_ij(e_V(c∘s⁻¹)u_s), so commuting forces c = c∘s⁻¹ on small V. Density of small sets makes c Λ-invariant, hence constant by minimality.
- Model test: c = 1 + (α−1)e_U commutes with every e_ij(e_V) but not with e_ij(u_s) when sU ≠ U.

**Change 2.**
- Margins w+1. The product fu_a e_V u_s f′u_b = f e_{aV}(f′∘(as)⁻¹)u_{asb} ∈ {0, ε_{a,(sb)⁻¹}}, since f′∘s⁻¹ is constant on V (s⁻¹ ∈ B_1 ⊆ B_{w+1}).
- h − I = ε_{e,s⁻¹}E_ij, and k − I = (ghg⁻¹ − h)h⁻¹, k⁻¹ − I = h(gh⁻¹g⁻¹ − h⁻¹) lie in M_n(A_V). With yy′ = eI: (k−I)(k⁻¹−I) = 2I − k − k⁻¹.
- Right (and left) multiplication by h^{±1} keeps first indices in B_w, so k has identity rows at the levels a ∈ B_{w+1}∖B_w (nonempty as Λ is infinite). A scalar with such rows is 1, and k ≠ 1, so k is not scalar.
- [k,x] ∈ K ∩ S_V, and [k,xy] = [k,x]·x[k,y]x⁻¹.
  - If K ∩ S_V were central, x ↦ [k,x] would be a homomorphism to an abelian group, trivial by perfectness of SL_d(F_q), d ≥ 3.
  - Then k would centralize S_V, and C_{GL_d}(SL_d) = scalars, a contradiction.
  - So S_V ⊆ K by quasisimplicity.

**Change 3.**
- The kernel of G → ∏_ω SL is normal and misses e_12(1), so it lies in Z.
- A word with scalar image μ_kI for infinitely many k: choose ω on which μ_k is constant (F_q^× is finite). Then [x,G] ⊆ Z, xZ is central in the infinite simple G/Z, so x ∈ Z.
- Words in Z map to μI for large k. So the PSL_{nN_k}(F_q) converge to G/Z, N_k → ∞, and the expanders pass to quotients.

**Fixes (verbatim).**
- **F1 (must):** the LaTeX sentence "Only the copy $S_V$ of $\SL_d(\F_q)$ lies in $G$;" → "The copy $S_V$ of $\SL_d(\F_q)$ lies in $G$;". The proof uses only S_V ⊆ G. Whether H_V ∩ G = S_V is neither proved nor needed; it is plausibly true through K_1, but unshown.
- **F2 (optional, +½ line):** after "…so it lies in $Z$." add "A word equal to $\mu I_n$ in $G$ maps to $\mu I$ for large $k$, by $\F_q$-linearity." It makes the words-in-Z half of marked convergence to G/Z explicit rather than "unchanged".
- **Length:** the artifact's own compile gives 9 pp. The 8-page decision is main's.

## 2. Line 58: no norm Popa corners for Kazhdan generators
- **Coarse bimodule.** π(g)ζ = λ_gζλ_g* acts on basis tensors by the free action (h,k) ↦ (gh, kg⁻¹), so it is a multiple of the regular representation. There are no invariant vectors (Γ infinite), and (K) max_s‖λ_sζ − ζλ_s‖ ≥ ε‖ζ‖ holds, since the right action by a unitary is isometric.
- **Canonical vector.** ζ = Σ_r c_r Σ_{ij} ê^r_{ij} ⊗ ê^r_{ji} commutes with each e^r_{kl}: both sides equal c_rΣ_j ê^r_{kj} ⊗ ê^r_{jl}. The tensors are orthogonal because τ(e_{ji}e_{kl})τ(e_{ij}e_{lk}) = δ_{ik}δ_{jl}τ(e_{jj})τ(e_{ii}), giving ‖ζ‖² = τ(p).
- **(A).** yζ − ζy = (pyp−b)ζ − ζ(pyp−b) + (1−p)[y,p]ζ + ζ[y,p](1−p), so ‖yζ − ζy‖ ≤ 2(d + ‖[y,p]‖)‖ζ‖.
- (K) and (A) give the ε/2 bound.
- **Corollary.** ‖[λ_s,e]‖ < η + 2θ and dist(eλ_se, B) < η + θ, so 2η + 3θ ≥ ε/2 for all η, hence θ ≥ ε/6.
- The model tests (finite Γ, Z, SL_3(Z)) are correct.

## 3. Line 59: orthogonal Popa corners
- **Exact transfer.** Orthogonality of the second factors gives ‖aζ‖² = Σ_r c_r² Σ_{ij} τ(a*ae_{ii})τ(e_{ii}). With k_rτ(e_{ii}) = τ(p_r) (equivalent matrix units), this is τ(a*ap). Symmetrically ‖ζa‖² = τ(paa*).
- **One corner.** ‖λ_sζ − ζλ_s‖ ≤ 4η‖ζ‖ + ‖a_sp‖_2 + ‖pa_s‖_2, which gives the relative badness bound and (C).
- **Families.** Σ_m τ(a*ap_m) = τ(a*aP) by normality, and ‖a_sP‖_2 ≤ ‖a_s‖_2.
- Constants as stated (ε − 4η, factors 2 and 4|S|).

## 4. Line 60: diagonal recognition fails up to G-conjugacy
- **Lemma 1.** Pg⁻¹P is a parametrix. The index is additive modulo finite rank. e_ij(r)² = I gives index 0. ind(wI_3) = 3ind_1(w), and ind(d) = ind_1(u) = −1 (the unilateral shift: injective, one-dimensional cokernel).
- **Lemma 2.**
  - Evaluating f_ij = kE_ijk⁻¹ ∈ LC(Ω, M_3(F_2)) pointwise gives locally constant c ∈ GL_3(B) with columns f_{i1}v and f_ij = cE_ijc⁻¹.
  - c⁻¹k is scalar, wI. Comparing centres B·I = c(wAw⁻¹·I)c⁻¹ = wAw⁻¹·I.
  - c is a product of commuting pieces e_W c_W + (1−e_W)I with c_W ∈ E_3(F_2), so c ∈ E_3(R) and ind(c) = 0.
- **(a)** d normalizes G by the listed conjugation formulas. The centralizer of all root elements in GL_3(R) is Z(R)^×I = {I}, which I re-derived entrywise with r = 1. So inn(d) inner would give d ∈ G, contradicting ind(d) = −1.
- **(b)** span_{F_2}GL_3(F_2) = M_3(F_2): it contains I and E_ij = e_ij(1) − I, and E_33 = P_(12) − E_12 − E_21. So spans transport, and ind(h⁻¹d) = −1 ∉ 3Z.
- **(c)** n = k′⁻¹g⁻¹k normalizes M_3(A), so ind(k) ≡ ind(k′) mod 3. I, d, d² have indices 0, −1, −2.
- The model test k = uI_3 (index −3, normalizes L) is correct.

## 5. Lines 61–63: Laurent elements (sk-algebraic-putnam part 1)
**Lemma C.**
- q_0 | P in F[u] because q_0(0) ≠ 0 and u^kP = q_0Q forces q_0 | P, so quotients are supported in [m, M−d].
- Back-substitution gives y(m,t) = Σ_{j=−w}^{i} λ_{i−j}ζ_j(T^{t+j}x) = η_i(T^tx) with i = m − t.
- With the entry convention a(m,n) = f_{m−n}(T^mx), y = Σ_i(η_i∘T^{−i})u^i ∈ R.
- Faithfulness at a dense orbit gives q_0y = z.

**Theorem S.**
- p_0 and r_0 are coprime.
- On a block of height h ≥ D, ρ⁰ is the CRT quotient F^h → F[u]/(p_0r_0), onto.
- E_h projects onto C_{1−p} along K ⊕ C_p, so ρ_pE_h = 0 and ρ_{1−p}(1−E_h) = 0.
- Translation covariance ρ(δ_{b+i}) = ū^bρ(δ_i) plus Lemma C give e ∈ pR and 1−e ∈ (1−p)R. This is Nicholson's suitability.

**Theorem L.**
- ρ_q∘a∘r = ρ_q, and ρ_q(aδ_n) = ū^nτ(T^nx).
- For h ≥ 2w_r + d + 2 there are ≥ d consecutive interior m, whose ū^m lie in im ψ_0 because r has propagation w_r. So ψ_0 is onto.
- Two surjections differ by G ∈ GL_h(F), chosen per atom. g = ⊕G ∈ GL_1(A_𝒫), and the columns of g − a lie in qV, so a + qc = g.

**Model tests.**
- u + u² is not suitable in F_2[u^{±1}].
- (1+u, Φ_5(u)): the images of the units are ⟨ū⟩ = {1, ū, ū², ū³, 1+ū+ū²+ū³} in F_16, which does not contain 1+ū. Independence of 1, ū, ū², ū³ checked.
- The corollary on rows (Bezout in the PID F[u^{±1}]) is correct.

## 6. Not checked
- For line 57: no compile rerun. Classical facts used without locator: SL_d(F_q) perfect and quasisimple for d ≥ 3; C_{GL_d}(SL_d) = scalars.
- For lines 58–59: the Connes–Jones remark (explicitly not claimed).
- For lines 61–63: Nicholson/Warfield definitions recalled.
- For line 60: `diagonal-recognition-makes-subshift-el3-isos-standard` and the corrected (Rec′) target (not in these queue lines).
- Novelty: none of these was searched beyond the lanes' own bounds.
