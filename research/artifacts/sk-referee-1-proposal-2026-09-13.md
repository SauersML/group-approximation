# sk-referee-1 proposal: fixes for the referee findings

- Findings and derivations: `research/artifacts/sk-referee-1-report-2026-09-13.md`.
- Line numbers refer to `manuscript-disk-1646.tex` (md5 `7b3dc4ec92b4581553bc0dc2b0edcb99`).
- Status of every item: these are referee fixes, with no Cairn nodes and no second reader yet. Each derivation is in the
  report. Proposals only: no `.tex` file was edited.
- If all items are adopted the note grows by about 12 lines.

## P1 (G1, U7 ii): the periodic-word lemma
(a) l.123-129, current:
```
For each $k$, there is a periodic sequence $y=w^\infty$ with exactly
the same words of length $2k+1$ as $X$. Indeed, minimality implies
uniform recurrence. In a point of $X$, choose two occurrences of the
same word of length $2k$, separated by a segment $w$ long enough to
contain every word of length $2k+1$. Repeating this segment introduces
no new word of that length at the join. Its length $N$ can be chosen
arbitrarily large.
```
Replacement:
```
For each $\ell$, there is a periodic sequence $y=w^\infty$ with exactly
the same words of length $2\ell+1$ as $X$. Indeed, minimality implies
uniform recurrence, so for some $L$ every word of $X$ of length $L$
contains every word of $X$ of length $2\ell+1$. Fix $x\in X$. Two of the
positions $0,L,2L,\dots,|A|^{2\ell}L$, say $p<p'$, carry the same word
$v$ of length $2\ell$. Put $w=x_p\cdots x_{p'-1}$. Then $wv$ is a word of
$X$, and every window of length $2\ell+1$ in $w^\infty$ lies in a copy
of $wv$. Its period $N=|w|\ge L$ can be chosen arbitrarily large.
```
Also l.137: `choose $k$` becomes `choose $\ell$`. The letter k then refers only to the commutator [g,h].
(c) +1 line. (d) The reader gets the one detail that makes "no new word at the join" true, and k stops meaning two things.

## P2 (G2, U9): the diagonal matrices
(a) l.131-133, current:
```
Let $P\delta_n=\delta_{n+1}$ on $\F_2^{\Z/N\Z}$. If $f$ depends on a
window $[-\rho,\rho]$, evaluate its local table along $y$ to obtain a
diagonal matrix $D_y(f)$. Send
```
Replacement:
```
Let $P\delta_n=\delta_{n+1}$ on $\F_2^{\Z/N\Z}$. If $f(x)=F(x_{-\rho}\cdots x_\rho)$
with $\rho\le\ell$, where $F$ is defined on the words of $X$, put
$D_y(f)\delta_n=F(y_{n-\rho}\cdots y_{n+\rho})\delta_n$. Send
```
And l.141-142, current `Nonzero coefficients remain nonzero, since every allowed` / `window occurs in $y$.` Replacement:
`Nonzero coefficients remain nonzero, since every word of $X$ of length` / `$2\ell+1$ occurs in $y$.`
(c) +0 lines. (d) The relation at l.140 becomes checkable, because it depends on this index convention.

## P3 (G3): why k acts on H_W by κ_0
(a) l.217-220, current:
```
Over $\F_2$, the group $\GL_d(\F_2)=\SL_d(\F_2)=\PSL_d(\F_2)$
is simple and centerless for $d\ge3$. Conjugation by $k$ on $H_W$
is conjugation by $\kappa_0$, so $[k,H_W]\subseteq N\cap H_W$
contains a nonidentity element. Since $N\cap H_W$ is normal in $H_W$,
```
Replacement:
```
Over $\F_2$, the group $\GL_d(\F_2)=\SL_d(\F_2)=\PSL_d(\F_2)$
is simple and centerless for $d\ge3$. Each $x\in H_W$ is $I_3+M$ with
$M\in M_3(B_m(W))$ constant over $W$. Since $k=(1-e)I_3+eke$ and the
tower over $W$ lies in the tower over $U$, we get
$kMk^{-1}=\kappa_0M\kappa_0^{-1}$. Then $[k,H_W]\subseteq N\cap H_W$
contains a nonidentity element. Since $N\cap H_W$ is normal in $H_W$,
```
(c) +2 lines. (d) It closes the only step where the reader must reconstruct why conjugation by k is constant on the tower.

## P4 (U1): "both forms"
(a) l.42, current: `The following construction answers both forms positively.`
Replacement: `The following construction answers all three positively.`
(c) +0. (d) The sentence then matches the three formulations it follows. This is the user's wording choice.

## P5 (U2, U8): define EL_3 and the commutator where they first appear
(a) l.86-87, current:
```
indicators. Put $G=\EL_3(R)$ and $e_{ij}(r)=I_3+rE_{ij}$, where $E_{ij}$
is the usual matrix unit. The elementary identities
```
Replacement:
```
indicators. Let $G=\EL_3(R)$ be the subgroup of $\GL_3(R)$ generated by
the matrices $e_{ij}(r)=I_3+rE_{ij}$, $i\ne j$, where $E_{ij}$ is the
usual matrix unit, and put $[g,h]=ghg^{-1}h^{-1}$. The elementary identities
```
Delete l.96: `Here $[g,h]=ghg^{-1}h^{-1}$.`
(c) +0. (d) The group in the theorem is defined before it is used.

## P6 (U3): coefficients are unique
(a) l.72-77, current:
```
\[
 R=\LC(X,\F_2)\rtimes_T\Z
   =\left\{\sum_j f_j u^j:\text{finite sums}\right\},
 \qquad ufu^{-1}=f\circ T^{-1},
\]
where $\LC$ denotes locally constant functions. Thus $ue_Uu^{-1}=e_{TU}$.
```
Replacement:
```
\[
 R=\LC(X,\F_2)\rtimes_T\Z
   =\bigoplus_{j\in\Z}\LC(X,\F_2)\,u^j,
 \qquad ufu^{-1}=f\circ T^{-1},
\]
where $\LC$ denotes locally constant functions. So every element of $R$
is uniquely a finite sum $\sum_jf_ju^j$, and $ue_Uu^{-1}=e_{TU}$.
```
(c) +1 line. (d) Three later steps (l.105, l.111, l.143) use uniqueness of the coefficients, and this states it.

## P7 (U4): display the identity behind absorption
(a) l.194-196, current:
```
Set $k=[g,h]\in N\setminus\{1\}$ and $m=w+1$.
By~\eqref{eq:absorb}, both $k-I_3$ and $k^{-1}-I_3$ lie in
$M_3(B_m(U))$. If $e$ is the unit of $B_m(U)$, then
```
Replacement:
```
Set $k=[g,h]\in N\setminus\{1\}$ and $m=w+1$. Both $k-I_3$ and
$k^{-1}-I_3$ lie in $M_3(B_m(U))$ by~\eqref{eq:absorb}, since
\[
 k-I_3=\bigl(g(h-I_3)g^{-1}-(h-I_3)\bigr)h^{-1},\qquad
 k^{-1}-I_3=h\bigl(g(h^{-1}-I_3)g^{-1}-(h^{-1}-I_3)\bigr).
\]
If $e$ is the unit of $B_m(U)$, then
```
(c) +3 lines. (d) The reader sees in one display why the exponents grow by at most w on each side.

## P8 (U6): state the tower hypothesis
(a) l.155, current: `Suppose the levels $T^aU$ are disjoint for all indices needed below.`
Replacement: `Let $U\subseteq X$ be clopen and $m\ge0$ with $U\cap T^jU=\varnothing$ for $0<|j|\le2m$.`
l.171, current: `provided the larger tower is disjoint. To see this, multiplying`
Replacement: `provided $U\cap T^jU=\varnothing$ for $0<|j|\le2(m+w)$. To see this, multiplying`
l.180, optional: `$0<|j|\le2w+3$` becomes `$0<|j|\le2w+2$`.
(c) +0. (d) The lemma gets a precise hypothesis, and the reader can check the constant at l.180.

## P9 (U7 i, iii): separate the two E's and fix "take q ≠ p"
(a) Rename the tower units `E_{ab}(W)` to `\varepsilon_{ab}(W)` at l.158, l.161, l.173, l.191-192 and l.209-214. The 3×3
matrix units E_ij stay. Then l.208-214, current:
```
A transvection between $(p,a)$ and $(q,b)$ with $p\ne q$ is
$e_{pq}(E_{ab}(W))$. For $p=q$ and $a\ne b$, take $q\ne p$ and use
\[
 [e_{pq}(E_{ab}(W)),e_{qp}(E_{bb}(W))]
       =I_3+E_{ab}(W)E_{pp}.
\]
This follows from $E_{ab}E_{bb}=E_{ab}$ and $E_{bb}E_{ab}=0$.
```
Replacement:
```
A transvection between $(p,a)$ and $(q,b)$ with $p\ne q$ is
$e_{pq}(\varepsilon_{ab}(W))$. For $(p,a)$ and $(p,b)$ with $a\ne b$,
choose $p'\ne p$ and use
\[
 [e_{pp'}(\varepsilon_{ab}(W)),e_{p'p}(\varepsilon_{bb}(W))]
       =I_3+\varepsilon_{ab}(W)E_{pp},
\]
which follows from $\varepsilon_{ab}\varepsilon_{bb}=\varepsilon_{ab}$
and $\varepsilon_{bb}\varepsilon_{ab}=0$.
```
(c) +2 lines. (d) The display at l.212 mixes the two E's, and "p=q ... take q≠p" contradicts itself. Both go.

## P10 (U10): the corollary is about finitely generated groups
(a) l.232-233, current: `Every Turing degree occurs as the word-problem degree of an infinite` / `simple Kazhdan LEF group.`
Replacement: `Every Turing degree occurs as the word-problem degree of an infinite` / `finitely generated simple Kazhdan LEF group.`
(c) +0. (d) Word-problem degrees are defined for finitely generated groups, so the statement becomes literally correct.

## P11 (U5): which products the LEF models must preserve
(a) l.146-149, current:
```
For a finite subset of $G$, include the entries of its elements,
their inverses, their differences, and all intermediate sums and
products needed for matrix multiplication. The resulting model is an
injective partial homomorphism into $\GL_{3N}(\F_2)$. Thus $G$ is LEF.
```
Replacement:
```
For a finite subset $S\subseteq G$, list the entries of $g$, $g^{-1}$
and $g-g'$ for $g,g'\in S$, together with the sums and products that
compute the entries of $gg'$ and $gg^{-1}$. The entrywise model then maps
$S$ injectively into $M_{3N}(\F_2)$, preserves the products that stay in
$S$, and sends $g$ to a matrix whose inverse is the image of $g^{-1}$.
So it lands in $\GL_{3N}(\F_2)$, and $G$ is LEF.
```
(c) +2 lines. (d) Invertibility of the image matrices becomes visible, and before it was implicit.

## P12 (S1): EL_n for every n ≥ 3
(a) After l.52 (`\textup{(LEF)}. In particular, it is sofic and hyperlinear.`), add inside the theorem:
`The same holds for $\EL_n\bigl(\LC(X,\F_2)\rtimes_T\Z\bigr)$ for every $n\ge3$.`
After l.67 (`\section{Proof of Theorem~\ref{thm:main}}`), add:
`We write the proof for $n=3$; for $n\ge3$ replace $3$ by $n$ throughout, so $d=n(2m+1)$.`
(c) +2 lines. (d) The theorem gets stronger (infinitely many groups for each X) at no cost in proof. sk-coefficients
may prefer a combined F_q and n statement, and should decide.

## P13 (S2): the irrational representative
(a) l.257-260, current:
```
Every Turing degree has an irrational representative: use $\sqrt2-1$
for the computable degree, and interleave the characteristic sequence
of a noncomputable set with $1$'s in a binary expansion for any other
degree.
```
Replacement:
```
Every Turing degree has an irrational representative: use $\sqrt2-1$
for the computable degree, and $\sum_{i\in S}2^{-i-1}$ for a
noncomputable set $S$, which is irrational since the binary expansion
of a rational number is eventually periodic, hence computable.
```
(c) +0. (d) The construction is simpler and the proof says why it is irrational.
