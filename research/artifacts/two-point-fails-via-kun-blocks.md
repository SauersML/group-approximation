# Surrogate orbits from Kun blocks, then the exact argument per block

Former Cairn route `two-point-fails-via-kun-blocks`, demoted with its
superseded quantitative lemma.

# Surrogate orbits from Kun blocks, then the exact argument per block

Immediate: `kun-block-transfer-lemma` is the statement that the
sheet-expander/deletion argument of
`exact-models-cannot-separate-marked-pair` runs on `(F,ε)`-multiplicative
models with Kun blocks in place of orbits and with matching control
supplied by one of its recorded mechanisms; its conclusion is verbatim the
present claim at the window `F ⊇ B_R ∪ F*` and precision `ε₀` it produces.
The lemma carries the entire mathematical content; this route exists so
that the consequence and the working lemma can be tracked, locked and
attacked separately.
