# SK review 3, part 4: marked groups of subshift algebras, and ring rigidity

Lane `sk-verify-3`, 2026-09-13. Every step below was re-derived. Main read at tip `18375ec10e`.

## 1. `subshift-algebra-groups-embed-in-marked-groups` (sk-fp-sofic-b, queue item 6)

Route `subshift-algebra-groups-embed-in-marked-groups-proof`, `requires: []`. Artifact
`research/artifacts/sk-fp-sofic-b-z2-soficity-map-2026-09-13.md` §§2–3.
**Verdict: PASS** for items 1–5.

**Item 1 (cylinder words).**
- Each factor u_δ χ_(p(δ)) u_δ^(−1) with |δ| ≤ ρ is a product of at most 2ρ+1 marking coefficients, so χ_[p] is a product of l ≤ |F|(2ρ+1) coefficients.
- The nested commutators e_ij(s_1⋯s_l) = [e_ik(s_1), e_kj(s_2⋯s_l)] use n ≥ 3. Their lengths satisfy L(1) = 1 and L(l) = 2 + 2L(l−1), so L(l) = 3·2^(l−1) − 2, within the stated bound.
- e_12(r) is scalar only for r = 0, and χ_[p] vanishes on Y iff p ∉ L(Y).

PASS.

**Item 2 (window bound).**
- A product of at most r generators has entries that are sums of products of at most r coefficients. So every term is f u_γ with |γ| ≤ r, and f depends on B(r)-patterns.
- Restriction R_(A^Γ) → R_Y has kernel {Σ f_γ u_γ : f_γ|_Y = 0}.
- For P_Y, test each of the finitely many scalars λ ∈ F_q^×.

PASS.

**Item 3 (homeomorphism onto a closed image).**
- Injectivity comes from W_p.
- Continuity: equal B(r)-languages give equal relations of length ≤ r.
- Inverse continuity comes from the length bound on W_p.
- The image is closed because the map is a continuous injection from a compact space to a Hausdorff one.
- Containment:
  - (⇒) Restriction gives a marked epimorphism.
  - (⇐) p ∉ L(Y′) gives W_p = 1 in G_(Y′), hence in G_Y, hence p ∉ L(Y).

PASS.

**Item 4 (isolation).**
- (⇐) Let Ω be a minimal SFT with window K. A subshift Y with L_K(Y) = L_K(Ω) satisfies Y ⊆ Ω, and minimality gives Y = Ω.
- (⇒) If X is not SFT, then X^(F) ⊋ X, and X^(F) → X, because F′-patterns of X^(F) with F′ ⊆ F are restrictions of patterns in L_F(X).
- Scope note: finite subshifts over f.g. Γ are SFTs through the f.g. normal core of the stabilizer. This is not needed for the infinite case used.

PASS.

**Item 5 (finite subshifts).**
- LC(Γ/H,F_q) ⋊ Γ ≅ M_([Γ:H])(F_q[H]) is the induced-representation identity.
- For Γ = Z^d, F_q[H] is a Laurent polynomial domain, so G_Y is a f.g. linear group and residually finite (Malcev).
- Minimal Z-subshifts are limits of the orbit closures of the note's periodic words, whose (2k+1)-languages agree with that of X.

PASS.

**Consequence for the frontier.** For a free minimal Z²-SFT Ω, S_n(Ω) = P_Ω is isolated among the marked groups G_Y and P_Y. So `free-minimal-sft-el-sofic-via-subshift-marked-limits` is dead. This claim is established, so its `invalidates:` entry is sound.

## 2. `cantor-crossed-product-matrix-ring-iso-forces-soe` (sk-rigidity-ring, queue item 8)

Route `cantor-crossed-product-matrix-ring-iso-forces-soe-proof`. **Verdict: PASS**, with one wiring note.

**Imports checked.** Each has a `-citation` route with verbatim quotes. I checked scope and fit, and did not re-read the primary sources.
- `cantor-z-crossed-product-embeds-in-continuous-factor`: Ara–Claramunt arXiv:1902.06476. It holds for an arbitrary field K and a homeomorphism of the Cantor set. The rank function is faithful, and rk(χ_U) = μ(U) for a full ergodic μ.
- `minimal-cantor-k0-positive-iff-positive-on-all-measures`: HPS simplicity, states = invariant measures, and Effros's criterion, all via Durand–Perrin's TeX.
- `gps-strong-orbit-equivalence-iff-unital-ordered-k0`: via Durand–Perrin, since the primary was paywalled.

**Steps.**
- **(R1).** ι(Σf_n u^n) = Σu^(−n) f_n. For a = fu^p and b = gu^q:
  - ab = f(g∘T^(−p))u^(p+q), so ι(ab) = u^(−p−q) f(g∘T^(−p));
  - g u^(−p) = u^(−p)(g∘T^(−p)), so ι(b)ι(a) = u^(−q) g u^(−p) f = u^(−p−q)(g∘T^(−p))f.
  - The two agree, so ι is an anti-automorphism. Transpose with entrywise ι turns M_m(R)^op into M_m(R).
- **(R2).** A ring isomorphism preserves V(·), hence K_0^+, and the unit class [1_(M_m)] = m[1].
- **(R3).** The K_0 computation (reviewed PASS in part 1) is stated for F_q. Its proof uses only that LC(X,k) is a directed union of the rings k^P, and that K_(−1)(k) = 0 for every field.
  - So the extension to arbitrary fields is correct by the same proof.
  - Wiring note: the required node's display still says q, and the owner may widen it.
- **(R4).**
  - Minimality makes every invariant μ full.
  - Faithfulness gives rk_μ(e) > 0 for e ≠ 0.
  - rk_μ is additive on orthogonal sums and constant on similar idempotents, so it induces a state s_μ on K_0 with s_μ[χ_U] = μ(U). Since K_0 is generated by the [χ_U], s_μ = ∫·dμ.
  - An affine continuous function on the compact convex set of invariant measures that is positive on ergodic measures is positive everywhere (Bauer maximum principle).
  - Effros's criterion then puts [e] in the HPS cone. The reverse inclusion comes from clopen indicators.
- **(R5).** nf = g − g∘T^(−1) gives ḡ ∈ C(X,Z/n) invariant, hence constant, hence g = c + nh and f = h − h∘T^(−1). So K^0 is torsion-free, and m(φ[1] − [1]) = 0 gives φ[1] = [1].
- **(R6).** GPS.

PASS.

## 3. `subshift-gl3-iso-forces-strong-orbit-equivalence-odd-q` (sk-rigidity-ring, queue item 9)

Route requires `subshift-gl3-iso-gives-matrix-ring-iso-odd-q` (not reviewed before this) and item 8. **Verdict: PASS**, conditional only on the Zel'manov import as quoted.

**Application of Zel'manov Corollary 4** (`zelmanov-standard-isomorphisms-linear-groups-with-half`: scan of the Plenum translation, pp. 528–529, quoted verbatim; I did not re-read the scan). The standing assumptions all hold:
- R, S ∋ 1/2, since q is odd.
- G = GL_3(R_X) lies between E_3 and GL_3, with n = 3.
- φ is the isomorphism followed by GL_3(R_Y) = S* ⊆ S.
- φ(G) = S* contains the orthogonal closure φ(Σ)_S, because that closure is a subgroup of S*.
- S = M_3(R_Y) is generated by GL_3(R_Y): rE_ij = e_ij(r) − 1 and rE_ii = (rE_ij)(e_ji(1) − 1).

Corollary 4 applies with S = A_m, m = 3, A = R_Y ∋ 1/2, φ(G) = GL_m(A) = S*.

**Simplicity.**
- R_X is simple by the note's argument (l.102–109), which goes through verbatim over F_q after scaling f_0, and by `steinberg-algebra-simple-iff-minimal-effective` (PASS).
- M_3 of a simple ring is simple.
- A simple ring is not a direct sum of two nonzero ideals, so exactly one of I_0, I_1 is nonzero, and correspondingly S_0 or S_1. This gives an isomorphism or an anti-isomorphism M_3(R_X) → M_3(R_Y).

**Assembly with item 8** (m = 3, k = k′ = F_q): infinite minimal subshifts are Cantor, so the systems are SOE. PASS.

**Scope** (as the nodes say): q odd only; GL_3 and not the simple groups EL_3/Z; SOE and not flip conjugacy. For the note's F_2 groups nothing here applies. Characteristic 2 is excluded by Zel'manov's 1/2 hypothesis.
