# sk-design-structure: named steps and citable lemmas for the rev4.5 proofs

**Base.** rev4.5 `simple_kazhdan_sofic_group.tex`, md5 29d63f7944bccccbf9a91b0b2714c0b4 (main 8b36733d78).

**Candidate.** `$SK2/lanes/sk-design-structure/candidate.tex`, md5 33a388311bec7d3c8e68632b9025802b.
- 827 lines: +52 source lines, 19 hunks.
- MSI texlive 2025, pdflatex ×3: 0 errors, 0 overfull boxes, 0 undefined references, 0 rerun warnings, 9 pages (rev4.5: 8).
- The 2 underfull boxes are the same two bibliography entries as in rev4.5.
- The body ends on p.7. Page 9 holds references [29]–[43], about 40% of the page.

**Verbatim OLD → NEW.** `research/artifacts/sk-design-structure-2026-09-14-diff.md` is the full unified diff.

No mathematical step changes. Every result, credit and citation stays.

## 1. Changes (base line numbers)

**S1. Preamble, +2.** `\newtheorem{lemma}{Lemma}` with lettered numbering, giving Lemmas A, B, C.
- Corollary 5 keeps its number.
- There is no "Lemma 1" next to "Theorem 1".

**S2. §1 Simplicity (l.150–208), +8.**
- New opening: "Let 1≠K⊴G and 1≠g∈K. We show in three steps that K=G."
- Run-in italic heads:
  - Step 1: a nontrivial commutator with a small elementary matrix.
  - Step 2: the commutator lies in a finite simple group over a tower.
  - Step 3: K contains every elementary matrix.
- "Put x=[g,h]∈K∖{1}" (l.187) moves to the end of Step 1, as "Fix such h and V. Then x=[g,h] lies in K∖{1}."
- Step 3 is titled by its conclusion rather than "J is R", because J is defined inside the step. "Finally, J=" becomes "The set J=".

**S3. Brown's formulation (l.274–284), 0.** Moved from the end of §2 to the end of §1, verbatim.
- Why: it is about every group of Theorem 2 and uses only what §1 proves (LEF, infinite, simple). §2 is then exactly the proof of Theorem 1.
- Best overall placement: a remark after the opening question on page 1. That is sk-design-page-one's scope; main decides.

**S4. §3 proof (l.327–459) split into lemmas, +37.**
- Structure, in order:
  1. a roadmap (main-adopted F7, rephrased around the lemmas);
  2. **Lemma A**: every countable LEF group is a subgroup of [Δ,Δ] for an infinite LEF group Δ generated by two elements;
  3. a definitions paragraph: for f.g. Δ, Ω, Λ, R_Δ, G_Δ;
  4. **Lemma B**: if Δ is infinite and LEF, then G_Δ is an infinite, f.g., simple Kazhdan group and the limit of finite simple groups SL_{3N}(F_2) whose Cayley graphs form expanders, and ξ↦diag(u_ξ,1,1) embeds [Λ,Λ], which contains [Δ,Δ], in G_Δ;
  5. **Lemma C**:
     - (a) every f.g. LEF group with solvable word problem is a subgroup of [Δ,Δ] for an infinite f.g. LEF group Δ with solvable word problem;
     - (b) if Δ has solvable word problem, so does G_Δ;
  6. "Proof of Corollaries 3 and 4".
- The old paragraphs move verbatim:
  - l.328–354 → proof of A, minus the first sentence and "Conversely,";
  - l.356–403 → proof of B;
  - l.412–455 → proof of C, with "This proves (a)." and "For (b), an element…";
  - l.404–410 and l.455–458 → the assembly.
- Restatements removed:
  - "By Theorem 2, G_Δ=EL_3(R_Δ) is an infinite … expanders." → "So Theorem 2 applies to R_Δ.";
  - "So G_Δ contains Γ≤[Δ,Δ]≤[Λ,Λ]." → ", and Δ≤Λ.";
  - "and the argument above applies to it" is dropped.
- New assembly sentence: "By Lemmas C and B, a finitely generated LEF group with solvable word problem is a subgroup of a group G_Δ as in Corollary 3 with solvable word problem."

**S5. Main-adopted reader fixes (sk-reader-grad 67819e1433), +5.**
- F6: "coefficients at u_e", in two places (l.171, l.172).
- F8: l.343–346, the two conjugates cannot both act nontrivially on a level other than 0.
- F9: l.428–429, the domain and image of θ have equally many right cosets, which gives κ.
- F10: l.153–155, "small" is split into (i) and (ii).
- E2: l.219–220, "τ_s with s=1".
- E4: l.245, cylinders defined in words ("the sets of points with a given word at given coordinates"). It is not written {x : x_[i,j]=v} because j is the exponent in the same sentence.

**S6. Cantor set Z → C, 0.** Changed in Theorem 2 (l.110–111), l.133, 136, 148, 206–207, and in F10.
- Capital C is otherwise unused in the mathematics; the only nearby C is C* on page 1.
- This touches Theorem 2 in the intro region, so merge it with sk-design-page-one's text.

## 2. Flags for main
- **Lemma C(b)** is stated for any f.g. Δ with solvable word problem. That is what the unchanged argument proves (l.445–455 use only f.g. and a solvable word problem for Δ). It restates the proof; it is not a new claim.
- **Citable envelope** (sk-usefulness P4): Lemmas A and C(a) are the LEF envelope, and Lemma B is the citable lamplighter host.
- **§1 heads** (ring and (T) / Simplicity, Steps 1–3 / Finite simple models / Brown's formulation) are compatible with splitting Theorem 2 into (a) simple and Kazhdan and (b) the finite models.
- **(T) subsection:** unchanged; it is one short paragraph. **Finite-models subsection:** a paragraph break was tried (+1 line) and reverted, since the steps already give the navigation.
- **Length.** Typeset cost is about +22 lines of 11pt, which pushes the references onto p.9. That is over the +15 target:
  - the adopted reader fixes cost about +5;
  - the lemma apparatus (statements, theorem spacing, three proof boxes) costs about +17.
- **Cheapest cuts if 8 pp is required:**
  - drop the Simplicity opening sentence (−1);
  - fold the definitions paragraph into Lemma B (−1);
  - drop E4 (−1);
  - shorten the Lemma B statement to "…expander limit, and it contains a copy of [Λ,Λ] ≥ [Δ,Δ]" (−1).

## 3. Checks
- **Cross-references:** every \ref and \eqref has a label. New labels are lem:nn, lem:host, lem:halfline. sec:proof, sec:main, sec:lef and sec:wp were already unreferenced in rev4.5.
- **Greps:** no math Z left, no τ_1, no "constant coefficients".
- **Visual pass** of pp.2–9 of the compiled PDF: the steps set as italic run-in heads, the lemmas as "Lemma A." etc., with no bad breaks.
- **F8 check:** checked against l.338–346. The conjugates act levelwise, so on a level where at most one is nontrivial the commutator is trivial. F9 was re-derived by sk-reader-grad (θ is an isomorphism between two subgroups of index |Q|).

**Sharpest next target:** main merges this candidate with sk-design-page-one, statements and example-figure, then runs one referee pass on the three lemma statements.
