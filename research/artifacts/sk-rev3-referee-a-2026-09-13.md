# sk-rev3-referee-a: correctness referee of revision 3 (2026-09-13)

Lane sk-rev3-referee-a, referee (a), correctness. Target: `sk/drafts/rev3.tex`, assembled 20:24 CDT, 614 lines, md5 2a0264d7ec918ea5b8ad7fc198a97c3d. Line numbers refer to this file. Every mathematical sentence was re-derived by hand. No Cairn nodes; referee prose only.

## 0. Verdict
- **ERROR:** none.
- **GAP:** none. The rev2 expander-generator gap (sk-panel-referee-5 B1) is closed at l.166–183: EL_3(F) is now generated by e_ij(1) and e_ij(τ), and e_ij(1) = ∏_a e_ij(τ_a) in both quotients.
- **UNCLEAR:** nine spots (U1–U9) where a competent reader stalls or a sentence says slightly more than is shown. Each has a verbatim fix of at most one line.
- **FREE STRENGTHENING:** one optional item (S1).
- **Recommendation on correctness:** rev3 can land after U9 at least; U1–U8 are polish.

## 1. What was checked (all correct)

**§1, ring and (T), l.98–126:**
- (fu^i)(f′u^j) = f(f′∘T^{-i})u^{i+j}; ue_Uu^{-1} = e_{TU}.
- u^j e_a u^{-j} = e_{\{x_{-j}=a\}}, so products are cylinder indicators.
- Generation from e_ij(u^{±1}), e_ij(e_a) through (1) and 1 = Σe_a.
- EJZ; infiniteness.

**§1, finite models, l.128–184:**
- Minimality gives a dense forward orbit.
- The border argument makes y_ℓ agree with x on [0,m+2ℓ). Windows starting in [0,m) are words of X, and every word starts in [0,m), so the (2ℓ+1)-languages agree. N_ℓ ≥ #words → ∞ (Morse–Hedlund 1938).
- D_ℓ is well defined on Z/N_ℓ, and PD(f)P^{-1} = D(f∘T^{-1}) (see U1).
- Additivity and multiplicativity for fixed r,s and large ℓ; φ_ℓ(r) ≠ 0 from extendability of words and disjoint cyclic diagonals once N_ℓ > 2|j|.
- ρ_ℓ is onto: distinct shifts give distinct length-N blocks, so the products of P^iD(e_a)P^{-i} are the diagonal units.
- EL_3(M_N(F_2)) = SL_{3N}(F_2).
- The triviality equivalence from φ_ℓ∘π = ρ_ℓ on entries of degree ≤ λ, then marked convergence and LEF.
- (T) of EL_3(F) for {e_ij(1), e_ij(τ)} gives expanders (Margulis), and the transfer to {e_ij(τ)} loses only a factor |A|.
- SL_{3N}(F_2) is simple.

**§1, simplicity, l.186–250:**
- g commutes with e_ij(r) ⟺ g_pi r = 0 (p≠i), r g_jq = 0 (q≠j), g_ii r = r g_jj. Checked entrywise from g·rE_ij and rE_ij·g.
- Small V ⊆ T^{-t}U detects c_t in ce_V, and V ⊆ U detects it in e_Vc. So g is diagonal.
- The u^t-coefficients (0<|t|≤w) of g_ii e_V = e_V g_jj live on the disjoint sets T^tV and V, so they vanish. The constants agree on every small set, hence everywhere. c ∈ LC(X,F_2)^× = {1}.
- ε_ab ε_a′b′ = δ_{ba′}ε_ab′ from V ∩ T^tV = ∅, 0<|t|≤2w.
- ψ is injective and multiplicative (the ε_ab E_pq are linearly independent because the T^aV are disjoint and nonempty). ψ(M) = eψ(M)e with e = ψ(I_d), so M ↦ I−e+ψ(M) is a group embedding.
- Between-block transvections are e_pq(ε_ab); within-block ones are commutators through p′ ≠ p.
- f u^a e_V f′ u^b = f e_{T^aV}(f′∘T^{-a})u^{a+b} ∈ {0, ε_{a,−b}}, since f is constant on T^aV and f′∘T^{-a} is constant on T^aV.
- k−I = (ghg^{-1}−h)h and k^{-1}−I = h(ghg^{-1}−h), with h^{-1} = h. k ∈ H by injectivity of ψ.
- GL_d(F_2) = PSL_d(F_2) is simple, d ≥ 3.
- J is a two-sided ideal: additive, and closed under left and right multiplication through [e_pl(s), e_lq(r)] and [e_pl(r), e_lq(s)]. It contains every translate of e_V, and a finite cover gives 1 ∈ J.
- The rank-n version, d = n(2w+1).

**§1, Brown's formulation, l.252–263:** hyperlinear ⇒ L(G) ⊂ 𝓡^ω; infinite simple ⇒ ICC, so a II_1 factor, and not RF; L(G)⊗̄𝓡 is McDuff, embeds in 𝓡^ω and contains G; Kirchberg Thm 1.1; LLP.

**§2, LEF groups, l.265–343:**
- **Overgroup.** Γ ↪ ∏_ω Q_n; doubled regular actions are even; Ore componentwise; Δ_0 is LEF (countable subgroup of an algebraic ultraproduct of finite groups); Δ = Δ_0×ℤ is infinite, f.g. and LEF; [Δ,Δ] = [Δ_0,Δ_0]×1 ⊇ Γ.
- **Lamplighter.**
  - L is a group, generated by Δ and the flip at e (u_δ-conjugates give 1_δ).
  - The action is minimal, since ⊕F_2 is dense.
  - Topologically free: f≠0 has no fixed points. For δ≠e, h ∉ W∪δW∪supp f gives h, δ^{-1}h ∉ W and (δx+f)(h) = x(δ^{-1}h) ≠ x(h) inside the cylinder. Δ is infinite, so h exists.
- **Simplicity transfer.**
  - Points with trivial stabilizer in B_{2w} form a dense open set (a finite union of closed nowhere dense sets is removed), so every nonempty clopen set contains a small set.
  - Every step of l.186–250 was rerun with u_ℓ, ℓV, products in L and word length: h-step, ε products, ψ, H, the product formula, k ∈ H, and covering. It is correct (see U6).
- **Finite models.** For large n:
  - the attached configuration of σ_ℓ(c,q) at h is c(qψ_n(δ^{-1}h)) + f(h) = (δx+f)(h);
  - σ_{ℓ1}σ_{ℓ2} = σ_{ℓ1ℓ2}, checked term by term including the F_2-cancellation on overlapping supports;
  - covariance P_ℓD_n(f)P_ℓ^{-1} = D_n(f∘ℓ^{-1});
  - so the unital ring map into ∏_ω M_{|E_n|}(F_2) is a homomorphism, G → ∏_ω GL is a group homomorphism, and simplicity plus e_12(1) ↦ e_12(I) ≠ I gives injectivity.
- **Embedding.** In characteristic 2, e_12(c)e_21(c^{-1})e_12(c) = [[0,c],[c^{-1},0]] (since 1+cc^{-1} = 0); times [[0,1],[1,0]] this gives diag(c,c^{-1}). diag(a,a^{-1},1)diag(b,b^{-1},1)diag((ba)^{-1},ba,1) = diag(aba^{-1}b^{-1},1,1). ℓ ↦ diag(u_ℓ,1,1) is a homomorphism taking [L,L] into G. No use of diag(a,a^{-1},1) as a homomorphism, and no double swap.

**Corollary 3, l.345–358:** Malcev; countably many isomorphism types; the restricted direct sum of RF groups is RF; Wilson Thm A; RF ⇒ LEF; Corollary 2.

**§3, l.360–401:**
- Restriction LC(A^ℤ,F_2)⋊ℤ → R is a ring homomorphism, so L(X) computes WP.
- u^{-t}e_au^t = e_{\{x_t=a\}}; the recursive commutator words give L(X) ≤ WP.
- Sturmian coding: arc [−(t+1)α, −tα); the closure adds no words (finite windows are open conditions); arcs between −jα, 0≤j≤m.
- c(θ)_t = ⌊θ+(t+1)α⌋ − ⌊θ+tα⌋, and the telescoped count is ⌊θ+mα⌋ since θ∈[0,1).
- Continued fraction with digits in {1,2}: the tail [a_k; a_{k+1}, …] lies in [(1+√3)/2, √3] when a_k = 1 and in [(3+√3)/2, 1+√3] when a_k = 2. The extremes are [1;2,1,2,…] = (1+√3)/2 and [2;1,2,1,…] = 1+√3. These intervals are disjoint with a gap, and α is a monotone Möbius function of the tail, so one rational comparison decides each digit.
- WP degree is an isomorphism invariant.

**§4, l.403–417:** f.p. LEF ⇒ RF; G_X not RF; the easy direction of Q3 (u ↦ u^{-1} for S^{-1}).

## 2. UNCLEAR findings, with fixes

**U1, l.152–155.** "Since $PD_\ell(f)P^{-1}=D_\ell(f\circ T^{-1})$": for f depending on [−ℓ,ℓ], f∘T^{-1} depends on [−ℓ−1,ℓ−1], so the right side is outside the definition at l.148–150. The conclusion only uses large ℓ.
Fix: "Since $PD_\ell(f)P^{-1}=D_\ell(f\circ T^{-1})$ whenever $f$ depends only on coordinates in $[-\ell+1,\ell-1]$, for fixed…"

**U2, l.171–176.** Marked convergence needs the equivalence uniformly for all words of length at most λ.
Fix: "…shows that for every $\lambda$ and large $\ell$, a word of length at most $\lambda$ is trivial in $G$ if and only if it is trivial in $\SL_{3N_\ell}(\F_2)$."

**U3, l.87–88.** "The same proof applies to the lamplighter action": the simplicity proof transfers, but §2 uses different finite models.
Fix: "The proof of simplicity also applies to the lamplighter action of a group $\Delta$ on $\F_2^{\Delta}$, and its finite models adapt. It shows…"

**U4, notation.**
- l.289 $\Z$ and l.293 $Z=\F_2^{\Delta}$ are adjacent different glyphs: rename the space $\Omega$.
- §2 reuses φ_n (l.282, cf. φ_ℓ l.152), P (l.285, cf. P l.147 and P_ℓ l.322), ψ_n (l.312, cf. ψ l.218), R and G.
- Suggested renames: $\Omega$, $\beta_n$ for the models of Γ, $\mathcal S$ for the ultraproduct of symmetric groups, $\theta_n$ for the models of Δ.

**U5, l.302–304.** Ring generation needs $u_s^{-1}$ as well.
Fix: "generated by the units $u_s$ of a symmetric generating set of $L$ and…"

**U6, l.306–310.** "word for word" leaves the reader to make four further replacements. Add one clause: "…and word length for $|t|$, so that $a,b$ range over the ball $B_w$, $d=3|B_w|$, $\epsilon_{ab}=e_{aV}u_{ab^{-1}}$ and $fu_ae_Vf'u_b\in\{0,\epsilon_{a,b^{-1}}\}$."

**U7, l.317–324.** The identity that makes the model a ring homomorphism is not displayed. Add: "For large $n$ we have $P_\ell D_n(f)P_\ell^{-1}=D_n(f\circ\ell^{-1})$, so…"

**U8, l.352–354.** For f.g. linear groups, give the reason for countability: "…every finitely generated linear group is a subgroup of $\GL_n(K)$ for a finitely generated field $K$, so there are countably many up to isomorphism."

**U9, l.409–412.** "Is there one that is sofic, or at least hyperlinear? A positive answer would also answer Open problem 6.1" overstates: a hyperlinear but nonsofic example would not answer 6.1, which asks for sofic groups.
Fix: "A sofic example would also answer Open problem~6.1 of Alekseev and Thom~\cite{AlekseevThom}, which asks…"

## 3. FREE STRENGTHENING (optional)

**S1, Corollary 3, l.345–348.** The same proof covers every recursively presented finitely generated residually finite group, since there are countably many recursive presentations. That class contains all finitely presented RF groups.
Statement: "One infinite finitely generated simple Kazhdan LEF group contains every recursively presented residually finite group and every finitely generated linear group."
The proof is unchanged ("up to isomorphism there are countably many groups of either kind"). Keep "finitely presented" if the reader should see the familiar class first.

## 4. Not in scope, noted for referee (b)
Credit wording is referee (b)'s domain. Kionke–Schesler Theorem 1.2 wording (l.268–270), Elek–Szabó Theorem 2 locator (l.184), the Rădulescu naming sentence (l.48–50), and the unverified end pages flagged in the rev3 changelog §I were not re-checked here.
