# sk-rev3-referee-a: correctness referee of revision 3 (2026-09-13)

Lane sk-rev3-referee-a, referee (a), correctness. Every mathematical sentence was re-derived by hand. No Cairn nodes; referee prose only.

**Text reviewed: revision 3 as LANDED on main**, commit 9047d0d3c9 ("Characterize LEF groups as subgroups of simple Kazhdan LEF groups"), `simple_kazhdan_sofic_group.tex`, 618 lines, md5 274f19367f7f706b05c017af721c3884. Line numbers refer to this blob.
- I first reviewed the pre-landing draft `sk/drafts/rev3.tex`, md5 2a0264d7ec918ea5b8ad7fc198a97c3d (a956f8d711, the earlier version of this file).
- The landed blob differs from that draft only in:
  - W6 (l.305): "units $u_s^{\pm1}$". This resolves finding U5 of the first pass.
  - W7 (l.311–312): "and the ball $B_w$ of $L$ for $\{a:|a|\le w\}$, so that $d=3|B_w|$". This resolves most of U6.
  - Typesetting: pdfkeywords and \keywords, an `\arxiv` macro, MSC 22D10→22D55 (Kazhdan's property (T) in MSC2020), `\qed`, `\section*{Origin and authorship}`, merged Ozawa preprint line, expanded Zap. Nauchn. Sem. POMI title.
- I checked every changed line against the full diff; none affects correctness.

## 0. Verdict
- **ERROR:** none.
- **GAP:** none. The rev2 expander-generator gap (sk-panel-referee-5 B1) is closed at l.168–185: EL_3(F) is generated by e_ij(1) and e_ij(τ), and e_ij(1) = ∏_a e_ij(τ_a) in both quotients.
- **UNCLEAR:** eight spots (U1–U4, U6′, U7–U9). Each has a verbatim fix of at most one line, for rev4.
- **FREE STRENGTHENING:** one optional item (S1).
- The mathematics of revision 3 is correct as landed.

## 1. What was checked (all correct)

**§1, ring and (T), l.100–128:**
- (fu^i)(f′u^j) = f(f′∘T^{-i})u^{i+j}; ue_Uu^{-1} = e_{TU}.
- u^je_au^{-j} = e_{\{x_{-j}=a\}}, so products are cylinder indicators.
- Generation from e_ij(u^{±1}), e_ij(e_a) via (1) and 1 = Σe_a.
- EJZ; infiniteness.

**§1, finite models, l.130–186:**
- Minimality gives a dense forward orbit.
- The border argument makes y_ℓ agree with x on [0,m+2ℓ). Windows starting in [0,m) are words of X, and every word starts in [0,m), so the (2ℓ+1)-languages agree. N_ℓ ≥ #words → ∞ (Morse–Hedlund 1938).
- D_ℓ is well defined on Z/N_ℓ; covariance holds for f of smaller radius (U1).
- φ_ℓ is additive and multiplicative for fixed r,s and large ℓ. φ_ℓ(r) ≠ 0 because words extend and cyclic diagonals are disjoint once N_ℓ > 2|j|.
- ρ_ℓ is onto: distinct shifts give distinct length-N blocks, so products of P^iD(e_a)P^{-i} give the diagonal units. EL_3(M_N(F_2)) = SL_{3N}(F_2).
- The triviality equivalence comes from φ_ℓ∘π = ρ_ℓ on entries of degree ≤ λ. It gives marked convergence and LEF.
- Property (T) of EL_3(F) for {e_ij(1), e_ij(τ)} gives expanders (Margulis). The transfer to {e_ij(τ)} loses at most a factor |A|.
- SL_{3N}(F_2) is simple.

**§1, simplicity, l.188–252:**
- g commutes with e_ij(r) ⟺ g_pi r = 0 (p≠i), r g_jq = 0 (q≠j), and g_ii r = r g_jj. Checked entrywise from g·rE_ij against rE_ij·g.
- Small V ⊆ T^{-t}U detects c_t in ce_V, and small V ⊆ U detects it in e_Vc. So g is diagonal.
- The u^t-coefficients (0<|t|≤w) of g_ii e_V = e_V g_jj live on the disjoint sets T^tV and V, so they vanish.
- The constants agree on every small set, hence everywhere. c ∈ LC(X,F_2)^× = {1}.
- ε_abε_a′b′ = δ_{ba′}ε_ab′, from V ∩ T^tV = ∅ for 0<|t|≤2w.
- ψ is injective and multiplicative: the ε_abE_pq are independent because the T^aV are disjoint and nonempty. ψ(M) = eψ(M)e with e = ψ(I_d), so M ↦ I−e+ψ(M) is a group embedding.
- Transvections between blocks are e_pq(ε_ab); those within a block are commutators through p′ ≠ p.
- fu^ae_Vf′u^b = fe_{T^aV}(f′∘T^{-a})u^{a+b} ∈ {0, ε_{a,−b}}.
- k−I = (ghg^{-1}−h)h and k^{-1}−I = h(ghg^{-1}−h), with h^{-1} = h. k ∈ H by injectivity.
- GL_d(F_2) = PSL_d(F_2) is simple for d ≥ 3.
- J is a two-sided ideal via [e_pl(s),e_lq(r)] and [e_pl(r),e_lq(s)]. It contains every translate of e_V, and a finite cover gives 1 ∈ J.
- Rank n: d = n(2w+1).

**§1, Brown's formulation, l.254–265:**
- Hyperlinear ⇒ L(G) ⊂ 𝓡^ω.
- Infinite simple ⇒ ICC, so L(G) is a II_1 factor, and G is not RF.
- L(G)⊗̄𝓡 is McDuff, embeds in 𝓡^ω, and contains G.
- Kirchberg Thm 1.1; the LLP consequence.

**§2, LEF groups, l.267–346:**
- **Overgroup:** Γ ↪ ∏_ωQ_n; doubled regular actions are even; Ore applies componentwise; Δ_0 is LEF (a countable subgroup of an algebraic ultraproduct of finite groups); Δ = Δ_0×ℤ is infinite, f.g. and LEF; [Δ,Δ] = [Δ_0,Δ_0]×1 ⊇ Γ.
- **Lamplighter:**
  - L is generated by Δ and the flip at e: u_δ-conjugates give 1_δ.
  - The action is minimal, since ⊕F_2 is dense.
  - Topologically free: f ≠ 0 has no fixed points. For δ≠e, take h ∉ W∪δW∪supp f; then h, δ^{-1}h ∉ W, and (δx+f)(h) = x(δ^{-1}h) ≠ x(h) inside the cylinder. Such h exists because Δ is infinite.
  - R is generated by u_s^{±1} and the indicator (W6 correct).
- **Simplicity transfer:**
  - Points with trivial stabilizer in B_{2w} form a dense open set (a finite union of closed nowhere dense sets is removed), so small sets exist inside every nonempty clopen set.
  - I reran l.188–252 with u_ℓ, ℓV, products in L, word length, a ∈ B_w and d = 3|B_w| (W7 correct), covering the h-step, the ε products, ψ, H, the product formula fu_ae_Vf′u_b ∈ {0, ε_{a,b^{-1}}}, k ∈ H, and covering. All steps hold.
- **Finite models (for large n):**
  - The attached configuration of σ_ℓ(c,q) at h is c(qψ_n(δ^{-1}h)) + f(h) = (δx+f)(h).
  - σ_{ℓ1}σ_{ℓ2} = σ_{ℓ1ℓ2}, checked term by term, including F_2-cancellation on overlapping supports.
  - P_ℓD_n(f)P_ℓ^{-1} = D_n(f∘ℓ^{-1}), so the unital map into ∏_ωM_{|E_n|}(F_2) is a ring homomorphism and G → ∏_ωGL is a group homomorphism.
  - Simplicity, and e_12(1) ↦ e_12(I) ≠ I, give injectivity.
- **Embedding:**
  - In characteristic 2, e_12(c)e_21(c^{-1})e_12(c) = [[0,c],[c^{-1},0]], since 1+cc^{-1} = 0. Times [[0,1],[1,0]] this is diag(c,c^{-1}).
  - diag(a,a^{-1},1)diag(b,b^{-1},1)diag((ba)^{-1},ba,1) = diag(aba^{-1}b^{-1},1,1).
  - So ℓ ↦ diag(u_ℓ,1,1) is a homomorphism taking [L,L] into G.
  - diag(a,a^{-1},1) is never used as a homomorphism, and there is no double swap.

**Corollary 3, l.348–361:** Malcev; countably many isomorphism types; a restricted direct sum of RF groups is RF; Wilson Thm A; RF ⇒ LEF; Corollary 2.

**§3, l.363–404:**
- Restriction LC(A^ℤ,F_2)⋊ℤ → R is a ring homomorphism, so L(X) computes WP.
- u^{-t}e_au^t = e_{\{x_t=a\}}, and recursive commutator words give L(X) ≤ WP.
- Sturmian arcs [−(t+1)α,−tα). The closure adds no words, because finite windows are open conditions. The arcs lie between the points −jα, 0≤j≤m.
- c(θ)_t = ⌊θ+(t+1)α⌋−⌊θ+tα⌋, and the telescoped count is ⌊θ+mα⌋ since θ∈[0,1).
- **Continued fraction with digits in {1,2}.**
  - The tail [a_k;a_{k+1},…] lies in [(1+√3)/2, √3] when a_k = 1, and in [(3+√3)/2, 1+√3] when a_k = 2.
  - The extremes are [1;2,1,2,…] = (1+√3)/2 and [2;1,2,1,…] = 1+√3.
  - These intervals are disjoint with a gap, and α is a monotone Möbius function of the tail, so one rational comparison decides each digit.
- WP degree is an isomorphism invariant.

**§4, l.406–420:** f.p. LEF ⇒ RF; G_X is not RF; the easy direction of Q3 (u ↦ u^{-1} for S^{-1}).

## 2. UNCLEAR findings, with fixes (for rev4)

**U1, l.154–157.** "Since $PD_\ell(f)P^{-1}=D_\ell(f\circ T^{-1})$". For f depending on [−ℓ,ℓ], f∘T^{-1} depends on [−ℓ−1,ℓ−1], which is outside the definition at l.150–152. The conclusion only uses large ℓ.
Fix: "Since $PD_\ell(f)P^{-1}=D_\ell(f\circ T^{-1})$ whenever $f$ depends only on coordinates in $[-\ell+1,\ell-1]$, for fixed…"

**U2, l.176–178.** Marked convergence needs the equivalence uniformly for all words of length at most λ.
Fix: "…shows that for every $\lambda$ and large $\ell$, a word of length at most $\lambda$ is trivial in $G$ if and only if it is trivial in $\SL_{3N_\ell}(\F_2)$."

**U3, l.89.** "The same proof applies to the lamplighter action". The simplicity proof transfers, but §2 uses different finite models.
Fix: "The proof of simplicity also applies to the lamplighter action of a group $\Delta$ on $\F_2^{\Delta}$, and its finite models adapt."

**U4, notation.**
- $\Z$ (l.291) and $Z=\F_2^{\Delta}$ (l.295) are different glyphs two lines apart. Rename the space $\Omega$.
- §2 reuses letters from §1: φ_n (l.284; §1 has φ_ℓ at l.154), P (l.287; §1 has P at l.149 and P_ℓ at l.326), ψ_n (l.315; §1 has ψ at l.221), R and G.
- Suggested replacements: $\beta_n$ for the models of Γ, $\mathcal S$ for the ultraproduct of symmetric groups, $\theta_n$ for the models of Δ.

**U6′, l.309–312.** W7 supplies the ball and d. The reader still has to translate "a−b" into $ab^{-1}$ in ε_ab and in the product formula.
Optional clause: "…so that $d=3|B_w|$, $\epsilon_{ab}=e_{aV}u_{ab^{-1}}$ and $fu_ae_Vf'u_b\in\{0,\epsilon_{a,b^{-1}}\}$."

**U7, l.320–327.** The identity that makes the model a ring homomorphism is not displayed.
Add: "For large $n$ we have $P_\ell D_n(f)P_\ell^{-1}=D_n(f\circ\ell^{-1})$, so…"

**U8, l.355–357.** Countability of f.g. linear groups up to isomorphism deserves its reason.
Fix: "…every finitely generated linear group is a subgroup of $\GL_n(K)$ for a finitely generated field $K$, so there are countably many up to isomorphism."

**U9, l.411–415.** "Is there one that is sofic, or at least hyperlinear? A positive answer would also answer Open problem 6.1". This overstates: a hyperlinear nonsofic example would not answer 6.1, which asks for sofic groups.
Fix: "A sofic example would also answer Open problem~6.1 of Alekseev and Thom~\cite{AlekseevThom}, which asks…"

## 3. FREE STRENGTHENING (optional)

**S1, Corollary 3, l.348–351.** The same proof covers every recursively presented finitely generated residually finite group, because there are countably many recursive presentations. This class contains all finitely presented RF groups.
Statement: "One infinite finitely generated simple Kazhdan LEF group contains every recursively presented residually finite group and every finitely generated linear group." The proof is unchanged.

## 4. Not in scope, noted for referee (b)
Credit wording is referee (b)'s domain. Not re-checked here:
- Kionke–Schesler Theorem 1.2 wording (l.270–272);
- the Elek–Szabó Theorem 2 locator (l.186);
- Rădulescu naming (l.50–52);
- the unverified end pages flagged in the rev3 changelog §I.
