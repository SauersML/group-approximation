# sk-lef-proof-compress proposal: a shorter proof of Corollary 2 (LEF iff subgroup of a simple Kazhdan LEF group)

Lane sk-lef-proof-compress (ROSTER-H, wave 10), 2026-09-13.
- **Base:** main 9047d0d3c9, `simple_kazhdan_sofic_group.tex` (618 lines, md5 274f19367f7f706b05c017af721c3884). Line numbers "l.N" refer to it.
- **Target:** the proof of Corollary~\ref{cor:lef}, l.281–346 (66 source lines).
- **Status:** the mathematics is re-derived by this lane but unreviewed, and queued with sk-verify-13. See (e).

## 0. Summary
- **Variant A** (rev4 architecture, with Theorem 2 of sk-simpler-unify, `\label{thm:general}`): 49 source lines against 66 (−17).
- **Variant B** (rev3 architecture, keeping the substitution list and the kernel sentence): 58 lines (−8).
- **Both variants:** delete the Ore bibitem (l.564–568, −5 lines), since only the old overgroup paragraph cites it. Net −22 lines (A) or −13 lines (B).
- **The brief's target was ≤ 35 lines, and it is NOT reached.** The floor is about 45 lines at the note's rigor.
  - The overgroup needs about 11 lines, the host's minimality, topological freeness and generation about 11, the models about 14, and the embedding about 7.
  - Going below that means dropping a checked step: the fixed-point argument, the covariance identity (which referee (a) U7 asked to display), or the Whitehead identities.
- **What changes mathematically:**
  1. **Overgroup without Ore.** In `Q ≀ Z/m` (with |Q| dividing m) the constant function with value q is the commutator [f,t] of f(i) = q^i with the shift. An ultraproduct of these wreath products puts Γ inside [Δ,Δ] with Δ = ⟨t, f_s⟩. This replaces doubled regular actions, parity, Ore's theorem and the ×Z factor: Δ is infinite because t has infinite order.
  2. **Exact models.** On Y_n = F_2^{Λ_n}, f acts by the diagonal matrix of f∘p_n with p_n(y) = y∘κ_n. That is an exact ring homomorphism, which replaces "values at the attached configurations". The covariance identity U_n(ℓ)D_n(f)U_n(ℓ)^{-1} = D_n(f∘ℓ^{-1}) is displayed (referee (a) U7).
  3. **Inline Whitehead identities** in place of the display (−7 lines).
- **Compile:** MSI texlive/2025, pdflatex ×3.
  - candA.tex (base with the Theorem 2 stub at l.95 and variant A; md5 83419a0b06bbef4f8128f456bf0d4fd4) and candB.tex (base with variant B; md5 7f69ec2779da201a316a715feb72fc62).
  - Both: 0 errors, 0 overfull, 0 undefined, 7 pages.
  - Both have one underfull box, the Champetier bibitem, identical to the base compile of main's rev3 in the same run.
  - Files: `sk/lanes/sk-lef-proof-compress/{proofA,proofB,candA,candB,stubA}.tex`; on MSI in `/scratch.global/sauer354/sk/sk-lef-proof-compress/`.

## (a) Current text, l.281–346 verbatim
```latex
\begin{proof}
Subgroups of LEF groups are LEF. Conversely, let $\Gamma$ be LEF and
generated by a finite set $S$, with finite models
$\varphi_n\colon B_n\to Q_n$ on word balls. Then
$\gamma\mapsto(\varphi_n(\gamma))_n$ embeds $\Gamma$ in the algebraic
ultraproduct $\prod_\omega Q_n$, and the doubled regular actions embed
this group in $P=\prod_\omega\operatorname{Sym}(Q_n\times\{1,2\})$ by even
permutations. Every even permutation of a finite set is a commutator of
two permutations~\cite[Theorem~1]{Ore}, so each $s\in S$ is a commutator
of two elements of $P$. Let $\Delta_0\le P$ be generated by $S$ and these
elements, and put $\Delta=\Delta_0\times\Z$. A countable subgroup of an
algebraic ultraproduct of finite groups is LEF, so $\Delta$ is an
infinite finitely generated LEF group with $\Gamma\le[\Delta,\Delta]$.

Let $\Delta$ act on $Z=\F_2^{\Delta}$ by $(\delta x)(h)=x(\delta^{-1}h)$,
and let $L$ be the group of maps $x\mapsto\delta x+f$ with
$\delta\in\Delta$ and $f$ finitely supported. It is generated by $\Delta$
and the map adding $1$ at the coordinate $e$. Every orbit contains
$x+\bigoplus_\Delta\F_2$, so the action is minimal. It is topologically
free: for $f\ne0$ the map $x\mapsto x+f$ has no fixed point, and if
$\delta\ne e$, every cylinder over a finite set $W$ contains points with
$x(h)\ne x(\delta^{-1}h)$ for some
$h\notin W\cup\delta W\cup\operatorname{supp}f$, and $x\mapsto\delta x+f$
moves them. Put $R=\LC(Z,\F_2)\rtimes L$ and $G=\EL_3(R)$. The ring $R$
is generated by the units $u_s^{\pm1}$ for the generators $s$ of $L$ and
the indicator of $\{x:x(e)=1\}$, so $G$ is finitely generated and has
property~\textup{(T)}~\cite[Theorem~1.1]{EJZ}, and it is infinite. Points
with trivial stabilizer in a given ball are dense, so every nonempty
clopen set contains a small one, in the sense of Section~1 with $u_\ell$
for $u^t$, $\ell V$ for $T^tV$, products in $L$ for sums of exponents,
word length for $|t|$, and the ball $B_w$ of $L$ for $\{a:|a|\le w\}$, so
that $d=3|B_w|$. With these replacements the proof of
simplicity in Section~1 applies word for word, so $G$ is simple.

For finite models $\psi_n\colon B_n\to Q'_n$ of $\Delta$ put
$E_n=\F_2^{Q'_n}\times Q'_n$, and attach to $(c,q)\in E_n$ the
configuration $h\mapsto c(q\psi_n(h))$ on $B_n$. Let $x\mapsto\delta x+f$
act on $E_n$ by
$(c,q)\mapsto\bigl(c+\sum_{h\in\operatorname{supp}f}1_{q'\psi_n(h)},q'\bigr)$
with $q'=q\psi_n(\delta^{-1})$. For fixed elements of $L$ and large $n$,
these permutations multiply as in $L$ and move the attached
configurations near $e$ as $L$ moves points of $Z$. Every finite pattern
occurs in $Z$, so a function depending on coordinates in a ball defines a
diagonal matrix $D_n(f)$ by its values at the attached configurations,
and $\sum_\ell f_\ell u_\ell\mapsto\sum_\ell D_n(f_\ell)P_\ell$, with
$P_\ell$ the permutation matrices, is a unital ring homomorphism from $R$
to the algebraic ultraproduct $\prod_\omega M_{|E_n|}(\F_2)$. It maps $G$
to the ultraproduct of the groups $\GL_{3|E_n|}(\F_2)$. The kernel is a
normal subgroup of the simple group $G$ that does not contain
$e_{12}(1)$, so $G$ embeds there and is LEF.

Finally, $\ell\mapsto u_\ell$ embeds $L$ in $\GL_1(R)$. Over $\F_2$, for
units $a,b,c$ of $R$,
\[
 \begin{aligned}
 e_{12}(c)e_{21}(c^{-1})e_{12}(c)\,e_{12}(1)e_{21}(1)e_{12}(1)
 &=\operatorname{diag}(c,c^{-1},1),\\
 \operatorname{diag}(a,a^{-1},1)\operatorname{diag}(b,b^{-1},1)
 \operatorname{diag}\bigl((ba)^{-1},ba,1\bigr)
 &=\operatorname{diag}(aba^{-1}b^{-1},1,1).
 \end{aligned}
\]
So $\ell\mapsto\operatorname{diag}(u_\ell,1,1)$ is an injective
homomorphism $L\to\GL_3(R)$ that maps $[L,L]$ into $G$, and
$\Gamma\le[\Delta,\Delta]\le[L,L]$.
\end{proof}
```

## (b) Replacement A (with Theorem 2), verbatim; replaces l.281–346
```latex
\begin{proof}
Subgroups of LEF groups are LEF. Conversely, let $\Gamma$ be LEF and
generated by a finite set $S$, with finite models
$\varphi_n\colon B_n\to Q_n$ on word balls. Let
$\Lambda_n=Q_n^{\Z/m_n}\rtimes\Z/m_n$ with $m_n=n|Q_n|$, where the
generator $t$ of $\Z/m_n$ acts by $(tft^{-1})(i)=f(i-1)$. The constant
function with value $q$ is the commutator $[f,t]$ with $f(i)=q^i$. So in
the algebraic ultraproduct $\prod_\omega\Lambda_n$ over a nonprincipal
ultrafilter $\omega$, which contains $\Gamma$ as the constant functions
with values $\varphi_n(\gamma)$, each $s\in S$ is a commutator
$[f_s,t]$. The group $\Delta$ generated by $t$ and the $f_s$ is infinite,
as $t$ has infinite order, and $\Gamma\le[\Delta,\Delta]$.

Let $L$ be the group of maps $x\mapsto\delta x+f$ of $Z=\F_2^{\Delta}$,
where $(\delta x)(h)=x(\delta^{-1}h)$ and $f$ is finitely supported. It is
generated by $\Delta$ and the map adding $1$ at $e$, and its orbits
contain the dense sets $x+\bigoplus_\Delta\F_2$. If $(\delta,f)\ne(e,0)$,
the fixed points of $x\mapsto\delta x+f$ satisfy
$x(h)=x(\delta^{-1}h)+f(h)$ for all $h$. In every cylinder over a finite
set $W$ this fails at some point, for $h\in\operatorname{supp}f$ if
$\delta=e$ and for $h\notin W\cup\delta W\cup\operatorname{supp}f$ if
$\delta\ne e$. So the action is minimal and topologically free. The ring
$R=\LC(Z,\F_2)\rtimes L$ is generated by the units $u_s^{\pm1}$ for the
generators $s$ of $L$ and the indicator of $\{x:x(e)=1\}$.

Choose representatives $\kappa_n(\delta)\in\Lambda_n$ of the elements of
$\Delta$, let $x\mapsto\delta x+f$ act on $Y_n=\F_2^{\Lambda_n}$ by
$\sigma_n(\ell)y=\kappa_n(\delta)y+\sum_{h\in\operatorname{supp}f}1_{\kappa_n(h)}$,
where $(qy)(q')=y(q^{-1}q')$, and put $p_n(y)=y\circ\kappa_n\in Z$. For
fixed $\ell,\ell'\in L$ and finitely many coordinates, $\kappa_n$ is
multiplicative and injective on the elements involved for
$\omega$-almost all $n$. Then $\sigma_n(\ell\ell')=\sigma_n(\ell)\sigma_n(\ell')$,
and $p_n\circ\sigma_n(\ell)$ agrees with $\ell\circ p_n$ at these
coordinates. So the diagonal matrices $D_n(f)$ of $f\circ p_n$ and the
permutation matrices $U_n(\ell)$ of $\sigma_n(\ell)$ satisfy
$U_n(\ell)D_n(f)U_n(\ell)^{-1}=D_n(f\circ\ell^{-1})$, and
$\sum_\ell f_\ell u_\ell\mapsto(\sum_\ell D_n(f_\ell)U_n(\ell))_n$ is a
unital ring homomorphism from $R$ to $\prod_\omega M_{|Y_n|}(\F_2)$. By
Theorem~\ref{thm:general}, $G=\EL_3(R)$ is an infinite finitely generated
simple Kazhdan LEF group.

Finally, $\ell\mapsto\operatorname{diag}(u_\ell,1,1)$ is an injective
homomorphism $L\to\GL_3(R)$. It maps $[L,L]$ into $G$, since over $\F_2$
every unit $c$ satisfies $\operatorname{diag}(c,c^{-1},1)
=e_{12}(c)e_{21}(c^{-1})e_{12}(c)\,e_{12}(1)e_{21}(1)e_{12}(1)$, and the
product of these matrices for $c=a$, $b$ and $(ba)^{-1}$ is
$\operatorname{diag}(aba^{-1}b^{-1},1,1)$. So $G$ contains
$\Gamma\le[\Delta,\Delta]\le[L,L]$.
\end{proof}
```

## (b′) Replacement B (rev3 architecture, no Theorem 2), verbatim; replaces l.281–346
```latex
\begin{proof}
Subgroups of LEF groups are LEF. Conversely, let $\Gamma$ be LEF and
generated by a finite set $S$, with finite models
$\varphi_n\colon B_n\to Q_n$ on word balls. Let
$\Lambda_n=Q_n^{\Z/m_n}\rtimes\Z/m_n$ with $m_n=n|Q_n|$, where the
generator $t$ of $\Z/m_n$ acts by $(tft^{-1})(i)=f(i-1)$. The constant
function with value $q$ is the commutator $[f,t]$ with $f(i)=q^i$. So in
the algebraic ultraproduct $\prod_\omega\Lambda_n$ over a nonprincipal
ultrafilter $\omega$, which contains $\Gamma$ as the constant functions
with values $\varphi_n(\gamma)$, each $s\in S$ is a commutator
$[f_s,t]$. The group $\Delta$ generated by $t$ and the $f_s$ is infinite,
as $t$ has infinite order, and $\Gamma\le[\Delta,\Delta]$.

Let $L$ be the group of maps $x\mapsto\delta x+f$ of $Z=\F_2^{\Delta}$,
where $(\delta x)(h)=x(\delta^{-1}h)$ and $f$ is finitely supported. It is
generated by $\Delta$ and the map adding $1$ at $e$, and its orbits
contain the dense sets $x+\bigoplus_\Delta\F_2$. If $(\delta,f)\ne(e,0)$,
the fixed points of $x\mapsto\delta x+f$ satisfy
$x(h)=x(\delta^{-1}h)+f(h)$ for all $h$. In every cylinder over a finite
set $W$ this fails at some point, for $h\in\operatorname{supp}f$ if
$\delta=e$ and for $h\notin W\cup\delta W\cup\operatorname{supp}f$ if
$\delta\ne e$. So the action is minimal and topologically free. Put
$R=\LC(Z,\F_2)\rtimes L$ and $G=\EL_3(R)$. The ring $R$ is generated by
the units $u_s^{\pm1}$ for the generators $s$ of $L$ and the indicator of
$\{x:x(e)=1\}$, so $G$ is finitely generated and has
property~\textup{(T)}~\cite[Theorem~1.1]{EJZ}, and it is infinite. Points
with trivial stabilizer in a given ball are dense, so every nonempty
clopen set contains a small one, in the sense of Section~1 with $u_\ell$
for $u^t$, $\ell V$ for $T^tV$, products in $L$ for sums of exponents,
word length for $|t|$, and the ball $B_w$ of $L$ for $\{a:|a|\le w\}$, so
that $d=3|B_w|$. With these replacements the proof of simplicity in
Section~1 applies word for word, so $G$ is simple.

Choose representatives $\kappa_n(\delta)\in\Lambda_n$ of the elements of
$\Delta$, let $x\mapsto\delta x+f$ act on $Y_n=\F_2^{\Lambda_n}$ by
$\sigma_n(\ell)y=\kappa_n(\delta)y+\sum_{h\in\operatorname{supp}f}1_{\kappa_n(h)}$,
where $(qy)(q')=y(q^{-1}q')$, and put $p_n(y)=y\circ\kappa_n\in Z$. For
fixed $\ell,\ell'\in L$ and finitely many coordinates, $\kappa_n$ is
multiplicative and injective on the elements involved for
$\omega$-almost all $n$. Then $\sigma_n(\ell\ell')=\sigma_n(\ell)\sigma_n(\ell')$,
and $p_n\circ\sigma_n(\ell)$ agrees with $\ell\circ p_n$ at these
coordinates. So the diagonal matrices $D_n(f)$ of $f\circ p_n$ and the
permutation matrices $U_n(\ell)$ of $\sigma_n(\ell)$ satisfy
$U_n(\ell)D_n(f)U_n(\ell)^{-1}=D_n(f\circ\ell^{-1})$, and
$\sum_\ell f_\ell u_\ell\mapsto(\sum_\ell D_n(f_\ell)U_n(\ell))_n$ is a
unital ring homomorphism from $R$ to $\prod_\omega M_{|Y_n|}(\F_2)$. It
maps $G$ into $\prod_\omega\GL_{3|Y_n|}(\F_2)$, and its kernel on $G$ is a
normal subgroup that does not contain $e_{12}(1)$. So the simple group $G$
embeds in an algebraic ultraproduct of finite groups, and it is LEF.

Finally, $\ell\mapsto\operatorname{diag}(u_\ell,1,1)$ is an injective
homomorphism $L\to\GL_3(R)$. It maps $[L,L]$ into $G$, since over $\F_2$
every unit $c$ satisfies $\operatorname{diag}(c,c^{-1},1)
=e_{12}(c)e_{21}(c^{-1})e_{12}(c)\,e_{12}(1)e_{21}(1)e_{12}(1)$, and the
product of these matrices for $c=a$, $b$ and $(ba)^{-1}$ is
$\operatorname{diag}(aba^{-1}b^{-1},1,1)$. So $G$ contains
$\Gamma\le[\Delta,\Delta]\le[L,L]$.
\end{proof}
```

**Bibliography:** in both variants delete l.564–568:
```latex
\bibitem{Ore}
O.~Ore, \emph{Some remarks on commutators},
Proc. Amer. Math. Soc. \textbf{2} (1951), 307--314.
\doi{10.1090/S0002-9939-1951-0040298-4}.

```

## (c) Length (source lines, `wc -l`)

| | rev3 l.281–346 | variant A | variant B |
|---|---|---|---|
| proof environment | 66 | 49 | 58 |
| overgroup paragraph | 12 | 12 | 12 |
| host paragraph (minimal, top. free, generation; B adds (T) and simplicity) | 20 | 11 | 20 |
| models paragraph (B adds the kernel sentence) | 16 | 15 | 19 |
| embedding paragraph | 14 | 7 | 7 |
| Ore bibitem | 5 | 0 | 0 |
| **net change** | | **−22** | **−13** |

Variant A's host paragraph is shorter because Theorem 2 absorbs (T), infiniteness, simplicity and LEF.

## (d) Reader-facing trade-off
- **Shorter, with nothing new to trust.** The overgroup is a displayed one-line commutator identity and needs no citation, instead of doubled regular actions, parity and Ore's theorem. The models are exact diagonal and permutation matrices with a displayed covariance identity, instead of the unexplained "attached configurations".
- **Referee fixes it absorbs:**
  - referee (a) U7: the covariance identity is displayed;
  - referees (a)/(b) U4, partly: ω is introduced as a nonprincipal ultrafilter; the ultraproduct group P and the ψ_n clash with §1's ψ are gone (κ_n); E_n and the attached configurations are gone.
- **What the reader loses:** nothing. Δ is now generated by |S|+1 elements, a small bonus the text doesn't advertise.

## (e) Status and nodes (landed with this artifact)
- **`lef-groups-lie-in-derived-subgroups-of-infinite-lef-groups`** (claim) + `-proof` (route, `requires: []`, complete derivation with model tests). ESTABLISHED, unreviewed. `distinct_from` the Ore-based `rf-groups-lie-in-derived-subgroups-of-rf-groups`.
- **`lef-groups-embed-in-simple-kazhdan-lef-groups-wreath-proof`** (route): the note-level proof, target `lef-groups-embed-in-simple-kazhdan-lef-groups` (already established with three PASSes on its Ore route). It requires the new claim and `topologically-free-matricial-crossed-el-simple-kazhdan-lef` (Theorem 2, unreviewed; with sk-verify-8/9). Body: the fixed-point argument, the exact covariance computation including the F_2 cancellation on overlapping supports, and both Whitehead identities.
- **Queued with sk-verify-13:** the claim and the route.
- **Firewalls respected:** diag(a,a^{-1},1) is never a homomorphism; no double swap. The copy of Γ is the diagonal of constant functions, which is a homomorphism.

## Verification record (this lane, by hand)
1. **Commutator:** with (tft^{-1})(i) = f(i−1), (t f^{-1} t^{-1})(i) = q^{−(i−1)}, so [f,t](i) = q^i q^{−(i−1)} = q. f is well defined on Z/m_n as q^{|Q_n|} = e and m_n = n|Q_n|.
2. **Γ ↪ ∏_ω Λ_n** through constant functions: a homomorphism, and injective since φ_n is injective on B_n with φ_n(e) = e.
3. **Δ is infinite** (t^k ≠ e once m_n > |k|) and LEF (a countable subgroup of an algebraic ultraproduct of finite groups).
4. **Topological freeness:**
   - fixed points satisfy x(h) = x(δ^{-1}h) + f(h);
   - δ = e, f ≠ 0: this fails at h ∈ supp f for every x;
   - δ ≠ e: in a cylinder over W, pick h ∉ W ∪ δW ∪ supp f (possible as Δ is infinite). Then h ≠ δ^{-1}h, both outside W, so some x in the cylinder has x(h) ≠ x(δ^{-1}h).
5. **σ_n multiplicative ω-a.e.:** ℓℓ′ = (δδ′, f + δf′), and σ_n(ℓ)σ_n(ℓ′) adds Σ_{h∈supp f}1_{κ_n(h)} + Σ_{h′∈supp f′}1_{κ_n(δ)κ_n(h′)}. This equals Σ_{h∈supp(f+δf′)}1_{κ_n(h)} once κ_n is multiplicative on the δh′ and injective on supp f ∪ δ supp f′.
6. **Covariance:** p_n(σ_n(ℓ)y)(h) = y(κ_n(δ)^{-1}κ_n(h)) + #{h′∈supp f: κ_n(h′) = κ_n(h)} = y(κ_n(δ^{-1}h)) + f(h) = (ℓ p_n(y))(h) on a fixed finite H, uniformly in y. Then U_n(ℓ)D_n(f)U_n(ℓ)^{-1} = D_n(f∘p_n∘σ_n(ℓ^{-1})) = D_n(f∘ℓ^{-1}), and (D(f)U(k))(D(f′)U(ℓ)) = D(f(f′∘k^{-1}))U(kℓ), matching (fu_k)(f′u_ℓ) = f(f′∘k^{-1})u_{kℓ}.
7. **Whitehead over F_2:** e_12(c)e_21(c^{-1})e_12(c) = [[0,c],[c^{-1},0]] since 1 + cc^{-1} = 0. Times e_12(1)e_21(1)e_12(1) = [[0,1],[1,0]] this gives diag(c,c^{-1},1). Also diag(a,a^{-1},1)diag(b,b^{-1},1)diag((ba)^{-1},ba,1) = diag(aba^{-1}b^{-1},1,1).

## Remarks for the editor
- **Compatible with item 71(c)** (hosts as marked limits of finite simple SL_N(F_2) expanders). Replace Λ_n by the subgroup generated by the images of the generators of Δ. Then the conjugates of the letter e_{x(e)=1} give all coordinate functions of Y_n, and the lamp translations act transitively, so the letters generate M_{|Y_n|}(F_2). The Section 1 quotient-of-free-algebra argument applies along a subsequence; this is sk-strong-8's route, not re-derived here.
- **Notation left as in rev3/Theorem 2:** Z for F_2^Δ (referee (b) suggests Ω), and ℓ for elements of L (Theorem 2's §1 uses u_ℓ throughout).
- **Intro sentence l.89–93** is unchanged. If variant A is adopted, sk-simpler-unify's Block A transition replaces it.
