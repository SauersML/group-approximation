# STW 99-problems: classifying actions cluster (L--LVI)

*2026-08-30 goal session.  Source: arXiv:2506.10902 v2, "Finding loops
of automorphisms" + "Classifying actions on C*-algebras" (extraction
#53--59; numerals shifted by commented #6, #19, #35).*

## Cluster logic

The Gabe--Szabo dynamical Kirchberg--Phillips theorem splits the
territory: everything `O_∞`-flavoured is classified (LI for Kirchberg
targets, flows with isometric shift absorption), and every open
problem here is a stably-finite/tracial shadow of it.  The recorded
factorization of the central challenge LIII: equivariant existence
(= equivariant Conjecture D, i.e. Problem LVII made equivariant) +
equivariant `Z`-stability (Problem XXXIX) + Szabo's January 2026
equivariant KK-uniqueness ⟹ stably finite dynamical KP.  LIV reduces
by Takai duality to wild-boundary trace-scaling Rokhlin theorems
(Bratteli--Kishimoto minus the finitely-many-extremal-rays crutch) —
the same equivariant-glueing frontier as XXXVIII/XXXIX/XLII.

## Sharp new records

* **LV (quasifree flows)**: the KMS inverse temperature `β(λ)` solving
  `e^{-β} + e^{-λβ} = 1` is proposed as a complete cocycle-conjugacy
  invariant for irrational `λ > 0`; the missing verification is only
  that KMS temperatures on the unique tracial ray are
  cocycle-perturbation-invariant, checked against
  Kishimoto--Kumjian.  Flagged as the cluster's cheapest potential
  complete solution.
* **LII (Blackadar lifting)**: the simple-AF involution case is
  isolated as an EQUIVARIANT Effros--Handelman--Shen problem —
  realize a `Z/2`-dimension group by a `Z/2`-system of `Z^n`'s with
  positivity.  Purely ordered-`K_0`; no known obstruction; a proof
  handles all finite cyclic `G` on AF algebras.
* **LVI (untwisting)**: conjecture-shaped receptacle
  `H^2(G, non-stable K-data of A)`; Popa's von Neumann theorem =
  triviality of the receptacle; links untwisting to the
  K1-injectivity cluster (Jiang K-stability explains the tractable
  `Z`-coefficient cases).
* **L (loops)**: inner loops contribute `K_0(A_θ)/Z[1] ≅ Z`; rotation
  contributes `Z^2`; the remaining generators must be
  `Hom(K_1, K_0)`-classes (rank 4 block matches `6 − 2` only if the
  inner `Z` is absorbed — the bookkeeping suggests the six generators
  are rotation `Z^2` + four `Hom(K_1,K_0) = M_2(Z)` classes, with
  inner loops landing inside them); groupoid models on
  `A_θ ⊗ O_∞` are STW's and this record's preferred visibility
  mechanism.

## Status

Seven roots (L--LVI), all open, no establishments; Attempts carry the
factorizations above.  Coverage now contiguous XVIII--LXII.
