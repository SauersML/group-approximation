# sk-design-structure: verbatim diff against rev4.5

OLD = rev4.5 `simple_kazhdan_sofic_group.tex` (md5 29d63f7944bccccbf9a91b0b2714c0b4). NEW = `$SK2/lanes/sk-design-structure/candidate.tex` (md5 33a388311bec7d3c8e68632b9025802b). Unified diff with one line of context. The report is `research/artifacts/sk-design-structure-2026-09-14.md`.

```diff
@@ -9,2 +9,4 @@
 \newtheorem{corollary}[theorem]{Corollary}
+\newtheorem{lemma}{Lemma}
+\renewcommand{\thelemma}{\Alph{lemma}}
 \newcommand{\F}{\mathbb F}
@@ -110,3 +112,3 @@
 Let a finitely generated group $\Lambda$ act minimally and topologically
-freely on a Cantor set $Z$, and let $R=\LC(Z,\F_2)\rtimes\Lambda$ be
+freely on a Cantor set $C$, and let $R=\LC(C,\F_2)\rtimes\Lambda$ be
 generated by a finite set $S\ni1$. Suppose that there are maps
@@ -132,4 +134,4 @@
 \subsection*{The ring and property (T)}
-Write $e_U$ for the indicator of a clopen set $U\subseteq Z$. The ring
-$R=\bigoplus_{\xi\in\Lambda}\LC(Z,\F_2)\,u_\xi$ has the multiplication
+Write $e_U$ for the indicator of a clopen set $U\subseteq C$. The ring
+$R=\bigoplus_{\xi\in\Lambda}\LC(C,\F_2)\,u_\xi$ has the multiplication
 $(fu_\xi)(f'u_\eta)=f\,(f'\circ\xi^{-1})\,u_{\xi\eta}$, so
@@ -147,10 +149,15 @@
 So $G$ has property~\textup{(T)}, and it is infinite because
-$e_{12}(\LC(Z,\F_2))$ is infinite.
+$e_{12}(\LC(C,\F_2))$ is infinite.
 
 \subsection*{Simplicity}
-Let $1\ne K\trianglelefteq G$ and $1\ne g\in K$, and let $w\ge0$ bound the
+Let $1\ne K\trianglelefteq G$ and $1\ne g\in K$. We show in three steps
+that $K=G$.
+
+\subsubsection*{Step~1: a nontrivial commutator with a small elementary matrix}
+Let $w\ge0$ bound the
 word lengths of the $\xi$ with $u_\xi$ occurring in the entries of $g$ and
-$g^{-1}$. Call a clopen set $V$ small if $V\cap\xi V=\varnothing$ for
-$\xi\in B_{2w}\setminus\{e\}$ and every $f\circ\xi$, with $\xi\in B_w$ and
-$f$ a coefficient of an entry of $g$ or $g^{-1}$, is constant on $V$.
+$g^{-1}$. Call a clopen set $V$ small if (i) $V\cap\xi V=\varnothing$ for every
+$\xi\in B_{2w}\setminus\{e\}$, and (ii) $f\circ\xi$ is constant on $V$ for
+every $\xi\in B_w$ and every coefficient $f\in\LC(C,\F_2)$ of an entry of
+$g$ or $g^{-1}$.
 Each $\xi\ne e$ fixes a closed set with empty interior, so the points
@@ -169,8 +176,10 @@
 $\xi V$ and $V$, so they vanish, and the same argument shows that the
-corresponding coefficients of $g_{ii}$ and $g_{jj}$ vanish. The constant
-coefficients agree on every small $V$, so they agree everywhere. So
-$g=cI_3$ with $c\in\LC(Z,\F_2)$, and comparing constant coefficients in
-$cc^{-1}=1$ gives $c=1$ and $g=1$.
+corresponding coefficients of $g_{ii}$ and $g_{jj}$ vanish. The coefficients
+at $u_e$ agree on every small $V$, so they agree everywhere. So
+$g=cI_3$ with $c\in\LC(C,\F_2)$, and comparing coefficients at $u_e$ in
+$cc^{-1}=1$ gives $c=1$ and $g=1$. Fix such $h$ and $V$. Then $x=[g,h]$
+lies in $K\setminus\{1\}$.
 
-Fix such $h$ and $V$, and put $d=3|B_w|$. For $a,b\in B_w$ put
+\subsubsection*{Step~2: the commutator lies in a finite simple group over a tower}
+Put $d=3|B_w|$. For $a,b\in B_w$ put
 $\epsilon_{ab}=e_{aV}u_{ab^{-1}}$. Since
@@ -186,3 +195,3 @@
 
-Put $x=[g,h]\in K\setminus\{1\}$. If $a,b\in B_w$ and $f,f'$ are
+If $a,b\in B_w$ and $f,f'$ are
 coefficients as above, then
@@ -201,3 +210,4 @@
 
-Finally, $J=\{r\in R:e_{pq}(r)\in K\text{ for all }p\ne q\}$ is a
+\subsubsection*{Step~3: $K$ contains every elementary matrix}
+The set $J=\{r\in R:e_{pq}(r)\in K\text{ for all }p\ne q\}$ is a
 two-sided ideal by~\eqref{eq:elementary}, since
@@ -206,3 +216,3 @@
 $e_{\xi V}=u_\xi e_Vu_\xi^{-1}$. By minimality and compactness finitely many
-of these sets cover $Z$, so $1=1-\prod_i(1-e_{\xi_iV})\in J$. So $J=R$ and
+of these sets cover $C$, so $1=1-\prod_i(1-e_{\xi_iV})\in J$. So $J=R$ and
 $K=G$.
@@ -218,4 +228,4 @@
 between blocks. The group $\EL_3(F)$ is generated by the $e_{ij}(1)$ and
-$e_{ij}(\tau_s)$, and in both quotients $e_{ij}(1)$ and $e_{ij}(\tau_1)$
-have the same image. A word of length $\lambda$ or less in
+$e_{ij}(\tau_s)$, and in both quotients $e_{ij}(1)$ and $e_{ij}(\tau_s)$
+with $s=1$ have the same image. A word of length $\lambda$ or less in
 the $e_{ij}(\tau_s)$ has entries of degree at most $\lambda$ in $F$, and
@@ -236,2 +246,14 @@
 
+\subsection*{Brown's formulation}
+Let $G$ be a group as in Theorem~\ref{thm:general}. LEF groups are
+sofic~\cite[Example~4.5]{Pestov}, and sofic groups are
+hyperlinear~\cite[Theorem~2]{ElekSzabo}. So $L(G)$ embeds in
+$\mathcal R^\omega$~\cite{Kirchberg,Radulescu},
+\cite[Proposition~7.1]{Ozawa}. Since $G$ is infinite and simple, its
+nontrivial conjugacy classes are infinite and $G$ is not residually
+finite. So $L(G)$ is a $\mathrm{II}_1$ factor, and
+$L(G)\mathbin{\bar\otimes}\mathcal R$ is a McDuff factor~\cite{McDuff}
+that embeds in $\mathcal R^\omega$ and whose unitary group contains $G$,
+as in Brown's question.
+
 \section{Proof of Theorem~\ref{thm:main}}\label{sec:main}
@@ -244,3 +266,4 @@
 $S=\{1,u,u^{-1}\}\cup\{e_a:a\in A\}$, where $e_a=e_{\{x:x_0=a\}}$, since
-products of the translates $u^je_au^{-j}$ are the cylinder indicators,
+products of the translates $u^je_au^{-j}$ are the indicators of the
+cylinders, the sets of points with a given word at given coordinates,
 which span $\LC(X,\F_2)$. We write $\tau_\pm$ and $\tau_a$ for the letters
@@ -272,15 +295,2 @@
 
-
-\subsection*{Brown's formulation}
-Let $G$ be a group as in Theorem~\ref{thm:general}. LEF groups are
-sofic~\cite[Example~4.5]{Pestov}, and sofic groups are
-hyperlinear~\cite[Theorem~2]{ElekSzabo}. So $L(G)$ embeds in
-$\mathcal R^\omega$~\cite{Kirchberg,Radulescu},
-\cite[Proposition~7.1]{Ozawa}. Since $G$ is infinite and simple, its
-nontrivial conjugacy classes are infinite and $G$ is not residually
-finite. So $L(G)$ is a $\mathrm{II}_1$ factor, and
-$L(G)\mathbin{\bar\otimes}\mathcal R$ is a McDuff factor~\cite{McDuff}
-that embeds in $\mathcal R^\omega$ and whose unitary group contains $G$,
-as in Brown's question.
-
 \section{LEF groups}\label{sec:lef}
@@ -326,5 +336,15 @@
 
+We embed a countable LEF group in the derived subgroup of a
+$2$-generated LEF group $\Delta$ (Lemma~\ref{lem:nn}), and then
+$[\Delta,\Delta]$ in a group of Theorem~\ref{thm:general} built from a
+lamplighter action of $\Delta$ (Lemma~\ref{lem:host}).
+Lemma~\ref{lem:halfline} keeps the word problem solvable in both steps.
+
+\begin{lemma}\label{lem:nn}
+Every countable LEF group is a subgroup of $[\Delta,\Delta]$ for an
+infinite LEF group $\Delta$ generated by two elements.
+\end{lemma}
+
 \begin{proof}
-Limits of finite groups in the space of marked groups are LEF, and so are
-their subgroups. Conversely, let $\Gamma=\{\gamma_1,\gamma_2,\dots\}$ be a
+Let $\Gamma=\{\gamma_1,\gamma_2,\dots\}$ be a
 countable LEF group, replaced by $\Gamma\times\Z$ so that it is infinite.
@@ -342,6 +362,7 @@
 $i$ as $a_k$ acts on the level $i+n$. As distinct pairs of powers of $2$
-have distinct differences, all below a quarter of the modulus, the word
-$w_j=[b^{-2^{2j-1}}ab^{2^{2j-1}},\,b^{-4^j}ab^{4^j}]$ evaluated at
-$(a_k,b_k)$ acts by $[f_{jk},t_k]$ on the level $0$ and trivially on the
-other levels, for $j\le k$. In the algebraic ultraproduct over $\omega$ of
+have distinct differences, all below a quarter of the modulus, the two
+conjugates in $w_j=[b^{-2^{2j-1}}ab^{2^{2j-1}},\,b^{-4^j}ab^{4^j}]$,
+evaluated at $(a_k,b_k)$, cannot both act nontrivially on a level other
+than $0$. So $w_j(a_k,b_k)$ acts by $[f_{jk},t_k]$ on the level $0$ and
+trivially on the other levels, for $j\le k$. In the algebraic ultraproduct over $\omega$ of
 the symmetric groups of these sets, sending $\gamma$ to the class of the
@@ -354,7 +375,20 @@
 coordinate, so $\Delta$ is infinite, finitely generated and LEF.
+\end{proof}
 
-Let $\Delta$ act on $\Omega=\F_2^{\Delta}$ by
-$(\delta x)(\eta)=x(\delta^{-1}\eta)$, and let $\Lambda$ be the group of
-maps $x\mapsto\delta x+c$ with $\delta\in\Delta$ and $c$ finitely
-supported. It is generated by $\Delta$ and the map $z$ adding $1$ at $e$,
+For a finitely generated group $\Delta$, let $\Delta$ act on
+$\Omega=\F_2^{\Delta}$ by $(\delta x)(\eta)=x(\delta^{-1}\eta)$, let
+$\Lambda$ be the group of maps $x\mapsto\delta x+c$ with $\delta\in\Delta$
+and $c$ finitely supported, and put
+$R_\Delta=\LC(\Omega,\F_2)\rtimes\Lambda$ and $G_\Delta=\EL_3(R_\Delta)$.
+
+\begin{lemma}\label{lem:host}
+If $\Delta$ is infinite and LEF, then $G_\Delta$ is an infinite, finitely
+generated, simple Kazhdan group and the limit of finite simple groups
+$\SL_{3N}(\F_2)$ whose Cayley graphs form expanders, and
+$\xi\mapsto\operatorname{diag}(u_\xi,1,1)$ embeds $[\Lambda,\Lambda]$, which
+contains $[\Delta,\Delta]$, in $G_\Delta$.
+\end{lemma}
+
+\begin{proof}
+The group $\Lambda$ is generated by $\Delta$ and the map $z$ adding $1$ at $e$,
 and its orbits contain the dense sets $x+\bigoplus_\Delta\F_2$. If
@@ -366,3 +400,3 @@
 action is minimal and topologically free. The ring
-$R_\Delta=\LC(\Omega,\F_2)\rtimes\Lambda$ is generated by $1$, the units
+$R_\Delta$ is generated by $1$, the units
 $u_s^{\pm1}$ for the generators $s$ of $\Lambda$, and the indicator $e_U$
@@ -391,6 +425,4 @@
 and the conjugates of $P_z$ are the translations by the $1_q$, so the
-images of the generators generate $M_{2^{|Q'_n|}}(\F_2)$. By
-Theorem~\ref{thm:general}, $G_\Delta=\EL_3(R_\Delta)$ is an infinite
-finitely generated simple Kazhdan group, and the limit of finite simple
-groups $\SL_{3N}(\F_2)$ whose Cayley graphs form expanders.
+images of the generators generate $M_{2^{|Q'_n|}}(\F_2)$. So
+Theorem~\ref{thm:general} applies to $R_\Delta$.
 
@@ -401,13 +433,14 @@
 and the product of these matrices for $v=v_1$, $v_2$ and $(v_2v_1)^{-1}$ is
-$\operatorname{diag}(v_1v_2v_1^{-1}v_2^{-1},1,1)$. So $G_\Delta$ contains
-$\Gamma\le[\Delta,\Delta]\le[\Lambda,\Lambda]$.
-This proves the first statements of Corollaries~\ref{cor:lef}
-and~\ref{cor:host}. For the second statement of Corollary~\ref{cor:host},
-apply the first to the restricted direct sum of representatives of
-countably many isomorphism classes of finitely generated LEF groups, which
-is a countable LEF group, as finite subsets lie in finite products.
-Conversely, a countable group has countably many finitely generated
-subgroups, and there are countably many recursive presentations.
+$\operatorname{diag}(v_1v_2v_1^{-1}v_2^{-1},1,1)$, and $\Delta\le\Lambda$.
+\end{proof}
 
-Now let $\Gamma$ be generated by a finite set $E$, LEF and with solvable
+\begin{lemma}\label{lem:halfline}
+\textup{(a)} Every finitely generated LEF group with solvable word problem
+is a subgroup of $[\Delta,\Delta]$ for an infinite finitely generated LEF
+group $\Delta$ with solvable word problem.
+\textup{(b)} If $\Delta$ has solvable word problem, then so does $G_\Delta$.
+\end{lemma}
+
+\begin{proof}
+Let $\Gamma$ be generated by a finite set $E$, LEF and with solvable
 word problem. A half-line form of the Neumann--Neumann construction, used
@@ -427,3 +460,4 @@
 $\{v\in Q^{[-3r,3r]}:v_{3r-1}=v_{3r}\}$ onto
-$\{v\in Q^{[-3r,3r]}:v_{-3r}=1\}$. Transversals give a bijection $\kappa$ of
+$\{v\in Q^{[-3r,3r]}:v_{-3r}=1\}$. The domain and the image of $\theta$ have equally many right
+cosets, so matching them through transversals gives a bijection $\kappa$ of
 $Q^{[-3r,3r]}$ with $\kappa(vx)=\theta(v)\kappa(x)$ for $v$ in the domain of
@@ -437,3 +471,3 @@
 injective there, as $5r>2r$ and $f$ is determined by its values for
-$|m|\le3r$. So $\Delta$ is LEF, and the argument above applies to it.
+$|m|\le3r$. So $\Delta$ is LEF.
 
@@ -443,4 +477,6 @@
 of $f$ there as words in $E$. It is trivial if and only if $\ell=0$ and
-these values are trivial, so $\Delta$ has solvable word problem. An element
-$x\mapsto\delta x+c$ of $\Lambda$ is given by a word for $\delta$ and words
+these values are trivial, so $\Delta$ has solvable word problem. This
+proves~(a).
+
+For~(b), an element $x\mapsto\delta x+c$ of $\Lambda$ is given by a word for $\delta$ and words
 for the lamps of $c$, so the word problem of $\Delta$ decides equality in
@@ -454,9 +490,25 @@
 assignment of its finitely many variables, as $\Omega$ is the full shift.
-So $G_\Delta$ has solvable word problem. Conversely, finitely generated
-subgroups of groups with solvable word problem have solvable word problem,
-and LEF passes to subgroups. This proves the second statement of
-Corollary~\ref{cor:lef}.
+So $G_\Delta$ has solvable word problem.
 \end{proof}
 
+\begin{proof}[Proof of Corollaries~\ref{cor:lef} and~\ref{cor:host}]
+Limits of finite groups in the space of marked groups are LEF, and so are
+their subgroups. Conversely, by Lemmas~\ref{lem:nn} and~\ref{lem:host}
+every countable LEF group is a subgroup of a group $G_\Delta$ as in the
+corollaries. This proves their first statements. For the second statement
+of Corollary~\ref{cor:host}, apply the first to the restricted direct sum
+of representatives of countably many isomorphism classes of finitely
+generated LEF groups, which is a countable LEF group, as finite subsets
+lie in finite products. Conversely, a countable group has countably many
+finitely generated subgroups, and there are countably many recursive
+presentations.
+
+By Lemmas~\ref{lem:halfline} and~\ref{lem:host}, a finitely generated LEF
+group with solvable word problem is a subgroup of a group $G_\Delta$ as in
+Corollary~\ref{cor:lef} with solvable word problem. Conversely, finitely generated subgroups of groups
+with solvable word problem have solvable word problem, and LEF passes to
+subgroups. This proves the second statement of Corollary~\ref{cor:lef}.
+\end{proof}
 
+
 \section{Word problems}\label{sec:wp}
```
