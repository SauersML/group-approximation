# sk-verify-15 part 4: the every-finite-field port to rev4 (2026-09-13)

Lane sk-verify-15. This covers queue line 57: `research/artifacts/sk-fq-port-proposal-2026-09-13.md` (sk-fq-port, LaTeX §b plus derivation §f), against landed rev4 (c8b6021ca9, md5 9144d853…). Re-derived by hand.

**Verdict: PASS-WITH-FIXES.** The mathematics of all three changes is correct. The printed block needs two fixes (F1, F2) before it goes in; W1–W3 are optional.

## 1. Hypotheses (§f)
- **F_q-linearity of φ_k is necessary, and holds for both instances.**
  - It is necessary: a Galois-twisted φ_k(α) could send a central scalar word to a non-scalar matrix.
  - The periodic models D_ℓ(f) and the configuration models D_n(f) are F_q-linear in f. ✓
- **S ∋ α.** EL_n(F) over the free F_q-algebra is generated by e_ij(1), e_ij(α) and e_ij(τ_s):
  - [e_il(α^{t−1}), e_lj(α)] = e_ij(α^t) and sums give e_ij(F_q);
  - e_ij(c·m) = [e_il(c), e_lj(m)], with monomials m built by commutators (n ≥ 3).

  With φ_k(1) = I and φ_k(α) = αI, the Kazhdan set maps into the marking. ✓

## 2. Change 1: noncommuting root
- *Characteristic-free steps.* Commuting with every e_ij(e_V) forces g diagonal, the u_ξ-coefficients (ξ ≠ e) vanish, and the constant coefficients agree on small sets, hence everywhere. So g = cI_n with c ∈ LC(Z,F_q) nowhere zero.
- *Extra roots.* Commuting with e_ij(e_Vu_s) gives c e_V u_s = e_V(c∘s^{-1})u_s, so c = c∘s^{-1} on V. Small sets are dense and c is locally constant, so c is Λ-invariant, hence constant by minimality. So g ∈ F_q^×I_n is central. ✓
- *Model test.* c = 1 + (α−1)e_U with sU ≠ U commutes with every e_ij(e_V) but not with e_ij(u_s). So the extra roots are needed.
- *Centre.* Z(G) = G ∩ F_q^×I_n. ✓

## 3. Change 2: tower step
- *Margin w+1.* Smallness now includes f∘ξ constant on V for ξ ∈ B_{w+1}. Then:
  - f u_a e_V u_s f′ u_b = f e_{aV}(f′∘(as)^{-1})u_{asb};
  - f is constant on aV (a ∈ B_w), and f′∘(as)^{-1} is constant on aV because f′∘s^{-1} is constant on V (s^{-1} ∈ B_1 ⊆ B_{w+1});
  - so the entry lies in F_q·ε_{a,(sb)^{-1}}, with a ∈ B_w and (sb)^{-1} ∈ B_{w+1}.
- **W2 (derivation wording).** §f writes "∈ {0, ε_{a,(sb)^{-1}}}". Over F_q the entry is an F_q-multiple. M_n(A_V) membership is unaffected.
- *h^{-1} ≠ h.* k − I_n = (ghg^{-1} − h)h^{-1} and k^{-1} − I_n = h(gh^{-1}g^{-1} − h^{-1}), both in M_n(A_V), so k ∈ H_V ≅ GL_d(F_q) with d = n|B_{w+1}| ≥ 3. ✓
- *Not scalar.* Right multiplication by h^{-1} = I − ε_{e,s^{-1}}E_ij keeps first indices. So the rows of k at the levels aV, a ∈ B_{w+1}∖B_w, are identity rows. That set is nonempty because Λ is infinite. A scalar μ(e)I + (1−e)I with identity rows has μ = 1, while k ≠ 1. ✓
- *Normal subgroup.* S_V ≅ SL_d(F_q) is normal in H_V, so [k,x] ∈ K ∩ S_V for x ∈ S_V.
  - If K ∩ S_V were central in S_V, x ↦ [k,x] would be a homomorphism into an abelian group (by [k,xy] = [k,x]·x[k,y]x^{-1}), trivial by perfectness.
  - Then k would centralize S_V, and C_{GL_d}(SL_d) is the scalars. Contradiction.
  - So K ∩ S_V is noncentral normal in the quasisimple SL_d(F_q), d ≥ 3, and S_V ⊆ K. ✓
- *Level ideal.* Characteristic-free, unchanged. G/Z(G) is simple, and infinite because Z(G) is finite. ✓

## 4. Change 3: marked limit in the S1 architecture
- *Kernel.* The kernel of G → ∏_ω SL_{nN_k}(F_q) is normal and misses e_12(1), so it lies in Z(G) by simplicity of G/Z(G). ✓
- *Words in Z(G).* x = μI_n: φ_k∘π = ρ_k on the finitely many entries of x − μI_n for large k, and φ_k(μ) = μI. So the image is scalar, trivial in PSL. ✓
- *Words not in Z(G).* If infinitely many approximants give scalar images, choose ω on a set with constant μ_k (F_q^× is finite). Then x is central in the ultraproduct, [x,G] ⊆ kernel ⊆ Z(G), and xZ(G) is central in the simple infinite G/Z(G), so x ∈ Z(G). Contradiction. ✓
- *Conclusions.* Marked convergence of PSL_{nN_k}(F_q) to G/Z(G), with N_k → ∞. Expanders for SL on the images of the Kazhdan set (inside the marking), hence for the PSL quotient graphs. PSL_{nN_k}(F_q) is simple. ✓

## 5. The printed block (§b): fixes
- **F1 (must).** "Only the copy $S_V$ of $\SL_d(\F_q)$ lies in $G$" asserts H_V ∩ G = S_V, which is neither proved nor used. Verify-13 part4's G1 said only that the transvections place the copy of SL_d(F_q) in G. Replace with:

  `The transvections place the copy $S_V\cong\SL_d(\F_q)$ in $G$; it is perfect and its proper normal subgroups are central, …`
- **F2 (must).** Notation clash. In rev4, Theorem 2's Cantor set is $Z$ (l.110–111, "$\LC(Z,\F_2)$"), and the block uses $Z$ for the centre while also writing "$\LC(Z,\F_q)$". Write $Z(G)$ for the centre throughout the block ("its centre $Z(G)$", "$G/Z(G)$", "$g\in K\setminus Z(G)$", "it lies in $Z(G)$", "$xZ(G)$ is central in $G/Z(G)$ and $x\in Z(G)$").
- **W1 (optional, 0–1 line).** After "so $k$ is not scalar in $H_V\cong\GL_d(\F_q)$", add "as $\Lambda$ is infinite, so $B_{w+1}\ne B_w$".
- **W3 (optional, 1 clause).** Before "If a word $x$ has scalar image…", add "a word $\mu I_n$ in $Z(G)$ maps to $\mu I$ for large $k$, and".

## 6. Scope
- Length: +14 net source lines; the lane's compile runs to 9 pp against rev4's 8. That is main's call.
- Reviewed dependencies are as the proposal lists. "SL_d(F_q) perfect and quasisimple for d ≥ 3" is classical, uncited, like rev4's GL_d(F_2) = PSL_d(F_2).
- No Cairn node is touched: the proposal has no claim node.
