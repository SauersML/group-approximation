# Multiplicity-two refutation of five global A4 compilers

Date: 2026-08-30

## Outcome

The exact binary frame

```text
F=401004080301c030
```

already used for the multiplicity-two packet/collision escape refutes five
additional global A4 statements, not merely five proposed proof methods:

- thirty-one-moment rounding to the classical alignment simplex;
- collision-conditioned shifted-centralizer classicalization;
- packet-commutator collapse;
- the exact A8 normal-generator holonomy compiler; and
- the approximate A8 multiplication-table compiler.

After regularizing in the finite group generated by the first chart and `F`,
the witness is a constant exact-regular thirty-context network with identity
stable letters and `q_19243=1`.  Its two raw S3 opcodes are nonidentity, so its
raw energy is exactly four.

## Exact binary replay

The dependency-free JavaScript verifier uses row-byte arithmetic over `F_2`.
It checks

```text
F^-1=8280414010200c04,
q_19243(F)=8040201008040201=I,
```

every distinct directed `H6`/`H18` mixed `C2`--`C3` context, and

```text
t21 opcode=8040281409070201,   order 4, rank(opcode-I)=4,
t12 opcode=8040201028143e29,   order 4, rank(opcode-I)=4.
```

There are twenty-four distinct directed context pairs; the thirty packet
occurrences repeat some of those pairs.  The established packet route checks
the occurrence list.  Positive binary rank makes both opcodes nonidentity.
In a finite regular representation each then has trace zero and squared
Hilbert--Schmidt distance two.

## Five direct contradictions

| proposed statement | forced consequence on the zero-residual frame | exact contradiction |
|---|---|---|
| moment-simplex rounding | classical mixture functional tends to `36` | every classical mixture is at most `30` |
| collision-conditioned classicalization | the frame lies in the closed classical branch union modulo commutant | collision has defect `sqrt(2)` on that union but defect `0` on the frame |
| packet-commutator collapse | `E_packet=0` | `E_packet>=5040/28363` from raw energy `4` |
| exact holonomy compiler | `pi(b)=1`, hence no nontrivial A8 mass and the frame is chart-central | raw energy is `4` |
| multiplication-table compiler | the exact table has `u(b)=1`, hence is trivial and the frame is chart-central | raw energy is `4` |

The fourth and fifth rows use the stated output inequalities of the compiler
claims themselves.  Thus they are counterexamples to those existential
compiler interfaces, not objections to one implementation.

## Established-dependency audit

Every noncomputational implication is already proved in Cairn:

- `atlas-multiplicity-two-packet-collision-hub-escape-proof` establishes the
  exact network and collision identity;
- `atlas-multiplicity-two-holonomy-refutation-proof` establishes raw energy
  four;
- `atlas-a4-packet-centralizer-product-compiler-proof` turns exact packet
  words into exact shifted-product membership;
- `atlas-a4-classical-mixture-trace-separator-proof` gives the bound `30`;
- `atlas-a4-19243-classical-collapse-proof` excludes collision on every
  classical branch;
- `atlas-packet-energy-controls-s3-covariance-proof` gives the packet-energy
  lower bound;
- `atlas-a8-19243-normal-generator-hs-stability-proof` kills nontrivial exact
  A8 representation mass when `b` is trivial;
- `finite-normal-generator-approximate-table-trivialization-proof` trivializes
  a zero-defect table from one normal generator; and
- `atlas-a4-19243-parabolic-normal-closure-proof` proves
  `<<b>>_(A8)=A8`.

No new compactness, stability, or classification assumption is used.

## Scope

The same exact audit gives q14 nonidentity.  The result therefore refutes the
five packet-plus-collision claims exactly as stated, but does not decide an
augmented q14 packet or any controller which excludes this frame.