# EX review, backlog, part 7: refill lanes, third batch (2026-09-13)

Lane `ex-verify-backlog`. Index: `ex-review-backlog-2026-09-13.md`. Read at tips `4a2087802` to `a5d2106cb`.

## 23. ex-hyperbolic-triangle-theory (61eee1d71c): `linear-quotient-exclusion-lifts-from-char-zero`: PASS

- **The systems `S_i`.** A `k`-point of `S_i` is a projective representation with `ρ(x) != 1`, and conversely:
  - `u_i det X_i = 1` makes each `X_i` invertible;
  - `r_j(X) = λ_j I` with `λ_j μ_j = 1` makes the relators scalar;
  - `t c_i(W) = 1` makes `W` non-scalar.
- **(b).** The weak Nullstellensatz over `Q` gives `1 = Σ g_j h_j` with rational `g_j`. Clearing denominators
  gives `N`, and the certificate survives over every `k` with `char k ∤ N`.
- **(a).** A `Q-bar`-point in `O_K[1/M]` reduces modulo `𝔭 | p` for every `p ∤ M`. The equation `t c_i(W) = 1`
  keeps the reduction non-scalar.

## 24. ex-nonore-relative-gate (ec7a6cd2db, 46537ddc7d, 4bed93fb62, 69385e4269)

### 24.1 `opposite-slice-contains-function-field-hecke-pair`: PASS at the checked steps

- **(HP1).** `k[x_1^(±1)] ∩ k[x_1..x_d] = k[x_1]`, and `SL_r(k[x_1]) = EL_r(k[x_1])` because the ring is
  Euclidean.
- **(HP2), building picture.**
  - `Λ_1 = SL_r(k[t, 1/t])` is dense in `SL_r(k((t)))`, and `K = SL_r(k[[t]])` is open. So `Λ_1 K = G_t` and
    `Λ_1/Γ_1 ≅ G_t/K`.
  - Incompressibility holds because `lKl^(-1) <= K` forces equality between compact open subgroups of equal
    Haar measure.
- **(HP3).** A reduced alternating word with letters in `Λ_1 \ Γ_1` has letters in `P \ Γ` by (HP1), so it is
  nontrivial in `D`.
- **Not re-derived.** The co-density (HP4), which rests on the congruence structure of finite quotients, and
  the units dichotomy of item 6.

### 24.2 `kt-double-hyperlinearity-requires-function-field-hecke-re`: PASS

- **(KH1).** The normal-form fact `A' *_(A' ∩ C) A' -> A *_C A`. For wreaths, lamps extend by zero on an
  invariant orbit.
- **Item 2.** Hyperlinearity passes to subgroups, and the double/`RE/C` equivalence
  (`binary-free-and-ordinary-coset-wreaths-share-hyperlinearity`, items 4 and 6) converts it.
- **Item 4.** This is the contrapositive. Its route `non-hyperlinear-from-function-field-hecke-non-re` requires
  the open `function-field-hecke-pair-is-not-relatively-embeddable`, so it does not fire.

## 25. ex-weak-sofic-deep-wreath (fa0a372c1f, dd113c6e6a, f414770b59)

### 25.1 `simple-nonsofic-width-over-bounded-base-symmetric-wreath`: PASS

- **Small moved set.** `<a>` lies in the variety `V_N` generated by finitely many finite groups. So it is a
  quotient of the finite relatively free group `Φ`, and (E1) and (E2) bound the width by `|Φ|`.
- **Block B.** A conjugacy class `C` of `Sym(Ω_0)` is closed under inverses, and `C^k` is one coset of
  `Alt(Ω_0)`, so `C^k C^(-k) = Alt(Ω_0)`.
- **Dipole lemma.**
  - `(h;1)(g;σ)(h^(-1);1)(g;σ)^(-1) = (h g σ(h^(-1)) g^(-1); 1)`.
  - Since `σ(S) ∩ S = ∅`, for `j in S` the point `σ^(-1)(j)` is not in `S`.
- **Block C.** The Hamiltonian path of dipoles solves every coordinate but one. Each dipole has
  `Ψ_0 = 0`, so the defect lies in `[H,H]`.
- **Block D.** `[[(k at i;1), L], (l at i;1)] = ([k,l] at i; 1)`, four letters.
- **Block E.**
  - `[Φ, Φ]` is generated by the balanced pairs `[u, v r^(±1) v^(-1)]`, since the relators normally generate
    `Φ`.
  - Lifting the conjugators to `Ω_1` makes each pair trivial on `Q_0`.

### 25.2 `iterated-simple-wreath-bounded-commutator-width`: PASS

- **Item 2.** If `g` moves a bottom vertex `v` and `u` has label `t != 1` at `v`, then `[g,u]` has labels
  `t` at `g(v)` and `t^(-1)` at `v`.
  - `N ∩ B` is a partial product of the simple factors.
  - The tower permutes those factors transitively, so `N ∩ B = B`.
- **Imports.** Lucchini--Menegazzo Theorem 1.1 and Nikolov--Segal Theorem 1.2, quoted verbatim in their
  citation routes. Not re-read.

### 25.3 `relator-width-bounded-over-simple-iterated-wreath-products`: PASS at the checked steps

- **Reduction to root-acting tuples.** The least acting level is also a level where some relator value acts,
  by (E1). Balanced templates evaluate coordinatewise.
- **Top block.** `(C C^(-1))^(|T|) = T`, because the increasing chain of normal subsets reaches the normal
  subgroup generated by `C C^(-1)`, which is `T`.
- **Base.**
  - Dipoles along a spanning tree solve every coordinate but one.
  - The remaining `z in H = [H,H]` is a product of `c(m)` commutators, each four letters.

## 26. ex-hs-instability-extension (86f98fb5bc, 22489455ed)

### 26.1 `one-compressor-wreath-has-models-with-exact-origin-vertex`: PASS at the checked steps

- **Permutation matrices.** `σ_N(v)` is a permutation matrix, and `P M_F P^* = M_(F∘v̂^(-1))`. So lamp
  translates are diagonal and commute exactly, and a sign constant on `Γ`-orbits commutes with `π_N(Γ)`.
- **Separability.** Truncation below degree `k` together with Suslin stability, as in §5.4 of part 3.
- **Not re-derived.** The Chebyshev and union-bound estimates for the random lamp (artifact §§3--4).

### 26.2 `unit-type-vertex-rounding-agrees-with-sigma-on-laurent-steinberg`: PASS at the checked steps

- **Row 3.**
  - `[x_23(x_i^(-1)), x_3k(x_i a)] = x_2k(a)`.
  - For `U` and `V` we get `UbU^*b^* = σ(e_2k(a)) = ρ'(x_2k(a)) = VbV^*b^*`.
  - Hence `W_i = V^*U` commutes with `b`.
- **Column 2** is the same argument. **The corner** `e_32(x_i² a)` is a commutator through a third index, since
  `r >= 5`. **Full roots** commute with `e_23` under both representations.
- **Same image.** `π_n(K_i) = π_n(Γ)`, because `x_i` is a unit in `A_n`.
- **Kazhdan projection** onto `π_n(Γ)'`.
- **Premises.** `laurent-extension-agrees-iff-inverse-defects-commute` and
  `mixed-level-root-group-is-kazhdan-in-rank-at-least-five` have routes; they were not re-derived here.

## 27. ex-weak-sofic-classical-covering (9d536319af, 5782bf1bb2, f157654193, 513d2f1740)

### 27.1 `simple-group-classical-width-finite-field-linear-soficity`: PASS at the checked steps

Checked from the two artifacts and the route.
- **Lemma 1.1.**
  - In case (O) with `q` odd, `{a x²}` and `{-c - b y²}` each have `(q+1)/2` elements, so they meet.
  - In case (U), the norm `F^× -> F_q^×` is onto.
- **Lemma 3.1.** The exponent is `Σ_(i<=s)(s - i + 1 + r) + Σ_(j<=n-s) j = s(s+1)/2 + rs + (n-s)(n-s+1)/2`.
  With `s(s+1)/2 + (n-s)(n-s+1)/2 = n(n+1)/2 - s(n-s)`, Lemma 3.2 follows from the big-cell bound of Lemma 2.1
  and `r <= s <= n/4`.
- **Lemma 4.1.** On `hK` the operator `hNh^(-1) - N` equals `-N`, which gives the lower bound
  `dim K - dim(K ∩ hK)`.
- **Lemma 4.2.**
  - The failure count gives exponent gap `ρ(h) - 6j + 4 >= 4`.
  - The companion polynomial `t^(r') + a t + (-1)^(r')` has determinant `1` and no root `1`.
  - `SL_(r')(q)` is perfect for `r' >= 3`.
  - `dim(Y + hY) >= 3r'`, so `rk([h,y] - 1) >= r'`.
- **Lemma 5.3.**
  - `s` and `s'` are conjugate involutions, so `y = s s'` lies in the derived subgroup.
  - `s'` commutes with `h_0` and with `s`, so `[h_0, y] = h_0 · s h_0^(-1) s`.
  - The dimensions match: `W^perp` has Witt index at least `3d_0 + d'` when `n >= 7d_0 + 2d' + 2`.
- **Route §4.**
  - `[c(a), c'(a)] = [c(b), c'(b)]`, since scalars are central.
  - `c(b)` lies in `I(W)' x 1 <= H = I(W''')'`, and `s = 2t <= 2d_0 <= d/4` with `d = 9d_0`.
  - The centre trick in `H`, with conjugators in `H x 1 <= Q_0`.
- **Import.** For `d >= 9`, `I(W)'` modulo its centre is simple (Kleidman--Liebeck), not re-read.

### 27.2 `simple-group-width-over-all-finite-simple-groups`: PASS

- **The case split.**
  - Sporadic groups and Lie type of rank `<= 8`, which includes every exceptional type: item (A).
  - Alternating groups: item (B), since `G` is nonsofic under (i).
  - `PSL_n(q)`: part 5 §14.
  - `PSp`, `PSU`, `PΩ`: §27.1 above.
- **Abelian targets.** By perfection, `a_x = Π_r r(a)^(c_(x,r))`.
- **(iii) => (i).** The `PSL_(3n_i)(q_i)` construction of part 5 §14.

## 28. ex-nh-dyadic-pair and ex-nh-deligne-hs (landings to 02:12)

### 28.1 `deligne-cover-hyperlinearity-is-monotone-in-genus`: PASS

- **The embedding.** `ι(A)` preserves `J_(g+1)`. On maximal compacts it is `u -> diag(u,1)`, and
  `det diag(u,1) = det u`. So `ι_*` is an isomorphism on `π_1 = Z`.
- **The lift.** `ι~` is injective: on `ker p_g` it acts as `ι_*`, and off the kernel `p_g` detects it. It
  carries `Γ~_g` onto the full preimage of `ι(Sp_(2g)(Z))`, and it induces injections of the finite covers.

### 28.2 `dyadic-hnn-family-is-monotone-in-rank-and-primes`: PASS

- **Britton's lemma.** A reduced word with no `a_i in C` between stable letters stays reduced in `H'`,
  because `A ∩ C' = C`.
- **Block embeddings.** `diag(x, I)` is integral iff `x` is, so `A ∩ C' = C_n`.

## 29. ex-hyperbolic-triangle-theory (e9403a8d61): `edge-twist-extensions-of-triangle-colimits-moment-test`: PASS

- **(M1).** `T_c` is a simple nonpositively curved complex of groups, so it is developable (Bridson--Haefliger
  III.C.4.17) and `C` injects. A finite central extension of a hyperbolic group is hyperbolic.
- **(M2), cocycles.**
  - `φ_v(a,z) = (f_v(a) + z·1, π(a))` is multiplicative iff `f_v` is a 1-cocycle, since `π(a)·1 = 1`.
  - The edge identification reads `f_i(x) = f_j(x) + c_e(x)·1`.
  - `M` is a free `F_p[A_v]`-module, so `H^1(A_v; M) = 0` and `f_v = (a-1)u_v`.
- **(M2), the affine intersection.**
  - `w_e^0(x_e^k q_0) = -k c_e` satisfies `(x_e - 1)w_e^0 = c_e·1`, because
    `w(x^(-1) x^k q_0) - w(x^k q_0) = c_e`.
  - A lift exists iff `w_13^0 - w_12^0 - w_23^0` lies in `Σ_e M^(E_e)`.
  - In finite dimension that sum is the annihilator of `Z_N`, and `<λ, w_e^0> = -c_e μ_e(λ)`.
- **(M3).**
  - `ker ρ` meets `C` trivially, so `s(N) = (preimage of N) ∩ ker ρ` is a normal complement of `C` over `N`.
  - Left multiplication on `Λ_c/s(N)` commutes with the free `C`-translation, so the image lies in
    `C^Q ⋊ Q = M ⋊ Q` over `π`.
- **(M4)--(M5).**
  - Pushforward preserves the moments.
  - For index prime to `p`, the transfer `tr` satisfies `push∘tr = [N:N']·id`, which is invertible mod `p`.
- **(M6).** Uses `finite-central-extension-rf-iff-virtually-splits`, not re-derived here.

## 30. ex-hs-instability-kazhdan (781c47abf0): `flexible-hs-stability-descends-to-retracts-finite-index`: PASS at the checked steps

- **The hypothesis is stated.** Item 2 assumes `Γ` Kazhdan, and the proof uses (T) only for the Kazhdan
  projection.
- **Retracts.** Rounding `α_n∘r` and restricting along `i` rounds `α_n`, since `r∘i = id`.
- **Finite index.**
  - The induced model preserves block 1 exactly on `H`.
  - The rounding gives `||[π_n(h), Q]||_(2,D_n) -> 0`.
  - The projection of `Q` onto `π_n(H)'` is within `δ_n/κ`, and its spectral projection cuts out the first
    block.
  - The final comparison (§4, last step) was not read line by line.

## 31. ex-weak-sofic-deep-wreath (78041189ef): `transitive-wreath-towers-bounded-commutator-width`: PASS

- **Base lemma.**
  - For `x = (u;1)` and `y = (q;β)`, the commutator is `(f;1)` with
    `f_i = u_i q_i u_(β^(-1) i)^(-1) q_i^(-1)`.
  - Going once around a cycle gives `u_(i_1) = G u_(i_1) V^(-1)`, with `G = g_(i_1) g_(i_L) ... g_(i_2)` and
    `V = q_(i_1) q_(i_L) ... q_(i_2)`.
  - Taking `q` supported at `i_1` makes `V = s`, and `G` is conjugate to `F s`. So `F = [t,s]` suffices.
- **Three-for-two absorption, re-computed.** With `A_k = 1` for `k < N`, `B_k = λ_k A_k^(-1)` and
  `A_N = ν_N Π λ_N`:
  - the product at `i` is `Π · λ_N A_N^(-1) ν_N = Π · Π^(-1) = 1`;
  - the product at `j_0` is the word `μ_1...μ_(N-1) ν_N λ_1 ν_1 ... λ_(N-1) ν_(N-1) λ_N μ_N`, in `3N` free
    commutators.
- **Recursion.** `c_W <= m! + (m-1) + 2⌈c_H/3⌉`. With `c_H <= C = 3(m!+m+2)`, this gives
  `c_W <= 3m! + 3m + 3 <= C`.

## 32. ex-nh-dyadic-pair (2066ea84bd): `nonamenable-relatively-kazhdan-infranormal-sofic-wreath`: PASS

- **Infranormality.** `t = 2I` is central in `Λ`, so `tΓt^(-1) = (2Z)² ⋊ SL_2(Z) <= Γ`. `Γ` and `t` generate
  all of `G`, and `(e_1/2, I)` is not in `Γ`.
- **Soficity.**
  - `N = (⊕_X Z/2) ⋊ B`, with `B = D² ⋊ <2I>`, is amenable and normal, and `H = N ⋊ SL_2(Z)`.
  - `SL_2(Z) = Z/4 *_(Z/2) Z/6` makes `H` an amalgam of amenable groups over an amenable group, which is
    sofic (Păunescu 3.4 and 3.9, as quoted).
- **Defect.** `(e_1, I) t o = t o` iff `(e_1/2, I) in Γ`, which fails.
- **Hyperlinear HNN.** Follows from items 1 and 5 of the coset-wreath equivalence.

## 33. Other refills read at this tip

- **`simple-group-relator-templates-over-finite-simple-groups`: PASS at the checked steps (§§1--2 of the route).**
  - The evaluations of a pair include `1`, every conjugate of a seed `[r(a), y]`, and `C C^(-1)`.
  - Liebeck--Shalev covers `H/Z(H)`, and the centre trick handles commutators.
  - Templates are valid coordinatewise over direct products.
- **`sl3-polynomial-congruence-subgroups-have-large-b2`: PASS as a conditional statement; computation not
  re-run.**
  - The claim names hypothesis (S), that the sector is a strict fundamental domain for `SL_3(F_q[t])`. This is
    Soulé's theorem, not imported as a node.
  - Stabilizers are block upper triangular with degree bounds, and cells over `σ` number
    `|SL_3(A)|/|π(Γ_σ)|`.
  - `H^*(Γ(f); R) = H^*(Y; R)`, since stabilizers are finite, and `b_2 = χ - 1 + b_1` with `b_1 = 0` by (T).
  - The table of values is an MSI computation, which I did not reproduce.

## 34. ex-weak-sofic-deep-wreath (78041189ef): `relator-width-over-towers-of-bounded-commutator-width`: PASS

This makes `simple-nonsofic-relator-width-over-iterated-wreath-products` correctly established. Its route
`deep-wreath-relator-width-from-commutator-width` requires this claim and §31.

- **Fixed coordinates.**
  - The section at a `<p>`-fixed coordinate `i` is a homomorphism on the stabilizer of `i`.
  - Left-multiplying a conjugator by `(h at i; 1)` changes a letter only at `i`.
  - The pairs of Sections 2--4 are commutators `[u, L]` in which `u` has trivial section at `i`, so they are
    trivial at `i`.
  - Induction on depth then realizes `a^(i)_x` inside the same pairs.
- **Top block.** `A^(-1) x` lies in `[F, F]`, and `[Φ_m, Φ_m]` is generated by the pairs
  `[φ, ψ r^(±1) ψ^(-1)]`. These specialize to `<p> <= Sym(m)`, which lies in the variety.
- **Dipole block.**
  - `Ψ_O` is a homomorphism on the stabilizer of the orbit `O`, and `Ψ_O(e) = 0`.
  - The tops `π r(p) π^(-1)` generate `<p>` by (E1), so spanning trees exist.
  - The defect lies in `[H,H]`.
- **Counts.** `|Φ_m| + Σ_O (|O| - 1) + 2c · #O <= |Φ_m| + (m-1) + 2cm = M` pairs per relator.
- **`full-cycle-wreath-towers-bounded-commutator-width`: PASS at the checked steps.**
  - The commutator formula `(u · α(q) · γ(u^(-1)) · τ(q^(-1)); τ)` holds.
  - The single-cycle solvability criterion holds.
  - `c_T <= |T|` holds, since the commutator set with `β ~ ζ` contains `1` and generates `[T,T]`.
  - The claim is superseded for bounded degree by §31.
