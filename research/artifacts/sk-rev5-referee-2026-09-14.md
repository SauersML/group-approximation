# sk-rev5-referee: hostile referee report on rev5 variant B (2026-09-14)

**Subject:** `$SK2/drafts/rev5b.tex`, md5 32207a761a1943b6857b5e9c0666bf46, 1002 lines, 11 pp (sk-editor-7b, merge log de2f82c543). Compared against main's note (md5 d3a3fd6e…, rev4.5 minus the Origin sentence). One-pass comparison with `$SK2/drafts/rev5a.tex` (md5 fded1743…, 950 lines) in §4.

## Verdict: ACCEPT WITH MINOR REVISION. No mathematical error, no gap.

Every new or moved sentence was re-derived by hand. Nothing forbidden remains ("responsible", OpenAI, Astra: none).

**Checked and correct:**
- **Expander-limit definition (l.88–102).**
  - The quantifiers match §1 "Finite simple models" (l.341–360): same-length tuples, "for each word w and all large k depending on w", Q_k finite, |Q_k|→∞ (from N_k→∞, l.356), and one ε for all k (Margulis/Kassabov with the fixed Kazhdan constant of EL_3(𝒜)).
  - The generator images {e_ij(1), e_ij(φ_k(τ_s))} equal the tuple entries e_ij(φ_k(s)) as a set, since 1∈S and φ_k(1)=I. Repeated entries do not affect expansion. Left/right neighbours are equivalent via inversion.
- **Theorem 2(a)/(b) (l.176–193).**
  - V1: n is bound in (b). V2: l.197 says "Theorem 2(b)". V3: l.363 says "Theorem 2(b)".
  - The φ_k quantifier ("all large k, depending on r and r′") is clear.
  - SL_{nN_k}(F_2) is simple for nN_k≥3.
- **P2 (l.200–211).** All three steps hold:
  - Kazhdan ⇒ f.g.; the ring is countable, so BHV applies to the discrete group.
  - Entries of generators and inverses generate the ring, since e_12(r) is a product of generators with entry r.
  - Every coefficient of every ring element lies in the Boolean algebra generated by the translates of the f_j. So the itinerary to the atoms of the f_j is a continuous injective equivariant map, hence a conjugacy onto a subshift.
  - Minimality: restriction to a proper closed invariant Y is onto (clopen subsets of Y extend), the kernel contains e_12(e_U) for a nonempty clopen U⊆X∖Y, and it misses e_12(1).
- **Table 1 (l.213–232):** every row is true with a correct pointer. "Not residually finite" now lives in Questions item 1, and l.226 points there.
- **Simplicity Steps 1–3 (l.256–321).** No step changed. The (i)/(ii) split matches rev4.5. The density sentence is correct: finitely many dense open sets, and small clopen neighbourhoods of a point moved by every ξ∈B_{2w}∖{e}. Moving "x=[g,h]∈K∖{1}" to the end of Step 1 is sound.
- **Example (l.417–449).**
  - L_3(Fibonacci) = {001,010,100,101}: no 11, no 000 since the zero runs have length 1 or 2. The cyclic windows of 01001 give the same set, and period 4 is impossible (checked by hand).
  - The displayed P satisfies Pδ_t=δ_{t+1}, and D_1(e_1)=diag(0,1,0,0,1).
  - The windows at 1 and 4 are both 010. P³D(e_1)P⁻³ has entry y_{t−3}. The product has entry y_t·y_{t−3}, which is 1 only at t=4, giving E_44. Then P^{t−4}E_44P^{4−t}=E_tt and P^{t′−t}E_tt=E_{t′t}.
  - Figure 1(a): the arcs run clockwise t→t+1 and positions 1 and 4 are shaded, matching the text.
- **Lemmas A/B/C (l.496–651).** Each statement is exactly what its proof proves:
  - Lemma A: the level isolation. Differences of distinct pairs of powers of 2 are distinct and below 4^k, so both conjugates act nontrivially only at level 0. Also the ultraproduct LEF step, and "Δ infinite because it contains Γ".
  - Lemma B: minimal and topologically free, uses infinite Δ, generation of M_{2^{|Q′_n|}}; the product identity diag(v,v⁻¹,1)=w(v)w(1) in characteristic 2 is checked.
  - Lemma C(b) holds for every f.g. Δ; its proof uses no LEF or infiniteness.
  - Lemma C(a): θ is an isomorphism because v_{3r−1}=v_{3r}; κ comes from equal coset counts; ζvζ⁻¹=θ(v); the shifts hold for |ℓ|≤r; injectivity from 5r>2r.
  - The corollary proof (l.653–672) assembles A+B and C+B correctly.
- **Brown's formulation (l.362–373) and the page-1 sentence (l.83–86):** correct, including R^ω⊗̄R ⊆ (R⊗̄R)^ω ≅ R^ω. Infinite simple ⇒ ICC.

## 1. REQUIRED

**R1. P3 remark (l.411–415): cut it, or reword it. Recommend CUT to Cairn.**
- **Collision:** "finite models" there is a THIRD meaning, besides the introduction's matrix models (l.127) and Lemma B's LEF approximations σ_n (l.560).
- **Omitted step:** the "same argument" needs ONE finite orbit for M_N-generation; permutation matrices of a non-transitive set generate only a block algebra. A finite invariant set contains an orbit, so the claim is true, but a reader cannot reconstruct this.
- **Unstated consequence:** the remark never says what it buys.
- CUT: delete l.411–415 (−5 lines).
- If kept, NEW text: `The same argument gives maps $\varphi_k$, so Theorem~\ref{thm:general}\textup{(b)} applies, for every infinite minimal topologically free subshift $X\subseteq A^\Lambda$ over a finitely generated group $\Lambda$ that is approximated by finite orbits: for every finite $F\subseteq\Lambda$ some finite $\Lambda$-orbit of configurations has all its patterns on $F$ in $X$.`

**R2. Theorem 1 (l.78) uses "expander limit" before its definition (l.94).** A theorem statement needs a pointer.
- OLD (l.77–79): `property~\textup{(T)}, and an expander limit of finite simple groups` / `$\SL_{3N}(\F_2)$.`
- NEW: `property~\textup{(T)}, and an expander limit, in the sense defined below, of finite simple groups $\SL_{3N}(\F_2)$.`

## 2. SUGGESTED

**S-A. Move P2 out of the introduction into §2, in "if and only if" form.** It reads better even at the same length.
- **Why move it:** it is a proof paragraph sitting between Theorem 2 and Table 1, before §1 starts. It interrupts the introduction. It also answers "why subshifts?" best after the reader has seen the ring.
- **Why the iff is fair:** both directions are proved (Theorem 1; and the (T) and minimality steps), so it is not the overclaim W1 warned against.
- OLD: delete l.200–211.
- NEW, inserted after l.409 (before `\subsection*{An example}`):
```
\subsection*{Sharpness over $\Z$}
For a homeomorphism $T$ of a Cantor set $X$, the group
$\EL_3(\LC(X,\F_2)\rtimes_T\Z)$ is simple and Kazhdan if and only if
$(X,T)$ is conjugate to a minimal subshift. One direction is
Theorem~\ref{thm:main}. Conversely, a Kazhdan group is finitely
generated~\cite[Theorem~1.3.1]{BHV}, and the entries of finitely many
generators and their inverses generate the ring, as $e_{12}(r)$ has the
entry $r$. So the ring is generated by finitely many $\sum_jf_ju^j$, the
translates of these $f_j$ generate the clopen sets, and the itinerary map
to the atoms they generate is a conjugacy onto a subshift. If a nonempty
proper closed set $Y$ is invariant, restriction maps the group onto
$\EL_3(\LC(Y,\F_2)\rtimes_T\Z)$ with a kernel that contains $e_{12}(e_U)$
for a nonempty clopen set $U\subseteq X\setminus Y$ but not $e_{12}(1)$,
so the group is not simple.
```
- Optional intro pointer, appended to l.195: `Over $\Z$ the subshift hypothesis is exactly right (Section~\ref{sec:main}).`
- The BHV locator is still with sk-rev5-locators.

**S-B. S2 sofic/hyperlinear sentence (l.107–111): cut it (−4).** Pestov's guide is cited where it is used.
- OLD: `Limits of finite groups in the space of marked groups are LEF. Sofic and hyperlinear groups are defined by approximately multiplicative maps into finite symmetric groups with the normalized Hamming distance, and into unitary groups with the normalized Hilbert--Schmidt distance, that keep nontrivial elements uniformly far from~$1$~\cite{Pestov}. Hyperlinear groups were named by`
- NEW: `Limits of finite groups in the space of marked groups are LEF, and LEF groups are sofic and hyperlinear (Section~\ref{sec:proof}). Hyperlinear groups were named by`

**S-C. Compress Brown's formulation (l.362–373, −3 lines).**
- NEW (replaces l.363–373): `Let $G$ be a group as in Theorem~\ref{thm:general}\textup{(b)}. It is LEF, so it is sofic~\cite[Example~4.5]{Pestov} and hyperlinear~\cite[Theorem~2]{ElekSzabo}, and $L(G)$ embeds in $\mathcal R^\omega$~\cite{Kirchberg,Radulescu},~\cite[Proposition~7.1]{Ozawa}. As $G$ is infinite and simple, its nontrivial conjugacy classes are infinite, so $L(G)$ is a $\mathrm{II}_1$ factor. Then $G$ lies in the unitary group of the McDuff factor $L(G)\mathbin{\bar\otimes}\mathcal R$~\cite{McDuff}, which embeds in $\mathcal R^\omega\mathbin{\bar\otimes}\mathcal R\subseteq(\mathcal R\mathbin{\bar\otimes}\mathcal R)^\omega\cong\mathcal R^\omega$, as in Brown's question.`

**S-D. Table 1: drop the rows "finitely generated" and "infinite" (l.220–221).** Both are in Theorem 1's statement (−2).

## 3. PORTS FROM rev5a WORTH TAKING INTO rev5b

- **P-1. Lemma C radius r → λ (rev5b l.610–630).** The NEW block is rev5a l.576–597 verbatim (identical apart from the letter). r and r′ are ring elements in Theorem 2, §1 and Lemma B. λ in §1 (l.344) also denotes a word length, the same meaning, so it is not a clash; no free Greek letter reads better (ν collides visually with v).
- **P-2. Darbinyan sentence (rev5b l.603–605).** NEW = rev5a l.569–570: `We use a half-line form of the Neumann--Neumann construction, which Darbinyan used to preserve solvable word problems~\cite{Darbinyan15}.` It removes the redundancy and keeps U2's meaning (−1).
- **P-3. Lemma B generators (rev5b l.556–558, l.579).** Use rev5a l.510–511 and l.532: `the units $u_\xi^{\pm1}$ for $\xi$ in a finite generating set of $\Lambda$` and `by products of the $P_\xi$ with $\xi$ in the generating set`. This removes the s∈S clash.
- **P-4. `\subsection*{Solvable word problem}` split (rev5a l.545–558).**
  - Order: Lemma A, Lemma B, then "Proof of Corollary 4 and of the first statement of Corollary 3", then the subsection with Lemma C and the second statement.
  - It realizes S4, so readers can stop before the WP apparatus (+2).
- **P-5. Arzhantseva sentence (rev5b l.63–67).** NEW = rev5a l.57–60 (`Arzhantseva listed the sofic form among open questions, and gave …`). It restores the count for "all three forms" at l.68, since four authors precede it now (−1).
- **P-6. Page-1 Brown sentence (rev5b l.83–86).** NEW = rev5a l.96–99 (`In Brown's form, $G_X$ lies in the unitary group of …`). Use it only together with S-C (−1).
- **P-7. Figure caption (rev5b l.161–167).** NEW = rev5a l.143–147 (−2).
- **P-8, optional. Lemma B self-contained.** Put the Ω, Λ, R_Δ, G_Δ definitions inside the statement, as rev5a l.490–495 does, but KEEP rev5b's conclusion clause (`$\xi\mapsto\operatorname{diag}(u_\xi,1,1)$ embeds $[\Lambda,\Lambda]$, which contains $[\Delta,\Delta]$, in $G_\Delta$`). This improves citability at 0 lines.

**Do NOT port:**
- **rev5a's definition (l.70–80):**
  - its expander sentence is incomplete ("at least ε times as many neighbors outside it" lacks "as it has elements");
  - its tuple letter s collides with s∈S.
- **rev5a Lemma A's ending (l.486–487):** "so Δ is infinite, finitely generated and LEF" gives no reason for infiniteness; rev5b's "infinite because it contains Γ" is right.
- **rev5a Lemma B's "contains a copy of":** it is vaguer than rev5b's conclusion.

**Main's other candidates:**
- **δ (group element / basis vector / Kronecker), 1 vs e, x (point vs [g,h]), m (B_m, levels, m_k):** leave them. Each role is local to one proof and standard.
- **E_st:** rev5b already prints E_{t′t} (l.447).

## 4. rev5a shared statements, one pass

- **Definition:** rev5a has the defects listed above; rev5b's is correct.
- **Theorem 2:** rev5a's (b) is correct but less explicit on the dependence of k.
- **Lemmas:** rev5a's Lemma A infiniteness reason is missing, and its "contains a copy" is vaguer. rev5a Lemma C (λ) is correct.
- **Placement:** rev5a places the definition before Theorem 1, which avoids R2 but delays the theorem; rev5b + R2 reads better.

## 5. Length: cuts toward 10 pp, ranked by value lost per line

1. **R1 P3:** −5.
2. **S-B, S2 sentence:** −4.
3. **S-C + P-6, Brown compression:** −4.
4. **P-7 caption, P-5 Arzhantseva, P-2 Darbinyan:** −4.
5. **S-D table rows:** −2.
6. **If 10 pp is a hard target:** cut S-A P2 entirely (−12). Otherwise keep it in §2.

Items 1–5 total about −19 printed lines, with +2 for P-4. p.11 holds only references [29]–[45], so items 1–5 alone will probably NOT reach 10 pp; with item 6 it plausibly will. Main needs one MSI compile to confirm.

Recommendation: apply R1, R2, S-A, S-B, S-C, S-D and P-1…P-8, and accept 11 pp with a references-only last page, rather than cutting P2.

## Flags still open (not re-verified here)

- **Locators:** Ozawa "p. 527", BHV Theorem 1.3.1 printed numbering and series, the Arzhantseva series abbreviation. All three are with sk-rev5-locators.
