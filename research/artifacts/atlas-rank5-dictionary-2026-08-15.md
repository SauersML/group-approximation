# The rank-five factorization dictionary, and the alternating-group screens

Date: 2026-08-15 (evening).  Working notes, not canonical.  Companion to
`notes/ATLAS_RELATOR_EXTRACTION_2026-08-15.md`; same setting and notation

```
Pbar = K1 * K2 = A8 * A8  --rhobar-->>  Q = L_(F2)(1,2)^times
Rbar = ker rhobar,   Nbar = [Pbar, Rbar],   Pbar/Nbar = U_Q
```

and `X` = the twelve adjacent transvections, six per factor.

---

## 1. Step 2 of `atlas-steinberg-rank-five-translation` is DONE

**All 100 rank-five Steinberg generators are now explicit words in `X`, each
verified exactly against its Leavitt target.**

### 1.1 The code

Take the *join* of the two atlas charts as the rank-five prefix code:

```
E = (E_1,...,E_5) = (000, 001, 010, 011, 1)
                  = (gamma_1 e, gamma_1 f, gamma_2 e, gamma_2 f, gamma_3)
```

over the rank-three code `gamma = (00, 01, 1)` of `EXPLICIT_LEAVITT_ATLAS.md`
§1.  `E` is a complete prefix code of size five, so `Theta_E` identifies
`M_5(L)` with `L` and `E_5(L)` with `Q`.

`E` is exactly the **join** of the two atlas charts `D_1 = {01,1,000,001}` and
`D_2 = {00,1,010,011}`, and it is a perfectly legal complete prefix code -- the
chart-overlap theorem does NOT forbid it.  What that theorem says is only that
`E` refines `D_1` and `D_2` **non-uniformly**, so `GL(E)` contains neither `K1`
nor `K2`.  That turns out to be **irrelevant**.
The criterion needs each `x_kl(b)` to be a *word* in `K1 cup K2`, not to lie in
a chart.  The uniform-refinement obstruction blocks the chart-containment
route, not the factorization route.  (A five-leaf code cannot be a *uniform*
refinement of a four-leaf code at all -- those have `4 * 2^k` leaves -- so
"five-leaf uniform prefix code" is not a thing to look for; the `4 * 2^k`
uniform refinements are the superperfect hosts `G_i` of §9, a different role.)

### 1.2 The mechanism

Write `p(k)` for the rank-three parent index of `E_k` and `s_k` for its suffix
(`p = (1,1,2,2,3)`, `s = (e,f,e,f,-)`).  Then

```
E_k b E_l^* = gamma_{p(k)} ( s_k b s_l^* ) gamma_{p(l)}^* .
```

**Case A, `p(k) != p(l)` (80 of the 100).**  The target lies in the abelian
root subgroup `U_ij(c) = 1 + gamma_i c gamma_j^*`, which is a homomorphic image
of `(L,+)`, and its coefficient `c = s_k b s_l^*` is a product of **at most
three** letters of `S cup {1} = {1,e,f,e^*,f^*}`.  Iterating the Steinberg
identity `[U_ik(a), U_kj(b)] = U_ij(ab)` over the three rank-three indices
reaches every such product from the thirty rank-three generators, which are
already explicit atlas words (`atlas_two_chart_search.q`: twenty are single
chart transvections, ten come from the identities (7) of §3).

`c` is never zero: the letter pattern is always
`[unstarred][b][starred]`, and a product of `S`-letters vanishes only across a
`starred -> different unstarred` adjacency, which cannot occur in that pattern.

**Case B, `p(k) = p(l)` (the 20 with `(k,l)` in `{(1,2),(2,1),(3,4),(4,3)}`).**
Route through index five, whose parent differs from both:

```
x_kl(b) = [ x_k5(b), x_5l(1) ].
```

Index five -- the leaf `gamma_3 = f`, the one leaf the two charts share -- is
the hinge of the whole dictionary.

### 1.3 Result

`experiments/atlas_relator_rank5_dictionary.py`:

```
rank-five generators built: 100
verified exactly:           100
syllable length: min 1  max 17  total 868
X-length:        min 1  max 92  total 3412
recipes: U_12 20, U_21 20, U_13 10, U_23 10, U_31 10, U_32 10,
         [x_k5(b), x_5l(1)] 20
```

Verification is exact binary Leavitt arithmetic against the target
`1 + E_k b E_l^*`; no step is taken on trust.  The dictionary is compact: the
median generator is a short commutator, and the 92-letter worst case is a
three-factor coefficient nested two deep.

---

## 2. The rank-five Steinberg relation family

`experiments/atlas_relator_rank5_relations.py` mirrors, at rank five, the
rank-three family already in `atlas_two_chart_search.relation_family()`:
for each ordered triple `(i,j,k)` of distinct indices, the Steinberg
multiplication relations `stL`/`stR`, the Cuntz relations `e^*e = f^*f = 1`,
the vanishing relations `e^*f = f^*e = 0`, the partition relation
`ee^* + ff^* = 1`, and the root-orthogonality commutators.

Every emitted word is **verified exactly to lie in `Rbar`**.

### 2.0 The reverse dictionary, and the surjectivity gap it closes

There is a gap in the naive Step 3 plan that has to be closed, and it is easy
to miss.  Put `G = Pbar/<<T_St>>`.  The defining relators of `St_5(L)` die in
`G` by construction, so there is a homomorphism

```
psi : St_5(L) -> G,     X_kl(b) |-> [ x_kl(b) ].
```

The centrality argument needs `psi` to be **surjective** -- only then is `G` a
quotient of `St_5(L)`, so that `ker(G ->> Q) = psi(K_2(5,L))` is central.  And
surjectivity is *not* automatic: `G` is generated by the images of `K1` and
`K2`, not by the `x_kl(b)`.

`experiments/atlas_relator_rank5_reverse.py` closes it.  Each atlas generator
is a chart leaf transvection `1 + d_u d_v^*`; the rank-five code `E` refines
both charts as a prefix code, so `d_u = sum_w d_w sigma_w^*` over the `E`-leaves
`w` extending `u`, with `|sigma_w| <= 1`.  Hence

```
d_u d_v^* = sum_{w,w'} d_w (sigma_w^* sigma_{w'}) d_{w'}^* ,
```

each coefficient `sigma_w^* sigma_{w'}` is a single letter of `{1,e,f,e^*,f^*}`
or zero, and all cross products of the rank-one terms vanish, so the terms are
pairwise annihilating and multiply out in any order.  Result (all twelve
verified exactly):

```
gen     chart u     v      rank-five factors        syll  X-len
p1_01   1     01    1      x_35(E) x_45(F)            17     70
p1_10   1     1     01     x_53(e) x_54(f)            16     66
p1_12   1     1     000    x_51(1)                     1      1
p1_21   1     000   1      x_15(1)                     1      1
p1_23   1     000   001    x_12(1)                     1      1
p1_32   1     001   000    x_21(1)                     1      1
p2_01   2     00    1      x_15(E) x_25(F)            17     70
p2_10   2     1     00     x_51(e) x_52(f)            16     66
p2_12   2     1     010    x_53(1)                     1      1
p2_21   2     010   1      x_35(1)                     1      1
p2_23   2     010   011    x_34(1)                     1      1
p2_32   2     011   010    x_43(1)                     1      1
```

**Eight of the twelve atlas generators are single rank-five generators**, and
the other four are products of exactly two.  Adding the twelve relators
`g w_g^{-1}` to `T_St` makes `psi` surjective.

Bonus: this also **removes a literature dependency**.  One would otherwise need
Khanh--Thanh at rank five to know `Theta_E(E_5(L)) = L^times`.  But the twelve
atlas generators generate `Q`, and each is now an explicit product of
`x_kl(b)`, so the rank-five Steinberg generators generate `Q` -- proved in
repo, not cited.

### The consistency check that matters

By `atlas-kernel-window-length-nine`, every usable relator list must contain a
word **outside** `ker((p_1,p_2) : Pbar ->> A8 x A8)`.  So if the rank-five
family were entirely inside that cartesian subgroup, either the dictionary or
the family would be wrong.  On the six-triple smoke run:

```
distinct nonidentity relators:      83
verified to lie in Rbar:            83 of 83
relators with nontrivial (p1,p2):   11 of 83
syllable length: min 9   max 139
X-length:        min 11  max 465
```

**The family escapes the cartesian subgroup.**  This is the first independent
confirmation that the two halves of this session's work agree: the obstruction
theorem says a usable list must reach X-length 9, and the Steinberg family
independently produces relators that do.

Full run, all 60 index triples plus root orthogonality (MSI job 15857924, 5.7 s):

```
index triples:                      60
distinct nonidentity relators:      1112
verified to lie in Rbar:            1112 of 1112
relators with nontrivial (p1,p2):   132 of 1112
syllable length: min 4   max 139
X-length:        min 4   max 595   total 157088
```

Artifacts: `experiments/atlas-rank5-dictionary.json`,
`experiments/atlas-rank5-relations.json` (on the MSI project dir; not fetched
back, they are regenerable in seconds).

### What remains for Step 3

Put `T_St` = the 1112 translated Steinberg relators **together with** the twelve
surjectivity relators of §2.0, so `|T_St| = 1124`.  All of it is verified to lie
in `Rbar`.  The chain to usability is then:

1. every defining relator of `St_5(L)` dies in `G = Pbar/<<T_St>>`, so
   `psi : St_5(L) -> G` exists;                        [by construction]
2. `psi` is surjective;                                [§2.0, proved in repo]
3. so `G = psi(St_5(L))`, and
   `ker(G ->> Q) = psi(ker(St_5(L) ->> Q)) = psi(K_2(5,L))`;
4. `K_2(5,L)` is central in `St_5(L)` for rank >= 5 (Kervaire--Steinberg), and
   the image of a central subgroup under a surjection is central, so
   `ker(G ->> Q)` is central;
5. hence `Nbar <= <<T_St>> <= Rbar`, and `T_St` is usable.
                                       [atlas-relator-central-sufficiency]

Step 1 needs the emitted family to be a **complete** presentation of `St_5(L)`
on the generators `X_kl(b)`, `b` in the five-letter ring generating set, over
`L = F2<e,f,e^*,f^*>/(e^*e = f^*f = 1, e^*f = f^*e = 0, ee^* + ff^* = 1)`.

**That single statement is now the entire remaining gap.**  It is the
Krstic--McCool input, a literature-transcription premise not verified in repo,
and it is a derivation task rather than a computation.  Everything else in the
chain is either machine-checked here or a one-line group-theoretic step.  Note
in particular that the argument needs *no* information about `K_2(5,L)` beyond
centrality: not vanishing, not finite generation, and no identification of `U_Q`
with `St_5(L)`.

---

## 2.5 AUDIT against (St1)-(St3): the emitted family is a strict subset

With the Steinberg presentation now pinned verbatim in
`research/artifacts/atlas-relator-extraction-2026-08-15.md` (from
Ershov--Jaikin-Zapirain arXiv:0809.4095v2 p. 35),

```
(St1)  E_ij(r) E_ij(s) = E_ij(r+s)
(St2)  [E_ij(r), E_kl(s)] = 1          if i != l and k != j
(St3)  [E_ij(r), E_jk(s)] = E_ik(rs)   if i != k
```

the 1112-relator family of §2 can be audited, and it **fails to be even the
instantiation on the ring generators**.  Three gaps:

1. **(St2) emitted for only 3 of the 25 coefficient pairs** -- `("1","1")`,
   `("e","f")`, `("E","F")`.  The rank-three family it mirrors describes this as
   "a compact but representative coefficient set", which is right for a
   necessary-constraint list and wrong for a presentation.
2. **(St2) with `(k,l) = (i,j)` -- root-subgroup commutativity
   `[x_ij(a), x_ij(b)] = 1` -- omitted entirely.**  The enumeration ranged over
   unordered pairs of DISTINCT roots.  Its side condition `i != l, k != j` is
   satisfied there (both reduce to `i != j`), so it is a genuine instance of
   the scheme, not an addition to it.
3. **(St3) emitted only where `rs` lands back in `{1,e,f,e^*,f^*} u {0}`** --
   about ten of 25 pairs.  Products like `ee`, `ef`, `ee^*`, `fe^*`, `e^*e^*`
   were absent.

So the 1112 relators are a **necessary** set, not a candidate presentation.
`experiments/atlas_relator_rank5_full_family.py` emits the full instantiation;
this needed extending the dictionary to multi-letter coefficients so that
(St3)'s right-hand side `x_ik(rs)` exists for two-letter products.

### (St1) contributes no relator, and where additivity really lives

`r+s` is not a generator, so `x_ij(r+s)` is *defined* as the product.  The
content of additivity is **consistency** of that definition: any two ways of
writing `c in L` as a sum of monomials must give the same group element.  Two
such writings differ by consequences of the defining ring relations, i.e. by
terms `u rho v` with `u, v` arbitrary monomials -- an infinite family of
contexts.

### Why bounded contexts should be derivable, and why the threshold is n >= 4

The infinitude is only apparent.  Given additivity and (St3), for monomials
`u, v` and a relation word `rho`,

```
x_ij(u rho v) = [ x_ik(u), x_kj(rho v) ],
x_kj(rho v)  = [ x_kl(rho), x_lj(v) ],
```

so **`x_kl(rho) = 1` for every root already forces `x_ij(u rho v) = 1` for all
contexts `u, v`** -- the relations absorb their own contexts.  The nesting needs
enough distinct indices to keep every root legal, which is exactly where the
`n >= 4` threshold of [KrM, Theorem 3] comes from, and it matches the sharp
`n = 3` failure of their Corollary 2.  At `n = 5` there is room to spare.

The five context-free relations we need are precisely the CK relations, and
**the emitted family already has all five**:

| ring relation | emitted as |
|:--|:--|
| `e^*e = 1`, `f^*f = 1` | `cuntz` |
| `e^*f = 0`, `f^*e = 0` | `zero` |
| `ee^* + ff^* = 1` | `partition` |

So after closing gaps 1-3, the remaining question is narrower than "read KrM":
it is whether the scheme with context-free ring relations presents `St_n(R)`
for `n >= 4`, the technical core being well-definedness of `x_ij(w)` with
respect to the chain of spare indices used to build it.  **This is a designed
derivation, not a proved one** -- the absorption step above is two lines, the
spare-index well-definedness is not.

### The full instantiation, emitted

`experiments/atlas_relator_rank5_full_family.py` (MSI job 15860125, 22 s):

```
two-letter dictionary entries verified exactly:  500
(St2) root-subgroup commutativity:               200
(St2) disjoint-root commutation:               2,980
(St3) Steinberg products (all 25 pairs):       1,432
distinct nonidentity relators:                 4,612
verified to lie in Rbar:                4,612 of 4,612
syllable length: min 4   max 117
X-length:        min 4   max 556   total 713,754
relators with nontrivial (p1,p2):         72 of 4,612
```

With the twelve reverse-dictionary relators, `|T_St| = 4,624`.

The (St3) block also carries the **spare-index well-definedness** content: for
every root `(i,k)` and every coefficient pair, all three intermediate indices
`j` are equated to the SAME canonical word `x_ik(rs)`, so the three chains are
identified by emitted relations rather than by assumption.  That is the half of
the technical core §2.5 flagged; what remains unproved is the same statement
for monomials longer than two, which the absorption argument reduces but does
not by itself settle.

Cost note for the eventual one-unitary optimization: what matters there is
syllable count, not X-length, since each syllable is one `A8` element evaluated
as `lambda(h) tensor I` or `U(lambda(h) tensor I) U^*`.  Total syllables across
the family are on the order of `10^5`.  The family is deliberately NOT
minimized -- a Tietze reduction pass is an obvious and unattempted follow-up.

---

## 3. Finite-quotient screen: the degree-nine case

The Goursat analysis of `ATLAS_RELATOR_EXTRACTION_2026-08-15.md` §(b) left one
family unscreened: `F0 = <alpha(A8), beta(A8)>` with both restrictions
injective and the images neither equal nor commuting.  Two observations reduce
it sharply.  Put `n = [F0 : alpha(A8)]`.

* If `beta(A8) <= alpha(A8)` then `F0 = alpha(A8)`: the fold case, already
  screened (0 of 40320 survive).  Otherwise `beta(A8) cap alpha(A8)` is proper
  in a copy of `A8`, so `n >= 8`, the minimal index of a proper subgroup of
  `A8`.
* `n = 8` is impossible.  The kernel `N` of the degree-eight action meets
  `alpha(A8)` trivially (otherwise `alpha(A8)` is normal and the perfect
  `beta(A8)` lands inside it), so `|N| <= 8` and `A8 -> Aut(N)` is trivial;
  then `alpha(A8) N = alpha(A8) x N`, and `beta(A8)`, being perfect, projects
  isomorphically into `alpha(A8)`, forcing `beta(A8) = alpha(A8)`.

**So the smallest genuinely uncovered case is `n = 9`, where `F0` is transitive
of degree nine with an `A8` point stabiliser, i.e. `F0 = A9`.**

Since the minimal faithful permutation degree of `A8` is 8, every `A8` subgroup
of `A9` is a point stabiliser, so up to global conjugacy `alpha = iota` is
fixed and every injective `beta` is `conj_d . iota . graph^eps` with `d in A9`,
`eps in {0,1}`: exactly `2 * 181440 = 362880` pairs, enumerated exhaustively.

There is no computer-algebra system on the MSI nodes (no `gap` module, no
binary), so `iota : GL_4(F2) -> A8` is built from scratch:
find a subgroup of order 2520 by random search, then take the action on its
eight cosets.  **Trap recorded:** the right-coset action is an
*anti*-homomorphism for the composition `(p*q)(i) = p(q(i))`; each value must be
inverted.  A positive control caught this (see §5).

Result: see §4.

---

## 4. Runs

MSI, `amdsmall` partition via `msi sub` (sioux/acn112/acn116 avoided, held by a
peer's compile loop).  Job logs under
`experiments/atlas-a9-screen-*.log` and `experiments/atlas-rank5-relations-*.log`.

| job | id | outcome |
|:--|:--|:--|
| A9 screen, windows through radius 5 | 15857813 | **0 survivors of 362880**, 76 s |
| full (St1)-(St3) family + reverse dictionary | 15860125 | 4,612 relators, all in `Rbar`, 22 s |
| Step-4 screens on the full family | 15860399 | see log |
| rank-five dictionary + relation family | 15857924 | 100 + 1112, all verified, 5.7 s |
| degree-10 screen, windows through radius 5 | 15858592 | **0 survivors of 3628800**, 88 s |

**Degree-nine screen verdict: PASSES.**  All `362880` pairs of injective
homomorphisms `A8 -> A9` were tested; none kills the enumerated kernel windows
through radius 5.  Stage 1 (the twenty radius-2 commuting conditions,
vectorised over `A9`) already eliminates every pair, in both the inner and the
graph-twisted sector.

Screen status for the radius-5 window is now:

| family of finite quotients | status |
|:--|:--|
| one factor restriction trivial: `A8 x A8` and quotients | passes (radius 5); FAILS at radius <= 4 |
| both injective, equal image: 40320 folds onto `A8` | passes |
| both injective, commuting images | subsumed by `A8 x A8` (Goursat) |
| `n = [F0 : alpha(A8)]` in `{2,...,8}` | impossible (proved, §3) |
| `n = 9`, i.e. `F0 = A9` | **passes** |
| every `F0 <= A10` generated by two copies of `A8` | **passes** (degree-10 run) |
| `Sp6(2)` (n=72), `GL5(2)` (496), `M24` (12144), ... | open |

The degree-10 run enumerates all `2 * 1814400 = 3628800` pairs and subsumes the
degree-nine one.  Together these close every alternating target through degree
10.  What remains open are finite quotients not embeddable in `A10`, i.e. the
matrix and sporadic candidates `Sp6(2)`, `GL5(2)`, `M24`; those need a group
library the nodes do not have, and would have to be built by hand the way
`iota` was.

This does not certify the window -- no enumeration can, per
`atlas-kernel-window-length-nine` -- but it removes the cheapest refutations.

---

## 5. Controls, and a bug they caught

Two positive controls are built into the scripts rather than run once by hand.

1. **`iota` is a homomorphism.**  Checked on 400 random products after the
   coset construction.  The first version of the screen failed this: the
   right-coset action gives `iota(gh) = iota(h) . iota(g)`.
2. **`p_1` evaluation cross-check.**  Evaluating a window word with `beta`
   trivial is the retraction `p_1`, so it must reproduce `iota(p_1(word))`
   computed independently in `GL_4(F2)`.  This exercises both outcomes: every
   `p_1` is trivial through radius 4, and 118 of the radius-5 words have
   `p_1 != 1`.

The first draft of control 2 asserted the *stronger* statement that `p_1` kills
every window word, and it fired at radius 5 -- correctly, since that is exactly
where the window escapes the cartesian subgroup.  Recording this because the
failure looks like a bug and is not: it is the window-length-nine theorem
showing up in the control.

---

## 6. Files

* `experiments/atlas_relator_rank5_dictionary.py` -- the 100 generators, verified
* `experiments/atlas_relator_rank5_relations.py` -- the partial (necessary) family
* `experiments/atlas_relator_rank5_full_family.py` -- the FULL (St1)-(St3) family
* `experiments/atlas_relator_screen_on_list.py` -- Step-4 screens on an emitted list
* `experiments/atlas_relator_rank5_reverse.py` -- the twelve surjectivity relators
* `experiments/atlas_relator_a9_screen.py` -- degree-nine finite-quotient screen
* `experiments/atlas_relator_alternating_screen.py` -- the same at degree `m <= 11`
* `experiments/atlas_relator_commuting_pairs.py` -- the cross-chart commuting
  relation and why the uniform argument does not close (see §7)

## 7. Why the screen could not be closed by a uniform argument

If every one-sided partner set `S_y = { x : [x,y] = 1 in Q }` generated `A8`,
then any finite quotient killing the radius-2 window would have commuting
images and the whole uncovered family would be empty.  It does not:

```
y = e12  S_y = {e12,e32}              |<S_y>| =   4
y = e21  S_y = {e21,e23}              |<S_y>| =   4
y = e23  S_y = {e21,e23,e34,e43}      |<S_y>| =  48
y = e32  S_y = {e12,e32,e34,e43}      |<S_y>| =  48
y = e34  S_y = {e23,e32,e34,e43}      |<S_y>| = 168
y = e43  S_y = {e23,e32,e34,e43}      |<S_y>| = 168
```

The commuting relation is symmetric, and the largest partner subgroup is
`GL_3(F2)` of order 168 -- the same rank-three corner that carries the
radius-5 boundary (`TF3`/`TF4` of `atlas_kernel_collision_results.md`).  So the
screen has to be run, not argued.
