# sk-verify-19: independent verification, part 1 (2026-09-14)

Lane sk-verify-19. I serve `sk2/queue/verify.txt` from the bottom; sk-verify-18 serves it from the top. Every step below was re-derived by hand. Main tip at review: cf951f0103.
- Queue line 1 (`comb-tooth-tree-stages-are-residually-finite-dimensional`): SKIPPED, because sk-verify-18 already has a PASS review line on the node.
- This part covers queue lines 3 and 2 plus the pending gate `subshift-elementary-group-lef-growth-complexity-bounds`. Lines 4 and later go to part 2.

## 1. Queue line 3: `dyadic-tower-subshift-full-group-non-uniform-growth` (Theorem Q′, sk-sturmian-growth). PASS-WITH-FIXES

**Claim.** An infinite minimal subshift with (H1) constant towers X = ⊔_{i<2^m}T^iV_m, T^{2^m}V_m = V_m, and (H2) a clopen partition 𝒫_m of V_m, |𝒫_m| ≤ K, whose S_m-translates generate the clopen algebra of V_m, at infinitely many m. Then ω([[T]]′) = 1, and [[T]]′ has exponential growth.

**Method.** This is a transfer claim, so the review is an inventory of every use of the substitution in the twice-passed dyadic proof (sk-cornulier-q7 parts 1–2; sk-verify-16 part4, sk-verify-17), checking that each use follows from (H1) or (H2), and that nothing else is used.
- **Part 1 §1 (M1)** (Mossé tower partition) is used only to produce the clopen V_m with X = ⊔T^iV_m and T^{2^m}V_m = V_m, i.e. (H1). The conjugacy ζ^m: (X,T) ≅ (V_m,S_m) is used only at §3 "Letter cylinders" (the Boolean algebra generated by the S-translates of U_a is the whole clopen algebra of V "because (V,S) ≅ (X,T) is a subshift"). That is exactly (H2) with 𝒫_m = {U_a}, K = |A|.
- **Part 1 §2 (tower model)** uses only (H1): uniform and partial level permutations, s_y, T(v,y) = (Sv, β^{-1}(0)) at the top level. Alt(Y_n) perfect needs N ≥ 5.
- **Part 1 §3 (generators)**: types 1–3 are defined from (H1) alone. Type 4 uses ζ_a over the disjoint clopen U_a covering V. Under (H2) the ζ_a with a ∈ 𝒫_m are disjoint, pointwise the same 3-cycle (3 4 5), so they commute and generate (Z/3)^{|𝒫_m|}. So |T_n| = 20166 + |𝒫_m| ≤ 20166 + K, independent of n. Padding 𝒫_m with repeats is unnecessary: the count only needs |𝒫_m| ≤ K.
- **Part 1 §4 Step 1** (Alt(8) and the linking graph) uses only the Grigorchuk levels and (H1).
- **Step 2(b):** 𝔅 = {B ⊆ V clopen : ζ_B^{(1,2,3)} ∈ W_n} is closed under complements (ζ_{V∖B} = ζ_V ζ_B^{-1}), intersections ([(123),(345)] = (143) pointwise) and S^{±1} (conjugation by ψ = σ_{1,4}σ_{2,5}σ_{3,6}). It contains 𝒫_m. So it contains the Boolean algebra generated by the S-translates of 𝒫_m, which is all clopen sets by (H2) (fix F1).
- **Step 2(c) and Step 3** (good σ_U as products of twisted 3-cycles on three consecutive levels, twists in {0,±1}) use only (H1) with N = 2^m ≥ 32, plus (M2) = Cornulier's Lemme `eng_cb`, which holds for every infinite minimal subshift.
- **Part 2 §5 Lemma 5.1** (commuting conjugates via disjoint fibre supports), the normal form and the counting P_1–P_4 use only levels, sheets and the per-point group (Z/3)^{|𝒫_m|} × Z. The only alphabet dependence is 3^{|A|} → 3^K. So C, β depend only on K.
- **Conclusion.** For each good scale, |B^{T_n}(ℓ)| ≤ exp(Cℓ^β) for ℓ ≤ r_n/3. The exponential growth rate of a generating set is inf_ℓ |B(ℓ)|^{1/ℓ} (submultiplicativity), so ω([[T]]′) ≤ exp(C⌊r_n/3⌋^{β−1}) → 1 along the infinitely many good scales. Exponential growth comes from Matui via `minimal-subshift-derived-full-group-exponential-growth`.

**Hypotheses model-tested.**
- **(H1) forces a continuous eigenvalue (Lemma O).** f = Σ_j e^{2πij/2^m}1_{T^jV_m} is continuous and f∘T = e^{2πi/2^m}f. So totally minimal subshifts, and in particular Sturmian subshifts, fail (H1). Consistent with the firewall.
- **Induced minimality is automatic.** The levels T^iV_m are disjoint and T^{2^m}V_m = V_m, so S_m is the first-return map of T to V_m, hence minimal.
- **The odometer fails (H2).** Coding (V_m,S_m) by 𝒫_m gives a continuous equivariant injection (since translates of 𝒫_m separate points) onto a subshift, which would make the odometer expansive. Consistent with part 2 l.69.
- **Thue–Morse satisfies (H1) and (H2)** with K = 2. Consistent.

**Fixes (0 lines).**
- **F1:** state (H2) as "whose S_m-translates generate, as a Boolean algebra, all clopen subsets of V_m". Step 2(b) needs finite Boolean combinations, not a generated topology or σ-algebra.
- **F2:** good scales need m ≥ 8 (n = m − 3 ≥ 5). Harmless, since infinitely many good m.

## 2. Queue line 2: sk-free-9 P1 note text (the simple limits of the Section 2 models are exactly the G_X). PASS-WITH-FIXES

**Text reviewed.** The LaTeX block in `research/artifacts/sk-free-9-proposal-2026-09-14.md`, against rev4.3 (md5 0648e5f8…) l.249–271 (§2 models) and l.507–514 (§4 criterion).

**Steps re-derived.**
1. **Marking.** For any periodic y of least period N, the images of the generators are e_ij(I), e_ij(P^{±1}) and e_ij(D(e_a)) with D(e_a)δ_t = [y_t = a]δ_t. They need no subshift, so the marked groups are well defined.
2. **Windows.**
   - A word of length ≤ λ has entries Σ_{|j|≤λ} D(f_j)P^j, each f_j with window [−λ,λ], by PD(f)P^{-1} = D(f∘T^{-1}).
   - For N > 2λ+1 distinct exponents do not alias mod N.
   - So the word is trivial iff every table of (word − I_3) vanishes on the cyclic (2λ+1)-windows W_λ(y).
3. **Convergence iff stabilization.**
   - (⇐) If each W_λ(y) is eventually constant, the triviality of each word is eventually constant.
   - (⇒) The word for e_12(∏_{t<m}u^{-t}e_{v_t}u^t) has length O(m) and is trivial iff v ∉ W(y), so convergence forces every W_λ(y) to stabilize.
4. **The limit is G_Y.**
   - L = ⋃_λ lim W_λ is factorial and bi-extendable (windows of cyclic words extend), so it is the language of a subshift Y.
   - A word is trivial in G_Y iff its tables vanish on L(Y). This is the §4 argument (l.507–511), which uses no minimality.
   - So the marked limit is G_Y with the marking e_ij(s), s ∈ S.
5. **Y is infinite.**
   - Suppose Y is finite, with N_0 = |Y|, and fix λ with 2λ > 2N_0.
   - Every long word of Y lies in a single periodic orbit with a determined phase.
   - For large N, overlapping cyclic windows of y therefore agree with one periodic point of period ≤ N_0.
   - So y has period ≤ N_0, contradicting N → ∞.
6. **Not minimal ⇒ not simple.**
   - Let ∅ ≠ Z ⊊ Y be closed and invariant, and I = {Σf_ju^j : f_j|_Z = 0}.
   - I is a two-sided ideal (Z invariant). It is nonzero (a cylinder U ∩ Y disjoint from Z gives e_U ∈ I) and proper (1 ∉ I as Z ≠ ∅).
   - The kernel of G_Y → GL_3(R_Y/I) contains e_12(e_U) ≠ 1 and misses e_12(1), since R_Y/I ≅ LC(Z,F_2)⋊Z ≠ 0.
7. **Attainment.** Theorem 1 (simplicity for X minimal) with y = y_ℓ exhibits every G_X as such a limit.

**Fixes to the printed block.**
- **F1 (MUST, 0 lines):** "A proper closed invariant subset of $Y$" → "A nonempty proper closed invariant subset of $Y$". The empty set gives the whole ring, which is not a proper ideal.
- **F2 (MUST, 0 lines):** "level subgroup" is never defined in the paper; "level" only appears for the overgroup levels in §3. Replace "gives a proper nonzero ideal of $\LC(Y,\F_2)\rtimes\Z$, whose level subgroup is a proper normal subgroup" with "gives a proper nonzero ideal $I$ of $\LC(Y,\F_2)\rtimes\Z$, and the kernel of the map from $G_Y$ to $\GL_3$ over the quotient by $I$ is a proper nontrivial normal subgroup".
- **F3 (recommended, 0–1 line):** "Mark $\SL_{3N}(\F_2)$ as there" relies on §2's $D_\ell(f)$, which evaluates at points of $X$. For a general periodic $y$ only the generator images are needed. Write "Mark $\SL_{3N}(\F_2)$ by the images $e_{ij}(I)$, $e_{ij}(P^{\pm1})$ and $e_{ij}(D(e_a))$, where $D(e_a)\delta_t=\delta_t$ if $y_t=a$ and $0$ otherwise, for a periodic sequence $y$ of least period $N$".

With F1–F3 the paragraph is correct and complete. Its mathematics is also on main, twice passed, as `generic-limit-of-subshift-sl-models-is-simple-kazhdan`.

## 3. Pending gate: `subshift-elementary-group-lef-growth-complexity-bounds` (+ `-proof`; artifacts sk-lef-growth-bounds parts 1–2). PASS-WITH-FIXES

**Theorem A (upper bound).** L_X(r) < 2^{9N²} with N = N_X(4r+1).
- (a) Entries of ball elements are Σ_{|j|≤r}f_ju^j with windows in [−r,r]; products use windows in [−2r,2r].
- (b) Model Σf_ju^j ↦ ΣD(f_j)P^j.
  - Multiplicative for g, h, gh ∈ B(r). Coefficients at exponents k′ ∈ [−2r,2r] add as in R, since D is additive and multiplicative on windows ≤ 2r. No aliasing, as N ≥ p_X(4r+1) ≥ 4r+2 > 4r.
  - Invertible, since g^{-1} ∈ B(r).
  - Injective: a nonzero f_j equals 1 on some (2r+1)-word, which occurs in y.
  - |GL_{3N}(F_2)| < 2^{9N²}.
- (c) N_X(n) ≤ 2R_X(n).
  - With v an (n−1)-word at p and q the first occurrence of v at or after p + R_X(n) − n + 1, the cyclic word x_{[p,q)} has as cyclic n-windows the windows of x_{[p,q+n−1)}. That word has length ≥ R_X(n), so all n-words occur, and only words of X.
  - q − p ≤ R_X(n) + R_X(n−1) ≤ 2R_X(n).
- (d) The covering closed walk in the strongly connected Rauzy graph has length ≤ E·V + V.

**Corollary A1 (Sturmian).** V = n, E = n+1. A strongly connected digraph is a cycle plus E − V further ears, so Γ = C ∪ P. The walk C, then P, then back along C covers everything with length ≤ 2|C| + |P| ≤ 2E = 2n+2. So L ≤ 2^{9(8r+4)²}.

**Lemma 0.** By induction on prefixes, all in B(r). φ(1) = φ(1)² gives φ(1) = 1.

**Lemma 1.** D = w_ik(u)w_ik(1) = diag(u at i, u^{-1} at k, 1 at j) (block [[0,u],[u^{-1},0]]·[[0,1],[1,0]]), length 6. Conjugation gives e_ij(ur), and the four variants each cost 12 letters.

**Lemma 2.**
- e_{[w]} = e_{[w′]}·u^{-2^h}e_{[w″]}u^{2^h}, since u^{-c}e_Vu^c = e_{T^{-c}V}.
- The commutator form gives T(h+1) ≤ 4T(h) + 48·2^h.
- Unrolled: T(h) = 4^h + 48·4^{h−1}Σ_{k<h}2^{−k} ≤ 25·4^h.

**Theorem B.**
- (B1) φ is injective on B(r), and G_X is non-amenable, hence of exponential growth.
- (B2):
  - relation lengths are 125·4^h for (i) and 100·4^h for (ii);
  - P_v is perfect and nontrivial;
  - if (p_v) ∈ ker μ, then p_v = ∏_{w≠v}p_w^{-1} centralizes P_v, so ker μ ⊆ ∏Z(P_v) and |Q| ≥ ∏|P_v/Z(P_v)| ≥ 60^{p_X(2^h)};
  - h maximal with 125·4^h ≤ r gives 4^{h+1} > r/125, hence 2^h > √(r/500) and so 2^h ≥ ⌈√(r/500)⌉;
  - monotonicity of p_X gives the stated bound. The direction is correct.

**Lemma 3 / Theorem C.**
- Height filtration: Ũ_h/Ũ_{h+1} is abelian of order ≤ 2^{d−h}, so |Ũ| ≤ 2^{d(d−1)/2} and Ũ ≅ U_d(F_2).
- A nontrivial normal subgroup of a finite 2-group meets the centre ⟨I + E_1d⟩.
- Transvection lengths are ≤ λ = 4(κ + 24m) (Lemma 1 with |a|, |b| ≤ m; the within-block ones are commutators).
- Relation lengths are ≤ 5λ = 20κ + 480m ≤ r.
- E_ab(W)E_cd(W) = δ_bc E_ad(W), because W ∩ T^{c−b}W = ∅ for 0 < |c−b| ≤ 2m.

**Consequence (D2)** (positive entropy, or p_X(n) ≥ n^{4+ε} infinitely often, against linearly recurrent Y).
- At r = 500n²: L_X(r) ≥ 60^{p_X(n)} = exp(c·r^{2+ε/2}).
- L_Y(Cr) ≤ exp(O(r²)) by Theorem A with N_Y ≤ 2R_Y = O(n).
- So L_X ⋠ L_Y, and G_X ≇ G_Y.

**Fixes (0 lines, node and artifact wording).**
- **W1:** "positive entropy gives doubly exponential LEF growth" (node consequences; artifact Corollary B3). The proved bounds are exp(c·exp(η√(r/500))) ≤ L_X(r) ≤ exp(exp(Cr)). Say "doubly exponential in √r below and in r above".
- **W2:** Corollary A1 "has E − V ears" means E − V ears beyond the initial cycle.
- **W3:** the node's status line "queued with sk-referee-2" is superseded by this review.
- **Not reviewed here:** part 2 Theorem D (Sturmian exp(Θ(r²)) lower bound) and (D4), which are not in this node's statement.

**Dependent node.** `positive-entropy-el-groups-avoid-low-complexity-hosts` (sk-verify-15 PASS, conditional on this node). The condition is now discharged, and a review line is merged there.
