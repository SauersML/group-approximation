---
rg: 2
id: finite-bi-index-clifford-tape-presentation
kind: claim
title: A finite-bi-index action gives a finitely presented infinite Clifford tape with a surviving central sign
artifacts:
  - research/artifacts/schur-clifford-predicate-rank-compiler-2026-08-19.md
distinct_from:
  schur-flip-wreath-compressor-has-infinite-cocycle-rank: that puts finite Schur flip packets on a Kun--Thom wreath action and computes arbitrary finite cocycle rank; this directly presents one lamp whose conjugates anticommute on every off-diagonal pair orbit.
  simple-core-bi-index-counts-coset-action-pair-orbits: that identifies double cosets with pair orbits for one Boone--Higman envelope; this uses the same elementary orbit dictionary to give a Clifford-tape presentation for any suitable subgroup pair.
---

Let `L=<S|R>` be finitely presented and let `H=<T>` be a finitely generated
subgroup of infinite index such that the double-coset set `H\L/H` is finite.
Choose representatives `g_1,...,g_s` of its nontrivial double cosets.  The
group

```text
Gamma(L,H)=< S,a,J |
  R,
  a^2=J^2=1,
  [J,a]=[J,z]=1                         (z in S),
  [a,t]=1                               (t in T),
  [a,g_j a g_j^(-1)]=J                  (1<=j<=s) >              (FCT1)
```

is finitely presented.  Its marked involution `J` is nontrivial.

Indeed, on the infinite coset set `X=L/H`, let `K_X` be generated by central
`J` and involutions `a_x` with

```text
[a_x,a_y]=J             for x!=y.                                (FCT2)
```

The off-diagonal form in `(FCT2)` is alternating on the finitely supported
`F_2`-space on `X`, so `K_X` is a consistent locally finite class-two
2-group.  The permutation action of `L` on `X` preserves `(FCT2)`, and
`K_X semidirect L` satisfies `(FCT1)` with `a=a_H`.  Its central sign is
nontrivial, proving the assertion for the presented group.

Thus finite presentation and exact marked completeness cost only finitely
many pair orbits.  The remaining analytic problem is to make an approximate
representation of `(FCT1)` define one coherent matrix `A_x` for every coset,
with a pair-error bound independent of the representatives.

**Augmentation--Heisenberg identification.**  This also contains the
apparently different invariant-dot-product construction.  Put

```text
M=ker(F_2^(X) -> F_2),
```

and let `K_X^0` be the inverse image of `M` in `K_X`.  The complete-graph
commutator form satisfies

```text
b(f,g)=(sum f)(sum g)+sum_x f(x)g(x),
```

so on `M` it is exactly the invariant dot product.  Hence
`K_X^0 semidirect L` is the corresponding Heisenberg augmentation group;
it is the parity-index-two subgroup of the Clifford tape and is finitely
presented as well.  Every exact finite-dimensional representation kills
`J`, since disjoint triples of sites supply arbitrarily many independent
Pauli pairs on a hypothetical `J=-1` block.  This still does not bypass the
analytic gate: every finite augmentation window has an exact `J=-1` Weyl
model, while turning those growing windows into one orbit-wide family is
precisely `uniform-hs-site-coherence-for-finite-bi-index-pair`.
