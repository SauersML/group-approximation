---
rg: 2
id: atomic-readable-translate-algebra-groupifies-zpc-gap
kind: claim
title: One atom of the readable translate algebra supplies a profile-pure finite projection orbit
distinct_from:
  finite-readable-translate-algebra-groupifies-zpc-gap: that assumes the whole invariant readable algebra is finite dimensional; this permits an arbitrary diffuse complementary summand.
  atomic-noncohyperlinear-irs-forces-nonhyperlinear: that assumes an atom of the subgroup-valued IRS law and obtains a regular quotient character; this assumes only an atom of an auxiliary readable von Neumann algebra and uses the verifier rows and game gap.
  finite-invariant-partition-only-reduces-irs-to-fiber-irs: that draws only a character-theoretic conclusion from a finite partition; this feeds the atom orbit to the game-aware stabilizer quotient.
---

In the setup of
`profile-pure-finite-projection-orbit-groupifies-zpc-gap`, let

```text
D_infinity=W*(pi(g)D_0pi(g)^*:g in Q_G)                 (ART1)
```

inside `q_-Mq_-`. Assume that `D_infinity` is abelian, as it is in the
fixed-point/stabilizer GNS model. If it has a nonzero minimal projection,
then that projection has a finite `Q_G`-conjugacy orbit and every point of the
orbit lies below a unique joint readable atom of `D_0`. Hence the
profile-pure finite-projection compiler applies and produces a finitely
presented nonhyperlinear group. No condition is imposed on the diffuse
complement of the atomic orbit.
