---
rg: 2
id: mixed-braid-relative-word-supplies-esb-bridge
kind: route
title: Cancel the mixed-braid spectator by comparing its output with the cross Whitehead
target: el20-six-moving-coefficient-square-bridges
requires:
  - mixed-completeness-braid-retains-s3-square-gauge
---

**INVALIDATED MIXED-BRAID COMPARISON.**  Compare the aggregate braid output
`G=ABA` with the literal cross Whitehead `K_0=UVU`.  Their scoped reservoir
models differ by exactly the channel-nine spectator, so the relative word
`L=GK_0^(-1)` is the shortest candidate which could cancel that spectator
and leave an ungauged coefficient bridge.

`mixed-braid-gauge-cancellation-misses-signed-hecke-targets` computes `L`
exactly.  It transports neither the signed Hecke source nor its native
Whitehead/Singer translate, so it fails the full-Gram condition `(ESB1)`
before reservoir typing is relevant.

