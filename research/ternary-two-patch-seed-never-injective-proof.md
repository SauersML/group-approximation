---
rg: 2
id: ternary-two-patch-seed-never-injective-proof
kind: route
title: One- and two-site collisions on nonamenable placements, and the Garden of Eden theorem with measure preservation on amenable ones
target: ternary-two-patch-seed-rule-is-never-injective
requires:
  - amenable-groups-satisfy-the-garden-of-eden-theorem
  - bijective-ca-preserve-uniform-bernoulli-measure
artifacts:
  - research/artifacts/two-patch-seed-relation-moves-2026-09-12.md
  - research/artifacts/w3-vf-nonlinear-verification-2026-09-12.md
---

Artifact Sections 1–3.

1. **Amenable memory group `H = <a, b>`.**
   - Injectivity over `G` restricts to injectivity over `H`, because configurations split over the
     cosets of `H`.
   - Over amenable `H`, injective means pre-injective, which means surjective by the Garden of Eden
     theorem.
   - A bijective automaton preserves the uniform measure, so `mu_2` would be balanced. Its counts are
     `10, 8, 9`.
2. **Nonamenable `H`.** Then `a^2 = b^2 = 1`, `ab = 1`, `b = a^2` and `a = b^2` all fail.
   - **No involution:** the single flip of Lemma 1. Its 21-pair coincidence table conflicts only under
     the five listed relations.
   - **`a^2 = 1`:** Move A. Its conflicts force `H` abelian, cyclic, or generated by two involutions.
   - **`b^2 = 1`:** Move B. Its conflicts force `H` abelian, cyclic or virtually cyclic, or generated
     by two involutions.
   - In each case the table shows equal outputs at every touched window, and the untouched windows read
     identical configurations.

**Verification (w3-vf-nonlinear, 2026-09-12): valid.** Every coincidence table of Lemmas 1–3 was re-derived
independently, and the case split over `H = <a, b>` is exhaustive. The amenable case uses only the Garden of Eden
theorem and measure preservation, with counts `10, 8, 9`. The Lemma 2 pair list and the wording note are applied at
efd28946a (`research/artifacts/w3-vf-nonlinear-verification-2026-09-12.md`, Section 28).
