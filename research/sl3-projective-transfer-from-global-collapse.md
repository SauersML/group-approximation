---
rg: 2
id: sl3-projective-transfer-from-global-collapse
kind: route
title: Recover full projective transfer from canonical rank-three commutant collapse
target: projective-trace-square-transfer-for-sl3-pair
requires:
  - sl3-pair-relative-commutant-route
  - rf-perfect-pair-transfer-equals-regular-rcc
---

The second prerequisite applies to `C=SL_3(Z)` in
`A=SL_3(Z[1/2])`. Its regular RCC hypothesis is exactly `(RC3)` in
the first prerequisite. It gives
`e_h<=L(q_C+delta)` for all finite matrix assignments, hence `(PTS1)`
with `K=L` and `eta(delta)=L delta`.
