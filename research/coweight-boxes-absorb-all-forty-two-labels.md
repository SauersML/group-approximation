---
rg: 2
id: coweight-boxes-absorb-all-forty-two-labels
kind: route
title: Take full multiplicity fibers over coweight Folner boxes
target: six-parahoric-finite-fiber-folds-remain-amenable
requires:
  - native-sl3-edge-parahoric-has-index-forty-two
---

Every labelled transition in `(SFF2)` moves the base by one of the same six
bounded coweight vectors and permutes the fiber bijectively.  Because
`A_N=B_N times F` contains the whole fiber above every point of `B_N`, the
fiber permutation contributes no boundary.  Only the base translation can
leave `A_N`, proving `(SFF3)`.

For a fixed word of length `ell`, disagreement between its compressed
permutation product and the compressed target word is supported over the
`ell`-neighborhood of the coweight-box boundary.  Its normalized squared HS
defect is therefore `O(ell/N)`.  Diagonalization over fixed word windows
gives the asserted local matrix models.  Any identities imposed on the
fiber permutations only shrink the class of cocycles and do not affect the
boundary calculation.

