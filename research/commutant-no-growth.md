---
rg: 2
id: commutant-no-growth
kind: claim
title: Commutants do not grow under genuine f.d. representations
---

`commutant_no_growth` (formalized). For a genuine finite-dimensional
representation, an endomorphism commuting with `π(tΓt⁻¹)` already commutes
with `π(Γ)` — the fixed-space collapse applied to the adjoint
representation, since `π(Γ)′ = Fix(Ad π|_Γ)`. The load-bearing algebraic
step of the flexible-stability reduction rigidity-route.
