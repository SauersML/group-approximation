---
rg: 2
id: returned-a2-fold-window-places-native-pauli-factors
kind: route
title: Use the returned A2 reverse-product fold window to place the five native Pauli factors
target: separate-native-quarter-carrier-recovery
requires:
  - five-factor-native-carrier-transport-certificate
---

Dead route.  This is the strongest presently audited finite native window
beginning with the first payload-coupled object-erasing row.  Put

```text
q_i=s_i t_i,                 q_0+q_1=A,
C_i=x_14(q_i),               D_i=x_41(q_i),
J=x_14(A),                   L=x_41(A),
W_i=C_iD_iC_i,               W_A=JLJ.                 (RAF1)
```

The fixed relation

```text
W_0W_1W_A^(-1)=1                                      (RAF2)
```

is the first word in the inventory which couples the literal reverse-product
occurrences from the range-sum fold to a noncommuting opposite-root payload.
Unlike an aggregate row/column Whitehead, its coefficient derivation uses
both `q_0q_1=0` and `q_0+q_1=A` essentially.

Strengthen `(RAF2)` by retaining the native Weyl transports of the same
positive and opposite occurrences into composable roots, both oriented
cross-zero rows, the complete relative order-three `A_2` Weyl orbit, and the
direct returned-source comparisons

```text
[D_0,C_1]=[D_1,C_0]=1.                                (RAF3)
```

The hoped inference was that this returned window locks the two branch
reservoir gauges.  Then its literal occurrences could be used to put
`q,T_0,T_1` and `comb,Q_P` in the two returned commuting algebras required by
`separate-native-quarter-carrier-recovery`.

`returned-opposite-cross-zero-has-finite-s6-model` refutes that inference.
Two disjoint Coxeter triples in `S_6` realize both child Whiteheads, their
nonlinear parent fold `(RAF2)`, both returned comparisons `(RAF3)`, and the
nontrivial parent mark.  Three cyclic copies add the complete native `A_2`
orbit.  Every row of the proposed finite window is exact, yet the cross-child
opposite factors have been re-gauged onto disjoint finite payload triples.
Thus this window does not identify a common returned reservoir and cannot
prove the five native placements.

The surviving boundary is strictly stronger than another equality,
conjugacy, Coxeter-order, or zero-product row.  A valid finite-coordinate
argument must lock the relative branch word to one word-visible payload
projection.  By
`relative-gauge-invariance-transfers-cross-orthogonality`, invariance of one
such projection already transfers target cross-orthogonality back to the
source; the exact `S_6` model survives by spending precisely this relative-
gauge defect.  Once a Pauli carrier is exposed, two mixed commutators of that
same relative word with its Pauli generators provide an explicit sufficient
lock.  Those two authenticated rows are absent from the returned window
above.

There is now a whole-inventory strengthening of this fence.
`returned-boolean-coefficient-window-has-finite-marked-model` observes that
every root and relative Weyl word appearing here has coefficient in the
finite Boolean ring `F_2[1,q_0,q_1]`.  Hence adjoining any further genuine
mixed commutator rows among these returned range-idempotent occurrences
still has one simultaneous finite exact marked model.  The missing rows
cannot be found by enlarging the `q_0,q_1,A` return census: a live occurrence
must expose an uncancelled `s_i` or `t_i` Morita arrow, or an equivalent
non-Boolean prefix matrix unit, on the prescribed marked carrier.
