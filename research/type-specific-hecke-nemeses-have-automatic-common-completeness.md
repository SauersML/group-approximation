---
rg: 2
id: type-specific-hecke-nemeses-have-automatic-common-completeness
kind: claim
title: Type-specific subgroup-Hecke nemeses have one automatic common completion
distinct_from:
  common-completion-amalgamates-compatible-bespoke-gadgets: that needs compatible tracial models on the shared packet wires; multiple HNN induction needs no compatibility between the individual extensions and preserves the base mark algebraically.
  subgroup-hecke-hnn-is-a-regular-safe-rank-actuator: that proves the local fixed-space payment for one edge; this records the countable common-completeness consequence needed by the approximation-specific compiler.
  existential-fock-library-has-zero-carrier-or-regular-firewall: that places arbitrarily many orthogonal ranges on one authenticated canonical carrier; these HNN edges impose no common proper-corner identity and reject a type only through its own reverse fixed-space gap.
---

Let `Gamma_0` be a countable base group with a nontrivial marked word `w`.
For every `i`, let `K_i,H_i<=Gamma_0` be finite subgroups and let
`phi_i:K_i->H_i` be injective.  Form the multiple HNN extension

```text
Gamma=<Gamma_0,u_i (i>=0) |
       u_i^(-1) k u_i=phi_i(k), k in K_i>.             (TCH1)
```

Then all type-specific gadgets have one common exact completion: Britton
normal form embeds `Gamma_0` in `Gamma`, so `w!=1` remains true.  More
representation-theoretically, every representation of `Gamma_0` detecting
`w` induces to a representation of `Gamma` whose restriction contains the
original representation.  No pairwise compatibility of the stable-letter
implementations is required.

Raw subgroup averages cannot distinguish canonical base types: in every
canonical trace `tau(e_L)=1/|L|`, so injectivity always points safely.  The
actuator must therefore be localized by a separately authenticated central
packet projection `z_i`, and its stable letter must preserve that packet. Put

```text
p_i=e_(H_i),       q_i=e_(K_i),
Delta_i(rho)=max(0,tr_rho(z_i p_i)-tr_rho(z_i q_i)).   (TCH2)
```

If a neighborhood of that type has `Delta_i>=eta_i>0`, then every extension
through the `i`th HNN cell which preserves `z_i` pays, after exactification of
the fixed finite tables,

```text
|K_i|^(-1) sum_(k in K_i)
 ||U_i^*rho(k)U_i-rho(phi_i(k))||_2^2 >= eta_i-o(1).  (TCH3)
```

with one additional fixed packet-covariance term controlling `[U_i,z_i]`.
Consequently a computably enumerable family of such **filtered** pairs which
gives one positive robust gap on every marked canonical CE extension type is
already a common-complete approximation-specific nemesis compiler.  The
remaining problem is solely the **payload-coupled Hecke separator theorem**:
manufacture `(z_i,K_i,H_i)` from the actual extension type.  Common HALT
completeness does not constrain the different pairs, but authentication and
preservation of each `z_i` are mandatory.

Without `z_i`, the proposal is impossible rather than merely incomplete:
`tau_can(e_H)=1/|H|<=1/|K|=tau_can(e_K)` for every canonical representation.
