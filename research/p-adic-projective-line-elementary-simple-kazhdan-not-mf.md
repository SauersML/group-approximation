---
rg: 2
id: p-adic-projective-line-elementary-simple-kazhdan-not-mf
kind: claim
title: For every prime p and prime power q, EL_N over LC(P^1(Q_p), F_q) ⋊ Γ_p mod centre is an infinite fg simple Kazhdan group with no nontrivial MF quotient
distinct_from:
  free-product-finite-groups-boundary-simple-kazhdan-not-mf: that is a virtually free group on tree ends with trivial edge stabilizers; this is the arithmetic group Γ_p ≤ PSL_2(Z[1/p]) acting by Möbius transformations on P^1(Q_p), where edge stabilizers are infinite.
  boundary-action-elementary-simple-kazhdan-not-mf: that is the free group on its boundary; this replaces it by an S-arithmetic group on the p-adic projective line.
artifacts:
  - research/artifacts/un-boundary-families-2026-09-13-part1.md
---

**ESTABLISHED (unreviewed; proof in `p-adic-projective-line-elementary-simple-kazhdan-not-mf-proof`).**

**Setting.**
- `p` is a prime.
- `Γ_p ≤ PSL_2(Q)` is generated by the images of `e_12(1)`, `e_21(1)` and `d_p = diag(p, p^(-1))`. Recalled, not
  used: `Γ_p = PSL_2(Z[1/p])`.
- `Γ_p` acts on `X = P^1(Q_p)` by Möbius transformations.
- `q` is a prime power, `N >= 3`, and `R = LC(P^1(Q_p), F_q) ⋊ Γ_p`.

**Statement.** `S = EL_N(R)/Z(EL_N(R))` is an infinite, finitely generated, simple group with property (T). Every
homomorphism from `S`, or from `EL_n(R)` for `n >= 2`, to an MF group is trivial.

This gives a family indexed by the prime `p`, the prime power `q` and `N >= 3`. It is the arithmetic mirror of the
Pestov 9.1 group: the minimal `Z`-subshift, which carries an invariant measure, is replaced by `PSL_2(Z[1/p])` on the
boundary of the Bruhat–Tits tree, where `diag(p, p^(-1))` acts with north–south dynamics.

**Review (un-verify-3, 2026-09-13): PASS.** Checked: `d_p^k e_12(1) d_p^(-k) = e_12(p^(2k))`, so `e_12(Z[1/p]) ⊆ Γ_p`; density in `SL_2(Q_p)` and transitivity give minimality; a Möbius map fixing three points is trivial; balls `a + p^mZ_p = e_12(b) d_p^k (p^εZ_p)` give (H-gen); `z ↦ p²z` is north–south with poles `0, ∞`. `research/artifacts/un-review3-2026-09-13-part4.md` §5.
