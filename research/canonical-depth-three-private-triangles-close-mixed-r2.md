---
rg: 2
id: canonical-depth-three-private-triangles-close-mixed-r2
kind: route
title: Splice the two canonical depth-three private R2 triangles into the mixed cell
target: bleak-quick-adjacent-seam-retains-one-mixed-r2-cell
requires: []
---

**INVALIDATED PROPOSAL.**  Use the input `s_i=(i p)` from the first canonical
triangle and the transported arm `t_j=(j q)` from the label-flipped second
triangle in the mixed R2 boundary `t_j s_i t_j r`.

The two chosen swaps are disjoint, so this word reduces in the actual finite
leaf permutation group to `s_i r=(i p)(p q)`, a nontrivial three-cycle.
`mixed-r2-boundary-is-one-finite-mark-centralizer-commutator` records this as
`(MRB7)--(MRB8)`.  Therefore the proposed boundary has no R1--R5 filling at
all.  The live occurrence claim must change the retained chart-one endpoint,
not merely splice the two equivariant private triangles.

