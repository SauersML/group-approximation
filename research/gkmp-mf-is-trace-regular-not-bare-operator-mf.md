---
rg: 2
id: gkmp-mf-is-trace-regular-not-bare-operator-mf
kind: claim
title: GKMP MF is trace-regular strong MF, not the formal bare operator-MF predicate
distinct_from:
  gkmp-amalgam-product-mf-permanence: that imports the product-amalgam theorem and audits its geometric scope; this audits the theorem's stronger MF convention against the repository's formal predicates.
  regular-mf-central-hnn-closure: that gives a research-level central-HNN argument from a regular-character operator-MF realization; this explains why neither that extra realization nor GKMP's stronger hypothesis follows from formal IsOperatorMF.
---

ESTABLISHED (definition and formal-interface audit; literature statement checked
against arXiv:2603.24502v5).

The formal compiler asks for

```text
IsOperatorMF (Carrier input) -> IsOperatorMF (Carrier output).
```

Here `IsOperatorMF` is the bare group-theoretic MF predicate: a faithful
homomorphism into the unitary group of a cofinite norm-matrix corona.  It does
not include normalized-trace convergence or convergence to reduced group
C-star norms.

By contrast, the property called `MF` in Gao--Kunnawalkam Elayavalli--
Manzoor--Patchell has all three approximation clauses:

1. asymptotic multiplicativity in operator norm;
2. normalized traces converging to the regular character; and
3. operator norms of group-ring polynomials converging to their reduced
   group-C-star norms.

The closest existing formal predicate is therefore `IsTraceRegularMF`, not
`IsOperatorMF`.  The repository proves only the downgrade

```text
IsTraceRegularMF G -> IsOperatorMF G
```

for countable `G`; it has no reverse implication.  The representation
variants now also expose the honest structural chain

```text
IsPPF -> IsPFF -> IsTracePMF -> IsTraceRegularMF -> IsOperatorMF.
```

The new `IsTracePMF -> IsTraceRegularMF` step merely forgets exact
multiplicativity while retaining the same trace and reduced-norm limits.  It
does not construct any approximation.  In particular, the current formal
positive switch branch proves no `IsTracePMF`, `IsPFF`, or
`IsTraceRegularMF` statement for its base or for any compiler stage.

This last assertion is a complete declaration-name inventory, not an
inference from filenames.  Searching all Lean sources for the three
predicates and their implication method names yields only:

* the definitions in `Sofic/MFDefinitions.lean` and
  `Sofic/MFRepresentationVariants.lean`;
* eliminations from trace-regular MF to weaker predicates;
* the structural implications `IsPPF.isPFF`, `IsPFF.isTracePMF`, and
  `IsTracePMF.isTraceRegularMF`; and
* negative obstruction fields and contrapositives.

There is no theorem with a concrete group as the conclusion of
`IsTraceRegularMF`, `IsTracePMF`, or `IsPFF`, and none of these predicates
occurs in the Higman compiler files.  Thus the formal implication chain has
no positive source to consume on the compiler branch.

## Consequence for the injected compiler tower

Even before the named Nielsen edges, `InjectedCompilerTower.FreeLetterTower`
starts from `ConjugatedDetector.FreeLetterExtension K`, namely the free
product of `K` with the infinite cyclic group generated by `q`.  This
identification is now a theorem rather than notation:

```text
ConjugatedDetector.freeLetterEquivCoprod :
  FreeLetterExtension K ~=* (K * Multiplicative Z).
```

There is no formal forward free-product permanence theorem for
`IsOperatorMF`, `IsTraceRegularMF`, or `IsTracePMF` in the repository.  For a
generic input to the parametric formal tower, the first tower-level analytic
theorem would therefore be

```text
IsTraceRegularMF K ->
  IsTraceRegularMF (K * Multiplicative Z).              (FL-TR)
```

If one starts from the compiler's currently displayed premise instead, the
even weaker but still absent obligation is

```text
IsOperatorMF K -> IsOperatorMF (K * Multiplicative Z). (FL-OP)
```

These are different targets: proving (FL-OP) would not provide the stronger
input GKMP needs at the outer edge.  Neither statement is needed as a new
paper theorem for the intended Mihailova base, however.  There `K` is a
finite direct product of finite-rank free groups, hence residually finite, so
the trivial subgroup is separable in `K`.  GKMP Theorem 1.1 itself applies
with `H = 1` and auxiliary factor `L = Z`, and produces `K * Z` in the same
MF/PMF/PFF convention supplied for `K`.

Accordingly, the earliest missing obligation in the intended formal positive
branch occurs even before (FL-TR): the repository has no concrete theorem

```text
IsPFF K_e
```

or even `IsTraceRegularMF K_e` for the Mihailova ambient `K_e`, a finite
product of finite-rank free groups.  The research observation that this group
is a RAAG and hence PFF by GKMP Corollary 1.4 has not been imported into Lean.
Once such a base theorem and a formal GKMP product-amalgam interface exist,
the initial free-letter edge is covered; without them, the algebraic
equivalence alone cannot advance the formal positive branch.

### The exact base theorem and why LEF does not prove it

For finite generator types `A` and `X`, the concrete next target can be stated
without mentioning the rest of the compiler:

```text
IsPFF (FreeGroup A x FreeGroup X x FreeGroup X),
```

A still useful weaker target is the same statement with `IsTraceRegularMF`.

The existing formal ingredients stop short of this target.  The repository
proves residual finiteness and LEF for every free group, and point-norm
matricial stability turns an almost representation of a free group into the
genuine representation determined by its generator matrices.  None of these
results proves reduced-norm convergence.  Nor is there a formal PFF/trace-
regular direct-product theorem with the exactness hypothesis needed by the
published tensor argument.

The missing free-group input is precisely the Bordenave--Collins strong-norm
selection statement.  For each finite rank `r`, one needs finite permutation
actions `pi_n : FreeGroup (Fin r) -> Sym(d_n)` whose standard representations
`rho_n` satisfy, simultaneously,

```text
tr_d_n(rho_n(g)) -> 1[g=1]
```

for every group element and

```text
|| sum_g c(g) rho_n(g) || ->
  || sum_g c(g) lambda(g) || in C*_r(FreeGroup (Fin r))
```

for every finitely supported complex coefficient `c`.  The models must have
positive standard dimension and finite image; a countable diagonal selection
then packages them as `PPFApproximation` (hence `PFFApproximation`).

LEF supplies only local injectivity and multiplication.  Its regular
permutation models contain the constant vector, on which a group-ring
polynomial sees the trivial representation; for a nonamenable free group this
can give the full/trivial norm rather than the reduced norm.  Passing to the
standard complement removes that scalar summand, but the required uniform
operator-norm convergence is exactly the random-permutation theorem, not a
consequence of LEF or of point-norm stability.

After the free-group theorem, reaching the actual three-factor `K_e` still
requires either a formal PFF direct-product theorem for exact factors or a
formal import of GKMP's graph-product/RAAG corollary.  Neither exists in the
current Lean tree.

For arbitrary trace-regular inputs that need not be residually finite,
Collins--Male's strong asymptotic freeness theorem for independent Haar
unitaries and deterministic matrices is the relevant primary analytic input
for adjoining a free cyclic generator to an already strongly convergent
matrix model.  It is not presently formalized here.  Applying it to this
interface would still require an explicit countable diagonalization over
group-ring coefficients (and, for `IsTraceRegularMF` rather than genuine
representations, a construction compatible with the approximate group maps).
It therefore identifies a plausible proof engine for (FL-TR), not an existing
formal theorem or a license to assume free-product permanence.

After that initial free-letter edge, the unresolved positive obligations are
strictly stronger than the earlier operator-MF list:

1. supply trace-regular MF for the input and preserve it through the `q`
   free-letter edge;
2. preserve trace-regular MF through every noncentral Nielsen `tau_j` HNN
   edge;
3. preserve trace-regular MF through the `d` free-subgroup HNN edge;
4. preserve trace-regular MF through the `sigma` free-subgroup HNN edge; and
5. prove that the transported detector subgroup is separable in the
   completed inner tower.

Only after 1--5 does GKMP Theorem 1.1 apply to the outer central detector,
with its added factor `Z`.  Its conclusion then downgrades to formal
`IsOperatorMF`.  None of the `ExplicitFreeEdge.Data` fields supplies matrix
models, joint strong convergence, conditional expectations, or profinite
separability: the data record only two injective free-group evaluations and
the induced HNN edge isomorphism.

This route also cannot prove the compiler's displayed `MFPermanence` from its
present bare `IsOperatorMF` premise.  It would require either strengthening
that premise to trace-regular MF (and discharging all stagewise obligations)
or a different theorem that genuinely starts from bare operator MF.

## The central-HNN literature does not remove the mismatch

There is no Lean theorem in this repository that directly preserves
`IsOperatorMF` under the outer central HNN extension.  The node
[[regular-mf-central-hnn-closure]] is a research argument with the additional
hypothesis of a faithful MF-algebra realization whose trace is the regular
character.  Bare `IsOperatorMF` does not provide that datum.

Shulman's central-HNN theorem (arXiv:2603.13564v2, Theorem 25) is a theorem
about the universal C-star HNN extension of an MF C-star algebra.  Applying it
to the abstract group still requires a faithful group embedding into that
C-star HNN algebra; the regular-character/Britton argument is exactly the
extra bridge used by the research node above.  No such bridge is formalized
for the compiler tower.

Nor do the two later HNN papers give bare-MF permanence.  arXiv:2604.06982
proves selflessness statements for reduced C-star HNN extensions, not MF
closure.  arXiv:2604.14106 transports strong convergence only from already
jointly compatible base, subgroup-expectation, and edge-isomorphism data.
Those inputs do not follow from `IsOperatorMF` or `ExplicitFreeEdge.Data`.

Thus no generic HNN MF permanence claim is justified edge by edge, and GKMP
is useful only after a still-missing trace-regular inner-tower construction
and transported-subgroup separability proof.
