---
rg: 2
id: whole-map-coarsening-proof
kind: route
title: Direct proof of Equivariant transitions coarsen to whole-map PVMs
target: whole-map-coarsening
requires: []
artifacts:
  - notes/TRUE_EQUIVARIANT_TRANSITIONS_COARSEN_TO_WHOLE_MAPS.md
---

# Direct proof of Equivariant transitions coarsen to whole-map PVMs

Direct establishment: the complete argument is recorded in the
artifact(s) above (kernel-checked where the artifact is a Lean
module; source-verified prose otherwise). This route asserts the
claim `whole-map-coarsening` outright — `requires: []` is a proof commitment,
not a placeholder.
