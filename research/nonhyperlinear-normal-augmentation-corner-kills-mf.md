---
rg: 2
id: nonhyperlinear-normal-augmentation-corner-kills-mf
kind: claim
title: A no-CE algebra in a finite-normal augmentation corner rules out weak MF
distinct_from:
  nonce-bcs-unital-algebraic-corner-map-forces-nonhyperlinear: that allows an arbitrary nonzero group-ring projection and uses the canonical group trace to rule out hyperlinearity; this uses the authenticated augmentation-complement projection of a finite normal subgroup and intrinsic rank normalization to rule out even a weak norm-corona embedding.
  perfect-lcs-gap-implies-weak-non-mf: that uses the negative sector of one central involution and the LCS solution-group correspondence; this accepts any finite no-CE constraint algebra and any finite normal subgroup, without requiring an LCS or a central cyclic mark.
  finite-normal-type-corner-is-twisted-group-algebra: that computes one invariant irreducible-type corner, which a faithful representation may omit; this uses the sum of every nontrivial type, which no faithful representation of the finite subgroup can omit.
  mf-finite-normal-tracially-visible: that produces an elementwise trace gap from a finite normal subgroup; this promotes an entire finite algebraic corner diagram to a Connes-embeddable tracial state.
artifacts:
  - research/nonhyperlinear-normal-augmentation-corner-kills-mf-proof.md
---

Let `B` be a finitely presented unital star algebra generated by uniformly
bounded elements (in particular, a finite BCS algebra), and assume `B` has no
Connes-embeddable tracial state.  Let `G` be a countable group with a
nontrivial finite normal subgroup `K`, and put

```text
e_K=(1/|K|) sum_(k in K) k,          q_K=1-e_K in C[K].       (NAC1)
```

If there is a unital star homomorphism

```text
Phi:B -> q_K C[G] q_K,               Phi(1_B)=q_K,            (NAC2)
```

then `G` is not MF even in the weak operator-norm sense

```text
G -> U(product_n M_(d_n) / directSum_(||.||) M_(d_n)).       (NAC3)
```

The proof uses no Property `(T)`, Kazhdan pair, stability theorem, expansion,
or implication from weak MF to hyperlinearity.  The finite normal subgroup is
used only to make `q_K` a central projection, and the special choice
`q_K=1-e_K` authenticates the corner: every faithful representation of `G`
must see it.  The rank of its matrix lifts may tend to zero relative to the
ambient dimension; normalizing by the corner rank itself removes that escape.

Thus a property-`(T)`-free norm-corona groupification of the fixed no-CE BCS
does not need an LCS compiler.  It is enough to construct the exact finite
algebraic diagram `(NAC2)`.  Conversely, replacing `q_K` by one selected
irreducible-type projection is unsafe: a faithful representation of `K` may
omit that type entirely.  The augmentation complement is the canonical
finite-normal projection for which faithfulness alone supplies nonvanishing.

