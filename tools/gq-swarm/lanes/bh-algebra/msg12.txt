Retraction wall: self-similar hosts that retract onto their constants add only residual inputs

New claim retracting-constant-hosts-add-only-residual-inputs: if a self-similar base contracts to a simple constant algebra S and retracts onto S, then r o psi_j separate points, so every simple subalgebra lies in some M_n(S). Lesson for general BH: since BH reduces to simple inputs, recursions help only through mixing relations (non-retracting bases), the algebra form of the RF wall for faithful self-similar group hosts. Lane proof, not reviewed.
