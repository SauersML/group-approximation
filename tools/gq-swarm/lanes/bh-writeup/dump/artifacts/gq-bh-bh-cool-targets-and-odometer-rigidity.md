# bh-cool: which "striking explicit" results are within reach, and one obstruction (lane bh-cool, 2026-09-18)

The lane was asked for the most striking explicit result in reach, with priority checked. This records what was
checked, what turned out to be known already, and what was landed.

## 1. Candidates checked and found already known (do not redo)

| Candidate | Status | Source |
|---|---|---|
| One f.p. simple group containing every countable abelian group | **Known** | Belk-Hyde-Matucci, *Finite germ extensions*, arXiv:2407.03149 (abstract: "a specific 2-generated, F_∞ simple group VA ... contains every countable abelian group") |
| Brin-Thompson `2V` contains every countable (virtually) abelian group | **On main** | `countable-virtually-abelian-groups-embed-in-2v` (from Kojima-Sheng's `Q <= 2V`, arXiv:2603.18410v3, Thm 1.2) |
| `V` contains every countable locally finite group | **Known** | BBMZ survey arXiv:2306.16356v3, Theorem 4.3(3), cited for Thm 5.1(11) |
| Every f.g. RAAG, and so every finite extension of a special group, embeds in some `nV` | **Known** | Belk-Bleak-Matucci, arXiv:1602.08635 (abstract) |
| Every Baumslag-Solitar group satisfies BH | **Known** | Bux-Llosa Isenrich-Wu, arXiv:2408.05673 (`baumslag-solitar-and-free-by-cyclic-satisfy-boone-higman`) |
| An explicit f.p. group containing `Q` (Kourovka 14.10(a)) | **Known** | Belk-Hyde-Matucci, arXiv:2005.02036; archived as solved in Kourovka v46 |
| One f.p. group with solvable WP containing every f.p. group with solvable WP | **Impossible** | Boone-Rogers (no uniform algorithm), recalled from memory, not re-read. So universal hosts exist at most for uniformly decidable families. |

Named items read from BBMZ v3 (WebFetch of the arXiv HTML):
- Problem 5.3 (1)-(13): braid groups; mapping class groups; Aut(F_n) and Out(F_n); non-solvable BS(m,n), e.g.
  BS(2,3); GL_n(Q); free Burnside groups; f.p. metabelian groups; free-by-cyclic groups; torsion-free one-relator
  groups; CAT(0) groups; automatic groups; f.p. RF groups; Artin groups with solvable WP.
- Question 4.7 (one-ended hyperbolic groups, e.g. closed surface groups, in `V`).
- Question 5.6 (every f.p. group inside a type F_∞ group?).

These belong to the class lanes (bh-mcg, bh-outfn, bh-one-relator, bh-cat0, and so on). None of them was decided here.

## 2. Candidates judged out of reach in one attempt

- **One f.p. simple group containing every BS(m,n) at once.**
  - Affine and odometer hosts see only finitely many primes (`affine-2v-hosts-reach-z-1-2n-by-doubling-but-not-q`).
    §3 adds that odometer generators cannot even carry `BS(2,3)` unless the odometer is sensitive to both 2 and 3.
  - Tree methods (BLIW) are per group. A f.g. GBS host has a f.g. modulus image, so it cannot contain all
    `BS(m,n)`.
- **H_3(Z) in some nV** (`heisenberg-group-embeds-in-some-brin-thompson-group`). This is an active program with
  landed obstructions for orbit growth up to `r^3`. The first open case is `W(Z^4)` or product-shift full groups.
  Not duplicated here.
- **BS(1,3) in 2V (removing the free coordinate from `bs13-embeds-in-brin-thompson-3v`).** The construction needs
  `s ≅ s ⊔ s ⊔ s` as systems. A minimal `s` (SMART's first-return map) cannot give that inside `C x C` without an
  extra product factor. No brick-local folding of the third coordinate into the tape was found.

## 3. Landed: odometer rigidity for Baumslag-Solitar relations

`odometer-generators-carry-no-nonsolvable-baumslag-solitar` (+ `-proof`, elementary lane proof). Take `a ≅ (+1) x id`
on `K x Y`, with `K` compact monothetic, and suppose `t a^m t^-1 = a^n`.
- If `nK = K`, then `t a t^-1` commutes with `a`.
- If `mK = K`, then `t^-1 a t` commutes with `a`.
- So for `|m|, |n| >= 2`, the map from `BS(m,n)` is not injective unless both `m` and `n` are non-invertible on `K`.

The key step is that the centralizer of the minimal rotation power `a^n = (+n) x id` consists of fibrewise
translations `(x, y) -> (x + delta(y), h(y))`, and these commute with `a`.

**How it was found.** In the affine host `2V_{BS(1,3)}` (generated by `2V`, `x -> x+1` and `x -> 3x` on the first
coordinate `Z_2`), `t = mu rho` satisfies `t a^2 t^-1 = a^3`. Here `a = x+1`, `rho` is the baker map and `mu = x -> 3x`,
using `rho a^2 rho^-1 = a` and `mu a mu^-1 = a^3`. Every twist `t = mu rho c` with `c` in the centralizer of `a^2`
also works. By the lemma, `t a t^-1` always commutes with `a`. Through the non-Hopfian map `a -> a^2` of `BS(2,3)`,
the kernel then contains `ker(phi^k)` for every `k` (remark in the route). So none of these hosts gives an explicit
faithful `BS(2,3)` through `x + 1`.

**What it tells a construction.** An explicit Thompson-like host for `BS(2,3)` needs a generator that is not a
2-adic odometer, e.g. a `Z_6` or `Z-hat` odometer together with the renormalizations dividing by 2 and by 3. Or it
needs a generator with non-equicontinuous (tree-boundary) dynamics, as in BLIW.

## 4. Honest verdict

No new "striking" theorem was found within one attempt. Every candidate from the directive was either already known
(§1) or blocked (§2). The landed lemma is a modest, checkable obstruction. Its use is to steer explicit-host
constructions away from odometer generators for non-solvable Baumslag-Solitar groups. Priority: not claimed.

## 5. Free-mode pass (after the creativity order): three designs for explicit non-solvable Baumslag-Solitar hosts

**5a. Rational-base (Katsura) odometers: landed, negative.** The natural design is to replace the binary odometer of
`2V_τ` by the Katsura "add one in rational base `n/m`" map `σ_{m,n}` (Exel-Pardo `KatsuraExample`, `A = (m)`,
`B = (n)`). Then `σ^m(jξ) = jσ^n(ξ)`, so the baker map conjugates `(σ × id)^m` to `(σ × id)^n`: the `BS(m,n)` relation
holds on the nose. But for coprime `m, n` the action is level-transitive, so `σ_{m,n}` is just the `m`-adic odometer in
disguise.
- The strengthened rigidity lemma (item 4: the image is abelian-by-cyclic for every `t`) then shows that this design,
  or any twist of the conjugator, gives only the metabelian shadow `Z[1/mn] ⋊_{n/m} Z` of `BS(m,n)`.
- Nodes: `katsura-one-vertex-actions-are-odometers` (+ `-proof`), and item 4 of
  `odometer-generators-carry-no-nonsolvable-baumslag-solitar`.

**5b. Root amalgams in plain Brin-Thompson groups: attempted, obstruction recorded (not a node).**
- *Design.* `BS(m, mk) = <a> *_{a^m = s} BS(1,k)`, e.g. `BS(3,9) = Z *_{3Z} BS(1,3)`, or `BS(2,6)`, which is
  non-Hopfian. With SMART's `BS(1,3) = <s, u> ≤ 3V` (`bs13-embeds-in-brin-thompson-3v`), `s = U × id` has cube roots in
  `3V` (register roots), and adjoining a root `a` in a fresh coordinate is easy.
- *Target.* Faithfulness via amalgam ping-pong needs sets `X_A`, `X_B` with `(B \ <s>) X_A ⊆ X_B`, disjoint from `X_A`.
- *Obstruction (proved here by direct computation, for the SMART realization).* Write the third coordinate in the
  complete code `c_0, c_1, c_2` as a ternary register. By induction on `j`, the root `s^{3^-j} = u^-j s u^j` sends
  `(x, c_{i_1} … c_{i_j} y)` to `(x, c_{i_1} … c_{i_j + 1} y)` whenever `i_j < 2`. So for every nonempty open
  `U ⊆ C^3` and all large `j`, `s^{3^-j} U ∩ U ≠ ∅`. No ping-pong set with interior exists for this realization.
- *Consequence.* The naive root-in-a-new-coordinate construction only gives the induced group, inside `B ≀ Z/m`,
  which is residually finite and so cannot contain `BS(m, mk)`.
- *What a repair needs.* A realization of `BS(1,k)` in some `nV` with an open "coset piece": a clopen `D` with
  `s D = D` and `b D ∩ D = ∅` for all `b ∉ <s>`. As computed here, this forces pairwise disjoint `s`-invariant clopen
  sets `F_j = u^j`-images carrying towers of height `k^j`, arranged like the induced action `B ×_{<s>} D`. So `s` needs
  clopen cycles of unbounded period accumulating somewhere. That is the precise open design question.

**5c. The faithful model is the two-vertex Bass-Serre graph (not landed; known territory).** `BS(m,n)` acting on the
ends of its tree is an Exel-Pardo self-similar action of `Z` on a two-vertex graph.
- The vertices are the arrival types "via an `m`-type edge" and "via an `n`-type edge". The cocycle multiplies by
  `n/m` on one type and `m/n` on the other. The stable letter is a path-shift bisection.
- Its full group contains `BS(m,n)` faithfully, since the tree action is faithful. This is the tree method of
  Bux-Llosa Isenrich-Wu (and Spielberg's path-category models of Baumslag-Solitar groups), so no priority is claimed
  and no node is landed.

**Verdict.** No striking new theorem after two real attempts. The landed content fully explains why rational-base
odometer hosts fail and why the plain-`nV` root-amalgam route needs a new `BS(1,k)` realization with an open coset
piece.
