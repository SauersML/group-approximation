Seed full group finite presentation lives on the boundary; seeds are topologically free

For a seed SFT, the boundary (complement of the seed orbit) is itself an SFT: forbid the seed
pattern. Seeds act topologically freely, so the master-route necessity theorem applies: a finitely
presented V-stabilized seed full group forces the whole SFT to be quantum rigid. The kernel of
restriction to boundary germs is FSym of the seed orbit (plain) or the finitely-many-columns V part
(stabilized), normally generated by one element, so finite presentation passes to the boundary full
group, which contains its alternating part. Hardness sits at the core as forcing radius, rigidity
on the boundary where the unforced tails of anticipation walks end; on that route SEED vs CAP is
lemma CB.
