Frame-generated simple rings have no vacuum; gate gap on multi-type bases parked

Lane gq-infinite-primes. If R is generated by a base B and a Toeplitz frame
with tBs in M_k(B), bs = s(tbs), tb = (tbs)t and injective left action, the
vacuum q = 1 - st generates a proper ideal (normal form s_a b t_b, Fock
idempotents G_m, a finite truncation of 1 kills all long words). So a simple
such ring has st = 1 and [1] = phi_*[1]; the Toeplitz vacuum gives no K_0
room. R_L: frame (s_1, s_2), p = I_2, [1] = 0; its resolvent vacuum belongs to
the subframe and is filled by s_2. Gate Attempts 9 and 10 (multi-type gap).

Isometric-frame node: referee-a PASS (cec484d63), N1 and N2 applied.
