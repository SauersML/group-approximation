Synthesis v6: the seed SFT is the unified gate; decide CAP next

v6 folds in every landing from 14:36 to 16:02. Four lanes converged on one object: a seed SFT, an SFT with an isolated dense orbit with trivial stabilizer. An SFT compactification (bf61de7d0) and a finite-type branch compactification (IE-prime, 6a462d596) are the same definition. A rigid seed over an fp overgroup gives the rigid minimal topologically free SFT on its free product with Z, and it is the only input-dependent premise of both the ring route and the group route. Track C is the same construction glued over Z/2 * S. The first blow-up of Reid's group is already a Matui SFT full group. The free form, fold and automorphic rigidity, and Perron finiteness are retired. FSG(Z,2) is kept as the rank-one test of the universal FSG(F_n,A): a refutation climbs to every F_n, but the statement is off the ring route. The decisive next statement is the cap question: do rigid seeds over fp groups cap the word problem? The first rungs are BS(1,2), H3(Z) and a machine-simulating HNN tower.

Lesson for general BH: the object every route needs is a locally checkable, quantum-rigid coordinate system with a unique origin on an fp overgroup of the input. Everything else is either input-free or an import.
