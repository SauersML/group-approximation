undef $/; $_ = <STDIN>;
s/- `R_T` \(`q-embeds-in-a-corner-of-a-fp-toeplitz-ring`\), in its concrete model, which is a\n  `Q`-algebra:/- `R_Toep`, the Toeplitz resolvent ring of `q-embeds-in-a-corner-of-a-fp-toeplitz-ring`, in\n  its concrete model, which is a `Q`-algebra. It is not the SL_3 Toeplitz harmonic ring\n  `R_T` on main, which is tracial and gets `Q` by harmonic elimination, not from a vacuum:/;
print;
