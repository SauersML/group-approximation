undef $/; $_ = <STDIN>;
s/`NK\(R_0, φ\)_± = NK\(D, φ̂\)_±`, so by Lemma 7\.2 it suffices that `D` is regular\nsupercoherent\./`NK(R_0, φ)_± = NK(D, φ̂)_±`: `S` is `H'`-unital, being a colimit of unital `Q`-algebras,\nhence `H`-unital and `ℤ`-flat (ABC Remark 2.2), and `Q` is regular supercoherent. So by\nLemma 7.2 it suffices that `D` is regular supercoherent./;
s/These are flat by part 4 of that node\./These are flat by part 4 of that node, which uses the point indicators\n    `δ_n ∈ B⁺` (the Setting)./;
print;
