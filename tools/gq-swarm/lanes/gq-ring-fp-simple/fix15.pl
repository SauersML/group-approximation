undef $/; $_ = <STDIN>;
s/  - The spine maps `φ` are untwisted corner embeddings\. Over the image corner, the\n    remaining blocks are standard column modules, which are finitely generated projective\./  - The spine maps `φ` are untwisted corner embeddings. Over the image corner\n    `e = φ(1)`, the remaining blocks are standard column modules, which are finitely\n    generated projective. On the `(1−e)` part, the unitized transition\n    `(a, λ) ↦ (φ(a) + λ(1−e), λ)` acts only through the `Q` factor, so it is flat as well\n    (referee a)./;
print;
