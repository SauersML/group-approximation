undef $/; $_ = <STDIN>;
s/- a \*\*spine\*\*: for every type `ρ`, a letter `x_ρ` with range `ρ` and full availability\n  `t_\(x_ρ\) s_\(x_ρ\) = p_ρ`\. The `R_L`-type and `R_cap`-type designs have one\./- a **spine**: for every type `ρ`, a letter `x_ρ` with range `ρ` and full availability\n  `t_(x_ρ) s_(x_ρ) = p_ρ`;\n- **point indicators**: the coefficient ring contains every point indicator `δ_n`, i.e.\n  contains `J = ⊕_ℕ Q` at each type (referee b, A1). This holds when some availability\n  indicator such as `[N = 0]` is present and letters shift `N` in both directions, as in\n  `R_cap`. It fails for `R_L`, whose coefficient ring `Q[N][(N+c)^(-1)]` has no\n  idempotents. There the reset `N ↦ 0` is not flat, and (P) for `R_L` stays open./;
s/`R` is a `Q`-algebra\./`R` is a `Q`-algebra./;
s/\*\*Consequence\.\*\* Within their stated settings, and provided those have a spine,/**Consequence.** Within their stated settings, and provided those have a spine and a\ncoefficient ring containing all point indicators,/;
s/They are repaired here following the referees' suggestions\. Re-review: PASS by\n`gq-referee-a` \(035fee55a\); `gq-referee-b` pending\./They are repaired here following the referees' suggestions. Re-review: PASS by\n`gq-referee-a` (035fee55a) and by `gq-referee-b` (852a0771f,\n`gq-referee-b-register-completion-rereview.md`), the latter with the scope amendment A1,\nnow made: point indicators are required./;
s/(  - research\/artifacts\/gq-referee-b-register-completion-pimsner-from-supercoherence\.md\n)/$1  - research\/artifacts\/gq-referee-b-register-completion-rereview.md\n/;
print;
