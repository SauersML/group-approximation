Cairn fix graph: one fix node per red module of the trusted main build 3c8b417428

32 route nodes research/fix-*.md (27 BooneHigman/Metabelian, 5 SimpleKazhdanSofic), each with the verbatim first errors, the owner lane, and requires-edges to fix nodes of red imports (none: every red module in that build fails on its own errors). Status lines flip to RESOLVED when a trusted probe builds the module green.
