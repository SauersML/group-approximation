# ms-green-nm
Direction: make the non-MF manuscript's endpoint closure (every module holding a declaration named in metadata/NON_MF_SENTENCE_CENSUS.tsv) build GREEN on current main, via gqprobe-lean.sh on MSI; minimal fixes; 2-hour rule for the other session's NM/GL files.
- 09-18 ~14:50: census decls 1741 → 710 candidate module files (short-name grep, over-approximate) → 215 maximal modules under imports (closure ≈ 4150 modules). Lists: $GQ/work/ms-green-nm/{modfiles,maximal}.txt.
- Probe 1 (origin/main as is, 215 maximal targets): queued behind the team-lead calibration probe.
- 14:55: refined by namespace → 486 module files, 110 maximal targets (closure ≈ 3358 modules; no BH/SK modules). None of the 486 was changed on main in the last 3 h. Probe 1 queued (task bj4tp6tlt) behind bh-pal-integrate.
- 15:30: static facts. All 3358 closure modules are inside the root import closure (8047 modules), so the default root build covers them. No code-level sorry/admit/axiom in the closure. The closure has no BooneHigman or SimpleKazhdanSofic modules, so none of the 30 red modules bh-pal-wire found at 10:40 is in it. The last change to any closure file is af2f33982 (09-18 00:04 UTC); the other session's live NM/GL work (NMWire2, GL03BPinch, TorsionFreeEndpoints) is outside the census closure.
- 15:27: the MSI VPN dropped (DNS failure at 15:26, since recovered); the probes of ms-nm-uncond-b and ms-green-sk failed to connect. Probe 1 is still queued on the lock.
- 15:52: coordinator stopped probe 1 (old v1 path). Re-queued the same 110-target probe on v2 (Slurm msismall, checksum sync), task bsjadrs1k.
- 16:42: coordinator stopped the v3 waiter. Re-queued on v4 (batched Slurm) as 3 requests of 37/37/36 targets (work/ms-green-nm/part.aa/ab/ac), tasks b63kx4asv, bqfo5hr90, bcn3zjr7p.
- 19:40: after the restart, the coordinator cleared the queue (the 16:42 requests got DEFERRED) and landed all lane files on main (8d6acdcd0c, a2646f3723; tip e6c93a1fe4). None of the 3358 closure files, the census or the root changed since base 7054f9da2, so the targets are unchanged. Re-probed the 3 parts with an empty overlay: tasks bwh5h31nz, b0tra7ubi, bpt14joor. Red goes to board/FIX-GRAPH.md and is landed via gqland-lean.sh; it is never wired into root.
- 20:08: still queued. The builder batch in flight (Slurm 1338076, since 19:40) is 2 ms-nm-uncond requests drained before mine arrived; my 3 requests are next.
- ~21:00: SLURM-BLOCKED (group association counter underflow; admin reset needed). My 3 requests never ran, so I have NO error index of my own. Existing evidence: (a) the trusted 3c8b417428 build (BH chain + Palomar targets, not root) has 32 red files, none in my closure; (b) no fleet probe log or pq result names an error in my closure. The in-flight batch Slurm 1338076 (ms-nm-uncond-a/b, 8 targets, submitted 19:40 before the block) covers 2738/3358 of my closure modules, so its verdict is my next error index. Nothing to fix yet and no FIX-GRAPH rows. Will re-probe once, when the flag is gone.
- 21:13 PARTIAL VERDICT from batch Slurm 1338076 (base e6c93a1fe4 = current main, empty overlays, request 1 = ms-nm-uncond-a, TIMEOUT at 90 min). Lake log gq-slurm/batch-b1789778448-1.log has 0 `error:` lines. 1303 modules completed (Built/Replayed), all inside my closure: list in work/ms-green-nm/certgreen.txt. The 2055 still unbuilt (work/ms-green-nm/unbuilt.txt) include all 110 maximal targets. So: red found = 0, fixes = 0, landed = none, FIX-GRAPH rows = none.
- 21:13: my 3 requests were taken into builder 70253's batch of 10 while SLURM-BLOCKED is set, so I expect DEFERRED. I will re-probe once when the flag is gone; the completed oleans stay warm.
- 21:15: all 3 requests came back DEFERRED as expected (sbatch AssocMaxSubmitJobLimit). Watcher bsuyp8aef waits for SLURM-BLOCKED to clear; then I re-probe once (3 parts, empty overlay).
