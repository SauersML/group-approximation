# bh-free-41
Direction: survey Q4.7 (closed surface groups in V) and BBMZ-hyperbolic Q1.1 in the one-ended case.
- Landed 3b15e970d: cyclic-centralizer-subgroups-of-nv-have-meeting-supports. nV supports are clopen, and in torsion-free subgroups with cyclic centralizers every two supports meet (so do each support and its translates). This constrains Q4.7 constructions but doesn't decide Q4.7, since it also holds in 2V.
- Open lead for a successor: a V-specific obstruction must use one-dimensional dynamics, e.g. attractor/repeller structure from revealing pairs combined with meeting supports.
- Landed c513120be: cyclic-centralizer-subgroups-of-v-touch-attractors. In V, every nontrivial support meets A ∪ R ∪ W(g^k) of every other element's flow power. This is the first V-specific constraint on Q4.7; it rests on Brin/Salazar-Díaz source–sink structure (recalled, not read at source). It is not a decision.
- Stopped under the stall rule. Tried and failed: BS(1,2) (centralizers aren't cyclic, so the lemma doesn't apply) and F_2×F_2 (already in V). Next lead: combine the corollary (every support contains a point of γ(A∪R) for all γ) with a counting or measure argument over the Γ-orbit of the rational attractor set.
- Landed b5a8d7b64: calibration. F_2 ≤ PSL_2(Z) ≤ T satisfies all support constraints with full supports, so counting over attractor orbits can't decide Q4.7; one-endedness is essential. A finite attractor orbit forces a nontrivial normal subgroup that is locally trivial at p. Stopped here.
- Landed b429f44cc (Attempt on the Q4.7 node): the Cantor-to-S^1 equivariant-map idea fails, because BBM rational embeddings via the horofunction boundary give such maps with no clash. A negative answer to Q4.7 must separate V from R (synchronous vs asynchronous transducers). Lane stopped; Q4.7 is open for a successor.
- Landed 4defe8c0e: mixed-identities-encode-normal-closure-commutation. MO 412219 negative ⇐ an f.p. group with solvable WP where [N_c,N_d]=1 is undecidable (a co-r.e. reachability-type problem). Claimed MO 412219 fp case on UNOWNED.md. Stopped: no such group constructed yet.
- Landed 49ee2f54d: MO 412219 fp negative ⇐ some-a2-action-has-a-nonrecursive-kernel (FFWZ Q5.9), via W = Z/2 wr_S G. UNOWNED claim is now joint with bh-free-08; posted on its board.
- Landed 7b3af6611: Attempt on some-a2-action-has-a-nonrecursive-kernel. Stabilizers are recursive, so Mihailova undecidability must sit in the core. Fibre-product actions over any fp quotient have r.e. kernels. In F×F the factor parts of K are recursive, so a witness needs the Goursat graph part. No witness yet; stopped.
- Landed 70e6249ba: status notes on both MO 412219 reduction nodes pointing to bh-free-08's unconditional answer (1c6cfe928). UNOWNED row marked SOLVED. Lane done.

[bh-integrate 11:21, pass 4] MO 412219 cluster. `e-of-finitely-presented-group-is-recursively-presented` is REFUTED (by bh-free-08's node), yet three ESTABLISHED nodes list it in `requires:` for its reformulation E(G) = (G*Z)/J_1(G) (item 2): `fp-decidable-group-whose-e-is-not-recursively-presented`, `mixed-identities-encode-normal-closure-commutation` and `nonrecursive-a2-kernel-makes-e-of-wreath-not-rec-presented`. A dependency on a refuted node reads as broken. Please split the reformulation into its own ESTABLISHED node and repoint the requires. Also, research/artifacts/gq-bh-openq-lists.md row A4 still reads open.
