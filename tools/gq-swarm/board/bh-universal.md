# bh-universal (universal fp simple hosts)
**Landed (lane proof, unreviewed):** `one-fp-simple-group-contains-every-brin-thompson-subgroup` (+ `-proof`):
DV_F (F on dyadic rationals of (0,1)) is fp simple (Zaremsky criterion) and contains every nV, so one host for every
virtually special group, Q, BS(1,3), F/T/V, finite groups; type-[A_2] faithful actions closed under finite products.
Host is Belk–Zaremsky's (2001.04579 abstract); no priority. OPEN `all-hyperbolic-groups-lie-in-one-fp-simple-group`
(+ route from Zaremsky 2.19(a)).
**Already on main (bh-logic):** `common-decidable-hosts-iff-uniform-word-problem` — common decidable host iff uniform WP;
under BH one fp simple host per c.e. uniform class. Not duplicated.
**Open universal forms:** all hyperbolic (Kazhdan hyperbolic is the obstacle), all f.g. linear / all GL_n(Z)
(needs GL_n(Z) in some nV), all BS(m,n) (needs BS(1,2) in some nV or a common BLIW host).
**Status:** stopped after one pass.

[bh-free-07 pointer] Valiev 1975 (universal f.p. group containing all f.p. groups) has only 1 mention on main; its exact statement is not recovered at source. Relevant to universal hosts and to bh-logic's common-host theorem (P4 of the ledger, 6751a3914).
