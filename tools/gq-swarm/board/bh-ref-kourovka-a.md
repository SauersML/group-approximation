# bh-ref-kourovka-a (referee)

Direction: adversarially referee the lane proofs of Kourovka 17.60 and 21.73.

- 09-18: 5821ec240e landed Referee sections on both nodes.
  - `ct-p-z-groups-are-pairwise-nonisomorphic` (17.60): **PASS**, with three presentation gaps:
    Hausdorff and second countability for infinite P; the germ of W_V being independent of V;
    the shell quotient read at the germ level.
    - Matui 1210.5800 citations checked at source (TeX at src/bh-ref-kourovka-a/).
    - The Rubin route (BEM 2203.05930) gives Step 1 without Matui.
  - `ct-z-conjugacy-problem-is-undecidable` (21.73): **PASS**, conditional on (H), which is
    standard (Morita 1996) but not read at source. One scope gap: the step-counter normal form
    adds a counter, so as written the explicit host is CT_{3,5,7,11,13,17}(Z). The
    {3,5,7,11,13} claim needs (H) for 2-RCMs from an initial configuration.
- **Priority** (arXiv API, "class transpositions"): no paper on 17.60 or 21.73.
  - FYI for the Kourovka lanes: Morrison arXiv:2607.17477 claims 8 Kourovka problems, including
    18.50 (the group generated by class transpositions of moduli ≤ k).
  - Pan arXiv:2604.12553 proves CT_(n) ≅ S_N (a Bardakov–Iskra conjecture in the 2026 issue).
- **Not reviewed:** the second 17.60 proof (gq-bh-bh-free-11-ct-p-z-second-proof.md), and the
  groupoid route to item 3 of `ct-p-z-is-a-one-vertex-k-graph-full-group`. I read the elementary
  route (0e0c97d15) through and found no gap.
- Status: done; idle.
- 09-18, E2 batch: cb53a036f4 landed Referee sections. **All four PASS.**
  - Path fold (3c8b41742): the commutation-transport induction is exact.
  - Extension localization (4a734659e): exact, in any dimension.
  - Tree-product transplant (e5b3fdf2e): the `H_i(n)` induction, with `u_i >= 1` used in Step 3.
  - Ã₂ transplant (5f79e3749): the cylinder induction and the Determination Lemma are correct.
    Two minor gaps: (1) the metric convention `d_Γ <= d_Δ`; (2) Step 6 (Q3) needs `M(N²) ⊆ C̄`, or
    else `z_* = z_p − N u_*` with a bigger constant.
- Status: done; idle.
- 09-18, batch 3:
  - ccf2e865a9: fixed the two Ã₂ gaps myself (the S_4 metric convention; the Step 6 base shift `z_* = z_p − N_0 u_*` with threshold `κ + D_Y`).
  - e515e56c36: four more PASS.
    - two-sided faults: the corollary needs `R(D) > 4D`, which holds;
    - seeded transport;
    - horizontal finiteness: blocks must carry parent cones, and the degree-0 clause is missing;
    - tree end shifts: `gt` should be `gt^(-1)` in §7.
  - c626199291: transplant minimality (b12f837af) PASS for the transfer. The F_m×F_n free minimal rigid SFT holds iff the fibre `Ω_min` (A4) does; Ã2 still needs (GP).
- Status: done; idle.
- 09-18, seed batch: 3ff9b63b29.
  - real-cut BG seed (1da44c879): PASS;
  - order seeds (3624d517a): PASS; Dubrovina–Dubrovin density unchecked;
  - modular reader (5a72aa6a9): parts 1–2 PASS. Part 3 needs its rules conditioned on the pointer orientation: at α ≡ 0 mod m in l^(-1)-child cosets the l-edge goes to the parent, off row 0. The claim holds on the sink coset. Part 4 OPEN.
  - d15e53b53 left to bh-ref-misc (PASS, d1a5012563).
- Status: done; idle.
