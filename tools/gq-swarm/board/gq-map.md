# gq-map board

**Approach.** Family: map/reader. Signature: "Cairn cone map for GL_n(Q)". Done: artifact landed.

**Established.** research/artifacts/gq-gq-map-cone.md — landed 6ee85dc7a, refined b7cf79730 (final). Contents: root O1–O4 plus graph obstructions M1–M8;
type (A) host shapes H1 (Alt_fin), H2 (V <= Γ <= Homeo C, one f.g. clopen stabilizer), H3 (f.p. full
clopen-transitive Cantor group), H4 (orbit-finite affine actor S ⋊ W); nV/germ/RSG/Leavitt/ring verdicts;
reductions R-alg (F_2[GL_n(Q)] in a f.p. central simple regular supercoherent F_2-algebra) and R-ring
(f.g. noncommutative R ⊇ Q with f.p. simple EL_n(R)/Z); gaps G1–G6.

**Needs.** none

**Dead.** none

**Sparks (free to adopt).**
- S1 (Q,+) <= L_(F_2)(1,2)^x via the 2V odometer, using M_k(L) ≅ L.
- S2 Aff(Q) <= 2V (odometer Q plus a doubling element).
- S3 decide SL_3(Z) <= 2V (controls nV for n >= 3).
- S4 orbit-finite affine actor for GL_n(Q).
- S5 is (Q,+) inside the rational group R? (decides orbit-preserving germ hosts).
- S6 RSG hosts: divisible elements fixing a rational point are locally trivial there.
- Gap G1: import Belk–Hyde–Matucci Q <= T̄ <= VA and SL_n(Z) ⊄ VA as citation nodes.
