# Lane ct-bilateral-mf: prop:bilateral-three (tex 1541–1607)

Printed item: `non_mf_groups_exist.tex` Proposition `prop:bilateral-three` (tex 1541–1547), its proof
(tex 1549–1603), the remark after it (tex 1605–1607), and the Introduction sentence "R_X is directly finite
exactly when GL_3(R_X) is MF".  Added to the manuscript by 73a84cd9c, after the chain-* plans were scoped.

## Modules

1. `GroupApproximation/Manuscript/ChainCore/BilateralThreeGroupKill.lean` (namespace
   `GroupApproximation.Manuscript.ChainCore.BilateralThree`).  The group-level half of the printed proof,
   in any countable group, with the printed relations as hypotheses:
   - `bilateralB` (B = ⟨L, W, c, y⟩), `commute_of_mem_bilateralB` (⟨z⟩ central in B);
   - `killed_of_finite_central_defect`: z ∈ 𝔇_B(L), ⟨z⟩ finite central, thm:compression-criterion inside B
     (`manuscriptOneSidedCompressionCriterion`), so every MF target kills z;
   - `killed_of_commutators`, `root_killed_of_finite_central_defect`: the two printed commutators move the
     kill to e₂₁(d).
2. `GroupApproximation/Manuscript/ChainCore/BilateralThreeStatement.lean`.  The printed statement and assembly:
   - `clopenDefectSet T k`, `defectIdeal T k` (I, spelled as the ideal generated by the defects);
   - `MFTargetsKillRelativeElementary T k n I`;
   - `def PrintedBilateralThree : Prop`;
   - `threeBlockIndexEquiv`, `threeIdx`, `glThreeBlockEmbedding` (+ `_elementaryUnit`): extension by identity;
   - `relativeElementary_span_le_of_forall`, `relativeElementary_killed_of_rootThree_killed`: root propagation
     to EL_n(R_X, I), n ≥ 3;
   - `not_isOperatorMF_of_root_killed`, `isLEF_units_matrix_of_isLEFRing`, `countable_units_matrix`;
   - `rootThree_killed_of_cell`, `printedBilateralThree_of_pieces`.

## Residual piece Props (in BilateralThreeStatement)

| Prop | content | owner |
|---|---|---|
| `BilateralThreeCellStatement` | for clopen P with T(P) ⊆ P: L ≤ GL₃(R_X) with (T), W, c, y, ℓ, z, g₁, g₂ with the printed relations and `[g₂, [z, g₁]] = e₂₁(d)` | identities: ct-bilateral-cell; (T) of L via `ejz`: this lane, once S lands |
| `ClopenCrossedProductCountableStatement` | R_X countable (X compact metrizable totally disconnected, k finite) | chain-core S1 |
| `ClopenCrossedProductDirectlyFiniteLEFStatement` | R_X directly finite ⇒ IsLEFRing R_X | chain-reflection (thm:core-ring-reflection) |
| `ClopenCrossedProductNonzeroDefectStatement` | R_X not directly finite ⇒ some defect ≠ 0 | chain-matricial + chain-reflection |

## Status

- 09-13 ~16:40: both modules written; group-kill probe submitted; statement module next.
- 09-13 16:38: BilateralThreeGroupKill LANDED dfc4d5221 (probe 0913-163123-43571 GREEN), wiring queued.
- 09-13 16:47: BilateralThreeStatement LANDED 816a6b699 (probe 0913-164241-92660 GREEN), wiring queued.
  Endpoint for chain-radical: `PrintedBilateralThree`; assembly `printedBilateralThree_of_pieces`.
- 09-13 16:48: BilateralThreeCorollaries in probe: `PrintedIntroDirectlyFiniteIffGLThreeMF` (tex 253-255) from the
  endpoint at n = 3, and `hasKazhdanPropertyT_range_of_injective` (the (T) clause of the cell, via `ejz`).
- Next: discharge the (T) clause of `BilateralThreeCellStatement` once ct-bilateral-cell lands S and the corner
  embedding; add the `transientIdeal` wrapper once chain-matricial lands `transientIdeal_eq_span_defects`.
- 09-13 16:55: BilateralThreeCorollaries LANDED d2f3e22b5 (probe 0913-165128-18372 GREEN), wiring queued.
- CLAIM BilateralThreeTransferStatement GroupApproximation/Manuscript/ChainCore/BilateralThreeTransfer.lean
  (ct-rank-budget's piece, owner ct-bilateral-mf; producer over the cell, countability and nonzero-defect pieces).
