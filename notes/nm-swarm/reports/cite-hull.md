# cite-hull lane report

## Scope
This lane covers Hull's cited results in `sec:torsion-free` other than Theorem 7.1 itself:
Theorem 3.12, Definition 1.4, Corollary 5.7 with Lemma 5.8, Corollary 7.4, and the remark
on induction over `m`. The sources were checked against the paper text
(tmp/lit-audit/hull.txt: Def 1.4 l.132, Thm 3.12 l.633, Cor 5.7 l.1177, Lem 5.8 l.1182,
Cor 7.4 l.1446).

## Landed
- `GroupApproximation/Manuscript/NonMF/HullCitationSentences.lean`, a new orphan module.
  - Landed as 18de0ce6a, first NM_UNVERIFIED.
  - Probe 0912-105023-79191 is GREEN: `BUILT GroupApproximation.Manuscript.NonMF.HullCitationSentences`,
    and every audit passes. The bytes on main are the probed bytes.
- Closed endpoints, audited with `#audit_closed_axioms`:
  - `HullCitationSentences.printedHullGeneratingSetLimitSet : PrintedHullGeneratingSetLimitSet`
  - `HullCitationSentences.printedHullTheorem312LimitSet : PrintedHullTheorem312LimitSet`
  - `HullCitationSentences.printedHullTheorem312Osin : PrintedHullTheorem312Osin`
  - `HullCitationSentences.printedHullCorollary57 : PrintedHullCorollary57`
  - `HullCitationSentences.printedSaturationHullPair : PrintedSaturationHullPair`
- Carrier, audited with `#audit_axioms`:
  - `HullCitationSentences.manuscriptSentence_hullCommonQuotient_of_leastAreaLeaves`
  - It rests on the two least-area leaves of Theorem 7.1 and proves
    `TorsionFreePrinted.HullCommonQuotientPrinted`.

## Per sentence

### tex 1629–1630, row a9dd4b90e479 (partial; proposed: formalized)
> For an acylindrically hyperbolic group $G$, choose the generating set $A$ provided by
> Hull~\cite[Theorem~3.12]{Hull}.

- **Closed.** `printedHullGeneratingSetLimitSet` states: for every `G` acylindrically
  hyperbolic at the limit-set notion (Osin's notion), `Nonempty (HullGeneratingSet G)`.
  - `HullGeneratingSet` is Hull's Theorem 3.12(4): `Γ(G,A)` is hyperbolic, and the
    action on it is acylindrical and non-elementary.
- **Proof.**
  - `GGT.OsinClassification.isAcylindricallyHyperbolicOsin_of_limitSet` is on main with
    no hypotheses (dgo-geometric, 6b5a5f75e).
  - Then `GGT.exists_hullGeneratingSet_of_osin`.
- **Theorem 3.12 (1) ⇔ (4)** is closed at both spellings: `printedHullTheorem312LimitSet`
  and `printedHullTheorem312Osin`.
- **Consequence for sec5 / nm-endpoints.** The `hhard` argument of
  `TorsionFreeLimitSetNotion.manuscriptSentence_hullGeneratingSetLimitSet`,
  `printedSaturationLimitSet_of_noOmega` and
  `manuscriptSaturationLimitSet_of_leastAreaLeaves` is exactly
  `isAcylindricallyHyperbolicOsin_of_limitSet`, so it can be discharged.

### tex 1630–1632, row 615720614d7a (definition; no change)
> A subgroup is \emph{suitable} if it acts non-elementarily on the Cayley graph
> $\operatorname{Cay}(G,A)$ and normalizes no nontrivial finite
> subgroup~\cite[Definition~1.4]{Hull}.

- Hull's Definition 1.4 has three clauses:
  1. `Γ(G,A)` hyperbolic and the action acylindrical;
  2. `S` non-elementary;
  3. `S` normalizes no finite subgroup.
- Clauses 2 and 3 are `TorsionFree.Suitable`. Clause 1 is data carried by
  `TorsionFree.HullGeneratingSet`.
- The manuscript's sentence prints clauses 2 and 3 and carries clause 1 in the choice of `A`.
- Graded definition since c1a7e96ec. Nothing is missing.

### tex 1632, row 0a882030dcd9 (structural)
> We use the following consequence of Hull's small cancellation
> theorem~\cite[Theorem~7.1(a), (c), and (e)]{Hull}.

This is Theorem 7.1 itself and is outside this lane.

### tex 1644–1646, row bcc99703f838 (partial; stays partial)
> Hull's proof treats $m=1$ by passing to $G/\normal{r}_G$ for one element $r$ and the
> general case by induction on $m$, using his clause~(d), that $\varphi(N)$ is again
> suitable~\cite[proof of Theorem~7.1]{Hull}, so $\ker\varphi$ is the normal closure of
> $m$ elements and $Q$ is finitely presented when $G$ is.

- The content is the one-step form of Theorem 7.1 (`HullOneStepStatement`) plus the
  induction. Carriers over the least-area leaves are on main:
  - `TorsionFreeHullParagraphGeneral.manuscriptSentence_hullOneTargetOneRelatorGeneral_of_leastAreaLeaves`
  - `TorsionFreeHullParagraphGeneral.manuscriptSentence_hullInductionOnTargetsGeneral_of_oneStep`
  - The census row still names the historical `hullOneStepStatement_of_quasiGeodesicLeaves`.
- The deduction "kernel normally generated by `m` elements ⇒ finitely presented" is
  closed (row c3246ffe1961).
- The only remaining input is the pair of Theorem 7.1 leaves. This lane has nothing
  separate to prove here.

### tex 1662, row 2d1cd22e5f49 (partial; proposed: formalized)
> By Hull~\cite[Corollary~5.7 and Lemma~5.8]{Hull}, $N$ contains two elements $h_1,h_2$
> such that $N_0=\langle h_1,h_2\rangle$ is suitable with respect to a generating set
> $A'\supseteq A$ to which Hull's small cancellation theorem again applies.

- **Closed.** `printedSaturationHullPair` states: over a torsion-free `G`, for every Hull
  generating set `A` and every `N` suitable for `A`, there are a Hull generating set
  `A' ⊇ A` and `h₁, h₂ ∈ N` with `⟨h₁,h₂⟩` suitable for `A'`.
  - "To which Hull's small cancellation theorem again applies" is that `A'` is a Hull
    generating set, so `thm:hull`'s hypotheses hold for `N₀` and `A'`.
  - The application of `thm:hull` is the next sentence (row 2f997e5af4e6), and that one
    rests on the Theorem 7.1 leaves.
- The current row carrier `manuscriptSentence_saturationPairAndHullAgain_of_leastAreaLeaves`
  bundles that application into this sentence. That bundling is the only reason the row
  is partial.
- **Proof.** `TorsionFreePrinted.hullSuitablePair_of_torsionFree` with `A' = A`:
  - two independent loxodromic elements of `N` already generate a non-elementary subgroup;
  - over a torsion-free group, clause 3 of Definition 1.4 is automatic.
- **Hull's Corollary 5.7 as printed is closed** (`printedHullCorollary57`):
  - for all `k`, loxodromic, pairwise non-commensurable `h₁,…,h_k ∈ S` with
    `E_G(hᵢ) = ⟨hᵢ⟩`;
  - and, for `k > 0`, the cone-off family `{⟨hᵢ⟩}` is hyperbolically embedded;
  - from `HullSC.yiSuitableFiniteFamily_closed` and
    `HullSC.isHyperbolicallyEmbedded_zpowers_of_elementaryClosure_eq_unconditional`.
- **Hull's Lemma 5.8 in its general form** (a subgroup containing a proper hyperbolically
  embedded infinite cyclic `⟨h⟩` is suitable for some `A ⊇ X`) is not formalized.
  - The manuscript applies it only over a torsion-free group, where `A' = A` suffices.
  - The general form is not a manuscript sentence.

### tex 1675–1684, row dab2f2bfe084 (partial; stays partial, carrier replaced)
> The group $G_0$ is obtained there as a common quotient of two finitely generated
> acylindrically hyperbolic groups by Hull's theorem~\cite[Corollary~7.4]{Hull}, which
> allows the quotient to be chosen acylindrically hyperbolic; we take $G_0$ to be such a
> quotient.

- **Carrier.** `manuscriptSentence_hullCommonQuotient_of_leastAreaLeaves (hgreendlinger)
  (hbridge) : TorsionFreePrinted.HullCommonQuotientPrinted`.
  - The statement is Corollary 7.4 at the paragraph's groups: `G₁, G₂` finitely
    presented, torsion-free, acylindrically hyperbolic, with trivial finite radical;
    `α₁` is injective on a finite `Ω`.
  - The route is `HullSC.hullOneStepStatement_of_leastAreaLeaves`, then
    `HullSC.hullCommonQuotientPrinted_of_oneStep`.
  - No hypotheses other than the two Theorem 7.1 leaves.
- **Stale row decl.** The row names `TheoremC.Configuration`, which rests on
  TheoremCAssembly's admissions. The carrier above should replace it.
- **Leaf producers on main are all conditional:**
  - Greendlinger leaf: `relativeGreendlingerQuasiGeodesicLeastArea_of_inputs`
    (O52, Lemma 9.4, section inputs), `_of_pocketInputs`, `_of_osinLemma97`, `_of_reduced`.
  - Bridge leaf: `relativeIsoperimetricBridgeQuasiGeodesicEmbeddedStatement_of_boundedTransfer`,
    `_of_letterPullback`.
  - These belong to the hull-unbound / hull-bridge / hull-component lanes.

## Split
No split is needed. Every non-7.1 Hull citation is either closed here or reduces to
Theorem 7.1's two least-area leaves, and those are owned elsewhere.
