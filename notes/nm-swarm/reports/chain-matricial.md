# Lane chain-matricial: sec:chain-core S5, lem:transient-matrices

Printed item: `non_mf_groups_exist.tex` line 1463 (the definition of a locally matricial algebra) and
lines 1467–1513 (`\label{lem:transient-matrices}` and its proof).  Scope report:
`notes/nm-swarm/reports/chain-core.md`, item S5.

## Keys owned (WORKLIST, census merge 0913-144720)

| key | tex | sentence (abridged) | planned carrier |
|---|---|---|---|
| `eda117e756e6` | 1463 | locally matricial: finite subsets inside finite products of `M_h(k)` | `IsLocallyMatricialIn`, `IsLocallyMatricial` |
| `7ff20576d4cf` | 1468 | restriction fits into the exact sequence `0 → I → R_X → R_Y → 0` | `transientIdeal`, `restrict_surjective`, `mem_transientIdeal_iff_forall_coeff` |
| `f0c16e5c8519` | 1474 | `I` is locally matricial and generated by the defect indicators | `isLocallyMatricialIn_transientIdeal`, `transientIdeal_eq_span_defects` |
| `910136e6e66d` | 1480 | LC functions on a closed subset extend by finite clopen partitions | `LocallyConstant.exists_comap_val_eq_of_isClosed` (module 2, theoremc-retire) |
| `c05b0ec8b821` | 1480 | the restriction kernel is the functions compactly supported in the complement | `comap_val_eq_zero_iff_support_subset_compl` (module 2, theoremc-retire) |
| `97e43ae93d14` | 1480 | Laurent normal forms give exactness | `mem_transientIdeal_iff_forall_coeff` |
| `2546c17d9884` | 1480 | defect translates cover the complement; clopen subdivision gives generation | `ker_restrict_le_of_forall_defectD` |
| `e60c16639f70` | 1487 | put `U = X∖Y` | the binder `U := Yᶜ` in the assembly |
| `f5264e48f943` | 1487 | compact `C ⊂ U`: `m` wandering clopen sets; each orbit meets `C` at most `m` times | `exists_wandering_clopen_cover`, `ncard_orbit_inter_le` |
| `4244fb3c658b` | 1487 | `U` has no periodic points | `zpow_apply_ne_self_of_mem_compl` |
| `34e1eeff13be` | 1487 | finite subset of `I` as combinations of compact clopen partial shift graphs | `transientIdeal_eq_span_graphs` (`C(1_V) u^n`, `V ⊆ U` clopen) |
| `cd20a742dd3c` | 1487 | include inverse partial shifts; `C` = union of sources and ranges | `generatorExponents`, `supportSet` (module 4, theoremc-retire) |
| `1687471008a1` | 1487 | the generated relation has classes of size `≤ m` | `card_reachableExponents_le` (module 4) |
| `ff376e50f433` | 1487 | erasing loops: paths of length `≤ m − 1` | `reachableExponents_eq_of_le` (module 4) |
| `a3598c08b42c` | 1487 | freeness makes the arrow between related points unique | `zpow_injOn_orbit` (module 4) |
| `741b64286b55` | 1487 | the relation is finitely many partial shifts with compact clopen domains | `isClopen_setOf_reachableExponents_eq` (module 4) |
| `1d0f9a56866c` | 1500 | a finite clopen partition separating distinct points in each class | `levelPartition`, `levelPartition_separates` (module 4) |
| `fd026a61f84f` | 1500 | order the atoms; representative = point in the first occupied atom | `baseSet` (module 4) |
| `46f3810145ee` | 1500 | the representative set is clopen | `isClopen_baseSet` (module 4) |
| `5079b22a3633` | 1500 | refine by occupied atoms, shift exponents, coefficient values | `baseCell`, `isClopen_baseCell` (module 4) |
| `d01cbef361ce` | 1500 | on each cell fixed size `h`, continuously enumerated clopen levels | `level`, `pairwiseDisjoint_level`, `iUnion_level_eq` (module 4) |
| `2a63696d979b` | 1500 | characteristic arrows between levels form a copy of `M_h(k)` | `towerUnit_mul_towerUnit`, `isMatrixUnitFamily_towerUnit` |
| `e55132ef58f5` | 1500 | the product contains the prescribed elements | `isLocallyMatricialIn_transientIdeal` |

Route note (keys `1d0f9a56866c`, `fd026a61f84f`, `ff376e50f433`).  The printed proof builds levels from a
separating partition.  Lean builds the exponent classes first:
- `D_r(x)` is the set of exponents reachable from `x` in at most `r` generator steps inside `C`.
- It is increasing in `r`, has at most `m` elements (freeness), and so is stable from `r = m − 1`.  This is the
  loop-erasing bound.
- The level partition is then a separating finite clopen partition.  Ordering its atoms by exponent, the first
  occupied atom is the minimal exponent, which gives `baseSet`.

Every printed claim is proved as stated; only the order of construction differs.

Why the generator set `E` matters: the full-orbit return set `{d | T^d x ∈ C}` is not locally constant on a
compact clopen `C ⊂ U`.  In the orbit closure of `z_k = a^∞ b 0^k c 2^∞`, with `C = {w_0 ∈ {b,c}}`, return
times are unbounded.  So the classes are those of the relation generated by the finitely many exponents in the
prescribed elements, as printed.

## Modules (each unwired, probed, landed alone)

1. `GroupApproximation/Algebra/LocallyMatricial.lean`, namespace `GroupApproximation.ChainCore`.  Landed (see log).
   - `IsLocallyMatricialIn k (S : Set A)`: every finite `F ⊆ S` lies in `range φ ⊆ S` for an injective
     `φ : (Π a : Fin m, Matrix (Fin (h a)) (Fin (h a)) k) →ₙₐ[k] A`.  `IsLocallyMatricial k A` is the case `S = univ`.
   - `IsMatrixUnitFamily e` (fields `mul_same`, `mul_of_ne`, `mul_of_block_ne`), `matrixUnitsBlock`,
     `matrixUnitsHom` (`→ₙₐ[k]`), `matrixUnitsHom_apply`, `matrixUnitsHom_injective` (each `e a i i ≠ 0`).
   - `isLocallyMatricialIn_of_forall_exists_family`: the criterion used by the assembly.
   - `IsLocallyMatricialIn.exists_idempotent`, `.exists_finite` (`k` finite), `.exists_nonUnitalRingHom` (`→ₙ+*`).
   - Model tests: `isLocallyMatricial_matrix`; `not_isLocallyMatricialIn_squareZero` (the square-zero line
     `k · E₀₁ ⊂ M₂(k)` is not locally matricial).
2. `GroupApproximation/Topology/LocallyConstantClosedExtension.lean`: reassigned by main to theoremc-retire (09-13).
3. `GroupApproximation/Algebra/CrossedProductTowerMatrixUnits.lean`, namespace
   `GroupApproximation.Pestov91.CrossedProduct`.  No commutativity of `A` is needed.
   - `towerUnit p n a i j := single (ofAdd (n a i − n a j)) (p a i)`, which is `C (p a i) * u^(n a i − n a j)`
     (`towerUnit_eq_C_mul_unit_zpow`).
   - Hypotheses: `hcov` (level `i` is the translate of level `j`), `hidem`, `hne` (levels of one block
     orthogonal), `hblock` (levels of distinct blocks orthogonal).
   - `towerUnit_mul_towerUnit` (normal form), `_same`, `_of_ne`, `_of_block_ne`, `isMatrixUnitFamily_towerUnit`,
     `towerUnit_self`, `towerUnit_self_ne_zero`.
4. `GroupApproximation/Dynamics/PartialShiftClasses.lean`: reassigned by main to theoremc-retire (09-13).
5. `GroupApproximation/Dynamics/TransientIdealMatricial.lean`, importing module 6 and theoremc-retire's module 4.
   - `exists_wandering_clopen_cover`, `ncard_orbit_inter_le`, `zpow_apply_ne_self_of_mem_compl`.
   - `transientIdeal_eq_span_graphs`, `C_mul_unit_zpow_eq_sum_towerUnit`, `isLocallyMatricialIn_transientIdeal`,
     and the positive model test on clopen towers.
6. `GroupApproximation/Dynamics/TransientIdealExact.lean`, landing before module 5, after chain-core's
   `Dynamics/ClopenCrossedProductComap.lean`.  Lead ruling 16:20: `R_X → R_Y` is chain-core's
   `ClopenCrossedProduct.restrict T k hY`; this lane declares no `restrict` or `restrictRingHom`.
   Namespace `GroupApproximation.ClopenCrossedProduct`, `[CompactSpace X] [T2Space X]
   [TotallyDisconnectedSpace X]`, `[Ring k]`, `(hYc : IsClosed Y) (hY : T '' Y = Y)`.
   - `restrict_surjective hYc hY`.
   - `transientIdeal T k hY : TwoSidedIdeal _ := TwoSidedIdeal.ker (restrict T k hY)`, `mem_transientIdeal`,
     `mem_transientIdeal_iff_forall_coeff`.
   - `ker_restrict_le_of_forall_defectD (hcover : DefectCoverInterface T Y) (ψ : _ →+* S)
     (hψ : ∀ P (hP : IsClopen P), T '' P ⊆ P → ψ (defectD T k hP) = 0) : ∀ x, restrict T k hY x = 0 → ψ x = 0`.
   - `transientIdeal_eq_span_defects hcover havoid : transientIdeal T k hY =
     TwoSidedIdeal.span {d | ∃ P (hP : IsClopen P), T '' P ⊆ P ∧ d = defectD T k hP}`.
   - `transientIdeal_eq_bot_iff hYc hY : transientIdeal T k hY = ⊥ ↔ Y = Set.univ`, `restrict_injective_iff`
     (`[Nontrivial k]`).
7. Later: `GroupApproximation/Manuscript/NonMFSentences/TransientMatrices.lean` (printed assembly).

## Interfaces needed (consumed through owners' interface lemmas, never duplicated)

- chain-core (S1): `ClopenCrossedProduct T k` (`R_X`), `coeff`, `defectD` (`Dynamics/ClopenDefectPair.lean`);
  `restrictHomeo`, `restrict`, `restrict_coeff`, `restrict_unit` (`Dynamics/ClopenCrossedProductComap.lean`).
- theoremc-retire (modules 2 and 4): `comap_val_eq_zero_iff_support_subset_compl`,
  `LocallyConstant.exists_comap_val_eq_of_isClosed`, the partial-shift classes and levels.
- chain-itinerary (S4): the defect cover, `exists_defect_translate_of_not_mem_chainRecurrentSet`.
- hull-euler (S2): `P∖T(P)` wandering for clopen `T(P) ⊆ P`; wandering sets avoid `CR(T)`.

## Interfaces provided

- To chain-reflection (S6a): `transientIdeal`, `ker_restrict_le_of_forall_defectD`,
  `transientIdeal_eq_span_defects`, `transientIdeal_eq_bot_iff`, `restrict_injective_iff`.
- To chain-radical (S6b): `IsLocallyMatricial`, `IsLocallyMatricialIn`, `.exists_finite`,
  `.exists_nonUnitalRingHom`, `.exists_idempotent`, `isLocallyMatricialIn_transientIdeal`.

## Residual Props (named, model-tested hypothesis Props until the owner lands)

| Prop (planned name) | content | owner | carried by |
|---|---|---|---|
| `DefectCoverInterface T Y` | every `x ∉ Y` is in some `T^j (P∖T(P))`, `P` clopen, `T(P) ⊆ P` | chain-itinerary S4 | modules 5, 6 |
| `DefectWanderingInterface T` | clopen `T(P) ⊆ P` ⇒ `P∖T(P)` wandering | hull-euler S2 | module 5 |
| `DefectAvoidsCoreInterface T Y` | clopen `T(P) ⊆ P` ⇒ `P∖T(P)` disjoint from `Y` | hull-euler S2 / chain-itinerary S4 | module 6 |

Planned model tests:
- Positive: the two-point compactification of `ℤ` with the shift, `Y = {±∞}`, `P = [0, +∞]`, so
  `P∖T(P) = {0}`.
- Negative: the identity on a nonempty `X` with `Y = ∅` fails `DefectCoverInterface`.

## Infra

- Probe clone: `$NM/lanes/chain-matricial.clone = xxii-fix` (shared, serialized by the lock).

## Progress log

- 09-13: plan landed at `614c1634a`.
- 09-13 16:16: module 1 probe `0913-161620-60280`, `# PROBE GREEN`, `BUILT GroupApproximation.Algebra.LocallyMatricial`.
  Landed at `839935133`; wiring queued in `$NM/wire-queue.txt`.  Names sent to chain-radical.
- 09-13: modules 2 and 4 reassigned to theoremc-retire; lead ruling 16:20 adopted for module 6.  Report landed at
  `9748f8a81`.
- 09-13 16:35: module 3 probe `0913-163529-70754`, `# PROBE GREEN`,
  `BUILT GroupApproximation.Algebra.CrossedProductTowerMatrixUnits`.  Landed at `5b3910136`; wiring queued.
- 09-13 17:08: module 6 first half (`Dynamics/TransientIdealExact`: `transientIdeal`, `mem_transientIdeal`,
  `coeff_restrict`, `mem_transientIdeal_iff_forall_coeff`, `restrict_injective_of_eq_univ`,
  `eq_univ_of_restrict_injective`, `restrict_injective_iff`, `transientIdeal_eq_bot_iff`).  Probe
  `0913-170754-83745`, `# PROBE GREEN`, `BUILT GroupApproximation.Dynamics.TransientIdealExact`.  Landed at
  `4af014f74`; wiring queued.  Built on chain-core's `restrict`; no restrict of its own.  `restrict_surjective` waits
  for theoremc-retire's `LocallyConstant.exists_comap_val_eq_of_isClosed` on origin.
