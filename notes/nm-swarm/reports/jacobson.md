# jacobson lane report

## Scope
Roster target: the four L1122 rows about the Toeplitz--Jacobson algebra, for the presented algebra
`J = F_2<s,t | ts=1>`:
- b47d06c5c362: the algebra J;
- 11137e57b26d: JeJ = finite matrices;
- d5e56b196927: ker(EL_n(J) -> EL_n(F_2[z,z^-1])) <= GL_fs(V^n);
- 0a08ffd095d6: equality of the inclusion.

The earlier carriers (`JacobsonSymbol`, `JacobsonFiniteMatrices`, `JacobsonKernelFinitary`) state
these sentences for the operator model `jacobsonAlgebra`, the subalgebra of `End(F_2[X])`
generated by the two shifts. This wave adds the presented algebra and moves every sentence to it.

## Closed (landed, compiled)
`Manuscript/OneSidedMFRadical/JacobsonPresented.lean` (ff4a0eea9; probes 0913-031644-40765 and
0913-043721-21532):
- `ToeplitzJacobson := RingQuot TSRelation` over `FreeAlgebra (ZMod 2) (Fin 2)`, with
  `presT_mul_presS : presT * presS = 1`;
- `shiftRep : ToeplitzJacobson →ₐ[ZMod 2] Module.End (ZMod 2) JacobsonSpace` and
  `shiftRep_injective`;
- `jacobsonEquiv : ToeplitzJacobson ≃+* ↥jacobsonAlgebra`.

`Manuscript/OneSidedMFRadical/JacobsonPresentedSentences.lean` (c927a1f1e + cba3ce8ba; probe
0913-043721-21532, BUILT, four `#audit_closed_axioms`). Each endpoint is a
`def Printed… : Prop` with a closed `theorem manuscriptSentence_…`:
- `PrintedToeplitzJacobsonAlgebra` (tex 1122--1126) says:
  - `ts = 1`;
  - the universal property of the presentation;
  - `1 - st ≠ 0` and `2 • (1 - st) = 0`;
  - the symbol `J → F_2[z,z^{-1}]` (`s ↦ T 1`, `t ↦ T (-1)`) is surjective with
    `TwoSidedIdeal.ker = span {1 - st}`;
  - `J/J(1-st)J ≃+* F_2[z,z^{-1}]` is nontrivial;
  - `span {1 - st} ≠ ⊤`;
  - `¬ IsCDEOperatorMF (EL_n(J))` for `n ≥ 4`.
- `PrintedToeplitzJacobsonShiftAction` (tex 1127--1131) says:
  - `shiftRep` is injective, with `s ↦ shiftUp` and `t ↦ shiftDown`;
  - `1 - st` sends `X^N` to `if N = 0 then 1 else 0`;
  - `x ∈ span {1 - st} ↔ IsFiniteMatrix (shiftRep x)`.
- `PrintedToeplitzJacobsonKernelFinitary` (tex 1133--1134) says, for every `n`:
  - `EL_n(J)` acts entrywise on `V^n` through `shiftRep`, faithfully;
  - each element of the kernel of `EL_n(J) → EL_n(F_2[z,z^{-1}])` acts in `binaryGLfs n`;
  - for `n ≥ 2` the kernel is `relativeElementary (Fin n) (span {1 - st})`, and its image is
    `binaryGLfs n`.
- `PrintedToeplitzJacobsonRadicalEquality` (tex 1126--1127): for `n ≥ 4`,
  `MFQuotientUnits.mfHomKernel (EL_n(J)) = relativeElementary (Fin n) (span {1 - st})`.

All four are proved by transport along `jacobsonEquiv`, using `Subgroup.map_normalClosure` for the
relative elementary subgroups.

## Census and wiring
- Rows appended to `metadata/nm-census-rows/jacobson.tsv`, keyed by row hash (b47d06c5c362,
  11137e57b26d, d5e56b196927, 0a08ffd095d6).
- Queued in `wire-queue.txt`: JacobsonPresented first, then JacobsonPresentedSentences.
  - JacobsonPresented imports the root-imported JacobsonFiniteMatrices.
  - JacobsonKernelFinitary is reachable through the root-imported JacobsonRankTwoCarrier.

## Residual Props
None for the four L1122 rows. The endpoints close once the two modules are root-wired.

## Next
L1145 c149d33e8f7e, the complementary sentence. `JacobsonComplementarySentence` was root-wired at
f495cf119 (ghw-charp2 green 0913-040230-50292). Next, check its endpoint against the printed
sentence and upgrade the row from partial.
