# Lane ct-involution (sk): simple_kazhdan_sofic_group.tex, tex 67–86, 102–113, 154–173 at bf961c128

Lead: main (ROSTER-sk, re-scoped 18:58). Tex read with `git show origin/main:simple_kazhdan_sofic_group.tex`
at bf961c128 (340 lines, md5 7b3dc4ec92b4581553bc0dc2b0edcb99). Census rows are keyed `LINE:<l>@bf961c128` in
`metadata/sk-census-rows/ct-involution.tsv`.

The b965d63ba text (970 lines) was replaced at bf961c128 (18:54). This lane's old sec:ring maps onto tex 70–86, 102–113 and
154–173 of the new text. Uniform recurrence (old lem:subshift (U)) moved into the Finite models (118–153, ms-units).

## Ledger (provisional; carriers verified on origin at ~19:05)

Verdicts: OK = closed carrier at printed generality along the printed route; GAP = no carrier; ROUTE = a carrier exists but
its generality or route differs; BUILD = this lane builds it.

| line | sentence (abridged) | carrier on origin | verdict |
|---|---|---|---|
| 67 | `\section{Proof of Theorem~\ref{thm:main}}` | none | structural |
| 69 | `\subsection*{The ring and property (T)}` | none | structural |
| 70 | "Write e_U for the indicator of a clopen set U ⊆ X." | `ClopenCrossedProduct.coeff T k (LocallyConstant.charFn k hU)` | definition; BUILD the named `e_U` |
| 70–77 | conventions (Tx)_n = x_{n+1}; R = LC(X,F₂) ⋊_T ℤ = {Σ f_j u^j}, u f u⁻¹ = f ∘ T⁻¹; LC | `ClopenCrossedProduct`, `exists_sum_coeff_mul_unit_zpow`, `unit_mul_coeff_mul_inv` (chain-core) | GAP: no shift homeomorphism on a general subshift X ⊆ A^ℤ; `Pestov91.Subshift` is the Toeplitz subshift only. BUILD the setting |
| 77 | "Thus u e_U u⁻¹ = e_{TU}." | `unit_mul_coeff_mul_inv` + `comap_symm_charFn` | ROUTE (two steps); BUILD the named identity |
| 78 | "Minimality and infiniteness imply that T has no periodic points." | none for a general subshift | GAP, BUILD |
| 79–80 | "For each L, every point therefore has a clopen neighborhood U with U ∩ T^jU = ∅ for 0<\|j\|≤L." | `Pestov91.exists_clopen_free_nbhd` (a variant for a nonzero function) | ROUTE; BUILD the point form |
| 80–82 | "Compactness gives finite clopen partitions with this property, refining any prescribed clopen partition." | none | GAP, BUILD |
| 84–86 | "The ring R is generated by u, u⁻¹ and the letter indicators e_a…: their translates and products give all cylinder indicators." | `Pestov91.isFinitelyGeneratedRing_crossedProduct` (Bool alphabet, raw SkewMonoidAlgebra, abstract action) | ROUTE (alphabet and spelling); BUILD for a finite alphabet A over `ClopenCrossedProduct` |
| 102 | "We will also need that R is simple and Z(R)=F₂." | simplicity: `Pestov91.isSimpleRing_of_minimal_free` (general free minimal); centre: none | simplicity ROUTE check pending; centre GAP, BUILD |
| 103–104 | "These are standard groupoid-algebra facts [BCFS,ClarkEdie,Steinberg]; here is the direct argument." | none | attribution |
| 104–106 | "A nonzero two-sided ideal contains an element r = Σ f_j u^j with f_0 ≠ 0, after multiplication by a power of u." | inside `Pestov91.crossedProduct_one_mem` | route check pending |
| 106–108 | "Choose a nonempty clopen U on which f_0 = 1, disjoint from T^jU for every nonzero exponent occurring in r. Then e_U r e_U = e_U." | `Pestov91.locallyConstant_cut_key`, `exists_clopen_free_nbhd` | route check pending |
| 108–109 | "Finitely many translates of U cover X, so the ideal contains 1 = 1 − ∏_i (1 − e_{T^{n_i}U})." | `Pestov91.exists_finset_cover_of_dense_orbits`, `crossedProduct_one_mem` | route check pending |
| 110–112 | "If r is central, commuting with all clopen indicators forces f_j = 0 for j ≠ 0: … separated by a clopen set." | none | GAP, BUILD |
| 112–113 | "Commuting with u then makes f_0 invariant, hence constant by minimality." | none | GAP, BUILD |
| 154 | `\subsection*{A finite tower detects every normal subgroup}` | none | structural |
| 155 | "Suppose the levels T^aU are disjoint for all indices needed below." | none | definition (hypothesis `U ∩ T^jU = ∅` for 0<\|j\|≤2m); BUILD |
| 156–159 | "For \|a\|,\|b\| ≤ m and clopen W ⊆ U, put E_ab(W) = e_{T^aW} u^{a−b}." | `Pestov91.CrossedProduct.towerUnit` (block form, chain-matricial) is close | definition; BUILD at printed spelling |
| 160–161 | "These satisfy E_ab(W)E_cd(W') = δ_bc E_ad(W ∩ W')." | `towerUnit_mul_towerUnit` (same-set towers only) | GAP for W ≠ W'; BUILD |
| 162–166 | "Their span is a subring B_m(U) ≅ M_{2m+1}(LC(U,F₂)), with unit e_{⋃_{\|a\|≤m} T^aU}." | none | GAP, BUILD |
| 166–171 | "If r, r' involve only powers u^j with \|j\| ≤ w, then r B_m(U) r' ⊆ B_{m+w}(U) (eq:absorb), provided the larger tower is disjoint." | none | GAP, BUILD |
| 171–173 | "To see this, multiplying E_ab(W) on the left by f u^i and on the right by f' u^j changes its indices to (a+i, b−j) and restricts its clopen coefficient." | none | GAP, BUILD |

## Plan (new modules under `GroupApproximation/Manuscript/SimpleKazhdanSofic/`)

1. `Setting.lean`: the printed setting (tex 44–46, 70–77). A finite alphabet A, a closed shift-invariant X ⊆ A^ℤ, the shift
   homeomorphism with (Tx)_n = x_{n+1}, `R = ClopenCrossedProduct T (ZMod 2)`, `e_U`, letter indicators, and u e_U u⁻¹ = e_{TU}.
   This is shared with ms-intro-3 (thm:main), ms-compress-4, ms-units, ct-bilateral-mf and skf-degrees; land it first.
2. `RingPartitions.lean` (78–82): no periodic points; clopen neighborhoods; refining clopen partitions.
3. `RingGeneration.lean` (84–86).
4. `RingSimpleCentre.lean` (102–113): simplicity over the Pestov91 carrier, if its route checks out; Z(R) = F₂.
5. `Towers.lean` (154–173): E_ab(W), the matrix units, B_m(U) ≅ M_{2m+1}(LC(U,F₂)) with unit, eq:absorb.

## Progress log

- 09-13 ~19:05: ledger drafted; blocker (tex replaced) reported and range re-keyed by main.
