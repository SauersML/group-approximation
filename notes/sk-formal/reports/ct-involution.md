# Lane ct-involution (sk): "The ring and property (T)" — ring facts and generation

Lead: main (ROSTER-sk, PROTOCOL 19:45). Target: the current origin/main tip of `simple_kazhdan_sofic_group.tex`, read with
`git show origin/main:simple_kazhdan_sofic_group.tex`. At e80dcf20a (386 lines, md5 4ad4921253626a4f858866c716a13385) this lane
owns the ring sentences of "The ring and property (T)" (tex 87–99). ms-compress-4 owns EL_3, eq:elementary, (T), infinite and
Z(G) (tex 99–114). Census rows are keyed by the 12-hex sentence hash of `metadata/SK_SENTENCE_CENSUS.tsv` (regenerated at
e80dcf20a, 09e580c38) in `metadata/sk-census-rows/ct-involution.tsv`.

History: the note was replaced twice (bf961c128 at 18:54, then e80dcf20a at 19:39). The old sec:ring towers, eq:absorb and the
standalone "R simple and Z(R) = F₂" sentences no longer exist. The Simplicity section (ct-bilateral-mf, skf-consequences) proves
them inline, and the carriers below were offered to that lane.

## Ledger at e80dcf20a

| key | line | sentence (abridged) | carrier | status |
|---|---|---|---|---|
| c6bd4bf41f69 | 87 | "Write e_U for the indicator of a clopen set U ⊆ X." | `SimpleKazhdanSofic.eU` (RingPartitions, 2f481625d) | definition (row landed) |
| d31438fef85b | 87–88 | "Our conventions are (Tx)_t = x_{t+1} and" | `SimpleKazhdanSofic.subshiftHomeo` (Setting, ms-intro-3): `shift 1`, `(shift 1 x) t = x (1 + t)` | definition (row landed) |
| 9a58ee4cb6c1 | 89–92 | [display] R = LC(X,F₂) ⋊_T ℤ = ⊕_j LC(X,F₂) u^j, u f u⁻¹ = f ∘ T⁻¹ | `SimpleKazhdanSofic.R` (Setting), `ClopenCrossedProduct.unit_mul_coeff_mul_inv` | definition (row landed) |
| 6fdf439f1009 | 93–95 | "So every element of R is uniquely a finite sum Σ f_j u^j, the product is (fu^i)(f'u^j) = f (f' ∘ T^{−i}) u^{i+j}, and u e_U u⁻¹ = e_{TU}." | `ClopenCrossedProduct.exists_sum_coeff_mul_unit_zpow`; `SimpleKazhdanSofic.eq_of_sum_coeff_mul_unit_zpow_eq`, `coeff_mul_unit_zpow_mul_coeff_mul_unit_zpow` (RingLaurent, 374408a7c); `unit_mul_eU_mul_inv` (RingPartitions, 2f481625d) | formalized (row landed 15cab13e4) |
| 16a4a7086323 | 97–99 | "The ring R is generated by u^{±1} and the letter indicators e_a…, since products of the translates u^j e_a u^{−j} are the cylinder indicators, which span LC(X,F₂)." | `SimpleKazhdanSofic.printedRingGeneration`, `printedSubshiftRingFinitelyGenerated` (RingGeneration, 22b15ce5e) | formalized (row landed) |

Route notes:
- Generation follows the printed route. Translates of `e_a` are coordinate indicators (`comap_letterIndicator`), a locally
  constant function depends on a finite window (`WordGraph.exists_radius`), and it is a 0/1 sum of cylinder products
  (`mem_closure_coeff`).
- Uniqueness of the Laurent form is coefficient extraction (`coeff_sum_coeff_mul_unit_zpow`).

## Interface for ms-compress-4 ("So G has property (T)", tex 113)

`PrintedSubshiftRingFinitelyGenerated := ∀ (A : Type) [TopologicalSpace A] [DiscreteTopology A] [DecidableEq A] [Finite A]
(S : Subshift A ℤ), ∃ gens : Finset (R S), Subring.closure (gens : Set (R S)) = ⊤`. This is `IsFinitelyGeneratedRing (R S)` by
definition, and it is the hypothesis of `PrintedElementaryPropertyT`. Closed endpoint: `printedSubshiftRingFinitelyGenerated`.

## Modules

| module | content | status |
|---|---|---|
| `Manuscript/SimpleKazhdanSofic/RingPartitions.lean` | `IsMinimalSystem`, `eU`, `unit_mul_eU_mul_inv`, no periodic points, clopen neighbourhoods and partitions | LANDED 2f481625d, wire-queued |
| `Manuscript/SimpleKazhdanSofic/RingSimpleCentre.lean` | `isSimpleRing`, `mem_center_iff`, `mem_center_iff_eq_zero_or_one` | LANDED 6aa8851c6, wire-queued |
| `Manuscript/SimpleKazhdanSofic/RingLaurent.lean` | uniqueness of the Laurent form, product formula | LANDED 374408a7c, wire-queued |
| `Manuscript/SimpleKazhdanSofic/RingGeneration.lean` | `printedRingGeneration`, `printedSubshiftRingFinitelyGenerated` | LANDED 22b15ce5e, wire-queued |

Released to ct-bilateral-mf (not in this lane's scope at e80dcf20a): `TowerUnits.lean` (attic 3ff509880), `TowerAbsorb.lean`
(attic 4a46b7660).

## Progress log

- 09-13 ~19:05: first ledger (b965d63ba ranges); tex replaced at bf961c128, re-keyed.
- 09-13 19:42: RingPartitions landed. 19:46: RingSimpleCentre landed.
- 09-13 ~19:50: note replaced again (e80dcf20a); scope re-cut to the ring sentences; towers released; census keys adopted.
- 09-13 19:55: three definition rows landed (b72fef5a3).
- 09-13 ~20:03: API outage; resumed 20:15.
- 09-13 20:2x: RingLaurent landed (374408a7c); row 6fdf439f1009 landed (15cab13e4); RingGeneration fixed and re-probing, with
  `PrintedSubshiftRingFinitelyGenerated` added.
- 09-13 20:3x: RingGeneration landed (22b15ce5e); row 16a4a7086323 landed; PrintedSubshiftRingFinitelyGenerated closed. The ring sentences of this section are all graded.

## Item 2 (main, 20:3x): conjugacy and flip conjugacy give isomorphic groups ("Questions")

CLAIM 26e9baa90d07 ("If (X,T) is conjugate to (Y,S) or to (Y,S⁻¹), then G_X ≅ G_Y"), moved from ms-compress-4 by main, in
`GroupApproximation/Manuscript/SimpleKazhdanSofic/CrossedProductConjugacy.lean`:
- a conjugacy φ gives `R_T ≃+* R_S` from chain-core `comap` along φ and φ⁻¹ (C(f) ↦ C(f ∘ φ⁻¹), u ↦ u);
- a flip conjugacy gives `R_T ≃+* R_S` through `R_S ≃+* R_{S⁻¹}` (C(f) ↦ C(f), u ↦ u⁻¹);
- `elementaryCoefficientEquiv` gives `EL_n(R_T) ≃* EL_n(R_S)` for every n, so `G_X ≃* G_Y`.
