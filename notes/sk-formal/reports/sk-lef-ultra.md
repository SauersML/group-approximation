# sk-lef-ultra: the embedding steps of the proof of cor:lef / cor:host

Lane sk-lef-ultra, a fork of the lead session launched ~21:22 CDT on 2026-09-13 and resumed at 23:15 after the coordinator
restart. Brief: the "TIP 23:15" table of the lead's `ct/ROSTER-sk.md`, row "LEF groups (l.299): sk-lef-assembly [sk-universal],
sk-lef-ultra [sk-ore]".

Target: `simple_kazhdan_sofic_group.tex` at origin/main 37551fd93 (767 lines, md5 b55c0d23b8c59e66d36df004a1df608b), section
"LEF groups", first paragraph of the proof after `cor:host`, l.331–356:

> Conversely, let Γ = {γ_1, γ_2, …} be a countable LEF group, replaced by Γ × ℤ so that it is infinite. There are finite groups
> Q_k and maps μ_k : Γ → Q_k that are injective on {1, γ_1, …, γ_k} and preserve the products that stay in this set. On
> Q_k × ℤ/m_k, with m_k = |Q_k|, let t_k add 1 to the second coordinate, and let f_jk multiply the first coordinate of (x,i)
> from the left by μ_k(γ_j)^i, which is well defined as |Q_k| divides m_k. Then [f_jk, t_k] multiplies the first coordinate by
> μ_k(γ_j). On Q_k × ℤ/m_k × ℤ/4^{k+1} let b_k add 1 to the last coordinate, and let a_k act on the level i by f_jk if
> i = 2^{2j−1} and by t_k if i = 4^j, for j ≤ k, and trivially on the other levels. Then b_k^{−n} a_k b_k^n acts on the level
> i as a_k acts on the level i+n. As distinct pairs of powers of 2 have distinct differences, all below a quarter of the
> modulus, the word w_j = [b^{−2^{2j−1}} a b^{2^{2j−1}}, b^{−4^j} a b^{4^j}] evaluated at (a_k, b_k) acts by [f_jk, t_k] on the
> level 0 and trivially on the other levels, for j ≤ k. In the algebraic ultraproduct over ω of the symmetric groups of these
> sets, sending γ to the class of the permutations multiplying the first coordinate on the level 0 by μ_k(γ) and acting
> trivially elsewhere is a homomorphism, injective as μ_k(γ) ≠ 1 for γ ≠ 1 and large k. It sends γ_j to w_j(a,b), where
> a = (a_k)_k and b = (b_k)_k, so Γ ≤ [Δ,Δ] for the group Δ generated by a and b. Finitely many products and inequalities in the
> ultraproduct hold in some coordinate, so Δ is infinite, finitely generated and LEF.

## CLAIM (23:40, replaces the 21:25 claim)

CLAIM algebraic ultraproducts of groups; subgroups of ultraproducts of finite groups are LEF; LEF passes to Γ × ℤ GroupApproximation/GroupTheory/AlgebraicUltraproductLEF.lean

CLAIM level permutations t, f_jk, a_k, b_k, the power-of-two differences and the words w_j GroupApproximation/GroupTheory/LevelCommutatorWords.lean

CLAIM the ultraproduct embedding, Δ = ⟨a,b⟩ and `lefUltraproductEmbeddingStatement` GroupApproximation/Manuscript/SimpleKazhdanSofic/LEFLevelEmbedding.lean

- The 21:25 claim on `LEFUltraproductPart.lean` is withdrawn. That module was never written: the tip's proof has no doubled regular
  action, no Ore commutators and no Δ₀ × ℤ.
- Ore's theorem (sk-ore, not resumed) is no longer a step at the tip, so nothing is absorbed. sk-ore left no Lean files
  (`lanes/sk-ore.files` empty, no attic copies); its `research/ore-*` notes are not formalization carriers.
- The piece Prop is sk-lef-assembly's `LEFUltraproductEmbeddingStatement` (countable Γ), declared in its
  `Manuscript/SimpleKazhdanSofic/LEFCharacterization`. `LEFLevelEmbedding` proves the construction as
  `exists_levelCommutatorEmbedding`, and proves `lefUltraproductEmbeddingStatement` once that module lands.

## Plan

1. `AlgebraicUltraproductLEF`: the null normal subgroup and the quotient `Ultraproduct ω G`; `mk_eq_mk_iff`; representatives;
   `isTextbookLEF_ultraproduct` (finitely many products and inequalities hold in one coordinate); subgroups are LEF;
   `isTextbookLEF_prod`.
2. `LevelCommutatorWords`: `levelwise` and `shift` permutations; t and f with the well-definedness lemma
   `pow_val_add_one` (|Q| divides m); `[f, t]` multiplies the first coordinate; `conjPow` (b^{−n} a b^n shifts levels);
   `two_pow_sub_two_pow_inj` (distinct pairs of powers of 2 have distinct differences); `word_a_b` (w_j at (a_k, b_k) acts by
   [f_jk, t_k] on level 0 only).
3. `LEFLevelEmbedding`: the test sets, the models μ_k, the homomorphism into ∏_ω Sym(X_k) (injective), γ_j ↦ w_j(a,b),
   Δ = closure {a,b} (infinite, f.g., LEF), Γ ≤ [Δ,Δ], the Γ × ℤ replacement, and the closed endpoints.

## Log

- 21:25 first claim (tip 9047d0d3c).
- 23:32 resumed; the tip is 37551fd93, and the proof was rewritten (see above). Re-claimed.
