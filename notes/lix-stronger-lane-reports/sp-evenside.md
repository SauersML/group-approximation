# Lane `sp-evenside` — Step D mod `p`, the even side

Program note: `notes/LIX_STRONGER_PROGRAM_2026-09-10.md` §1.4.  Design source:
`notes/lix-stronger-lane-reports/sp-design.md` §3 (the uniform theorem §3.2, lemma list
§3.3, `F₂`↦`F_p` correspondence §3.4).  Clone: `spare1`.

Owns `GroupApproximation/CharClass/ParityP*.lean` and `StepDModP*.lean`, and nothing else.
`ParityP*` sits inside `sp-coeff`'s `Parity*` exclusion and does not collide with
`sp-steenrod`'s `OddP*` prefix.

**Deliverable 1** (independent of every other lane): sp-design §3.2 as pure algebra,
generic in `p`, in the `ParityData` shape.  **Deliverable 2** (later): the bridge to the
real objects, the mod-`p` analogue of `LemmaTwoStepD.lean`.

---

## GREEN (with job counts)

Nothing yet.  `spare1` was being filled with a real copy of the main tree's artifacts, and
the MSI wrapper's SSH master was down with an auth cooldown to roughly 12:10 CDT, so no
probe has been run.  Both files below are authored and read-audited only.

## AUTHORED, UNVERIFIED

### `GroupApproximation/CharClass/ParityPData.lean` (~470 lines)

The uniform theorem of sp-design §3.2, over an abstract pair of commutative rings.  No
cohomology, no `Y`, no rank, no stage.

* `ParityP.aProd`, `ParityP.bPart`, `ParityP.gammaProd` — the two Künneth components of a
  product `∏_{α ∈ L} γ_α` over a list of indices, and the product itself.
* `ParityP.IsDecomposable γ N` — **the decomposable part, as a property**: the additive
  subgroup of `H` generated by the products of *at least two* Chern classes of positive
  index whose indices sum to `N`.  This is the whole of what the argument needs to know
  about the universal Wu polynomial `E_{i+1}`, i.e. sp-design §3.3's L4a: `E_{i+1}` is
  `e_{ip+1}` with coefficient `1` plus such products.  The universal polynomials
  themselves are never written down, and no closed form is assumed (there is none at odd
  `p` in the two-factor shape `WuSymmetric.lean` enjoys at `p = 2`).
* `ParityP.bPart_eq_zero` — **L5**: the `z`-part of a decomposable monomial vanishes once
  every `b` at an earlier index in the same residue class does.  This is the one real
  induction in the file: strip factors off the list, carrying a counter for the multiples
  of `p` already removed, and use `a_q = 0` for `p ∤ q` to kill every branch in which the
  surviving `b`-index is not `≡ N (mod p)` and at most `N − p`.
* `ParityPData p R H` — the hypotheses as a structure, 19 fields, each with a docstring
  naming who discharges it.  **(W) carries a unit**, not the coefficient `1`: the field is
  `c : ℕ → R` with `c_isUnit`, and the relation is
  `P^i(γ_{i+1}) − ι(c i)·γ_{ip+1} ∈ IsDecomposable`.  This is sp-design's correction of
  2026-09-10, ratified by the lead: under sp-steenrod's approved normalisation
  `P(h) = h + κ·h^p` on a degree-two class, the diagonal coefficient is `(−1)^{i(p−1)}·κ^i`,
  and hard-coding `1` would make the even side hostage to a normalisation constant nobody
  intends to pin.  The induction only ever cancels the coefficient, so a unit is exactly
  the right strength; `ParityPTwo.lean` sets it to `1`.
* `ParityPData.b_step` — the inductive step, the `z`-component of the Wu relation at index
  `i`.
* `ParityPData.b_eq_zero` — **`b_{ip+1} = 0` for every `i`**; `b_eq_zero_of_mod` restates
  it as `N % p = 1`.  At `p = 2` this is `ParityData.b_odd_eq_zero`.
* `ParityPData.gamma_eq_zero`, `ParityPData.gamma_rank_eq_zero` — `γ_r(W) = 0` for
  `r ≡ 1 (mod p)` with `a_r = 0`, and the tower form `p ∣ n`, `p ∣ m` ⟹ `γ_{n+1+m}(W) = 0`.
  At `p = 2`, `n = 2` this is `ParityData.gamma_top_eq_zero` (`m` even, `r = m + 3`).
* `ParityP.pR_b_succ_of_instability`, `ParityP.pH_z_mul_of_cartan` — the two derivation
  helpers, so an instantiator who has instability in its general form and the Cartan
  formula discharges those two fields in one line each.

**Three things the theorem does not ask for**, each checked against the proof:

| not asked | why |
|---|---|
| `(p : R) = 0` | `ParityEven.lean` needs `2 = 0` to cancel `u + u`; this proof subtracts instead. The theorem is characteristic-free — the prime enters only through the arithmetic of the indices. |
| `p.Prime` | only `2 ≤ p` is used, once, to turn `c·p ≤ i·p + 1` into `c ≤ i`. |
| instability in general | only `P^i(b_{i+1}) = 0` is a field. The general form is `ParityP.Instability`, and the helper derives the field from it as soon as the weight of `z` is `≥ 2` (`n ≥ 1`). |

Likewise the Cartan formula is not a field: only `P^i(z·ι c) = z·ι(P^i c)` is, and `t`,
`x` never appear separately — only `z = t x`, so `H` may be taken commutative at odd `p`
without assuming graded commutativity anywhere.

### `GroupApproximation/CharClass/ParityPSlice.lean` (~110 lines)

Hypothesis **(A)** from the tower, and the top index.  This is sp-design §3.3's L1 + L3a.

* `ParityP.coeff_pow_expChar_eq_zero` — in characteristic `p` the coefficients of a `p`-th
  power are supported in indices divisible by `p`, via `Polynomial.map_frobenius_expand`
  and `Polynomial.coeff_expand`.  This replaces `ParityEvenSlice.lean`'s mod-2
  antidiagonal argument.
* `ParityP.sliceClass_eq_pow`, `ParityP.sliceClass_coeff_eq_zero_of_not_dvd` — the slice
  class `∏_j (1 + h_j)^{d_j}` is a `p`-th power when `p ∣ d_j` for every `j`, hence
  discharges the field `a_eq_zero`.
* `ParityPData.gamma_top_eq_zero_of_slice` — the tower-facing endpoint, the general-`p`
  replacement for `ParityData.gamma_top_eq_zero_of_slice`.  It reuses
  `ParityEvenSlice.sliceClass` and `sliceClass_coeff_eq_zero_of_lt` unchanged; those are
  already characteristic-free.

Only this file needs a characteristic hypothesis, and it takes the weakest one that names
the Frobenius: `[ExpChar R p]`.

### `GroupApproximation/CharClass/ParityPTwo.lean` (~130 lines) — the calibration case

`ParityData.toParityPData : ParityPData 2 R H`, field by field, plus the two `F₂`
conclusions re-derived from the general theorem (`b_odd_eq_zero'`, `gamma_top_eq_zero'`).
`ParityEven.lean` is **not** edited.  This is the check that the general statement really
is a generalisation rather than a lookalike; a design that failed here would be wrong even
if it compiled.

The only step with content is the Wu field: the `j = i` term of the `F₂` diagonal relation
is `γ_0 · γ_{2i+1}`, and `γ_0 = 1` is not a field of `ParityData` — it has to be derived
(`ParityData.gamma_zero`, from instability at index `0` plus `Sq⁰ = id`) before that term
can be recognised as the distinguished `γ_{ip+1}` of hypothesis (W).  The remaining
`j < i` terms are two-factor monomials, which is what `ParityP.IsDecomposable.pair` and
`IsDecomposable.sum` are for.

### Why it is uniform in the stage

Nothing in either file mentions the stage, `m`, the number of projective factors, or `Y`.
The two arithmetic inputs are `p ∣ d_j` for every `j` and `p ∣ n + m`, and for the tower
`d_j = n · 2^j` (so `m = n(2^s − 1)`, `n + m = n · 2^s`) both follow from `p ∣ n` alone at
every stage.  A stage-by-stage proof was explicitly not acceptable; this one is
stage-free by construction rather than by a uniformity argument bolted on afterwards.

## NEEDS

**Nothing for deliverable 1.**  It is pure algebra and depends on no other lane.

For deliverable 2, the mod-`p` `LemmaTwoStepD.lean` (`WuStepDData`, `stepD_of_wu`,
`ne_zero_of_degree_ne_zero`), by name:

* From **`sp-coeff`**: `Hmod K X n` and its `ZMod 2` instance `Hmod2` (landed, per their
  GREEN table at job 2057), and then the coefficient-generic `TotalH` with
  `TotalH.of_eq_zero_iff` and `TotalH.map`, the Chern classes via Leray–Hirsch over `K`,
  and the Künneth uniqueness statement that becomes the field `z_inj`.  I need `TotalH`
  over `K` before I can write the mod-`p` `WuStepDData`, because the `F₂` record is stated
  in `TotalH N`, not in a single graded piece.
* From **`sp-steenrod`**: the `CharClass/OddP*` export of `P^i` as natural additive maps
  with `P^0 = id`, instability, and the Cartan formula (their `CartanOfP p X`).  My two
  helper lemmas consume exactly `P^0 = id`, Cartan, `P^i z = 0` for `i > 0`, naturality
  through the Künneth inclusion, and instability — nothing else, and in particular not the
  normalisation constant discussion of their §4 beyond what `P^0 = id` needs.
* **L4a itself is the one thing neither lane is currently assigned.**  My hypothesis (W)
  is exactly sp-design §3.3's L4a in property form, so someone must eventually prove, from
  the splitting principle, that `P^i(e_{i+1}(y)) − e_{ip+1}(y)` is an integral combination
  of products of at least two `e`'s.  sp-design gives the proof (ring endomorphism of the
  symmetric functions, descend to indecomposables, compute on power sums) and calls it the
  one genuinely new ingredient.  It is a symmetric-function statement with no cohomology
  in it, so it can be formalised in parallel with everything else.  I can take it if the
  lead wants it in this lane; it is not part of deliverable 1 as scoped.

## L4a (deliverable 3, assigned 2026-09-10)

**The statement, diagonal case only**, which is all `ParityPData`'s field (W) consumes.
Over `ℤ`, modulo the ideal of decomposables (products of two positive-weight symmetric
polynomials), with `φ_κ` the ring endomorphism induced by `y ↦ y + κ·y^p`:

```text
  φ_κ(e_{i+1})  ≡  (−1)^{i(p−1)} · (1 + p·i) · κ^i · e_{ip+1}   (mod decomposables)
```

so mod `p` the coefficient is the unit `(−1)^{i(p−1)}·κ^i`.  sp-design's proof: `φ_κ` is a
ring endomorphism, hence descends to the indecomposables `Q(Λ)`; there Newton gives
`p_M ≡ (−1)^{M−1}·M·e_M`, and `φ_κ(p_b) = Σ_j C(b,j)·κ^j·p_{b+j(p−1)}` by the binomial
theorem; combining and dividing by `N` exactly gives the coefficient
`(−1)^{i(p−1)}[C(N−1,i) + p·C(N−1,i−1)]·κ^i`, and at the diagonal `N = i+1`, `j = i` that
is `(1 + p·i)·κ^i`.

**The two Mathlib assets that make it formalisable** (both present at pin `81a5d257`, both
checked by reading the file, not by name alone):

* `Mathlib/RingTheory/MvPolynomial/Symmetric/NewtonIdentities.lean` —
  `MvPolynomial.mul_esymm_eq_sum`, `MvPolynomial.psum_eq_mul_esymm_sub_sum`,
  `MvPolynomial.sum_antidiagonal_card_esymm_psum_eq_zero`.
* `Mathlib/RingTheory/MvPolynomial/Symmetric/FundamentalTheorem.lean` —
  `MvPolynomial.esymmAlgEquiv (hn : Fintype.card σ = n) : MvPolynomial (Fin n) R ≃ₐ[R]
  symmetricSubalgebra σ R`, plus `esymmAlgHom_injective` / `esymmAlgHom_surjective`.

The fundamental theorem is what makes "modulo decomposables" a well-behaved *free*
quotient: it is why the exact division by `N` in sp-design's proof can be performed in the
quotient rather than only in `ℚ ⊗ Λ`.  Without it the division step has nothing to land in.

**The formalisation plan, after reading both Mathlib files.**  Do not build a quotient
ring.  `esymmAlgEquiv σ R hn : MvPolynomial (Fin n) R ≃ₐ[R] symmetricSubalgebra σ R` sends
`X i` to `esymm σ R (i+1)`, so "the coefficient of `e_N` modulo decomposables" is a plain
**linear functional**

```text
  lam N : symmetricSubalgebra σ R →ₗ[R] R,
  lam N f  :=  ((esymmAlgEquiv σ R hn).symm f).coeff (Finsupp.single ⟨N-1⟩ 1)
```

with three properties, each cheap: it is `R`-linear (an `AlgEquiv.symm` composed with
`coeff`); it kills a product of two constant-term-free symmetric polynomials, because the
linear coefficient of a product of two such polynomials is `0`; and `lam N (e_M) = δ_{MN}`.
That replaces steps (1) and (4) of the naive plan — no ideal, no quotient, and no
cancellation lemma, because the division by `N` in sp-design's argument is performed once,
on integer coefficients, before it ever meets a ring element.

Then: (2) Newton mod decomposables, i.e. `lam N (psum M) = (−1)^{M+1}·M·δ_{MN}`, straight
from `psum_eq_mul_esymm_sub_sum` — every term of its subtracted sum is
`esymm a * psum (k−a)` with `0 < a < k`, hence a product of two constant-term-free
symmetric polynomials, hence killed by `lam`.  (3) `φ_κ` as an `AlgHom` with
`φ_κ(psum b) = Σ_j C(b,j)·κ^j·psum (b+j(p−1))`, by the binomial theorem.  (5) the diagonal
specialisation, and the transport of the universal identity to the Chern roots.

Estimate: 800–1500 lines.  Nothing in it is cohomological, so it runs in parallel with
`sp-coeff` and `sp-steenrod`.

**Not to be attempted**, on sp-design's evidence (`tools/diag_wu_shape.py`): a closed form
for the odd-`p` diagonal Wu polynomial in the two-factor shape `WuSymmetric.lean` has at
`p = 2`.  At `p = 3`, `i = 1` it is already `e_2e_1² + e_2² + 2e_3e_1 + e_4`; at `p = 7` it
runs to hundreds of terms of length up to `p`.  The closure predicate is the right response
and stays even if a formula were found.

## TRAPS

* **`-DwarningAsError=true` on the `GroupApproximation` lib** (lakefile.toml, line 13).
  Any linter warning is a hard error, so an unused case binder from
  `induction … with | add u v hu hv ihu ihv` is fatal even though the proof is correct.
  The two non-recursive hypotheses of an `add`/`neg` closure constructor are exactly the
  ones a closure induction never uses; they must be written `_hu`, `_hv`.  Cost me nothing
  only because I checked the lakefile before the first probe.
* **A leading coefficient of `1` is an over-commitment, not a simplification.**  My first
  draft hard-coded the `γ_{ip+1}` coefficient at `1` because that is what the `F₂` diagonal
  Wu relation has.  It is `κ^i` at odd `p` under any normalisation that fixes `P^0 = id`,
  and the induction never needs more than cancellation, so a unit field costs one
  multiplication and buys independence from another lane's constant.  Caught by sp-design
  in review, not by me: the `F₂` template silently supplies `κ = 1` and nothing in the
  mod-2 file records that a constant was ever there.
* **`omega` abstracts `i * p` and `c * p` as unrelated atoms**, which is what makes the
  index arithmetic here workable at all — but `(i' + c) * p` is a *third* atom, so every
  step that needs distributivity has to `rw [add_mul]` first and only then call `omega`.
  Every `omega` in `ParityPData.lean` is preceded by either an explicit `add_mul` rewrite
  or an explicit `Nat.mul_le_mul` step for exactly this reason.
* **A closure predicate beats a `Finset`/`Multiset` of monomials.**  Writing the
  decomposable part of the Wu relation as an explicit indexed sum forces a coefficient
  function and a `Multiset.map`/`sum` bookkeeping layer; writing it as a four-constructor
  inductive `Prop` closed under `+` and `-` makes the consuming lemma a four-case
  `induction` with no big-operator API at all, and is strictly more general (any integral
  combination, not just a chosen enumeration).
* **`Polynomial.coeff_expand` has `R` implicit but `p` explicit**, and
  `Polynomial.map_frobenius_expand` has `p` explicit and the polynomial after it
  (`Mathlib/Algebra/Polynomial/Expand.lean`: `variable (R : Type u) … (p q : ℕ)` at line 37
  is undone for `R` by `variable {R}` at line 46).  Reading the `variable` block alone
  gives the wrong arity for both.
