# Kun--Thom return left-correspondence audit

Date: 2026-08-26

## Question

Does the asymptotically full right module generated by one vanished exterior
return become useful after adding its left `B`-action or abstract
correspondence data?

## Exact countermodel

Not by those data alone.  In canonical regular finite quotients of the full
actor, tensor with one balanced qubit.

- `u=1 tensor Z` is orthogonal to `B`, commutes with `B`, and is unitarily
  conjugate to the image of a balanced root involution.  It is the exact
  return-form gauge.
- `z=lambda(pi(t)) tensor Z` is orthogonal to `B` and satisfies
  `z b=Ad(rho(t))(b) z`.  Finite quotient blindness makes this an exact
  compressor automorphism correspondence.

Both generate full-support orthogonal index-one correspondences, and both
have normalized adjoint rank at most `1/(4|Q|)`.  Their module invariants are
identical, but they are separate cyclic vectors.

## Remaining gate

The needed assertion is no longer “find a left action.”  It is a same-vector
occurrence theorem: the off-edge vector extracted from the returned root must
also implement the compressor correspondence (or a marked strict submodule of
it).  Finite quotient blindness and spectator signs show this cannot follow
from left/right dimensions, supports, orthogonality, or the correspondence
isomorphism class.  It must consume a mixed full-actor word tying the return
and transporter gauges together.

No local heavy computation was used.
