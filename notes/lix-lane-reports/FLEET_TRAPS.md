# Fleet traps (cross-lane, append-only)

One line per trap: `- (lane, date) symptom → cause → fix.` Lanes APPEND at the end; never edit or
reorder another lane's line; the lead merges duplicates. Before a SECOND failed probe on the same
error, grep this file for a word from the error message. Cross-lane facts belong here, not only in
your lane report (a lane report is read by people already looking at that lane).

## Elaboration and names
- (cc-thom, cc-relative, 09-06) `Functor.map_id` rewrites a pattern shaped like `id <$> x` → under `open CategoryTheory` the name also resolves to Lean's monad `Functor` → write `CategoryTheory.Functor.map_id` in full and prove the fact once.
- (cc-relative, cc-bundle, 09-06) heartbeat timeouts at UNRELATED lines, or "function expected" → an unimported name was auto-bound as a variable by `autoImplicit` → look for the missing import; never raise `maxHeartbeats` for this.
- (cc-relative, cc-bundle, 09-06) a correct file fails under the probe flags with a message that reads like a style note → deprecation renames are ERRORS under `-DwarningAsError` → grep the failed log for `deprecated` before reading it. Known at the pin: `push_neg`→`push Not`, `continuous_finset_sum`→`continuous_finsetSum`, `finRotate_succ_apply`→`finRotate_apply`, `Fin.coe_castSucc`→`Fin.val_castSucc`.
- (cc-bundle, 09-06) `unknown free variable _fvar.NNN` on a membership proof written inline in an anonymous constructor → extract it as a named lemma and pass it.
- (cc-lix-odd, 09-06) bare failure to synthesize an instance at an application, naming neither the class nor the definition → the top space is a plain `def`, which instance search does not unfold → ascribe the instance on the unfolded product, or make the def reducible.
- (cc-lix-odd, 09-06) contractibility transfers along a homeomorphism from the TARGET to the source; the wrong direction is a bare instance failure with no direction signal.
- (cc-lix-odd, 09-06) two subtypes of propositionally equal sets (e.g. `U ∩ V` vs `V ∩ U`) do not compose; bridge with the set-congruence homeomorphism.
- (cc-projective, 09-06) a `Fin` sum cannot become a range sum while the summand's cast depends on the `Fin` bound → give the coefficient family a TOTAL form first; an index step whose coefficient type moves with the index is a dependent rewrite → transport lemma by `subst` + proof irrelevance, not `rw`; `Fin.ext` on a raw arithmetic equation leaves both indices as metavariables → name the equation in a `have` with explicit values; an anonymous `Fin` constructor's coercion is its value definitionally but `omega` treats it as an atom → restate the degree goal first; `Finset.range_subset` does not take an inequality at this pin.
- (cc-cartan, 09-06) `map_add` does not fire on the cochain coboundary (not a bundled additive map at that spelling) → `cochainCoboundary_add`; a `by omega` bound inside a `Fin` literal captures hypotheses and blocks a later `subst`; omega cannot see through the successor on a `Fin`; a rewrite whose pattern is visibly present can still fail to match → supply the same equation as a congruence term.
- (cc-thom, 09-06) the ambient of a relative pair must be a `TopCat`, not a bare type (a subset cannot recover `TopCat.of`).
- (cc-bundle, 09-06) a Python end-marker search without a start offset can match an EARLIER occurrence, and `str.replace("", new)` silently PREPENDS → symptom: parse error at line 1 plus "import must be first"; pass the start index and grep the file head before probing.
- (cc-thom, 09-06) `Set.image` of an `excisedSub` fails to unify with a homeomorphism's domain, error reported at the image application and not at its cause → `excisedSub U V : Set ↥(TopCat.of ↥V)` while the homeomorphism expects `Set ↥V`; definitionally equal but not unifiable while `U`, `V` are metavariables → spell the excised set directly as a set-builder over the subtype, or pass both sets explicitly in one vocabulary.
- (cc-thom, 09-06) `rewrite failed: Did not find an occurrence` where the pattern is visibly the goal → an APPLIED `LinearEquiv` (e.g. `(pullEquivOfHomotopyEquiv e k) a`) is definitionally but not syntactically its underlying `pull (cmap e.toFun) k a`, so `rw` sees no occurrence → restate the goal in `pull` form with `show` first, then rewrite. Cost two rounds of seven on one file.

## Library facts at the pin
- (cc-relative, 09-06) the usable five lemma is `LinearMap.bijective_of_surjective_of_bijective_of_bijective_of_injective` in `Mathlib/Algebra/FiveLemma.lean` (unbundled ALGEBRA file), NOT in `CategoryTheory/Abelian/DiagramLemmas/Four.lean` (third slot only) → for anything over `ModuleCat`, grep the unbundled algebra files too; three lanes concluded absence by searching only the categorical half.
- (cc-relative, 09-06) no five lemma reaches degree zero of a pair sequence (nothing to the left of the relative group) → degree zero needs its own argument (injectivity of `relToAbs` there).
- (cc-bundle, 09-06) no continuous inverse square root of a positive matrix field, no polar decomposition, no tensor product / dual / Hom of bundles in the projection model; at RANK ONE normalise `A·p` by its Hilbert–Schmidt norm instead (`lineIntertIso`, hypotheses: carries into the fibre, does not kill it). "The invertibles are connected" gives a path POINTWISE only.
- (cc-cohom-api, 09-06) `Nontrivial (TotalH X)` is `nontrivial_totalH_inst` from `[Nonempty X]`; `Nonempty (Proj p)` is `RelativeSupport.nonempty_proj` from `0 < p.rank x` (not an instance: introduce by `haveI`).

## Statements and seams
- (cc-thom, cc-lix-odd, cc-cohom-api, 09-06) two green files can be about DIFFERENT SPACES and no probe detects it (total-space pair vs projectivised pair; `(Y × S⁵) × S¹` vs `S¹ × (S⁵ × Y)`) → rule 17: read both statements verbatim from origin and name the space, map, class and index each carries, before either side builds on the other.
- (cc-bundle, cc-projective, cc-wu, 09-06) a hypothesis that occurs exactly ONCE in the body is the tell of an over-ask (a structure requested where a property is consumed); a Leray–Hirsch structure for a flag TOWER cannot exist (free on monomials in several classes) → rule 16.
- (cc-cohom-api, cc-thom, 09-06) model-test index arithmetic before proving: the restriction one degree below the top of an LH column is a BIJECTION between families of equal size, not a projection; truncated subtraction makes the statement FALSE at rank zero → carry `1 ≤ r`. Transports hurt when the index changes VALUE, not type.
- (cc-bundle, cc-wu, cc-lix-odd, 09-06) state a rank relation ADDITIVELY (`rank + n = r`), never as a natural subtraction; a dimension that is right but asserted rather than derived is invisible to review.
- (cc-lix-odd, cc-thom, 09-06) a file visible on the shared disk is not a file landed on the branch → `git cat-file -e origin/main:<path>`; an absence claim names the search that was run and the time (rules 14, 15).
- (cc-cartan, 09-05) a lemma whose hypothesis occurs only in its conclusion is unusable as a rewrite rule and fails by doing nothing; a definition is pinned down only by the first property distinguishing it from its neighbours — prove that property EARLY (composite B lacked its middle transposition through four green probes).
- (cc-relative, cc-thom, 09-06) an isomorphism in a slot that the chain needs as a MAP discharges nothing; the content is the equation identifying the composite.
- (cc-bundle, 09-06) `sed -i '' 's/\bfoo\b/bar/g'` on macOS changes NOTHING and exits 0 → BSD `sed` has no `\b` → the next probe is GREEN on the *unrenamed* file and certifies nothing; use `python3` + `re.sub` for identifier renames and grep the result before probing.
- (cc-bundle, 09-06) under concurrent pushes `Everything up-to-date` is not evidence your work landed and `[remote rejected]` is not evidence it did not (the ref-lock error names an already-stale commit) → verify with `git merge-base --is-ancestor <commit> origin/main` plus a blob-hash compare; when a peer's absence report and your fix race, only the COMMIT TIME adjudicates it, so an absence claim should carry the time checked and a landing claim the commit time.
- (cc-bundle, 09-06) `congrArg Subtype.val h` fails with an application type mismatch that names the right lemma and reads like a statement error → the subtype coercion is ambiguous → pin the function: `congrArg (fun w : T => (w : A)) h`.
- (cc-bundle, 09-06) `failed to synthesize HMul (Matrix κ ι ℂ) ℕ` inside a rectangular-matrix `calc` → a literal `1` defaulted to `ℕ` → write `(1 : Matrix ι ι ℂ)`; the follow-on symptom is `rw [Matrix.mul_one]` reporting the pattern `?M * 1` absent from a goal that visibly contains it.
- (cc-bundle, 09-06) `rw` fails on a goal containing an applied lambda such as `(fun k => if k < n then _ else _) k` → `rw` does not beta-reduce → use `simp only [...]`, which does.
- (cc-bundle, 09-06) `unusedSectionVars` fires on an instance that appears in the STATEMENT but not in the proof term → `omit [Inst] in` goes ABOVE the docstring and above any attribute; placed between docstring and `theorem` it is a parse error, not a linter message.
- (cc-bundle, 09-06) a cover or an `Opens` hands you a subset OF A SUBTYPE, while every space in the bundle lane is a subtype of a product; these are different types and the flattening has been needed four times (`projOverHomeo`, `chartHomeoOpens`, `notZeroHomeoOpens`, `interHomeoOpens`) → ask the owner for it rather than casting: four lines there, a propagating cast layer otherwise.
- (cc-thom, cc-bundle, 09-06) excision quotients by the FIRST open and lands on the SECOND, so a packaged cover stated `chart ⊔ notZero` is the reverse of what `excisionIso` consumes → pass `notZeroOpens` first and `chartOpens` second; `sup_comm` is the whole difference, and it is worth fixing at the source rather than in every consumer.
- (cc-cohom-api, 09-06) a lemma matching on a SUM of degrees (`pull_cup`, `TotalH.of_mul`) does not fire against a product-shaped index such as `m + 2*(k+1)` or `2*(j+1)`, though the two are definitionally equal → the pattern `?p + ?q` cannot see the summand boundary → normalise in the EQUATION LEMMA (state it at `m + 2*k + 2`, proved by `rfl`), not with `show` at the goal: the degrees inside the unfolded definition keep the old form. Three unrelated files in one night.
- (cc-cohom-api, 09-06) `pull f (2*m) (cupPowE e m)` and `cupPowE (pull f 2 e) m` are related by `pull_cupPowE`, a THEOREM and not a defeq; closing across it with `exact` makes `isDefEq` unfold the power recursion and time out at a million heartbeats → the symptom is a heartbeat timeout, which reads as cost and is a missing rewrite → rewrite first. The tell: a genuine cost improves when the limit is raised, a missing rewrite does not.
- (cc-cohom-api, 09-06) `pull f n` and `⇑(pullLinear f n)` are not syntactically equal, so `rw [map_sum (pullLinear f n)]` reports the pattern absent from a goal that visibly contains the sum → apply `map_sum` in TERM mode (`map_sum (pullLinear f n) _ _`), where defeq is checked; same for `Finset.range_subset.mpr` against `Finset.sum_subset`, where giving the subset proof directly is shorter than making the iff match.
- (cc-cartan, 09-06) two modules declaring the SAME name in one namespace is a ROOT-ONLY failure: neither imports the other so both probe green, and the root imports both and dies with "already declared" → no probe can ever see it → when a redesign supersedes a definition, retire the old module in the SAME wave, and check the root's import list rather than a probe; `dupscan.py` catches it in seconds.
- (cc-cartan, 09-06) a degree that is the TYPE of a cochain or a class cannot be carried as a parameter with an equation to it — `sqCochain m j φ` with `hm : p + q = m` does not typecheck for `φ = α ⌣ β` → write the degree as the sum it is; and a REINDEXING that needs one number spelled two ways inside such a type is impossible at that layer → do it where the index is data, not a type (for the Steenrod squares, the total ring: equal values name the same component and `of_cohCast` absorbs every transport).
- (cc-cartan, 09-06) `split_ifs` on a totalised constructor (`if h : a + b = k then … else 0`) times out at `whnf` inside 200k heartbeats, because deciding the degree equation unfolds the tensor's structure literal → `by_cases` plus two one-line `_of_eq` / `_of_ne` projection lemmas costs nothing; four of nine errors in one round were this.
- (cc-cartan, 09-06) a predecessor degree stated as a successor equation (`f + 1 = e`) makes the lemma VACUOUS exactly at `e = 0`, and it fails by proving nothing rather than by erroring → state it with truncated subtraction (`f = e - 1`), which omega reads identically and which covers the zero case the caller needs.
- (cc-cartan, 09-06) `rw [← generator_lemma]` cannot find its own right-hand side when the complexes are left to be inferred, because the pattern's index type is a metavariable applied to a structure projection → restate the nested rewrites as ONE lemma with every argument explicit; same cure as the term-mode `map_sum` line above.
- (cc-cartan, 09-06) `Finset.sum_bij'` wants both index sets already cut down to the surviving range → do the two `Finset.sum_subset` steps first, and prove each vanishing from a NEGATED CONJUNCTION built by hand (`fun h => hxn (Finset.mem_filter.mpr ⟨hx, h⟩)`), because `omega` cannot read filter membership; also, an induction whose statement carries a proof ABOUT the thing being inducted on must quantify that proof (`∀ hs, …`), or the rewrite reads as a motive failure.
- (cc-cartan, 09-06) rewriting a complex's differential inside a COMPOSITION fails with "motive is not type correct", because the two sides of the `_d` lemma carry syntactically different morphism types → state the differential's action on an ELEMENT as its own equation and rewrite that (same cure as `tgtCx_d_apply`).
- (cc-relative, 09-06) `cannot coerce to sort {0}ᶜ`, followed by a `whnf`/`isDefEq` heartbeat timeout several lines LOWER → a bare set-builder or complement under `↥` has no expected type to disambiguate the overloaded brace notation, and the resulting error term then makes later defeq checks churn → name the set (an `abbrev` stays reducible, so callers' original spelling still unifies) or ascribe `({…}ᶜ : Set _)`; fix the coercion, never `maxHeartbeats`. Same spelling works fine where an expected type IS present, which is why it looks inconsistent within one file.
- (cc-relative, 09-06) a nested type ascription `((x : T1) : T2)` in a STATEMENT fails when `x`'s native type is only `rfl`-equal to `T1` (e.g. `↑((relCochainComplex R X A).X n)`) → coercion insertion needs the source type syntactically a `Subtype`/`Submodule`, unlike unification → state a `∀ ψ : T1, …` helper and APPLY it to `x`; application unifies at default transparency where coercion does not.
- (cc-relative, 09-06) instance search fails on `IsIso (f.f n)` inside `g ≫ inv (f.f n)` although the instance exists and is stated at that exact shape → `≫` fixes the middle object from the FIRST morphism's codomain, so the implicit arguments differ from the instance's by a non-reducible `def` unfold, and instance search runs at `instances` transparency → give the morphism its own `def` ascribed at its native type, then compose.
- (cc-relative, 09-06) a `show`/`rfl` failure naming two VISIBLY IDENTICAL terms, with a local constant on one side → the term was introduced by `have`, and `have` on a **data** term discards the definition and leaves an OPAQUE local, so nothing downstream can unfold it — no `show`, `rw` or `rfl` reaches through, whatever library lemma you reach for → bind data with `let`, or lift it to a top-level `def` with its own projection lemma. Applies to any `HomotopyEquiv`, `Homeomorph`, `Iso` or bundled map introduced mid-proof. (Superseded diagnosis: that `prodContractibleEquiv_toFun` is a theorem rather than a definitional equality. True, but not the cause, and it sends you at Mathlib instead of at your own binding.)
- (cc-projective, 09-06) a lemma STATED at a truncated-subtraction degree (`m + 2 - 2*(j+1)` seen as `m - 2*j`) is a `whnf` loop and times out before any proof is attempted → write every degree as a SUM in the statement and convert at the point of use; solving for an index size (`k := N - a`) makes the application an `isDefEq` timeout rather than a type error → take the common size as an EQUATION and discharge it by `subst`; two morphisms that are pointwise `rfl` can still time out as an `isDefEq` between morphisms → state the equality and `rw` it; in a `Fin`-indexed coefficient the cast must be written at the BOUND index, not at `0`, even when the index type is a singleton, because the expected type mentions the binder.
- (cc-projective, cc-wu, cc-thom, 09-06) the over-ask tell is visible in a SIGNATURE too, not only in a body: a published signature that bakes in one consumer's use site (`p.plusOne` where the proof takes a bare bundle) is met by the NEXT consumer as an impossibility rather than a mismatch, which is much harder to diagnose → before publishing, check that every hypothesis is one the PROOF needs, not one the first caller happened to have.
- (cc-steenrod, 09-06) a duplicate-scan that greps for a structure's DECLARATION passes while an existing CONSTRUCTION of it goes unseen, and a differently-named content duplicate defeats every name-based scan too → grep for USES of the structure (`grep -rl` the tree, then `grep -nE '^(theorem|def)'` per hit) before writing any constructor, instance or standard consequence of it; I rebuilt `twoSetCover` and `subordinate_or_of_isSmallSimplex` this way and had to remove them one commit later. Corollary: a survey goes stale under you in a live tree — re-verify a NEGATIVE claim before repeating it in a second message, not only before making it (I repeated "nothing proves `Nontrivial (TotalH X)`" three and a half minutes after cc-cohom-api landed it).
- (cc-steenrod, 09-06) `rw` NEVER matches a term whose instance was pinned by an `@`-application: `LerayHirschData.algebra` is a `@[reducible] def` and not an instance, so `D.chernPolynomial` is `@chernPoly _ _ _ _ D.algebra D.powerBasis` and is never syntactically `chernPoly D.powerBasis`; `rw [natDegree_chernPoly]` and `rw [hr', hr]` both fail with "did not find an occurrence" on goals that are definitionally exactly that → cross the junction with a TERM whose type checks by defeq (`(@natDegree_chernPoly … D.algebra _ D.powerBasis).trans hr`, `hr'.trans hr.symm`), and reserve `rw` for equations whose left side you wrote yourself. Both red rounds of that lane were this one trap.
- (cc-cartan, 09-06) a stray goal reading `⋯.choose = n + 1`, reported at a rewrite rather than where you wrote the proof → the degree side condition `cyclesMk` carries is NOT discharged by `simp [ComplexShape.next]` at this pin → `ComplexShape.next_eq' _ rfl`.
- (cc-cartan, 09-06) `i_cyclesMk` and its neighbours are stated with a `forget₂ … Ab` coercion, so as a REWRITE the pattern never matches a goal in plain `ConcreteCategory.hom` form and reports the pattern absent from a goal that visibly contains it → supply the lemma as a TERM (`(K.i_cyclesMk _ _ _ _)` inside `Eq.trans`); same shape as the `map_sum` line above, and the cure is the same.
- (cc-steenrod, 09-06) a degree written as a SUCCESSOR to make a successor-shaped lemma apply is worse than a positivity hypothesis, and the difference is invisible until it times out: with degrees `p` and `q + 1` the product has degree `p + (q + 1)` where the lemma wants `(p + q) + 1`, and although those are definitionally equal, closing across them with `exact` sends `isDefEq` through the whole cochain structure and dies at `whnf`, 200k heartbeats, reported at the theorem's first line rather than at the step → keep every degree the SAME term (`p + q` throughout, one `0 < p + q` hypothesis) and destructure the successor inside a wrapper lemma where the degree is a VARIABLE and `subst` applies. Same family as cc-projective's "take the common size as an equation and discharge it by subst", met from the other side.
- (cc-lix-odd, 09-06) a missing `open scoped Matrix` reports as "elaboration function for `Mathlib.Tactic.subscriptTerm` has not been implemented", pointing at the subscript character of `*ᵥ` and naming neither `Matrix` nor `mulVec` → when an error blames a subscript elaborator, the missing thing is a scoped notation, not a tactic or an instance.
- (cc-lix-odd, 09-06) backticks inside a double-quoted `git commit -m "…"` are COMMAND SUBSTITUTION: a message quoting `open scoped Matrix` silently ran it, the shell printed "The files …/scoped and …/Matrix do not exist", the commit succeeded, and the phrase was deleted from the landed message → always write commit messages with a quoted heredoc (`git commit -F - <<'MSG'`), never `-m` with double quotes, and read back `git log -1 --format=%B` when a message contains code.
- (cc-lix-odd, 09-06) `Homeomorph.contractibleSpace` runs from the TARGET's contractibility to the SOURCE's, `[ContractibleSpace Y] (e : X ≃ₜ Y) : ContractibleSpace X`; getting the direction wrong reports as a bare "failed to synthesize instance" at each use, naming neither the class nor the direction → check which end the instance argument sits on before adding `.symm`; I had both of two uses inverted at once.
- (cc-lix-odd, 09-06) a set defined as an IMAGE (`e '' s`) and the same set defined as a restricted chart's TARGET are equal, never definitionally → name the equality (`image_source_eq_target` plus the source computation) and transport; four separate failures tonight were two descriptions of one object failing to interchange.
- (cc-cohom-api, cc-projective, 09-06) expecting a finite-support argument to be the bulk of a surjectivity onto a DIRECT SUM (three of us estimated it that way and all three were wrong) → the cost is an artefact of assembling the preimage degree by degree, which forces you to prove the assembly finitely supported → if the map is additive its range is a subgroup and a direct sum is generated by its concentrated elements, so hit `of n w` only, where the witness is a FINITE explicit tuple and no infinite object is ever built. Check the witness contributes nothing in the OTHER degrees; that is where the real case analysis is.
- (cc-bundle, cc-thom, 09-06) `Homeomorph.toContinuousMap` does not exist at the pin → build the bundled map by hand as `⟨h, h.continuous⟩` with the target type ascribed, e.g. `(⟨BundleIso.projHomeo e, (BundleIso.projHomeo e).continuous⟩ : C(Proj p, Proj q))`; the `cc-bundle` lane uses that idiom in five places and has never needed a coercion.
- (cc-bundle, cc-thom, 09-06) an absence concluded from `grep … | head -N` is only as good as `N`, and a truncated listing looks exactly like a complete one → run `grep -c` FIRST and read the count beside the listing; this is self-checking, where "do not truncate" is another thing to remember. Same shape at other scales: reading a peer's declaration tells you what it says, not whether your file imports it; reading a signature does not tell you the enclosing namespace. Print the thing that reveals the partiality — a count, an import list, or the namespace.
- (cc-wu, 09-06) deterministic `whnf`/`isDefEq` heartbeat timeout when applying a lemma about `TopCat.isoOfHomeo e |>.hom` over a bundle-defined subtype (e.g. `Proj (flagRest p n)`) → the unifier reduces the subtype's defining predicate, which mentions the bundle applied at points, instead of matching syntactically → drop categorical isomorphisms: prove morphism equalities pointwise with `TopCat.ext`, and get injectivity of a pullback from a continuous LEFT INVERSE stated with plain `C(A,B)` maps. Same shape as the degree hazards: the elaborator answers a definitional question by unfolding a large construction.
- (cc-projective, cc-lix-odd, 09-06) an absence claim from grepping the underlying term is WRONG when the fleet also has an `abbrev` for it: an instance or lemma registered on the abbreviation (`sphereFive` for `unitVectors (Fin 3)`) never matches a search for the term → search both spellings before reporting a gap; two of four "missing" instances existed. Duplicate instances of `Prop`-valued classes (`CompactSpace`, `Nonempty`) are harmless by proof irrelevance, but for a DATA-valued class the same duplication is a real diamond.
- (cc-wu, 09-06) `automatically included section variable(s) unused in theorem X: [Inst]` under `-DwarningAsError=true` → an instance in the `variable` block that only SOME declarations in the file use; the linter reports one declaration per probe, so fixing them individually costs a cycle each → do not add `omit [Inst] in` one at a time and do not `set_option linter.unusedSectionVars false`: take the instance OUT of the `variable` block and declare it explicitly on the declarations that need it. Cost me three probes across two files before I stopped chasing.
- (cc-lix-odd, cc-steenrod, 09-06) an unimported ABBREVIATION does not report as an unknown identifier: with `autoImplicit` on it silently becomes an implicit variable and reports as "function expected at `f` but this term has type `?m`", pointing at a wrapper or a structure that is entirely innocent → the diagnosis to reach for on "function expected" is a missing import of the abbreviation, never the surrounding construction; cc-lix-odd cut a bundle wrapper for this and passed me the wrong cause, and a wrong cause travels further than a wrong fix. Corollary, from the same pair of files: "unknown identifier in this layer is usually a missing `open`, not a missing import" is true often enough to check the `open` first and NOT often enough to state without the qualifier — one of the two we hit tonight was a genuine missing import.
- (cc-steenrod, 09-06) `f _` where the argument appears only under a PROJECTION in the conclusion (`VmatY_eq (m) : VmatY m.2 = Vmat m`, used to prove something about `y`) cannot be inferred: unifying `?m.2 =?= y` is not a pattern, and the error prints the conclusion with metavariables rather than saying the argument is undetermined → supply the argument explicitly; the tell is a type mismatch whose two sides differ only by metavariable projections.
(cc-projective, 2026-09-06) `apply TopCat.Hom.ext; ext y; rfl` on a morphism into a product of SPHERES leaves two unsolved `.ofLp` goals at sphere coordinates → `ext` recursed past the ContinuousMap into `EuclideanSpace`, destroying the shared head that made it `rfl` → stop the recursion by hand: `apply TopCat.Hom.ext; exact ContinuousMap.ext fun _ => rfl`.
(cc-projective, 2026-09-06) every lemma about `LerayHirschData.powerBasis` seems to need `@`-pinning because `.algebra` is a `def`, not an instance → instance search never finds it → `letI := D.algebra` first and the pinning disappears, since the power bases are explicit arguments whose types fix every instance by unification; but `rw` cannot cross between the `letI` fvar and `D.algebra`, so state intermediate `have`s in the `letI` form and let `exact` close the last step by defeq.
- (cc-lix-odd, cc-steenrod, 09-06) two lanes closing the same gap in parallel landed ONE NAME for TWO objects (`vLineY`, defined by a match in one file and by reading a family at a constant coordinate in the other); each probes green in isolation and the ROOT dies, because in one environment that is a duplicate declaration rather than an ambiguity → this is the collision the standing duplicate gate cannot see: every other collision tonight was two spellings of one object, which comparing statements catches, and here the statements agree. Before landing a DEFINITION, check whether the name is already taken on origin (`git grep -n "def <name>" origin/main`), not only whether an equivalent statement exists. Resolution when it happens: the rooted one keeps the name, the other renames in place and restates its facts about the surviving definition, which costs nothing when the two are definitionally equal.
(cc-projective, 2026-09-06) `rw [...]` leaves an unsolved goal that PRINTS as `X = X`, both sides character-for-character identical → `rw` finishes with `with_reducible rfl`, so any plain `def` in the term (`lixN`, and hidden Prop-valued proof arguments) is left folded → put a bare `rfl` on the next line; it closes at default transparency. Do not go hunting for a real mismatch, and do not make the `def` reducible.
- (cc-wu, 09-06) CORRECTION to my earlier entry above about isomorphisms and heartbeat timeouts: the cause is NOT the isomorphism, it is leaving a compound bundle to UNIFICATION. `eulerOfBundle_comap _ _ _ (projComap f q)` makes the unifier solve `comap (projComap f q) ?p =?= pushforward emb hinj (tautLine (comap f q))` for an unknown `?p`, a search through a subtype of a product of matrices. cc-projective's `LH.tautEulerOf_comap` supplies EVERY explicit argument and the same defeq is then checked once, cheaply, with no heartbeat bump. Rule: when applying a naturality lemma whose subject is a compound bundle, WRITE THE BUNDLE OUT rather than passing `_`. The cost difference is not a constant factor. (My left-inverse workaround still works and is still fine, but it was treating a symptom.)
(cc-projective, cc-cohom-api, 2026-09-06) a name grepped FREE on origin/main before you build can be taken by a peer's in-flight file by the time you push, and wiring both reds the root with a duplicate declaration → the shared working tree holds untracked peer modules that `git grep origin/main` cannot see; two lanes reading the same message derived the same theorem within minutes and gave it the same name → this is exactly why dupscan runs before the PUSH and not before the build: it scans the working tree. Never skip it because the pre-build grep was clean. Resolution rule: the name on origin wins, the in-flight file drops its copy and imports the landed one; do not rename the published side.
- (cc-thom, 09-06) `failed to synthesize instance HAdd ↑(Hmod2 X (2*r - 2*↑i.castSucc)) ↑(Hmod2 X (2*r - 2*↑i)) ?m` on a STATEMENT whose two summands have definitionally equal types → the `+` elaborator (`binop%`) picks a common type from the operands' INFERRED types before any unification, so a `Fin` index written `i.castSucc` in one summand and `i` in the other never meets, and a type ascription `(a i.castSucc : Hmod2 X (2*r - 2*↑i))` does NOT fix it — the ascription typechecks and the error is unchanged. Cure: never spell the sum in the statement. State it through a definition whose addition was already elaborated at one type (`ThomDeg.restrictMap g a i`), and let `exact` close the defeq inside the proof, where argument elaboration runs at default transparency. Same family as the `castSucc`/`last` coercion hazards: `↑(Fin.last r) ≡ r` and `↑(i.castSucc) ≡ ↑i` are fine for `exact` and for function arguments, and are invisible to instance search, to `rw`, and to `simp` matching. (A `simp` set that includes `lhTerm_zero` will also silently fail to fire on `lhTerm … ↑i (0 : M i.castSucc)` for the same reason; `exact Finset.sum_eq_zero (fun i _ => lhTerm_zero …)` closes it.)
- (cc-cohom-api, cc-steenrod, 09-06) two declarations named `southPole` are in scope at once in the LIX layer, one in `CharClass` and one in `SphereOddDegree`, and a file needing the `Sphere`-typed one silently resolves to the other → write it fully qualified whenever both models are open in the same file; the failure is a type error at a use site far from the name, not an ambiguity report at the name. Same family as the one-name-two-objects collision above, but WITHIN one environment rather than across two lanes, so no gate sees it at all.
- (cc-cohom-api, cc-steenrod, 09-06) "my lemma is true at any point" is not the fact a consumer needs; "which points a consumer may actually take" is → the mapping-torus slice identification holds only where the clutching field drops out, that is at the two poles, so a statement quantified over an arbitrary circle point is free in the statement and NOT free in practice, and the specialisation is what stops the next lane rediscovering it. Check whether a general hypothesis is satisfiable at the shapes your consumers have before reporting it as general; this is the over-ask pattern read from the producer's side.
- (cc-thom, 09-06) writing a NEW module with `cat > path` silently overwrites a peer's module of the same name, and the symptom is not a name collision but `error: build cycle detected` naming YOUR module in a chain of a dozen peer modules, plus a wall of `bad import` lines for files you never touched → a shell redirect does not warn, and the shared tree holds peer modules that no dupscan of declaration NAMES will surface, because the collision is on the PATH. Check `test -e <path>` (or `git ls-tree origin/main`) before every new-module write, and when a cycle error names a file you just created, suspect that you replaced something rather than that a peer broke the graph. Detecting it takes seconds: walk `GroupApproximation/**.lean`, read the leading `import` lines, and DFS for a back edge; a clean tree reports zero. Recovery is `git show origin/main:<path> > <path>` followed by comparing `git hash-object` against `git rev-parse origin/main:<path>` — never a reset, and never trust the restore without the hash.
(cc-projective, 2026-09-06) `ext x` on a target with a CHAIN of `@[ext]` lemmas recurses past the point of the proof and hands back a goal your pointwise hypothesis cannot close: on `Bundle`s it goes through `Bundle.ext` into matrix entries (`p x i j = q x i j`), on a `TopCat` morphism into a product of spheres it goes through `ContinuousMap.ext` into `EuclideanSpace` coordinates (`.ofLp i`) → the tactic keeps applying ext lemmas as long as any match → name the TOP ext lemma as a term instead: `DFunLike.ext _ _ h`, `ContinuousMap.ext fun _ => rfl`. `ext x : n` also stops it but you have to guess n. Three occurrences in one night across two lanes.
(cc-projective, 2026-09-06) `declaration uses 'sorry'` reported for a declaration that provably contains none, while a lexical scan of the source is clean → a PARSE error earlier in the file (here `ᴴ` without `open scoped Matrix`) makes Lean insert `sorryAx` and the report surfaces dozens of lines later in an innocent declaration → fix the FIRST parse error and the sorry report goes; never chase the reported location, and do not conclude a banned token reached the file.
- (cc-steenrod, 09-06) the CONSUMER side of cc-thom's clobber above, which needs the opposite reflex: `error: build cycle detected` naming only modules you have NEVER touched, together with `bad import` for files that plainly exist, is a peer's transient path clobber seen through the shared tree, not a defect in their code and not a lying probe → re-probe the identical file before editing anything, and settle it by DFS over the import graph of BOTH `origin/main` and the working tree; if the restore has already landed you will find both acyclic and the evidence gone, which is itself the confirmation. Do not conclude the probe hallucinated, which is what I concluded and it was wrong; and above all do not go and edit the peer modules the error names, which is what the message invites. Detection cost is under a minute; the wrong reflex costs a peer's file.
- (cc-wu, 09-06) a consumer's class and a producer's class differing by the round trip `TotalH.of ∘ TotalH.component` (here `lixChernOf` against `LerayHirschData.chern`) looks like it needs a "Chern classes are homogeneous" lemma, which nobody owns and which `chern` — a coefficient of a minimal polynomial — does not obviously satisfy → do not go build the grading. The SPLITTING already contains the fact: `chern_split` says the pulled-back class is an elementary symmetric function of `r` classes of degree two, visibly a sum of products of exactly `k` factors, and the pullback is injective and commutes with components, so the class downstairs is concentrated in degree `2k`. `Wu.of_component_chern` (ParityEvenLixSplitting, 7d48ef397) states it for any class with a `HasSplitting`; cite it rather than rediscovering the route. General shape: when a missing fact is about the grading of an object you only know through a hypothesis, look for it INSIDE that hypothesis before assigning it to the lane that owns the grading.
- (cc-wu, 09-06) `rw` fails with "Did not find pattern" on a goal that displays `(lerayHirschData_of_graded ⋯).taut`, and the same rewrite works everywhere else → `rw` matches up to reducible unfolding only, so a STRUCTURE PROJECTION of a constructor application is opaque to it while `exact` crosses it definitionally → finish with `exact congrArg (TotalH.of _ 2) (LH.tautEulerOf_comap f p).symm` rather than rewriting. Same family as cc-projective's `with_reducible rfl` entry above: that one is the projection left folded in a goal printing `X = X`, this one is the projection blocking the match in the first place.
(cc-projective, cc-cohom-api, 2026-09-06) CORRECTION to my earlier entry on the `chernSliceNatural_lixChern` collision: I wrote that the peer's copy was "untracked in the shared tree", which was true when I looked and FALSE by the time it mattered. Both copies landed on origin, a501b49a1 at 06:58:09 and 1e16e403a at 07:06:50, so the branch carried a live duplicate declaration for about twenty minutes; the root escaped only because one of the two was not yet wired. So dupscan did not prevent a near miss, it detected an existing defect. The operational lesson is unchanged and now better supported: grep origin before BUILDING tells you nothing about push time, the shared tree hides nothing either once a peer pushes, and the only check that catches this is the one immediately before your own push. Resolution rule stands: earlier commit keeps the name, the later one deletes and imports.
- (cc-wu, 09-06) `type of theorem X is not a proposition`, followed by `(kernel) unknown constant X` at the NEXT declaration and at `#print axioms X` → `WuStepDLix` and the other Step D data are STRUCTURES, not `Prop`s, so anything producing one must be `def`, and `theorem` fails in a way whose second and third errors point at innocent lines. Read the FIRST error; the cascade is one defect. Rule of thumb for this layer: `HasSplitting`, `ChernSliceValue`, `SplitRelation` and `LemmaTwoHolds` are `Prop`s and take `theorem`; `SplittingData`, `ChernSplit`, `WuStepD*` and `LerayHirschData` are data and take `def`.
- (cc-wu, 09-06) `don't know how to synthesize implicit argument hGc` on a transport between two Step D structures (`WuStepDSlice.toWuStepDPinned`, `WuStepDPinned.toWuStepDLix`) → the continuity and corner-unitary proofs are implicit STRUCTURE PARAMETERS that do not appear in the result type, so no expected type can fix them, and being `Prop`s does not help: the elaborator still needs a term → pass them by name (`(G := G) (hGc := hGc) (hGu := hGu)`) at every such transport. General shape: a structure indexed by proofs cannot have those proofs inferred from its own type when the target type mentions only the other indices.
- (cc-thom, 09-06) MATHEMATICAL, not an elaboration trap: restricting a **bundle pair** `(E, E ∖ 0)` over an open set of the BASE is not an excision and the induced map is not an isomorphism → excision needs the excised set's closure inside the punctured set, and the part of the total space over the complement of the neighbourhood contains zero-section points, which are exactly what the punctured set omits. The Thom class does not localise to a ball: `H^{2r}(E, E∖0)` has rank the number of components of the base, the local model has rank one. What IS true is that under the Thom isomorphism on each side the restriction becomes restriction of `H^0` from the base to the neighbourhood, injective exactly when the base is CONNECTED. I stated the excision version in a landed docstring and in a request to another lane before catching it; the tell was that the two ranks disagree as soon as the base is disconnected, which is a thirty-second check nobody has to run a probe for. Rule: before calling a restriction an excision, name the excised set and check its closure against the pair's open half.
- (cc-bundle, 09-06) WIDENS my earlier `congrArg Subtype.val` entry: **any** `congrArg` at a projection or coercion whose implicit types are not forced by the goal fails with an application type mismatch naming the right hypothesis — `Prod.fst` and `Prod.snd` too, not only `Subtype.val` → pin the function, `congrArg (fun q : A × B => q.1) h`. I hit the `Prod.fst` spelling six hours after writing the `Subtype.val` one and did not recognise it as the same trap.
- (cc-wu, cc-lix-odd, 09-06) two spellings of a point that meet definitionally nowhere, one under a DEPENDENT argument: `chernOf` takes the restricted rank as `fun y => hs (sliceMap … u y)`, so the slice map appears inside the TYPE of a later argument → a lemma moving the MAP (`lixSliceMap_south_marked`) is the obvious cite and it does not apply: rewriting the map is a dependent rewrite whose motive does not typecheck, because it moves the map and strands the rank proof. Substituting the POINT moves both together. General rule, and it covers every instance of this shape tonight: when two spellings meet definitionally nowhere, move the thing everything else depends on, not the thing you are looking at. Both lemmas are right; which one applies is decided by what sits under a dependent binder at the call site, not by which is closer to the mismatch.
- (cc-steenrod, cc-projective, 09-06) two lanes independently proved `rank_vBundleY_lixRank`, in `CharClass` and in `LH`, 32 minutes apart and neither checked. Duplicate PROOFS break nothing — proof irrelevance makes them defeq — but they are not free: wherever a proof is an ARGUMENT (`chernOf q s hs hs1`, `lerayHirschGraded_compact`), the two proofs give two syntactically different terms, so every seam between a producer using one and a consumer using the other crosses by `show`/`exact` and NEVER by `rw`, and the failure prints two goals that are character-identical. So the duplicate-scan lesson applies to Props as well as to definitions, with a different reason: not correctness, but that you have silently doubled the terms at every use site. Grep before proving a named lemma about a peer's object, even when the statement is a Prop.
- (cc-wu, cc-steenrod, 09-06) the rule-17 statement comparison is worth doing BEFORE the producer's theorem lands, not after: comparing `VSliceValue` against the assembly's `LixSliceValue` binder by binder found three agreements the producer had assumed rather than checked (generators passed through as a parameter, base space, and that the interface is stated at the slice polynomial's COEFFICIENT so the reciprocal-Vieta convention never crosses it) and exactly one real gap, the index type, already closed by a bridge the producer had themselves landed → the whole seam came to one `congrArg`, written and landed while the producer was still working. Comparing early costs one read and converts "will it meet?" into a landed module; comparing late means discovering the gap with a green theorem in hand and nowhere to put the bridge.
(cc-projective, 2026-09-06) `Matrix.blockDiagonal'_apply_ne _ i j h` reports your block inequality as an inequality of Fin SIZES (`dd b₁.fst + 1 ≠ dd b₂.fst + 1`) → passing `_` for `M` leaves the index family `{o, m', n'}` metavariable and the explicit `i j` solve them wrongly; the expected type cannot drive it because the term elaborates bottom-up → pin `(m' := fun b => Fin (dd b.1 + 1))` and `(n' := …)`, after which `_` for `M` is fine. Same shape for any lemma whose implicit type FAMILY is only determined by the goal.
- (cc-thom, 09-06) an explicit argument you WROTE OUT can still elaborate to a metavariable, and the error then blames a later argument: in `f (g := fun i => …) (jm := …)` where `f`'s implicit `M` is determined by `jm`, the lambda `g` is elaborated FIRST, its body's type mentions `M i.castSucc` with `M` still unknown, so the body is postponed to `?m` and the NEXT argument fails with "expected `restrictMap fun i => ?m`" → pass the implicit that fixes the expected type as a NAMED argument BEFORE the dependent explicit one (`f (M := …) … g jm …`). Related, same file: a `(by simp [foo])` argument is postponed too, and a postponed proof leaves a metavariable that blocks unifying two spellings of a proof-carrying term, so bind such side conditions as named theorems instead of tactic blocks at the call site. Three of four failed probes on one file were this family plus its cousin — restating a type (`lixN dd` where the producer says `TopCat.of (↥sphereOne × baseM dd)`, or re-declaring a composite's type) forces a delta on both sides of a unification and dies at `whnf`. Rule: spell every argument exactly as the theorem that discharges it spells it, and pin every implicit `TopCat`.
- (cc-wu, cc-projective, 09-06) an existential hypothesis and a free structure field are the SAME shape, and both are correct exactly while the canonical term is unproved: `WuStepDLix.S` and the generator family `gen` each looked like a choice offered for flexibility, and each collapsed to a named term the moment a peer proved the identification that forces it (`lixChernSplit` is the only splitting `hslice_of_props` discharges; `sliceGen_eq_root` says every line of `V` over factor `j` carries one class, so the multiplicity `sliceClass` expects is forced). Rule: while nobody has proved the identification, quantify existentially and take the witness at the consumer, which is strictly stronger than pinning and needs no naming rights over another lane's object; once it is proved, state the endpoint at the named term so the producer's theorem lands with no introduction step in between. Pinning early recreates the trap pointing the other way, because only the lane proving the statement is entitled to choose the spelling. Twice in one night.
(cc-projective, 2026-09-06) `fun i => f (i : ℕ) (a i)` reports `a i` as "argument i has type ℕ but is expected to have type Fin …", followed by a whnf timeout, in a STATEMENT that looks identical to one that compiled → inside a lambda whose binder type is still being inferred, `(i : ℕ)` is a TYPE ASCRIPTION on the binder, not a coercion out of `Fin`; it compiles wherever an argument position pins the binder first and fails wherever the expected type arrives later → annotate the binder, `fun i : Fin (lhDomainCard r n) => …`. The timeout is a consequence, not the cause: do not raise heartbeats.
- (cc-thom, 09-06) a DEPRECATION is a build failure under the probe's `-DwarningAsError=true`, and it reports at the use site with the replacement named, so it is cheap — but it costs a whole probe cycle, and the deprecated spelling is often the one that reads more natural (`Fin.coe_castSucc` for `Fin.val_castSucc`). Two Fin-index gaps in one small file cost two probes: `omega` cannot see through `↑(i.castSucc)` or `↑(Fin.last r)` and needs `rw [Fin.val_castSucc]` / `rw [Fin.val_last]` first, and the first spelling I reached for was the deprecated one. When a proof needs a `Fin` coercion normalised, reach for the `val_` name, not the `coe_` name; and expect the normalisation to be needed at all, since these are the same definitional-but-not-syntactic equalities that defeat instance search and `simp` matching elsewhere in this file.
- (cc-thom, 09-06) THIRD instance in one lane of the same failure, so it is a pattern and not an accident: **a correct declaration travelling with an overstated account of what it buys**. The declarations were right every time; the docstring sentence, and the message to the peer acting on it, were wrong. Instances tonight: "excision makes the restriction an isomorphism" (it does not apply at all to a bundle pair over the base); "hres follows from excision" (same); "this contractibility lemma removes the need for a Leray-Hirsch instance over the neighbourhood" (it removes the final computation only — applying it needs the restricted class to BE a combination of column terms, which is surjectivity downstairs, i.e. exactly the instance). No probe sees any of these, and a peer acting on the account burns work that the declaration itself would never have caused. The discriminator that catches it: state what the lemma does NOT do, in the docstring, in the same breath as what it does — writing the negative sentence forces the check that writing the positive one does not. Two lanes independently corrected their own docstrings tonight by that route.
- (cc-bundle, cc-thom, 09-06) DESIGN, validated twice tonight: state a pair-membership condition as an **iff**, never as an implication or a one-way image containment — `totalInclOn_mem_puncturedSet_iff`, `chartOpensHomeoTotal_mem_puncturedSet`. An iff composes with itself in both directions with nothing to adapt, so an inclusion iterated at a restricted bundle needs no new lemma (`totalInclOnComp`); a one-way version forces the consumer to reprove the other direction at every stage. Same reason an IMAGE equation beats a membership for a relative-cohomology congruence (`totalTrivStd_image_punctured`): pick the form that survives composition, not the form that is shortest to state.
- (cc-thom, cc-bundle, 09-06) TWO independent wrong methods for checking a Mathlib fact, in one exchange, both giving a confidently wrong picture. (a) Grepping the local mathlib CLONE'S WORKING TREE is not grepping the pin: `/Users/user/mathlib4` sits at its own checked-out revision, and `lake-manifest.json` pins `81a5d257c8e410db227a6665ed08f64fea08e997` (inputRev v4.32.0). The two disagreed by eleven lines on `Subsingleton.compactSpace`, which is how we noticed. Read the pin: `git -C /Users/user/mathlib4 show <pinned-rev>:Mathlib/…`, and get the rev from `lake-manifest.json`, never from the clone's HEAD. (b) Grepping the QUALIFIED name misses the declaration: `Set.uniqueSingleton` is declared as `instance uniqueSingleton` inside `namespace Set`, so a search for the qualified form returns only USE sites and reads as "exists but somewhere odd" or, if the uses are few, as absent. Grep the unqualified name, or grep for `namespace <Ns>` and the bare declaration. Both of us reported to the other with confidence and both pictures were wrong in different ways; the fact itself (`Set.uniqueSingleton` at `Mathlib/Data/Set/Insert.lean:253` at the pin, `Subsingleton.compactSpace` at `Compact.lean:781`) survived only because the build is the real check and the file was already green.
