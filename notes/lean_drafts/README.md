# Preserved Lean drafts

These files were moved out of `GroupApproximation/` on 2026-08-21 because
they were not in the root import closure; several also still contained
`sorry`. Their contents are preserved verbatim as `.lean.txt` files so no
collaborator work is lost, while the compiled source tree remains fully
covered by the repository audit.

Move a draft back into `GroupApproximation/` only after closing its placeholders
and wiring the module into `GroupApproximation.lean` in the same change.
