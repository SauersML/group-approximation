AlgTop: BundleCalculusTransport -- pin the implicit function argument, mark examples noncomputable

Three timeouts/type-mismatch failures in the SectionAlgebra section: the
elaborator was leaving `f` a metavariable while trying to unify
`unitaryConj_of_path`/`murrayVonNeumannEquiv_of_path`/
`unitaryConj_of_isPreconnected`'s conclusion against the goal before `P` was
determined, hitting the heartbeat limit or picking the wrong instance path.
Pinned `(f := P)` explicitly at all three call sites; same fix pattern as
recorded elsewhere in this repo for instance-binder-reached-before-implicit-
is-known traps. Also marked the three `example : ... := inferInstance` lines
`noncomputable`, since they depend on `ContinuousMap.instCStarAlgebra`/
`CommCStarAlgebra.toNonUnitalCommCStarAlgebra`, both noncomputable.

Probed green in isolation (2970 jobs, PROBE GREEN). Root import list
untouched; the lead wires. BundleCalculusInvariant, ModelBridge, and
Pullback -- now unblocked since Transport (their shared dependency) compiles
-- each have their own, unrelated, substantive failures (API drift on
`Exists.map`, type mismatches, instance-synthesis failures, and in
Pullback's case an unknown kernel constant); not attempted here.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
