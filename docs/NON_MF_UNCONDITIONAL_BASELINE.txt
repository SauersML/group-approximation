# Findings of `scripts/check_non_mf_unconditional.py` that are known,
# written up, and not yet discharged.  One line per accepted finding:
#
#     <detector> <declaration>  -- why it is accepted
#
# This register is not a budget.  A finding not listed here fails the
# gate, and a line here that matches nothing fails it too -- so a
# discharged item cannot be left behind to license its own return.
# Every line must correspond to a row of docs/NON_MF_CONDITIONAL_INVENTORY.md.
# Regenerate with:
#
#     python3 scripts/check_non_mf_unconditional.py --write-baseline
#
# and delete a line the moment its mathematics lands.

conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_group_with_every_nontrivial_quotient_not_isCDEOperatorMF  -- inventory row 4: FournierFacioDefectData and DefectRoutingData, neither built anywhere in the corpus; the printed prose already says this answer is not unconditional
conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_twoGenerated_finitelyPresented_torsionFree_kazhdan_fullMFRadical  -- inventory row 3: same two undischarged packages, same printed caveat
conditional-data GroupApproximation.MarkovMFConsequences.operatorMF_recognition_undecidable  -- inventory row 1: AdianRabinReduction is Rabin 1958 as quantified data; no inhabitant is constructed or postulated anywhere
known-conditional GroupApproximation.ClosedEnvelopeCompression.manuscriptEnvelopeCompressionBlind  -- inventory row 5: the Zariski descending chain condition enters as `Closed` plus `WellFoundedLT`, which rem:chaincondition says in print
known-conditional GroupApproximation.ContinuumMultiplicity.manuscriptContinuumMultiplicity  -- inventory row 2: B. H. Neumann's continuum family enters as quantified premises the corpus never exhibits
buried-conditional GroupApproximation.MarkovMFConsequences.presentationCodes_recognition_undecidable_of_haltingReduction  -- inventory row 18: `haltingProperty` sits below the head of the premise; it is the halting problem, named, and not a further hidden input.  The `conditional-data` half came back with 3eeaa793, which pinned the coding to `PresentationCodes.semantics` and restated the premise as an `AdianRabinReduction` again -- deliberately, so the hypothesis cannot be met by an ineffective semantics; the structure is corpus-defined, so the detector sees it once more and it is accepted below on its own line.  cor:undecidable is PRINTED conditional ("suppose given a computable map...") and ledger rows UN.02/UN.03 carry the record
buried-conditional GroupApproximation.MarkovMFConsequences.presentationCodes_negative_side_not_re_of_haltingReduction  -- inventory row 18: same premise, same reading, same note about the retired `conditional-data` half
conditional-data GroupApproximation.MarkovMFConsequences.presentationCodes_recognition_undecidable_of_haltingReduction  -- inventory row 18: the premise is `AdianRabinReduction (haltingProperty n) (operatorMFProperty PresentationCodes.semantics)`, restored by 3eeaa793 to pin the coding.  Nothing in the corpus produces one, and nothing should: cor:undecidable is PRINTED conditional -- "suppose given a computable map sending each instance of the halting problem to a presentation code whose group is MF exactly when that instance halts" -- and the prose says outright that "one object remains hypothetical, the map itself, and the corollary quantifies it rather than citing it".  Constructing it is the Adian--Rabin argument (Rabin 1958), which is not formalized here.
conditional-data GroupApproximation.MarkovMFConsequences.presentationCodes_negative_side_not_re_of_haltingReduction  -- inventory row 18: same premise, same printed caveat; this is the non-r.e. half of the same corollary
