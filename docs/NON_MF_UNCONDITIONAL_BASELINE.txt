# Findings of `scripts/check_non_mf_unconditional.py` that are known,
# written up, and not yet discharged.  One line per accepted finding:
#
#     <detector> <declaration>  -- why it is accepted
#
# This register is not a budget.  A finding not listed here fails the
# gate, and a line here that matches nothing fails it too -- so a
# discharged item cannot be left behind to license its own return.
# Every line must correspond to a row of docs/NON_MF_CONDITIONAL_INVENTORY.md.
# Regenerate with:
#
#     python3 scripts/check_non_mf_unconditional.py --write-baseline
#
# and delete a line the moment its mathematics lands.

conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_group_with_every_nontrivial_quotient_not_isCDEOperatorMF  -- inventory row 4: FournierFacioDefectData and DefectRoutingData, neither built anywhere in the corpus; the printed prose already says this answer is not unconditional
conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_twoGenerated_finitelyPresented_torsionFree_kazhdan_fullMFRadical  -- inventory row 3: same two undischarged packages, same printed caveat
conditional-data GroupApproximation.MarkovMFConsequences.operatorMF_recognition_undecidable  -- inventory row 1: AdianRabinReduction is Rabin 1958 as quantified data; no inhabitant is constructed or postulated anywhere
known-conditional GroupApproximation.ClosedEnvelopeCompression.manuscriptEnvelopeCompressionBlind  -- inventory row 5: the Zariski descending chain condition enters as `Closed` plus `WellFoundedLT`, which rem:chaincondition says in print
known-conditional GroupApproximation.ContinuumMultiplicity.manuscriptContinuumMultiplicity  -- inventory row 2: B. H. Neumann's continuum family enters as quantified premises the corpus never exhibits
header-binder GroupApproximation.NuclearityAmenability.not_isAmenable_of_base_embeds  -- inventory row 15: hygiene only, nothing mathematical; the binders are the theorem's subject matter -- an embedding of the base and the group it embeds into, which the conclusion names
header-binder GroupApproximation.SoficByAmenablePermanence.isSofic_of_isSofic_ker_of_isAmenable  -- inventory row 16: hygiene only; the surjection, its surjectivity, and the soficity/amenability premises are the statement of Elek--Szabo permanence, not inputs hidden from it
header-binder GroupApproximation.ZariskiClosedSubgroup.manuscriptZariskiClosureOfRepresentation  -- inventory row 17: hygiene only; sigma, Gamma, t and the compression hypothesis are exactly the data rem:chaincondition prints
open-variable GroupApproximation.ZariskiClosedSubgroup.manuscriptZariskiClosureOfRepresentation  -- inventory row 17: the in-scope n and k are the space and the field; rem:chaincondition prints "over an arbitrary field", so this quantification is the printed one and not a leading input the header conceals
