# Findings of `scripts/check_non_mf_unconditional.py` that are known,
# written up, and not yet discharged.  One line per accepted finding:
#
#     <detector> <declaration>  -- why it is accepted
#
# This register is not a budget.  A finding not listed here fails the
# gate, and a line here that matches nothing fails it too -- so a
# discharged item cannot be left behind to license its own return.
# Every line must correspond to a row of docs/NON_MF_CONDITIONAL_INVENTORY.md.
# Regenerate with:
#
#     python3 scripts/check_non_mf_unconditional.py --write-baseline
#
# and delete a line the moment its mathematics lands.

conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_nontrivial_group_with_every_nontrivial_quotient_not_isCDEOperatorMF  -- inventory row 4.  REPOINTED 2026-08-16: the TeX now badges the `exists_nontrivial_*` endpoints here, and the former `exists_group_with_every_nontrivial_quotient_not_isCDEOperatorMF` is no longer cited anywhere in the manuscript, so its baseline line was removed rather than left to match nothing.  Hypothesis is `Nonempty (DefectRoutingData D)`, which is now exactly the SIX conditions the TeX prints, and the undischarged inputs are `DefectRoutingData` and `FournierFacioDefectData`, neither built anywhere in the corpus (`ConcreteCompressionSource` deliberately builds only the weaker `CompressionSourceData`).  Printed caveat that licenses this line: "No unconditional torsion-free finitely presented non-MF group is proved here"
conditional-data GroupApproximation.FullMFRadicalEndpoint.exists_nontrivial_twoGenerated_finitelyPresented_torsionFree_kazhdan_fullMFRadical  -- inventory row 3: same hypothesis, same two undischarged packages, same printed caveat, same repointing note
conditional-data GroupApproximation.orderPreserving_quotient_not_subsingleton  -- inventory row 3b, NEW 2026-08-16.  This line exists because the zero-input repair MOVED the binders after the colon: with the data in the header the detector emitted `header-binder` and no premise findings, and with the data after the colon the premises `(R : OrderPreservingRoutingData D)` and `{D : FournierFacioDefectData P E}` become visible, neither head appearing in the conclusion `¬ Subsingleton R.Quotient`.  Accepted because the statement is a REFUTATION universally quantified over data, not an existence claim: it says every order-preserving routing has a nontrivial quotient, which is exactly the printed sentence, and the printed sentence is itself conditional ("Hull's small-cancellation theorem ... WOULD give four of these five ... That same order preservation would supply the sixth condition as well").  DELETE THIS LINE if the gate reports it matching nothing -- it is a prediction about the detector, not an observation
conditional-data GroupApproximation.MarkovMFConsequences.operatorMF_recognition_undecidable  -- inventory row 1: AdianRabinReduction is Rabin 1958 as quantified data; no inhabitant is constructed or postulated anywhere
buried-conditional GroupApproximation.MarkovMFConsequences.presentationCodes_recognition_undecidable_of_haltingReduction  -- inventory row 18: `haltingProperty` sits below the head of the premise; it is the halting problem, named, and not a further hidden input.  The `conditional-data` half of this row is gone from the detector, not from the mathematics: the premise is now the printed data itself -- a transform, `Computable transform`, and the correctness equivalence -- rather than an `AdianRabinReduction`, and a hypothesised plain function is invisible to a detector keyed on corpus-defined structures.  cor:undecidable is PRINTED conditional ("suppose given a computable map...") and ledger rows UN.02/UN.03 carry the record
buried-conditional GroupApproximation.MarkovMFConsequences.presentationCodes_negative_side_not_re_of_haltingReduction  -- inventory row 18: same premise, same reading, same note about the retired `conditional-data` half
